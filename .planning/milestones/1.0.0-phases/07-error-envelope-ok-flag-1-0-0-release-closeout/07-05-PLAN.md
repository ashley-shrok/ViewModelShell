---
phase: 07-error-envelope-ok-flag-1-0-0-release-closeout
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - viewmodel-shell/package.json
  - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
  - MIGRATION.md
  - CHANGELOG.md
  - AGENTS.md
  - README.md
autonomous: false
requirements: [RELEASE-01, RELEASE-02, RELEASE-03, RELEASE-04, RELEASE-05]
must_haves:
  truths:
    - "npm @ashley-shrok/viewmodel-shell ships at 1.0.0; NuGet AshleyShrok.ViewModelShell ships at 1.0.0 — aligned major bump per RELEASE-01 (D-18: version LAST)."
    - "MIGRATION.md gains ONE consolidated 0.4.x → 1.0.0 section covering the entire milestone: context-payload elimination (Phase 6), bind paths (Phase 6), action-name uniqueness rule (Phase 6), error envelope + ok flag + UnknownActionError pattern (Phase 7), BadRequestError semantic split (D-08) — single end-to-end recipe (D-16, RELEASE-02)."
    - "CHANGELOG.md gains a 1.0.0 entry with crisp before/after framing for consumers, scoped to the full milestone (RELEASE-03)."
    - "AGENTS.md is surgically rewritten per D-17: Critical gotchas, Action payload (JSON body), Non-obvious framework behaviors, and ShellResponse<TState> reference table are updated for the new model; everything else gets an accuracy-only pass — no restructure (RELEASE-04)."
    - "README.md version references and wire-shape examples are accuracy-swept for 1.0.0 — RELEASE-04."
    - "Cross-backend parity is green at release time; vitest is green; dotnet test is green — RELEASE-05."
    - "The Phase 5 EXAMPLES-03 / Phase 6 RELEASE-04 bounded-accuracy convention is honored — no AGENTS.md structural rewrite."
  artifacts:
    - path: "viewmodel-shell/package.json"
      provides: "Version bumped to 1.0.0"
      contains: "\"version\": \"1.0.0\""
    - path: "viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj"
      provides: "Version bumped to 1.0.0"
      contains: "<Version>1.0.0</Version>"
    - path: "MIGRATION.md"
      provides: "Consolidated 0.4.x -> 1.0.0 section covering the full milestone"
      contains: "## 1.0.0"
    - path: "CHANGELOG.md"
      provides: "1.0.0 entry with before/after framing"
      contains: "## 1.0.0"
    - path: "AGENTS.md"
      provides: "Surgical accuracy rewrite of the four targeted sections per D-17"
      contains: "VmsActionError"
  key_links:
    - from: "MIGRATION.md 1.0.0 section"
      to: "developer upgrading from 0.4.x or 0.16.x"
      via: "single end-to-end recipe"
      pattern: "Migration recipe"
    - from: "AGENTS.md gotcha section"
      to: "ok flag + VmsActionError + UnknownActionError"
      via: "surgical rewrites of four named subsections"
      pattern: "VmsActionError\\|UnknownActionError"
---

<objective>
Close out the 1.0.0 milestone: align the npm + NuGet version bump (RELEASE-01), produce the consolidated MIGRATION.md 1.0.0 section covering the entire milestone in ONE recipe (D-16, RELEASE-02), write the CHANGELOG.md 1.0.0 entry with crisp before/after framing (RELEASE-03), surgically rewrite the four AGENTS.md sections per D-17 (RELEASE-04), sweep the README for version + wire-shape accuracy, and run the full cross-backend parity + vitest + dotnet test gate one final time (RELEASE-05).

Purpose: this is the user-visible end of the milestone. Per D-18 sequencing, this plan runs LAST — only after Plan 04's parity is green. Version-bumping before parity is the exact "publish breakage to consumers mid-milestone" anti-pattern D-19 calls out. The MIGRATION.md "one consolidated recipe" is the consumer's upgrade path; a developer on 0.4.x reads ONE section and moves to 1.0.0 — they don't read it in two chunks (D-16).

Output: tagged, releasable artifacts (npm package + NuGet package both at 1.0.0), docs that accurately describe the new model, and a final green parity / test gate. The plan includes a `checkpoint:human-verify` for the docs review (per D-17 "drafted in the final plan, reviewed before commit") and another for the publish step (which is gated on user confirmation per project conventions — auto-bumping versions is fine; auto-publishing to npm/NuGet is not).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-PATTERNS.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-01-SUMMARY.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-02-SUMMARY.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-03-SUMMARY.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-04-SUMMARY.md
@MIGRATION.md
@CHANGELOG.md
@AGENTS.md
@README.md

<interfaces>
<!-- Version bump targets (verified by grep): -->
<!-- viewmodel-shell/package.json:3 — "version": "0.16.0" -->
<!-- viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj:13 — <Version>0.16.0</Version> -->

<!-- AGENTS.md target sections per D-17 (verified line numbers): -->
<!-- Line 19 — "## Critical gotchas (read first)" -->
<!-- Line 167 — "## Non-obvious framework behaviors" -->
<!-- Line 368 — "### Action payload — JSON body (curl/agent ergonomics)" -->
<!-- Line 400 — "### ShellResponse<TState> reference" -->

<!-- MIGRATION.md analog: existing 0.16.0 section at MIGRATION.md:9 is the closest header style (## Upgrading to 'X.Y.Z' (npm + NuGet) / ### What changed / ### Migration recipe). -->

<!-- CHANGELOG.md analog: existing 0.16.0 entry at CHANGELOG.md:9 — ## X.Y.Z — title / ### Why / ### Added / ### Demo + parity / ### Consumers. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MIGRATION.md 1.0.0 consolidated section + CHANGELOG.md 1.0.0 entry + AGENTS.md surgical rewrites + README accuracy sweep</name>
  <files>MIGRATION.md, CHANGELOG.md, AGENTS.md, README.md</files>
  <read_first>
    - MIGRATION.md (full file — read once; the existing 0.16.0 section starting at line 9 is the canonical header pattern. Newer sections go ABOVE older ones per the append-only convention)
    - CHANGELOG.md (full file — read once; same append-at-top convention)
    - AGENTS.md (read sections at lines 19, 167, 368, 400 — the four D-17 targets; do not read the full file unless needed for cross-references)
    - README.md (full file — sweep for version references; pin any 0.16.x mention or 0.4.x example to 1.0.0)
    - .planning/phases/07-01-SUMMARY.md through 07-04-SUMMARY.md (the cumulative milestone artifact list — what shipped in each plan; informs the MIGRATION recipe and CHANGELOG bullets)
    - .planning/phases/06-wire-shape-change/06-05-SUMMARY.md (Phase 6 closeout — for the context-elimination + bind-paths + action-name-uniqueness migration content; D-16 says ONE recipe covers both phases)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md (D-01..D-21 — the locked decisions; every recipe item should trace to a decision)
    - viewmodel-shell/src/server.ts post-Phase-7 (the new UnknownActionError shape — recipe needs the exact import path)
    - viewmodel-shell-dotnet/ViewModels.cs post-Phase-7 (the new UnknownActionException + ShellErrorResponse + ErrorCodes — recipe needs the exact class names)
  </read_first>
  <action>
    MIGRATION.md — prepend a NEW consolidated 1.0.0 section ABOVE the existing 0.16.0 section. Per D-16, ONE comprehensive section covering the entire milestone (Phase 6 + Phase 7). Structure (mirrors the 0.16.0 section's header pattern):

    ```markdown
    ## Upgrading to '1.0.0' (npm @ashley-shrok/viewmodel-shell + NuGet AshleyShrok.ViewModelShell)

    1.0.0 is the milestone where the wire becomes truly self-describing: an agent reading only `{vm, state}` from a GET response and walking the tree can drive any VMS app end-to-end identically to the browser renderer. Two breaking changes ship together: the `context` payload is GONE from the wire, and every response now carries a framework-set `ok` flag with a uniform `{ok: false, errors: [...]}` envelope for failures.

    ### What changed (consolidated across Phase 6 + Phase 7)

    1. **Context payload eliminated.** The dispatch wire is now `{action, state, files?}`. Every input node declares a `bind` path naming where its value lives in state; the renderer reads/writes through the path. The seven distinct context-assembly code paths in the renderer collapsed into one bind-path interpreter. WIRE-01..WIRE-08.

    2. **Action-name uniqueness rule.** Every dispatch-bearing node carries an action name only. Per-row identity is encoded IN the action name (`delete-row-42`, not `delete-row` with `context: {id: 42}`). The framework enforces "one action name = one operation" at tree-build time; `ValidateActionNames` throws on violations. WIRE-04, WIRE-05.

    3. **Framework-owned `ok` flag on every response.** Every framework-rendered response now carries `ok: true | false` at the top level. Normal renders, redirects, sideEffects-only responses, polls — all uniformly carry `ok: true`. Framework-detected failures (malformed payload, unknown action, uncaught exception) carry `ok: false` with a structured `errors[]` array. Agents check one field. ERROR-03.

    4. **`{ok: false, errors: [{path?, message, code?}]}` envelope.** Replaces the old `{error: msg}` body shape on framework failures. `errors` is always an array; entries always have `message`; `path` and `code` are optional and ABSENT from the wire when not set (no `"path": null` etc.). Initial `code` vocabulary is small + stable + framework-only: `"parse_error"`, `"unknown_action"`, `"invalid_tree"`, `"uncaught_exception"`. App handlers never set `code`. ERROR-01, ERROR-02.

    5. **`UnknownActionError` (TS) / `UnknownActionException` (.NET).** New public exception classes — your dispatch `default:` arm throws this with the unknown name; the framework catches and surfaces `code: "unknown_action"`. No framework-shipped router primitive; you keep your switch-or-startsWith convention.

    6. **`VmsActionError` on the client side.** New exported class extending `Error` with `errors: ErrorEntry[]`, `status: number`, and a `code?` shortcut. Surfaced via the existing `onError` callback — non-VMS apps that wired `onError` for fetch failures keep working unchanged. Apps that want structured failure handling do `if (err instanceof VmsActionError) { ... err.errors ... }`.

    7. **`BadRequestError` / `BadRequest("...")` semantic split.** Stays in the public API on both backends, but the wire shape changes: the framework now wraps it into `{ok: false, errors: [{message: ...}]}` (no `code`). Reserved for structurally-invalid requests the user can't see (missing required action field); NOT for routine app validation. State-based validation (TextNode error + ValidationError state field — AGENTS.md gotcha #4) is unchanged — those responses are still `ok: true`.

    ### Migration recipe (single end-to-end pass for any consumer on 0.4.x or 0.16.x)

    For each VMS app you maintain:

    **Step 1 — Inputs.** Every input node now declares a `bind` path. Audit `FieldNode`, `CheckboxNode`, `TextareaNode`, `SelectNode`, `FileInputNode`, etc. — add `bind: "path.to.field"` matching where the value lives in your state record. Remove any code that harvests field values out of the DOM on submit.

    **Step 2 — Action names.** Every per-row / per-operation action name must be unique. Replace `{name: "delete-row", context: {id: 42}}` patterns with `{name: "delete-row-42"}`. The convention is yours — slashes (`row/42/delete`), dashes (`delete-row-42`), or anything else you like; the framework only checks uniqueness. Run a build / GET against your dev server — `ValidateActionNames` will throw on the first violation with a precise diagnostic.

    **Step 3 — Dispatch handlers.** Read your action handlers — anywhere they pull from `payload.context` becomes `payload.state` (the state record carries the values now). Switch on `payload.name`; if you need to extract a per-row id, parse the action name (`name.startsWith("delete-row-")` -> `parseInt(name.slice("delete-row-".length))`).

    **Step 4 — `default:` arm.** Replace:
      - `.NET`: `return BadRequest($"Unknown action: {name}")` → `throw new UnknownActionException(name)`.
      - `TS`: `throw new BadRequestError(\`Unknown action: ${name}\`)` → `throw new UnknownActionError(name)`.
    Add the new exception class to your import block (`using ViewModelShell;` already covers .NET; on TS, add `UnknownActionError` to your `@ashley-shrok/viewmodel-shell/server` import).

    **Step 5 — .NET only: register the framework exception filter.** In each `Program.cs`, add `options.Filters.Add<ShellExceptionFilter>()` to your `AddControllers(...)` lambda. This wires the envelope-construction edge — without it, .NET demos won't emit envelope responses on thrown exceptions.

    **Step 6 — Client `onError`.** Optional: if you want to branch on failure class, change your `onError` handler from `(err) => { console.error(err.message) }` to:
    ```typescript
    onError: (err) => {
      if (err instanceof VmsActionError) {
        // err.errors[] has structured info; err.code === "unknown_action" / "parse_error" / etc.
      } else {
        // network/parse failure — plain Error
      }
    }
    ```

    **Step 7 — Side effects.** No changes needed: redirects, sideEffects, polling, busy, preventUnload, and the multipart file channel are unchanged.

    ### What you DON'T need to change

    - State-based validation (TextNode + ValidationError state field — gotcha #4): unchanged, still `ok: true`.
    - Frontend wiring: same `ViewModelShell(...)` constructor, same `BrowserAdapter`, same imports — only behavior change is `onError` now optionally receiving `VmsActionError`.
    - CSS / themes / layout presets / density / card variants: unchanged.
    - Polling, redirects, side effects, busy, preventUnload, file uploads: unchanged.

    ### Backwards-compat policy

    None. No compatibility shims, no legacy-context reader, no deprecation warnings. The framework ships the corrected protocol; apps migrate; the doc is the upgrade path. This is by design per the milestone charter.
    ```

    Keep prior MIGRATION sections (0.3.13, 0.4.0, 0.13.0, 0.14.0, 0.15.0, 0.16.0) verbatim below.

    CHANGELOG.md — prepend a NEW 1.0.0 entry above the existing 0.16.0 entry. Match the existing structure (## X.Y.Z — title / ### Why / ### Added / ### Demo + parity / ### Consumers):

    ```markdown
    ## 1.0.0 — Truly Self-Describing Wire (npm + NuGet)

    Before: the wire was self-describing only when paired with the browser renderer — agents driving the API had to mentally simulate the renderer to know what `context` payload to send. After: an agent reading only `{vm, state}` from a GET and walking the tree can dispatch any action identically to the browser. The `context` field is gone; every input binds to a state path; action names are unique per operation; the renderer is a thin interpreter. Paired with a framework-owned `ok` flag + `{ok:false, errors:[...]}` envelope so failures are uniformly legible across every VMS app.

    ### Why

    The original framework pitch — "agents drive what the browser drives" — had an asterisk: the browser renderer assembled a `context` payload using scope rules absent from the wire. v1.0.0 removes the asterisk. Agents now have one stable failure-check (`body.ok`) and one stable dispatch shape (`{action, state, files?}`).

    ### Added (wire-shape and protocol)

    - Every input node declares a `bind` path (`bind: "fields.title"`); the renderer reads/writes through it.
    - Dispatch wire is `{action: {name}, state, files?}` — no `context` field.
    - Action-name uniqueness enforced at tree-build time (`ValidateActionNames`).
    - Top-level `ok: true | false` on every response.
    - `{ok: false, errors: [{path?, message, code?}]}` envelope on framework failures.
    - Stable framework-only `code` vocabulary: `parse_error`, `unknown_action`, `invalid_tree`, `uncaught_exception`.
    - New exception classes: `UnknownActionError` (TS, `@ashley-shrok/viewmodel-shell/server`) / `UnknownActionException` (.NET, `ViewModelShell` namespace).
    - New client error class: `VmsActionError extends Error` exported from `@ashley-shrok/viewmodel-shell` — surfaced via the existing `onError` callback with `errors`, `status`, and `code` shortcut.
    - .NET: `ShellExceptionFilter` registered in `Program.cs` translates thrown exceptions to envelope responses (no per-controller boilerplate).

    ### Demo + parity

    - All 14 demo backends (.NET + bun, plus FeatureProbe-node) migrated to the new shape — `default:` arms throw the new exception classes, FeatureProbe gains a `boom` action exercising the uncaught-throw path.
    - Cross-backend parity: 8 fixtures (the existing 7 + new `feature-probe-envelope`) across all 15 backends byte-identical. The new fixture covers all three envelope cases (parse / unknown-action / uncaught) with strict status-code assertions per case.
    - `vitest run`: full TS suite green; new tests cover the envelope wrap on the server side and the parse-then-branch on the shell side.
    - `dotnet test`: full .NET suite green; new tests cover the envelope types, exception filter, and round-trip through every demo controller.

    ### Consumers

    Breaking change — aligned npm + NuGet major bump. No compatibility shims. Upgrade path is one consolidated section in MIGRATION.md (single 0.4.x → 1.0.0 recipe; you don't read it in two chunks). The migration is mechanical: add bind paths on inputs, make action names unique, swap `default:` arms to throw the new exception, register the .NET filter, and optionally branch on `VmsActionError` in `onError`.
    ```

    AGENTS.md — surgical rewrite of the four D-17 sections, accuracy-only pass elsewhere. Per the Phase 5 / Phase 6 bounded-accuracy convention.

    **Critical gotchas (line 19):** Drop the context-related footguns (already invalid post-Phase-6 — if any remain in the existing list, remove them). Refine the existing gotcha #4 (`Inline validation goes in the state record, not BadRequest`) to mention the new envelope: the rule still holds for app-level validation (state-based, `ok: true`), AND now `BadRequest`/`BadRequestError` returns an `{ok: false, errors: [{message}]}` envelope (no `code`) reserved for structurally-invalid requests. Add a new gotcha (numbering decision belongs to the writer): "Check `body.ok` ONCE at the response edge — the framework sets it on every response, so a single `if (err instanceof VmsActionError)` in `onError` covers every failure class. Don't try to branch on HTTP status — that's framework-internal."

    **Action payload — JSON body (line 368):** Rewrite to show the new `{action, state, files?}` wire shape (no `context` field). Add a paragraph and a small JSON example showing the failure envelope:
    ```
    On failure, the response body is `{ok: false, errors: [{path?, message, code?}]}` at 4xx/5xx — the same shape regardless of whether the failure was a parse error (400), unknown action (400), invalid tree (500), or uncaught exception (500). Agents check `body.ok`; framework-classified failures carry a `code` from the stable vocabulary (`parse_error`, `unknown_action`, `invalid_tree`, `uncaught_exception`).
    ```

    **Non-obvious framework behaviors (line 167):** Add three bullets:
    - "**Uniform `ok` flag.** Every framework-rendered response — normal render, redirect, sideEffects-only, poll, busy/preventUnload toggle — carries `ok: true`. Every framework-detected failure carries `ok: false` with structured `errors[]`. Apps don't set `ok`; the framework does."
    - "**`VmsActionError` on existing `onError`.** A 4xx/5xx response with a parseable `{ok: false, errors: [...]}` body surfaces as a `VmsActionError` via your existing `onError` callback (status, errors, code shortcut). Apps that wired `onError` for fetch failures keep working unchanged; apps that want structured branching add `if (err instanceof VmsActionError)`."
    - "**`UnknownActionError` / `UnknownActionException`.** Throw this from your dispatch `default:` arm; the framework's catch produces `{ok: false, errors: [{message: \"Unknown action: ...\", code: \"unknown_action\"}]}` at 400."

    **ShellResponse<TState> reference (line 400):** Add a new row to the existing table:
    | `Ok` | `bool` (defaults to `true`) | Framework-set on every response. Apps don't set this. Present in the wire as `"ok": true` (or `"ok": false` on framework-built error envelopes). Single check across every response shape; the shell surfaces `ok: false` responses as `VmsActionError` via the existing `onError` callback. |

    Sweep the rest of AGENTS.md for accuracy ONLY:
    - Any reference to a `context` payload in body text (not in the gotchas section): rewrite to use bind paths + `{action, state}` shape.
    - Any reference to the old `{error: msg}` failure shape: rewrite to the envelope.
    - Any code example that constructs a `ShellResponse<T>` positionally past `Busy` (now needs to accommodate the new `Ok` parameter): verify it still compiles with the parameter ordering chosen in Plan 01's SUMMARY.

    README.md — accuracy sweep:
    - Any `0.16.x` / `0.4.x` version reference in installation snippets or "current version" pins: bump to `1.0.0`.
    - Any wire-shape example showing `_action` with a `context` field: rewrite to the new `{name}`-only shape (or remove if it's only a sketch).
    - The headline framework description: if it mentions any context-payload mechanic, rewrite to mention bind paths.
    - No structural rewrite — this is the same bounded-accuracy convention as AGENTS.md.
  </action>
  <verify>
    <automated>cd /home/ubuntu/ViewModelShell && grep -c "^## 1.0.0" MIGRATION.md CHANGELOG.md && grep -c "VmsActionError\|UnknownActionError" AGENTS.md && grep -c "context" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Upgrading to '1.0.0'" MIGRATION.md` returns one match (the new section header).
    - `grep -n "^## 1.0.0" CHANGELOG.md` returns one match.
    - `grep -n "0.16.0" MIGRATION.md` returns the old section still present (append-only convention held).
    - `grep -n "VmsActionError" AGENTS.md` returns at least 2 matches (Non-obvious behaviors + the gotcha refinement or the ShellResponse table).
    - `grep -n "UnknownActionError\|UnknownActionException" AGENTS.md` returns at least 2 matches.
    - `grep -in "context" AGENTS.md | grep -viE "context for|in this context|context window|gather phase context"` returns 0 matches in normative code/wire prose (the only `context` references that survive should be the term used in non-protocol senses — read each grep hit and verify).
    - `grep -n "0.16\|0.4" README.md` returns 0 matches in the prose body (the only references should be in historical/changelog-style content if any).
    - `grep -n "\"version\": \"0.16\|<Version>0.16" viewmodel-shell/package.json viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns 0 matches (Task 2 below bumps these, but verify they're STILL at 0.16 going INTO Task 1 — Task 1 must not touch them).
  </acceptance_criteria>
  <done>
    MIGRATION.md has the consolidated 1.0.0 section; CHANGELOG.md has the 1.0.0 entry; AGENTS.md's four target sections are surgically updated; README is accurate; no version files touched yet (Task 2 owns those).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Docs review checkpoint — user reads MIGRATION + CHANGELOG + AGENTS edits before version bump</name>
  <what-built>
    MIGRATION.md has a new "Upgrading to '1.0.0'" section above the existing 0.16.0 section; CHANGELOG.md has a new "## 1.0.0 — Truly Self-Describing Wire" entry above the existing 0.16.0 entry; AGENTS.md has surgical rewrites of the four D-17 target sections (Critical gotchas, Action payload — JSON body, Non-obvious framework behaviors, ShellResponse<TState> reference); README.md has a version + wire-shape accuracy sweep.
  </what-built>
  <how-to-verify>
    1. `cd /home/ubuntu/ViewModelShell && git diff MIGRATION.md CHANGELOG.md AGENTS.md README.md` — read the diffs.
    2. Confirm the consolidated 1.0.0 MIGRATION recipe makes sense as ONE pass for a developer on 0.4.x — they should be able to migrate without needing to also read Phase 6's chunk separately. (D-16 lock.)
    3. Confirm the CHANGELOG.md entry's before/after framing is crisp for a non-VMS consumer (RELEASE-03).
    4. Confirm AGENTS.md's gotcha refinement keeps gotcha #4's state-vs-throw rule intact while adding the envelope-era nuance (D-17).
    5. Confirm AGENTS.md still flows after the surgical rewrite — no broken cross-references, no orphaned mentions of `context`.
    6. Confirm README's wire-shape examples (if any) use the new `{name}`-only shape.
    7. If any wording feels wrong, comment specifically what to change; we'll fix and re-checkpoint.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to the version bump (Task 3), or describe what needs editing.</resume-signal>
  <files>(checkpoint — no files modified; operator reads the diffs from Task 1)</files>
  <action>Pause execution and wait for the user to read the diffs against MIGRATION.md, CHANGELOG.md, AGENTS.md, and README.md. The operator will type "approved" (resume) or describe edits needed. If edits are requested, fix Task 1's output and re-run this checkpoint. Do NOT proceed to Task 3 without an explicit "approved" — per D-17 "drafted in the final plan, reviewed before commit," this checkpoint is the lock.</action>
  <verify><human-check>Operator types "approved" or describes specific edits</human-check></verify>
  <done>Docs reviewed and approved by user; safe to proceed to version bump.</done>
</task>

<task type="auto">
  <name>Task 3: Aligned npm + NuGet 1.0.0 version bump</name>
  <files>viewmodel-shell/package.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj</files>
  <read_first>
    - viewmodel-shell/package.json (read full once — confirm the version key is at line 3 as expected; check for any other version references in the file like `peerDependencies` self-pins, although unlikely)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (read full once — the version is at line 13 inside a `<Version>` element; confirm no `<PackageVersion>` separate element that also needs updating)
  </read_first>
  <action>
    Edit `viewmodel-shell/package.json` line 3: change `"version": "0.16.0"` → `"version": "1.0.0"`.

    Edit `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` line 13: change `<Version>0.16.0</Version>` → `<Version>1.0.0</Version>`.

    Per D-18/D-19, this is the LAST mutation in the milestone — version bump goes after the docs are reviewed (Task 2) and before the final test sweep (Task 4). No publish step happens autonomously; publishing is a separate user-gated action documented in Task 5.
  </action>
  <verify>
    <automated>cd /home/ubuntu/ViewModelShell && grep "\"version\":" viewmodel-shell/package.json | head -1 && grep "<Version>" viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\"version\": \"1.0.0\"" viewmodel-shell/package.json` returns one match.
    - `grep "<Version>1.0.0</Version>" viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns one match.
    - `grep "0.16.0" viewmodel-shell/package.json viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns 0 matches.
    - `cd viewmodel-shell-dotnet && dotnet build --nologo -v minimal` exits 0 (the version-only bump doesn't change compilation, but verify).
    - `cd viewmodel-shell && node -e "console.log(require('./package.json').version)"` outputs `1.0.0`.
  </acceptance_criteria>
  <done>
    Both packages report 1.0.0 on the filesystem; no stale 0.16.0 references; build still green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Final cross-backend test gate (RELEASE-05) — parity + vitest + dotnet test green</name>
  <files></files>
  <read_first>
    - .planning/phases/07-04-SUMMARY.md (Plan 04's parity exit-0 baseline — confirm Plan 04's gate is preserved)
    - .planning/phases/07-01-SUMMARY.md through 07-03-SUMMARY.md (the test counts each plan locked — verify the cumulative totals match here)
  </read_first>
  <action>
    Run the three test gates in this order. Each must exit 0 before the next.

    1. `cd /home/ubuntu/ViewModelShell/viewmodel-shell && npx vitest run` — TS framework tests. Expect: 174 (Phase 6 baseline) + new envelope tests from Plan 01 + new parse-then-branch tests from Plan 02 = total ~180+ tests passing, 0 failing.

    2. `cd /home/ubuntu/ViewModelShell/viewmodel-shell && npm run check:core-globals` — core platform-agnosticism CI guard. Expect: `✓ AGNOSTIC-03: viewmodel-shell/src/index.ts references zero platform globals.`

    3. `cd /home/ubuntu/ViewModelShell/viewmodel-shell-dotnet/Tests && dotnet test --nologo` — .NET framework + new EnvelopeTests + ShellExceptionFilterTests. Expect: 16 (Phase 6 baseline) + new tests from Plan 01 + Plan 03 = 25+ tests passing, 0 failing.

    4. Per-demo .NET test suites:
       ```
       for d in demo/Tasks demo/ContactManager demo/ExpenseTracker demo/RetroBoard demo/HelpDesk; do
         (cd "$d/AspNetCore.Tests" && dotnet test --nologo) || exit 1
       done
       ```
       Expect: Tasks 28+, ContactManager 39+, ExpenseTracker 29+, RetroBoard 33+, HelpDesk 43+ — cumulative 172+ tests passing.

    5. `cd /home/ubuntu/ViewModelShell/parity && bun run run.ts` — cross-backend parity. Expect: 8 fixtures (existing 7 + new `feature-probe-envelope`) across all 15 backends, all green:
       ```
       Fixture 'tasks' across 2 backends:                  ✓ all backends agree
       Fixture 'contacts' across 2 backends:               ✓ all backends agree
       Fixture 'retro' across 2 backends:                  ✓ all backends agree
       Fixture 'expenses' across 2 backends:               ✓ all backends agree
       Fixture 'helpdesk' across 2 backends:               ✓ all backends agree
       Fixture 'feature-probe' across 3 backends:          ✓ all backends agree
       Fixture 'feature-probe-envelope' across 3 backends: ✓ all backends agree
       Fixture 'reorder' across 2 backends:                ✓ all backends agree
       ```

    If ANY gate fails, this plan halts — do NOT proceed to Task 5. A failure here is the milestone's go/no-go check; debug, fix the root cause, and re-run.

    Note: per the Phase 6 deferred-items.md, the per-demo frontend adapter tests (~33 failing test cases under demo/*/frontend/src/adapter.test.ts) are pre-existing failures from Plan 06-04 and NOT a regression introduced by Phase 7 — those gates are deliberately NOT in this list. If they happen to now pass because of incidental rework, that's a bonus; if they still fail, that's the same baseline as Phase 6 closeout and does not block release per RELEASE-05's "tests" definition (vitest = framework, dotnet test = framework + per-demo backend, parity = cross-backend; the per-demo frontend adapter tests are explicitly outside the gate).
  </action>
  <verify>
    <automated>echo "Final gate verification is the cumulative action above; all five commands must exit 0."</automated>
  </verify>
  <acceptance_criteria>
    - vitest exit 0 with test count >= 174 (Phase 6 baseline + new tests).
    - check:core-globals exits 0.
    - viewmodel-shell-dotnet/Tests dotnet test exits 0 with test count >= 16.
    - Each of the 5 demo .NET test suites exits 0 with their respective baseline counts.
    - parity run.ts exits 0 with 8 fixtures across 15 backends green.
    - The captured output shows "Fixture 'feature-probe-envelope' across 3 backends: ✓ all backends agree" — Plan 04's new fixture is green.
  </acceptance_criteria>
  <done>
    All test gates green; the framework is publishable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Publish gate checkpoint — user confirms before npm publish + NuGet push</name>
  <what-built>
    All Phase 7 code + docs + parity work is committed; viewmodel-shell/package.json and AshleyShrok.ViewModelShell.csproj report 1.0.0; vitest + dotnet test + parity are all green at the new shape. The framework is in a publishable state, but the publish step itself (npm publish + dotnet nuget push) is operator-gated per project conventions — never run autonomously.
  </what-built>
  <how-to-verify>
    1. `cd /home/ubuntu/ViewModelShell && git log --oneline -20` — confirm the Phase 7 commit chain is sane.
    2. `cd /home/ubuntu/ViewModelShell && git status` — clean working tree (no untracked test artifacts, no leftover scratch).
    3. `cd parity && bun run run.ts` one more time — green.
    4. If approving the publish: run the project's standard publish recipe. The NuGet package is published to the public nuget.org feed (confirmed via `<PackageProjectUrl>https://github.com/ashley-shrok/ViewModelShell</PackageProjectUrl>` + `<PackageLicenseExpression>MIT</PackageLicenseExpression>` in `AshleyShrok.ViewModelShell.csproj` — public MIT package; no custom feed):
       - `cd viewmodel-shell && npm publish --access public` (after `npm login` if needed).
       - `cd viewmodel-shell-dotnet && dotnet pack --configuration Release` then `dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.1.0.0.nupkg --source https://api.nuget.org/v3/index.json --api-key "$NUGET_API_KEY"` (set `NUGET_API_KEY` from the user's nuget.org account at https://www.nuget.org/account/apikeys before running). If the package has historically shipped to a different feed, the operator should override `--source` accordingly — but the .csproj + repo metadata above confirm api.nuget.org is the established target.
    5. After publish: tag the release in git (`git tag v1.0.0 && git push --tags`) ONLY if you want the public marker — per AGENTS.md, agents never push without explicit user request, so this happens by your hand.
    6. Verify the published versions are visible: `npm view @ashley-shrok/viewmodel-shell version` returns `1.0.0`; the NuGet feed shows AshleyShrok.ViewModelShell 1.0.0.

    If anything fails between local-green and publish, the rollback is: do NOT publish, revert the version-bump commit (or amend the version back), keep parity green, and re-checkpoint after fixing.
  </how-to-verify>
  <resume-signal>Type "published" if both npm and NuGet are up at 1.0.0, "abort" if blocking issue found and we need to fix before publish, or describe the next step.</resume-signal>
  <files>(checkpoint — no files modified; operator drives the publish from a terminal)</files>
  <action>Pause execution and wait for the user to run the publish recipe described in `<how-to-verify>`. Per AGENTS.md operating conventions, `npm publish` and `dotnet nuget push` are NEVER autonomous — they always require explicit user execution. The agent's job here is to wait for the "published" / "abort" / next-step signal, NOT to invoke publish commands. If the operator aborts, document the blocker in the final SUMMARY and do not attempt publish.</action>
  <verify><human-check>Operator types "published" / "abort" / next-step description</human-check></verify>
  <done>Publish step completed (or explicitly deferred) by the operator; the milestone closeout phase is finished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → npm registry | published artifact crosses here; auth + content cross to the public registry |
| local repo → NuGet feed | same shape, different feed |
| docs → consumer's mental model | MIGRATION/CHANGELOG describes the breaking change; misleading docs cause botched upgrades |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | (Operational — publish-of-broken-artifact) | npm publish + NuGet push | mitigate | Task 5 is a `checkpoint:human-verify` gate. Per AGENTS.md operating conventions, publishing is operator-driven; no autonomous publish step. Task 4's full gate (vitest + check:core-globals + dotnet test + per-demo dotnet test + parity 8/8 across 15 backends) is the pre-publish guard. |
| T-07-14 | (Operational — version mismatch between npm and NuGet) | Task 3 | mitigate | The aligned bump is mechanical (`0.16.0` -> `1.0.0` in both files); acceptance criteria assert both files report 1.0.0 and no stale 0.16.0 references survive. |
| T-07-15 | (Information Disclosure — leaked secrets in release artifacts) | npm publish payload | accept | The viewmodel-shell/ tree is library code only; no .env files or credentials. The published package is generated from `package.json`'s `files` whitelist (or `.npmignore`) — verify before publish (Task 5 step 1 — git status should show no untracked files in the publish path). If any new artifact slipped in during Phase 7 (e.g., the new test files), confirm `package.json`'s `files` whitelist excludes them per the existing convention. |
| T-07-16 | (Misleading Docs) | MIGRATION/CHANGELOG/AGENTS rewrite | mitigate | Task 2 is a `checkpoint:human-verify` for the docs; the operator reads the diffs before the version bump. D-17's "drafted in the final plan, reviewed before commit" guarantee is satisfied. |
| T-07-SC | Tampering | npm/nuget installs | accept | No new packages added in this phase; the publish artifact itself is built FROM the same dependency set parity has been running against. The release ships exactly what was tested. |
</threat_model>

<verification>
- Task 1: docs files have the new sections + accuracy sweeps; no stale `context` prose; version files NOT touched (Task 3's job).
- Task 2: docs-review checkpoint passed by user.
- Task 3: both package.json and .csproj report 1.0.0; no 0.16.0 stragglers.
- Task 4: vitest green, check:core-globals green, dotnet test green (framework + 5 demo suites), parity green 8 fixtures × 15 backends.
- Task 5: publish checkpoint passed by user (gate, not autonomous).
</verification>

<success_criteria>
- npm @ashley-shrok/viewmodel-shell published at 1.0.0 (after Task 5 approval).
- NuGet AshleyShrok.ViewModelShell published at 1.0.0 (after Task 5 approval).
- MIGRATION.md has the consolidated 1.0.0 section covering Phase 6 + Phase 7 as one recipe.
- CHANGELOG.md has the 1.0.0 entry with crisp before/after framing.
- AGENTS.md surgically updated per D-17 (four target sections rewritten; everything else accuracy-only).
- README.md accurate at 1.0.0.
- Full test gate is green at the new shape (RELEASE-05).
- No backwards-compat shims introduced (AGENTS.md operating conventions held).
- Phase 7 closes out; the v1.0.0 milestone is complete.
</success_criteria>

<output>
Create `.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-05-SUMMARY.md` when done. Include: the final test counts on every gate (vitest / check:core-globals / dotnet test framework / 5 demo dotnet tests / parity 8 × 15), the npm + NuGet version-verification commands and their outputs, the published-version check (npm view + NuGet feed query) IF publish was approved in Task 5 (or "publish deferred per user request" if not), confirmation that the docs sections D-17 named were all touched, and a Phase 7 closeout statement.
</output>
