---
phase: 07-error-envelope-ok-flag-1-0-0-release-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/src/server.ts
  - viewmodel-shell-dotnet/ViewModels.cs
autonomous: true
requirements: [ERROR-01, ERROR-02, ERROR-03]
must_haves:
  truths:
    - "Every successful response built by the framework carries `ok: true` at the top level (D-04, D-06)."
    - "Every framework-detected failure response carries `ok: false` with a structured `errors[]` array; entries always have `message`; `path` and `code` are optional and omitted when absent (D-01, D-02)."
    - "Apps surface unknown actions by throwing `UnknownActionError(name)` (TS) / `UnknownActionException(name)` (.NET); the framework catches and serializes `code: \"unknown_action\"` (D-03, D-09)."
    - "HTTP status policy: 200 on ok:true; 400 on parse failure; 400 on UnknownAction*; 500 on uncaught throw; 500 on validateActionNames violation (D-11)."
    - "Nullable wire fields (`path`, `code`) carry `WhenWritingNull` (.NET) and conditional-spread (TS) per the canonical null-omission contract (PROJECT gotcha #6)."
    - "Controllers / app handlers never set `ok` themselves — the framework adds it at the response edge (D-06)."
    - "Backwards-compat shim policy: the old `{error: msg}` body shape is GONE — replaced, not paralleled (per AGENTS.md operating conventions: no compat shims)."
  artifacts:
    - path: "viewmodel-shell/src/server.ts"
      provides: "UnknownActionError export, ErrorEntry type, envelope construction in createAction, ok:true on success path"
      contains: "export class UnknownActionError"
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "UnknownActionException class, ErrorEntry record, ShellResponse<TState>.Ok property, ShellErrorResponse (or equivalent envelope shape)"
      contains: "public class UnknownActionException"
  key_links:
    - from: "viewmodel-shell/src/server.ts createAction"
      to: "UnknownActionError + ErrorEntry"
      via: "try/catch arm constructing {ok:false, errors:[{message, code:\"unknown_action\"}]}"
      pattern: "instanceof UnknownActionError"
    - from: "viewmodel-shell-dotnet/ViewModels.cs ShellResponse<TState>"
      to: "Ok property default true"
      via: "additive non-nullable bool"
      pattern: "bool Ok = true"
---

<objective>
Land the wire-shape addition that the rest of Phase 7 sits on: the framework-owned `ok` flag, the `{ok: false, errors: [{path?, message, code?}]}` envelope, the `UnknownActionError` (TS) / `UnknownActionException` (.NET) public exports, and the response-edge envelope construction in `createAction` (TS) and the .NET equivalent path. Per D-18 sequencing this is plan 1: expose the breakage internally before consumers see it, before any demo / shell / parity / release work touches it.

Purpose: every subsequent plan in Phase 7 depends on these types and behaviors existing. The TS `createAction` wrapper becomes the central point where every successful response acquires `ok: true` and every caught exception becomes `ok: false`; the .NET `ShellResponse<TState>` record gains a default-true `Ok` property and a parallel framework-construction path for error envelopes (apps don't supply a `ShellResponse<T>` for failures — the framework constructs the envelope from caught exceptions).

Output: backwards-incompatible wire-shape changes to BOTH backends, framework types only, no demo changes yet. Demos and parity will see broken responses until Plan 03 sweeps the controllers — that's intentional per D-18.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-PATTERNS.md
@.planning/REQUIREMENTS.md
@AGENTS.md

<interfaces>
<!-- Current shapes the executor builds on. -->

From viewmodel-shell/src/server.ts (existing):
```typescript
export class BadRequestError extends Error {
  constructor(message: string) { super(message); this.name = "BadRequestError"; }
}

export function createAction<TState>(
  handler: (payload: ActionPayload<TState>) => Promise<ShellResponseBody<TState>> | ShellResponseBody<TState>
): (request: Request) => Promise<Response> { /* parse -> handle -> validateActionNames -> respond */ }
```

From viewmodel-shell-dotnet/ViewModels.cs (existing):
```csharp
public record ShellResponse<TState>(
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] ViewNode? Vm,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] TState? State,
    /* Redirect, SideEffects, NextPollIn, PreventUnload, Busy */ )
{
    public ShellResponse<TState> Validate() { if (Vm is not null) ViewTreeValidation.ValidateActionNames(Vm); return this; }
}
```

The `validateActionNames` path already throws `InvalidOperationException` on duplicate action names — that exception flows through the same framework-edge wrapper this plan installs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add envelope + Ok flag types and createAction envelope wrapping on the TS server subpath</name>
  <files>viewmodel-shell/src/server.ts, viewmodel-shell/src/server.test.ts</files>
  <read_first>
    - viewmodel-shell/src/server.ts (the file being modified — read in full once; createAction lives at lines 343-393, BadRequestError at 314-326, shellSideEffect.download conditional-spread at line 308)
    - viewmodel-shell/src/index.ts (lines 395-422 for the ShellResponse interface — kept platform-agnostic; only the body construction in server.ts changes)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-PATTERNS.md (envelope migration shape section)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md (D-01, D-03, D-04, D-06, D-08, D-09, D-11)
    - AGENTS.md gotcha #6 (null-omission contract) and "Action payload — JSON body" section (current shape for context)
  </read_first>
  <behavior>
    - Test: `createAction` success path returns body containing `{ok: true, vm, state}` (and ok:true on redirect-only / sideEffects-only paths too).
    - Test: thrown `UnknownActionError("missing")` returns status 400 with body `{ok: false, errors: [{message: "...missing...", code: "unknown_action"}]}` — `path` key absent (not `"path": null`).
    - Test: thrown `BadRequestError("bad input")` returns status 400 with body `{ok: false, errors: [{message: "bad input"}]}` — both `path` and `code` keys absent.
    - Test: thrown generic `Error("boom")` returns status 500 with body `{ok: false, errors: [{message: "boom", code: "uncaught_exception"}]}` (researcher locks the code string; `"uncaught_exception"` is a sensible default — D-03 lets the planner pick the small stable set).
    - Test: malformed JSON body returns status 400 with body `{ok: false, errors: [{message: "...", code: "parse_error"}]}`.
    - Test: a tree that violates `validateActionNames` returns status 500 with body `{ok: false, errors: [{message: "...", code: "invalid_tree"}]}` (or equivalent stable code; pick once and document at the constant).
    - Test: redirect-only response (`{redirect: "/x"}`) carries `ok: true`.
    - Test: error envelope JSON.stringify output contains no `"path":null` or `"code":null` substring on entries without those fields (null-omission contract).
    - Test (T1 info-disclosure mitigation): the uncaught-throw 500 body's `errors[0].message` MUST NOT include a stack trace or a stringified Error.prototype.toString that leaks internal class names. Lock the policy: framework copies `err.message` verbatim for `Error` instances; non-Error throws become `"Internal server error"` to avoid leaking unknown shapes.
  </behavior>
  <action>
    Add to viewmodel-shell/src/server.ts (mirror the BadRequestError shape at line 321):
    - `export class UnknownActionError extends Error` with constructor `(public readonly name: string)` storing the bad action name on a public readonly field, setting `super(\`Unknown action: \${name}\`)` and `this.name = "UnknownActionError"`. (Note: the `name` field on Error is the class name by convention; this class stores the offending action name on a different public field. Use a property name that doesn't collide with `Error.prototype.name` — name the field `actionName: string` and store the offender there. Override `this.name = "UnknownActionError"` for the class-tag convention.)
    - `export interface ErrorEntry { path?: string; message: string; code?: string; }` — the envelope entry shape.
    - Module-level constants for the framework's stable code vocabulary: `const ERR_CODE_PARSE = "parse_error"`, `ERR_CODE_UNKNOWN_ACTION = "unknown_action"`, `ERR_CODE_INVALID_TREE = "invalid_tree"`, `ERR_CODE_UNCAUGHT = "uncaught_exception"`. Export them (`export const ERR_CODES = { ... } as const`) so MIGRATION.md can reference them and tests can import them. The lock per D-03 is "small, stable, framework-only set."
    - A small private helper `function errorEnvelope(entries: ErrorEntry[]): string` that builds `{ok: false, errors}` and JSON-stringifies it. Each entry uses the conditional-spread pattern from server.ts:308 to keep `path` / `code` absent when undefined: `{ message, ...(path != null ? { path } : {}), ...(code != null ? { code } : {}) }`.
    - A small private helper `function jsonResponse(body: string, status: number): Response` (returns `new Response(body, { status, headers: { "Content-Type": "application/json" } })`) to deduplicate the four current call sites.

    Modify `createAction` (lines 343-393):
    - Parse-error catch (line 356-361): replace body construction with `errorEnvelope([{ message: (err as Error).message, code: ERR_CODES.PARSE }])`, status 400.
    - BadRequestError catch (line 366-371): replace with `errorEnvelope([{ message: err.message }])`, status 400 (NO `code` — per D-08, BadRequestError is the apps' structurally-invalid channel; agents distinguish it by absence of `code`).
    - Insert a NEW catch arm BEFORE the rethrow at line 372: `if (err instanceof UnknownActionError) { return jsonResponse(errorEnvelope([{ message: err.message, code: ERR_CODES.UNKNOWN_ACTION }]), 400); }`. The 400 status per D-11 (app sent a name the tree doesn't expose — fault attribution is shared/ambiguous; matches today's BadRequestError status).
    - Convert the rethrow at line 372 into a wrapping catch: catch any remaining `err`, log to console.error (server-side observability per threat T1), then return `jsonResponse(errorEnvelope([{ message: errorMessageFromUnknownThrow(err), code: ERR_CODES.UNCAUGHT }]), 500)` where `errorMessageFromUnknownThrow` returns `err.message` when `err instanceof Error` and `"Internal server error"` otherwise (T1 mitigation: never leak unknown shapes). Stack traces NEVER reach the wire.
    - validateActionNames catch (line 380-386): replace body construction with `errorEnvelope([{ message: (err as Error).message, code: ERR_CODES.INVALID_TREE }])`, status 500. (Tree-build violations are a server bug — distinct code from uncaught.)
    - Success-path return (line 389-391): swap to `jsonResponse(JSON.stringify({ ok: true, ...result }), 200)`. This is the central point per D-06 where every successful response acquires `ok: true`.

    Create viewmodel-shell/src/server.test.ts — a fresh vitest file colocated with server.ts (matches the existing vitest layout under viewmodel-shell/src/). Cover every Test case in `<behavior>`. Use `createAction` directly with synthesized `Request` objects — no Bun/Hono runtime needed; `Request` and `Response` are universal in Bun's vitest runner. Reference: the existing test file naming pattern (search for `*.test.ts` under viewmodel-shell/src/) — match it.

    Do NOT touch viewmodel-shell/src/index.ts in this task (Plan 02 owns the shell-side parse-then-branch). Do NOT touch any demo files (Plan 03 owns the sweep).
  </action>
  <verify>
    <automated>cd /home/ubuntu/ViewModelShell/viewmodel-shell && npx vitest run src/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export class UnknownActionError" viewmodel-shell/src/server.ts` returns one match.
    - `grep -n "export interface ErrorEntry" viewmodel-shell/src/server.ts` returns one match.
    - `grep -n "ERR_CODES" viewmodel-shell/src/server.ts` returns at least 6 matches (definition + 4 uses + at least one re-export).
    - `cd viewmodel-shell && npx vitest run src/server.test.ts` exits 0, all new tests pass.
    - `cd viewmodel-shell && npx vitest run` exits 0 — pre-existing 174 framework tests still green (no regressions from the body-shape swap; index.ts unchanged so the shell side is unaffected).
    - `cd viewmodel-shell && npm run check:core-globals` exits 0 (only server.ts touched, not index.ts; but verify nothing slipped).
    - Manual eyeball: `grep -c "ok: true" viewmodel-shell/src/server.ts` >= 1 (the success-path wrap exists).
    - Manual eyeball: `grep -c '"path": null\|"code": null' viewmodel-shell/src/server.ts` == 0 (no literal null serialization).
  </acceptance_criteria>
  <done>
    Envelope construction is centralized in server.ts; the new `UnknownActionError` and `ErrorEntry` exports exist; all five envelope cases (parse / BadRequest / UnknownAction / uncaught / invalid-tree) plus the success path are covered by vitest; pre-existing tests still pass; no platform-globals regression.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Ok flag, UnknownActionException, ErrorEntry, and framework-edge envelope construction on the .NET backend</name>
  <files>viewmodel-shell-dotnet/ViewModels.cs, viewmodel-shell-dotnet/Tests/EnvelopeTests.cs</files>
  <read_first>
    - viewmodel-shell-dotnet/ViewModels.cs (the file being modified — read top section + line 85-132 for ShellResponse<TState>, line 369-395 for ViewTreeValidation.ValidateActionNames + the existing InvalidOperationException throw)
    - viewmodel-shell-dotnet/Tests/ — list directory; pick the existing test file structure as the template for EnvelopeTests.cs (likely a single .csproj with multiple .cs test files using xUnit + `[Fact]` attributes)
    - viewmodel-shell/src/server.ts post-Task-1 (the TS twin — keeps the two backends byte-aligned per the canonical wire contract; if Task 1 used `"unknown_action"`, this task uses the same string)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-PATTERNS.md (.NET section: ViewModels.cs analog patterns + Busy/PreventUnload default template + InvalidOperationException template)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md (D-01..D-15 as a register; the .NET section of <code_context> integration-points)
  </read_first>
  <behavior>
    - Test: serializing a new `ShellResponse<TState>` with default `Ok = true` produces JSON containing `"ok":true`. (Per the maintainer rule at ViewModels.cs:7-22, non-nullable members keep serializing their value — `Ok` is non-nullable with default `true`, so it MUST appear on the wire on EVERY response, including default-constructed ones. This is D-04's "uniform on every response.")
    - Test: serializing `ShellResponse<T>.RedirectTo("/x")` produces JSON containing `"ok":true` AND `"redirect":"/x"`.
    - Test: `ErrorEntry` with only `Message` set, when serialized, contains `"message":"..."` and contains NEITHER `"path"` NOR `"code"` (WhenWritingNull omission).
    - Test: `ErrorEntry` with `Code = "unknown_action"` and `Message` set serializes both fields and omits `"path"`.
    - Test: `new UnknownActionException("foo")` exposes `.Message` containing `"foo"` and a public `.Name` (or `.ActionName`) property returning `"foo"` (matches the TS twin's `actionName` field — pick one name and use it consistently; recommend `ActionName` to avoid colliding with `Exception.GetType().Name`).
    - Test: a framework-built `ShellErrorResponse` (or whatever envelope record name is locked — see <action>) with `Ok = false` and one entry serializes to `{"ok":false,"errors":[{"message":"..."}]}` with no Vm/State/Redirect leakage.
    - Test (T1 info-disclosure mitigation): a helper `ShellErrorResponse.FromUncaught(Exception ex)` (or framework-edge equivalent) uses ONLY `ex.Message` for the entry — never `ex.ToString()`, `ex.StackTrace`, or `ex.GetType().FullName`. The test asserts the serialized message does not contain `at ` (.NET stack trace marker) or `System.` (BCL type prefix).
  </behavior>
  <action>
    Add to viewmodel-shell-dotnet/ViewModels.cs (place near the ShellResponse<TState> record around line 85; group with the other wire records):

    - New public record `ErrorEntry`:
      ```
      public record ErrorEntry(
          string Message,
          [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Path = null,
          [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Code = null
      );
      ```
      Matches the existing `ShellSideEffect` shape at lines 60-66 — same null-omission contract. Per the maintainer-rule comment at the file head, every new nullable field MUST carry the attribute; verify in the test.

    - Module-level static class `public static class ErrorCodes { public const string Parse = "parse_error"; public const string UnknownAction = "unknown_action"; public const string InvalidTree = "invalid_tree"; public const string Uncaught = "uncaught_exception"; }`. Same vocabulary as the TS twin's `ERR_CODES`; keep the two backends byte-aligned per the AGENTS.md "two backends are kept byte-aligned" rule.

    - New public exception class `UnknownActionException` (place near `ViewTreeValidation` around line 369):
      ```
      public class UnknownActionException : Exception
      {
          public string ActionName { get; }
          public UnknownActionException(string actionName)
              : base($"Unknown action: {actionName}")
          {
              ActionName = actionName;
          }
      }
      ```
      Mirrors the BadRequestError TS shape: public field carrying the offending name for the framework edge to extract into `errors[0].code`. (.NET's `Exception` already has a class-name introspection via `.GetType().Name` — no name collision worry, so `ActionName` is the clean property name.)

    - New public record `ShellErrorResponse`:
      ```
      public record ShellErrorResponse(
          IReadOnlyList<ErrorEntry> Errors,
          bool Ok = false
      );
      ```
      Per D-06, the framework constructs this WITHOUT an app-supplied `ShellResponse<T>` — apps don't write to this record. `Ok = false` default + name `ShellErrorResponse` makes the intent self-documenting at construction sites. Non-nullable `Ok` and `Errors`; both serialize unconditionally. (No `WhenWritingDefault` on `Ok` here because the whole point is to always emit it on the wire.)

    - Static helpers on `ShellErrorResponse` (the framework-edge construction surface):
      ```
      public static ShellErrorResponse OfParseError(string message)
          => new([new ErrorEntry(message, Code: ErrorCodes.Parse)]);
      public static ShellErrorResponse OfBadRequest(string message)
          => new([new ErrorEntry(message)]);  // no code per D-08
      public static ShellErrorResponse OfUnknownAction(string actionName)
          => new([new ErrorEntry($"Unknown action: {actionName}", Code: ErrorCodes.UnknownAction)]);
      public static ShellErrorResponse OfInvalidTree(string message)
          => new([new ErrorEntry(message, Code: ErrorCodes.InvalidTree)]);
      public static ShellErrorResponse OfUncaught(Exception ex)
          => new([new ErrorEntry(ex.Message, Code: ErrorCodes.Uncaught)]);
      ```
      T1 mitigation: `OfUncaught` reads ONLY `ex.Message`. Stack traces and type names never appear in the constructed envelope. Tests assert this directly.

    - Modify `ShellResponse<TState>` (line 85): add a NEW non-nullable parameter `bool Ok = true` at the end of the parameter list, alongside `PreventUnload` and `Busy`. Per the comment at lines 17-21 ("Non-nullable members deliberately keep serializing their value") and D-04 ("uniform on every response"), `Ok` does NOT carry `JsonIgnore` — it serializes on EVERY response, including default-constructed redirect-only and sideEffects-only paths. (Contrast with `PreventUnload`/`Busy` which DO carry `WhenWritingDefault` because their default-false omission is the intent. `Ok` is the opposite: always present.)

      Verify ordering/positional-record compatibility: the existing `RedirectTo(string url)` factory and `WithEffect` method need to compile without changes. Since `Ok` is positional with a default, all existing call sites that omit it still work — but if any non-test code constructs `new ShellResponse<TState>(...)` positionally past `Busy`, it breaks. `grep -rn "new ShellResponse<" viewmodel-shell-dotnet/ demo/` should not turn up positional calls past the third parameter; verify before merging.

    - The existing `Validate()` method (line 127) stays untouched in this task; Plan 03's demo sweep will rely on it. Per D-06, controllers don't set `Ok` themselves — the default-true on the parameter list is the framework's contribution.

    Add viewmodel-shell-dotnet/Tests/EnvelopeTests.cs — new xUnit file following the existing test layout (read `Tests/` to confirm; mirror namespace + file structure). Cover every Test case in `<behavior>`. Use `JsonSerializer.Serialize` with the same default ASP.NET-flavored options used by the host to assert the wire shape — the existing tests under Tests/ already establish the serialization helper pattern.

    Do NOT modify any controller wiring (where the framework actually catches exceptions and constructs the envelope). The integration is staged in Plan 03 (demos throw UnknownActionException; an exception filter or createAction-equivalent translates to ShellErrorResponse). Researcher discretion per D-discretion: this plan ships the types and the static factories; the actual filter/middleware lands when the demos need it.
  </action>
  <verify>
    <automated>cd /home/ubuntu/ViewModelShell/viewmodel-shell-dotnet/Tests && dotnet test --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public class UnknownActionException" viewmodel-shell-dotnet/ViewModels.cs` returns one match.
    - `grep -n "public record ErrorEntry" viewmodel-shell-dotnet/ViewModels.cs` returns one match.
    - `grep -n "public record ShellErrorResponse" viewmodel-shell-dotnet/ViewModels.cs` returns one match.
    - `grep -n "public static class ErrorCodes" viewmodel-shell-dotnet/ViewModels.cs` returns one match.
    - `grep -n "bool Ok = true" viewmodel-shell-dotnet/ViewModels.cs` returns one match (the ShellResponse<TState> addition).
    - `cd viewmodel-shell-dotnet/Tests && dotnet test --nologo` exits 0, all new EnvelopeTests cases pass, all 16 pre-existing framework tests still pass (total >= 16 + new test count).
    - `cd viewmodel-shell-dotnet && dotnet build --nologo -v minimal` exits 0 — the additive parameter on `ShellResponse<TState>` did not break any positional construction (the demos compile against this assembly).
    - Manual eyeball: `grep -c "WhenWritingNull" viewmodel-shell-dotnet/ViewModels.cs` increased by at least 2 (Path + Code on ErrorEntry).
  </acceptance_criteria>
  <done>
    .NET envelope types exist (`ErrorEntry`, `UnknownActionException`, `ShellErrorResponse`, `ErrorCodes`); `ShellResponse<TState>.Ok` defaults to true and serializes on every response; static factories cover the four framework-detected failure cases (parse / BadRequest / UnknownAction / Uncaught / InvalidTree); WhenWritingNull holds for nullable entry fields; .NET framework tests + new EnvelopeTests are green; positional `ShellResponse<TState>` construction in demos still compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → action endpoint | untrusted JSON / multipart body crosses here; parse-error envelopes flow back |
| handler → framework edge | thrown exceptions from app handlers cross here; the framework wraps them into envelopes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `OfUncaught` / TS uncaught catch arm in createAction | mitigate | Construct envelope from `ex.Message` only — NEVER `ex.ToString()`, `ex.StackTrace`, `ex.GetType().FullName`. For non-Error throws in TS, substitute `"Internal server error"` instead of stringifying unknown shapes. Tests assert the serialized message contains no stack-trace markers (`"at "`, `"System."`). |
| T-07-02 | (Status Misclassification, mapped to S/T category) | createAction status branching | mitigate | Lock the policy in code: 200 on ok:true, 400 on parse / BadRequest / UnknownAction, 500 on uncaught / invalid-tree. Unit tests assert HTTP status per case so a future refactor that "simplifies to 200" fails the build. |
| T-07-03 | Denial of Service | unbounded request body buffering | accept | Out of scope for this plan; existing bun/Bun.serve / ASP.NET request-body limits apply unchanged. Documented here so it's not silently dropped — if a downstream phase rebuilds parsing, revisit. |
| T-07-SC | Tampering | npm/nuget installs | accept | No new packages added in this phase; existing dependency set is unchanged. Package legitimacy gate not applicable (no install tasks). |
</threat_model>

<verification>
- `cd viewmodel-shell && npx vitest run` — all 174 + new framework tests green.
- `cd viewmodel-shell-dotnet/Tests && dotnet test --nologo` — all 16 + new EnvelopeTests cases green.
- `cd viewmodel-shell-dotnet && dotnet build --nologo -v minimal` — additive `Ok` parameter doesn't break positional construction across demos.
- `cd viewmodel-shell && npm run check:core-globals` — index.ts unchanged, but verify guard still passes (defensive).
- `grep -c '"path": null\|"code": null' viewmodel-shell/src/server.ts viewmodel-shell-dotnet/ViewModels.cs` returns 0 — null-omission contract holds in source.
- Parity suite is NOT expected to pass yet — demos still throw `BadRequestError` for unknown actions and emit the old `{error:msg}` body shape. Plan 03 fixes that. Per D-18, this is by design.
</verification>

<success_criteria>
- Both backends export the new envelope types (`UnknownActionError`/`UnknownActionException`, `ErrorEntry`, code vocabulary, error-response shape).
- `createAction` (TS) wraps every response with `ok: true` on success and constructs `{ok: false, errors: [...]}` envelopes for all five failure classes (parse, BadRequest, UnknownAction, uncaught, invalid-tree).
- `ShellResponse<TState>.Ok` (.NET) defaults to true and always serializes; `ShellErrorResponse` provides the framework-construction surface for failures.
- New code adheres to the project's null-omission contract (WhenWritingNull on nullable entry fields, conditional-spread on TS construction).
- T1 information-disclosure mitigation is locked in tests: uncaught-exception envelopes carry only `ex.Message`, no stack/type leakage.
- All TS + .NET framework tests pass; new EnvelopeTests cases pass.
</success_criteria>

<output>
Create `.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-01-SUMMARY.md` when done. Include: confirmed code vocabulary (`parse_error` / `unknown_action` / `invalid_tree` / `uncaught_exception`), exact public API additions on both backends, where the success-path `ok: true` wrap is centralized in createAction, the chosen `ActionName` (.NET) / `actionName` (TS) public-field name on the new exception class, and the test counts on both backends post-change.
</output>
