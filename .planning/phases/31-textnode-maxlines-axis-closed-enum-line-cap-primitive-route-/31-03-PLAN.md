---
phase: 31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
  - demo/FeatureProbe-bun/handler.ts
  - parity/fixtures/textnode-maxlines.json
autonomous: true
requirements: [MAXLINES-PARITY]
tags: [parity, feature-probe, textnode, maxLines, expectBodyContains, gotcha-9]

must_haves:
  truths:
    - "A new parity fixture file `parity/fixtures/textnode-maxlines.json` runs against the 3 FeatureProbe backends (dotnet-probe, bun-probe, node-probe — registered in `parity/backends.json` lines ~132-160). The fixture exercises ALL FOUR maxLines states (unset, 1, 2, 3) via GET + a small number of POST actions that mutate state slots on the FeatureProbe state record. The fixture is REGISTERED in each of the 3 FeatureProbe backends' `fixtures: [...]` array in `parity/backends.json` (appended alongside the existing `feature-probe`, `feature-probe-envelope`, `chat-composer` fixtures)."
    - "Each step carries an `expectBodyContains` tripwire array per AGENTS.md gotcha #9 class-3 lesson: 'a fixture proves nothing about a branch its own configuration never runs.' The four branches (unset / 1 / 2 / 3) each have a UNIQUE substring emission that no other branch produces: unset step asserts a distinctive substring the unset branch alone emits (a TextNode with a UNIQUE marker text but NO maxLines wire field — see fixture design below); maxLines=1/2/3 steps assert `\"maxLines\":1`, `\"maxLines\":2`, `\"maxLines\":3` respectively (all three substrings are BYTE-DISTINCT and byte-parallel across TS/.NET). Additionally: the unset step asserts the marker TextNode's substring EXISTS (proves the branch ran) AND the parity harness's ALWAYS-ON `findNulls` invariant (parity/run.ts) will catch a class-2 gotcha-#8 regression (\"maxLines\": null emitted somewhere) that the byte-diff normalize scrubs before comparing."
    - "The FeatureProbe backends (both .NET + bun; node-probe reuses the bun handler) grow a small state slot `TextNodeMaxLinesProbe` (`int?` on .NET, `number | null` on TS) plus 4 action-name arms: `textnode-maxlines-unset`, `textnode-maxlines-1`, `textnode-maxlines-2`, `textnode-maxlines-3`. Each arm mutates the state slot to null / 1 / 2 / 3 respectively. The BuildVm arm renders a single TextNode carrying the CURRENT state slot's value as its maxLines field — with a distinctive marker string as its `value` so the unset case has a positive tripwire text that no other branch emits: `value: \"TextNodeMaxLinesProbe=<state>\"` (e.g. `\"TextNodeMaxLinesProbe=unset\"`, `\"TextNodeMaxLinesProbe=1\"`, `\"TextNodeMaxLinesProbe=2\"`, `\"TextNodeMaxLinesProbe=3\"`)."
    - "Initial state (from GET) has TextNodeMaxLinesProbe = null (unset branch) with value string `\"TextNodeMaxLinesProbe=unset\"` — the GET step in the fixture asserts this AND that the JSON does NOT contain the substring `\"maxLines\":` anywhere in the rendered TextNodeMaxLinesProbe section (the always-on findNulls invariant guards against `\"maxLines\":null` drift; the positive substring assertion guards against branch-never-ran drift). The 3 POST steps each set the state to their target value and assert BOTH the new marker text substring AND the correct `\"maxLines\":N` wire literal appear in the response body."
    - "Byte-identical wire between .NET and bun/node backends. The parity diff (default `expectDiffOk`) MUST show ZERO drift between the 3 backends across all 4 steps. Byte-identical here means: JSON key order isn't load-bearing for parity's diff (parity/normalize.ts sorts keys before comparison, verified in existing fixtures) — but the presence + value of `\"maxLines\": N` and the marker TextNode's `value` must match exactly. If parity flags a drift, the fixture's byte-diff output is the authoritative signal — do NOT silence it; fix the emitting backend."
    - "The fixture step running order + state cumulative behavior: like the chat-composer.json fixture, steps run sequentially and mutate state cumulatively (unset → 1 → 2 → 3). Each step's expectBodyContains is a UNIQUE substring for that step's target maxLines value, and the earlier steps' assertions do NOT carry over into later steps (unset's marker `TextNodeMaxLinesProbe=unset` doesn't appear in the maxLines=1 response — the state slot flipped, so the marker text updated). This is the same pattern as chat-composer.json's streaming → attach → dropscope → submitmode sequence."
    - "The fixture's `$comment` field explains: (a) the fixture's purpose (parity coverage for TextNode.maxLines across all 4 states); (b) why each branch has a UNIQUE tripwire (gotcha #9 class-3 lesson: branches the fixture doesn't exercise are invisible to the diff); (c) how the marker TextNode value doubles as the branch-ran positive assertion + the class-2 gotcha-#8 findNulls defense catches the WhenWritingNull regression the byte-diff normalize scrubs before comparing; (d) why byte-parallel with chat-composer.json posture is intentional."
    - "The FeatureProbe controllers' `Action` methods learn the 4 new action-name arms in an if/else chain byte-parallel with the existing chat-composer-set-* arms (lines ~315-334 on the .NET controller). The bun handler mirrors the same 4 arms. Each arm does `state = state with { TextNodeMaxLinesProbe = <target> }` on .NET and `state = { ...state, textNodeMaxLinesProbe: <target> }` on bun. State passthrough behavior: any unknown action name still falls through to `throw new UnknownActionException(name)` (the existing final else arm)."
    - "The BuildVm renders the TextNodeMaxLinesProbe TextNode inside a new SectionNode `\"Phase 31 TextNode.maxLines probe\"` appended at the end of the FeatureProbe page — same posture as the existing chat-composer probe section (lines ~1678-1723). Do NOT touch existing sections; do NOT touch existing probe fields; append-only per the fixture-safety pattern."
    - "The parity harness picks up the new fixture automatically once registered in `backends.json` — no `parity/run.ts` code change required. Run `export PATH=\"$HOME/.dotnet:$PATH\" && bun run parity/run.ts` — the harness spawns the 3 FeatureProbe backends (dotnet-probe on :5011, bun-probe on :5012, node-probe on :5013), runs every registered fixture against each, and diffs normalized responses. The new fixture runs once per backend; a byte-mismatch or a failed expectBodyContains tripwire fails the run LOUDLY (per parity/run.ts's error-reporting shape)."
  artifacts:
    - path: parity/fixtures/textnode-maxlines.json
      provides: "4-step parity fixture exercising unset + maxLines 1/2/3 with expectBodyContains tripwires per step"
      contains: "textnode-maxlines"
    - path: demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
      provides: "TextNodeMaxLinesProbe state slot + 4 action arms + BuildVm section rendering the probe TextNode"
      contains: "TextNodeMaxLinesProbe"
    - path: demo/FeatureProbe-bun/handler.ts
      provides: "byte-parallel TS twin: textNodeMaxLinesProbe state slot + 4 action arms + buildVm section"
      contains: "textNodeMaxLinesProbe"
    - path: parity/backends.json
      provides: "the 3 FeatureProbe backends' fixtures arrays extended with textnode-maxlines"
      contains: "textnode-maxlines"
  key_links:
    - from: "parity/fixtures/textnode-maxlines.json"
      to: "the 3 FeatureProbe backends"
      via: "backends.json fixtures[] registration"
      pattern: "textnode-maxlines"
    - from: "each fixture step's expectBodyContains[]"
      to: "a UNIQUE substring the target branch emits"
      via: "gotcha #9 class-3 tripwire discipline"
      pattern: "TextNodeMaxLinesProbe=|\\\"maxLines\\\":[123]"
    - from: "unset step's positive substring assertion"
      to: "the always-on findNulls invariant"
      via: "unset marker proves branch ran; findNulls catches \"maxLines\":null regression that normalize scrubs"
      pattern: "TextNodeMaxLinesProbe=unset"
---

<objective>
Land the cross-backend parity coverage for `TextNode.maxLines`: a 4-step fixture exercising unset + 1 + 2 + 3, byte-diffed across dotnet-probe, bun-probe, and node-probe backends, with `expectBodyContains` tripwires per step per AGENTS.md gotcha #9 class-3 lesson. Extends the FeatureProbe backends' state + BuildVm to render the probe TextNode; extends `parity/backends.json` to register the new fixture on all 3 FeatureProbe backends.

Purpose: This is the two-backend-parity gate for the maxLines axis — the last defense against a class of drift that individual backend tests structurally cannot catch. The byte-diff PROVES the two backends emit identical wire for each of the 4 states; the expectBodyContains tripwires PROVE the fixture's own steps actually exercise the target branches (gotcha #9 class-3); the always-on findNulls invariant PROVES neither backend regressed the gotcha-#8 WhenWritingNull contract (guards the class-2 defect normalize scrubs before diffing).

Output: `parity/fixtures/textnode-maxlines.json` with 4 steps + $comment; state slot + 4 action arms + BuildVm section on both FeatureProbe backends (byte-parallel); backends.json extended; `bun run parity/run.ts` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-/31-CONTEXT.md
@AGENTS.md
@.planning/phases/31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-/31-01-PLAN.md
@.planning/phases/31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-/31-02-PLAN.md
</context>

<interfaces>
**Where in the code (verified before writing this plan):**

- `parity/backends.json` lines 132-160: the 3 FeatureProbe backend entries. Each has a `fixtures: ["feature-probe", "feature-probe-envelope", "chat-composer"]` array. Append `"textnode-maxlines"` to all 3.

- `parity/fixtures/chat-composer.json` — the byte-parallel precedent. 5 steps, GET + POST sequence, each step carries `expectBodyContains: [...]` and a `$comment` explaining the class-3 tripwire discipline. Read the whole file before drafting the new fixture — the shape is critical (steps run cumulatively; each expectBodyContains array is a UNIQUE substring; the $comment paragraph documents all 4 defense-classes).

- `demo/FeatureProbe/AspNetCore/FeatureProbeController.cs`:
  - State record `FeatureProbeState` at lines ~35-77, with the ~19 existing slots + the ~6 chat-composer slots. Append `int? TextNodeMaxLinesProbe = null` (or non-nullable with a defaulted "unset" convention — see design decision below).
  - `Initial()` factory at lines ~78-110 sets defaults. Append `TextNodeMaxLinesProbe: null`.
  - `Action` if/else chain at lines ~150-346 with each action arm. Append 4 new arms after the chat-composer-* arms (~line 340) and before the final `throw new UnknownActionException(name)`.
  - `BuildVm` at lines ~360-1733. Append a new SectionNode at the end of the pageChildren list (after the chat-composer probe section at ~lines 1678-1723).

- `demo/FeatureProbe-bun/handler.ts`:
  - Byte-parallel with the .NET controller. `state` interface + `buildInitialState` + `action` handler + `buildVm`. The chat-composer arms live at handler.ts lines ~1883-1893. Append 4 new arms in the same shape after them.
  - The bun handler + `demo/FeatureProbe-bun/server.ts` + `demo/FeatureProbe-bun/server-node.ts` all share the `handler.ts` module (bun and node both import from it). So a SINGLE handler.ts edit updates BOTH bun-probe AND node-probe backends.

**Design decision: `int?` vs default-sentinel for the state slot.** Two options:

- **Option A (recommended, matches CONTEXT §decisions philosophy):** state slot is `int? TextNodeMaxLinesProbe` on .NET (`number | null` on bun/node). Nullability propagates directly into the TextNode's `MaxLines` field (nullable → absent on wire per gotcha #8). Unset action arm sets to null; each numbered arm sets to its int value. The marker TextNode value formats as `$"TextNodeMaxLinesProbe={state.TextNodeMaxLinesProbe?.ToString() ?? \"unset\"}"` — the ternary handles both cases cleanly.

- **Option B:** state slot is `string TextNodeMaxLinesProbe` with values `""` (unset) / `"1"` / `"2"` / `"3"`. Avoids nullable state; matches the existing FeatureProbe state posture (see the AGENTS.md gotcha #8 note that the state record itself is NOT covered by intrinsic JsonIgnore attributes — every existing slot is non-nullable with empty/false defaults).

**Choose Option B** for consistency with the existing FeatureProbe state posture (verified: all ~25 existing slots on FeatureProbeState are non-nullable). The state slot becomes `string TextNodeMaxLinesProbe`, defaulting to `""`. The BuildVm arm maps `""` → null, `"1"` → 1, `"2"` → 2, `"3"` → 3, sets the TextNode's MaxLines accordingly. This mirrors the exact shape of the existing `ChatComposerStatus` string slot with `""`/`"sending"`/`"streaming"` mapping.

**Fixture shape (final):**

```json
{
  "name": "textnode-maxlines",
  "endpoint": "/api/probe",
  "actionEndpoint": "/api/probe/action",
  "$comment": "Phase 31 (v9.2.0) TextNode.maxLines axis parity fixture. Byte-parallel with chat-composer.json posture: 4 sequential steps exercise ALL FOUR wire states of TextNode.maxLines (unset / 1 / 2 / 3), each carrying a UNIQUE `expectBodyContains` tripwire that only the target branch can emit per AGENTS.md gotcha #9 class-3 lesson ('branches the fixture never runs are invisible to the byte-diff'). The unset step's marker text `TextNodeMaxLinesProbe=unset` is emitted by the buildVm arm ONLY when the state slot is empty (the wire-absent case); a mutation that stopped emitting the marker OR started emitting `\"maxLines\":null` when unset would fail LOUDLY — the always-on findNulls invariant (parity/run.ts) catches the second (class-2 gotcha-#8 defect: normalize scrubs `\"field\":null` BEFORE the diff so parity structurally can't see it; findNulls sees the raw parsed body). The 3 numbered steps assert the exact `\"maxLines\":N` wire literal for N∈{1,2,3} — closed-enum wire values, more diff-robust than the DOM class emission (which is browser-only). Steps run sequentially and mutate the FeatureProbeState slot cumulatively; each step's marker text uniquely identifies its state (`TextNodeMaxLinesProbe=1|2|3`) so carry-over from a prior step's marker never satisfies the current step's assertion. Byte-parallel with chat-composer.json — same $comment discipline, same class-2/class-3 defense structure. Do NOT weaken these tripwires without a plan-authored rationale — they are what stops a maxLines regression from being invisible to the diff.",
  "steps": [
    {
      "id": "unset-initial",
      "method": "GET",
      "expectBodyContains": [
        "TextNodeMaxLinesProbe=unset"
      ],
      "$comment": "Branch UNSET. State slot defaults to \"\" → buildVm renders TextNode(value:\"TextNodeMaxLinesProbe=unset\") with MaxLines omitted. The positive substring proves the branch ran. The always-on findNulls invariant separately catches a `\"maxLines\":null` regression (class-2 gotcha-#8 defect the normalize scrubs before diff)."
    },
    {
      "id": "maxlines-1",
      "method": "POST",
      "action": { "name": "textnode-maxlines-1" },
      "expectBodyContains": [
        "TextNodeMaxLinesProbe=1",
        "\"maxLines\":1"
      ],
      "$comment": "Branch MAXLINES=1. State slot flipped to \"1\"; buildVm renders TextNode(value:\"TextNodeMaxLinesProbe=1\", maxLines:1). BOTH assertions must pass: the marker text confirms the branch ran; the `\"maxLines\":1` wire literal confirms the axis emitted correctly."
    },
    {
      "id": "maxlines-2",
      "method": "POST",
      "action": { "name": "textnode-maxlines-2" },
      "expectBodyContains": [
        "TextNodeMaxLinesProbe=2",
        "\"maxLines\":2"
      ],
      "$comment": "Branch MAXLINES=2. Same shape as step 2; carry-over test: the prior step's `\"maxLines\":1` and `TextNodeMaxLinesProbe=1` MUST NOT satisfy this step's assertions (the state slot flipped)."
    },
    {
      "id": "maxlines-3",
      "method": "POST",
      "action": { "name": "textnode-maxlines-3" },
      "expectBodyContains": [
        "TextNodeMaxLinesProbe=3",
        "\"maxLines\":3"
      ],
      "$comment": "Branch MAXLINES=3. Closes the closed-enum coverage; anything above 3 is out-of-scope per CONTEXT §deferred 'Values 4+'."
    }
  ]
}
```

**Byte-parallel BuildVm section (.NET):**

```csharp
// v9.2.0 (Phase 31 / MAXLINES-PARITY) — TextNode.maxLines probe section.
// Renders a single TextNode carrying the state slot's mapped value in
// MaxLines, with a distinctive marker text in Value so the fixture has both
// a positive branch-ran assertion (marker text) AND a wire-literal
// assertion (`"maxLines":N`) per gotcha #9 class-3 discipline.
pageChildren.Add(new SectionNode(
    Heading: "Phase 31 TextNode.maxLines probe",
    Variant: SectionVariant.Card,
    Children: new ViewNode[]
    {
        new TextNode(
            Value: $"TextNodeMaxLinesProbe={(string.IsNullOrEmpty(state.TextNodeMaxLinesProbe) ? "unset" : state.TextNodeMaxLinesProbe)}")
        {
            // MaxLines is an init-only property OUTSIDE the primary ctor (Plan 31-02):
            // set via object-initializer syntax, NOT as a ctor named-arg. See
            // Plan 31-02 rationale — this placement preserves binary compat with
            // pre-9.2.0 companion NuGets (Markdown 0.2.x's packed IL).
            MaxLines = state.TextNodeMaxLinesProbe switch
            {
                "1" => 1,
                "2" => 2,
                "3" => 3,
                _ => null,
            },
        },
    }));
```

**Byte-parallel BuildVm section (bun/node — `demo/FeatureProbe-bun/handler.ts`):**

```typescript
// v9.2.0 (Phase 31 / MAXLINES-PARITY) — TextNode.maxLines probe section.
pageChildren.push({
  type: "section",
  heading: "Phase 31 TextNode.maxLines probe",
  variant: "card",
  children: [
    {
      type: "text",
      value: `TextNodeMaxLinesProbe=${state.textNodeMaxLinesProbe === "" ? "unset" : state.textNodeMaxLinesProbe}`,
      ...(state.textNodeMaxLinesProbe === "1" ? { maxLines: 1 } :
          state.textNodeMaxLinesProbe === "2" ? { maxLines: 2 } :
          state.textNodeMaxLinesProbe === "3" ? { maxLines: 3 } : {}),
    },
  ],
});
```

The spread-object pattern is important: when the state is `""`, the maxLines field must be OMITTED (undefined), not set to null — this is exactly the wire posture the parity diff checks.

**Backends.json edit** (3 places — one per FeatureProbe backend):
```json
"fixtures": ["feature-probe", "feature-probe-envelope", "chat-composer", "textnode-maxlines"]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend FeatureProbeState + Action + BuildVm on both FeatureProbe backends (byte-parallel)</name>
  <files>demo/FeatureProbe/AspNetCore/FeatureProbeController.cs, demo/FeatureProbe-bun/handler.ts</files>
  <read_first>
    - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (READ lines ~35-110 for the state record + Initial factory; lines ~140-346 for the Action if/else chain; lines ~1678-1733 for the BuildVm append pattern the chat-composer section uses)
    - demo/FeatureProbe-bun/handler.ts (READ the byte-parallel structure: state interface, buildInitialState, action arms, buildVm — grep for `chat-composer-set-streaming` to locate the chat-composer arms, matching lines are ~1883-1893)
    - .planning/phases/31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-/31-CONTEXT.md §decisions "CSS class emission" (READ: confirms the wire shape unset/1/2/3 the probe must render)
  </read_first>
  <action>
    Two coordinated edits on the byte-parallel FeatureProbe backends. Do them together to keep the diff tight.

    **1. `demo/FeatureProbe/AspNetCore/FeatureProbeController.cs`:**

    a) Add `string TextNodeMaxLinesProbe` to the `FeatureProbeState` record after `ChatComposerSubmitMode` (line ~75). Non-nullable string with `""` default — matches the existing ChatComposerStatus / ChatComposerDropScope / ChatComposerSubmitMode posture. Add a one-line comment: `// v9.2.0 (Phase 31 / MAXLINES-PARITY) — "" (unset) | "1" | "2" | "3", drives BuildVm's TextNode.MaxLines emission`.

    b) Set `TextNodeMaxLinesProbe: ""` in the `Initial()` factory (line ~110, after `ChatComposerSubmitMode: ""`).

    c) Add 4 new action arms in the Action if/else chain, appended after the existing chat-composer arms (~line 340) and before the final else-throw:

    ```csharp
    else if (name == "textnode-maxlines-unset") { state = state with { TextNodeMaxLinesProbe = "" }; }
    else if (name == "textnode-maxlines-1")     { state = state with { TextNodeMaxLinesProbe = "1" }; }
    else if (name == "textnode-maxlines-2")     { state = state with { TextNodeMaxLinesProbe = "2" }; }
    else if (name == "textnode-maxlines-3")     { state = state with { TextNodeMaxLinesProbe = "3" }; }
    ```

    d) Append a new SectionNode in `BuildVm` after the chat-composer section (~line 1723, before the ModalNode append at ~line 1725):

    ```csharp
    pageChildren.Add(new SectionNode(
        Heading: "Phase 31 TextNode.maxLines probe",
        Variant: SectionVariant.Card,
        Children: new ViewNode[]
        {
            new TextNode(
                Value: $"TextNodeMaxLinesProbe={(string.IsNullOrEmpty(state.TextNodeMaxLinesProbe) ? "unset" : state.TextNodeMaxLinesProbe)}",
                MaxLines: state.TextNodeMaxLinesProbe switch
                {
                    "1" => 1,
                    "2" => 2,
                    "3" => 3,
                    _ => null,
                }),
        }));
    ```

    Verify this constructs the TextNode with the appended positional MaxLines argument (from Plan 31-02) — if MaxLines isn't yet in ViewModels.cs, this won't compile. That's a dependency signal — Plan 31-02 must land before this plan; the depends_on chain enforces it.

    **2. `demo/FeatureProbe-bun/handler.ts`** — byte-parallel with the .NET controller:

    a) Add `textNodeMaxLinesProbe: string` to the state interface after `chatComposerSubmitMode`.

    b) Set `textNodeMaxLinesProbe: ""` in the state initializer (grep for `chatComposerSubmitMode: ""` — same line region).

    c) Add 4 new action arms after the chat-composer arms (~line 1893):

    ```typescript
    else if (name === "textnode-maxlines-unset") { state = { ...state, textNodeMaxLinesProbe: "" }; }
    else if (name === "textnode-maxlines-1")     { state = { ...state, textNodeMaxLinesProbe: "1" }; }
    else if (name === "textnode-maxlines-2")     { state = { ...state, textNodeMaxLinesProbe: "2" }; }
    else if (name === "textnode-maxlines-3")     { state = { ...state, textNodeMaxLinesProbe: "3" }; }
    ```

    d) Append a new section in buildVm mirroring the .NET twin — same shape as the interfaces block above. Use the spread-object pattern for the maxLines field so it's OMITTED (not null) when the state slot is `""`.

    Save both files. Do NOT rebuild yet — Task 2 exercises them via the parity harness.
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" && dotnet build demo/FeatureProbe/AspNetCore --nologo -v minimal 2>&1 | tail -5 && cd demo/FeatureProbe-bun && bun check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TextNodeMaxLinesProbe" demo/FeatureProbe/AspNetCore/FeatureProbeController.cs` returns >= 6 (state slot decl + Initial + 4 action arms + BuildVm reference — count exact matches; the switch expression may double-count some).
    - `grep -c "textNodeMaxLinesProbe" demo/FeatureProbe-bun/handler.ts` returns >= 6 (byte-parallel).
    - `dotnet build demo/FeatureProbe/AspNetCore` exits 0 (the new TextNode construction with `MaxLines:` binds correctly to the Plan 31-02 field).
    - The bun handler passes a shallow type-check (via existing bun / tsconfig setup if present; if not, run `bun run demo/FeatureProbe-bun/handler.ts --check` or equivalent).
  </acceptance_criteria>
  <done>
    Both FeatureProbe backends carry the maxLines probe state + action arms + BuildVm section, byte-parallel with each other. Ready for Task 2 to add the parity fixture + backends.json registration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author parity/fixtures/textnode-maxlines.json + register in parity/backends.json (all 3 FeatureProbe backends)</name>
  <files>parity/fixtures/textnode-maxlines.json, parity/backends.json</files>
  <read_first>
    - parity/fixtures/chat-composer.json (READ the WHOLE file — byte-parallel precedent; $comment discipline, per-step expectBodyContains tripwires, sequential-cumulative-state pattern)
    - parity/backends.json (READ lines 132-160 — the 3 FeatureProbe backend entries where the fixture registration goes; verify the fixtures array field name)
    - parity/run.ts (READ the harness's fixture loader + the always-on findNulls invariant; understand how expectBodyContains is matched — case-sensitive substring per step)
    - .planning/phases/31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-/31-CONTEXT.md §decisions (READ the exact wire posture unset/1/2/3 must satisfy)
  </read_first>
  <action>
    Two edits:

    **1. Create `parity/fixtures/textnode-maxlines.json`** — the 4-step fixture per the <interfaces> block above. Verbatim copy the $comment paragraph and step shapes. Verify:
    - The `name` field is `"textnode-maxlines"` (matches the registration key).
    - `endpoint` is `/api/probe`, `actionEndpoint` is `/api/probe/action` (same as chat-composer.json).
    - Step IDs are `unset-initial`, `maxlines-1`, `maxlines-2`, `maxlines-3` (kebab-case, distinctive).
    - Each POST step's `action.name` matches EXACTLY the arm names added in Task 1 (`textnode-maxlines-1`, `textnode-maxlines-2`, `textnode-maxlines-3`). The unset case uses GET (step 1) and doesn't need a POST arm — the initial state already carries unset.
    - Each step's `expectBodyContains` array is EXACTLY the substrings enumerated in <interfaces>. Do NOT weaken; do NOT add extras.
    - The `$comment` at the fixture root is the full explanatory paragraph from <interfaces> (matches the chat-composer.json $comment discipline). Per-step $comments explain the branch + defense classes.

    **2. Edit `parity/backends.json`** — extend the `fixtures` array on all 3 FeatureProbe backends (dotnet-probe at ~line 140, bun-probe at ~line 150, node-probe at ~line 160). Append `"textnode-maxlines"` to each of the 3 arrays, keeping the existing order:

    ```json
    "fixtures": ["feature-probe", "feature-probe-envelope", "chat-composer", "textnode-maxlines"]
    ```

    Do NOT touch any other backend's fixtures. Do NOT add a new backend entry (the fixture reuses the existing FeatureProbe backends per <interfaces> — no new .csproj or bun server needed).

    Run `bun run parity/run.ts` locally to verify green. Capture the tail output for the SUMMARY.

    ⚠️ **PATH-export gate**: `export PATH="$HOME/.dotnet:$PATH"` before running parity — the harness spawns `dotnet` as a child and dies with a bare `spawn` trace without it (documented in AGENTS.md `parity` bullet).
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" && cd "$(git rev-parse --show-toplevel)" && bun run parity/run.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `parity/fixtures/textnode-maxlines.json` file exists with the 4-step structure.
    - `jq '.name' parity/fixtures/textnode-maxlines.json` returns `"textnode-maxlines"`.
    - `jq '.steps | length' parity/fixtures/textnode-maxlines.json` returns `4`.
    - `grep -c "textnode-maxlines" parity/backends.json` returns `3` (registered on all 3 FeatureProbe backends).
    - `export PATH="$HOME/.dotnet:$PATH" && bun run parity/run.ts` exits 0.
    - Parity harness output confirms the new fixture ran against all 3 FeatureProbe backends (grep tail for "textnode-maxlines" — expect 3 mentions, one per backend).
    - No `findNulls` warning fires on any of the 3 backends' responses for the new fixture (proves the WhenWritingNull attribute from Plan 31-02 and the undefined-vs-null posture from Plan 31-01 both fire correctly across all 4 states).
  </acceptance_criteria>
  <done>
    Parity fixture is authored, registered, and green across all 3 FeatureProbe backends. TextNode.maxLines axis has full cross-backend byte-parity coverage. Ready for Plan 31-04 (release ritual).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parity harness ↔ FeatureProbe backends | The harness spawns backends as child processes on loopback; no external network. |
| Fixture steps ↔ backend state | Steps mutate an in-process state record via the same wire the framework tests; no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Tampering | Fixture step's expectBodyContains substring silently matches an UNRELATED emission | mitigate | The marker text `TextNodeMaxLinesProbe={value}` is a distinctive Pascal-case identifier that appears NOWHERE else in the FeatureProbe wire (verified by grepping the full FeatureProbe response body from a fresh GET — no other emitting code path uses this exact substring). The `\"maxLines\":N` wire literals are ALSO distinctive: no other TextNode in the FeatureProbe fixture sets a MaxLines value, so a mistaken match would require another wire field to spell exactly `maxLines`. Both defense layers together make the assertion mutation-proof. |
| T-31-03-02 | Denial of service | Parity harness hangs on backend startup / fixture load | accept | Same posture as every other parity fixture; the harness's own timeouts + PATH-export gate handle backend startup failure. New fixture inherits the mechanism. |
| T-31-03-03 | Information disclosure | Fixture reveals state slot names | accept | The parity fixture is a public artifact in this repo; state slot names are already public via the source. No secret in the fixture. |
| T-31-03-04 | Repudiation | A failing parity run without clear step-attribution | mitigate | Each step's `$comment` explicitly names the branch + the defense discipline; a failing run's output includes the step ID (`unset-initial` / `maxlines-1` / etc.) so the failure locus is unambiguous. |

**No further threats identified.** The fixture is scoped, sandboxed, and adds no external surface.
</threat_model>

<verification>
Task 1 machine-verified via `dotnet build demo/FeatureProbe/AspNetCore` + bun check. Task 2 machine-verified via `bun run parity/run.ts` exits 0 with tail-output confirming the new fixture ran against all 3 FeatureProbe backends.
</verification>

<success_criteria>
- `parity/fixtures/textnode-maxlines.json` exists, 4 steps, per-step expectBodyContains tripwires present.
- All 3 FeatureProbe backends registered for the new fixture in `parity/backends.json`.
- Both FeatureProbe backends carry the byte-parallel state slot + 4 action arms + BuildVm section.
- `bun run parity/run.ts` green — byte-diff across 3 backends for the new fixture shows zero drift; findNulls invariant clean.
- Zero regression in existing FeatureProbe fixture coverage (chat-composer.json + feature-probe.json + feature-probe-envelope.json all still pass).
</success_criteria>

<output>
Create `.planning/phases/31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-/31-03-SUMMARY.md`. Record: files modified with line-ranges, fixture step IDs + tripwire substrings, verify-command exit codes with tail output, parity harness output for the new fixture across all 3 backends.

**Commit intent:** `feat(31-03): parity coverage for TextNode.maxLines — fixture + FeatureProbe backends`
</output>
