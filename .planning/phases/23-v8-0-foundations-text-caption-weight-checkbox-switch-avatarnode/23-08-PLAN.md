---
phase: 23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode
plan: 08
type: execute
wave: 5
depends_on: ["23-01", "23-02", "23-03", "23-04"]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "`CHANGELOG.md` gains an `## Unreleased - v8.0.0 (in progress)` heading if it doesn't already have one."
    - "Under the Unreleased heading, an `### Added` subsection lists all 4 foundations with their COMP-XX IDs: TextNode.style: caption (COMP-01), TextNode weight axis (COMP-02), CheckboxNode.variant: switch (COMP-03), AvatarNode (COMP-04)."
    - "Each entry names the specific wire shape / class name / rendering behavior a consumer would need to know about."
    - "The Unreleased heading explicitly notes 'batch-then-ship - v8.0.0 publishes at Phase 26 closeout' so no operator accidentally publishes Phase 23 as its own release."
    - "No version bump on `viewmodel-shell/package.json` or `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` (batch-then-ship is LOCKED in CONTEXT.md section 5)."
    - "The pre-existing CHANGELOG content (v7.0.0 icons + every prior release) is UNTOUCHED - verified by no-diff on the pre-existing sections."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "New Unreleased - v8.0.0 (in progress) heading + Added subsection with 4 foundation entries."
      contains: "Unreleased"
  key_links:
    - from: "CHANGELOG.md"
      to: ".planning/design/composite-nodes-layer.md"
      via: "Unreleased entries cite the design of record"
      pattern: "composite-nodes-layer"
---

<objective>
Accumulate Phase 23's 4 foundation additions into `CHANGELOG.md` under an `## Unreleased - v8.0.0 (in progress)` heading. Per LOCKED CONTEXT.md section 5: batch-then-ship - v8.0.0 publishes only at Phase 26 closeout with all 10 composites + 3 wire tweaks + 4 foundations in one aligned release.

Purpose: CHANGELOG is the append-only version-specific history AGENTS.md requires. Every change that ships to consumers needs an entry, but Phase 23 doesn't publish - the entries accumulate under Unreleased. When Phase 26 flips the version and publishes, that heading becomes the release entry.

Output: One new heading + 4 additions section under it, citing the design doc + COMP-XX IDs, with an explicit batch-then-ship reminder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Unreleased heading + Added subsection to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (read the top of the file - need to see the current top-of-file structure, any existing `## Unreleased` heading, the format of the most recent release entry - e.g. `## 7.1.0`, `## 7.0.0` - for tone/format matching)
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md section 5 (LOCKED batch-then-ship rule) + section Specific ideas / CHANGELOG entry format
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-PATTERNS.md section 12 (release ritual is OUT-OF-SCOPE for Phase 23; the CHANGELOG format template)
    - AGENTS.md "Working agreement" - `CHANGELOG.md` + `MIGRATION.md` are release-gated (append at bump time). Phase 23 is a PRE-release accumulation.
  </read_first>
  <action>
    Read `CHANGELOG.md`. Determine whether an `## Unreleased` heading already exists (either from a prior in-progress accumulation, or none at all).

    **Case A - no existing `## Unreleased` heading:**
    INSERT at the very top of the file (before the first `## X.Y.Z` release heading - likely `## 7.1.0` or similar currently), a new heading block. Exact content:

    ```
    ## Unreleased - v8.0.0 (in progress)

    > **Batch-then-ship:** v8.0.0 publishes at Phase 26 closeout with all 10 composites + 3 wire tweaks + 4 foundations in one aligned release. Do NOT publish Phase 23 as its own release. See `.planning/design/composite-nodes-layer.md` for the milestone design of record.

    ### Added
    - `TextNode.style: "caption"` - the 3rd typographic tier: renders `.vms-text--caption` at `font-size: var(--vms-text-xs)`, `color: var(--vms-text-muted)`, `opacity: 0.85`, `line-height: 1.4`. Downstream composites (`ListRowNode.meta[]`, `MessageNode.timestamp`, `TimelineEntryNode.time`) consume it for micro-meta lines. (COMP-01)
    - `TextNode.weight?: "regular" | "medium" | "bold"` - new orthogonal weight axis: renders `.vms-text--weight-{value}` at `font-weight: 400/500/700`. Semi-bold body-size weight for row primaries (`ListRowNode.primary`, `MessageNode.author`, `UserRowNode.name`, `SettingRowNode.label`). Omitted = the framework default weight for the node's `style`. (COMP-02)
    - `CheckboxNode.variant?: "checkbox" | "switch"` - visual switch-slider render mode; wire and dispatch semantics UNCHANGED (still boolean, still `bind`/change dispatch, still `<input type="checkbox">`). `"switch"` emits `.vms-field--switch` + native `role="switch"` on the input; older adapters gracefully degrade to standard checkbox on unknown variant. (COMP-03)
    - `AvatarNode` - new standalone primitive: circular slot with content-resolution priority image > initials > icon > empty. Wire shape: `{ type: "avatar", initials?, image?, icon?: IconName, size?: "sm"|"md"|"lg"|"xl", tone?: Tone, alt? }`. Sizes map to 1.5/2/2.5/3rem. Tone applies to non-image modes only. Reuses the v7.0.0 `IconName` closed union for the fallback icon slot and `renderIconSvg` for icon-mode rendering. Consumed by `UserRowNode`, `MessageNode`, `ChipNode` downstream (Phase 24-25); usable standalone in mention pickers, assignee columns, comment threads. Circular only for v1 (other shapes deferred). (COMP-04)
    ```

    **Case B - existing `## Unreleased` heading:**
    Under the existing Unreleased block, extend the `### Added` subsection (creating one if absent) with the same 4 entries above. Do NOT duplicate the batch-then-ship reminder if already present.

    Do NOT modify any content BELOW the Unreleased heading. All prior release entries (`## 7.1.0`, `## 7.0.0`, `## 6.x.x`, etc.) are strictly preserved.

    Do NOT bump `viewmodel-shell/package.json` or `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` - LOCKED in CONTEXT.md section 5.

    Do NOT touch `MIGRATION.md` - per CONTEXT.md the note would be trivial ("all additions are additive; no changes required for existing consumers"), and MIGRATION.md is release-gated (touched at Phase 26 alongside CHANGELOG heading flip).
  </action>
  <verify>
    <automated>grep -c 'Unreleased' CHANGELOG.md && grep -c 'v8.0.0' CHANGELOG.md && grep -c 'batch-then-ship' CHANGELOG.md && grep -c 'AvatarNode' CHANGELOG.md && grep -c 'COMP-01' CHANGELOG.md && grep -c 'COMP-04' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Unreleased' CHANGELOG.md` returns >= 1.
    - `grep -c 'v8.0.0' CHANGELOG.md` returns >= 1.
    - `grep -c 'batch-then-ship' CHANGELOG.md` returns >= 1 (the reminder is prominent).
    - `grep -c 'composite-nodes-layer.md' CHANGELOG.md` returns >= 1 (design doc cited).
    - All 4 COMP-XX IDs appear: `grep -c 'COMP-01' CHANGELOG.md` >= 1, same for COMP-02, COMP-03, COMP-04.
    - All 4 additions named: caption, weight axis, switch variant, AvatarNode - verify each via a distinct grep.
    - No version bump: `grep -c '"version": "8' viewmodel-shell/package.json` returns 0. `grep '<Version>8' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns nothing.
    - No MIGRATION.md change: `git diff --stat MIGRATION.md` shows no changes.
    - Existing content preserved: `grep -c '## 7' CHANGELOG.md` returns >= 1 (v7.x release headings still present).
  </acceptance_criteria>
  <done>
    CHANGELOG has Unreleased heading with 4 foundation entries; batch-then-ship reminder in place; no version bump; existing history intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure documentation - CHANGELOG accumulation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08-01 | (none) | CHANGELOG.md Unreleased heading | accept | Pure documentation. No code, no wire change, no runtime behavior. The primary risk here is OPERATIONAL, not security: an operator seeing 4 changelog entries might assume they should publish. The batch-then-ship reminder callout at the top of the Unreleased section is the mitigation - prominent enough that any accidental publish would require overriding an explicit "do NOT publish" note. |
</threat_model>

<verification>
- All acceptance criteria are grep-based (documented above).
- Full green-tree gate exercised by 23-09.
</verification>

<success_criteria>
- CHANGELOG.md has Unreleased - v8.0.0 (in progress) heading + Added subsection with 4 entries.
- Batch-then-ship reminder is prominent.
- Design doc cited by exact path.
- Every COMP-XX ID appears.
- No version bump; MIGRATION.md untouched; existing CHANGELOG content preserved.
</success_criteria>

<output>
Create `.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-08-SUMMARY.md` recording the exact insertion point + verifying no existing content was touched.
</output>
