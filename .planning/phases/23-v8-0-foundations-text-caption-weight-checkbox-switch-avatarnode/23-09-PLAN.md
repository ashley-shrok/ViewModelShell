---
phase: 23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode
plan: 09
type: execute
wave: 6
depends_on: ["23-01", "23-02", "23-03", "23-04", "23-05", "23-06", "23-07", "23-08"]
files_modified: []
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "The FULL green-tree gate runs green: `npm run build && check:test-types && check:core-globals && check:aa-contrast && check:no-demo-style && check:demo-types && npx vitest run` in `viewmodel-shell/`, PLUS `dotnet test viewmodel-shell-dotnet/Tests`, PLUS every `demo/**/*.Tests.csproj`, PLUS `bun run parity/run.ts`."
    - "The AA-contrast hand-check is done and documented in the SUMMARY: caption text, switch on/off track vs thumb, avatar initials/icon vs each tone circle - default + all 12 themes."
    - "Ashley (operator) has done a visual smoke on the Showcase Foundations tab served over the tailnet: caption reads right, weight variants distinguishable, switch renders as a slider (not a checkbox), avatars at all 4 sizes / 4 tones / 4 content modes render as expected."
    - "Every COMP-XX requirement is closed: COMP-01 (caption), COMP-02 (weight axis), COMP-03 (switch variant), COMP-04 (AvatarNode) - each has code shipped + tests green + parity tripwire green + demo adoption + CHANGELOG entry."
    - "NO release ships in this plan (batch-then-ship LOCKED per CONTEXT.md section 5). No `npm publish`, no `dotnet nuget push`, no version bump, no tag."
  artifacts:
    - path: ".planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md"
      provides: "Records the green-tree gate results + AA hand-check + Ashley's visual sign-off + COMP-XX requirement-to-artifact cross-check."
      contains: "green"
  key_links:
    - from: "All prior plans (23-01..08)"
      to: "The green-tree gate + Ashley sign-off"
      via: "Phase 23 landing verification"
      pattern: "green"
---

<objective>
Run the FULL local green-tree gate one final time with everything from plans 23-01..08 in place, hand-check AA contrast for every new fg/bg pair across default + all 12 themes, run parity green across all 3 FeatureProbe backends, and get Ashley's visual sign-off on the Showcase Foundations tab served over the tailnet. Records the requirement-to-artifact cross-check for COMP-01..04.

Purpose: Phase 23 does NOT ship a release (batch-then-ship LOCKED). But every AGENTS.md gate must be green before commit lands, and every banked lesson (parity tripwires, AA-contrast per new pair, fleet-adoption discipline) requires operator eyeballing for a visual change. This plan is the "landing verification" - it doesn't add code, it proves the code that landed is correct + coherent.

Output: SUMMARY.md documenting green-tree evidence, per-theme AA verdicts, Ashley's sign-off note, COMP-XX cross-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md
@.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full green-tree gate + parity + all .NET test projects</name>
  <files></files>
  <read_first>
    - AGENTS.md "Working Agreement" - the exact green-tree gate command list (`npm run build && check:test-types && check:core-globals && check:aa-contrast && check:no-demo-style && check:demo-types && npx vitest run` + .NET tests + parity + every `demo/**/*.Tests.csproj`)
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md (framework conventions section - green-tree gate is non-negotiable before commit even without a publish)
  </read_first>
  <action>
    Ensure `~/.dotnet` is on PATH first (per AGENTS.md gotcha #9: `export PATH="$HOME/.dotnet:$PATH"`). Then run each gate command in sequence, capturing output. Any RED result blocks Phase 23 closeout.

    **Gate command sequence:**

    (a) Framework build + all `check:*` scripts + vitest, in `viewmodel-shell/`:
    ```
    cd viewmodel-shell
    npm run build
    npm run check:test-types
    npm run check:core-globals
    npm run check:aa-contrast
    npm run check:no-demo-style
    npm run check:demo-types
    npx vitest run
    ```

    (b) Framework .NET Tests project (the one AGENTS.md gotcha #9 named as easy to forget - was uncompilable 3.0.0 -> 3.3.0):
    ```
    export PATH="$HOME/.dotnet:$PATH"
    dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal
    ```

    (c) EVERY `demo/**/*.Tests.csproj` (per AGENTS.md gotcha #9):
    ```
    for p in $(find demo -name '*.Tests.csproj'); do
      echo "=== $p ==="
      dotnet test "$p" --nologo -v minimal
    done
    ```

    (d) Cross-backend parity:
    ```
    cd parity
    bun run run.ts
    ```

    RECORD every gate's result in the phase SUMMARY. Any RED result (missing tool, failing test, dotnet not on PATH) STOPS Phase 23 closeout - fix the underlying issue (or, if pre-existing per AGENTS.md rule, surface to operator with an explicit waiver request).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npm run build && npm run check:test-types && npm run check:core-globals && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types && npx vitest run && cd .. && export PATH="$HOME/.dotnet:$PATH" && dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal && (for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done) && cd parity && bun run run.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `viewmodel-shell/` succeeds (TypeScript compiles).
    - `npm run check:test-types` green.
    - `npm run check:core-globals` green (core `index.ts` has ZERO platform globals - the AvatarNode renderer legitimately lives in `browser.ts`).
    - `npm run check:aa-contrast` green (the FIXED 13-pair gate).
    - `npm run check:no-demo-style` green.
    - `npm run check:demo-types` green (Showcase's Foundations adoption compiles against strict tsconfig).
    - `npx vitest run` green - all shipped tests + the 4 new test files (text-caption, text-weight, checkbox-switch, avatar-render, avatar-wire) pass.
    - `dotnet test viewmodel-shell-dotnet/Tests` green including the 4 new test files (TextCaptionSerializationTests, TextWeightSerializationTests, CheckboxSwitchSerializationTests, AvatarNodeSerializationTests).
    - Every `demo/**/*.Tests.csproj` green (verify by iterating over `find demo -name '*.Tests.csproj'` output - no red results).
    - `bun run parity/run.ts` byte-identical GREEN across all 3 FeatureProbe backends AND all 9 `expectBodyContains` tripwires match on every backend.
    - Every gate's output is captured in the phase SUMMARY with pass/fail state.
  </acceptance_criteria>
  <done>
    Every AGENTS.md gate green; no red results anywhere; parity + all .NET test projects included; ready for the visual smoke + AA re-verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Everything from plans 23-01..08:
    - `TextNode.style: "caption"` renders as text-xs muted with 0.85 opacity.
    - `TextNode.weight` axis: three CSS classes at font-weight 400/500/700.
    - `CheckboxNode.variant: "switch"` renders as a slider track + thumb; native role=switch on the input.
    - `AvatarNode` at 4 sizes x 4 tones x 4 content modes (image/initials/icon/empty).
    - Showcase demo has a Foundations section demonstrating all 4 in situ.
    - AGENTS.md has a new "Route B composite-nodes layer" governance section.
    - `.planning/design/composite-nodes-layer.md` design of record is written.
    - CHANGELOG has Unreleased - v8.0.0 (in progress) with 4 entries.
    - Full green-tree gate + parity + every .NET test project all green (from this plan's Task 1).
  </what-built>
  <how-to-verify>
    (1) Serve the Showcase demo over the tailnet:
    ```
    cd demo/Showcase/frontend
    npm run dev -- --host 0.0.0.0 --port <a-free-port>
    ```
    (Or the demo's existing preview / build+serve incantation - grep `demo/Showcase/package.json` scripts to find the right command.)

    (2) Navigate to the Showcase served URL on the tailnet in a real browser. Find the Foundations section/tab (added by plan 23-07). Verify VISUALLY:
    - **Caption tier:** The `body` and `muted` and `caption` labels look right - each visibly smaller/lighter than the previous. Caption is the smallest, muted-with-opacity.
    - **Weight variants:** Three body-styled TextNodes with regular/medium/bold weights are visibly distinct.
    - **Switch variant:** The three switch checkboxes render as SLIDER tracks + THUMBS, not as boxes with checkmarks. The classic checkbox alongside for contrast renders as a normal square. Toggle one on: the thumb slides + track becomes accent-colored.
    - **AvatarNode:** All 4 sizes (sm/md/lg/xl) visible, clearly different diameters. All 4 tones (danger/warning/success/info) render as tone-tinted circles. Icon-mode avatars have a white icon centered in the tinted circle. Image-mode avatar loads (or shows the browser broken-image icon if the placeholder URL doesn't resolve - that's fine, the render behavior is what matters). Initials read cleanly on all tone backgrounds.

    (3) Toggle the theme picker (Showcase has a theme switcher) - verify the Foundations section looks right on default + at least 2-3 dark themes + at least 2-3 light themes. The AA-contrast hand-check tests done in plans 23-01/03/04 covered contrast values; this is a live sanity check.

    (4) Confirm all 4 additions "look right" per your (Ashley's) judgment. If any look wrong (e.g. switch thumb doesn't animate, avatar clipping is wrong, caption is indistinguishable from muted), name specifically what's wrong so a follow-up task can fix it.

    Additionally, re-verify the AA-contrast hand-check by inspecting the SUMMARY.md files from plans 23-01/03/04 (records for caption text, switch on/off, avatar initials/icon x tone). Any theme documented as failing should have a deepening or KNOCKOUT mitigation recorded. If a theme was skipped, name it now.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 foundations look right on default + representative themes AND the AA hand-check is complete. Type "fix: {description}" if any addition needs a visual fix before Phase 24 opens. Type "AA-gap: {description}" if an AA pair was missed and needs a hand-check re-run.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Requirement-to-artifact cross-check + write SUMMARY.md</name>
  <files>.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md</files>
  <read_first>
    - All 8 prior plan SUMMARY files (23-01-SUMMARY.md through 23-08-SUMMARY.md) - if they exist. If not, the plans themselves.
    - .planning/REQUIREMENTS.md (COMP-01..04 definitions - the criteria Phase 23 closes)
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md section Locked Decisions (each of the 7 locked items has a landing artifact)
  </read_first>
  <action>
    Write `.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md` documenting Phase 23 closeout. Sections:

    (a) **Green-tree gate results** - table with columns `Gate | Result | Command | Notes`. Every command from Task 1 gets a row.

    (b) **Requirement-to-artifact cross-check** - a table with COMP-01..04 as rows. Columns: `Requirement | Wire type shipped | Renderer | CSS | .NET twin | Parity tripwire | Tests | Demo adoption | CHANGELOG`. Every cell filled with the exact file+line evidence.

    Example row (COMP-01):
    | COMP-01 caption | `TextNode.style: "caption"` in `index.ts:997` | `browser.ts:3213` (unchanged - template covers it) | `default.css:1129` `.vms-text--caption` rule | `TextStyle.Caption` in `ViewModels.cs:163` | `"style":"caption"` tripwire in `parity/fixtures/feature-probe.json` | `test/text-caption.test.ts` + `Tests/TextCaptionSerializationTests.cs` | `demo/Showcase/frontend/src/main.ts` Foundations section | `CHANGELOG.md` under Unreleased v8.0.0 |

    (c) **AA-contrast hand-check re-verification** - restate the per-theme results from plans 23-01, 23-03, 23-04's SUMMARY files. Anywhere any theme required deepening or KNOCKOUT mitigation, name the deepening percentage or knockout pattern.

    (d) **Design-doc + AGENTS.md landing** - one line each confirming:
    - `.planning/design/composite-nodes-layer.md` exists and is >= 300 lines (plan 23-05).
    - AGENTS.md has the "Route B composite-nodes layer" section (plan 23-06).
    - CHANGELOG.md has the Unreleased v8.0.0 heading + 4 additions (plan 23-08).

    (e) **Ashley's visual sign-off** - transcribe the exact resume-signal text from Task 2.

    (f) **NO release ship** - explicitly note: no version bump, no npm publish, no dotnet nuget push, no git tag. Batch-then-ship LOCKED per CONTEXT.md section 5; v8.0.0 releases at Phase 26 closeout.

    (g) **Ready for Phase 24** - explicit "green light to run `/gsd:plan-phase 24`" if all gates + sign-off are green.

    Update ROADMAP.md if convention allows - mark Phase 23 plans complete. (Grep `.planning/ROADMAP.md` for the Phase 23 plan checklist and tick boxes if that convention is in use.)
  </action>
  <verify>
    <automated>test -f .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md && grep -c 'COMP-01' .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md && grep -c 'COMP-04' .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md && grep -c 'green' .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md && grep -c 'batch-then-ship' .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `23-09-SUMMARY.md` exists.
    - Contains a green-tree gate results table with every gate command from Task 1.
    - Contains a requirement-to-artifact table with COMP-01, COMP-02, COMP-03, COMP-04 as rows and 9+ columns of evidence per row.
    - `grep -c 'COMP-01' 23-09-SUMMARY.md` >= 1, same for COMP-02, COMP-03, COMP-04.
    - `grep -c 'AA' 23-09-SUMMARY.md` >= 1 (AA-contrast section present).
    - `grep -c 'Ashley' 23-09-SUMMARY.md` >= 1 (sign-off transcribed).
    - `grep -c 'batch-then-ship' 23-09-SUMMARY.md` >= 1 (no-release confirmation).
    - `grep -c 'Phase 24' 23-09-SUMMARY.md` >= 1 (next-phase readiness statement).
  </acceptance_criteria>
  <done>
    Phase 23 SUMMARY records green-tree + AA + sign-off + requirement cross-check + no-release confirmation + next-phase readiness. Phase 23 is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan. No code, no wire change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09-01 | (none) | Green-tree verification + operator sign-off | accept | This plan verifies that the code shipped in plans 23-01..08 is correct + coherent. It doesn't add new code. Attack surface delta: zero. |
| T-23-09-02 | Operational | Accidental publish (operator sees the "Unreleased" CHANGELOG and reflexes to publish) | mitigate | The CHANGELOG's Unreleased heading (from plan 23-08) contains an explicit batch-then-ship reminder. This plan's Task 3 SUMMARY.md also states no-release-in-this-phase explicitly. Two documented mitigations against accidental publish. |
</threat_model>

<verification>
Task-scoped verifications are inside each task. The overall plan is complete when Task 3's SUMMARY is written and Ashley's sign-off is transcribed.
</verification>

<success_criteria>
- Full green-tree gate green across `viewmodel-shell`, `viewmodel-shell-dotnet/Tests`, every `demo/**/*.Tests.csproj`, and `parity/run.ts`.
- AA-contrast hand-check documented across default + 12 themes for every new fg/bg pair.
- Ashley's visual sign-off recorded.
- Requirement-to-artifact cross-check complete for COMP-01..04.
- No release, no version bump - batch-then-ship LOCKED.
- Phase 24 is unblocked; explicit readiness statement in SUMMARY.
</success_criteria>

<output>
`.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-09-SUMMARY.md` is the phase closeout artifact.
</output>
