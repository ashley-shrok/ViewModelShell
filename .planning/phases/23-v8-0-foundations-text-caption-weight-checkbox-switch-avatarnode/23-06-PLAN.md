---
phase: 23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode
plan: 06
type: execute
wave: 2
depends_on: ["23-05"]
files_modified:
  - AGENTS.md
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "`AGENTS.md` contains a new dedicated section titled 'Route B composite-nodes layer' under 'Conventions for evolving the framework'."
    - "The section states the Route A / Route B split: Route A = primitive axes + open composition (what VMS ships today); Route B = pre-made composite recipes with typed slots (what v8.0.0 adds)."
    - "The section states the governance rule (a shape earns a composite when the best-effort with today's primitives is a 'pretty bad approximation'; bar is VISUAL; served tasting confirms), copy-consistent with `.planning/design/composite-nodes-layer.md` §2."
    - "The section names the typed-slots pattern: every composite is `{ leading?, primary/heading, secondary/description?, meta?, trailing?, tone?, state? }`-shaped; each slot accepts a full ViewNode subtree; the recipe owns layout/typography/spacing."
    - "The section names the two failure modes to guard against (bloated grab-bag and too-rigid recipe) with their mitigations."
    - "The section names the precedent (MUI ListItemText, Ant List.Item.Meta, Chakra Card composites, Phoenix LiveView function-component slots, Blazor Razor typed content) — every surveyed server-driven peer ships this layer."
    - "The section states that recipes coexist with primitives — consumers with a shape the framework didn't foresee still drop to primitives and compose; recipes NEVER deprecate primitives."
    - "The section points to `.planning/design/composite-nodes-layer.md` as the design of record for the milestone."
    - "The section is INITIAL — it explicitly notes that the 'current recipe inventory' table will grow as Phases 24-26 land, but is NOT populated in this initial version (Phase 23 lands the frame, not the inventory)."
  artifacts:
    - path: "AGENTS.md"
      provides: "New 'Route B composite-nodes layer' section under 'Conventions for evolving the framework'."
      contains: "Route B composite-nodes layer"
  key_links:
    - from: "AGENTS.md"
      to: ".planning/design/composite-nodes-layer.md"
      via: "the section cites the design doc as design of record"
      pattern: "composite-nodes-layer.md"
---

<objective>
Add a **new AGENTS.md governance section** titled "Route B composite-nodes layer" under the existing "Conventions for evolving the framework" section. This canonicalizes the earn-a-composite rule + typed-slots pattern as maintainer policy so the "when does a shape earn a composite?" call stays consistent for every future proposal (v8.0.0 and beyond).

Purpose: AGENTS.md is the maintainer's canonical policy doc — every rule that "must stay consistent across contributors" lives here (per the existing "Working Agreement", "Conventions for evolving the framework", "Design system" sections). The Route B governance rule is a permanent policy addition, not phase-scoped. Established in the tasting served 2026-07-29 (CONTEXT.md §7).

Output: One new section in AGENTS.md. Grows as Phase 24-26 land the recipe inventory; the initial version establishes the frame only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md
@.planning/design/composite-nodes-layer.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Route B composite-nodes layer" section to AGENTS.md</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md ("Conventions for evolving the framework" section — read the full section, identify a natural insertion point AFTER the existing "Don't add features the framework doesn't have a clean place for" clause and BEFORE the "Non-blocking dispatch" clause; also read the "Design system" section to understand the tone + section-header format the file uses)
    - .planning/design/composite-nodes-layer.md §2 (the governance rule text — copy-consistent target)
    - .planning/design/composite-nodes-layer.md §3 (the typed-slots pattern definition)
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md §7 (LOCKED — the AGENTS.md section's content is enumerated here)
  </read_first>
  <action>
    Read AGENTS.md and locate the "Conventions for evolving the framework" section header. Identify the natural insertion point: AFTER the existing bullet "Don't add features the framework doesn't have a clean place for. When a request would require a workaround, that's usually a signal that the framework needs a new primitive — ask before patching around it." and BEFORE the "🚨 Survey by CAPABILITY CATEGORY proactively..." bullet.

    Insert a NEW dedicated subsection with header format matching the rest of the file. Use `---` separators before and after (per the AGENTS.md convention of `---` between major topic blocks). Title: `### Route B composite-nodes layer (v8.0.0)`.

    Body (copy-consistent with `.planning/design/composite-nodes-layer.md` §2-§3 — the design doc from plan 23-05 is the source of truth):

    ```markdown
    ### Route B composite-nodes layer (v8.0.0)

    VMS ships two parallel tracks and both coexist.

    - **Route A — primitives + axes.** Section/TextNode/Button/Field/… plus closed enums (tone/emphasis/size/state/layout/density). Consumers with shape-variance the framework didn't foresee drop to primitives and compose. This is what VMS shipped through v7.0.
    - **Route B — pre-made composite recipes with typed slots.** For common web shapes with shared visual convention (list rows, chat messages, alerts, empty states, user rows, key-value details, timeline entries, settings toggles, dismissable chip clusters), the framework ships a NAMED recipe with typed slots and closed-enum variance axes. The recipe owns layout/typography/spacing; apps own the content. This is what v8.0.0 adds.

    **Recipes never deprecate primitives.** A consumer with an unforeseen shape still drops to primitives; the recipes coexist with the axes, they don't replace them.

    **The governance rule — earn a composite.** A shape earns a composite when the best-effort with today's primitives is a "pretty bad approximation" of the common shape. **The bar is visual** — the after has to look right; the before has to look wrong enough to justify the primitive earning a promotion. Judgment per shape, eyeballed against a served tasting BEFORE the composite earns its place in the wire. Every proposed composite requires a before/after tasting served for review (Ashley canonicalized this in the 2026-07-29 tasting that gated the v8.0.0 milestone).

    **Two failure modes to guard against:**
    - **Bloated grab-bag** — recipes that don't earn their weight (mitigation: high bar for new recipes; served tasting non-negotiable).
    - **Too-rigid recipe** — a recipe that boxes in the consumer (mitigation: typed slots stay unconstrained-content ViewNode subtrees; axes stay closed enums so variance is expressed by the axis, not by escape-hatch fields).

    **The typed-slots pattern (uniform across every composite):** typed slots with semantic names — `leading?`, `primary` (or `heading`), `secondary?` (or `description?`), `meta?`, `trailing?` — plus axes `tone?`, `state?`. Each slot accepts a full ViewNode subtree (any node the framework ships: text, badge, icon, button, avatar, checkbox). The recipe owns DOM layout, typography (which slot uses which text style/weight), and spacing (gap, padding, alignment). Variance is expressed through CLOSED-ENUM axes, not raw CSS or free-form fields.

    **The precedent — every surveyed server-driven peer ships this layer.** MUI `ListItemText`, Ant `List.Item.Meta`, Chakra `Card` composites, Phoenix LiveView function-component slots, Blazor Razor typed content. VMS was the outlier in shipping only Route A; Route B closes the gap.

    **Design of record:** `.planning/design/composite-nodes-layer.md`. Read it before proposing any addition to the Route B layer.

    **Current recipe inventory:** [to be populated as Phases 24-26 land the recipes; initial version omits the table by design so it doesn't drift ahead of what actually ships].
    ```

    Add a `---` separator after the section per the AGENTS.md convention.

    Ensure the insertion:
    - Preserves EVERY existing bullet in "Conventions for evolving the framework" (no accidental delete).
    - Uses the same tone / voice / formatting as the neighboring sections.
    - Does NOT include a recipe inventory table (Phase 23 lands the frame; the inventory grows).
    - Cites the design doc from plan 23-05 by exact path.
  </action>
  <verify>
    <automated>grep -c 'Route B composite-nodes layer' AGENTS.md &amp;&amp; grep -c 'typed slots' AGENTS.md &amp;&amp; grep -c 'earn a composite' AGENTS.md &amp;&amp; grep -c 'composite-nodes-layer.md' AGENTS.md &amp;&amp; grep -c 'Don.t add features the framework doesn.t have a clean place' AGENTS.md &amp;&amp; grep -c 'Non-blocking dispatch' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Route B composite-nodes layer' AGENTS.md` returns ≥ 1 (section header present).
    - `grep -c 'typed slots' AGENTS.md` returns ≥ 1 (pattern named).
    - `grep -c 'earn a composite' AGENTS.md` returns ≥ 1 (rule named).
    - `grep -c 'pretty bad approximation' AGENTS.md` returns ≥ 1 (verbatim Ashley phrase).
    - `grep -c 'composite-nodes-layer.md' AGENTS.md` returns ≥ 1 (design doc cited by exact filename).
    - `grep -c 'Non-blocking dispatch' AGENTS.md` still returns ≥ 1 (existing content preserved — the "Non-blocking dispatch" clause is a well-known anchor that must survive the insert).
    - `grep -c 'Don.t add features the framework doesn.t have a clean place' AGENTS.md` still returns ≥ 1 (the pre-existing clause immediately before the insertion point is intact).
    - `grep -c 'MUI \`ListItemText\`' AGENTS.md` returns ≥ 1 (precedent named).
    - `grep -c 'Route A' AGENTS.md` returns ≥ 1 (both tracks named).
    - No recipe inventory table in the new section (deferred to Phase 24-26 land — verified by reading; the section explicitly says "to be populated" and does NOT list any specific composite name in a table format).
  </acceptance_criteria>
  <done>
    AGENTS.md governance section added at the correct insertion point; every existing convention preserved; design doc cited; recipe inventory deferred.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | (none) | AGENTS.md governance section addition | accept | Pure documentation. No code, no wire change, no runtime behavior. Attack surface delta: zero. |
</threat_model>

<verification>
- Grep-based verifications listed in acceptance criteria.
- Read the surrounding "Conventions" section after the edit to confirm no existing bullet was accidentally deleted or reformatted.
</verification>

<success_criteria>
- AGENTS.md has a new "Route B composite-nodes layer" section under "Conventions for evolving the framework".
- The section states Route A/B split, governance rule, typed-slots pattern, two failure modes, precedent, and coexistence-with-primitives.
- Design doc is cited by exact path.
- Recipe inventory table intentionally omitted (grows in Phase 24-26).
- Existing content preserved.
</success_criteria>

<output>
Create `.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-06-SUMMARY.md` recording the insertion point + what content was added + confirmation that no existing text was lost.
</output>
