---
phase: 23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/browser.ts
  - viewmodel-shell/styles/default.css
  - viewmodel-shell-dotnet/ViewModels.cs
  - viewmodel-shell/test/text-caption.test.ts
  - viewmodel-shell-dotnet/Tests/TextCaptionSerializationTests.cs
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "A `TextNode` with `style: \"caption\"` renders `<span class=\"vms-text vms-text--caption\">` in the browser adapter."
    - "The .NET `TextStyle` enum contains a `Caption` member that serializes to the wire string `\"caption\"`."
    - "CSS rule `.vms-text--caption` sets `font-size: var(--vms-text-xs)`, `color: var(--vms-text-muted)`, `opacity: 0.85`, `line-height: 1.4`."
    - "A jsdom render test asserts the emitted class + computed font-size, and fails when the switch-arm class emission is removed (mutation-tested FAIL-before / PASS-after)."
    - "A .NET serialization test asserts `TextStyle.Caption` round-trips as `\"style\":\"caption\"` on the wire."
    - "AA-contrast for caption text against `--vms-bg` and `--vms-surface` is ≥ 4.5:1 on default + all 12 themes (hand-checked)."
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "`TextNode.style` closed union grows to include `\"caption\"`."
      contains: "\"caption\""
    - path: "viewmodel-shell/src/browser.ts"
      provides: "Existing `private text()` renderer emits `.vms-text--caption` for `style:\"caption\"` (no code change needed — the `vms-text--${n.style}` template covers it)."
    - path: "viewmodel-shell/styles/default.css"
      provides: "`.vms-text--caption` CSS rule."
      contains: ".vms-text--caption"
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "`TextStyle` enum grows to include `Caption`."
      contains: "Caption"
    - path: "viewmodel-shell/test/text-caption.test.ts"
      provides: "jsdom render test for the caption tier."
    - path: "viewmodel-shell-dotnet/Tests/TextCaptionSerializationTests.cs"
      provides: ".NET serialization test for `TextStyle.Caption`."
  key_links:
    - from: "viewmodel-shell/src/index.ts"
      to: "viewmodel-shell-dotnet/ViewModels.cs"
      via: "byte-identical closed-union grow"
      pattern: "caption"
    - from: "viewmodel-shell/src/browser.ts (private text())"
      to: "viewmodel-shell/styles/default.css"
      via: ".vms-text--caption class emission"
      pattern: "vms-text--\\$\\{n\\.style\\}"
---

<objective>
Add `TextNode.style: "caption"` as a closed-union extension across both backends, ship the CSS rule that makes it render at `var(--vms-text-xs)` with muted+opacity, add jsdom + .NET tests, hand-check AA contrast on default + all 12 themes.

Purpose: The 3rd typographic tier every downstream composite (`ListRowNode.meta[]`, `MessageNode.timestamp`, `TimelineEntryNode.time` in Phase 24-25) consumes for micro-meta lines. Approved via the tasting served 2026-07-29 (CONTEXT.md §1).
Output: `TextNode` closed union grows to 7 members on both backends; `.vms-text--caption` CSS renders correctly at all viewport sizes and passes AA on every shipped theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md
@.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-PATTERNS.md
@AGENTS.md
</context>

<interfaces>
<!-- Pulled from viewmodel-shell/src/index.ts:997-1001 (per PATTERNS.md §1). -->

Current TextNode.style shape (before):
```typescript
style?: "heading" | "subheading" | "body" | "muted" | "strikethrough" | "pre";
tone?: "danger" | "warning" | "success" | "info";
```

Current .NET TextStyle enum (ViewModels.cs:163, before):
```csharp
[JsonConverter(typeof(KebabEnum<TextStyle>))]
public enum TextStyle { Heading, Subheading, Body, Muted, Pre, Strikethrough }
```

Current .vms-text--* CSS pattern (default.css:1123-1128, before):
```css
.vms-text--muted         { color: var(--vms-text-muted); font-size: var(--vms-text-sm); }
.vms-text--strikethrough { color: var(--vms-done-text); text-decoration: line-through; }
```

Renderer emission (browser.ts:3213, unchanged — the template covers new values automatically):
```typescript
el.className = `vms-text${n.style ? ` vms-text--${n.style}` : ""}${n.tone ? ` vms-text--${n.tone}` : ""}`;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend TextNode.style union (TS + .NET) + ship the CSS rule</name>
  <files>viewmodel-shell/src/index.ts, viewmodel-shell/styles/default.css, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell/src/index.ts (read lines 970-1010 for the TextNode interface, extract exact style union at :997 per PATTERNS.md §1)
    - viewmodel-shell/styles/default.css (read lines 1120-1140 for the `.vms-text--*` block per PATTERNS.md §7a)
    - viewmodel-shell-dotnet/ViewModels.cs (read lines 155-175 for the `TextStyle` enum + KebabEnum converter per PATTERNS.md §8a)
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md §1 (Locked decision — caption is a closed-union extension, NOT a new axis)
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-PATTERNS.md §1, §7a, §8a (exact analog patterns)
  </read_first>
  <action>
    In `viewmodel-shell/src/index.ts` at the TextNode `style` union (currently at :997), add `| "caption"` at the end of the union. Update the TSDoc paragraph to mention the new value: "the text-xs muted-opacity tier used by `ListRowNode.meta[]`, `MessageNode.timestamp`, `TimelineEntryNode.time` (Phase 24-25)." Do NOT touch `browser.ts` — the existing `` vms-text--${n.style} `` template at :3213 handles the new value automatically (per PATTERNS.md §4).

    In `viewmodel-shell/styles/default.css`, insert AFTER the existing `.vms-text--pre` rule (near line 1129) and BEFORE the `.vms-text--danger` tone block, the exact rule:
    `.vms-text--caption       { color: var(--vms-text-muted); font-size: var(--vms-text-xs); opacity: 0.85; line-height: 1.4; }`
    (matches CONTEXT.md §1 exactly.)

    In `viewmodel-shell-dotnet/ViewModels.cs`, extend `enum TextStyle` at :163 by appending `Caption` at the END of the enum member list (so `{ Heading, Subheading, Body, Muted, Pre, Strikethrough, Caption }`). Placement at the end preserves ordinal indices for any downstream consumer. The `KebabEnum<TextStyle>` converter naturally emits `"caption"` — no per-member `[EnumMember]` needed for single-word members (per PATTERNS.md §8a note on IconName kebab convention).

    NO wire shape change beyond the closed-union grow — no new fields on TextNode itself.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'caption' src/index.ts &amp;&amp; grep -c '\.vms-text--caption' styles/default.css &amp;&amp; grep -c 'Caption' ../viewmodel-shell-dotnet/ViewModels.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"caption"' viewmodel-shell/src/index.ts` returns ≥ 1 (the new union value).
    - `grep -c '\.vms-text--caption' viewmodel-shell/styles/default.css` returns ≥ 1 (the new CSS rule).
    - `grep -c 'Caption' viewmodel-shell-dotnet/ViewModels.cs` returns ≥ 1 (the new enum member).
    - The CSS rule contains all four properties: `color: var(--vms-text-muted)`, `font-size: var(--vms-text-xs)`, `opacity: 0.85`, `line-height: 1.4`.
    - `cd viewmodel-shell &amp;&amp; npm run build` succeeds (TypeScript compiles).
    - `cd viewmodel-shell-dotnet &amp;&amp; ~/.dotnet/dotnet build --nologo -v minimal` succeeds.
    - `.NET` enum member is at the end of the list (verify by reading — Caption appears after Strikethrough).
    - No change to `browser.ts` (verify by `git diff --stat viewmodel-shell/src/browser.ts` shows 0 files changed).
  </acceptance_criteria>
  <done>
    Union grown on both backends; CSS rule shipped; build green on both sides; no browser.ts change.
  </done>
</task>

<task type="auto">
  <name>Task 2: jsdom render test + .NET serialization test + AA-contrast hand-check</name>
  <files>viewmodel-shell/test/text-caption.test.ts, viewmodel-shell-dotnet/Tests/TextCaptionSerializationTests.cs</files>
  <read_first>
    - viewmodel-shell/test/icon-render.test.ts (read lines 20-80 for the jsdom setup + class assertion pattern per PATTERNS.md §10)
    - viewmodel-shell-dotnet/Tests/IconNodeSerializationTests.cs (read lines 20-70 for the .NET serialization test pattern per PATTERNS.md §10)
    - viewmodel-shell/styles/default.css (need :root token values for the AA hand-check — grep for `--vms-text-muted` in default + every file under `viewmodel-shell/styles/themes/`)
    - .planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-CONTEXT.md §Specific ideas → AA-contrast checklist (caption pair definition)
  </read_first>
  <action>
    Create `viewmodel-shell/test/text-caption.test.ts` following the `icon-render.test.ts` structure. Use `describe("TextNode style:caption (COMP-01)", () => { ... })`. Write these tests:
    (a) `it("renders <span class='vms-text vms-text--caption'>", ...)` — call `render({ type: "text", value: "Meta line", style: "caption" })`, assert `container.querySelector("span.vms-text--caption")` is not null AND its className equals `"vms-text vms-text--caption"` exactly.
    (b) `it("computed font-size equals --vms-text-xs", ...)` — use `getComputedStyle(el).getPropertyValue("font-size")` and assert it non-empty; ALSO assert `getComputedStyle(el).getPropertyValue("opacity")` returns `"0.85"` and `getComputedStyle(el).getPropertyValue("line-height")` returns a value corresponding to 1.4.
    (c) Mutation-test proof — a comment block at the end names the exact revert (removing `| "caption"` from the union OR removing the CSS rule) that makes (a) and (b) fail respectively.

    Create `viewmodel-shell-dotnet/Tests/TextCaptionSerializationTests.cs` following `IconNodeSerializationTests.cs`. Include `global using Xunit;` if not already in `Tests/GlobalUsings.cs` (per AGENTS.md gotcha #7). Write these tests:
    (a) `TextNode_CaptionStyle_SerializesAsKebabCase()` — `new TextNode("Meta line", Style: TextStyle.Caption)` serialized as `ViewNode` (polymorphic) contains `"style":"caption"`.
    (b) `TextStyle_Caption_EnumMemberExists()` — `Assert.True(Enum.IsDefined(typeof(TextStyle), TextStyle.Caption))`.

    Hand-check AA contrast for the caption text pair AND record results in a comment at the top of `text-caption.test.ts`:
    - Compute `--vms-text-muted` color (opacity 0.85 applied) against `--vms-bg` and `--vms-surface` on `default` + all 12 theme files under `viewmodel-shell/styles/themes/*.css`.
    - Target: ≥ 4.5:1 (WCAG AA normal — caption is text-xs so normal-text threshold applies).
    - If any pair fails, deepen the failing tone via `color-mix(in srgb, var(--vms-text-muted) N%, #000)` per the shipped v3.5.0 pattern (documented in AGENTS.md). Record theme-by-theme in the comment.
    - If all pass, the comment records "AA hand-check green on default + 12 themes @ text-xs muted × opacity 0.85".
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npx vitest run test/text-caption.test.ts &amp;&amp; cd ../viewmodel-shell-dotnet/Tests &amp;&amp; ~/.dotnet/dotnet test --filter 'TextCaptionSerializationTests'</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/test/text-caption.test.ts` exists, has ≥ 2 `it(...)` blocks, both green when the code from Task 1 is in place.
    - Removing `| "caption"` from the union in `index.ts` makes at least one vitest assertion FAIL (mutation-tested; verify by temporarily commenting out and running the test, then reverting).
    - `viewmodel-shell-dotnet/Tests/TextCaptionSerializationTests.cs` exists with the two `[Fact]` methods described.
    - `~/.dotnet/dotnet test viewmodel-shell-dotnet/Tests` filtered to the new tests: green.
    - The comment block at the top of `text-caption.test.ts` names each of the 13 themes (default + 12) with a per-theme AA verdict ≥ 4.5:1 or documented deepening.
    - `grep -c 'AA hand-check' viewmodel-shell/test/text-caption.test.ts` ≥ 1.
  </acceptance_criteria>
  <done>
    Both tests exist and pass; mutation-verified; AA hand-check recorded in-file; green-tree gate ready (parity extension is a separate plan — 23-08 for CHANGELOG, 23-09 for full gate).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This addition changes no trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | (none) | `TextNode.style: "caption"` is a rendering-only class-modifier extension | accept | No input parsing, no external data flow, no auth interaction, no new URL, no user-supplied content channel. The wire adds one enum value that renders as a CSS class; the class-name is fixed by the framework. Attack surface delta: zero. |

Explicit no-surface-change rationale: caption is a closed-enum value on a text-role field; a hostile server sending `style:"caption"` is exactly what the framework wants (renders as text-xs muted). A hostile client CANNOT supply this — clients round-trip the state opaquely and the render is server-driven.
</threat_model>

<verification>
Before this plan's commit lands, the FULL green-tree gate runs (per AGENTS.md Working Agreement + CONTEXT.md fleet-adoption discipline). Task-scoped verifications:
- `cd viewmodel-shell &amp;&amp; npm run build` — TypeScript compiles.
- `cd viewmodel-shell &amp;&amp; npx vitest run test/text-caption.test.ts` — new render test green.
- `cd viewmodel-shell-dotnet/Tests &amp;&amp; ~/.dotnet/dotnet test --filter 'TextCaptionSerializationTests'` — new .NET test green.

FULL green-tree gate (parity + demo type-check + AA-contrast script + Showcase visual) is exercised by 23-09.
</verification>

<success_criteria>
- `TextNode.style` includes `"caption"` on both backends, byte-identical.
- `.vms-text--caption` CSS emits exactly the four properties from CONTEXT.md §1.
- Vitest + .NET unit tests exist and pass (mutation-tested for the vitest assertions).
- AA hand-check documented in-file for default + all 12 themes @ text-xs muted × opacity 0.85.
- No new field on TextNode; no browser.ts change; wire protocol token stays `viewmodel-shell/1.0`.
</success_criteria>

<output>
Create `.planning/phases/23-v8-0-foundations-text-caption-weight-checkbox-switch-avatarnode/23-01-SUMMARY.md` when done, recording the four artifacts touched, the AA hand-check results, and the mutation-test evidence.
</output>
