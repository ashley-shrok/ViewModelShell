---
phase: 33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr
plan: "06"
type: execute
wave: 4
depends_on:
  - 33-04
  - 33-05
files_modified: []
autonomous: false
requirements:
  - REQ-CF2-13
  - REQ-CF2-14

must_haves:
  truths:
    - "All 11 green-tree gate commands exit 0 before any commit, push, publish, or tag"
    - "Verification page served on the tailnet at http://100.113.23.63:<port>/ using the REAL built bundle (viewmodel-shell/dist/browser.js) + shipped CSS"
    - "Page contains 3 sections: (i) all 5 value-kinds always-visible input + type-and-enter, (ii) popover full flow including operator picker / add-rule / joiner / Apply / Clear / outside-click / Escape, (iii) icon-state grammar showing all 3 states"
    - "Ashley exercises the page in a real browser and provides 'taste ok' (or equivalent) confirmation BEFORE any publish / push / tag / announce"
    - "Popover viewport-edge clamping is verified on the served page — a near-edge column filter button must not clip the popover"
  artifacts:
    - path: "viewmodel-shell/dist/browser.js"
      provides: "Rebuilt bundle containing Phase 33 adapter changes"
  key_links:
    - from: "Verification page in-page reducer"
      to: "viewmodel-shell/dist/browser.js (BrowserAdapter)"
      via: "real bundle import, no server.js import (gotcha #13)"
      pattern: "BrowserAdapter"
---

<objective>
Run the full green-tree gate (11 commands), rebuild the bundle, serve a real-bundle verification page on the tailnet, and wait for Ashley's real-browser exercise confirmation before Plan 07 can proceed.

Purpose: REQ-CF2-13 (green-tree gate) + REQ-CF2-14 (real-browser exercise gate). Both are hard prerequisites for the release ritual in Plan 07. This plan has `autonomous: false` because it waits for Ashley's human confirmation.

Output: Green gate + Ashley's "taste ok" confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-SPEC.md
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full green-tree gate (11 commands)</name>
  <files></files>
  <read_first>
    - AGENTS.md "Working agreement for agents" — the NEVER PUBLISH OR PUSH ANYTHING BROKEN rule with the exact 11-command gate. Read the exact set of commands before running to ensure no command is missed.
    - .planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/VERIFICATION.md — Phase 32's gate run as precedent for format.
  </read_first>
  <action>
    Run the full 11-command green-tree gate per AGENTS.md "NEVER PUBLISH OR PUSH ANYTHING BROKEN". The 11 commands in order:

    1. `cd /home/thenasty/ViewModelShell/viewmodel-shell && npx vitest run` — all TS tests (includes filter-adapter.test.ts + column-filter.test.ts + adapter.test.ts + all others).
    2. `cd /home/thenasty/ViewModelShell/viewmodel-shell && npm run check:test-types` — type-check test files.
    3. `cd /home/thenasty/ViewModelShell/viewmodel-shell && npm run check:core-globals` — AGNOSTIC-03 guard (src/index.ts references no platform globals).
    4. `cd /home/thenasty/ViewModelShell/viewmodel-shell && npm run check:demo-types` — all TS demo strict tschecks.
    5. `cd /home/thenasty/ViewModelShell && ~/.dotnet/dotnet test viewmodel-shell-dotnet/Tests/AshleyShrok.ViewModelShell.Tests.csproj` — framework .NET tests.
    6. `cd /home/thenasty/ViewModelShell && for p in $(find demo -name "*.Tests.csproj"); do ~/.dotnet/dotnet test "$p" || exit 1; done` — all demo .NET tests.
    7. `cd /home/thenasty/ViewModelShell && export PATH="$HOME/.dotnet:$PATH" && bun run parity/run.ts` — cross-backend parity suite.
    8. `cd /home/thenasty/ViewModelShell/viewmodel-shell && npm run check:no-demo-style 2>/dev/null || true` — if this check exists, run it; otherwise skip and note.
    9. `cd /home/thenasty/ViewModelShell/viewmodel-shell && npm run build` — build the full bundle (produces dist/browser.js that the verification page uses).
    10. Verify `grep -nE "\b(filterable|filterValue|filterBinds|filterAction)\b" viewmodel-shell/src/index.ts` returns zero matches (old wire removal verification from REQ-CF2-07).
    11. Verify `grep -nE "\b(Filterable|FilterValue|FilterBinds|FilterAction)\b" viewmodel-shell-dotnet/ViewModels.cs` returns zero matches.

    If ANY command fails:
    - STOP immediately.
    - Surface the failure output to the operator.
    - Do NOT proceed to Task 2 or Task 3.
    - Fix the root cause, then re-run the full gate from command 1.
    - "Pre-existing" or "unrelated" failures are NOT exceptions — fix or get explicit waiver before proceeding.
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell/viewmodel-shell && npm run check:test-types && npm run check:core-globals && npm run check:demo-types && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 11 gate commands exit 0. `npm run build` completed successfully and `viewmodel-shell/dist/browser.js` exists with today's timestamp. The gate results are documented in SUMMARY.md (each command + exit code).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build and serve real-bundle verification page on tailnet</name>
  <files></files>
  <read_first>
    - AGENTS.md "How to build the run-through page" — the canonical pattern: serve the REAL bundle (dist/browser.js) + shipped CSS + in-page reducer + fake fetch. The in-page reducer must inline any needed helpers (gotcha #13: NEVER import from server.js in a browser page).
    - AGENTS.md "SECURE-CONTEXT features" — the filter UI uses no clipboard or WebCrypto API, so plain http:// over the tailnet is fine for this verification page.
    - AGENTS.md gotcha #13 — if the in-page reducer needs `matchesFilter`, it must be inlined in the HTML page (not imported from server.js). A simple inline version of `matchesFilter` or a reduced stub that works for the demonstration is acceptable for the verification page.
    - .planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-SPEC.md REQ-CF2-14 — the 3 required table sections for the verification page.
    - .planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-CONTEXT.md D-03 — viewport-edge clamping must be verified; use a table positioned near the right edge of the page.
  </read_first>
  <action>
    Build a self-contained static verification page. Create a scratch directory at `/tmp/vms-v10-verify/` with:

    - `browser.js` — copy of `viewmodel-shell/dist/browser.js` (already built in Task 1).
    - `styles.css` — copy of `viewmodel-shell/styles/default.css`.
    - `theme-dark-purple.css` — copy of `viewmodel-shell/styles/themes/dark-purple.css` (for the theme-swap check).
    - `index.html` — the verification page (see structure below).

    **index.html structure:**
    - Head: `<link id="theme-css" rel="stylesheet" href="./styles.css">` + a `<select id="theme-switch">` for light/dark toggle.
    - Import map resolving `viewmodel-shell` subpath to `./browser.js` (the page imports `BrowserAdapter` from `viewmodel-shell/browser` but the dist file may be a self-contained ES module — read the actual dist/browser.js first to determine the correct import pattern for a static page, whether it uses import maps or is a global export).
    - Per AGENTS.md gotcha #13: import ONLY from `./browser.js` (the dist bundle) — NEVER reference the server subpath.

    **Page sections (REQ-CF2-14):**

    **Section i — All 5 value-kinds, always-visible input + type-and-enter:**
    One table with 5 columns (text, number, date, fixed-set, yes-no). In-page reducer: on any action, if the action carries a `filterDescriptors` state key, filter the rows and re-render. The filter reducer should be a simple inline function — it does NOT need to be byte-parallel with the server's FilterHelper; it just needs to demonstrate the UI grammar visually. Use a small in-page stub of matchesFilter logic (10-15 lines is fine).
    Sample rows: 6-8 rows with variety across all 5 column types (e.g., Name=text, Amount=number, Date=date, Status=fixed-set with options open/closed/pending, Active=yes-no).

    **Section ii — Popover full flow:**
    A second table (text columns) with a filter column in a constrained width area. Exercise: open popover → change operator → type value → Add Rule → joiner toggle → Apply. Also: open → type → click outside (should discard). Open → Escape (should discard). Open → Clear (should commit empty). The page's in-page reducer must handle the Apply/Clear state updates and re-render the table.

    **Section iii — Icon state grammar:**
    Three static renders side-by-side (three separate small tables OR three filter cells in one table):
    - Column A: no descriptor in state → shows filter-slash glyph.
    - Column B: simple contains descriptor → shows plain funnel.
    - Column C: escalated descriptor (is-empty rule) → shows plain funnel + dot.

    **Near-viewport-edge column (D-03 verification requirement):**
    Add a fourth table positioned at the far right of the page (use `margin-left: calc(100vw - 200px)` or similar so the rightmost filter button is within 50px of the viewport edge). Open that column's popover and verify it clamps leftward (does not clip off-screen). Ashley must exercise this during the real-browser check.

    **Links section:** Include links to the running HelpDesk demo (if running locally) and the Showcase demo so Ashley can exercise migration in situ. Note: if those demos aren't running, just note "start them separately" in the page.

    Serve via: `python3 -m http.server 8099 --bind 100.113.23.63 --directory /tmp/vms-v10-verify/` (run in background).

    Print the URL to the operator: `http://100.113.23.63:8099/`.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://100.113.23.63:8099/ 2>/dev/null || echo "server not yet started"</automated>
  </verify>
  <done>
    The verification page is served at `http://100.113.23.63:8099/` and returns HTTP 200. The page loads the real `browser.js` bundle and shipped CSS. All 4 table sections are visible. The near-viewport-edge table is positioned correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 33 adapter implementation is built and gated (Plans 01-05 complete, green-tree gate passed, verification page served). The page at http://100.113.23.63:8099/ contains:
    - Section i: 5-column table covering all value-kinds with always-visible filter inputs + icon buttons.
    - Section ii: Popover full flow (operator picker, add-rule, joiner, Apply, Clear, outside-click discard, Escape discard).
    - Section iii: Icon state grammar (filter-slash / plain funnel / funnel+dot) side by side.
    - Near-viewport-edge table for popover clamping verification.
    The bundle is the REAL dist/browser.js (not a headless approximation).
  </what-built>
  <how-to-verify>
    Open http://100.113.23.63:8099/ in a real browser (Chrome or Firefox).

    **Section i — all 5 kinds, type-and-enter:**
    1. In the text column filter input, type "Alice" and press Enter. The table should filter to matching rows.
    2. Clear by deleting the input and pressing Enter. All rows should return.
    3. Verify filter-slash glyph shows when column has no filter; plain funnel when it does.

    **Section ii — Popover flow:**
    4. Click the filter-icon button on the text column. The popover should open above/below the button, fully visible (not clipped).
    5. Change the operator in the select from "contains" to "starts-with". The value input should update.
    6. Type a value. Click "Add rule". A second rule row should appear. The AND/OR joiner toggle should become visible.
    7. Click Apply. The popover closes; the inline area shows the read-only summary.
    8. Re-open the popover. Click Clear. The popover closes; the inline area reverts to an editable input.
    9. Open the popover. Change the operator. Click OUTSIDE the popover. The popover closes and the operator change is DISCARDED (the table did not change).
    10. Open the popover. Press Escape. Same discard behavior; focus returns to the filter button.

    **Section iii — Icon states:**
    11. Confirm all 3 icon states are visually distinct: slash-through funnel (empty), plain funnel (simple), plain funnel + small dot (escalated).

    **Near-viewport-edge clamping:**
    12. Click the filter button on the far-right column. The popover should appear on-screen (clamped leftward if it would overflow the right edge). It should NOT clip off the right side of the viewport.

    **Theme swap:**
    13. Switch the theme dropdown to "Dark". The page should switch to the dark-purple theme; all filter UI elements should remain styled correctly.
  </how-to-verify>
  <resume-signal>
    Type "taste ok" to proceed to the release ritual, or describe any issues found. Ashley's confirmation is required before Plan 07 begins — commit, push, publish, tag, and announce cannot happen before this checkpoint.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification page served over http:// on tailnet | No secure context needed; no clipboard/WebCrypto. Tailnet network-isolates the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06-01 | Information Disclosure | Verification page served over plain http:// on tailnet IP | accept | Tailnet provides network-level isolation; no sensitive data in the verification page |
| T-33-06-02 | Tampering | Verification page imports server.js bundle accidentally | mitigate | Verify import sources before serving: grep for "server.js" in index.html before the operator visits |
| T-33-06-SC | Tampering | npm/pip/cargo installs | accept | No new installs; dist/browser.js already built by Task 1 |
</threat_model>

<verification>
1. All 11 gate commands exit 0 (documented in SUMMARY).
2. `http://100.113.23.63:8099/` returns HTTP 200.
3. `grep -c "server.js" /tmp/vms-v10-verify/index.html` returns 0 (gotcha #13 — no server bundle imported).
4. Ashley provides "taste ok" (or equivalent) before Plan 07 begins.
</verification>

<success_criteria>
Plan 06 complete when:
- Full green-tree gate passed (all 11 commands exit 0).
- Verification page served and accessible on the tailnet.
- Ashley has exercised the page in a real browser and confirmed "taste ok".
- No commit / push / publish / tag / announce has happened before the checkpoint passes.
</success_criteria>

<output>
Create `.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-06-SUMMARY.md` when done. Include: gate command outputs, verification page URL, and Ashley's confirmation quote.
</output>
