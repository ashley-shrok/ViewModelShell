---
phase: 33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr
plan: "05"
type: execute
wave: 3
depends_on:
  - 33-02
  - 33-03
files_modified:
  - MIGRATION.md
  - AGENTS.md
  - CHANGELOG.md
autonomous: true
requirements:
  - REQ-CF2-11

must_haves:
  truths:
    - "MIGRATION.md has a new section for v10.0.0 describing the 8 removed wire fields + their new-shape replacements + HelpDesk before/after code snippet"
    - "AGENTS.md has a new section describing the typed column-filter primitive (wire shape + UI grammar + popover semantics + icon state grammar)"
    - "CHANGELOG.md `## Unreleased` section is promoted to a versioned `## 10.0.0 — 2026-08-18` header with Phase 33 additions appended alongside Phase 32's staged content"
  artifacts:
    - path: "MIGRATION.md"
      provides: "v10.0.0 section with wire replacement guidance + HelpDesk before/after snippet"
      contains: "10.0.0"
    - path: "AGENTS.md"
      provides: "Typed column-filter primitive section (wire + UI)"
      contains: "FilterSpec"
    - path: "CHANGELOG.md"
      provides: "Promoted versioned header replacing ## Unreleased"
      contains: "10.0.0"
  key_links:
    - from: "MIGRATION.md v10.0.0 section"
      to: "demo/HelpDesk/AspNetCore/AgentController.cs"
      via: "Before/after diff sourced from the Plan 03 migration"
      pattern: "FilterSpec"
---

<objective>
Write the three doc artifacts for the v10.0.0 release: MIGRATION.md wire-replacement section, AGENTS.md typed-filter primitive section, CHANGELOG.md promotion of `## Unreleased` to the versioned header.

Purpose: REQ-CF2-11 — consumers need a clear migration path; the AGENTS.md section teaches agents and developers the new primitive; the CHANGELOG promotion enables the release ritual in Plan 07.

Output: Three files updated with v10.0.0 doc content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-SPEC.md
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-CONTEXT.md
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-02-SUMMARY.md
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-03-SUMMARY.md

<interfaces>
<!-- MIGRATION.md before/after shape (D-05): -->
// Before (old wire — HelpDesk AgentController):
// TableColumn: Filterable: true, FilterValue: state.TitleFilter.Length > 0 ? state.TitleFilter : null
// TableNode: FilterBinds: { ["title"] = "titleFilter" }, FilterAction: new ActionDescriptor("filter-text")
// Action handler: custom contains logic on state.TitleFilter

// After (new wire — HelpDesk AgentController after Plan 03 migration):
// TableColumn: Filter: new FilterSpec("text")
// TableNode: FilterDescriptorBinds: { ["title"] = "filterDescriptors.title" }
// Action handler: FilterHelper.MatchesFilter(descriptor, ticket.Title, ticket.Title, "text")

<!-- CHANGELOG.md current ## Unreleased content (Phase 32 staged): -->
// Read CHANGELOG.md to find the current ## Unreleased section content
// It contains Phase 32's additive wire additions (FilterSpec, FilterDescriptor, matchesFilter, etc.)
// Phase 33 appends its own bullets: adapter UI, icon grammar, filter-slash glyph, demo migration, old wire removal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MIGRATION.md v10.0.0 section</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md — read the full file (or at least the last 2 major sections) to understand the format of existing migration entries (e.g., the v8.0.0 EmptyStateNode section is the closest precedent for a breaking wire change). Follow the same section heading style and prose structure.
    - demo/HelpDesk/AspNetCore/AgentController.cs — read the file as migrated by Plan 03; extract the actual before/after diff for the MIGRATION.md code snippet. The before/after is the REAL file content, not a hypothetical.
    - .planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-CONTEXT.md D-05 — the exact shape of the MIGRATION.md reference example.
    - .planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-03-SUMMARY.md — confirm the exact .NET migration shape that was implemented.
  </read_first>
  <action>
    Append a new section to MIGRATION.md for the v10.0.0 release. The section must include:

    **Heading:** `## Migrating to v10.0.0`

    **Summary paragraph:** Explain that v10.0.0 replaces the old `filterable`/`filterValue`/`filterBinds`/`filterAction` wire fields with the new typed `filter`/`filterDescriptorBinds` shape. This is a breaking change — apps using the old wire must update their column declarations and action handlers. No compatibility shim; upgrade in place.

    **The 8 removed fields (as a table or bullet list):**
    - TypeScript (`viewmodel-shell/src/index.ts`): `TableColumn.filterable: boolean`, `TableColumn.filterValue: string`, `TableNode.filterBinds: Record<string, string>`, `TableNode.filterAction: ActionEvent`
    - .NET (`AshleyShrok.ViewModelShell`): `TableColumn.Filterable`, `TableColumn.FilterValue`, `TableNode.FilterBinds`, `TableNode.FilterAction`

    **Replacement fields:**
    - Column: `filter: FilterSpec` (TS) / `Filter: FilterSpec` (.NET) — declares the column's value-kind and options.
    - Table: `filterDescriptorBinds: Record<string, string>` (TS) / `FilterDescriptorBinds: Dictionary<string,string>` (.NET) — maps column keys to state bind paths where `FilterDescriptor` instances live.

    **Before/after C# code snippet** (from HelpDesk AgentController.cs migration — use the REAL code from the file, not a synthetic example):
    The "before" block shows the old Filterable/FilterValue/FilterBinds/FilterAction + the old manual filter logic in the action handler.
    The "after" block shows the new FilterSpec/FilterDescriptorBinds + FilterHelper.MatchesFilter.

    **TypeScript equivalent** (brief, showing the HelpDesk-bun or Showcase pattern from Plan 02 migration):
    Before: `filterable: true, filterValue: state.titleFilter` + `filterBinds: { title: "titleFilter" }, filterAction: { name: "filter-text" }`
    After: `filter: { kind: "text" }` + `filterDescriptorBinds: { title: "filterDescriptors.title" }` + `matchesFilter(descriptor, row.title, row.title, "text")`

    **Consumer note:** "Apps must upgrade on v10.0.0 — the old wire fields are gone. No compatibility layer. The framework's `matchesFilter` / `FilterHelper.MatchesFilter` reference truth function handles all filter evaluation; consumers no longer write bespoke filter loops."

    Follow MIGRATION.md's existing append-only discipline: the new section goes at the END (most recent first, or after the last existing section — follow the existing order).
  </action>
  <verify>
    <automated>grep -c "10.0.0" /home/thenasty/ViewModelShell/MIGRATION.md && grep -c "FilterSpec" /home/thenasty/ViewModelShell/MIGRATION.md</automated>
  </verify>
  <done>
    `MIGRATION.md` contains a new section `## Migrating to v10.0.0` with the 8 removed fields listed, replacement shape described, and a real before/after code snippet sourced from the HelpDesk AgentController.cs migration. `grep -c "10.0.0" MIGRATION.md` returns ≥ 1. `grep -c "FilterSpec" MIGRATION.md` returns ≥ 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write AGENTS.md typed column-filter section + promote CHANGELOG.md Unreleased</name>
  <files>AGENTS.md, CHANGELOG.md</files>
  <read_first>
    - AGENTS.md — read the existing "Non-obvious framework behaviors" section and the "Route B composite-nodes layer" section as format precedents for a new primitive section. The new section should be self-contained: wire shape, operator vocabulary summary, UI grammar (hybrid affordance, icon state grammar, popover semantics), and code examples for both backends. Look at how `TableNode.selection` is documented in AGENTS.md for the style reference.
    - CHANGELOG.md — read the current `## Unreleased` section (which Phase 32 staged). It contains the additive wire additions. The promotion replaces `## Unreleased` with `## 10.0.0 — 2026-08-18` and appends Phase 33's own bullets below the Phase 32 content. Do NOT delete or modify the Phase 32 content — only promote the header and append Phase 33 additions.
    - .planning/design/shape-column-filter-expansion.md — the canonical "Shape" section for prose describing the feature.
    - .planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-01-SUMMARY.md — the adapter implementation details (exact class names, behaviors) for the AGENTS.md description.
  </read_first>
  <action>
    **AGENTS.md new section:**

    Add a new section titled "Typed column-filter primitive" in the framework patterns area of AGENTS.md (look for a logical insertion point near the TableNode.selection section or the "Patterns" heading). The section covers:

    1. **Wire shape recap** (brief — Phase 32 additive wire): `TableColumn.filter?: FilterSpec` with `kind: ValueKind`, `options?` (for fixed-set), `matchingHints?` (for ignore-punctuation). `TableNode.filterDescriptorBinds?: Record<string,string>` mapping column keys to state bind paths. `FilterDescriptor` in state: `{ rules: FilterRule[], joiner: "all-of" | "any-of" }`. `FilterRule`: `{ operator: ..., value?: unknown }`.

    2. **Reference truth function**: `matchesFilter(descriptor, rawValue, displayString, kind, matchingHints?)` (TS server) / `FilterHelper.MatchesFilter(descriptor, rawValue, displayString, kind, matchingHints?)` (.NET). NASA-tested. Covers contains on every kind, all operators, multi-rule all-of/any-of. Consumers call it in their action handlers; the framework never calls it implicitly.

    3. **UI grammar** (adapter-owned): Always-visible inline input + filter-icon button per filterable column. Icon state: slash-through funnel (no filter) → plain funnel (simple contains) → plain funnel + dot (escalated). Popover: operator picker + typed value inputs + add-rule + all-of/any-of joiner + Apply/Clear. Popover pre-loads inline value. Outside-click/Escape discards; Apply commits. Inline read-only summary for nontrivial descriptors.

    4. **Brief C# + TS usage snippets** (column declaration + bind path + FilterHelper call in action handler). These can be concise — 15-20 lines of code each, matching the style of other AGENTS.md code blocks.

    5. **The key non-obvious behaviors**: (a) The adapter writes FilterDescriptor to state via the bind path — no named "filter" action; any subsequent dispatch carries the descriptor to the server. (b) Contains on every kind (including dates and numbers) — users can type "2026" on a date column. (c) The truth function evaluates against the display string for contains (case-insensitive, what the user sees). (d) "Is empty" treats null, undefined, and "" as empty; whitespace-only is NOT empty.

    **CHANGELOG.md promotion:**

    Read the current `## Unreleased` section to find its exact opening line. Replace `## Unreleased` with `## 10.0.0 — 2026-08-18`. Then append Phase 33's additions below the existing Phase 32 staged content (do NOT move or remove Phase 32's bullets — they stay as the earlier bullets in the same version section). Phase 33 additions to append:

    ```
    ### Breaking changes
    - **Typed column-filter wire — old fields removed.** `TableColumn.filterable`, `TableColumn.filterValue`, `TableNode.filterBinds`, `TableNode.filterAction` removed from both backends. Consumers must declare `filter: FilterSpec` and `filterDescriptorBinds` per the new shape. See `MIGRATION.md` for the before/after guide.

    ### New features
    - **Column-filter UI (browser adapter).** Every filterable column now renders an always-visible inline input + filter-icon button. The filter-icon button opens an escalation popover with an operator picker, typed value inputs (text, number, date, fixed-set, yes-no), add-rule, all-of/any-of joiner, Apply/Clear. Icon state grammar: slash-through funnel (empty), plain funnel (simple contains), plain funnel + dot (escalated). Popover portals out of the table wrapper's overflow clip. Inline read-only summary for nontrivial descriptors.
    - **New icon glyph: `filter-slash`.** Shipped in the framework's Lucide icon inventory. Use as `icon: "filter-slash"` on any `IconNode` or icon-bearing component.
    - **Demo migration.** HelpDesk, Showcase, FeatureProbe demos updated to the new filter wire shape as reference implementations for consumer migration.
    ```
  </action>
  <verify>
    <automated>grep -c "10.0.0" /home/thenasty/ViewModelShell/CHANGELOG.md && grep -c "Unreleased" /home/thenasty/ViewModelShell/CHANGELOG.md && grep -c "FilterSpec" /home/thenasty/ViewModelShell/AGENTS.md</automated>
  </verify>
  <done>
    `CHANGELOG.md`: `## Unreleased` is gone (grep returns 0); `## 10.0.0` is present (grep returns ≥ 1); both Phase 32 and Phase 33 content is under the versioned header. `AGENTS.md`: a new "Typed column-filter primitive" section exists with `FilterSpec` and UI grammar description. `grep -c "FilterSpec" AGENTS.md` returns ≥ 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md accuracy | The AGENTS.md section is the framework's own documentation used by agents — inaccurate descriptions here cause agents to misuse the primitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | Information Disclosure | MIGRATION.md before/after snippet exposes implementation details | accept | These are reference documentation by design; no secrets in code snippets |
| T-33-05-02 | Tampering | CHANGELOG.md Unreleased section inadvertently deleted | mitigate | Read the current ## Unreleased section fully before editing; only change the header line + append new bullets |
| T-33-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `grep -c "Migrating to v10.0.0" MIGRATION.md` — ≥ 1.
2. `grep -c "FilterSpec" MIGRATION.md` — ≥ 2 (in before/after snippet).
3. `grep -c "Unreleased" CHANGELOG.md` — 0 (the section was promoted).
4. `grep -c "10.0.0" CHANGELOG.md` — ≥ 1.
5. `grep -c "filter-slash" CHANGELOG.md` — ≥ 1.
6. `grep -c "Typed column-filter" AGENTS.md` — ≥ 1.
7. `grep -c "FilterSpec" AGENTS.md` — ≥ 1.
</verification>

<success_criteria>
Plan 05 complete when:
- `MIGRATION.md` has a `## Migrating to v10.0.0` section with real before/after code.
- `AGENTS.md` has a typed column-filter primitive section with wire shape + UI grammar.
- `CHANGELOG.md` has no `## Unreleased` section; Phase 32 + Phase 33 additions are under `## 10.0.0 — 2026-08-18`.
</success_criteria>

<output>
Create `.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-05-SUMMARY.md` when done.
</output>
