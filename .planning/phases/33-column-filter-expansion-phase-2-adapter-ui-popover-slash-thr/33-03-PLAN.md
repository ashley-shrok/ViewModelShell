---
phase: 33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr
plan: "03"
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - demo/HelpDesk/AspNetCore/AgentController.cs
  - demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs
  - demo/ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
autonomous: true
requirements:
  - REQ-CF2-07
  - REQ-CF2-08

must_haves:
  truths:
    - "All 4 .NET consumer files compile without referencing Filterable, FilterValue, FilterBinds, or FilterAction"
    - "HelpDesk AgentController declares Title column with Filter: new FilterSpec('text') and FilterDescriptorBinds; uses FilterHelper.MatchesFilter in its filter logic"
    - "HelpDesk AgentControllerTests asserts on FilterSpec/FilterDescriptor shape (not FilterValue)"
    - "ExpenseTracker.Tests does not reference c.Filterable (removed; Assert.False(c.Filterable) replaced)"
    - "FeatureProbe.AspNetCore controller uses Filter + FilterDescriptorBinds + FilterHelper.MatchesFilter"
    - "All 4 associated test suites pass under dotnet test"
  artifacts:
    - path: "demo/HelpDesk/AspNetCore/AgentController.cs"
      provides: "Migrated filter columns (Title: text, Status: fixed-set) + FilterDescriptorBinds + FilterHelper.MatchesFilter in action handler"
      contains: "FilterSpec"
    - path: "demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs"
      provides: "Tests updated to assert on FilterSpec/FilterDescriptor structure; c.FilterValue test removed"
      contains: "FilterSpec"
    - path: "demo/ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs"
      provides: "Assert.False(c.Filterable) removed; test remains semantically valid"
    - path: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs"
      provides: "Migrated filter columns + FilterHelper.MatchesFilter"
      contains: "FilterSpec"
  key_links:
    - from: "demo/HelpDesk/AspNetCore/AgentController.cs BuildVm()"
      to: "FilterSpec + FilterDescriptorBinds on TableNode"
      via: "new FilterSpec('text') on Title column; new FilterSpec('fixed-set', Options: [...]) on Status column"
      pattern: "FilterSpec"
    - from: "demo/HelpDesk/AspNetCore/AgentController.cs action handler"
      to: "FilterHelper.MatchesFilter"
      via: "descriptor from state.FilterDescriptors[colKey]"
      pattern: "FilterHelper.MatchesFilter"
---

<objective>
Migrate 4 .NET consumer files (HelpDesk AgentController + its tests, ExpenseTracker tests, FeatureProbe controller) to the new filter wire shape after the old fields are gone from ViewModels.cs (Plan 02). Run all affected dotnet test suites to confirm green.

Purpose: HelpDesk AgentController.cs is the MIGRATION.md reference source (D-05). The migrated diff — old Filterable/FilterValue/FilterBinds/FilterAction → new FilterSpec/FilterDescriptorBinds/FilterHelper.MatchesFilter — is what Plan 05 uses verbatim for the MIGRATION.md before/after snippet.

Output: 4 .NET files compiling on new wire; all associated test suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-SPEC.md
@.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-CONTEXT.md
@.planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-02-SUMMARY.md

<interfaces>
<!-- FilterHelper.MatchesFilter API (from 32-02-SUMMARY.md): -->
```csharp
// In viewmodel-shell-dotnet/FilterHelper.cs:
public static bool MatchesFilter(
    FilterDescriptor descriptor,
    object? rawValue,
    string displayString,
    string kind,
    IEnumerable<string>? matchingHints = null
)
// Usage: using ViewModelShell; (namespace)
// Returns true if the row matches the descriptor per the column's kind
// descriptor: read from state via ActionPayload<TState>.State.FilterDescriptors[colKey]
//   where FilterDescriptors is Dictionary<string, FilterDescriptor?> in state record
```

<!-- .NET wire types (from Phase 32 32-02-SUMMARY.md): -->
```csharp
// FilterSpec — schema on the column (declared by the app):
public record FilterSpec(string Kind, IReadOnlyList<string>? Options = null, IReadOnlyList<string>? MatchingHints = null);

// FilterDescriptor — instance in state (the user's current filter):
public record FilterDescriptor(IReadOnlyList<FilterRule> Rules, string Joiner);
public record FilterRule(string Operator, object? Value = null);

// On TableColumn (Phase 32 addition — KEPT):
//   FilterSpec? Filter = null
// On TableNode (Phase 32 addition — KEPT):
//   Dictionary<string, string>? FilterDescriptorBinds = null
```

<!-- HelpDesk AgentController state record (read the file to confirm exact shape): -->
// Current: TitleFilter: string (or similar) in state record
// After: FilterDescriptors: Dictionary<string, FilterDescriptor?> (or per-column fields like TitleDescriptor + StatusDescriptor)
// Bind paths: FilterDescriptorBinds: new() { ["title"] = "filterDescriptors.title", ["status"] = "filterDescriptors.status" }

<!-- ExpenseTracker test context: -->
// Line 198: Assert.False(c.Filterable);
// After removal: replace with either Assert.Null(c.Filter) or just remove the filterable assertion entirely
// The test is checking that ledger columns have NO filter — keep that semantic by asserting c.Filter == null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HelpDesk AgentController.cs and its tests</name>
  <files>demo/HelpDesk/AspNetCore/AgentController.cs, demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs</files>
  <read_first>
    - demo/HelpDesk/AspNetCore/AgentController.cs — read the FULL file. Identify: (a) the state record type and field names for title filter and status filter; (b) the `BuildVm()` method's TableColumn and TableNode declarations for the agent queue table; (c) the action handler switch — the "filter-text" action case; (d) the row filtering logic (where `titleFilter` is applied to ticket rows). This is the MIGRATION.md reference — the before/after diff MUST be clearly representable.
    - demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs lines 115-130 — the test asserting `table.Columns.Single(c => c.Key == "title").FilterValue` equals "xyzzy"; lines 195-200 — the Assert.False(c.Filterable) assertion in a loop. These two assertion patterns must be updated.
    - viewmodel-shell-dotnet/FilterHelper.cs — confirm the namespace (should be `ViewModelShell`) and method signature for `FilterHelper.MatchesFilter`.
    - .planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-CONTEXT.md D-05 — the exact MIGRATION.md before/after shape for HelpDesk.
  </read_first>
  <action>
    **AgentController.cs migration:**

    State record: Add a `Dictionary<string, FilterDescriptor?> FilterDescriptors` field (or `FilterDescriptor? TitleFilter` + `FilterDescriptor? StatusFilter` if that matches the existing state record's style — prefer a generic dict to keep the state shape open). Remove the old `string TitleFilter` field. Update `Initial()` / static init to initialize `FilterDescriptors` as an empty dictionary.

    BuildVm changes:
    - Title column: remove `Filterable: true, FilterValue: state.TitleFilter.Length > 0 ? state.TitleFilter : null`. Add `Filter: new FilterSpec("text")`. (D-05: the MIGRATION.md "after" snippet for the column.)
    - If there is a Status column currently using `Filterable: true, FilterValue: state.StatusFilter`: add `Filter: new FilterSpec("fixed-set", Options: new[] { "open", "in-progress", "closed", "resolved" })` (or whatever the actual status values are — read the file to confirm).
    - TableNode: remove `FilterBinds: new Dictionary<string, string> { ["title"] = "titleFilter" }` and `FilterAction: new ActionDescriptor("filter-text")`. Add `FilterDescriptorBinds: new Dictionary<string, string> { ["title"] = "filterDescriptors.title" }` (and `["status"] = "filterDescriptors.status"` if status is filterable).

    Action handler: Remove the `case "filter-text":` (and any status filter action case). Add `FilterHelper.MatchesFilter` call to the row-filtering logic. The canonical pattern:
    ```csharp
    var filtered = allTickets.Where(ticket =>
      state.FilterDescriptors.All(kv =>
        kv.Value == null || kv.Value.Rules.Count == 0 ||
        FilterHelper.MatchesFilter(kv.Value,
          GetCellValue(ticket, kv.Key),
          GetDisplayValue(ticket, kv.Key),
          GetKind(kv.Key))
      )
    ).ToList();
    ```
    Where `GetCellValue`, `GetDisplayValue`, `GetKind` are inline helpers (or a switch) per column key. Adapt to the actual ticket model shape.

    Also remove `UnknownActionException` throws that reference the removed action names, and verify the switch default still throws `UnknownActionException`.

    Using directive: add `using ViewModelShell;` if not already present (for `FilterHelper` + `FilterSpec` + `FilterDescriptor`).

    **AgentControllerTests.cs migration:**
    - Find the test asserting `table.Columns.Single(c => c.Key == "title").FilterValue` equals "xyzzy" (line ~120). Replace with: assert that after the filter action (now a state-write followed by a render dispatch), the column's `Filter` is a `FilterSpec` with `Kind == "text"`, and the table's `FilterDescriptorBinds` contains the title key. Or, if the test was checking that the controller round-trips the filter value, update it to dispatch a state with a `FilterDescriptor` at "filterDescriptors.title" and assert that the rendered table's filtered rows match.
    - Find `Assert.False(c.Filterable)` (line ~198). Replace with `Assert.Null(c.Filter)` (or remove the assertion if the column's filter absence is not a meaningful test in this context).
    - Verify any other test assertions referencing `Filterable`, `FilterValue`, `FilterBinds`, `FilterAction` on the controller.
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && ~/.dotnet/dotnet test demo/HelpDesk/AspNetCore.Tests/HelpDesk.AspNetCore.Tests.csproj 2>&1 | tail -10</automated>
  </verify>
  <done>
    `dotnet test demo/HelpDesk/AspNetCore.Tests/` exits 0 with all tests passing. `AgentController.cs` uses `FilterSpec` + `FilterDescriptorBinds` + `FilterHelper.MatchesFilter`. `AgentControllerTests.cs` has no references to `FilterValue`, `Filterable`, `FilterBinds`, or `FilterAction` on table nodes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ExpenseTracker tests and FeatureProbe controller</name>
  <files>demo/ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs, demo/FeatureProbe/AspNetCore/FeatureProbeController.cs</files>
  <read_first>
    - demo/ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs lines 185-205 — the `Assert.False(c.Filterable)` assertion; read surrounding context (what the test is verifying about the ledger table columns). The semantic intent is "ledger columns have no filter" — replace `Assert.False(c.Filterable)` with `Assert.Null(c.Filter)`.
    - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs lines 2020-2050 — the current filter column declaration (Name column with `Filterable: true, FilterValue: state.TableFilters.Name`); the `FilterBinds` and `FilterAction` on the TableNode; the row-filtering logic in the "table-filter" action handler. Read the full state record shape to understand `TableFilters.Name`.
    - viewmodel-shell-dotnet/FilterHelper.cs — confirm the static method signature.
  </read_first>
  <action>
    **ExpenseTracker tests (minimal change):**
    In the test at line ~198, change `Assert.False(c.Filterable)` to `Assert.Null(c.Filter)`. This preserves the semantic assertion ("this column has no filter declared") using the new wire shape. Run `dotnet test` to confirm.

    **FeatureProbeController.cs migration:**
    State record: Add `FilterDescriptor? NameFilter` (or a dict `FilterDescriptors`) to the state. Remove the old string-based `TableFilters.Name` filter field (or if `TableFilters` is a nested record, add `FilterDescriptor? NameFilter` to it — read the file to determine the exact structure). Update initial state.

    BuildVm changes:
    - Name column: remove `Filterable: true, FilterValue: state.TableFilters.Name.Length > 0 ? state.TableFilters.Name : null`. Add `Filter: new FilterSpec("text")`.
    - TableNode: remove `FilterBinds: new Dictionary<string, string> { ["name"] = "tableFilters.name" }` and `FilterAction: new ActionDescriptor("table-filter")`. Add `FilterDescriptorBinds: new Dictionary<string, string> { ["name"] = "nameFilter" }` (or whatever bind path matches the state field).

    Action handler: Remove `case "table-filter":`. Replace row filtering with `FilterHelper.MatchesFilter(state.NameFilter, feature.Name, feature.Name, "text")` (or generic pattern using the dict). Ensure the switch default remains `throw new UnknownActionException(payload.Name)`.

    Run `dotnet test demo/FeatureProbe/AspNetCore.Tests/` (if a test project exists — check with `find demo/FeatureProbe -name "*.Tests.csproj"`; if no test project exists for FeatureProbe .NET, confirm via `ls demo/FeatureProbe/` and document in SUMMARY).
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && ~/.dotnet/dotnet test demo/ExpenseTracker/AspNetCore.Tests/ExpenseTracker.AspNetCore.Tests.csproj 2>&1 | tail -5 && find demo/FeatureProbe -name "*.Tests.csproj" 2>/dev/null && ~/.dotnet/dotnet build demo/FeatureProbe/AspNetCore/FeatureProbe.AspNetCore.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>
    `dotnet test demo/ExpenseTracker/AspNetCore.Tests/` exits 0. `FeatureProbeController.cs` builds clean with no references to `Filterable`, `FilterValue`, `FilterBinds`, or `FilterAction`. `Assert.False(c.Filterable)` in ExpenseTracker tests is replaced with `Assert.Null(c.Filter)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C# positional record parameter removal | Removing positional parameters from TableColumn/TableNode records shifts ctor arities — any demo that uses positional (non-named) ctor calls will fail to compile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Tampering | FilterDescriptor from state in .NET controller | mitigate | FilterHelper.MatchesFilter returns false for unknown operators (Phase 32 T-32-01-01); tamper-safe |
| T-33-03-02 | Elevation of Privilege | Removed action cases leaving switch with no default | mitigate | Verify switch default still throws UnknownActionException after removing filter action cases |
| T-33-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `~/.dotnet/dotnet test demo/HelpDesk/AspNetCore.Tests/` — all tests pass.
2. `~/.dotnet/dotnet test demo/ExpenseTracker/AspNetCore.Tests/` — all tests pass.
3. `grep -rn "Filterable\|FilterValue\|FilterBinds\|FilterAction" demo/HelpDesk/AspNetCore/ demo/FeatureProbe/AspNetCore/ demo/ExpenseTracker/AspNetCore.Tests/` — zero matches.
4. `grep -c "FilterSpec" demo/HelpDesk/AspNetCore/AgentController.cs` — ≥ 1.
5. `grep -c "FilterHelper.MatchesFilter" demo/HelpDesk/AspNetCore/AgentController.cs` — ≥ 1.
</verification>

<success_criteria>
Plan 03 complete when:
- All 4 .NET files compile and test-suites pass.
- Zero references to old filter wire in migrated .NET consumer files.
- HelpDesk AgentController is the clean MIGRATION.md reference source (FilterSpec + FilterDescriptorBinds + FilterHelper.MatchesFilter).
</success_criteria>

<output>
Create `.planning/phases/33-column-filter-expansion-phase-2-adapter-ui-popover-slash-thr/33-03-SUMMARY.md` when done.
</output>
