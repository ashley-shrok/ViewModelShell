---
phase: 18-chart-base-set-primitive-reshape-chartnode-to-multi-series-n
plan: 06
type: execute
wave: 3
depends_on: [18-03, 18-04, 18-05]
files_modified: []
autonomous: true
requirements: [CHARTBASE-06]
must_haves:
  truths:
    - "npx vitest run (jsdom) is green across the whole framework suite"
    - "bun run parity/run.ts is green"
    - "npm run check:core-globals is green (core stays platform-agnostic)"
    - "dotnet test viewmodel-shell-dotnet/Tests is green"
    - "every demo/**/*.Tests.csproj is green"
    - "npm run check:aa-contrast is green (fixed pair-set unaffected)"
    - "every CHARTBASE requirement (01-06) maps to a landed artifact; ChartPoint is fully retired; zero raw color on the wire; core/.NET/bun stay dependency-free"
  artifacts:
    - path: ".planning/phases/18-chart-base-set-primitive-reshape-chartnode-to-multi-series-n/18-06-SUMMARY.md"
      provides: "The full green-tree gate log + requirement-to-artifact cross-check table"
      contains: "CHARTBASE"
  key_links:
    - from: "all wave-1/2 plans"
      to: "green-tree gate"
      via: "vitest + parity + core-globals + dotnet Tests + demo *.Tests + aa-contrast"
      pattern: "vitest"
---

<objective>
Run the FULL green-tree gate after the reshape lands, fix any integration breakage the individual plans
didn't catch, and produce a requirement-to-artifact cross-check proving CHARTBASE-01..06 are all
satisfied. This is the phase's goal-backward closer — Phase 18 ends when the primitive + rendering +
palette + parity are built AND the whole gate is green. It owns no source files of its own; it verifies
the integrated result and applies only surgical fixes needed to make the gate pass.

Purpose: the AGENTS.md working agreement forbids ever leaving a broken tree — a reshape this broad must
leave every suite green (framework vitest, parity, core-globals, the framework's own .NET Tests, AND every
demo *.Tests.csproj), with no "pre-existing/unrelated/just-a-demo" exceptions.

Output: a green full gate + a cross-check table in the summary. (NO release/publish/tag/announce and NO
human-verification page — those are Phase 19, explicitly out of scope.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/design/chart-base-set.md
@.planning/phases/18-chart-base-set-primitive-reshape-chartnode-to-multi-series-n/18-CONTEXT.md
@AGENTS.md

<gate_commands>
The full green-tree gate (AGENTS.md working agreement + CONTEXT). `dotnet` is at `~/.dotnet/dotnet`.
1. `cd viewmodel-shell && npx vitest run`            # jsdom — NOT `bun test` (no DOM → spurious fails)
2. `cd .. && bun run parity/run.ts`                  # cross-backend structural parity + check-skill.ts
3. `cd viewmodel-shell && npm run check:core-globals` # core (src/index.ts) references zero platform globals
4. `cd viewmodel-shell && npm run check:aa-contrast`  # fixed pair-set (chart palette not gate-covered)
5. `export PATH="$HOME/.dotnet:$PATH" && for p in viewmodel-shell-dotnet/Tests $(find demo -name '*.Tests.csproj'); do dotnet test --nologo -v minimal "$p" || echo "FAILED: $p"; done`
</gate_commands>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full green-tree gate; fix any integration breakage</name>
  <files>(surgical fixes only, if the gate surfaces a break not owned by 18-01..05)</files>
  <read_first>
  - The `<gate_commands>` block above
  - AGENTS.md — the "NEVER PUBLISH OR PUSH ANYTHING BROKEN" working-agreement gate (the exact suite list) and the green-tree-gate maintainer rule
  - The 18-01..18-05 SUMMARY files (to know what each plan changed + any noted transient-red they expected to be resolved by the time this runs)
  </read_first>
  <action>
  Run all five gate commands in order. For any failure: diagnose and apply the minimal fix in the owning
  file. Expected integration seams to check specifically because the reshape is broad: (a) any framework
  vitest suite beyond chart.test.ts that referenced the old ChartNode (e.g. a conformance/tree-walk test
  that constructs a ChartNode); (b) the TUI conformance/lifecycle suites (tui.tsx changed); (c) the .NET
  demos + their *.Tests.csproj compiling against the reshaped ViewModels record (FeatureProbe has no
  .Tests, but confirm the 5 demo test projects + the framework Tests all build/pass); (d) any lingering
  `ChartPoint`/`points` reference anywhere in tracked source. Do NOT introduce new features or scope — only
  the fixes required to make the gate green. If a failure belongs to a wave-1/2 plan's file and is
  substantive, note it; but apply the fix here to reach green (the phase cannot close red).
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" && cd /home/thenasty/ViewModelShell/viewmodel-shell && npx vitest run 2>&1 | tail -6 && npm run check:core-globals 2>&1 | tail -3 && npm run check:aa-contrast 2>&1 | tail -3 && cd .. && bun run parity/run.ts 2>&1 | tail -6 && for p in viewmodel-shell-dotnet/Tests $(find demo -name '*.Tests.csproj'); do dotnet test --nologo -v minimal "$p" 2>&1 | tail -2; done</automated>
  </verify>
  <acceptance_criteria>
  - `npx vitest run` passes (all suites, 0 failures).
  - `bun run parity/run.ts` passes.
  - `npm run check:core-globals` passes (core references zero platform globals).
  - `npm run check:aa-contrast` passes.
  - `dotnet test viewmodel-shell-dotnet/Tests` passes.
  - Each of the 5 `demo/**/*.Tests.csproj` passes (no `FAILED:` line).
  - `grep -rn "ChartPoint" --include=*.ts --include=*.tsx --include=*.cs viewmodel-shell viewmodel-shell-dotnet demo | grep -v /dist/ | grep -v node_modules` returns nothing (ChartPoint fully retired in tracked source).
  </acceptance_criteria>
  <done>Every gate command is green; no ChartPoint/old-shape reference remains; any integration break was fixed surgically.</done>
</task>

<task type="auto">
  <name>Task 2: Requirement-to-artifact cross-check + dependency-free confirmation</name>
  <files>(verification only — recorded in the SUMMARY)</files>
  <read_first>
  - .planning/phases/18-chart-base-set-primitive-reshape-chartnode-to-multi-series-n/18-CONTEXT.md (the CHARTBASE-01..06 definitions)
  - The 18-01..18-05 SUMMARY files
  </read_first>
  <action>
  Build a cross-check table mapping each CHARTBASE-01..06 requirement to the concrete landed artifact(s) +
  the passing check that proves it. Additionally confirm the three cross-cutting invariants: (1) ZERO raw
  color on the wire — grep the reshaped wire type + FeatureProbe emission + fixture for any hex/rgb literal
  crossing the wire (there must be none; color is CSS-token / tone-enum only); (2) core/.NET/bun stay
  DEPENDENCY-FREE — confirm `chart.js` appears only as an optional/peer dep of the browser package and only
  via a dynamic `import("chart.js")` in browser.ts (not in index.ts, ViewModels.cs, or any bun demo import);
  (3) ChartPoint fully retired (from Task 1's grep). Record the table + these confirmations in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && echo "=== dynamic import only ===" && grep -n "chart.js" viewmodel-shell/src/browser.ts | grep import; echo "=== core/backends free of chart.js import ===" && grep -rn "from \"chart.js\"\|require(.chart.js\|import(\"chart.js\")" viewmodel-shell/src/index.ts viewmodel-shell-dotnet/ViewModels.cs demo/FeatureProbe-bun/handler.ts || echo "none (good)"; echo "=== raw color on wire? ===" && grep -nE "#[0-9a-fA-F]{3,8}|rgb\(" parity/fixtures/feature-probe.json | grep -i chart || echo "no raw color in chart fixture (good)"</automated>
  </verify>
  <acceptance_criteria>
  - The SUMMARY contains a table mapping CHARTBASE-01, 02, 03, 04, 05, 06 each to a landed artifact + a passing check.
  - The `import("chart.js")` in browser.ts is dynamic; `index.ts`, `ViewModels.cs`, and the bun demo handler carry NO chart.js import (verify prints "none (good)").
  - No raw color (hex/rgb) crosses the wire in the chart fixture (verify prints "no raw color…").
  - ChartPoint is confirmed retired (references Task 1's grep result).
  </acceptance_criteria>
  <done>Every CHARTBASE requirement is traced to a landed artifact; dependency-free + zero-raw-color + ChartPoint-retired invariants are confirmed and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integrated build → release readiness | This plan verifies the integrated result; it introduces no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-12 | Tampering | shipping on a broken/red tree | mitigate | The full gate (vitest + parity + core-globals + .NET Tests + all demo *.Tests + aa-contrast) must be green before the phase closes |
| T-18-13 | Elevation of privilege | core losing platform-agnosticism | mitigate | `check:core-globals` gate confirms src/index.ts references zero platform globals after the reshape |
| T-18-SC | Tampering | package installs | accept | No installs — gate + cross-check only |
</threat_model>

<verification>
- All five gate commands green.
- Cross-check table + dependency-free + zero-raw-color + ChartPoint-retired confirmations recorded in the SUMMARY.
</verification>

<success_criteria>
- The full green-tree gate is green (vitest, parity, core-globals, aa-contrast, framework .NET Tests, all 5 demo *.Tests.csproj).
- CHARTBASE-01..06 each trace to a landed artifact; core/.NET/bun stay dependency-free; zero raw color on the wire; ChartPoint retired.
- NO release/publish/tag/announce and NO human-verification page performed (Phase 19).
</success_criteria>

<output>
Create `.planning/phases/18-chart-base-set-primitive-reshape-chartnode-to-multi-series-n/18-06-SUMMARY.md` when done,
including the full gate log + the CHARTBASE requirement-to-artifact cross-check table.
</output>
