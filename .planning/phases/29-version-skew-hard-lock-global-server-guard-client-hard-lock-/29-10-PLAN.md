---
phase: 29-version-skew-hard-lock-global-server-guard-client-hard-lock-
plan: 10
type: execute
wave: 8
depends_on: ["29-05", "29-07", "29-08", "29-09"]
files_modified: []
autonomous: true
requirements: [SKEW-09]

must_haves:
  truths:
    - "Full green-tree gate per AGENTS.md 🚨 NEVER PUBLISH OR PUSH ANYTHING BROKEN rule — the EXACT set CI runs, byte-copied from the working agreement section. Machine-verifiable; no theatrical human checkpoint per banked Ashley 2026-07-31 directive that established the autonomous-gate discipline for Phase 28."
    - "TS-side gate (from viewmodel-shell/): `npm run build && npm run check:test-types && npm run check:core-globals && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types && npm run check:theme-byte-identity && npm run check:theme-function && npx vitest run` — ALL exit 0. check:test-types is CI-only-catches per banked lesson; do not skip. check:aa-contrast MUST stay green — Plan 29-06's modal reuses shipped tone pairs and should not introduce new pairs."
    - "PATH-export gate: `export PATH=\"$HOME/.dotnet:$PATH\"` before any .NET or parity command — the parity harness spawns dotnet as a child and dies with a bare `spawn` trace if not on PATH."
    - ".NET-side gate: `dotnet test viewmodel-shell-dotnet/Tests` — exit 0. This project must stay green through Plan 29-05's test additions (SKEW-01 filter behavior + self-registration + double-call dedup)."
    - "Every demo .NET tests project runs: `for p in $(find demo -name '*.Tests.csproj'); do dotnet test \"$p\"; done` — every project exits 0. IF any demo test project shows a PRE-EXISTING failure NOT related to Phase 29's work: HALT per AGENTS.md working-agreement rule ('It was already failing on main is NOT an exception') and report the failure to the operator; do NOT proceed to Plan 29-11/12 until the pre-existing failure is fixed OR explicitly waived by the operator."
    - "Markdown companion compile: `dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj` — exit 0. Phase 29 did NOT touch the Markdown companion; this build is the defense-in-depth against inadvertent-cross-project breakage."
    - "**Companion binary-compat gate: `bash parity/check-companion-binary-compat.sh` — exit 0.** Phase 29 is a core MAJOR bump (v9.0.0). Per AGENTS.md rule 'On any VMS core MAJOR bump, every companion NuGet is rebuilt + republished' at line ~693: this script is the ONE gate that catches companion binary-compat drift — the in-tree source-rebuild path (framework Tests, Markdown Tests, demo Tests, parity) all reach the core via `<ProjectReference>` and are STRUCTURALLY BLIND to a positional-parameter change in a core record's primary ctor. The gate exercises packed IL against a packed core so any silent binary-break surfaces here. Runs after the .NET test gates + Markdown compile so any earlier failure short-circuits before spending time here."
    - "Parity: `bun run parity/run.ts` — exit 0. Plan 29-07 landed the GET-side skew fixtures; this run is the last cross-backend byte-check before release."
    - "Additionally: `npm run check:theme-byte-identity` + `npm run check:theme-function` — theme consistency gates; historically added; run for defense-in-depth."
    - "SUMMARY records the exact exit code + last-few-lines of output for EACH gate command, so 29-11/12 can point to concrete evidence green-tree was verified."
    - "IF ANY gate fails: HALT. Do NOT create a SUMMARY marking Phase 29 green. The failing gate's output is recorded in a FAILURE SUMMARY; the executor fixes the failure OR reports it to the operator; the plan reopens only after the failure is genuinely green."
  artifacts:
    - path: ".planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md"
      provides: "Per-gate exit-code + tail output evidence + timing + final GREEN or FAILURE verdict"
      contains: "GREEN"
  key_links:
    - from: "AGENTS.md 🚨 gate specification"
      to: "This plan's task list"
      via: "The command set is byte-copied from AGENTS.md working-agreement + Phase 28's Plan 28-10 (which is the shipped precedent for this gate's exact wording), PLUS the companion binary-compat gate per AGENTS.md core-major-bump rule (line ~693)"
      pattern: "check:test-types|check:core-globals|check:aa-contrast|check-companion-binary-compat"
    - from: "parity/check-companion-binary-compat.sh gate"
      to: "Plan 29-12 companion NuGet republish step"
      via: "the gate PROVES the current core is binary-compatible with the current companion IL; if the gate is red, republishing the companion is the only correct next action (documented in AGENTS.md core-major-bump rule)"
      pattern: "check-companion-binary-compat|Markdown.*republish"
---

<objective>
Run the FULL green-tree gate — the exact machine-verifiable set CI runs PLUS the AGENTS.md-mandated companion binary-compat gate for core MAJOR bumps — before docs staging + release. Load-bearing prerequisite for Plans 29-11 (docs) + 29-12 (release) per AGENTS.md 🚨 NEVER PUBLISH OR PUSH ANYTHING BROKEN rule. Autonomous; no theatrical human checkpoint per banked 2026-07-31 Ashley directive.

Purpose: The gate exists to catch the class of defect that historically slipped through: (a) test additions that break test-types compilation (check:test-types); (b) new CSS rules failing AA contrast (check:aa-contrast) — Plan 29-06's modal reuses shipped pairs but the gate is the safety net; (c) demo files that don't type-check (check:demo-types) — Plan 29-07 modified demo/HelpDesk-bun/ + Plan 29-09 created demo/VersionSkewVerification-bun/; (d) any pre-existing framework .NET test failure (the 3.0.0-3.3.0 uncompilable stretch); (e) companion NuGet binary-break invisible to source-rebuild paths (the Markdown 0.2.1 rebuild after 8.0.0 precedent — the one that cost a downstream a day and prompted the rule at AGENTS.md line ~693). Phase 29 IS a core MAJOR bump; the companion binary-compat gate is mandatory here, not optional.

Output: SUMMARY marking every gate GREEN, or a HALT with a FAILURE SUMMARY naming the failing gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
</context>

<interfaces>
The gate is copied verbatim from AGENTS.md §"NEVER PUBLISH OR PUSH ANYTHING BROKEN" and vms-map.md §"The local green-tree gate", EXTENDED with the AGENTS.md-mandated companion binary-compat gate for core MAJOR bumps. If either doc updates the command set, this plan's task must be updated in the same commit — the rule is that the gate matches what CI runs.

The gate commands (batch A — TS-side, run from `viewmodel-shell/`):
```bash
npm run build && npm run check:test-types && npm run check:core-globals \
  && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types \
  && npm run check:theme-byte-identity && npm run check:theme-function \
  && npx vitest run
```

Batch B — .NET + companion binary-compat + parity (repo root, PATH exported):
```bash
export PATH="$HOME/.dotnet:$PATH"
dotnet test viewmodel-shell-dotnet/Tests
for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p"; done
dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj
bash parity/check-companion-binary-compat.sh
bun run parity/run.ts
```

⚠️ Pre-existing failure handling (from AGENTS.md working agreement):
> `It was already failing on main / it's unrelated to my change / it's just a demo test` are **NOT** exceptions — a red suite is a red suite. If a pre-existing failure surfaces mid-release, STOP, surface it, and fix or explicitly waive before proceeding.

⚠️ Companion binary-compat gate (from AGENTS.md line ~693, core-major-bump rule):
> On any VMS core MAJOR bump, every companion NuGet is rebuilt + republished with a bumped floor dep on the new major, in the same session. Adding a positional parameter to a C# record's primary ctor is binary-breaking regardless of the default value — the "trailing-append zero-retype convention" that governs in-tree source recompiles says NOTHING about downstream compatibility with previously-packed companion assemblies. A companion packed against core vN carries baked `newobj` opcodes referencing the vN ctor arities; under core v(N+1) with any changed positional arity, the JIT raises `MissingMethodException` at first-real-use.

Phase 29 IS core v9.0.0 (a MAJOR bump). The `parity/check-companion-binary-compat.sh` gate is the enforcement mechanism for this rule — it exercises packed IL against a packed core, which the source-rebuild path (all the other .NET gates above) cannot see. If this gate is RED here, the response is: (a) DO NOT proceed to Plan 29-11/29-12 with the current companion IL; (b) Plan 29-12 MUST rebuild + republish the companion NuGet alongside the core publish (already in 29-12's scope per the AGENTS.md rule); (c) after the companion republish, this gate re-runs green.

The plan MUST NOT insert a `checkpoint:human-verify` for "confirm the gate is green" per banked Ashley 2026-07-31 directive (the theatrical checkpoint class). The gate's exit codes are objective; if all green, autonomous proceed; if any red, halt.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full TS-side gate (from viewmodel-shell/)</name>
  <files></files>
  <read_first>
    - AGENTS.md §"Working agreement for agents" (the 🚨 gate rule + the exact command set)
    - viewmodel-shell/package.json (READ the scripts block to confirm every check:* target exists; a script name change since gate-copying would silently break the gate)
  </read_first>
  <action>
    Run the TS gate:
    ```bash
    cd viewmodel-shell
    npm run build && \
    npm run check:test-types && \
    npm run check:core-globals && \
    npm run check:aa-contrast && \
    npm run check:no-demo-style && \
    npm run check:demo-types && \
    npm run check:theme-byte-identity && \
    npm run check:theme-function && \
    npx vitest run
    ```
    Capture: exit code + last 10 lines of output for EACH command. If any command exits non-zero, HALT and report to SUMMARY as FAILURE with the failing command name + output. Do NOT proceed to Task 2 until Task 1 is fully green.

    Note on check:core-globals: this guard is scoped to `viewmodel-shell/src/index.ts` per AGENTS.md; Plan 29-01/03 modified only that file (no new DOM references); Plan 29-06 modified browser.ts (excluded). The guard should stay green trivially.

    Note on check:aa-contrast: Plan 29-06 reused shipped tone pairs — no new fg/bg pair introduced. The gate should stay green trivially.

    Note on check:demo-types: Plan 29-07 modified demo/HelpDesk-bun/server.ts + Plan 29-09 created demo/VersionSkewVerification-bun/. Both must type-check.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:test-types 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:core-globals 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:aa-contrast 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:no-demo-style 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:demo-types 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:theme-byte-identity 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:theme-function 2&gt;&amp;1 | tail -3 &amp;&amp; npx vitest run 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the batch exits 0 (verified by the chained `&&` — if any fails, the chain stops).
    - No `TypeError`, `AssertionError`, or `FAIL` in any tail output.
    - vitest report shows "passed" for every test file including the extended `test/version-skew.test.ts` from Plans 29-03/04/06.
  </acceptance_criteria>
  <done>
    TS-side gate green. All 9 sub-gates + vitest exit 0. Ready for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the .NET + companion binary-compat + parity gate (from repo root with PATH exported)</name>
  <files></files>
  <read_first>
    - AGENTS.md §"Working agreement for agents" (the 🚨 gate rule + the .NET-side test discipline)
    - **AGENTS.md line ~693** (the core-major-bump companion-rebuild-and-republish rule — the reason `bash parity/check-companion-binary-compat.sh` is mandatory in this batch for Phase 29 specifically)
    - parity/check-companion-binary-compat.sh (READ the script to understand what it exercises — packed companion IL against packed core, catching the binary-break class the source-rebuild path cannot see)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (READ to confirm the project builds cleanly)
    - viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj (READ to confirm the companion project builds cleanly)
  </read_first>
  <action>
    Export PATH, then run the .NET-side gate:
    ```bash
    export PATH="$HOME/.dotnet:$PATH"
    dotnet test viewmodel-shell-dotnet/Tests
    ```
    Capture exit code + tail 15 lines. Halt on non-zero.

    Then run every demo .NET tests project:
    ```bash
    for p in $(find demo -name '*.Tests.csproj'); do
      echo "===== Testing: $p ====="
      dotnet test "$p" 2>&1 | tail -5
    done
    ```
    Capture exit code per project. Halt on ANY non-zero — pre-existing failures are NOT exceptions per AGENTS.md.

    Then compile the Markdown companion:
    ```bash
    dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj
    ```
    Halt on non-zero.

    Then run the companion binary-compat gate (Phase 29 core-major-bump mandatory step):
    ```bash
    bash parity/check-companion-binary-compat.sh
    ```
    Capture exit code + tail 15 lines. Halt on non-zero.

    **On failure here:** Do NOT proceed. The Markdown companion IL is not binary-compatible with the current core (whose v9.0.0 core changes broke a positional-parameter contract that the packed companion still references). The correct response is: (a) HALT this gate; (b) note the failure in SUMMARY as "BLOCKING: companion binary-compat break — Plan 29-12 MUST rebuild + republish the Markdown companion as part of the release, per AGENTS.md core-major-bump rule"; (c) do NOT unblock 29-11/29-12; (d) surface to the operator so they can either revert the offending core change or fix the companion before proceeding. This gate can only re-turn-green after either (i) the offending core change is reverted or (ii) the companion is rebuilt against the new core AND this gate is re-run on the new build.

    Then run parity:
    ```bash
    bun run parity/run.ts
    ```
    Capture exit code + tail 30 lines. This is the CROSS-BACKEND byte-check; Plan 29-07's GET-side fixtures MUST fire correctly here.
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests 2&gt;&amp;1 | tail -10 &amp;&amp; for p in $(find demo -name '*.Tests.csproj'); do echo "== $p =="; dotnet test "$p" 2&gt;&amp;1 | tail -5; done &amp;&amp; dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj 2&gt;&amp;1 | tail -5 &amp;&amp; bash parity/check-companion-binary-compat.sh 2&gt;&amp;1 | tail -15 &amp;&amp; bun run parity/run.ts 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test viewmodel-shell-dotnet/Tests` exits 0.
    - Every `*.Tests.csproj` under `demo/` exits 0 — none skipped, none pre-existing red waived silently.
    - `dotnet build viewmodel-shell-dotnet/Markdown/...` exits 0.
    - **`bash parity/check-companion-binary-compat.sh` exits 0** (AGENTS.md core-major-bump rule enforcement — Phase 29 is core v9.0.0 MAJOR bump; the gate proves the current Markdown companion IL is binary-compatible with the current core).
    - `bun run parity/run.ts` exits 0 — including the new GET-side skew fixtures from Plan 29-07 (parity output should mention `agt-get-build-match` + `agt-get-build-stale` completing successfully on both backends).
    - No `Failed!` / `error` / `FAIL` in any tail output.
  </acceptance_criteria>
  <done>
    .NET + companion binary-compat + parity gate green. Combined with Task 1's TS-side gate, Phase 29 is machine-verifiable as GREEN and ready for docs staging + release. Core-major-bump rule satisfied — the companion is binary-compatible with the new core, so Plan 29-12's companion republish will ship a working binary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record per-gate evidence in SUMMARY</name>
  <files>.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md</files>
  <read_first>
    - Both preceding tasks' outputs (captured during execution).
  </read_first>
  <action>
    Write the SUMMARY per `$HOME/.claude/get-shit-done/templates/summary.md` template. Include:

    - Final verdict: **GREEN** (all gates exited 0) OR **FAILURE: <gate-name>** (with the failing gate's tail output verbatim).
    - Per-gate exit code table:
      ```
      | Gate | Exit code | Last 3 lines of output |
      |------|-----------|-------------------------|
      | npm run build | 0 | ... |
      | npm run check:test-types | 0 | ... |
      | ... | ... | ... |
      | bash parity/check-companion-binary-compat.sh | 0 | ... |
      | bun run parity/run.ts | 0 | ... |
      ```
    - Timing: total runtime for the full gate (record start + end timestamps).
    - Notes: any warnings or advisories that didn't fail the gate but are worth carrying forward (e.g., pre-existing test that showed a warning).
    - Explicit statement: "Green-tree gate satisfied per AGENTS.md 🚨 NEVER PUBLISH OR PUSH ANYTHING BROKEN — Plans 29-11 + 29-12 unblocked. Companion binary-compat gate green — Plan 29-12's Markdown companion republish will ship a binary-compatible package."

    On FAILURE: do NOT write GREEN. Write FAILURE with the offending gate + output; halt; Plans 29-11/12 stay blocked until the failure is fixed (or explicitly waived by the operator).
  </action>
  <verify>
    <automated>test -f .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md &amp;&amp; grep -c 'GREEN\|FAILURE' .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY file exists.
    - Either "GREEN" or "FAILURE" verdict appears.
    - Per-gate exit code table present, including the `check-companion-binary-compat.sh` line.
    - If GREEN: unambiguous statement that Plans 29-11 + 29-12 are unblocked AND that the companion binary-compat gate confirmed the Markdown companion is binary-compatible with the new core.
    - If FAILURE: unambiguous statement that Plans 29-11 + 29-12 are BLOCKED + operator must waive or fix. If the failure is the companion binary-compat gate specifically, the SUMMARY names the offending core change (or asks the operator to identify it) since that's the class of defect this gate exists to catch.
  </acceptance_criteria>
  <done>
    Green-tree gate verdict recorded. Phase 29 is either release-ready (GREEN) or a halt-and-fix state (FAILURE).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-parity of the gate command set | The local gate MUST match what CI runs. If the two drift (e.g., a new CI-only check was added), we could ship code that fails CI. AGENTS.md is the source of truth; a discrepancy is a documentation-vs-CI defect, not a plan-execution defect. |
| Pre-existing failures as opportunistic red-herring | AGENTS.md forbids waiving pre-existing failures silently. This plan explicitly halts on ANY red suite, whether or not caused by Phase 29 work. |
| Companion binary-compat as a class-of-defect invisible to source-rebuild | Every OTHER .NET gate above uses `<ProjectReference>` (source rebuild). Only `parity/check-companion-binary-compat.sh` exercises packed IL vs packed core. Silently skipping this gate on a core MAJOR bump is precisely the Markdown 0.2.1 precedent AGENTS.md line ~693 was written to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-32 | Repudiation | Publishing on top of broken tree | mitigate | This plan's `&&`-chained gate + halt-on-failure discipline. |
| T-29-33 | Denial of Service | Gate takes too long to run | accept | Historical gate runtime is ~2-3 minutes; acceptable for a release-gate. The added companion binary-compat gate adds ~10-30s (single shell script). |
| T-29-34 | Tampering | Test-time flag that skips a gate | mitigate | Every gate command is a fixed script from AGENTS.md; no env-var-driven skip logic in this plan. |
| T-29-34a | Repudiation | Silent binary-break on companion NuGet ships to downstream on core MAJOR bump | mitigate | `bash parity/check-companion-binary-compat.sh` in Task 2 catches the exact class-of-defect (packed IL vs packed core) that the source-rebuild path cannot see. Phase 29 is a core MAJOR bump; this gate is mandatory here per AGENTS.md line ~693. |
</threat_model>

<verification>
Per-task verifications run each gate. The `&&` chain enforces cascade-halt on any failure. Task 3's SUMMARY records the verdict.
</verification>

<success_criteria>
- Every gate in AGENTS.md's specification exits 0, INCLUDING the core-major-bump-mandatory `parity/check-companion-binary-compat.sh`.
- SUMMARY records the verdict + per-gate evidence.
- If GREEN: Plans 29-11 + 29-12 explicitly unblocked.
- If FAILURE: halt; operator waiver or fix required.
</success_criteria>

<output>
Create `.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md` per Task 3.
</output>
