---
phase: 29-version-skew-hard-lock-global-server-guard-client-hard-lock-
plan: 12
type: execute
wave: 10
depends_on: ["29-11"]
files_modified:
  - viewmodel-shell/package.json
  - viewmodel-shell/package-lock.json
  - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
  - viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj
  - CHANGELOG.md
  - MIGRATION.md
autonomous: false
requirements: [SKEW-10]

must_haves:
  truths:
    - "OPERATOR-GATED per AGENTS.md 'A version bump is NOT a release' + 'Publishing is operator-gated' rules — the credentials for npm + NuGet live outside version control and are held by the operator. Publishing itself (npm publish, dotnet nuget push) runs under the operator's shell where credentials are loaded. Per banked Ashley 2026-07-30 directive: the AUTH PRECHECK runs INLINE by the agent (not a theatrical human-action checkpoint); publishing runs under operator credentials."
    - "Version bump surgical: `viewmodel-shell/package.json` `\"version\"` bumped `8.2.0` → `9.0.0` (verify current value first; if it differs, use ACTUAL current → 9.0.0); `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` `<Version>` bumped `8.2.0` → `9.0.0`; `viewmodel-shell/package-lock.json` self-version bumped in EXACTLY TWO spots (root + packages.\"\") — assert count==2."
    - "CHANGELOG.md v9.0.0 heading date placeholder `<YYYY-MM-DD>` REPLACED with today's actual ISO date (via `date +%Y-%m-%d`). MIGRATION.md v9.0.0 heading finalized similarly if it carries a date placeholder."
    - "AUTH PRECHECK executed INLINE by the agent: source `.env` at repo root; verify `NPM_TOKEN` + `NUGET_API_KEY` are present; sync `~/.npmrc` from `NPM_TOKEN` per the vms-map ritual; run `npm whoami` — expect `ashley-shrok`. On any failure: HALT and report to operator with the specific failure. Do NOT bump versions until precheck passes."
    - "PRE-BUMP full green-tree gate re-run: Plan 29-10's gate may have staled during Plan 29-11's doc edits. Re-run the full gate (TS batch + .NET batch + parity) BEFORE the bump commit. On any failure: HALT and report; do NOT bump."
    - "🚨 COMPANION NUGET REBUILD (per AGENTS.md CORE MAJOR BUMP RULE): v9.0.0 is a MAJOR bump — every companion NuGet csproj under `viewmodel-shell-dotnet/*/` that carries a `<ProjectReference>` to the core MUST be rebuilt + republished with a bumped floor dep. Currently the shipped Markdown companion (`viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj`) references the core; identify + verify via `grep -l 'ProjectReference.*AshleyShrok.ViewModelShell' viewmodel-shell-dotnet/*/*.csproj`. For each companion: (a) bump its patch version by 1; (b) update its `<PackageReference Include=\"AshleyShrok.ViewModelShell\">` floor to `9.0.0` if that reference exists; (c) `dotnet pack -c Release`; (d) operator runs `dotnet nuget push`; (e) CHANGELOG entry adds a subsection listing the companion versions bumped."
    - "npm publish command (operator runs in shell with NPM_TOKEN active): `cd viewmodel-shell && npm publish` (prepublishOnly hook rebuilds dist/). Verify registry-latest via `curl -s https://registry.npmjs.org/@ashley-shrok/viewmodel-shell | python3 -c \"import sys,json;print(json.load(sys.stdin)['dist-tags']['latest'])\"` returns `9.0.0`. NEVER `npm view` (cached per AGENTS.md)."
    - "NuGet publish commands (operator runs): core: `cd viewmodel-shell-dotnet && dotnet pack -c Release && dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.9.0.0.nupkg --api-key \"$NUGET_API_KEY\" --source https://api.nuget.org/v3/index.json`; then Markdown companion: `cd viewmodel-shell-dotnet/Markdown && dotnet pack -c Release && dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.Markdown.<bumped-version>.nupkg --api-key \"$NUGET_API_KEY\" --source https://api.nuget.org/v3/index.json`. Registry-latest verified via curl to flat-container index. NuGet indexer is EVENTUALLY CONSISTENT — 404s for ~5-15 min are NORMAL; the push receipt is authoritative."
    - "Annotated tag `v9.0.0` created at the release-commit SHA + pushed: `git tag -a v9.0.0 <sha> -m 'viewmodel-shell 9.0.0'` + `git push origin v9.0.0`. Use the SHA from the bump-commit, NOT HEAD (HEAD may drift)."
    - "Main-advance verified: `git merge-base --is-ancestor v9.0.0 main` exits 0. If STRANDED: reconcile per AGENTS.md — rebase main onto v9.0.0 OR merge the tag in, then push origin main; re-run the ancestor check."
    - "🚨 ANNOUNCE ROOM IS `#vms-announcements` (`!QvlInhfVNZRUxQPtcR:thenasty.taild9b663.ts.net`) per Phase 28's corrected room-id lesson. VERIFY the room ID against Nelly's `/joined_rooms` output BEFORE posting; if drifted or Nelly no longer owns it, HALT and confirm with operator."
    - "DM Molly with specific integration confirmation: 'v9.0.0 shipped — the Metis:5000 flag you raised on 2026-08-02 (silent bundle-stale window) is closed. Server-side global filter enforces on GET+POST; browser shipped hard-lock modal replaces silent reload. If Metis has any custom onError affordance for stale_client, opt out via ShellOptions.onVersionSkew:\"custom\".' (Molly is Metis maintainer per CONTEXT §canonical_refs.)"
    - "The Plan 29-09 verification server (PID in `demo/VersionSkewVerification-bun/server.pid`) is killed at plan close: `kill $(cat demo/VersionSkewVerification-bun/server.pid) 2>/dev/null; echo cleaned`."
    - "The plan SUMMARY records: precheck outputs (npm whoami + NUGET_API_KEY presence WITHOUT leaking), pre-bump gate exit codes, bump-commit SHA, npm publish output, NuGet push output (core + Markdown companion), both curl-registry-latest verifications, tag creation output, main-ancestor verification (STRANDED reconciliation if it happened), Ashley's announce message text + Matrix ref, Molly DM confirmation ref (or unreachable note), companion NuGet republish evidence, PID cleanup output."
  artifacts:
    - path: "viewmodel-shell/package.json"
      provides: "version bumped to 9.0.0"
      contains: "9.0.0"
    - path: "viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj"
      provides: "<Version> bumped to 9.0.0"
      contains: "9.0.0"
    - path: "viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj"
      provides: "<Version> patch-bumped per AGENTS.md core-major-bump rule; floor dep on core updated to 9.0.0"
      contains: "9.0.0"
    - path: ".planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-12-SUMMARY.md"
      provides: "Full release evidence — commit SHA, both registry publish verifications (core + Markdown companion), tag + main-advance verification, announce message text, Molly DM ref, PID cleanup"
      contains: "9.0.0"
  key_links:
    - from: "All prior Phase 29 plans (shipped guard + client + modal + tests + parity + docs)"
      to: "npm @ashley-shrok/viewmodel-shell 9.0.0 + NuGet AshleyShrok.ViewModelShell 9.0.0 + AshleyShrok.ViewModelShell.Markdown <bumped>"
      via: "This plan's operator-gated commit + core publish + companion republish + tag + advance-main"
      pattern: "v?9\\.0\\.0"
    - from: "Molly DM"
      to: "Metis:5000 (the consumer that raised the v3.8.0 gap 2026-08-02)"
      via: "closes the feedback loop"
      pattern: "Molly|Metis"
---

<objective>
Aligned MAJOR release of v9.0.0 on both npm + NuGet, PLUS mandatory Markdown companion NuGet republish per AGENTS.md core-major-bump rule. Operator-gated per AGENTS.md publishing runbook. Mirrors Plan 28-12's structure with 9.0.0 substitutions + the AGENTS.md-mandated companion-rebuild-and-republish for the Markdown package.

Purpose: Publishing is the ONLY way v9.0.0 reaches consumers. A version bump in git without the registry push is stranded silence. Additionally, a MAJOR bump on the core silently breaks the Markdown companion's binary compat with any consumer who upgrades the core (per AGENTS.md's Amelia/Athena lesson) — the companion MUST be republished with a bumped floor dep against v9.0.0 in the same release session. This plan enforces the full runbook step-for-step.

Output: All registries serve 9.0.0 (core) + bumped-patch (Markdown companion); tag v9.0.0 exists + pushed; main advanced; #vms-announcements posted; Molly DM'd about the Metis flag closure; verification server cleaned up; SUMMARY records every verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md
@.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-11-SUMMARY.md
</context>

<interfaces>
Reference plan (READ before executing): `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-12-PLAN.md` — the byte-aligned template with 8.2.0 substituted for 9.0.0. The core structural difference for v9.0.0 is the MANDATORY companion republish (Phase 28 was a MINOR bump; v9.0.0 is MAJOR).

AGENTS.md rules governing this plan:
- "A version bump is NOT a release" — publish in the same operator session.
- "Publishing is operator-gated" — `autonomous: false`; every registry command runs under operator credentials.
- "Credential precheck" — verify auth BEFORE bump-commit; halt on failure. INLINE, not human-action.
- "Advance main" — verify with `git merge-base --is-ancestor v9.0.0 main`; reconcile if failing.
- "🚨 CORE MAJOR BUMP RULE" — rebuild + republish every companion NuGet that ProjectReferences the core; bump patch; bump floor dep to v9.0.0; update CHANGELOG.
- "Never publish or push anything broken" — Plan 29-10's GREEN verdict + this plan's pre-bump gate re-run are the prerequisites.

vms-map.md §Publishing runbook — the exact `.env` sync ritual.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inline auth precheck (banked Ashley 2026-07-30 directive — NOT a human checkpoint)</name>
  <files></files>
  <read_first>
    - vms-map.md §Publishing runbook (if available in session; else defer to the runbook comment inside `.env` at repo root)
    - AGENTS.md §"Working agreement for agents" §Credential precheck
  </read_first>
  <action>
    Execute the auth precheck INLINE. Steps:

    1. Source `.env` at repo root. If `.env` not found, HALT with 'ENV_FILE_MISSING'.
    2. Verify `NPM_TOKEN` and `NUGET_API_KEY` are set in the environment (non-empty). If either is missing, HALT with the specific variable name.
    3. Sync `~/.npmrc` from `NPM_TOKEN` — write the line `//registry.npmjs.org/:_authToken=$NPM_TOKEN` and chmod 600.
    4. Run `npm whoami`. Expect output exactly `ashley-shrok`. On mismatch: HALT with the observed username + hint that the token likely expired (mint new at npmjs.com/settings/tokens).

    On any halt: STOP the plan, record the failure in SUMMARY (redacting credentials), report to operator. Do NOT proceed.

    Record redacted precheck outputs in SUMMARY.
  </action>
  <verify>
    <automated>test -n "$NPM_TOKEN" &amp;&amp; test -n "$NUGET_API_KEY" &amp;&amp; npm whoami 2&gt;&amp;1 | grep -c 'ashley-shrok'</automated>
  </verify>
  <acceptance_criteria>
    - Both `NPM_TOKEN` and `NUGET_API_KEY` are non-empty in the shell environment.
    - `~/.npmrc` synced.
    - `npm whoami` returns `ashley-shrok`.
  </acceptance_criteria>
  <done>
    Auth precheck passed inline; credentials active for release commands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-bump green-tree gate re-run</name>
  <files></files>
  <read_first>
    - .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md (confirm GREEN verdict)
    - AGENTS.md §"Working agreement for agents" §NEVER PUBLISH OR PUSH ANYTHING BROKEN
  </read_first>
  <action>
    Re-run the full green-tree gate one more time:

    ```bash
    cd viewmodel-shell
    npm run build && npm run check:test-types && npm run check:core-globals \
      && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types \
      && npm run check:theme-byte-identity && npm run check:theme-function \
      && npx vitest run
    cd ..
    export PATH="$HOME/.dotnet:$PATH"
    dotnet test viewmodel-shell-dotnet/Tests
    for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p"; done
    dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj
    bash parity/check-companion-binary-compat.sh
    bun run parity/run.ts
    ```

    HALT on ANY non-zero exit. Do NOT bump versions on a red tree.

    🚨 The `bash parity/check-companion-binary-compat.sh` step is MANDATORY here per AGENTS.md line ~693 ("On any VMS core MAJOR bump, every companion NuGet is rebuilt + republished"). Phase 29 is a core MAJOR bump (v9.0.0). This gate exercises packed companion IL against a packed core — the ONE gate that catches the binary-compat drift class that the in-tree source-rebuild path (all the .NET tests above) is structurally blind to. If this gate is RED here after Plan 29-10's green verdict, something changed in the tree between 29-10 and now — likely the companion csproj was touched by 29-11's doc edits (unlikely) or another background process. Halt and investigate BEFORE bumping.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npx vitest run 2&gt;&amp;1 | tail -5 &amp;&amp; cd .. &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; bun run parity/run.ts 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Every command exits 0, INCLUDING `bash parity/check-companion-binary-compat.sh` (the core-major-bump-mandatory step per AGENTS.md line ~693 — proves the current Markdown companion IL is binary-compatible with the current core; the subsequent Task 3 companion patch-bump + Task 4 companion republish then ship a binary-compatible package).
    - Parity mentions the new GET-side skew fixtures completing.
  </acceptance_criteria>
  <done>
    Green-tree gate green immediately before version bump. Companion binary-compat gate confirms the Markdown companion is compatible with the new core (v9.0.0), so Task 3's patch-bump + Task 4's republish will ship a working binary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump versions surgically + finalize date placeholders + companion prep</name>
  <files>viewmodel-shell/package.json, viewmodel-shell/package-lock.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj, viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj, CHANGELOG.md, MIGRATION.md</files>
  <read_first>
    - viewmodel-shell/package.json (confirm current `"version"` — expect 8.2.0)
    - viewmodel-shell/package-lock.json (grep for `"version"` — expect at least 2 occurrences to bump)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (READ `<Version>`)
    - viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj (READ `<Version>` + any `<PackageReference Include="AshleyShrok.ViewModelShell">`)
    - CHANGELOG.md (find `<YYYY-MM-DD>` in v9.0.0 heading)
    - MIGRATION.md (find `<YYYY-MM-DD>` in v9.0.0 heading if present)
  </read_first>
  <action>
    Six surgical edits:

    (a) `viewmodel-shell/package.json`: `"version": "8.2.0"` → `"version": "9.0.0"`.

    (b) `viewmodel-shell/package-lock.json`: bump BOTH self-`"version"` occurrences (root + `packages.""`) to `9.0.0`. Do NOT touch dependency-block versions. Verify count via `grep -c '"version": "9.0.0"' viewmodel-shell/package-lock.json` returns 2.

    (c) `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj`: `<Version>8.2.0</Version>` → `<Version>9.0.0</Version>`.

    (d) `viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj`: per AGENTS.md CORE MAJOR BUMP RULE, this companion (which ProjectReferences the core) MUST be rebuilt + republished. Bump its `<Version>` by ONE PATCH (e.g., if it's 0.2.1, bump to 0.2.2). If it contains a `<PackageReference Include="AshleyShrok.ViewModelShell" Version="..." />` line (a floor-dep declaration), update the version to `9.0.0`. If the ref uses ProjectReference only (in-repo build), no floor-dep to update — the compat contract is enforced by the rebuild-and-republish itself. Record both actions in SUMMARY.

    (e) `CHANGELOG.md`: replace `<YYYY-MM-DD>` in the v9.0.0 heading with today's ISO date (`date +%Y-%m-%d`). Also add a `### Companion NuGet packages` subsection at the end of the v9.0.0 entry listing: `AshleyShrok.ViewModelShell.Markdown` bumped to `<new-version>` (rebuilt against core 9.0.0 per AGENTS.md core-major-bump rule).

    (f) `MIGRATION.md`: if v9.0.0 heading has a `<YYYY-MM-DD>` placeholder, replace it. Also add (if not already present) a companion-note bullet: 'Companion `AshleyShrok.ViewModelShell.Markdown` bumped to `<new-version>`; consumers who use this companion should upgrade both packages together.'

    Verify each edit + counts.
  </action>
  <verify>
    <automated>grep -c '"version": "9.0.0"' viewmodel-shell/package.json &amp;&amp; grep -c '"version": "9.0.0"' viewmodel-shell/package-lock.json &amp;&amp; grep -c '9.0.0' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj &amp;&amp; grep -c "$(date +%Y-%m-%d)" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"version": "9.0.0"' viewmodel-shell/package.json` returns exactly 1.
    - `grep -c '"version": "9.0.0"' viewmodel-shell/package-lock.json` returns exactly 2.
    - `grep -c '<Version>9.0.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns exactly 1.
    - Markdown companion `<Version>` bumped by patch — verify via `git diff viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj` shows a `<Version>` change.
    - CHANGELOG.md has today's date in the v9.0.0 heading.
    - CHANGELOG.md v9.0.0 entry contains a `Companion NuGet packages` subsection.
  </acceptance_criteria>
  <done>
    All version bumps surgical; date placeholders finalized; companion prepared for republish. Ready for the operator-gated publish commands.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: OPERATOR runs npm publish + NuGet push (core + Markdown companion) + tag + advance main</name>
  <files></files>
  <read_first>
    - AGENTS.md §"Working agreement for agents" §Publishing runbook (the exact commands + credential handling)
    - vms-map.md §Publishing runbook (if available)
  </read_first>
  <what-built>
    Version bumps landed in the working tree (Task 3), auth precheck passed (Task 1), green-tree gate green (Task 2). Companion csproj bumped for the core-major republish requirement. All ready for the operator to run the registry-touching commands under credentials.
  </what-built>
  <how-to-verify>
    OPERATOR runs these commands in their own shell (where `.env` credentials are loaded):

    ```bash
    # (1) Commit the bump.
    git add viewmodel-shell/package.json viewmodel-shell/package-lock.json \
            viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj \
            viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj \
            CHANGELOG.md MIGRATION.md AGENTS.md
    git commit -m "release: npm 9.0.0 + NuGet 9.0.0 + Markdown companion — version-skew hard-lock (BREAKING)

    - Server-side global filter enforces on GET + POST (was per-controller opt-in POST-only)
    - Browser shipped hard-lock modal replaces silent auto-reload (opt-out via ShellOptions.onVersionSkew:custom)
    - createVersionGuard TS-server factory + ShellVersionGuardFilter .NET IActionFilter
    - Client attaches X-VMS-Client-Build on GETs (was POST-only)
    - Markdown companion rebuilt + republished per AGENTS.md core-major-bump rule

    Wire protocol token stays viewmodel-shell/1.0; behavior-only major bump.

    Design of record: bounty version-skew-recovery-affordance."
    RELEASE_SHA=$(git rev-parse HEAD)
    echo "Release SHA: $RELEASE_SHA"

    # (2) npm publish core.
    cd viewmodel-shell
    npm publish
    curl -s https://registry.npmjs.org/@ashley-shrok/viewmodel-shell \
      | python3 -c "import sys,json; print(json.load(sys.stdin)['dist-tags']['latest'])"
    # Expect: 9.0.0

    # (3) NuGet publish core.
    cd ../viewmodel-shell-dotnet
    dotnet pack -c Release
    dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.9.0.0.nupkg \
      --api-key "$NUGET_API_KEY" --source https://api.nuget.org/v3/index.json
    curl -s https://api.nuget.org/v3-flatcontainer/ashleyshrok.viewmodelshell/index.json \
      | python3 -c "import sys,json; print(json.load(sys.stdin)['versions'][-1])"
    # Expect: 9.0.0 (may 404 for 5-15 min; push receipt is authoritative)

    # (4) NuGet publish Markdown companion.
    cd Markdown
    dotnet pack -c Release
    # (Substitute the actual new companion version in the file name.)
    dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.Markdown.<new-version>.nupkg \
      --api-key "$NUGET_API_KEY" --source https://api.nuget.org/v3/index.json
    curl -s https://api.nuget.org/v3-flatcontainer/ashleyshrok.viewmodelshell.markdown/index.json \
      | python3 -c "import sys,json; print(json.load(sys.stdin)['versions'][-1])"

    # (5) Tag + push tag.
    cd ../..
    git tag -a v9.0.0 $RELEASE_SHA -m "viewmodel-shell 9.0.0"
    git push origin v9.0.0

    # (6) Advance main (if not on main branch, merge/rebase per AGENTS.md).
    # Verify.
    git merge-base --is-ancestor v9.0.0 main && echo "OK: v9.0.0 reachable from main" \
      || echo "STRANDED — reconcile per AGENTS.md"

    # (7) Push main if it advanced.
    git push origin main

    # (8) Announce in #vms-announcements — VERIFY room ID first via Nelly's /joined_rooms.
    # Post release line with the SKEW-01..10 IDs + BREAKING notice.

    # (9) DM Molly with the Metis flag-closure confirmation per <must_haves>.

    # (10) Kill Plan 29-09 verification server.
    kill $(cat demo/VersionSkewVerification-bun/server.pid) 2>/dev/null; echo cleaned
    ```

    Operator reports: bump SHA, both curl-verified registry latest values, tag push output, main-advance ancestor check, announce message text, Molly DM ref, PID cleanup output.
  </how-to-verify>
  <resume-signal>
    Operator posts: `release complete — v9.0.0 live on npm + NuGet + Markdown companion; main advanced; announced; Molly DM'd` (or lists any halts encountered).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Record full release evidence in SUMMARY</name>
  <files>.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-12-SUMMARY.md</files>
  <read_first>
    - Task 4's operator report (captured during execution)
  </read_first>
  <action>
    Write the SUMMARY per `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
    - Auth precheck output (redacted).
    - Pre-bump gate exit codes.
    - Bump commit SHA.
    - npm publish output (last 20 lines).
    - NuGet publish output for core + Markdown companion (last 20 lines each).
    - Both curl-registry-latest outputs.
    - Tag creation output.
    - Main-ancestor verification (STRANDED reconciliation if it happened).
    - Announce message text + Matrix room ref.
    - Molly DM confirmation ref (or 'unreachable — will follow up' note).
    - Companion NuGet republish evidence (path + version + push receipt).
    - PID cleanup output.
    - Final verdict: **PHASE 29 RELEASED** with the release SHA, tag, and both registry versions.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-12-SUMMARY.md &amp;&amp; grep -c 'PHASE 29 RELEASED\|9.0.0' .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-12-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY file exists with `PHASE 29 RELEASED` verdict and 9.0.0 version.
    - Every registry verification captured (npm + NuGet core + NuGet Markdown companion).
    - Molly DM ref captured or unreachable-note recorded.
    - PID cleanup output recorded.
  </acceptance_criteria>
  <done>
    Phase 29 release evidence complete. v9.0.0 lives on both registries; tag + main + announce all landed; Molly notified; verification server cleaned up.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Credentials leaving `.env` | Task 1 sources `.env` into the environment; auth precheck must NOT leak credentials in SUMMARY output. Redact via `NPM_TOKEN=<redacted>` etc. |
| Version-bump-vs-registry-push atomicity | AGENTS.md forbids bump-without-publish (stranded silence). This plan wraps all steps in the same operator session — a halt at any step leaves the tree bumped but the registries unshipped, requiring operator manual completion or revert. |
| Main-advance verification | The missed-2-releases-2026-06-14 lesson: tag + registry publish is not enough; `main` must contain the release commit or a clone gets stale code. Task 4's Step 6 enforces. |
| Companion NuGet binary compat | AGENTS.md's Amelia/Athena lesson: a core-major bump without companion republish silently breaks downstream consumers on first-real-use with MissingMethodException. Task 3 bumps the companion; Task 4 republishes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-38 | Repudiation | Registry publish silently fails | mitigate | curl-registry-latest verification after each publish; halt on mismatch. |
| T-29-39 | Denial of Service | Companion republish skipped | mitigate | Task 3 bumps the companion csproj; Task 4 publishes; SUMMARY records both. If skipped, downstream consumers upgrading core hit MissingMethodException. |
| T-29-40 | Repudiation | Stranded main branch | mitigate | Task 4 Step 6 verifies `git merge-base --is-ancestor`. |
| T-29-41 | Information Disclosure | Credentials in SUMMARY | mitigate | Explicit redaction in Task 1 + Task 5. |
</threat_model>

<verification>
Per-task verifications + Task 4's operator-report + Task 5's SUMMARY. The release is complete when the SUMMARY records `PHASE 29 RELEASED`.
</verification>

<success_criteria>
- Auth precheck passes inline.
- Pre-bump gate re-run green.
- Versions bumped surgically (core npm + core NuGet + Markdown companion + docs date-finalized).
- Operator publishes core npm + core NuGet + Markdown companion NuGet.
- Both registries serve 9.0.0 (core) verified via curl.
- Companion NuGet republished with bumped patch.
- Tag v9.0.0 created + pushed.
- Main advanced (verified `git merge-base --is-ancestor v9.0.0 main`).
- Announce posted in #vms-announcements.
- Molly DM'd with Metis flag-closure confirmation.
- Verification server PID cleaned up.
- SUMMARY records `PHASE 29 RELEASED` with full evidence.
</success_criteria>

<output>
Create `.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-12-SUMMARY.md` per Task 5.
</output>
