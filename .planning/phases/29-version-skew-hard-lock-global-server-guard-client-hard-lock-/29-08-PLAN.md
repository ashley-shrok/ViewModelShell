---
phase: 29-version-skew-hard-lock-global-server-guard-client-hard-lock-
plan: 08
type: execute
wave: 6
depends_on: ["29-03", "29-06"]
files_modified:
  - viewmodel-shell/agent-skill.md
  - viewmodel-shell-dotnet/AgentSkill.md
autonomous: true
requirements: [SKEW-10]

must_haves:
  truths:
    - "`viewmodel-shell/agent-skill.md` (the CANONICAL source) — the existing '## Client build / version skew' section (currently lines 125-128) is REVISED to reflect Phase 29's shipped behavior: (1) mention the SERVER-side global filter now enforces on BOTH GET + POST (was POST-only in v3.8.0); (2) mention the CLIENT-side sends X-VMS-Client-Build on BOTH GET + POST (was POST-only); (3) note that browser consumers see a hard-lock modal on skew (opt-out via ShellOptions.onVersionSkew:'custom'); (4) explicitly note the agent-facing wire behavior is UNCHANGED — an agent driving the wire cold STILL sees 400 stale_client on mismatch and STILL reloads to consume a fresh vm/state (the modal is a browser affordance; agents don't need it)."
    - "The existing stale_client row in the errors table (currently line 119) is REVISED to name the new source: 'The server rejected your request because the X-VMS-Client-Build header you sent no longer matches the server's current deployed build — the guard now enforces on GET + POST (was POST-only in v3.8.0).' Preserve the rest of the row's copy verbatim (fix action: reload; nothing was applied)."
    - "`viewmodel-shell-dotnet/AgentSkill.md` is BYTE-IDENTICAL to the TS source after the edits — the parity gate `parity/check-skill.ts` diffs both source files AND the served HTTP bodies on the HelpDesk twins. See AGENTS.md §Documentation twins for the maintainer rule."
    - "The agent-facing wire behavior is UNCHANGED (agents get the same 400 stale_client on mismatch as v3.8.0). What IS new is the SERVER-side scope (guards GETs too) and the CLIENT-side header attachment (attached on GETs too). This is why the agent-skill's actionable copy for agents doesn't materially change — the WIRE contract is stable; the doc is updated to STATE the new coverage."
    - "`bun run parity/check-skill.ts` (or whichever entry parity/ uses to gate the skill diff — grep parity/ for check-skill) exits 0. If the file has a different name in shipped parity, adjust; the canonical parity script must pass."
    - "The bunched edits are grouped in ONE section revision (not scattered across the file). Placement: adjacent to the existing 'Client build / version skew' section per PATTERNS §Documentation twins. The section header stays the same ('## Client build / version skew') — same anchor for existing cross-references from other docs."
    - "`grep -c 'GET + POST' viewmodel-shell/agent-skill.md` returns at least 2 (the guard-scope note + the header-attachment note). If the previous text used any language implying POST-only, that language MUST be removed or updated."
    - "`grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md` returns exactly 1 (mentioning the opt-out for browser consumers, in the browser-affordance note; agents don't set this, but the doc explains it exists for context)."
    - "Both files byte-identical: `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` produces zero output."
    - "Byte-parity via load-bearing string grep — NOT just aggregate byte-count: after byte-copy, `grep -c 'GET + POST' viewmodel-shell-dotnet/AgentSkill.md` MUST equal `grep -c 'GET + POST' viewmodel-shell/agent-skill.md` AND `grep -c 'onVersionSkew' viewmodel-shell-dotnet/AgentSkill.md` MUST equal `grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md`. A partial-parity assertion (only checking one string) is a class-2 gotcha #9 defect: the docs could drift on the OTHER load-bearing string while the check prints 'passed'. Grep for both strings explicitly."
  artifacts:
    - path: "viewmodel-shell/agent-skill.md"
      provides: "Revised Client build / version skew section reflecting Phase 29's GET+POST guard scope + hard-lock modal + opt-out"
      contains: "GET + POST"
    - path: "viewmodel-shell-dotnet/AgentSkill.md"
      provides: "Byte-identical copy of the edited agent-skill.md"
      contains: "GET + POST"
  key_links:
    - from: "Revised agent-skill.md Client build / version skew section"
      to: "viewmodel-shell-dotnet/AgentSkill.md"
      via: "parity/check-skill.ts diffs both source files AND HTTP bodies served by helpdesk twins"
      pattern: "byte-identical"
    - from: "Doc updates reflect shipped behavior"
      to: "Plan 29-03 (client header on GET) + Plan 29-02/04 (server guard on GET+POST) + Plan 29-06 (browser modal)"
      via: "the doc reads the shipped code as ground truth, NOT the CONTEXT/design docs"
      pattern: "GET + POST|hard-lock modal|onVersionSkew"
---

<objective>
Update the CANONICAL `viewmodel-shell/agent-skill.md` Client build / version skew section to reflect Phase 29's shipped behavior (server-side guard on GET+POST; client header on GET+POST; browser hard-lock modal; onVersionSkew opt-out for browser consumers), then byte-copy to `viewmodel-shell-dotnet/AgentSkill.md`. Verify `parity/check-skill.ts` green, AND grep-cross-verify BOTH load-bearing strings (`GET + POST` and `onVersionSkew`) in the .NET twin equal the TS source (per AGENTS.md gotcha #9 — a partial-parity check is a class-2 defect). Wire behavior for agents is UNCHANGED — the doc communicates the NEW coverage of the SAME wire contract.

Purpose: Per AGENTS.md §Documentation twins maintainer rule, any wire-shape or documented-behavior change MUST update the skill in the same change. Phase 29 doesn't add a new wire field, but it CHANGES the DOCUMENTED behavior of the shipped stale_client error code (GETs also fail-close now; the client also sends the header on GETs). Agents driving VMS apps cold need this info to reason about GET-side behavior. The .NET twin is a byte-parity requirement — the parity gate fails the build on drift.

Output: agent-skill.md revised; AgentSkill.md byte-identical; parity gate green; load-bearing strings equal on both sides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-CONTEXT.md
@.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-PATTERNS.md
@AGENTS.md
</context>

<interfaces>
Dependencies from Plans 29-03 + 29-06 (both MUST be committed):
- 29-03: client sends X-VMS-Client-Build on GETs + hard-lock retires silent auto-reload
- 29-06: BrowserAdapter.showSkewLock modal DOM + Reload button

Analogs to model against:

1. `viewmodel-shell/agent-skill.md` existing '## Client build / version skew' section (currently lines 125-128 per CONTEXT/PATTERNS — verify by grep on read):
   ```
   Optional, opt-in. When the app enables versioning, every response carries a `serverBuild` string ...
   ```
   The section stays; content is revised to describe Phase 29's shipped scope.

2. `viewmodel-shell/agent-skill.md` errors-table row for `stale_client` (currently line 119) — revised to mention the guard now enforces on GET + POST.

3. `parity/check-skill.ts` — the parity gate that diffs both source files AND the served HTTP bodies on the HelpDesk twins. This plan does not modify the gate; it makes the gate pass with the edited files.

CRITICAL: The `## Non-blocking actions (blocking:false)` section at ~line 137 (per shipped structure) is out of scope; do not touch. The `## Auth` section (~line 135) is out of scope. Only the errors table row + the Client build / version skew section are touched.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Revise Client build / version skew section + errors-table stale_client row in agent-skill.md</name>
  <files>viewmodel-shell/agent-skill.md</files>
  <read_first>
    - viewmodel-shell/agent-skill.md (READ the ENTIRE file — you need to see: (1) the errors table's stale_client row at ~line 119 for the row-level revision; (2) the '## Client build / version skew' section at ~lines 125-128 for the section-level revision; (3) surrounding structure to preserve heading anchors + section ordering)
    - viewmodel-shell/src/index.ts (READ the shipped checkVersionSkew + stale_client catch arm + load() header block Plan 29-03 landed — the doc's actionable statements must match the shipped code as ground truth)
    - viewmodel-shell-dotnet/Versioning.cs (READ the shipped ShellVersionGuardFilter Plan 29-02 landed — the doc's server-side statements must match the shipped behavior)
    - .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-PATTERNS.md (§Documentation twins — the specific line references to revise; the section stays with the same heading anchor)
    - AGENTS.md (§Agent discoverability — the maintainer rule that any wire-shape or documented-behavior change updates the skill in the same change)
  </read_first>
  <action>
    Perform TWO edits to `viewmodel-shell/agent-skill.md`:

    (a) Revise the errors-table row for `stale_client` (~line 119). Current row text is roughly:

    > `stale_client` | Your request advertised an `X-VMS-Client-Build` header that no longer matches the server's current deployed build. The mutation was rejected **before your `_state` was read — nothing was applied.** The fix is to reload to the current app (re-`GET` the endpoint for a fresh `vm`/`state`), not to retry the same request. See *Client build / version skew*.

    Revise to (adjust to match the exact wording of the shipped row for style consistency):

    > `stale_client` | Your request advertised an `X-VMS-Client-Build` header that no longer matches the server's current deployed build. The request was rejected **before any controller runs — on a POST, your `_state` is never deserialized and nothing is applied.** As of v9.0.0 the guard enforces on **both GET and POST** (was POST-only in v3.8.0), so a GET refresh with a stale header also fails-closed. The fix is to reload to the current app (re-`GET` the endpoint for a fresh `vm`/`state`), not to retry the same request. See *Client build / version skew*.

    Three notable changes: "The mutation was rejected" → "The request was rejected" (GET requests are not mutations); the pre-guard timing is described in GET-coherent form ("before any controller runs — on a POST, your `_state` is never deserialized and nothing is applied") so a GET reader isn't left wondering about a POST-specific `_state` that doesn't apply to their situation; one new clause added ("As of v9.0.0 the guard enforces on both GET and POST"); the rest is preserved.

    (b) Revise the '## Client build / version skew' section (~lines 125-128). Current section is roughly:

    > Optional, opt-in. When the app enables versioning, every response carries a `serverBuild` string (the client bundle the server currently deploys), and you may advertise the build you are running by sending an `X-VMS-Client-Build: <your-build-id>` header on every action POST.
    >
    > - **Detection.** On any successful response, if you sent a build and `serverBuild` differs from it, the server has rolled forward while you kept running an old bundle. Reload to the current app (re-`GET` the endpoint) so you are driving the current tree.
    > - **Fail-closed guard.** If you send a mismatching `X-VMS-Client-Build`, a *mutating* action is rejected with `ok: false`, HTTP 400, `code: "stale_client"` — **before** your `_state` is deserialized, so nothing is applied. Do not retry the same request against the same build; reload first. If you do NOT send the header, no request is ever rejected on this basis (the guard only fires for a client that advertised a stale build).

    Revise to:

    > Optional, opt-in. When the app enables versioning, every response carries a `serverBuild` string (the client bundle the server currently deploys), and you may advertise the build you are running by sending an `X-VMS-Client-Build: <your-build-id>` header on **every** request — GET and POST alike (as of v9.0.0; v3.8.0 shipped POST-only).
    >
    > - **Detection.** On any successful response, if you sent a build and `serverBuild` differs from it, the server has rolled forward while you kept running an old bundle. Reload to the current app (re-`GET` the endpoint) so you are driving the current tree.
    > - **Fail-closed guard.** If you send a mismatching `X-VMS-Client-Build`, the request is rejected with `ok: false`, HTTP 400, `code: "stale_client"` — **before any controller runs** (so on a POST, your `_state` is never deserialized and nothing is applied). As of v9.0.0 the guard enforces on **GET + POST** (was POST-only in v3.8.0): a GET refresh with a stale header also fails-closed. Do not retry the same request against the same build; reload first. If you do NOT send the header, no request is ever rejected on this basis (the guard only fires for a client that advertised a stale build).
    > - **Browser hard-lock.** The shipped `BrowserAdapter` renders a non-dismissible modal on skew (both signals — a `serverBuild` mismatch on any response AND a `stale_client` rejection on any request) with a single [Reload] button. Browser consumers with a pre-existing custom skew affordance can opt out via `ShellOptions.onVersionSkew: "custom"` (restores v3.8.0 behavior: signal via `onError`, no framework modal). This is a browser-only affordance — an agent driving the wire cold never sees the modal; the actionable behavior on `stale_client` is the same as v3.8.0 (reload, don't retry).

    CRITICAL:
    - Preserve the section heading `## Client build / version skew` exactly — existing cross-references from other docs may anchor to it.
    - Preserve the shipped voice + reading level (agent-facing, not developer-facing).
    - No mention of `AddVmsShellVersioning` (that's a developer-side helper, not agent-facing); no mention of `createVersionGuard` (same reason). The doc talks about wire behavior, not source-code identifiers.
    - Do NOT touch any other section (Auth, Polling, Non-blocking actions, etc.).
    - **GET-coherence check:** After editing, re-read the Fail-closed guard bullet aloud as if you were a reader hitting the doc via a GET query. The phrase describing pre-controller timing MUST parse for BOTH GET and POST readers — the shipped copy achieves this via "before any controller runs (so on a POST, your `_state` is never deserialized and nothing is applied)". A phrase like "before your `_state` is deserialized on a POST" as a standalone dangler is INCOHERENT for a GET-reader (they have no `_state`). Fix before shipping if you find yourself with a POST-specific note dangling on a GET-covered section.

    Verify:
    - `grep -c 'GET + POST' viewmodel-shell/agent-skill.md` returns at least 2 (the guard-scope + the header-attachment mentions).
    - `grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md` returns exactly 1 (Browser hard-lock bullet).
    - `grep -c 'as of v9.0.0' viewmodel-shell/agent-skill.md` returns at least 2 (case-insensitive; the doc's own style).
    - `grep -c 'POST-only in v3.8.0' viewmodel-shell/agent-skill.md` returns at least 1.
  </action>
  <verify>
    <automated>grep -c 'GET + POST' viewmodel-shell/agent-skill.md &amp;&amp; grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md &amp;&amp; grep -c 'BrowserAdapter' viewmodel-shell/agent-skill.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GET + POST' viewmodel-shell/agent-skill.md` returns at least 2.
    - `grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md` returns exactly 1.
    - `grep -c 'v9.0.0' viewmodel-shell/agent-skill.md` returns at least 2 (both edits mention the version).
    - `grep -c 'POST-only in v3.8.0' viewmodel-shell/agent-skill.md` returns at least 1 (both edits or one; both is fine).
    - `grep -c '## Client build / version skew' viewmodel-shell/agent-skill.md` returns exactly 1 (heading anchor preserved).
    - `grep -c 'stale_client' viewmodel-shell/agent-skill.md` INCREASED by 0 or more vs pre-plan baseline (the token count may stay the same or grow slightly; grep confirms no accidental removal).
    - **GET-coherence gate:** `grep -c 'before your `_state` was read' viewmodel-shell/agent-skill.md` returns exactly 0 (the pre-revision POST-only phrasing must NOT remain in the file — the revised phrasing uses "before any controller runs" as the anchor). AND `grep -c 'before any controller runs' viewmodel-shell/agent-skill.md` returns at least 1 (the new anchor phrase that parses for both GET and POST readers).
    - No modification to sections OTHER than the errors table row + Client build / version skew section — a spot-check via `grep -c '^## ' viewmodel-shell/agent-skill.md` returns the same count as pre-plan baseline (no accidental section-header edits).
  </acceptance_criteria>
  <done>
    agent-skill.md Client build / version skew section revised; errors-table stale_client row updated. Actionable copy for agents preserved (wire behavior unchanged); coverage note (GET + POST) added. Pre-guard timing described in GET-coherent form (parses for both GET and POST readers). Section heading anchor preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Byte-copy edited agent-skill.md to viewmodel-shell-dotnet/AgentSkill.md + verify parity + load-bearing string cross-parity</name>
  <files>viewmodel-shell-dotnet/AgentSkill.md</files>
  <read_first>
    - viewmodel-shell/agent-skill.md (READ the just-edited canonical source in full — the .NET twin is a byte-for-byte copy)
    - viewmodel-shell-dotnet/AgentSkill.md (READ current state to confirm the pre-plan file is what we're overwriting; grep parity/ for check-skill to find the parity gate)
    - parity/check-skill.ts (READ the parity gate — the exact diff logic to understand what MUST match)
    - AGENTS.md (§gotcha #9 — the class-2 defect where a diff normalizes away a load-bearing property; parity byte-diff is generic but this task adds targeted load-bearing string cross-parity as belt-and-braces so a subtle diff-configuration change on either side cannot silently drift the load-bearing tokens)
  </read_first>
  <action>
    Byte-copy `viewmodel-shell/agent-skill.md` to `viewmodel-shell-dotnet/AgentSkill.md`. Use `cp` (or an equivalent atomic file replacement) to ensure a true byte-copy:

    ```bash
    cp viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md
    ```

    Then run the parity gate. Per PATTERNS + AGENTS.md:

    ```bash
    export PATH="$HOME/.dotnet:$PATH"
    bun run parity/check-skill.ts
    ```

    Then cross-verify the load-bearing strings on BOTH sides. Per AGENTS.md gotcha #9, a partial-parity assertion (checking only ONE load-bearing string) is a class-2 defect — the docs could drift on the OTHER load-bearing string while the check prints "passed". Grep both `GET + POST` (the guard-scope note) AND `onVersionSkew` (the opt-out affordance note) — both are critical enough that a silent drift on either would materially mislead an agent driving the wire.

    ```bash
    ts_get=$(grep -c 'GET + POST' viewmodel-shell/agent-skill.md)
    net_get=$(grep -c 'GET + POST' viewmodel-shell-dotnet/AgentSkill.md)
    echo "GET + POST: TS=$ts_get NET=$net_get"
    [ "$ts_get" = "$net_get" ] || { echo "FAIL: GET + POST count drift"; exit 1; }

    ts_ovs=$(grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md)
    net_ovs=$(grep -c 'onVersionSkew' viewmodel-shell-dotnet/AgentSkill.md)
    echo "onVersionSkew: TS=$ts_ovs NET=$net_ovs"
    [ "$ts_ovs" = "$net_ovs" ] || { echo "FAIL: onVersionSkew count drift"; exit 1; }
    ```

    (`BrowserAdapter` count parity is also worth grepping if the revised section mentions it — extend the pattern if so.)

    Verify:
    - `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` — exits 0 (byte-identical).
    - `bun run parity/check-skill.ts` — exits 0.
    - `wc -c` on both files matches (identical byte counts).
    - Cross-parity grep counts for `GET + POST` and `onVersionSkew` are equal on both sides.
    - If parity/check-skill.ts also verifies served HTTP body byte-parity on running HelpDesk twins, ensure the demos are running (may need to start them; check the parity script's requirements). If the check is source-only, no server needed.
  </action>
  <verify>
    <automated>cp viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md &amp;&amp; diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md &amp;&amp; wc -c viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md &amp;&amp; test "$(grep -c 'GET + POST' viewmodel-shell/agent-skill.md)" = "$(grep -c 'GET + POST' viewmodel-shell-dotnet/AgentSkill.md)" &amp;&amp; test "$(grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md)" = "$(grep -c 'onVersionSkew' viewmodel-shell-dotnet/AgentSkill.md)" &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; bun run parity/check-skill.ts 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` produces zero output (byte-identical).
    - `wc -c viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` returns identical byte counts on both lines.
    - `bun run parity/check-skill.ts` exits 0.
    - **Load-bearing string cross-parity (class-2 gotcha #9 defense):**
      - `grep -c 'GET + POST' viewmodel-shell-dotnet/AgentSkill.md` returns the same count as `grep -c 'GET + POST' viewmodel-shell/agent-skill.md`.
      - `grep -c 'onVersionSkew' viewmodel-shell-dotnet/AgentSkill.md` returns the same count as `grep -c 'onVersionSkew' viewmodel-shell/agent-skill.md`.
      - Both counts must be EQUAL (not merely nonzero) — a partial-parity assertion where one side has 2 and the other has 1 would silently indicate drift on a load-bearing token.
  </acceptance_criteria>
  <done>
    AgentSkill.md byte-copied from canonical source. Parity gate green. Load-bearing string counts (`GET + POST` AND `onVersionSkew`) equal on both sides — class-2 gotcha #9 partial-parity defect closed. Both files satisfy the AGENTS.md byte-parity maintainer rule.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documented behavior vs shipped behavior | The doc's actionable statements must match the shipped code. Task 1's revisions describe behavior Plans 29-02/03/04/06 shipped; if any of those drifted from the description, the doc would be lying. Cross-check: read the shipped code, then the doc; the doc's statements must be true. |
| Byte-parity of the twin files | The parity gate diffs both source files. A whitespace change on one side (e.g., trailing newline, encoding difference) would fail the gate. Task 2's `cp` is a true byte-copy — safer than a hand re-edit. |
| Load-bearing string cross-parity | Byte-diff catches ANY change; targeted string counts catch DRIFT on load-bearing tokens specifically. Both are checked in Task 2 — the diff proves general byte-parity; the counts prove the specific tokens agents grep for stay equal on both sides. This is the gotcha #9 class-2 mitigation applied to the docs twin. |
| Agent-facing wire contract | The wire contract for `stale_client` is UNCHANGED (same shape, same code). The doc communicates the NEW COVERAGE of the same contract. Agents that already implement `stale_client` handling continue to work byte-for-byte; new agents get the fuller picture. |
| GET-coherence of pre-guard timing description | A POST-specific phrase dangling on a GET-covered section (e.g. "before your _state is deserialized on a POST") would confuse a GET reader — they have no `_state`. Task 1's revised phrasing ("before any controller runs (so on a POST, your `_state` is never deserialized and nothing is applied)") parses cleanly for both GET and POST readers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-26 | Repudiation | Doc describes behavior that doesn't ship | mitigate | Read-first-then-write: Task 1 reads the shipped code before revising the doc. Task 2's parity gate confirms the twin is byte-identical. |
| T-29-27 | Repudiation | Byte-parity drift between twin files | mitigate | Task 2's `diff` + `wc -c` + `bun run parity/check-skill.ts` all exit 0 confirm byte-parity. |
| T-29-27a | Repudiation | Silent drift on ONE load-bearing token while diff shows byte-parity everywhere else | mitigate | Task 2's explicit `grep -c` cross-parity on BOTH `GET + POST` AND `onVersionSkew` (partial-parity defense per gotcha #9). Extending to any future load-bearing token added to the revised section is documented in the acceptance criteria pattern. |
| T-29-28 | Information Disclosure | Doc leaks implementation details | accept | The doc mentions BrowserAdapter as a name (already mentioned elsewhere in the file); onVersionSkew as a name (a public ShellOptions field). No internal identifiers (AddVmsShellVersioning, createVersionGuard, ShellVersionGuardFilter) appear per the "wire behavior, not source-code identifiers" rule in Task 1. |
</threat_model>

<verification>
Task 2's parity gate + load-bearing string cross-parity are the primary verification. Byte-diff + wc -c are the intra-file consistency proof. Cross-check with actual agent-driving behavior — running the parity fixtures from Plan 29-07 exercises the wire-level behavior the doc describes, so a doc claim that doesn't match reality would surface in Plan 29-07's fixtures too.
</verification>

<success_criteria>
- agent-skill.md Client build / version skew section revised to describe Phase 29's GET+POST guard scope + hard-lock modal + opt-out, with GET-coherent pre-guard timing phrasing.
- Errors table stale_client row updated to note the guard now enforces on GET + POST.
- AgentSkill.md byte-identical to agent-skill.md.
- Load-bearing string counts (`GET + POST` AND `onVersionSkew`) equal on both sides.
- `bun run parity/check-skill.ts` exits 0.
</success_criteria>

<output>
Create `.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-08-SUMMARY.md` recording: exact line ranges revised in agent-skill.md, `diff` exit code + `wc -c` output confirming byte-parity, load-bearing string counts on both sides for `GET + POST` + `onVersionSkew`, and parity gate exit output.
</output>
