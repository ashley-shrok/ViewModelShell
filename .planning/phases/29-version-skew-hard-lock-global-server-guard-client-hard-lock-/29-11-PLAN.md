---
phase: 29-version-skew-hard-lock-global-server-guard-client-hard-lock-
plan: 11
type: execute
wave: 9
depends_on: ["29-10"]
files_modified:
  - CHANGELOG.md
  - MIGRATION.md
  - AGENTS.md
autonomous: true
requirements: [SKEW-10]

must_haves:
  truths:
    - "CHANGELOG.md gains a new `## 9.0.0 — <YYYY-MM-DD> (npm + NuGet aligned) — BREAKING` section at the top of the Added/Notes stream. Date placeholder stays `<YYYY-MM-DD>` — Plan 29-12 replaces at bump time (banked lesson from Phase 27's 27-10 → 27-11 handoff)."
    - "CHANGELOG entry documents the TWO BREAKING behavior changes: (a) server-side global filter now enforces on BOTH GET + POST (was per-controller opt-in POST-only in v3.8.0); (b) browser hard-lock modal replaces silent auto-reload — the user must click Reload to consent. AND the ADDITIVE additions: (c) `Adapter.showSkewLock?` capability verb; (d) `ShellOptions.onVersionSkew?: \"default\" | \"custom\"` opt-out flag; (e) `createVersionGuard` TS-server factory; (f) `ShellVersionGuardFilter` .NET IActionFilter (self-registered by `AddVmsShellVersioning`); (g) client attaches `X-VMS-Client-Build` header on GETs (was POST-only)."
    - "CHANGELOG explicitly names the SKEW-XX requirement IDs (SKEW-01..10) for traceability + points at the design of record path (`~/.claude/identities/vicky/bounties/version-skew-recovery-affordance/mechanism-sketch.md` + Ashley post-tasting sign-off 2026-08-02)."
    - "MIGRATION.md gains a new `## Upgrading to v9.0.0` section at the top of the upgrade-notes stream. Content: (a) opening note calling out this is a MAJOR bump for two documented BEHAVIOR changes (guard now global on GET+POST; auto-reload retired for hard-lock modal); (b) 'What consumers get for free' — the fail-closed guard on GET routes closes the pre-9.0.0 silent-partial-coverage gap; consumers already calling `AddVmsShellVersioning(...)` (or wiring `createAction`) get the new behavior automatically; (c) 'What consumers might need to do' — TS-server subpath consumers who want GET-side enforcement wire `createVersionGuard({ currentBuild: BUILD_ID })(getHandler)` around their GET route (one line per route); browser consumers with a custom skew affordance (Kitsune, PBMInvoices, other Pantheon apps) set `onVersionSkew: 'custom'` in ShellOptions to preserve v3.8.0 behavior; (d) 'Nothing to do for the common case' — the vast majority of consumers see the new default (modal fires instead of silent reload) and this is the DESIRED behavior; no code change; (e) bump-both-packages code block per Phase 27 template; (f) usage example — showing the createVersionGuard wire-around for GET + the onVersionSkew opt-out pattern; (g) A note on the .NET side: `AddVmsShellVersioning(...)` now co-registers BOTH filters (stamp + guard) from a single call — consumers who were manually adding `ShellVersionResultFilter` continue to work (dedup-guarded) but the manual registration is now redundant."
    - "AGENTS.md gains TWO edits: (a) a new gotcha #10 (or the next available number — grep AGENTS.md for the current highest gotcha #) documenting the v9.0.0 auto-reload retirement + hard-lock modal + onVersionSkew opt-out (a short entry — one paragraph + code snippet); (b) the existing gotcha #9 (parity testing) is EXTENDED with a Phase-29 addendum note referencing the fixture `agt-get-build-stale` from `parity/fixtures/helpdesk.json` (owned by Plan 29-07). The doc string MUST match the fixture that actually exists — Task 3 cross-verifies via grep that the fixture ID is still present in helpdesk.json so the doc doesn't hard-code a phantom fixture reference."
    - "All 3 doc edits STAGE the file changes but do NOT bump versions or commit. Version bumps happen in Plan 29-12 (release) atomically with the release commit."
    - "The green-tree gate from Plan 29-10 remains green after these doc edits — no code changes here. Verify by re-running `cd viewmodel-shell && npm run build && npm run check:test-types` briefly at the end (guardrail; if a doc-line-numbered command misread and touched a code file, this catches it)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v9.0.0 BREAKING Added/Notes entry with date placeholder for Plan 29-12 to finalize"
      contains: "9.0.0"
    - path: "MIGRATION.md"
      provides: "v9.0.0 upgrade section with BOTH behavior-change guidance AND additive-additions guidance + usage examples"
      contains: "v9.0.0"
    - path: "AGENTS.md"
      provides: "New gotcha for v9.0.0 hard-lock behavior + Phase 29 addendum to existing gotcha #9 with cross-verified fixture reference"
      contains: "onVersionSkew"
  key_links:
    - from: "CHANGELOG.md + MIGRATION.md v9.0.0 sections"
      to: "Plan 29-12 (release — finalizes the date placeholder + commits atomically)"
      via: "date placeholder <YYYY-MM-DD> handoff pattern from Phase 27"
      pattern: "<YYYY-MM-DD>"
    - from: "AGENTS.md gotcha #9 Phase 29 addendum"
      to: "Plan 29-07's helpdesk.json GET-side fixtures (agt-get-build-*)"
      via: "the addendum names the specific fixture as the tripwire — Task 3 cross-verifies the fixture ID actually exists in the JSON via grep to prevent doc-vs-fixture drift"
      pattern: "agt-get-build-stale"
---

<objective>
Stage doc edits for v9.0.0: CHANGELOG.md (BREAKING notice + additive list + SKEW-01..10 IDs), MIGRATION.md (behavior-change guidance + opt-out example + wire-around example), AGENTS.md (new gotcha for hard-lock behavior + Phase 29 addendum to existing gotcha #9 with a cross-verified fixture reference). Follow Phase 27/28's template — this plan stages docs with a `<YYYY-MM-DD>` date placeholder; Plan 29-12 (release) finalizes the date + commits atomically with the version bump.

Purpose: Per AGENTS.md release rule, docs land in the SAME commit as the version bump; staging separately (this plan) means we know exactly what to bump-commit in Plan 29-12 without confusion or accidental doc-only commits. The BREAKING notice + the opt-out example are load-bearing for consumers — they need to know (a) the default behavior changed on both server + client, (b) how to preserve v3.8.0 behavior if they have their own affordance, (c) what the ONE line to add for GET-side enforcement is. The AGENTS.md gotcha #9 addendum hard-codes a fixture ID (`agt-get-build-stale`) owned by Plan 29-07 — Task 3 cross-verifies that fixture actually exists in the JSON so the doc doesn't reference a phantom.

Output: 3 files with staged v9.0.0 content; date placeholders intact for Plan 29-12; no version bumps; no commits (git is operator-driven per AGENTS.md); AGENTS.md fixture reference cross-verified against `parity/fixtures/helpdesk.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-CONTEXT.md
@AGENTS.md
</context>

<interfaces>
Analogs to model against:

1. `CHANGELOG.md` — read the top-of-file structure for the entry template. Phase 28's `## 8.2.0` entry is the most recent analog; Phase 27's `## 8.1.0` for pre-major-bump precedent; Phase 24-25's `## 8.0.0` for the most recent MAJOR bump precedent (BREAKING notice format).

2. `MIGRATION.md` — read the top-of-file structure. Phase 28's `## Upgrading to v8.2.0` for additive template; Phase 24-25's `## Upgrading to v8.0.0` for BREAKING template (behavior-change guidance + opt-out example pattern).

3. `AGENTS.md` — read the "Critical gotchas" section top to find the current highest gotcha number (grep for `^\d\. `). The new gotcha for v9.0.0 lands adjacent to the version-related gotcha #6 or #8 (the version-check + null-omission cluster).

4. `parity/fixtures/helpdesk.json` — the Plan 29-07 fixture landing. Task 3's cross-verification greps this file for `agt-get-build-stale` to confirm the AGENTS.md gotcha #9 addendum's hard-coded ID actually exists in the fixture set. If it does not (Plan 29-07 renamed, dropped, or scoped it differently), the AGENTS.md addendum's fixture reference must be updated to match reality.

CRITICAL: This plan does NOT modify any source-code file (viewmodel-shell/src/, viewmodel-shell-dotnet/, demo/, parity/). Only three docs are touched. Verify no accidental cross-file edit at the end via `git status`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Stage CHANGELOG.md v9.0.0 BREAKING entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (READ the top 100 lines — you need to see: (1) the current top entry Phase 28 landed for 8.2.0 as byte-aligned template; (2) any prior BREAKING/MAJOR entries — Phases 24-25 for 8.0.0 or earlier — for the exact wording of the BREAKING notice; (3) the section-separator convention)
    - .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-CONTEXT.md (§decisions + §domain for the exact behavior-change list + the SKEW-XX requirement IDs; §requirements_map for the canonical SKEW-XX definitions)
  </read_first>
  <action>
    Insert a new section at the top of the Added/Notes stream in CHANGELOG.md. Content:

    ```markdown
    ## 9.0.0 — <YYYY-MM-DD> (npm + NuGet aligned) — BREAKING

    Version-skew hard-lock: close two fleet-foundational gaps in v3.8.0's version-skew primitive. **Wire is UNCHANGED (no new fields or codes); this is a BEHAVIOR-only major bump.**

    ### Breaking behavior changes (both preserve opt-out)

    - **Server-side global filter (SKEW-01):** the `X-VMS-Client-Build` guard now enforces on BOTH GET + POST, self-registered by `AddVmsShellVersioning(...)`. The pre-9.0.0 per-controller `ActionPayload<T>.Parse(HttpRequest, currentBuild)` overload silently accepted stale-client requests when a controller used the plain `Parse(actionJson, stateJson)` overload — that path is closed. **Consumer defense-in-depth preserved:** the per-controller `Parse(HttpRequest, currentBuild)` overload continues to compile + work (SKEW-02); nothing to remove.
    - **Client-side hard-lock modal (SKEW-04/05):** the shipped `BrowserAdapter` now renders a non-dismissible framework-owned modal on any skew signal (`VmsVersionSkewError` from detection OR `VmsActionError code:"stale_client"` from dispatch). Silent `adapter.reload()` is retired — the user clicks [Reload] to consent (SKEW-06). **Consumer opt-out:** set `ShellOptions.onVersionSkew: "custom"` to preserve pre-9.0.0 behavior (signal via `onError`, no framework modal).

    ### Additive additions

    - **`Adapter.showSkewLock?` capability verb (SKEW-05):** optional adapter method for a custom target's skew affordance. Fail-quiet by absence (modeled on `toast?`/`reload?`, NOT on `navigate?`/`storage?`/`saveFile?`) — a custom adapter without the verb still learns of the skew via `onError`.
    - **`ShellOptions.onVersionSkew?: "default" | "custom"` (SKEW-06):** closed-union opt-out flag.
    - **`createVersionGuard({ currentBuild })` TS-server factory (SKEW-01):** wraps ANY route handler (GET or POST) with the fail-closed guard. Consumer wiring: `app.get("/api/x", createVersionGuard({ currentBuild: BUILD_ID })(getHandler))`.
    - **`ShellVersionGuardFilter` .NET `IActionFilter` (SKEW-01):** self-registered by `AddVmsShellVersioning` alongside the shipped `ShellVersionResultFilter`. No consumer wiring change — the same one-line adoption now covers GET + POST fail-close.
    - **Client attaches `X-VMS-Client-Build` on GET requests (SKEW-03):** previously POST-only; now symmetric on `load()` and `performRoundTrip()`.

    ### Notes for adopters

    - **The common case:** most consumers see the new default (modal fires instead of silent reload) — this is the DESIRED behavior; no code change required.
    - **Consumers with custom skew affordances** (Kitsune, PBMInvoices, any Pantheon app that handled `VmsVersionSkewError`/`stale_client` in a custom `onError`): set `onVersionSkew: "custom"` — one-line preservation of pre-9.0.0 behavior.
    - **TS-server subpath consumers wanting GET-side enforcement:** wire `createVersionGuard({ currentBuild })` around each GET handler (one line per route). Not required to keep working — the shipped in-`createAction` guard preserves POST-side v3.8.0 behavior automatically.
    - **Wire protocol token stays `viewmodel-shell/1.0`** — no wire fields added/removed.

    Design of record: `~/.claude/identities/vicky/bounties/version-skew-recovery-affordance/mechanism-sketch.md` + Ashley post-tasting sign-off 2026-08-02.

    Requirements: SKEW-01, SKEW-02, SKEW-03, SKEW-04, SKEW-05, SKEW-06, SKEW-07, SKEW-08, SKEW-09, SKEW-10 (see .planning/phases/29-.../29-CONTEXT.md §requirements_map for canonical definitions).
    ```

    Ensure the section-separator matches surrounding style (blank lines around the H2). Do NOT commit; do NOT bump package.json / .csproj versions here.

    Verify:
    - `grep -c '## 9.0.0' CHANGELOG.md` returns exactly 1.
    - `grep -c 'SKEW-0' CHANGELOG.md` returns at least 1 (the requirements list mentions the IDs).
    - `grep -c '<YYYY-MM-DD>' CHANGELOG.md` INCREASED by exactly 1 (the placeholder Plan 29-12 will finalize).
  </action>
  <verify>
    <automated>grep -c '## 9.0.0' CHANGELOG.md &amp;&amp; grep -c 'SKEW-01' CHANGELOG.md &amp;&amp; grep -c '<YYYY-MM-DD>' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## 9.0.0' CHANGELOG.md` returns exactly 1.
    - `grep -c 'BREAKING' CHANGELOG.md` INCREASED by at least 1 vs pre-plan baseline (the new BREAKING notice).
    - `grep -c 'onVersionSkew' CHANGELOG.md` INCREASED by at least 1.
    - `grep -c 'createVersionGuard' CHANGELOG.md` INCREASED by at least 1.
    - `grep -c '<YYYY-MM-DD>' CHANGELOG.md` INCREASED by exactly 1 (Plan 29-12 finalizes).
    - The section is at the TOP of the entry stream: `grep -n '## 9.0.0' CHANGELOG.md` line number is LESS than any `## 8.` entry's line number.
  </acceptance_criteria>
  <done>
    CHANGELOG.md 9.0.0 BREAKING entry staged. Date placeholder intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stage MIGRATION.md Upgrading to v9.0.0 section</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md (READ the top 100 lines — you need to see: (1) Phase 28's `## Upgrading to v8.2.0` for additive template; (2) any earlier MAJOR upgrade section — Phases 24-25's `## Upgrading to v8.0.0` — for BREAKING template shape)
    - .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-CONTEXT.md (§decisions for the exact opt-out shape + createVersionGuard wire-around pattern)
  </read_first>
  <action>
    Insert a new section at the top of the upgrade-notes stream in MIGRATION.md. Content:

    ```markdown
    ## Upgrading to v9.0.0

    v9.0.0 is a **BEHAVIOR-only major bump** — no wire shape change. The two behavior changes are (1) the server-side version-skew guard now enforces on both GET + POST (was per-controller opt-in POST-only), and (2) the browser's shipped adapter renders a non-dismissible hard-lock modal on skew instead of silently auto-reloading.

    ### What consumers get for free

    - **Server-side:** consumers already calling `AddVmsShellVersioning(...)` (or wiring `createAction`) get the new global-guard behavior automatically. The per-controller `ActionPayload<T>.Parse(HttpRequest, currentBuild)` overload continues to compile + work as defense-in-depth; nothing to remove.
    - **Browser-side:** consumers who did NOT have a custom `onError` affordance for skew signals see the new hard-lock modal on skew — this is the DESIRED behavior; no code change required.

    ### What consumers might need to do

    #### 1. Consumers with a custom skew affordance → opt-out one line

    If your app has custom handling for `VmsVersionSkewError` or `stale_client` in `onError` (e.g., Kitsune's status banner, PBMInvoices' toast), preserve that affordance by opting out of the framework modal:

    ```typescript
    new ViewModelShell({
      // ... existing options ...
      onVersionSkew: "custom",  // preserves v3.8.0 behavior: signal via onError, no framework modal
    });
    ```

    #### 2. TS-server subpath consumers wanting GET-side enforcement → one line per route

    If you're on the TS server subpath (Bun/Hono/Deno/Cloudflare Workers) and want the guard to enforce on GET refreshes too, wire `createVersionGuard` around each GET handler:

    ```typescript
    import { createAction, createVersionGuard } from "@ashley-shrok/viewmodel-shell/server";

    const guard = createVersionGuard({ currentBuild: BUILD_ID });

    app.get("/api/tasks", guard(async (req) => { /* GET handler */ }));
    app.post("/api/tasks/action", guard(createAction<TasksState>(async (p) => { /* action handler */ })));
    ```

    Not required to keep working — the shipped in-`createAction` guard preserves POST-side v3.8.0 behavior automatically. The wrap closes the GET-refresh gap.

    #### 3. .NET consumers → nothing to do

    `AddVmsShellVersioning(...)` now co-registers BOTH filters (the shipped `ShellVersionResultFilter` for the `serverBuild` stamp + the new `ShellVersionGuardFilter` for the GET+POST guard) from a single call. Consumers who were manually adding `ShellVersionResultFilter` continue to work (dedup-guarded); the manual registration is now redundant.

    ### Bumping both packages

    ```bash
    # TS (from your app's dir)
    npm install @ashley-shrok/viewmodel-shell@^9.0.0

    # .NET (from your app's csproj dir)
    dotnet add package AshleyShrok.ViewModelShell --version 9.0.0
    ```

    ### Ceremony reference

    Design of record: `~/.claude/identities/vicky/bounties/version-skew-recovery-affordance/mechanism-sketch.md` + Ashley post-tasting sign-off 2026-08-02.
    ```

    Verify:
    - `grep -c '## Upgrading to v9.0.0' MIGRATION.md` returns exactly 1.
    - `grep -c 'onVersionSkew: "custom"' MIGRATION.md` returns at least 1 (the opt-out code snippet).
    - `grep -c 'createVersionGuard' MIGRATION.md` returns at least 2 (the import + the usage code snippets).
  </action>
  <verify>
    <automated>grep -c '## Upgrading to v9.0.0' MIGRATION.md &amp;&amp; grep -c 'onVersionSkew' MIGRATION.md &amp;&amp; grep -c 'createVersionGuard' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Upgrading to v9.0.0' MIGRATION.md` returns exactly 1.
    - `grep -c 'onVersionSkew' MIGRATION.md` returns at least 2 (mentioned in options list + code snippet).
    - `grep -c 'createVersionGuard' MIGRATION.md` returns at least 2 (import + wire-around).
    - `grep -c 'AddVmsShellVersioning' MIGRATION.md` INCREASED by at least 1 vs pre-plan baseline (the .NET no-op note).
    - The section is at the TOP: `grep -n '## Upgrading' MIGRATION.md` first line is v9.0.0's.
  </acceptance_criteria>
  <done>
    MIGRATION.md v9.0.0 upgrade section staged with three consumer paths + bump instructions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add new AGENTS.md gotcha + Phase 29 addendum to gotcha #9 + cross-verify fixture reference</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (READ the "Critical gotchas (read first)" section fully — you need: (1) the current highest gotcha number to pick the next one; (2) the exact voice/style of adjacent gotchas as template; (3) the existing gotcha #9 text to know exactly where to append the Phase 29 addendum without disrupting its structure)
    - .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-CONTEXT.md (§decisions for the exact new-gotcha content)
    - **parity/fixtures/helpdesk.json** (READ or grep — confirm the fixture ID `agt-get-build-stale` referenced in the gotcha #9 addendum below actually exists in the file; if Plan 29-07 renamed/scoped it differently, adjust the addendum's ID string to match reality BEFORE writing to AGENTS.md — a doc that hard-codes a phantom fixture ID is worse than one that doesn't reference one)
  </read_first>
  <action>
    Perform TWO edits to `AGENTS.md`:

    (a) Add a new gotcha (pick the next available number — likely #10 or #11; grep for `^\d+\. \*\*` to confirm). Insert alphabetically or numerically in the gotchas section per surrounding style. Content:

    ```markdown
    N. **Version-skew is a HARD LOCK on the client since v9.0.0, and the server-side guard is GLOBAL, not per-controller opt-in.** Two gotchas fold into one:

       - **Server-side:** `AddVmsShellVersioning(...)` (and the equivalent `createVersionGuard({ currentBuild })` TS-server factory) now enforces on BOTH GET + POST via a global filter. The per-controller `ActionPayload<T>.Parse(HttpRequest, currentBuild)` overload continues to compile + work (defense-in-depth) but the plain `Parse(actionJson, stateJson)` overload — which pre-v9.0.0 silently accepted stale-client requests when used from a controller lacking the guard — now ALSO fail-closes because the global filter short-circuits first. The change is transparent for the consumer's happy path; the RISK is if you were RELYING on the pre-9.0.0 silent-accept for some workflow, that workflow now fails with 400 stale_client. Reload to fresh bundle; nothing was applied.

       - **Client-side:** the shipped `BrowserAdapter` fires a non-dismissible hard-lock modal on any skew signal (detection via `VmsVersionSkewError` OR fail-closed via `VmsActionError code:"stale_client"`). Silent `adapter.reload()` is retired — the user clicks [Reload] to consent. **Consumer opt-out for pre-existing custom affordances:** set `ShellOptions.onVersionSkew: "custom"` — the shell still surfaces the signals via `onError` (byte-parallel to v3.8.0) but does NOT lock, NOT stop polling, and NOT call `showSkewLock`. Consumers with their own affordance (Kitsune, PBMInvoices) set this and keep their own path.

       See CHANGELOG v9.0.0 + MIGRATION v9.0.0 for wiring examples.
    ```

    (b) Extend the existing gotcha #9 (cross-backend parity testing, per PATTERNS §Documentation twins references it). Locate the gotcha #9 block via `grep -n 'gotcha #9\|Cross-backend parity' AGENTS.md`. Append the following addendum at the END of gotcha #9's text (as a new bullet or paragraph, preserving the surrounding structure):

    ```markdown
    - **v9.0.0 postscript (Phase 29):** the pre-9.0.0 server-side version-skew guard was per-controller opt-in — a class-1 gotcha #9 defect where controllers using the plain `Parse(actionJson, stateJson)` overload silently accepted stale-client requests. v9.0.0's global filter (`ShellVersionGuardFilter` on .NET; `createVersionGuard` on TS server subpath) closes it. Parity fixture `agt-get-build-stale` in `parity/fixtures/helpdesk.json` is the tripwire per this gotcha's class-3 lesson — a substring-check on the response body proves the guard fires on GET (the branch the pre-9.0.0 fixture could not reach).
    ```

    **PRE-WRITE FIXTURE CROSS-VERIFICATION:** Before pasting the addendum, run `grep -c 'agt-get-build-stale' parity/fixtures/helpdesk.json`. If it returns 0, HALT — the fixture Plan 29-07 shipped is under a different ID; find the correct ID via `grep -o '"id"[^,]*' parity/fixtures/helpdesk.json | grep -i 'get\|build\|stale'` and substitute the correct ID into the addendum text before writing to AGENTS.md. A doc string that hard-codes a phantom fixture ID would leave readers chasing a fixture that doesn't exist.

    CRITICAL:
    - Do NOT modify gotcha #9's existing text — only APPEND.
    - Do NOT modify other gotchas.
    - Pick the correct next gotcha number by grep-then-count.
    - The fixture ID cited in the addendum MUST exist in `parity/fixtures/helpdesk.json` — verified via grep BEFORE writing to AGENTS.md.

    Verify:
    - `grep -c 'onVersionSkew' AGENTS.md` INCREASED by at least 1 vs pre-plan baseline (the new gotcha mentions it).
    - `grep -c 'ShellVersionGuardFilter' AGENTS.md` INCREASED by at least 1.
    - `grep -c 'v9.0.0 postscript' AGENTS.md` returns exactly 1 (the gotcha #9 addendum marker).
    - `grep -c '^\d\+\. \*\*' AGENTS.md` INCREASED by exactly 1 (the new gotcha).
    - **Fixture cross-verification:** `grep -c 'agt-get-build-stale' parity/fixtures/helpdesk.json` returns exactly 1 (the fixture Plan 29-07 shipped exists at the ID the AGENTS.md addendum references).
  </action>
  <verify>
    <automated>grep -c 'onVersionSkew' AGENTS.md &amp;&amp; grep -c 'ShellVersionGuardFilter' AGENTS.md &amp;&amp; grep -c 'v9.0.0 postscript' AGENTS.md &amp;&amp; grep -c 'agt-get-build-stale' parity/fixtures/helpdesk.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onVersionSkew' AGENTS.md` INCREASED by at least 1.
    - `grep -c 'ShellVersionGuardFilter' AGENTS.md` INCREASED by at least 1.
    - `grep -c 'createVersionGuard' AGENTS.md` INCREASED by at least 1.
    - `grep -c 'v9.0.0 postscript' AGENTS.md` returns exactly 1.
    - `grep -c 'agt-get-build-stale' AGENTS.md` returns exactly 1 (in the gotcha #9 addendum).
    - **Fixture cross-verification (checker-mandated):** `grep -c 'agt-get-build-stale' parity/fixtures/helpdesk.json` returns exactly 1 (Plan 29-07's fixture exists at the exact ID the AGENTS.md addendum references — the doc string and the fixture agree). If this criterion fails, the AGENTS.md addendum must be adjusted to name the ID actually present in helpdesk.json before landing.
    - New gotcha count increased by exactly 1: `grep -cE '^[0-9]+\. \*\*' AGENTS.md` INCREASED by 1 vs pre-plan baseline.
    - Existing gotcha #9 text preserved (a spot-check: any pre-existing phrase from gotcha #9 like "The diff is only half the gate" still appears via grep).
  </acceptance_criteria>
  <done>
    AGENTS.md gets new v9.0.0 gotcha + gotcha #9 addendum. Framework doctrine reflects Phase 29's shipped behavior + the design-of-record lesson. Fixture reference cross-verified — the doc string names an ID that actually exists in parity/fixtures/helpdesk.json.
  </done>
</task>

<task type="auto">
  <name>Task 4: Guardrail — confirm green-tree gate is still green after doc edits</name>
  <files></files>
  <read_first>
    - .planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-10-SUMMARY.md (confirm Plan 29-10's GREEN verdict was recorded — this task's guardrail is a defense against a mis-numbered doc edit having accidentally touched source code)
  </read_first>
  <action>
    Quick re-check that no code file was accidentally touched by the doc edits:

    ```bash
    git status --porcelain | grep -v '\(CHANGELOG.md\|MIGRATION.md\|AGENTS.md\|\.planning/\)' 2>&1
    ```

    Should return NO output (or only the expected doc files + planning summaries). If any source-code file appears, HALT and investigate.

    Then re-run a QUICK TS build to confirm no source-code drift:
    ```bash
    cd viewmodel-shell && npm run build 2>&1 | tail -3
    ```
    Exit 0.
  </action>
  <verify>
    <automated>git status --porcelain | grep -v -E 'CHANGELOG.md|MIGRATION.md|AGENTS.md|.planning/' &amp;&amp; echo "OK: no unexpected files" || echo "unexpected files above" &amp;&amp; cd viewmodel-shell &amp;&amp; npm run build 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` shows only expected files (CHANGELOG.md, MIGRATION.md, AGENTS.md, planning artifacts).
    - No source-code file (viewmodel-shell/src/, viewmodel-shell-dotnet/, demo/, parity/) appears in the status.
    - `cd viewmodel-shell && npm run build` exits 0 (guardrail confirmation).
  </acceptance_criteria>
  <done>
    Doc-only changes confirmed; no source code touched. Green-tree gate remains green. Ready for Plan 29-12's release.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc claims vs shipped behavior | The docs describe behavior Plans 29-01..07 shipped. A doc claim that doesn't match reality is a documentation defect; Task 1-3's read-first-then-write discipline mitigates. |
| Date placeholder handoff | The `<YYYY-MM-DD>` placeholder is finalized in Plan 29-12. If a consumer clones between this plan's commit and 29-12's, they see the placeholder — but that's a transient state; the phase is a coherent release once both plans commit. |
| Accidental cross-file edit | Task 4's guardrail catches source-code drift from the doc edits. |
| Doc string vs fixture ID drift | AGENTS.md's gotcha #9 addendum names a specific fixture ID (`agt-get-build-stale`) owned by Plan 29-07. If Plan 29-07 shipped under a different ID, the addendum would name a phantom. Task 3's cross-verification grep against `parity/fixtures/helpdesk.json` closes this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-35 | Repudiation | Docs missing a documented behavior | mitigate | Content lists SKEW-01..10 explicitly; a missing SKEW-XX ID means the doc under-describes and reviewers catch it. |
| T-29-36 | Tampering | Cross-file drift into source code | mitigate | Task 4's `git status` guardrail. |
| T-29-36a | Repudiation | Doc names phantom fixture ID | mitigate | Task 3's fixture cross-verification grep against parity/fixtures/helpdesk.json — the ID cited in the addendum MUST return exactly 1 in the fixture file. |
| T-29-37 | Denial of Service | Doc entry too long / obscures other release notes | accept | Length matches Phase 28's 8.2.0 entry (comparable structure); CHANGELOG scrolls. |
</threat_model>

<verification>
Per-task grep counts + Task 4's guardrail + Task 3's fixture cross-verification. The docs are staged, not committed.
</verification>

<success_criteria>
- CHANGELOG.md 9.0.0 BREAKING entry staged at the top of the entry stream.
- MIGRATION.md v9.0.0 upgrade section staged with three consumer paths.
- AGENTS.md new gotcha + gotcha #9 Phase 29 addendum both landed.
- Fixture ID `agt-get-build-stale` (cited in the addendum) confirmed present in `parity/fixtures/helpdesk.json`.
- No source code accidentally modified.
- Date placeholder intact for Plan 29-12.
</success_criteria>

<output>
Create `.planning/phases/29-version-skew-hard-lock-global-server-guard-client-hard-lock-/29-11-SUMMARY.md` recording: line ranges of the CHANGELOG + MIGRATION + AGENTS.md edits, the specific new-gotcha number chosen for AGENTS.md, output of the fixture cross-verification grep (proving the addendum's fixture ID exists in helpdesk.json), and confirmation from Task 4 that no source-code file was touched.
</output>
