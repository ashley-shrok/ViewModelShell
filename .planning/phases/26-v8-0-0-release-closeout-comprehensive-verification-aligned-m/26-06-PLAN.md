---
phase: 26-v8-0-0-release-closeout-comprehensive-verification-aligned-m
plan: 6
type: execute
wave: 5
depends_on: ["26-05"]
files_modified: [.planning/ROADMAP.md]
autonomous: true
requirements: [MILESTONE-v8.0.0]

must_haves:
  truths:
    - "A release announcement has been posted to the fleet `#vms-changelog` room (Continuwuity room id `!E211RrsKCygK7Ev6uacpswousKy9JZiGEVLquJpC3cU`) via the agent-relay skill. The message names the milestone (v8.0.0), summarizes what shipped (4 foundations + 10 composites + 1 new primitive + 3 wire tweaks), calls out the SOLE BREAKING (`EmptyStateNode` field rename), links to CHANGELOG.md + MIGRATION.md entries, and names both registry URLs so consumers can `npm install @ashley-shrok/viewmodel-shell@8.0.0` / `dotnet add package AshleyShrok.ViewModelShell --version 8.0.0` directly."
    - "Before posting, the agent verifies membership of the target room. If not a member, the plan STOPS with a specific report so the operator can either add the agent or post the message themselves."
    - "The phase 26 SUMMARY (`26-06-SUMMARY.md`) exists and links to every prior 26-NN SUMMARY, records the announce URL, and produces a phase-level rollup: the v8.0.0 milestone is CLOSED across both packages, both registries serve 8.0.0, tag v8.0.0 exists + main-ancestor confirmed."
    - "`.planning/ROADMAP.md` Phase 26 plan-list block is updated from `**Plans:** 0 plans (to be created by /gsd:plan-phase 26)` to `**Plans:** 6 plans complete` with all 6 boxes ticked."
    - "The v8.0.0 milestone is officially CLOSED — every follow-up 8.0.x patch or 8.x.0 minor gets its own future phase."
  artifacts:
    - path: ".planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-06-SUMMARY.md"
      provides: "The phase-06 announce + rollup record."
      contains: "8.0.0"
  key_links:
    - from: "The v8.0.0 registry-serve state (from Plan 26-05)"
      to: "Fleet-visible announcement in #vms-changelog"
      via: "This plan's agent-relay Matrix post + phase SUMMARY"
      pattern: "!E211RrsKCygK7Ev6uacpswousKy9JZiGEVLquJpC3cU\\|vms-changelog"
---

<objective>
Announce v8.0.0 to the fleet via Continuwuity `#vms-changelog` (room id `!E211RrsKCygK7Ev6uacpswousKy9JZiGEVLquJpC3cU` per Nelly's fleet heads-up post-Continuwuity-rebuild). Write the Phase 26 SUMMARY as a phase-level rollup linking all 6 sub-plan SUMMARYs. Update ROADMAP.md to close Phase 26. Close the v8.0.0 milestone.

Purpose: A release nobody knows about is a release that doesn't exist to consumers. Every prior release announced on `#vms-changelog` per the fleet convention. Phase 26's announce closes the loop from "code merged" → "packages published" → "fleet aware." The phase SUMMARY makes future backlog recovery + retro possible (per AGENTS.md — append-only history under `.planning/milestones/` + phases).

Output: Fleet-visible announcement in Matrix + phase SUMMARY on disk + ROADMAP closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@CHANGELOG.md
@MIGRATION.md
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-02-SUMMARY.md
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-04-SUMMARY.md
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Announce v8.0.0 to #vms-changelog via agent-relay</name>
  <files></files>
  <read_first>
    - The agent-relay skill documentation (`~/.claude/skills/agent-relay/` — the fleet Matrix post convention Nelly maintains). The skill is loaded via `/skill agent-relay` in normal use. Load the reference before invoking.
    - CHANGELOG.md v8.0.0 section (top of file) — the announce text mirrors its Added / Changed (BREAKING) headings.
    - MIGRATION.md `## Upgrading to v8.0.0 — ONE break: EmptyStateNode field rename` — the migration surface to link.
  </read_first>
  <action>
    (a) Verify the agent is a member of Continuwuity room `!E211RrsKCygK7Ev6uacpswousKy9JZiGEVLquJpC3cU`. If the agent-relay skill exposes a `list-rooms` / `whoami` verb, use it; otherwise attempt a probe send + catch the "not a member" error early.

    If NOT a member: STOP. Report to the operator: "Not a member of !E211Rrs... (#vms-changelog) — please invite `@aitherbot:matrix.contiuwuity.tld` (or whatever the current agent identity is, per Nelly's box-map) OR post the announce message yourself using the text below." Then include the announce text as a preformatted block in the SUMMARY (Task 2) so the operator can copy-paste.

    (b) If a member: post the announce message. Format (Markdown-flavored, single message):

    ```
    🚀 **viewmodel-shell v8.0.0** — aligned major release (npm + NuGet)

    The Route B composite-nodes layer ships in full — 10 composite recipes across 3 phases, all aligned as one major bump.

    **Added (all additive — no consumer action needed):**
    - **4 foundations** (COMP-01..04): TextNode `style:"caption"` tier + `weight` axis, CheckboxNode `variant:"switch"`, AvatarNode
    - **10 composite recipes** (COMP-05..13a): ListRow + ListNode `variant:"rows"`, Message + MessageList (with `followTail` scroll-pin), Alert, EmptyState (see BREAKING), UserRow, DetailRow + DetailList (`<dl>/<dt>/<dd>`), TimelineEntry + Timeline (new `::before` rail-and-dot CSS mechanism), SettingRow + SettingList (natural pairing with switch), Chip + ChipList (with caller-supplied `dismissAction`)
    - **1 new primitive**: DividerNode
    - **3 wire tweaks**: additive fields on foundations + discriminator additions per composite

    **⚠️ SOLE BREAKING — `EmptyStateNode` field rename (COMP-08):**
    Old: `{ heading, message?, action? }` → New: `{ icon?, title, description?, action? }`. Every internal fleet callsite already migrated in Phase 24. See MIGRATION.md for the 1-line sed for TS + .NET consumers.

    **Install:**
    - `npm install @ashley-shrok/viewmodel-shell@8.0.0`
    - `dotnet add package AshleyShrok.ViewModelShell --version 8.0.0`

    **Details:** CHANGELOG.md `## v8.0.0` section + MIGRATION.md `## Upgrading to v8.0.0`.

    Tag: `v8.0.0` (`git checkout v8.0.0` for backlog recovery).

    Green-tree at release commit: vitest, framework .NET Tests, all demo Tests projects, cross-backend parity, and check-skill all green (see .planning/phases/26-.../26-03-SUMMARY.md).
    ```

    (Adjust room-specific formatting per Continuwuity's Markdown flavor — the agent-relay skill knows the current convention.)

    (c) Capture the returned event id (or permalink) so it can be included in the SUMMARY as `announce_event_id`.
  </action>
  <verify>
    <automated>echo "manual verify — agent-relay outcome recorded in Task 2's SUMMARY (message posted OR operator asked to post OR not-a-member reported)"</automated>
  </verify>
  <acceptance_criteria>
    - Either the announce message was posted to `!E211RrsKCygK7Ev6uacpswousKy9JZiGEVLquJpC3cU` and the event id captured, OR the not-a-member state was surfaced to the operator with the full announce text ready to copy-paste.
    - The announce text names v8.0.0, the 4 foundations, the 10 composites, the SOLE BREAKING (EmptyStateNode), and the install commands for both packages.
    - The tag `v8.0.0` is referenced as the checkout path for backlog recovery.
  </acceptance_criteria>
  <done>
    Fleet is aware of v8.0.0 (or knows the announce is pending on the operator).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 26-06 SUMMARY + phase-level rollup + update ROADMAP</name>
  <files>.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-06-SUMMARY.md, .planning/ROADMAP.md</files>
  <read_first>
    - All 5 prior plan SUMMARYs (26-01, 26-02, 26-03, 26-04, 26-05) — the phase-level rollup links each one
    - .planning/ROADMAP.md Phase 26 block (currently ends with `**Plans:** 0 plans (to be created by /gsd:plan-phase 26)`)
    - Task 1's announce event id + or the not-a-member escalation state (from agent memory)
  </read_first>
  <action>
    (a) Write `.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-06-SUMMARY.md`. Sections:

    (i) **Announce state** — either `posted: <event-id>` or `escalated: operator to post — text ready in this SUMMARY below` with the full announce text quoted.

    (ii) **v8.0.0 milestone rollup** — a per-plan table:
    | Plan | Objective | Result | SUMMARY |
    |---|---|---|---|
    | 26-01 | Comprehensive tailnet verification page served | Green (Vite PID <n> on port <p>; killed in 26-05) | 26-01-SUMMARY.md |
    | 26-02 | agent-skill.md audit + byte-copy verify | Green (no-op or single-edit — see SUMMARY) | 26-02-SUMMARY.md |
    | 26-03 | Full green-tree gate + Ashley visual sign-off | Green + approved | 26-03-SUMMARY.md |
    | 26-04 | CHANGELOG/MIGRATION heading finalize + version bump to 8.0.0 | Green | 26-04-SUMMARY.md |
    | 26-05 | Operator publish + tag + advance main | Green — both registries serve 8.0.0; tag v8.0.0 pushed; main-ancestor confirmed | 26-05-SUMMARY.md |
    | 26-06 | Announce + phase SUMMARY | Green (or escalated per (i)) | this file |

    (iii) **Milestone-scope state** — copy from Plan 26-05 SUMMARY: registry-latest for both packages, tag `v8.0.0` created + pushed, main-ancestor OK.

    (iv) **v8.0.0 fully closed** — explicit statement + green light for future 8.0.x patches / 8.x.0 minors to open as new phases.

    (b) Update `.planning/ROADMAP.md` Phase 26 plan-list block. Replace:

    ```
    **Plans:** 0 plans (to be created by /gsd:plan-phase 26)
    ```

    with:

    ```
    **Plans:** 6 plans complete

    Plans:
    - [x] 26-01-PLAN.md — Comprehensive tailnet verification page (wave 1, Showcase served on 100.113.23.63:<port>)
    - [x] 26-02-PLAN.md — agent-skill.md audit + byte-copy verify (wave 1, file-disjoint from 26-01)
    - [x] 26-03-PLAN.md — Full green-tree gate + Ashley visual sign-off (wave 2, depends on 26-01 + 26-02)
    - [x] 26-04-PLAN.md — CHANGELOG/MIGRATION heading finalize + aligned version bump to 8.0.0 (wave 3, depends on 26-03)
    - [x] 26-05-PLAN.md — Operator publish + tag v8.0.0 + advance main (wave 4, depends on 26-04)
    - [x] 26-06-PLAN.md — Announce on #vms-changelog + phase SUMMARY (wave 5, depends on 26-05)
    ```

    Do NOT touch prior phase blocks. Do NOT re-order phases.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-06-SUMMARY.md &amp;&amp; grep -c '8\.0\.0' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-06-SUMMARY.md &amp;&amp; grep -c '26-01-SUMMARY\|26-02-SUMMARY\|26-03-SUMMARY\|26-04-SUMMARY\|26-05-SUMMARY' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-06-SUMMARY.md &amp;&amp; grep -c '26-01-PLAN\|26-06-PLAN' .planning/ROADMAP.md &amp;&amp; grep -c '6 plans complete\|\\[x\\] 26-' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 26-06-SUMMARY.md exists.
    - `grep -c '8.0.0' 26-06-SUMMARY.md` >= 3 (multiple mentions across sections).
    - `grep -c '26-01-SUMMARY\|26-02-SUMMARY\|26-03-SUMMARY\|26-04-SUMMARY\|26-05-SUMMARY' 26-06-SUMMARY.md` >= 5 (every prior SUMMARY linked).
    - Contains explicit "v8.0.0 fully closed" statement.
    - Contains announce state (posted event id OR escalated with quoted announce text).
    - ROADMAP.md Phase 26 block updated with 6 ticked plans.
    - `grep -c '26-01-PLAN\|26-06-PLAN' .planning/ROADMAP.md` >= 2.
    - Prior phase blocks in ROADMAP UNCHANGED.
  </acceptance_criteria>
  <done>
    v8.0.0 milestone officially closed on disk: fleet announced (or escalated), phase SUMMARY rolls up all 6 sub-plans, ROADMAP shows Phase 26 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `#vms-changelog` Continuwuity room | External visibility to consumers. Announce content becomes the release "voice" and is quoted in follow-up conversations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Information Disclosure | Announce message has a factual error (e.g. wrong version, misses the BREAKING) that becomes the release's public description | mitigate | Task 1's message body is derived directly from CHANGELOG.md + MIGRATION.md headings — the single source of truth for what shipped. The announce is written LAST, after all 5 prior plans confirmed the release state. |
| T-26-06-02 | Denial of Service | Agent is not a member of the room and post silently fails | mitigate | Task 1 explicitly checks membership first + falls back to escalation with the full announce text, so the operator can post if needed. No silent failure. |
| T-26-06-03 | Spoofing | Announce posts before the release is actually live (race: Task 5 of 26-05 caches an incorrect registry state and Task 1 here posts stale) | mitigate | This plan depends on 26-05 which itself verifies both registries served 8.0.0 via direct curl (not cached npm view). The announce always happens AFTER the registry-live confirmation. |
</threat_model>

<verification>
Task-scoped verifications inside each task. Overall plan complete when 26-06-SUMMARY.md exists + ROADMAP Phase 26 closed.
</verification>

<success_criteria>
- Fleet aware of v8.0.0 via #vms-changelog announce (posted OR escalated with copy-ready text).
- 26-06-SUMMARY.md rolls up all 6 sub-plan SUMMARYs.
- ROADMAP.md Phase 26 shows 6 plans complete + all ticked.
- v8.0.0 milestone officially CLOSED.
</success_criteria>

<output>
`.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-06-SUMMARY.md` is the phase closeout artifact.
</output>
