---
phase: 26-v8-0-0-release-closeout-comprehensive-verification-aligned-m
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [MILESTONE-v8.0.0]

must_haves:
  truths:
    - "A comprehensive tailnet verification page — the existing `demo/Showcase/` served on `100.113.23.63:<free-port>` via `vite --host 100.113.23.63` — is reachable and returns HTTP 200 to `curl` from the same host."
    - "The served page is driven by the REAL bundle: Vite's alias config in `demo/Showcase/frontend/vite.config.ts` resolves `@ashley-shrok/viewmodel-shell/{browser,styles.css,themes/*.css}` and `@ashley-shrok/viewmodel-shell` directly to the local `viewmodel-shell/src/{browser,index}.ts` + `viewmodel-shell/styles/*.css` sources, so any bundle drift is impossible."
    - "The page demonstrates ALL 10 shipped composites (COMP-05..13a — ListRow / MessageNode + MessageList / Alert / EmptyState / UserRow / DetailRow + DetailList / Timeline + TimelineEntry / SettingRow + SettingList / Chip + ChipList), the 4 foundations (COMP-01..04 — TextNode caption + weight, CheckboxNode variant switch, AvatarNode), the 1 new primitive (DividerNode from Phase 24), and the 3 wire tweaks (EmptyStateNode rewrite + any additive composites/fields landed 23-25) IN ONE PAGE, with the built-in theme switcher available. This is the FULL-MILESTONE visual sign-off scope — NOT a Phase-25-only scope."
    - "A short smoke log records the port chosen, the tailnet-scoped URL (`http://100.113.23.63:<port>/`), and the `curl -sI` first line + first-render HTML-length numbers so Ashley can open the URL knowing it's live and healthy."
  artifacts:
    - path: ".planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md"
      provides: "Records the Vite dev-server invocation, tailnet port + URL, curl smoke output, and the coverage checklist (10 composites + 4 foundations + 1 primitive + 3 wire tweaks all present in Showcase source)."
      contains: "100.113.23.63"
  key_links:
    - from: "demo/Showcase/frontend/src/main.ts"
      to: "viewmodel-shell/src/{index,browser}.ts + styles/default.css + themes/*.css"
      via: "Vite alias regex map in demo/Showcase/frontend/vite.config.ts"
      pattern: "resolve\\(__dirname, \"\\.\\./\\.\\./\\.\\./viewmodel-shell"
---

<objective>
Stand up the comprehensive tailnet verification page for the v8.0.0 milestone. The page IS the existing `demo/Showcase/` (per AGENTS.md — Showcase is the canonical fleet-adoption "one page across all shipped nodes"), served over the tailnet via `vite --host 100.113.23.63` on a free port. Confirm it renders live before handing Ashley the URL in Plan 26-03.

Purpose: v8.0.0 is a MAJOR release (major-line bump — aligned 7.x → 8.0). The AGENTS.md standing directive requires operator eyeballing over the tailnet for the ENTIRE milestone scope in one served page BEFORE registry publish. Phase 25-10 served the Showcase at `100.113.23.63:8186` for the Phase-25-only sign-off; Phase 26 re-serves it for the WHOLE-milestone sign-off (10 composites + 4 foundations + 1 primitive + 3 wire tweaks — everything shipped in Phases 23, 24, 25). No new page is BUILT — the Showcase already covers the milestone by the fleet-adoption discipline; this plan just proves that coverage AND makes the page reachable.

Output: A live Showcase over the tailnet + a smoke log recording the URL, curl proof, and the coverage checklist proving all shipped nodes are represented in the served page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Showcase covers the full v8.0.0 milestone scope</name>
  <files></files>
  <read_first>
    - demo/Showcase/frontend/src/main.ts (the source that drives the served page)
    - demo/Showcase/frontend/vite.config.ts (Vite alias map — proves the REAL bundle)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md (Phase 25 sign-off confirms Showcase already covers 10 composites)
  </read_first>
  <action>
    Grep `demo/Showcase/frontend/src/main.ts` for each v8.0.0 milestone item and record hit-counts. Every item MUST be present in the served page for the full-milestone sign-off to be honest.

    **Coverage checklist — every row MUST hit >= 1:**

    (a) 10 composites (COMP-05..13a):
    - COMP-05 ListRowNode: `grep -c 'list-row\|listRow\|ListRow' demo/Showcase/frontend/src/main.ts`
    - COMP-05a ListNode variant:"rows": `grep -c 'variant: "rows"\|variant:"rows"' demo/Showcase/frontend/src/main.ts`
    - COMP-06 MessageNode: `grep -c 'message-list\|messageList\|Message\\|MessageNode' demo/Showcase/frontend/src/main.ts`
    - COMP-06a MessageListNode: `grep -c 'message-list\|messageList' demo/Showcase/frontend/src/main.ts`
    - COMP-07 AlertNode: `grep -c 'alert\|Alert' demo/Showcase/frontend/src/main.ts` (careful — must be node-type usage, not error-string)
    - COMP-08 EmptyStateNode (rewritten shape): `grep -c 'empty-state\|emptyState\|EmptyState' demo/Showcase/frontend/src/main.ts` AND `grep -c 'title:\|description:' demo/Showcase/frontend/src/main.ts` (proves new field names, not old `heading`/`message`)
    - COMP-09 UserRowNode: `grep -c 'user-row\|userRow\|UserRow' demo/Showcase/frontend/src/main.ts`
    - COMP-10 DetailRowNode: `grep -c 'detail-row\|detailRow\|DetailRow' demo/Showcase/frontend/src/main.ts`
    - COMP-10a DetailListNode: `grep -c 'detail-list\|detailList\|DetailList' demo/Showcase/frontend/src/main.ts`
    - COMP-11 TimelineEntryNode: `grep -c 'timeline-entry\|timelineEntry\|TimelineEntry' demo/Showcase/frontend/src/main.ts`
    - COMP-11a TimelineNode: `grep -c 'timeline' demo/Showcase/frontend/src/main.ts`
    - COMP-12 SettingRowNode: `grep -c 'setting-row\|settingRow\|SettingRow' demo/Showcase/frontend/src/main.ts`
    - COMP-12a SettingListNode: `grep -c 'setting-list\|settingList\|SettingList' demo/Showcase/frontend/src/main.ts`
    - COMP-13 ChipNode: `grep -c 'chip\|Chip' demo/Showcase/frontend/src/main.ts`
    - COMP-13a ChipListNode: `grep -c 'chip-list\|chipList\|ChipList' demo/Showcase/frontend/src/main.ts`

    (b) 4 foundations (COMP-01..04):
    - COMP-01 TextNode caption tier: `grep -c 'style: "caption"\|style:"caption"' demo/Showcase/frontend/src/main.ts`
    - COMP-02 TextNode weight axis: `grep -c 'weight: "\|weight:"' demo/Showcase/frontend/src/main.ts`
    - COMP-03 CheckboxNode variant:"switch": `grep -c 'variant: "switch"\|variant:"switch"' demo/Showcase/frontend/src/main.ts`
    - COMP-04 AvatarNode: `grep -c 'avatar\|Avatar' demo/Showcase/frontend/src/main.ts`

    (c) 1 new primitive (Phase 24):
    - DividerNode: `grep -c 'divider\|Divider' demo/Showcase/frontend/src/main.ts`

    (d) The 3 wire tweaks are already covered by their host composites (EmptyStateNode rewrite = COMP-08; any additive fields land as part of COMP-05..13a). Confirmed by the grep above.

    RECORD every grep count in the SUMMARY table. If ANY coverage cell is 0, STOP and report the gap (Phase 25-07 was supposed to have added Secondary Composites to Showcase in situ — a zero on any COMP-09..13a is a Phase 25 regression, not a Phase 26 gap).

    **DO NOT MODIFY `demo/Showcase/frontend/src/main.ts`.** This task is READ-ONLY. If a coverage gap is found, the SUMMARY records it and Plan 26-03 (the checkpoint) surfaces it to Ashley; do not silently patch the Showcase from a plan whose scope is verification.
  </action>
  <verify>
    <automated>test -f demo/Showcase/frontend/src/main.ts &amp;&amp; test -f demo/Showcase/frontend/vite.config.ts &amp;&amp; grep -c 'user-row\|userRow\|UserRow' demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'timeline' demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'chip' demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'detail-row\|detailRow\|DetailRow' demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'setting-row\|settingRow\|SettingRow' demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'variant: "switch"\|variant:"switch"' demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'avatar\|Avatar' demo/Showcase/frontend/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every coverage grep from (a) 10 composites returns >= 1 (a 0 on any composite proves a Phase 25-07 fleet-adoption gap — STOP and report).
    - Every coverage grep from (b) 4 foundations returns >= 1 (a 0 on any foundation proves a Phase 23 Showcase-adoption gap — STOP and report).
    - The 1 primitive (DividerNode) grep returns >= 1 (a 0 proves a Phase 24 gap — STOP and report).
    - EmptyStateNode uses the NEW field names (`title:`/`description:`) NOT the old (`heading:`/`message:`) — verifies the sole v8.0 BREAKING landed at all callsites: `grep -c 'heading:\|message:' demo/Showcase/frontend/src/main.ts` == 0 for EmptyState callsites (check contextually — `message:` may legitimately appear inside a MessageNode's error-string field; the acceptance is EmptyState-scoped, use `grep -B2 -A2` for context).
    - No file modifications made in this task (READ-ONLY discipline).
  </acceptance_criteria>
  <done>
    Full-milestone Showcase coverage proven at grep level. Any gap reported to the SUMMARY for the Phase 26-03 checkpoint. Ready to serve.
  </done>
</task>

<task type="auto">
  <name>Task 2: Serve Showcase over tailnet + curl smoke + record URL</name>
  <files>.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md</files>
  <read_first>
    - AGENTS.md — "GREEN-TREE GATE" section (fleet-adoption discipline; verification pages are tailnet-served for the ENTIRE fleet to reach)
    - demo/Showcase/frontend/package.json — confirms Vite dev script
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md — the prior tailnet serve was `100.113.23.63:8186` (Vite dev bound to tailnet IP). Same tailnet IP; port may differ if 8186 is taken.
  </read_first>
  <action>
    Prepare a repeatable smoke procedure and record the invocation + curl proof + URL in `26-01-SUMMARY.md`.

    (a) Pick a free tailnet port. Preferred: `8186` (Phase 25-10 precedent). Fall back to next free in the range 8180-8199. Verify with `ss -ltn | grep -c ':8186 '` — 0 means free.

    (b) Start Vite dev bound to the tailnet IP in the background (this task's own long-lived process, so use `run_in_background: true` for the invocation via Bash):
    ```
    cd demo/Showcase/frontend
    npx vite --host 100.113.23.63 --port 8186
    ```
    (Adjust `--port` if 8186 was busy.) Do NOT `cd` back — Bash sessions reset cwd; use absolute paths for the follow-up curl.

    (c) Wait for "ready" — poll with `curl -sI http://100.113.23.63:8186/ | head -1` until it returns `HTTP/1.1 200 OK` (usually within 2s of Vite starting). Do NOT sleep-loop; run the curl a couple of times spaced by natural command overhead.

    (d) Fetch the served HTML once to prove real render:
    ```
    curl -s http://100.113.23.63:8186/ > /tmp/26-01-showcase-index.html
    wc -c /tmp/26-01-showcase-index.html
    grep -c '<script.*main.ts\|main\.ts"' /tmp/26-01-showcase-index.html
    ```
    Byte-count > 0 and the main.ts reference present = live.

    (e) Do NOT kill the Vite process — it stays running for Plan 26-03's checkpoint. Record the PID in the SUMMARY so Ashley (or Plan 26-06) can `kill <pid>` after the milestone verification is complete.

    (f) Write `.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md`. Sections:
    - **Tailnet URL** — `http://100.113.23.63:<port>/` (the concrete URL Ashley opens in Plan 26-03).
    - **Vite invocation** — the exact command + PID.
    - **Curl smoke** — first line of `curl -sI` + first-render byte count.
    - **Coverage checklist** — the full-milestone table from Task 1 (10 composites + 4 foundations + 1 primitive + 3 wire tweaks) with grep counts.
    - **Bundle-real proof** — quote the 4 relevant regex aliases from `vite.config.ts` (browser / styles.css / themes/*.css / index) that resolve to the local `viewmodel-shell/` sources — the "REAL bundle" invariant.
    - **Handoff to Plan 26-03** — explicit "Ashley: open URL above in a real browser; verify the full v8.0.0 milestone renders correctly across default + representative themes."
  </action>
  <verify>
    <automated>test -f .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md &amp;&amp; grep -c '100.113.23.63' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md &amp;&amp; grep -c 'COMP-05\|COMP-06\|COMP-07\|COMP-08\|COMP-09\|COMP-10\|COMP-11\|COMP-12\|COMP-13' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md &amp;&amp; grep -c 'AvatarNode\|DividerNode\|caption\|switch' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md &amp;&amp; grep -c 'HTTP/1.1 200\|HTTP/2 200' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Vite dev server is running bound to `100.113.23.63` on a documented port.
    - `curl -sI http://100.113.23.63:<port>/` returns `HTTP/1.1 200 OK` (or `HTTP/2 200`) — captured in SUMMARY.
    - First-render HTML byte count > 0 — captured in SUMMARY.
    - SUMMARY contains the concrete tailnet URL Ashley will open in Plan 26-03.
    - SUMMARY coverage table lists all 15 milestone rows (10 composites + 4 foundations + 1 primitive) with grep counts.
    - SUMMARY quotes the 4 Vite alias regexes proving the bundle is REAL (not a stale published copy).
    - SUMMARY records the Vite PID so the process can be cleanly killed after Plan 26-06.
    - No modifications to any Showcase file (this task is serve-only + record-only).
  </acceptance_criteria>
  <done>
    Comprehensive verification page is live on the tailnet with proof-of-life curl output + full-milestone coverage checklist + bundle-real proof + PID for later cleanup, all recorded in 26-01-SUMMARY.md. Plan 26-03 can hand Ashley the URL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tailnet-served dev server | The Vite dev server binds to the tailnet IP `100.113.23.63`. Reachable to every fleet peer (Nelly, Jeeves, Merlin, ashley-laptop, GigaAshleyPC, zoeybattlestation, ashleydeck). Not reachable from public internet (tailnet-only). Threat surface: fleet-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Information Disclosure | Vite dev server exposes source maps + hot-reload endpoint to any tailnet peer | accept | The tailnet is trust-boundary-enforced (Tailscale identity); Showcase carries no PII, no secrets, no auth — it's the framework's public demo. This is why the tailnet exists as the fleet-eyeball surface. |
| T-26-01-02 | Availability | Left-running Vite process (task 2's background) never gets killed, accumulating over milestones | mitigate | Task 2 records the PID in SUMMARY.md; Plan 26-06 SUMMARY includes the cleanup step. If a prior milestone's Vite is still running on port 8186, Task 2 explicitly falls back to a free port + records the collision. |
| T-26-01-03 | Tampering | Silent Showcase-source patch to hide a Phase 25 fleet-adoption gap | mitigate | Task 1 is READ-ONLY discipline (explicit — no file modifications); Task 2 serves what's already committed. Any gap surfaces to Ashley at Plan 26-03 checkpoint. |
</threat_model>

<verification>
Task-scoped verifications inside each task. Overall plan complete when 26-01-SUMMARY.md exists with tailnet URL + curl 200 + full-milestone coverage table + bundle-real proof + PID.
</verification>

<success_criteria>
- `demo/Showcase/frontend/src/main.ts` covers all 10 composites (COMP-05..13a) + 4 foundations (COMP-01..04) + 1 new primitive (DividerNode) — the full v8.0.0 milestone scope.
- Vite dev server live on `http://100.113.23.63:<port>/`; `curl -sI` returns 200.
- 26-01-SUMMARY.md records the URL, PID, curl smoke, coverage checklist, and bundle-real proof.
- No source files modified (READ-ONLY + serve-only discipline).
</success_criteria>

<output>
`.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md` records the tailnet URL Plan 26-03 hands to Ashley + the full-milestone coverage proof.
</output>
