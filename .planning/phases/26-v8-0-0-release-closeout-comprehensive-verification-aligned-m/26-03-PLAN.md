---
phase: 26-v8-0-0-release-closeout-comprehensive-verification-aligned-m
plan: 3
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified: []
autonomous: false
requirements: [MILESTONE-v8.0.0]

must_haves:
  truths:
    - "The FULL AGENTS.md green-tree gate runs GREEN one final time BEFORE any registry action: `npm run build && check:test-types && check:core-globals && check:aa-contrast && check:no-demo-style && check:demo-types && npx vitest run` in `viewmodel-shell/`, PLUS `dotnet test viewmodel-shell-dotnet/Tests`, PLUS every `demo/**/*.Tests.csproj`, PLUS `bun run parity/run.ts` (which itself includes `check-skill.ts`)."
    - "The pre-publish EmptyStateNode consumer-safety grep is CLEAN: `grep -rn 'EmptyStateNode(Heading\\|EmptyStateNode(Message\\|heading:.*empty-state\\|message:.*empty-state' demo/ viewmodel-shell-dotnet/Tests/ viewmodel-shell/test/` returns ZERO results (or only comments explaining the migration) — proves every internal callsite has already migrated to the new `Title`/`Description` field names per Phase 24."
    - "**Ashley (operator) has opened the tailnet URL from Plan 26-01 in a real browser** and confirmed the FULL v8.0.0 milestone renders correctly: all 10 composites (COMP-05..13a) + 4 foundations (COMP-01..04) + 1 new primitive (DividerNode) + the sole BREAKING (EmptyStateNode field rename) render as designed, with the theme switcher exercised across default + representative dark themes + representative light themes."
    - "Ashley's specific verification points from Phase 25-10 remain green: (a) Timeline ::before rail-and-dot mechanism visible, (b) Chip dismissAction fires the caller-supplied name (not `dismiss`), (c) SettingRow + CheckboxNode(variant:'switch') natural pairing renders correctly."
    - "Ashley's explicit `approved` resume-signal is recorded. This gate must clear BEFORE Plan 26-04 (version bump + CHANGELOG finalize) may run."
  artifacts:
    - path: ".planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md"
      provides: "Records the green-tree gate results per command (numbers: vitest count, framework .NET count, demo .NET count, parity status) + Ashley's visual sign-off transcript + EmptyStateNode consumer-safety grep result."
      contains: "approved"
  key_links:
    - from: "Plans 26-01 (tailnet URL) + 26-02 (agent-skill audit clean)"
      to: "Ashley's visual sign-off + green-tree gate green"
      via: "This checkpoint plan"
      pattern: "approved"
---

<objective>
Run the FULL AGENTS.md green-tree gate ONE FINAL TIME across every suite (framework build, all `check:*` gates, vitest, framework .NET Tests, every `demo/**/*.Tests.csproj`, cross-backend parity including `check-skill.ts`); run the pre-publish EmptyStateNode consumer-safety grep across the internal fleet; then hand Ashley the tailnet URL from Plan 26-01 for the full-milestone visual sign-off in one served page. This is the LAST gate before Plans 26-04/05 bump versions + publish + tag; anything red here STOPS Phase 26 closeout per AGENTS.md GREEN-TREE-GATE rule.

Purpose: v8.0.0 is an aligned MAJOR release on both packages (npm 7.1.0 → 8.0.0, NuGet 7.0.0 → 8.0.0). AGENTS.md rule #1 "NEVER PUBLISH OR PUSH ANYTHING BROKEN" is absolute — pre-existing red is NOT an exception. This plan enforces that rule + the AGENTS.md standing directive on fleet-adoption visual sign-off (Ashley eyeballs the whole milestone in one served page BEFORE publish, not after).

Output: SUMMARY records green-tree gate numeric results per command + EmptyStateNode grep clean + Ashley's `approved` transcript. If ANY gate fails or Ashley returns anything other than "approved", Phase 26 STOPS and the specific issue is surfaced for a follow-up fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-02-SUMMARY.md
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full green-tree gate + EmptyStateNode consumer-safety grep</name>
  <files></files>
  <read_first>
    - AGENTS.md "Working agreement for agents" — the EXACT green-tree gate command list (the load-bearing list). Reproduce it verbatim.
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md — the Phase 25 closeout numbers (vitest 1250/1, framework .NET 428, demo .NET 191 across 5 projects, parity green). Phase 26 numbers should equal-or-exceed since no code has been added since (only version files change in Plans 26-04, which comes LATER).
  </read_first>
  <action>
    Ensure `~/.dotnet` is on PATH first: `export PATH="$HOME/.dotnet:$PATH"`. Then run each gate command in sequence, capturing pass/fail + numeric result per command. Any RED result blocks Phase 26 closeout per AGENTS.md — either fix or surface to Ashley with explicit waiver request.

    Gate command sequence (verbatim from AGENTS.md):

    (a) Framework build + all `check:*` + vitest:
    - `cd viewmodel-shell && npm run build`
    - `npm run check:test-types`
    - `npm run check:core-globals`
    - `npm run check:aa-contrast`
    - `npm run check:no-demo-style`
    - `npm run check:demo-types`
    - `npx vitest run` (record test-count numbers)

    (b) Framework .NET Tests project (the one that was uncompilable 3.0.0-3.3.0 — must not regress):
    - `cd .. && dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal` (record pass count)

    (c) Every `demo/**/*.Tests.csproj` — the load-bearing "or exit 1" loop from AGENTS.md:
    - `for p in $(find demo -name '*.Tests.csproj'); do echo "=== $p ==="; dotnet test "$p" --nologo -v minimal || exit 1; done`
    (Record per-project pass count + total.)

    (d) Cross-backend parity (INCLUDES `check-skill.ts` internally):
    - `cd parity && bun run run.ts` (record byte-identical GREEN per backend + skill parity green)

    (e) Pre-publish EmptyStateNode consumer-safety grep — proves every internal callsite has migrated per Phase 24:
    - `grep -rn 'EmptyStateNode(Heading\|EmptyStateNode(Message' demo/ viewmodel-shell-dotnet/Tests/ 2>&1` (must return ZERO — .NET old-shape ctor)
    - `grep -rn 'heading:.*empty-state\|message:.*empty-state\|type: *"empty-state".*heading:\|type: *"empty-state".*message:' demo/ viewmodel-shell/test/ 2>&1` (must return ZERO — TS old-field names on empty-state)
    - Contextual grep for stray `.Tooltip` on the .NET EmptyStateNode record (removed in Phase 24): `grep -rn 'EmptyStateNode.*Tooltip\|Tooltip.*EmptyStateNode' demo/ viewmodel-shell-dotnet/Tests/ 2>&1` (must return ZERO)

    RECORD every gate command's result + numeric outputs. Any RED result STOPS Phase 26 closeout — surface the failure to Ashley before continuing.

    DO NOT bump any version files, DO NOT modify CHANGELOG, DO NOT publish. Those are Plans 26-04 and 26-05.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; npm run check:test-types &amp;&amp; npm run check:core-globals &amp;&amp; npm run check:aa-contrast &amp;&amp; npm run check:no-demo-style &amp;&amp; npm run check:demo-types &amp;&amp; npx vitest run &amp;&amp; cd .. &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal &amp;&amp; (for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done) &amp;&amp; cd parity &amp;&amp; bun run run.ts &amp;&amp; cd .. &amp;&amp; test $(grep -rn 'EmptyStateNode(Heading\|EmptyStateNode(Message' demo/ viewmodel-shell-dotnet/Tests/ 2>/dev/null | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `viewmodel-shell/` green.
    - All 5 `check:*` gates (test-types, core-globals, aa-contrast, no-demo-style, demo-types) green.
    - `npx vitest run` >= 1250 passed (Phase 25-10 baseline; no code added since).
    - `dotnet test viewmodel-shell-dotnet/Tests` >= 428 passed.
    - Every `demo/**/*.Tests.csproj` green — the `|| exit 1` loop completes without triggering.
    - Total demo .NET pass count >= 191 across 5 projects (Phase 25-10 baseline).
    - `bun run parity/run.ts` green (all backends byte-identical + skill parity green).
    - EmptyStateNode consumer-safety greps ALL return ZERO — no stale field names anywhere internal.
    - Every gate command's numeric result captured verbatim for Task 3's SUMMARY (with the exact command + output).
    - NO version files touched (package.json still 7.1.0; .csproj still 7.0.0 at this point).
  </acceptance_criteria>
  <done>
    Full green-tree gate green; consumer-safety greps clean; every result captured for the SUMMARY; ready to hand Ashley the URL.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The FULL v8.0.0 milestone accumulated across Phases 23-25 (with Phase 26's audits + gates run in Task 1 above):
    - 4 foundations (COMP-01..04): TextNode caption tier + weight axis, CheckboxNode variant switch, AvatarNode.
    - 10 composite recipes (COMP-05..13a): ListRow + ListNode variant rows, MessageNode + MessageListNode, AlertNode, EmptyStateNode (BREAKING rewrite), UserRowNode, DetailRowNode + DetailListNode, TimelineEntryNode + TimelineNode (with new ::before rail-and-dot CSS mechanism), SettingRowNode + SettingListNode, ChipNode + ChipListNode.
    - 1 new primitive: DividerNode (from Phase 24).
    - 3 wire tweaks: EmptyStateNode field rename + new icon slot (the SOLE v8.0 BREAKING); additive fields on foundations; discriminator additions for every composite.
    - Full green-tree gate GREEN per Task 1.
    - Tailnet Showcase live from Plan 26-01 at `http://100.113.23.63:<port>/`.
    - agent-skill audit clean per Plan 26-02.

    Package versions are STILL 7.1.0 / 7.0.0 at this checkpoint — the bump to 8.0.0 happens in Plan 26-04 AFTER your approval here.
  </what-built>
  <how-to-verify>
    (1) Read the tailnet URL from Plan 26-01's SUMMARY (`.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-01-SUMMARY.md` — grep for `http://100.113.23.63`). Open in a real browser on your workstation.

    (2) Scroll through the Showcase and verify the FULL v8.0.0 milestone renders as designed:

    Foundations (COMP-01..04):
    - **TextNode caption tier** — `style: "caption"` renders visibly smaller + muted (used in ListRow meta, Timeline time, Message timestamp).
    - **TextNode weight axis** — `weight: "medium"` renders semi-bold body-sized text (used in row primaries).
    - **CheckboxNode variant switch** — renders as a switch slider (not a checkbox) in SettingRow trailing slots and standalone.
    - **AvatarNode** — renders circular; content priority image > initials > icon > empty; sizes sm/md/lg/xl visibly distinct.

    Primary composites (COMP-05..08):
    - **ListRowNode + ListNode variant rows** — dense list rows with 3-tier typography (primary/secondary/meta). Whole-row `action` clickable + keyboard-activatable.
    - **MessageNode + MessageListNode** — chat/comment thread with `followTail` scroll-pin. Assistant role tints info.
    - **AlertNode** — 4 tones with tone-appropriate default icons (danger→x-circle, warning→alert-triangle, success→check-circle, info→info). Dismissible fires the RESERVED `dismiss` action name.
    - **EmptyStateNode (BREAKING rewrite)** — new shape `{icon?, title, description?, action?}` renders with tinted icon backdrop above title.

    Secondary composites (COMP-09..13a) — the Phase 25 additions, re-verified:
    - **UserRowNode** — all 4 status dots (online=green, away=yellow, offline=gray, busy=red).
    - **DetailListNode** — label column visibly aligned; `tone:"danger"` value in shipped danger color.
    - **🚨 TimelineNode ::before rail-and-dot** — vertical rail spans container; each entry has a dot with tone-encoded border. Inspect via dev-tools; verify across dark + light themes.
    - **SettingRowNode + CheckboxNode(variant:"switch") natural pairing** — switch vertically centered in trailing slot; extended stopPropagation prevents double-fire.
    - **🚨 ChipNode.dismissAction end-to-end** — dismissible chip's X fires the CALLER-SUPPLIED action name (verify via dev-tools console or Showcase onError/onAction hook), NOT the fixed `dismiss` name (that's AlertNode's posture).

    New primitive:
    - **DividerNode** — thematic-break separator (horizontal `<hr>` or vertical `role="separator"`).

    Wire tweaks:
    - **EmptyStateNode** — every appearance in Showcase uses the new field names (`title`/`description`), never the old (`heading`/`message`). This is the SOLE v8.0 BREAKING; every consumer migration should already be visible-clean.

    (3) Toggle the Showcase theme switcher across default + at least 2-3 dark themes + at least 2-3 light themes. Verify:
    - Timeline rail + dot visibility on tinted-background themes.
    - Chip tinted-pill text still readable at AA-normal 4.5:1.
    - Alert tone icons + backgrounds render correctly per theme.
    - UserRow status dots visible on light + dark surfaces.

    (4) Optionally probe wire-level correctness with `curl` from another tailnet peer (proves the framework's core value prop — agents can drive it without a browser):
    - `curl -s http://100.113.23.63:<port>/api/showcase | jq '.state | keys'` (or whatever endpoint the Showcase actually mounts — check Showcase source if unsure). The response should be the `{vm, state, ok}` envelope; agents can drive it just like the browser does.

    (5) Read the CHANGELOG "Unreleased — v8.0.0 (in progress)" section top-to-bottom. Confirm every entry describes what you SEE in the Showcase. Confirm the MIGRATION section "Upgrading to v8.0.0" has the SOLE BREAKING documented (EmptyStateNode rename) + the additive-only note for every other composite.

    (6) Sign off:
    - `approved` if every visible check passes AND every AGENTS.md gate from Task 1 was green.
    - `fix: <description>` if any specific composite / theme / wire tweak looks wrong.
    - `defer: <description>` if a rough edge should be fixed AFTER v8.0.0 ships as an 8.0.x patch or 8.1.0 minor.

    Note: package versions are STILL 7.1.0 / 7.0.0 at this checkpoint. Bumping to 8.0.0 happens in Plan 26-04 AFTER your `approved`.
  </how-to-verify>
  <resume-signal>Type "approved" if the full v8.0.0 milestone looks right on the tailnet Showcase across default + representative themes AND every AGENTS.md gate from Task 1 was green AND the EmptyStateNode consumer-safety grep was clean. Type "fix: {description}" for a blocker. Type "defer: {description}" for a post-release rough edge. Anything other than "approved" STOPS Phase 26 — no version bump, no publish.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 26-03 SUMMARY with green-tree numbers + Ashley's transcript</name>
  <files>.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md</files>
  <read_first>
    - Task 1's captured gate output (kept in agent memory)
    - Task 2's resume-signal transcript (Ashley's verbatim response)
  </read_first>
  <action>
    Write `.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md`. Sections:

    (i) **Green-tree gate results (Task 1)** — table with columns `Gate | Result | Command | Numeric result`:
    - Every command from Task 1 (framework build, 5 check:* gates, vitest, framework .NET Tests, every demo Tests project by name, parity, EmptyStateNode consumer-safety greps).
    - Numeric result = pass count (vitest, .NET) or "byte-identical GREEN" (parity) or "0 hits" (consumer-safety greps) or "compiled" (build/check).
    - Every row must have Result column = green (or the plan STOPPED and this SUMMARY isn't being written).

    (ii) **EmptyStateNode consumer-safety grep detail** — record the exact grep commands + zero-hit output. Explicitly document why this matters: proves every internal callsite has migrated from the pre-v8 `heading`/`message` field names, so npm/NuGet consumers upgrading to 8.0.0 who follow the same pattern will find their own callsites the same way.

    (iii) **Ashley's visual sign-off (Task 2)** — transcribe the exact resume-signal text. If `approved`, note the timestamp. If `fix:` or `defer:`, this SUMMARY IS NOT WRITTEN — the plan STOPS at Task 2 and a follow-up task addresses the issue before Phase 26 continues.

    (iv) **Milestone scope verified** — quick checklist:
    - 4 foundations (COMP-01..04) — visually confirmed
    - 10 composites (COMP-05..13a) — visually confirmed
    - 1 new primitive (DividerNode) — visually confirmed
    - 3 wire tweaks (EmptyStateNode rewrite + additive fields + discriminator additions) — visually confirmed
    - Theme switcher exercised across default + 2-3 dark + 2-3 light themes

    (v) **Version files verification** — record: `grep '"version":' viewmodel-shell/package.json` returns `7.1.0`; `grep '<Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns `7.0.0`. Confirms Task 1 + Task 2 did NOT bump versions — the bump is Plan 26-04's job.

    (vi) **Ready for Plan 26-04** — explicit "green light to bump versions + finalize CHANGELOG/MIGRATION headings" if all above are green.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md &amp;&amp; grep -c 'approved' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md &amp;&amp; grep -c 'green' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md &amp;&amp; grep -c 'vitest\|check:' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md &amp;&amp; grep -c 'EmptyStateNode' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md &amp;&amp; grep '"version": "7\.1\.0"' viewmodel-shell/package.json &amp;&amp; grep '<Version>7\.0\.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj</automated>
  </verify>
  <acceptance_criteria>
    - 26-03-SUMMARY.md exists.
    - Contains green-tree gate results table with every command + numeric result.
    - `grep -c 'green' 26-03-SUMMARY.md` >= 10 (every gate row is green).
    - `grep -c 'approved' 26-03-SUMMARY.md` >= 1 (Ashley's sign-off transcribed).
    - `grep -c 'EmptyStateNode' 26-03-SUMMARY.md` >= 2 (consumer-safety grep detail + milestone scope mention).
    - Version files STILL UNCHANGED (package.json = 7.1.0; .csproj = 7.0.0) — confirms bump comes in Plan 26-04, not this plan.
    - Explicit "ready for Plan 26-04" statement.
  </acceptance_criteria>
  <done>
    Phase 26 landing verification recorded: green-tree gate + EmptyStateNode consumer-safety + Ashley's sign-off + version-file untouched confirmation. Plan 26-04 unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The green-tree gate is the LAST safety net before registry publish | Any red gate here that gets waived becomes a bug shipped to npm + NuGet consumers. |
| Operator sign-off is the LAST semantic gate before registry publish | Ashley's "approved" is the human "I have looked at the whole milestone and it's ready to ship." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Tampering | Pre-existing red test is silently normalized as "unrelated" and the release ships on top of a broken tree | mitigate | Task 1 verify-block includes the `|| exit 1` loop for every demo Tests project; AGENTS.md rule #1 explicitly cited in the read_first + action. Any red result triggers STOP with an explicit operator surface. |
| T-26-03-02 | Elevation of Privilege | Version bump / publish happens BEFORE this checkpoint clears | mitigate | Plans 26-04 (bump) and 26-05 (publish) both declare `depends_on: ["26-03"]` in their frontmatter; the wave-order forbids running before this plan completes. Task 3's SUMMARY explicitly verifies version files are STILL 7.1.0/7.0.0, so any premature bump surfaces here. |
| T-26-03-03 | Information Disclosure | Ashley signs off without actually opening the tailnet URL (rubber-stamp) | mitigate | Task 2's how-to-verify enumerates 6 specific visual + wire-level checks (foundations, 4 primary composites incl. EmptyState rewrite, 5 secondary composites incl. Timeline + Chip end-to-end, theme switcher, curl wire probe, CHANGELOG cross-check) — signing "approved" without actually running through them is a personal-integrity choice, not a mechanical bypass. Task 2's resume-signal explicitly names "fix" and "defer" as first-class alternatives so Ashley has a natural escape hatch without needing to reject the whole plan. |
| T-26-03-04 | Information Disclosure | EmptyStateNode migration is incomplete — some internal fleet callsite still uses the pre-v8 field names, and the release ships breaking that callsite | mitigate | Task 1 explicitly greps `demo/`, `viewmodel-shell-dotnet/Tests/`, and `viewmodel-shell/test/` for the old-shape field names + old .NET ctor signature. Zero-hit is required for acceptance; any hit surfaces as a specific file+line that needs migration before Plan 26-04 runs. |
</threat_model>

<verification>
Task-scoped verifications inside each task. Overall plan complete when 26-03-SUMMARY.md exists with green-tree table + Ashley's `approved` transcript + version-files-unchanged confirmation.
</verification>

<success_criteria>
- Full AGENTS.md green-tree gate green (framework build + 5 check:* + vitest + framework .NET Tests + every demo Tests + parity/skill).
- EmptyStateNode consumer-safety greps return zero hits.
- Ashley's `approved` resume-signal recorded verbatim.
- Version files UNCHANGED (bump happens in Plan 26-04).
- Plan 26-04 explicitly unblocked in SUMMARY.
</success_criteria>

<output>
`.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md` is the pre-publish landing verification record.
</output>
