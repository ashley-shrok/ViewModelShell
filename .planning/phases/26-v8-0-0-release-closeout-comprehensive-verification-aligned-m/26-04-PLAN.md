---
phase: 26-v8-0-0-release-closeout-comprehensive-verification-aligned-m
plan: 4
type: execute
wave: 3
depends_on: ["26-03"]
files_modified: [CHANGELOG.md, MIGRATION.md, viewmodel-shell/package.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj]
autonomous: true
requirements: [MILESTONE-v8.0.0]

must_haves:
  truths:
    - "`CHANGELOG.md` heading `## Unreleased — v8.0.0 (in progress)` has been renamed to `## v8.0.0 — <publish-date>` where `<publish-date>` is the concrete YYYY-MM-DD date the release lands (today, at the time this plan runs). The batch-then-ship reminder blockquote at the top of that section is REMOVED (it was a planning-time note, not consumer-facing text)."
    - "`MIGRATION.md` heading `## Upgrading to v8.0.0 (in progress) — ONE break: EmptyStateNode field rename` has been renamed to `## Upgrading to v8.0.0 — ONE break: EmptyStateNode field rename`. The `(in progress)` marker and the batch-then-ship blockquote at the top of that section are REMOVED."
    - "`viewmodel-shell/package.json` `\"version\": \"7.1.0\"` has been changed to `\"version\": \"8.0.0\"`. Exact string match — no whitespace drift."
    - "`viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` `<Version>7.0.0</Version>` has been changed to `<Version>8.0.0</Version>`. Exact string match."
    - "The framework re-builds green after the version bumps: `cd viewmodel-shell && npm run build && npx vitest run` still green (version-bump-only changes should not affect any test); `dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj -c Release` still green."
    - "The 4 modifications are made as a SINGLE atomic set — no partial commits. This is the release commit that Plan 26-05 will publish + tag."
    - "NO git commit is made in this plan. AGENTS.md working agreement: git is operator-driven. This plan STAGES the release changes; Plan 26-05 begins with the operator running `git add` + `git commit` for the release."
  artifacts:
    - path: ".planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-04-SUMMARY.md"
      provides: "Records the CHANGELOG heading diff, MIGRATION heading diff, both version-file diffs, and the post-bump verify-green output."
      contains: "8.0.0"
  key_links:
    - from: "Plan 26-03 Ashley 'approved' sign-off"
      to: "Plan 26-05 operator publish"
      via: "This plan's 4-file atomic version-bump + CHANGELOG/MIGRATION finalize"
      pattern: "8\\.0\\.0"
---

<objective>
Finalize CHANGELOG.md + MIGRATION.md headings for v8.0.0 (rename `Unreleased — v8.0.0 (in progress)` → `v8.0.0 — <publish-date>` and remove the batch-then-ship reminders). Bump both package version files to the aligned 8.0.0. Verify the framework re-builds + tests still pass post-bump. Do NOT commit — that's Plan 26-05's operator-gated first step.

Purpose: v8.0.0 is an aligned MAJOR release on both packages. AGENTS.md publishing runbook requires the version bump + CHANGELOG heading finalize + MIGRATION heading finalize to happen in a SINGLE atomic release commit (so the release commit exactly matches what's published to npm + NuGet). This plan stages that commit; Plan 26-05 commits + publishes + tags + advances main.

Output: 4 files modified (CHANGELOG.md, MIGRATION.md, package.json, .csproj) + post-bump green build + SUMMARY documenting the diffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CHANGELOG.md
@MIGRATION.md
@viewmodel-shell/package.json
@viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
@.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize CHANGELOG.md v8.0.0 heading</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-32 (the Unreleased v8.0.0 heading + batch-then-ship blockquote + Added + Changed sections)
    - AGENTS.md "CHANGELOG.md + MIGRATION.md are release-gated" rule (append-only + release-time finalize)
  </read_first>
  <action>
    Use the Edit tool for BOTH edits below. Do NOT re-read the file after the edit — Edit reports failures reliably.

    Edit 1: Rename the heading. Today's date at execution time is the `<publish-date>` — determine it via `date +%Y-%m-%d` from the shell if needed. Replace:

    ```
    ## Unreleased — v8.0.0 (in progress)
    ```
    with:
    ```
    ## v8.0.0 — <YYYY-MM-DD>
    ```

    Edit 2: Remove the batch-then-ship blockquote that immediately follows the heading (currently at CHANGELOG.md line 11):

    ```
    > **Batch-then-ship:** v8.0.0 publishes at Phase 26 closeout with all 10 composites + 3 wire tweaks + 4 foundations in one aligned release. Do NOT publish Phase 24 as its own release. See `.planning/design/composite-nodes-layer.md` for the milestone design of record.

    ```
    (Delete the blockquote line AND the blank line that follows it, so the `### Added` heading directly follows the new `## v8.0.0 — <date>` heading with just one blank line between.)

    Preserve every other line under the v8.0.0 section verbatim. Do NOT re-order composites in the ### Added list. Do NOT touch any prior release entry (7.1.0, 7.0.1, 7.0.0, etc.) — this plan only finalizes the Unreleased-labelled section.
  </action>
  <verify>
    <automated>grep -c '^## v8\.0\.0 — 20' CHANGELOG.md &amp;&amp; test $(grep -c '^## Unreleased' CHANGELOG.md) -eq 0 &amp;&amp; test $(grep -c 'Batch-then-ship' CHANGELOG.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## v8\.0\.0 — 20' CHANGELOG.md` == 1 (the finalized heading with a 20XX date).
    - `grep -c '^## Unreleased' CHANGELOG.md` == 0 (no Unreleased header left).
    - `grep -c 'Batch-then-ship' CHANGELOG.md` == 0 (blockquote removed from finalized section — the phrase should NOT appear ANYWHERE in CHANGELOG since it was a planning-time note not a consumer-facing convention).
    - The `### Added` list under v8.0.0 is UNCHANGED byte-for-byte from the pre-edit state (the composites entries are the ones consumers read as release notes).
    - The `### Changed (BREAKING)` EmptyStateNode entry is UNCHANGED byte-for-byte.
    - No other CHANGELOG entry (7.1.0, 7.0.1, 7.0.0, etc.) is touched.
  </acceptance_criteria>
  <done>
    CHANGELOG.md v8.0.0 heading finalized with the publish date and the planning-time reminder removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize MIGRATION.md v8.0.0 heading</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md lines 1-80 (the Upgrading v8.0.0 heading + batch-then-ship blockquote + Phase 23 + EmptyStateNode BREAKING + additive-only sections)
  </read_first>
  <action>
    Use the Edit tool for BOTH edits.

    Edit 1: Rename the heading. Replace:
    ```
    ## Upgrading to v8.0.0 (in progress) — ONE break: `EmptyStateNode` field rename
    ```
    with:
    ```
    ## Upgrading to v8.0.0 — ONE break: `EmptyStateNode` field rename
    ```

    Edit 2: Remove the batch-then-ship blockquote at line ~11 (immediately follows the heading):
    ```
    **Batch-then-ship:** v8.0.0 publishes at Phase 26 closeout with all 10 composites + 3 wire tweaks + 4 foundations in one aligned release. This entry accumulates as Phase 23-26 land; it flips to a versioned release heading at Phase 26.

    ```
    (Delete that paragraph AND the blank line that follows it, so the `### Phase 23 foundations — purely additive` sub-heading directly follows the new `## Upgrading to v8.0.0 ...` heading with just one blank line between.)

    Preserve every other line verbatim. Do NOT touch prior migration entries (v7.0.0 TrackerCell.label rename, etc.).
  </action>
  <verify>
    <automated>grep -c '^## Upgrading to v8\.0\.0 — ONE break' MIGRATION.md &amp;&amp; test $(grep -c '## Upgrading to v8\.0\.0 (in progress)' MIGRATION.md) -eq 0 &amp;&amp; test $(grep -c 'Batch-then-ship' MIGRATION.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Upgrading to v8\.0\.0 — ONE break' MIGRATION.md` == 1 (finalized heading, no `(in progress)` marker).
    - `grep -c '(in progress)' MIGRATION.md` == 0 (marker fully removed).
    - `grep -c 'Batch-then-ship' MIGRATION.md` == 0 (blockquote removed).
    - The EmptyStateNode BREAKING section is UNCHANGED byte-for-byte.
    - The Phase 23 foundations additive-only section is UNCHANGED byte-for-byte.
    - The COMP-09..13a additive-only section is UNCHANGED byte-for-byte.
    - No prior migration entry (v7.0.0 etc.) is touched.
  </acceptance_criteria>
  <done>
    MIGRATION.md v8.0.0 heading finalized without the `(in progress)` marker and batch-then-ship blockquote removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump both package versions to 8.0.0 + post-bump green-verify + SUMMARY</name>
  <files>viewmodel-shell/package.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj, .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-04-SUMMARY.md</files>
  <read_first>
    - viewmodel-shell/package.json (target the `"version": "7.1.0"` line — should be around line 4)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (target the `<Version>7.0.0</Version>` line)
    - AGENTS.md "A version bump is NOT a release" rule — the bump alone doesn't ship anything; Plan 26-05 is the actual ship
  </read_first>
  <action>
    (a) Bump `viewmodel-shell/package.json` via Edit tool. Replace exactly:
    ```
      "version": "7.1.0",
    ```
    with:
    ```
      "version": "8.0.0",
    ```
    (Preserve exact indentation and trailing comma.)

    (b) Bump `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` via Edit tool. Replace exactly:
    ```
        <Version>7.0.0</Version>
    ```
    with:
    ```
        <Version>8.0.0</Version>
    ```
    (Preserve exact indentation. Note: the .csproj is currently 7.0.0 while npm was 7.1.0 — an intentional asymmetry from the 7.1.0 npm-only Markdown-companion release. Both go to 8.0.0 for the aligned major.)

    (c) Post-bump green-verify — the bumped files should not have affected any test. Re-run the minimal fast subset (NOT the full green-tree — that was Plan 26-03):
    - `cd viewmodel-shell && npm run build` (rebuilds dist/ with new version)
    - `cd viewmodel-shell && npx vitest run` (all tests still green)
    - `dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj -c Release` (the .NET package still builds — no compile errors from the version bump; also generates the .nupkg that Plan 26-05 will push)

    (d) Write `.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-04-SUMMARY.md`. Sections:
    - CHANGELOG.md diff — before/after of the heading line + the removed blockquote (show the exact strings).
    - MIGRATION.md diff — before/after of the heading line + the removed blockquote.
    - viewmodel-shell/package.json diff — before/after of the version line (`"version": "7.1.0"` → `"version": "8.0.0"`).
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj diff — before/after of the version line.
    - Post-bump green-verify — output line each for the 3 verify commands.
    - Explicit "NO git commit made" — AGENTS.md working agreement: git is operator-driven; Plan 26-05 begins with the commit.
    - Explicit "Plan 26-05 unblocked" — the release commit + operator publish + tag + advance main is the next step.
  </action>
  <verify>
    <automated>grep -c '"version": "8\.0\.0"' viewmodel-shell/package.json &amp;&amp; grep -c '<Version>8\.0\.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj &amp;&amp; test $(grep -c '"version": "7\.1\.0"' viewmodel-shell/package.json) -eq 0 &amp;&amp; test $(grep -c '<Version>7\.0\.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj) -eq 0 &amp;&amp; cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; npx vitest run &amp;&amp; cd .. &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj -c Release &amp;&amp; test -f .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-04-SUMMARY.md &amp;&amp; grep -c '8\.0\.0' .planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"version": "8.0.0"' viewmodel-shell/package.json` succeeds; `grep '"version": "7.1.0"' viewmodel-shell/package.json` returns nothing (bump landed).
    - `grep '<Version>8.0.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` succeeds; `<Version>7.0.0</Version>` returns nothing.
    - `npm run build` green post-bump.
    - `npx vitest run` green post-bump (all Phase 25-10 baseline pass counts preserved).
    - `dotnet build ... -c Release` green post-bump — generates `bin/Release/AshleyShrok.ViewModelShell.8.0.0.nupkg` (Plan 26-05 will `dotnet nuget push` this).
    - 26-04-SUMMARY.md exists with all 4 file diffs + verify output.
    - `grep -c '8.0.0' 26-04-SUMMARY.md` >= 4 (both file bumps + CHANGELOG new heading + MIGRATION new heading all reference 8.0.0).
    - NO git commit made in this plan — verified by no new commit on `git log --oneline -1` (compare before/after; unchanged HEAD).
  </acceptance_criteria>
  <done>
    Release commit STAGED: 4 files modified (CHANGELOG.md heading, MIGRATION.md heading, package.json version, .csproj version). Post-bump build + tests green. Plan 26-05 unblocked to commit + publish.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Version files are the primary release identity | Wrong version string in either file causes registry mismatch; a slip to `8.0.1` or `8.1.0` is a wrong-release-shipped disaster. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | Version bump lands with a typo (e.g. `8.0.` or `8.O.0`) that npm/NuGet accept as syntactically valid but semantically wrong | mitigate | Task 3 verify-block does an exact-string grep for `"version": "8.0.0"` and `<Version>8.0.0</Version>` — a typo would fail the grep. Both version formats are string-strict (npm rejects `.0.` and NuGet requires SemVer). |
| T-26-04-02 | Tampering | CHANGELOG heading rename leaves the batch-then-ship blockquote in place (partial edit) | mitigate | Task 1 verify-block requires `grep -c 'Batch-then-ship' CHANGELOG.md` == 0 — the plan fails if the blockquote survived either the manual edit or a regex glitch. |
| T-26-04-03 | Elevation of Privilege | This plan accidentally commits the release changes (undermines Plan 26-05's operator-gated commit + auth precheck) | mitigate | Task 3 action explicitly says "NO git commit made in this plan"; acceptance criteria include verifying HEAD is unchanged from plan start. AGENTS.md working agreement is quoted in read_first. |
| T-26-04-04 | Availability | The post-bump build breaks (e.g. because `dotnet pack` picks up a stale bin/) and Plan 26-05 starts with a broken state | mitigate | Task 3 explicitly runs `dotnet build ... -c Release` which is what Plan 26-05's `dotnet pack` invokes; any breakage surfaces here, not in the middle of the operator's publish ritual. |
</threat_model>

<verification>
Task-scoped verifications inside each task. Overall plan complete when 4 file modifications land + post-bump build/tests green + SUMMARY records the diffs.
</verification>

<success_criteria>
- CHANGELOG.md heading renamed to `## v8.0.0 — <date>` with batch-then-ship blockquote removed.
- MIGRATION.md heading renamed to `## Upgrading to v8.0.0 — ...` (no `(in progress)`) with batch-then-ship blockquote removed.
- viewmodel-shell/package.json bumped to 8.0.0.
- viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj bumped to 8.0.0.
- Post-bump `npm run build` + `npx vitest run` + `dotnet build -c Release` all green.
- No git commit made — HEAD unchanged from plan start.
- 26-04-SUMMARY.md records all diffs + Plan 26-05 readiness.
</success_criteria>

<output>
`.planning/phases/26-v8-0-0-release-closeout-comprehensive-verification-aligned-m/26-04-SUMMARY.md` is the version-bump + heading-finalize record.
</output>
