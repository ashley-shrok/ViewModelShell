---
phase: 07-error-envelope-ok-flag-1-0-0-release-closeout
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - viewmodel-shell-dotnet/ShellExceptionFilter.cs
  - demo/Tasks/AspNetCore/Program.cs
  - demo/Tasks/AspNetCore/TasksController.cs
  - demo/ContactManager/AspNetCore/Program.cs
  - demo/ContactManager/AspNetCore/ContactsController.cs
  - demo/ExpenseTracker/AspNetCore/Program.cs
  - demo/ExpenseTracker/AspNetCore/ExpensesController.cs
  - demo/RetroBoard/AspNetCore/Program.cs
  - demo/RetroBoard/AspNetCore/RetroBoardController.cs
  - demo/HelpDesk/AspNetCore/Program.cs
  - demo/HelpDesk/AspNetCore/AgentController.cs
  - demo/HelpDesk/AspNetCore/RequesterController.cs
  - demo/FeatureProbe/AspNetCore/Program.cs
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
  - demo/Reorder/AspNetCore/Program.cs
  - demo/Reorder/AspNetCore/ReorderController.cs
  - demo/Tasks/AspNetCore.Tests/TasksControllerTests.cs
  - demo/ContactManager/AspNetCore.Tests/ContactsControllerTests.cs
  - demo/ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs
  - demo/RetroBoard/AspNetCore.Tests/RetroBoardControllerTests.cs
  - demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs
  - demo/HelpDesk/AspNetCore.Tests/RequesterControllerTests.cs
  - demo/Tasks-bun/server.ts
  - demo/ContactManager-bun/server.ts
  - demo/ExpenseTracker-bun/server.ts
  - demo/RetroBoard-bun/server.ts
  - demo/HelpDesk-bun/server.ts
  - demo/Reorder-bun/server.ts
  - demo/FeatureProbe-bun/handler.ts
autonomous: true
requirements: [ERROR-02, ERROR-03]
must_haves:
  truths:
    - "Every demo controller's dispatch `default:` arm throws `UnknownActionException(name)` (.NET) or `UnknownActionError(name)` (TS) — the framework's catch-arm routes these to `{ok:false, errors:[{message, code:\"unknown_action\"}]}` (D-09)."
    - "FeatureProbe gains a `boom` action handler in BOTH backends that throws a generic exception — exercised by Plan 04's parity fixture (D-21)."
    - "The .NET stack gains a framework-edge `ShellExceptionFilter` registered in every demo `Program.cs` that catches UnknownActionException / generic Exception and constructs the ShellErrorResponse envelope — so .NET demos don't need per-controller try/catch (D-06)."
    - "BadRequestError throws for routine input validation stay in their controllers UNCHANGED; the framework still wraps them into the envelope on the way out (D-08)."
    - "App-level state-based validation (TextNode + ValidationError state field, AGENTS.md gotcha #4) is NOT touched — it remains `ok: true` (D-07)."
    - "Existing controller unit tests that assert on the unknown-action `BadRequestObjectResult` shape are updated to assert `Assert.Throws<UnknownActionException>` instead — verified file:line mapping per file (6 tests across 6 test files)."
  artifacts:
    - path: "viewmodel-shell-dotnet/ShellExceptionFilter.cs"
      provides: ".NET framework-edge exception filter constructing ShellErrorResponse envelopes from thrown exceptions"
      contains: "IAsyncExceptionFilter"
    - path: "demo/Tasks/AspNetCore/TasksController.cs"
      provides: "Canonical .NET dispatch default-arm migration"
      contains: "throw new UnknownActionException"
    - path: "demo/Tasks-bun/server.ts"
      provides: "Canonical TS dispatch default-arm migration"
      contains: "throw new UnknownActionError"
    - path: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs"
      provides: "Default-arm migration + new `boom` action handler"
      contains: "throw new Exception"
    - path: "demo/FeatureProbe-bun/handler.ts"
      provides: "Default-arm migration + new `boom` action handler (shared by bun + node parity backends)"
      contains: "throw new Error"
  key_links:
    - from: "demo/*/AspNetCore/*Controller.cs default-arm"
      to: "ShellExceptionFilter -> ShellErrorResponse.OfUnknownAction"
      via: "throw -> filter catch -> envelope"
      pattern: "throw new UnknownActionException"
    - from: "demo/*-bun/server.ts default-arm"
      to: "createAction's UnknownActionError catch arm"
      via: "throw -> wrapper catch -> envelope"
      pattern: "throw new UnknownActionError"
---

<objective>
Sweep every demo dispatch handler to use the new `UnknownActionException` (.NET) / `UnknownActionError` (TS) exports from Plan 01 in their `default:` arms, replacing today's `return BadRequest("Unknown action: ...")` (.NET) and `throw new BadRequestError("Unknown action: ...")` (TS) patterns. Wire the .NET framework-edge exception filter that converts thrown exceptions into a `ShellErrorResponse` envelope — so the .NET side gets the "apps don't write per-controller boilerplate" property the TS `createAction` wrapper already provides. Add a `boom` action handler to FeatureProbe in BOTH backends (the deliberate uncaught-throw target Plan 04 exercises). Update the 6 existing unknown-action unit tests across the 5 .NET test projects to assert the new `Assert.Throws<UnknownActionException>` shape.

Purpose: this is the demos-and-runtime-wiring half. Without it, every demo still emits the old `{error: msg}` body shape on unknown actions, and Plan 04's parity fixture cannot run. The .NET exception filter is the critical piece — the existing `createAction` wrapper (TS) already lives at the runtime edge in Plan 01, but ASP.NET controllers throw into the framework with no equivalent edge unless a global filter is registered.

Output: a new `ShellExceptionFilter` in the NuGet source, 7 .NET demo `Program.cs` files registering it, 8 .NET controller call-sites swept (7 controllers; HelpDesk has Agent + Requester), 8 TS server call-sites swept (HelpDesk-bun has TWO `createAction` calls; FeatureProbe-bun has ONE shared `handler.ts` that both `server.ts` and `server-node.ts` import), FeatureProbe's `boom` action added on both backends, and 6 existing controller-test unknown-action assertions updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-PATTERNS.md
@.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-01-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Canonical .NET dispatch default arm, from demo/Tasks/AspNetCore/TasksController.cs around line 60-63: -->

```csharp
else
{
    return BadRequest($"Unknown action: {name}");
}
return new ShellResponse<TasksState>(BuildVm(state), state).Validate();
```

<!-- Canonical TS dispatch default arm, from demo/Tasks-bun/server.ts line 221: -->

```typescript
throw new BadRequestError(`Unknown action: ${name}`);
```

<!-- HelpDesk has TWO createAction calls in one file (Agent at line 601 + Requester at line 851). -->
<!-- FeatureProbe's TS handler lives in demo/FeatureProbe-bun/handler.ts and is shared by both server.ts (bun) and server-node.ts (node). One edit to handler.ts updates BOTH parity backends. -->

<!-- Line numbers of the `Unknown action:` throw/return per file (verified via grep before this plan): -->
<!-- .NET:     Tasks:62  ContactManager:108  ExpenseTracker:69  RetroBoard:62  HelpDesk-Agent:97  HelpDesk-Requester:101  FeatureProbe:193  Reorder:99 -->
<!-- TS:       Tasks-bun:221  ContactManager-bun:302  ExpenseTracker-bun:293  RetroBoard-bun:233  HelpDesk-bun:601 + 851  Reorder-bun:152  FeatureProbe-bun/handler.ts:272 -->

<!-- Existing test sites that assert BadRequestObjectResult on UNKNOWN-ACTION cases (verified via grep). These tests get updated in Task 2a. -->
<!-- Tasks/AspNetCore.Tests/TasksControllerTests.cs:    line 449-454  Action_UnknownName_ReturnsBadRequest  ("fly-to-moon")     → Assert.Throws<UnknownActionException> -->
<!-- ContactManager/AspNetCore.Tests/ContactsControllerTests.cs:    line 530-537  Action_UnknownAction_ReturnsBadRequest  ("teleport")    → Assert.Throws<UnknownActionException> -->
<!-- ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs:    line 432-438  Action_UnknownName_ReturnsBadRequest  ("fly-to-moon")  → Assert.Throws<UnknownActionException> -->
<!-- RetroBoard/AspNetCore.Tests/RetroBoardControllerTests.cs:    line 417-423  Action_UnknownName_ReturnsBadRequest  ("blast-off")    → Assert.Throws<UnknownActionException> -->
<!-- HelpDesk/AspNetCore.Tests/AgentControllerTests.cs:    line 402-408  UnknownAction_ReturnsBadRequest  ("do-the-thing")           → Assert.Throws<UnknownActionException> -->
<!-- HelpDesk/AspNetCore.Tests/RequesterControllerTests.cs:    line 362-368  UnknownAction_ReturnsBadRequest  ("fly-to-moon")        → Assert.Throws<UnknownActionException> -->
<!-- All OTHER BadRequestObjectResult assertions in these test files (16 hits total across the 6 files) are for INPUT VALIDATION (empty title, empty draft, etc.) and STAY UNCHANGED per D-08 — those controllers still `return BadRequest("title required")` for structurally-invalid input. -->

<!-- Note: FeatureProbe AspNetCore and Reorder AspNetCore do NOT have AspNetCore.Tests/ projects — verified: `ls demo/*/AspNetCore.Tests/` returns exactly 5 directories (Tasks, ContactManager, ExpenseTracker, RetroBoard, HelpDesk). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ShellExceptionFilter in the NuGet source + register it in every .NET demo Program.cs</name>
  <files>viewmodel-shell-dotnet/ShellExceptionFilter.cs, viewmodel-shell-dotnet/Tests/ShellExceptionFilterTests.cs, demo/Tasks/AspNetCore/Program.cs, demo/ContactManager/AspNetCore/Program.cs, demo/ExpenseTracker/AspNetCore/Program.cs, demo/RetroBoard/AspNetCore/Program.cs, demo/HelpDesk/AspNetCore/Program.cs, demo/FeatureProbe/AspNetCore/Program.cs, demo/Reorder/AspNetCore/Program.cs</files>
  <read_first>
    - viewmodel-shell-dotnet/ViewModels.cs (Plan 01 additions — ShellErrorResponse static factories, UnknownActionException, ErrorCodes; the existing InvalidOperationException throw in ViewTreeValidation around line 386)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (confirm whether it uses `<Compile Include="**/*.cs">` so ShellExceptionFilter.cs auto-includes, or needs an explicit `<Compile Include>`)
    - demo/Tasks/AspNetCore/Program.cs (canonical Program.cs — read full once: services registration, AddControllers configuration, middleware order, ConfigureJsonOptions config if any)
    - demo/FeatureProbe/AspNetCore/Program.cs (second analog — confirm Program.cs files are structurally similar across demos)
    - .planning/phases/07-01-SUMMARY.md (the exact factory signatures Plan 01 locked: `ShellErrorResponse.OfUnknownAction`, `OfBadRequest`, `OfParseError`, `OfInvalidTree`, `OfUncaught`)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md (D-06, D-08, D-11)
  </read_first>
  <action>
    Create `viewmodel-shell-dotnet/ShellExceptionFilter.cs` — an ASP.NET MVC filter implementing BOTH `IAsyncExceptionFilter` AND `IAsyncResultFilter` (or pick whichever single-interface implementation cleanly handles both throw paths and `BadRequestObjectResult` rewriting; document the choice inline).

    Throw-path responsibilities (IAsyncExceptionFilter / OnExceptionAsync):
    - Catch `UnknownActionException` → set `context.Result = new ContentResult { StatusCode = 400, ContentType = "application/json", Content = JsonSerializer.Serialize(ShellErrorResponse.OfUnknownAction(ex.ActionName)) }`. Mark `context.ExceptionHandled = true`.
    - Catch `InvalidOperationException` originating from `ViewTreeValidation.ValidateActionNames` (recognize by inspecting the type and message; the safe check is `ex is InvalidOperationException && ex.StackTrace?.Contains("ValidateActionNames") == true`, OR introduce a dedicated `ShellTreeValidationException` in a follow-up. For this plan, the safe check is sufficient — keep the diagnostic message verbatim, status 500, body `ShellErrorResponse.OfInvalidTree(ex.Message)`).
    - Catch `JsonException` (parse failure originating from `ActionPayload<T>.Parse` / `ParseJson`) → status 400, body `ShellErrorResponse.OfParseError(ex.Message)`.
    - Catch generic `Exception` (the fallback) → log via injected `ILogger<ShellExceptionFilter>` (full ex including stack), status 500, body `ShellErrorResponse.OfUncaught(ex)`. T1 mitigation: `OfUncaught` reads only `ex.Message` per Plan 01.

    BadRequest-rewrite responsibilities (IAsyncResultFilter / OnResultExecutionAsync):
    - When `context.Result is BadRequestObjectResult bro` AND the request's `Accept` header includes `application/json` (or content-type negotiation routes JSON), rewrite to `ContentResult { StatusCode = 400, ContentType = "application/json", Content = JsonSerializer.Serialize(ShellErrorResponse.OfBadRequest(bro.Value as string ?? "Bad request")) }`. Per D-08, no `code` on the entry. This handles `return BadRequest("title required")` in controllers (their structurally-invalid-input channel) so the wire is envelope-shaped even on returns rather than throws.
    - When `context.Result is BadRequestResult` (no body), rewrite to `ShellErrorResponse.OfBadRequest("Bad request")` with the same shape.

    JsonSerializer config: use the same options the demos use (`new JsonSerializerOptions { PropertyNamingPolicy = JsonNamingPolicy.CamelCase }` matching the existing wire convention) — confirm by reading one demo's `Program.cs` `ConfigureHttpJsonOptions` / `AddControllers().AddJsonOptions(...)` config. The output MUST be `{"ok":false,"errors":[...]}` (camelCase) to match the TS twin.

    Register the filter in `AshleyShrok.ViewModelShell.csproj` if needed (add `<Compile Include="ShellExceptionFilter.cs" />` only if the .csproj doesn't already glob `**/*.cs`).

    Wire ALL 7 .NET demo `Program.cs` files using EXACTLY this form (recommended per WARNING 9 — uniform across demos for diff-grep symmetry):
    ```csharp
    builder.Services.AddControllers(options =>
    {
        options.Filters.Add<ShellExceptionFilter>();
    });
    ```
    For demos that already pass a lambda to `AddControllers(...)` (likely for JsonSerializerOptions), add the `.Filters.Add<ShellExceptionFilter>()` line INSIDE the existing lambda. For demos that use the no-arg `AddControllers()`, convert to the lambda form. One line per demo; mechanical. Use the angle-bracket generic form `Filters.Add<ShellExceptionFilter>()` (NOT `Filters.Add(typeof(ShellExceptionFilter))` or `Filters.Add(new ShellExceptionFilter())`) for uniformity — this is what the acceptance criteria grep matches.

    Add `viewmodel-shell-dotnet/Tests/ShellExceptionFilterTests.cs` — xUnit `[Fact]`-style tests instantiating the filter directly and calling `OnExceptionAsync(ExceptionContext)` / `OnResultExecutionAsync(ResultExecutingContext, ResultExecutionDelegate)` with fake contexts. Cover each exception class + BadRequestObjectResult rewrite + BadRequestResult rewrite. Use the existing Tests/ namespace and `<FrameworkReference Include="Microsoft.AspNetCore.App" />` convention (already present per AGENTS.md gotcha #5).

    Test cases (one `[Fact]` each):
    - UnknownActionException routes to 400 with `{ok:false,errors:[{message:"Unknown action: foo",code:"unknown_action"}]}` (assert via deserializing the ContentResult.Content as JsonElement and inspecting fields).
    - Generic Exception routes to 500 with `{ok:false,errors:[{message:"...",code:"uncaught_exception"}]}` AND assert that the serialized message contains NO stack-trace markers (`"at "`, `"System."`) — T1 mitigation in code.
    - JsonException routes to 400 with `{ok:false,errors:[{message:"...",code:"parse_error"}]}`.
    - InvalidOperationException with stack containing `ValidateActionNames` routes to 500 with `{ok:false,errors:[{message:"...",code:"invalid_tree"}]}`.
    - BadRequestObjectResult containing string "title required" rewrites to `{ok:false,errors:[{message:"title required"}]}` — NO code (D-08).
    - BadRequestResult (no body) rewrites to `{ok:false,errors:[{message:"Bad request"}]}`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/ViewModelShell/viewmodel-shell-dotnet/Tests && dotnet test --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "IAsyncExceptionFilter\|IExceptionFilter" viewmodel-shell-dotnet/ShellExceptionFilter.cs` returns at least one match.
    - `grep -n "ShellErrorResponse.OfUnknownAction\|ShellErrorResponse.OfUncaught\|ShellErrorResponse.OfBadRequest" viewmodel-shell-dotnet/ShellExceptionFilter.cs` returns at least 3 matches (one per failure class used in the filter).
    - `grep -lE "Filters.Add<ShellExceptionFilter>\\(\\)" demo/*/AspNetCore/Program.cs | wc -l` returns 7 (every .NET demo registers the filter using the uniform angle-bracket generic form).
    - `cd viewmodel-shell-dotnet/Tests && dotnet test --nologo` exits 0 — pre-existing 16 framework tests + Plan 01 EnvelopeTests + new ShellExceptionFilterTests all pass.
    - `cd viewmodel-shell-dotnet && dotnet build --nologo -v minimal` exits 0 — the new filter compiles cleanly into the NuGet source.
    - Per-demo build smoke: each of the 7 .NET demos builds cleanly (verifies the filter registration didn't break any Program.cs).
    - Manual eyeball: the ShellExceptionFilterTests T1 assertion (no stack-trace markers in serialized body) is present and passing.
  </acceptance_criteria>
  <done>
    The .NET framework-edge envelope wrapper exists in the NuGet source, is registered in every .NET demo, has full xUnit coverage for all five failure classes plus the BadRequest rewrite, and ships information-disclosure mitigation in code.
  </done>
</task>

<task type="auto">
  <name>Task 2a: .NET demo sweep — 8 controller default-arms + 6 unknown-action test updates + FeatureProbe boom action + filter test smoke</name>
  <files>demo/Tasks/AspNetCore/TasksController.cs, demo/ContactManager/AspNetCore/ContactsController.cs, demo/ExpenseTracker/AspNetCore/ExpensesController.cs, demo/RetroBoard/AspNetCore/RetroBoardController.cs, demo/HelpDesk/AspNetCore/AgentController.cs, demo/HelpDesk/AspNetCore/RequesterController.cs, demo/FeatureProbe/AspNetCore/FeatureProbeController.cs, demo/Reorder/AspNetCore/ReorderController.cs, demo/Tasks/AspNetCore.Tests/TasksControllerTests.cs, demo/ContactManager/AspNetCore.Tests/ContactsControllerTests.cs, demo/ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs, demo/RetroBoard/AspNetCore.Tests/RetroBoardControllerTests.cs, demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs, demo/HelpDesk/AspNetCore.Tests/RequesterControllerTests.cs</files>
  <read_first>
    - demo/Tasks/AspNetCore/TasksController.cs (the canonical .NET analog — read full once; lines 60-63 are the default arm; line 36 has the structurally-invalid BadRequest("title required") pattern that STAYS unchanged per D-08)
    - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (read around line 193 for the .NET default arm; understand which action-name patterns the dispatch already handles so the new `boom` action slots in cleanly before the default arm)
    - viewmodel-shell-dotnet/ViewModels.cs (the new UnknownActionException — confirm constructor signature is `(string actionName)`)
    - demo/Tasks/AspNetCore.Tests/TasksControllerTests.cs lines 449-454 (the Action_UnknownName_ReturnsBadRequest test — canonical template for the test rewrite)
    - demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs lines 402-408 (UnknownAction_ReturnsBadRequest — second analog)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-PATTERNS.md (Demo controller sweep tables for the .NET backend)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md (D-07, D-08, D-09, D-21)
  </read_first>
  <action>
    Migration #1 — .NET controllers, mechanical sweep. For each of the 7 .NET controllers (8 call-sites; HelpDesk has Agent + Requester):
    - Replace `return BadRequest($"Unknown action: {name}");` with `throw new UnknownActionException(name);`. The variable name is `name` in every controller per the PATTERNS.md verified file:line table.
    - Verify the `using ViewModelShell;` directive is already present at the top of the file (it is — every controller already uses `ShellResponse<TState>` etc. from the namespace).
    - DO NOT change any other `BadRequest("...")` calls in the controller (the input-validation channel per D-08 — those stay as `BadRequestObjectResult` returns and get rewritten by Task 1's filter on the way out).

    Per-file line (verified via grep before this plan):
    | File | Line | Today | After |
    |------|------|-------|-------|
    | demo/Tasks/AspNetCore/TasksController.cs | 62 | `return BadRequest($"Unknown action: {name}");` | `throw new UnknownActionException(name);` |
    | demo/ContactManager/AspNetCore/ContactsController.cs | 108 | same | same |
    | demo/ExpenseTracker/AspNetCore/ExpensesController.cs | 69 | same | same |
    | demo/RetroBoard/AspNetCore/RetroBoardController.cs | 62 | same | same |
    | demo/HelpDesk/AspNetCore/AgentController.cs | 97 | same | same |
    | demo/HelpDesk/AspNetCore/RequesterController.cs | 101 | same | same |
    | demo/FeatureProbe/AspNetCore/FeatureProbeController.cs | 193 | same | same |
    | demo/Reorder/AspNetCore/ReorderController.cs | 99 | same | same |

    Migration #2 — Add `boom` action to FeatureProbe BEFORE the default arm (so the throw path is exercised before the unknown-action path).

    - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs: in the dispatch switch (the if/else chain around line 60-193), add a new arm:
      ```csharp
      else if (name == "boom")
      {
          throw new Exception("deliberate test failure");
      }
      ```
      Place it immediately before the `else { return BadRequest(...)` arm (which becomes the new `throw new UnknownActionException(name)` arm after Migration #1). The `boom` action takes no state mutations — it just throws synchronously, exercising the generic-Exception path through ShellExceptionFilter.

      The string `"deliberate test failure"` is the parity-fixture-visible message; Plan 04's fixture asserts the envelope's `errors[0].message === "deliberate test failure"` to confirm both backends produce byte-identical messages.

    Migration #3 — Update the 6 existing unknown-action unit tests across the 5 .NET test projects. The controller now THROWS `UnknownActionException` instead of returning `BadRequestObjectResult`; since the unit tests call the controller method directly (not through the filter pipeline), the throw propagates and the test must assert on it.

    Per-file changes (verified via grep before this plan):
    | File | Line | Today | After |
    |------|------|-------|-------|
    | demo/Tasks/AspNetCore.Tests/TasksControllerTests.cs | 449-454 | `var result = Act(ctrl, TasksState.Initial(), "fly-to-moon"); Assert.IsType<BadRequestObjectResult>(result.Result);` | `Assert.Throws<UnknownActionException>(() => Act(ctrl, TasksState.Initial(), "fly-to-moon"));` |
    | demo/ContactManager/AspNetCore.Tests/ContactsControllerTests.cs | 530-537 | same shape, action name "teleport", state ContactsState.Initial() | same |
    | demo/ExpenseTracker/AspNetCore.Tests/ExpensesControllerTests.cs | 432-438 | same shape, action name "fly-to-moon", state ExpensesState.Initial() | same |
    | demo/RetroBoard/AspNetCore.Tests/RetroBoardControllerTests.cs | 417-423 | same shape, action name "blast-off", state RetroState.Initial() | same |
    | demo/HelpDesk/AspNetCore.Tests/AgentControllerTests.cs | 402-408 | same shape, action name "do-the-thing", state AgentState.Initial() | same |
    | demo/HelpDesk/AspNetCore.Tests/RequesterControllerTests.cs | 362-368 | same shape, action name "fly-to-moon", state RequesterState.Initial() | same |

    Verify the `using ViewModelShell;` directive is present in each test file (it likely already is; if not, add it — UnknownActionException lives in the ViewModelShell namespace per Plan 01). The 10 OTHER `BadRequestObjectResult` assertions in these 6 test files (16 total minus 6 unknown-action = 10) are for INPUT VALIDATION (empty title, empty draft, etc.) — those STAY UNCHANGED per D-08, since input-validation controllers still `return BadRequest("title required")` and the test calls the controller method directly without the filter pipeline, so the BadRequestObjectResult is what comes back.

    Rename hint (optional, not required): the test method names like `Action_UnknownName_ReturnsBadRequest` are now misleading (it throws, not returns). The planner DOES NOT require renames in this plan — the tests still functionally verify the unknown-action behavior, and renaming methods churns git blame for no behavior change. If a reviewer asks, the executor MAY rename to `Action_UnknownName_Throws` — but that's discretion, not a requirement.

    Final smoke: run the 5 .NET demo test suites + the framework test suite end to end. Test count gains: each demo's unknown-action test still counts as 1 (now asserts throw instead of return); ShellExceptionFilterTests from Task 1 add ~6 new tests.
  </action>
  <verify>
    <automated>cd /home/ubuntu/ViewModelShell && for d in Tasks ContactManager ExpenseTracker RetroBoard HelpDesk; do (cd "demo/$d/AspNetCore.Tests" && dotnet test --nologo) || exit 1; done && (cd viewmodel-shell-dotnet/Tests && dotnet test --nologo)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "throw new UnknownActionException" demo/*/AspNetCore/*Controller.cs | wc -l` returns 8 (7 controllers; HelpDesk has 2: Agent + Requester).
    - `grep -rn "Unknown action:" demo/*/AspNetCore/*Controller.cs | grep -v "throw new Unknown" | wc -l` returns 0 (no stale `Unknown action:` strings remain in BadRequest / BadRequestError calls — input-validation `BadRequest("title required")` etc. don't contain that string).
    - `grep -n "boom" demo/FeatureProbe/AspNetCore/FeatureProbeController.cs` returns at least one match.
    - `grep -n "deliberate test failure" demo/FeatureProbe/AspNetCore/FeatureProbeController.cs` returns at least one match.
    - `grep -rn "Assert.Throws<UnknownActionException>" demo/*/AspNetCore.Tests/*.cs | wc -l` returns 6 (one per unknown-action test across the 5 test projects; HelpDesk has 2 — Agent + Requester).
    - `grep -rn "Assert.IsType<BadRequestObjectResult>" demo/*/AspNetCore.Tests/*.cs | wc -l` returns 10 (the 16 original minus 6 unknown-action assertions — input-validation assertions stay).
    - All 5 .NET demo unit-test suites pass: Tasks 28+, ContactManager 39+, ExpenseTracker 29+, RetroBoard 33+, HelpDesk (Agent+Requester) 43+. (FeatureProbe and Reorder do not have AspNetCore.Tests projects.)
    - `cd viewmodel-shell-dotnet/Tests && dotnet test --nologo` exits 0 (framework + EnvelopeTests + ShellExceptionFilterTests all green).
    - `cd viewmodel-shell-dotnet && dotnet build --nologo -v minimal` exits 0 — every demo builds against the post-change framework.
  </acceptance_criteria>
  <done>
    Every .NET demo's dispatch default arm throws the new framework exception class; FeatureProbe gains the `boom` action; all 6 unknown-action unit tests updated to `Assert.Throws<UnknownActionException>`; the 10 input-validation BadRequestObjectResult assertions remain unchanged per D-08; all 5 .NET demo test suites + framework tests stay green.
  </done>
</task>

<task type="auto">
  <name>Task 2b: TypeScript demo sweep — 6 bun servers + 1 shared handler.ts + FeatureProbe `boom` action</name>
  <files>demo/Tasks-bun/server.ts, demo/ContactManager-bun/server.ts, demo/ExpenseTracker-bun/server.ts, demo/RetroBoard-bun/server.ts, demo/HelpDesk-bun/server.ts, demo/Reorder-bun/server.ts, demo/FeatureProbe-bun/handler.ts</files>
  <read_first>
    - demo/Tasks-bun/server.ts (the canonical TS analog — read full once; line 221 is the default arm; the BadRequestError throws for validation around line 200-ish STAY unchanged per D-08)
    - demo/HelpDesk-bun/server.ts (the file with TWO createAction calls — read both at lines 601 and 851 to confirm both need migration)
    - demo/FeatureProbe-bun/handler.ts (the shared handler — read full once; line 272 is the default arm)
    - viewmodel-shell/src/server.ts (the new UnknownActionError — confirm constructor signature is `(name: string)` and the import path is `import { UnknownActionError } from "@ashley-shrok/viewmodel-shell/server"`)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-PATTERNS.md (Demo controller sweep tables for the TS backend)
    - .planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-CONTEXT.md (D-07, D-08, D-09, D-21)
  </read_first>
  <action>
    Migration — TS servers, mechanical sweep. For each of the 6 -bun servers + 1 shared handler (8 call-sites total because HelpDesk-bun has Agent + Requester):
    - Replace `throw new BadRequestError(\`Unknown action: ${name}\`);` with `throw new UnknownActionError(name);`.
    - Add `UnknownActionError` to the existing import block at the top of each file: the import already pulls from `@ashley-shrok/viewmodel-shell/server` and includes `BadRequestError` and `createAction` etc.; add `UnknownActionError` to that list.
    - DO NOT change any other `BadRequestError(...)` throws (input validation per D-08 — those stay as BadRequestError and get wrapped by the createAction wrapper into `{ok:false, errors:[{message}]}` envelopes without a code).

    Per-file line (verified via grep before this plan):
    | File | Line | Today | After |
    |------|------|-------|-------|
    | demo/Tasks-bun/server.ts | 221 | `throw new BadRequestError(\`Unknown action: ${name}\`);` | `throw new UnknownActionError(name);` |
    | demo/ContactManager-bun/server.ts | 302 | same | same |
    | demo/ExpenseTracker-bun/server.ts | 293 | same | same |
    | demo/RetroBoard-bun/server.ts | 233 | same | same |
    | demo/HelpDesk-bun/server.ts | 601 | same | same |
    | demo/HelpDesk-bun/server.ts | 851 | same | same |
    | demo/Reorder-bun/server.ts | 152 | same | same |
    | demo/FeatureProbe-bun/handler.ts | 272 | same | same |

    (FeatureProbe-bun/handler.ts is shared between `server.ts` and `server-node.ts` — one edit covers both `bun-probe` and `node-probe` parity backends.)

    FeatureProbe `boom` action — add to demo/FeatureProbe-bun/handler.ts immediately before the `default:` (or final `else`) arm:
    ```typescript
    } else if (name === "boom") {
        throw new Error("deliberate test failure");
    }
    ```
    The string `"deliberate test failure"` matches the .NET twin in Task 2a — Plan 04's parity fixture asserts byte-identical messages across all three FeatureProbe backends.

    Smoke test each -bun demo starts cleanly and serves a GET that responds with `"ok":true`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/ViewModelShell && bash -c 'set -e; for d in Tasks ContactManager ExpenseTracker RetroBoard HelpDesk Reorder; do echo "smoke $d-bun"; PORT=$((5050 + RANDOM % 1000)) bun --bun-disable-jit run "demo/$d-bun/server.ts" >/dev/null 2>&1 & PID=$!; sleep 2; kill $PID 2>/dev/null || true; done; echo "smoke FeatureProbe-bun"; bun --bun-disable-jit run "demo/FeatureProbe-bun/server.ts" >/dev/null 2>&1 & PID=$!; sleep 2; kill $PID 2>/dev/null || true; echo "OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "throw new UnknownActionError" demo/*-bun/ demo/FeatureProbe-bun/handler.ts | wc -l` returns 8 (Tasks, ContactManager, ExpenseTracker, RetroBoard, HelpDesk-bun has 2, Reorder, FeatureProbe-bun/handler.ts).
    - `grep -rn "Unknown action:" demo/ --include="*.ts" | grep -v "throw new Unknown" | wc -l` returns 0 (no stale strings remain).
    - `grep -n "UnknownActionError" demo/*-bun/server.ts demo/FeatureProbe-bun/handler.ts | grep -v "throw new" | wc -l` returns 7 (one import line per file).
    - `grep -n "boom" demo/FeatureProbe-bun/handler.ts` returns at least one match.
    - `grep -n "deliberate test failure" demo/FeatureProbe-bun/handler.ts` returns at least one match.
    - Each `-bun` demo starts cleanly (smoke verify exits 0).
  </acceptance_criteria>
  <done>
    Every TS demo's dispatch default arm throws the new framework exception class; FeatureProbe-bun gains the `boom` action throwing the exact message `"deliberate test failure"`; no stale `Unknown action:` strings remain in TS demos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → demo controller | untrusted action submissions cross here; the .NET filter / TS createAction wrapper enforces the envelope on the way out |
| controller throw → framework filter | thrown UnknownActionException / generic Exception cross here on the .NET side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | ShellExceptionFilter generic-Exception catch arm | mitigate | `ShellErrorResponse.OfUncaught` reads only `ex.Message` (locked in Plan 01); StackTrace is logged via ILogger server-side only. Tests assert serialized message contains no stack-trace markers (`"at "`, `"System."`). |
| T-07-08 | Tampering | client-submitted action names | accept | Action names are arbitrary strings the client controls; the `UnknownActionException(name)` echoes the name back in the envelope message. Worst case: a client sees their own crafted name in their own error response. Not a new vector — the same was true under the BadRequest("Unknown action: {name}") flow. |
| T-07-09 | Denial of Service | `boom` action triggers uncaught throw on FeatureProbe in production-like demos | accept | FeatureProbe is a parity-exercise demo, never deployed to production. The `boom` action is a deliberate test surface; demo Program.cs files MAY add a check that disables `boom` unless an env var is set if anyone ever ships FeatureProbe externally — out of scope for v1.0.0 (FeatureProbe is dev-only). Documented inline at the boom handler. |
| T-07-SC | Tampering | npm/nuget installs | accept | No new packages added in this phase. Existing dependency set is unchanged across all demo workspaces. |
</threat_model>

<verification>
- `cd viewmodel-shell-dotnet/Tests && dotnet test --nologo` — framework tests + EnvelopeTests + ShellExceptionFilterTests all green.
- All 5 .NET demo test suites (Tasks, ContactManager, ExpenseTracker, RetroBoard, HelpDesk) pass — verified by Task 2a's verify command iterating exactly those five.
- All -bun demos start cleanly and serve a GET that responds with `"ok":true`.
- `grep -rn "Unknown action:" demo/ --include="*.cs" --include="*.ts" | grep -v "throw new Unknown"` returns nothing (full sweep verification across both backends).
- Parity is now READY to be run by Plan 04. (Don't run it in this plan — the new envelope fixture doesn't exist yet, and the existing fixtures' assertions will diff on the new `ok:true` field unless Plan 04 adds it. That's Plan 04's gate.)
</verification>

<success_criteria>
- 8 .NET controller call-sites + 8 TS dispatch sites (including the two HelpDesk-bun calls and the shared FeatureProbe-bun handler) all throw the new framework exception class on unknown actions.
- The .NET ShellExceptionFilter exists, is registered in every .NET demo Program.cs via the uniform `Filters.Add<ShellExceptionFilter>()` form, and has full xUnit coverage with T1 information-disclosure mitigation asserted.
- FeatureProbe has a `boom` action on both backends throwing a generic exception with the exact message `"deliberate test failure"`.
- 6 existing unknown-action unit tests across 5 .NET test projects are updated to `Assert.Throws<UnknownActionException>`; 10 input-validation BadRequestObjectResult assertions remain unchanged per D-08.
- All pre-existing demo + framework tests on both backends stay green.
- No stale `Unknown action:` BadRequest strings remain in the codebase.
</success_criteria>

<output>
Create `.planning/phases/07-error-envelope-ok-flag-1-0-0-release-closeout/07-03-SUMMARY.md` when done. Include: confirmed file:line changes per controller / server (matches the planner's tables or notes any drift), the exact ShellExceptionFilter interface signature and any deviation from `IAsyncExceptionFilter + IAsyncResultFilter`, demo test counts pre- and post-change (Tasks / ContactManager / ExpenseTracker / RetroBoard / HelpDesk — note FeatureProbe and Reorder do not have AspNetCore.Tests projects), the 6 updated unknown-action test method names and assertion shape, and the per-demo TS smoke-test result.
</output>
