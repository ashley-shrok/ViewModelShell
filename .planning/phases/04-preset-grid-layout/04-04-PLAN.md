---
phase: 04-preset-grid-layout
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - AGENTS.md
autonomous: true
requirements: [LAYOUT-05]
must_haves:
  truths:
    - "AGENTS.md node-types table documents the optional `layout` field on `page` and `section` with its closed-union values and byte-identical default"
    - "AGENTS.md CSS-class table lists `.vms-page--split` / `.vms-page--cards` and `.vms-section--split` / `.vms-section--cards`"
    - "The update is accurate-only — no broader doc polish, no version-string change, no other table churn"
  artifacts:
    - path: "AGENTS.md"
      provides: "node-table page/section rows + CSS-class table page/section rows updated for the layout field"
      contains: ".vms-page--split"
  key_links:
    - from: "AGENTS.md node table page/section rows"
      to: "the actual TS/.NET layout? field shipped in Plan 01 + the CSS classes shipped in Plan 02"
      via: "accurate-only documentation of the closed-union values and emitted classes"
      pattern: "layout.*split.*cards"
---

<objective>
Keep AGENTS.md's two reference tables merely *accurate* for the `layout` field
this phase ships: add the optional `layout` preset to the `page` and `section`
rows of the node-types table, and add `.vms-{page|section}--split` /
`.vms-{page|section}--cards` to the CSS-classes table. ACCURATE-ONLY — Phase 5
EXAMPLES-03 owns the full doc polish; do NOT rewrite prose, restructure tables,
add a usage guide, or touch the version-alignment rule (D-11 — Phase 5 RELEASE-01
owns the 0.4.0 bump).

Purpose: An agent reading AGENTS.md alone must see the new field exists with its
closed-union values and byte-identical default (consistent with how `density`/
`variant` were documented in Phase 3). This is the documentation half of LAYOUT-05.

Output: two surgically-updated table rows + the CSS-class rows in AGENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-preset-grid-layout/04-CONTEXT.md
@.planning/phases/04-preset-grid-layout/04-RESEARCH.md
@.planning/phases/04-preset-grid-layout/04-01-SUMMARY.md

<interfaces>
<!-- VERIFIED current AGENTS.md rows. These mirror exactly how Phase 3
     documented density/variant — extend the SAME parenthetical idiom. -->

From AGENTS.md "## Node types" table (lines 105-106, VERIFIED current):
```
| `page` | Root container with optional title; optional `density`: `"comfortable"` \| `"compact"` (compact emits `.vms-page--compact`; omitted/comfortable = no modifier, byte-identical) |
| `section` | Grouped content with optional heading; optional `variant`: `"card"` → `.vms-section--card` (grouped surface; omitted = no modifier, byte-identical) |
```

From AGENTS.md "### CSS classes emitted by BrowserAdapter" table (lines 141-142, VERIFIED current):
```
| page | `.vms-page`, `.vms-page__title`, `.vms-page--compact` |
| section | `.vms-section`, `.vms-section__heading`, `.vms-section--card` |
```

The field/classes being documented (shipped by Plans 01/02):
- TS/.NET: `layout?: "stack" | "split" | "cards"` — omitted OR `"stack"` = no
  modifier (byte-identical); `"split"`/`"cards"` emit the modifier.
- CSS classes: `.vms-page--split`, `.vms-page--cards`, `.vms-section--split`,
  `.vms-section--cards`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the layout field + classes to AGENTS.md's two reference tables (accurate-only)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (lines 100-150 — the "## Node types" table page/section rows ~105-106 and the "### CSS classes emitted by BrowserAdapter" table page/section rows ~141-142; observe the exact parenthetical idiom Phase 3 used for density/variant and mirror it)
    - viewmodel-shell/src/index.ts (lines 59-73 — confirm the exact `layout?: "stack" | "split" | "cards"` union and JSDoc wording from Plan 01 so the doc matches the code)
    - .planning/phases/04-preset-grid-layout/04-CONTEXT.md domain section "Out of this phase" + D-11 (AGENTS.md accurate-only, NO full polish — Phase 5 EXAMPLES-03; NO version bump — Phase 5 RELEASE-01)
  </read_first>
  <action>
    Make exactly two surgical edits to `AGENTS.md`. Mirror the existing
    density/variant parenthetical phrasing precisely — do NOT reword the existing
    text, do NOT restructure either table, do NOT add new sections.

    EDIT 1 — "## Node types" table. Append the `layout` clause to the END of the
    existing `page` and `section` row text (after the existing density/variant
    clause, same sentence style):

    `page` row becomes:
    ```
    | `page` | Root container with optional title; optional `density`: `"comfortable"` \| `"compact"` (compact emits `.vms-page--compact`; omitted/comfortable = no modifier, byte-identical); optional `layout`: `"stack"` \| `"split"` \| `"cards"` (`split`/`cards` emit `.vms-page--split`/`.vms-page--cards`; omitted/`stack` = no modifier, byte-identical vertical flow) |
    ```

    `section` row becomes:
    ```
    | `section` | Grouped content with optional heading; optional `variant`: `"card"` → `.vms-section--card` (grouped surface; omitted = no modifier, byte-identical); optional `layout`: `"stack"` \| `"split"` \| `"cards"` (`split`/`cards` emit `.vms-section--split`/`.vms-section--cards`; omitted/`stack` = no modifier, byte-identical vertical flow) |
    ```

    EDIT 2 — "### CSS classes emitted by BrowserAdapter" table. Append the new
    classes to the END of the existing `page` and `section` class lists:

    `page` row becomes:
    ```
    | page | `.vms-page`, `.vms-page__title`, `.vms-page--compact`, `.vms-page--split`, `.vms-page--cards` |
    ```

    `section` row becomes:
    ```
    | section | `.vms-section`, `.vms-section__heading`, `.vms-section--card`, `.vms-section--split`, `.vms-section--cards` |
    ```

    Hard constraints:
    - ACCURATE-ONLY: change ONLY these 4 table rows. Do NOT polish other prose,
      add a presets usage guide, add examples, or restructure tables (Phase 5
      EXAMPLES-03 owns full polish — explicitly out of this phase).
    - Do NOT change any version string / the major.minor-alignment rule text
      (D-11 — Phase 5 RELEASE-01 owns the 0.4.0 bump).
    - Do NOT edit any other file.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff --name-only -- AGENTS.md | grep -qx AGENTS.md && [ "$(git diff --name-only | wc -l)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md `page` node-table row contains `optional \`layout\`` and the values `"stack"`, `"split"`, `"cards"` and references `.vms-page--split` and `.vms-page--cards`
    - AGENTS.md `section` node-table row contains the equivalent `layout` clause referencing `.vms-section--split` and `.vms-section--cards`
    - AGENTS.md CSS-class table `page` row contains `.vms-page--split` and `.vms-page--cards`; `section` row contains `.vms-section--split` and `.vms-section--cards`
    - The existing density/variant clauses and `.vms-page--compact`/`.vms-section--card` entries are STILL present (appended to, not replaced)
    - `git diff` for this plan touches ONLY `AGENTS.md` (no other file changed)
    - No version string or major.minor-alignment rule text changed in AGENTS.md (D-11)
    - The diff is limited to the 4 targeted table rows (no unrelated prose/section churn — accurate-only)
  </acceptance_criteria>
  <done>AGENTS.md's node table + CSS-class table accurately document the `layout` field/classes; nothing else changed; no version bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits a Markdown documentation file only. No code path, no input parsing, no network/file/exec surface, no wire-format change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | AGENTS.md content | accept | The file documents only the already-public closed-union `layout` field and its emitted CSS classes (shipped in Plans 01/02). No secret, credential, or non-public detail is added. No residual risk. |

**Severity conclusion:** No `high` or `medium` threats. A documentation-only,
accurate-only Markdown edit has no attack surface; it merely describes the
closed-union field already mitigated-by-design in Plans 01/02.
</threat_model>

<verification>
- `git diff` touches ONLY `AGENTS.md`.
- The `page` and `section` rows of the node-types table now include the `layout`
  closed-union clause; the CSS-class table `page`/`section` rows now include
  `.vms-{page|section}--split` and `.vms-{page|section}--cards`.
- Existing density/variant documentation and `.vms-page--compact`/`.vms-section--card`
  entries remain (appended, not replaced).
- No version string / alignment-rule text changed (D-11); no other prose touched
  (accurate-only — Phase 5 EXAMPLES-03 owns full polish).
</verification>

<success_criteria>
- LAYOUT-05 (doc half): an agent reading AGENTS.md alone sees the `layout` field
  on `page`/`section` with its closed-union values, byte-identical default, and
  emitted classes — documented in the same idiom as Phase 3's density/variant.
- Accurate-only: exactly 4 table rows changed, nothing else; no version bump (D-11).
</success_criteria>

<output>
After completion, create `.planning/phases/04-preset-grid-layout/04-04-SUMMARY.md`
</output>
