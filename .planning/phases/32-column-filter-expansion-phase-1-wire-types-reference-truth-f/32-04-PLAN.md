---
phase: 32-column-filter-expansion-phase-1-wire-types-reference-truth-f
plan: 04
type: execute
wave: 3
depends_on:
  - 32-03
files_modified:
  - viewmodel-shell/CHANGELOG.md
autonomous: true
requirements:
  - REQ-CF1-01
  - REQ-CF1-02
  - REQ-CF1-03
  - REQ-CF1-04
  - REQ-CF1-05
  - REQ-CF1-06
  - REQ-CF1-07
  - REQ-CF1-08

must_haves:
  truths:
    - "CHANGELOG.md gains a new entry staged under ## Unreleased (NOT a versioned release header — Phase 32 does NOT publish). The entry documents: the additive wire additions (FilterSpec on TableColumn, FilterDescriptorBinds on TableNode, FilterDescriptor/FilterRule/ValueKind/operator vocabularies/MatchingHint types on both backends); the reference truth function (matchesFilter on TS server subpath; FilterHelper.MatchesFilter on .NET main package); the parity coverage (wire-shape fixture + helper fixture); consumer action (None — additive optional, existing filterable/filterValue/filterBinds/filterAction shapes remain valid). Entry follows Keep-a-Changelog format used throughout the file."
    - "The full green-tree gate runs and exits 0 BEFORE the CHANGELOG entry is written (gate is the precondition). Every command in the gate list runs in order. Any failure halts this plan immediately and surfaces to the operator — do NOT write the CHANGELOG entry if the gate is red."
    - "No version bump in package.json or .csproj — Phase 32 is additive internal work; the version bump + publish + tag + main-advance + announce happen in Phase 33."
    - "MIGRATION.md is NOT touched in this phase (migration notes belong with the removal of the old wire in Phase 33)."
    - "No npm publish, no dotnet nuget push, no git tag, no main advance, no #vms-announcements post — per SPEC boundary 'Out of scope: Publish'."
    - "The gate commands run in this order: (1) cd viewmodel-shell && npm run build; (2) cd viewmodel-shell && npm run check:test-types; (3) cd viewmodel-shell && npm run check:core-globals; (4) cd viewmodel-shell && npm run check:aa-contrast; (5) cd viewmodel-shell && npm run check:no-demo-style; (6) cd viewmodel-shell && npm run check:demo-types; (7) cd viewmodel-shell && npx vitest run; (8) export PATH=$HOME/.dotnet:$PATH && dotnet test viewmodel-shell-dotnet/Tests; (9) for p in $(find demo -name '*.Tests.csproj'); do dotnet test \"$p\"; done; (10) dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal; (11) bun run parity/run.ts. All 11 steps must exit 0."
  artifacts:
    - path: viewmodel-shell/CHANGELOG.md
      provides: "Staged ## Unreleased entry documenting Phase 32 additive wire additions and reference truth function"
      contains: "matchesFilter"
  key_links:
    - from: "Green-tree gate (all 11 commands exit 0)"
      to: "CHANGELOG entry authorization"
      via: "AGENTS.md working agreement — NEVER PUSH ANYTHING BROKEN"
      pattern: "GREEN|exits 0"
---

<objective>
Run the full green-tree gate and stage the CHANGELOG.md entry under ## Unreleased for Phase 32's additive wire additions. No version bump, no publish, no MIGRATION.md — those belong to Phase 33.

Purpose: Close Phase 32 cleanly — every prior plan's work is proven by the gate; the CHANGELOG documents what shipped for consumers and for the Phase 33 release author who will write the versioned header. The gate is the proof that Phase 32 delivered what it promised without breaking anything.

Output: Green gate (all 11 commands exit 0); CHANGELOG.md gains a Unreleased entry for Phase 32.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-SPEC.md
@.planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-CONTEXT.md
@AGENTS.md
</context>

<interfaces>
**CHANGELOG.md current structure (read before writing):**
The file uses Keep-a-Changelog format with `## Unreleased` at the top (if no Unreleased section exists, create one above the most recent versioned header). The entry format follows the existing style — bullet-pointed sections with `### Added` / `### Changed` etc. sub-headers. The most recent version entry follows the textnode-maxlines / chat-composer entries for reference style.

**Green-tree gate command list (from AGENTS.md working agreement + SPEC constraints):**
1. `cd viewmodel-shell && npm run build`
2. `cd viewmodel-shell && npm run check:test-types`
3. `cd viewmodel-shell && npm run check:core-globals`
4. `cd viewmodel-shell && npm run check:aa-contrast`
5. `cd viewmodel-shell && npm run check:no-demo-style`
6. `cd viewmodel-shell && npm run check:demo-types`
7. `cd viewmodel-shell && npx vitest run`
8. `export PATH="$HOME/.dotnet:$PATH" && dotnet test viewmodel-shell-dotnet/Tests --nologo`
9. `for p in $(find /home/thenasty/ViewModelShell/demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo; done`
10. `dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal`
11. `export PATH="$HOME/.dotnet:$PATH" && bun run parity/run.ts`

**CHANGELOG Unreleased entry target content:**

```
## Unreleased

### Added

- **Typed column filter wire vocabulary** (Phase 32 — additive, no version bump yet): `TableColumn.filter?: FilterSpec` declares a filterable column's value-kind (`"text" | "number" | "date" | "fixed-set" | "yes-no"`), optional fixed-set option list, and optional matching hints (`"ignore-punctuation"`). `TableNode.filterDescriptorBinds?: Record<string, string>` maps column keys to state paths holding each column's current `FilterDescriptor` (an ordered list of rules + `"all-of" | "any-of"` joiner). Types on both backends: `FilterSpec`, `FilterDescriptor`, `FilterRule`, `ValueKind`, `MatchingHint`, and per-type closed operator aliases (`TextOperator`, `NumberOperator`, `DateOperator`, `FixedSetOperator`, `YesNoOperator`). The existing `filterable`/`filterValue`/`filterBinds`/`filterAction` fields remain valid and untouched — removal happens in Phase 33 alongside the adapter UI and major-version release.
- **Reference truth function** — `matchesFilter(descriptor, rawValue, displayString, kind, matchingHints?)` exported from `@ashley-shrok/viewmodel-shell/server` (TypeScript); `FilterHelper.MatchesFilter(descriptor, rawValue, displayString, kind, matchingHints?)` in `AshleyShrok.ViewModelShell` (C#). Given a `FilterDescriptor` from state and a row's cell data, returns `true`/`false`. Every operator × applicable value-kind is implemented: `contains` (case-insensitive, display-string match, honors `ignore-punctuation` hint); `equals`, `starts-with`, `ends-with` (text); `equals`, `does-not-equal`, `greater-than`, `greater-than-or-equal`, `less-than`, `less-than-or-equal`, `between` (number); `is`, `before`, `after`, `in-range` (date — ISO-8601 string comparison, format-agnostic per D-03); `is`, `is-not` (fixed-set); `is-true`, `is-false` (yes-no); `is-empty`, `is-not-empty` (all kinds — `null`/`undefined`/`""` = empty; whitespace-only = non-empty). Multi-rule combination via `all-of`/`any-of`, no short-circuit. Both backends proven byte-identical by parity fixture. Consumer action: import and call in your action handler — no middleware or DI.
- **Parity coverage**: `column-filter-wire-shape` fixture proves the new wire types serialize byte-identically across all FeatureProbe backends (the four `FilterDescriptor` combinations from SPEC Req 3, including the `is-empty` rule's absent `value` field); `column-filter-helper` fixture runs ~100 representative truth-function cases through both backends via HTTP and diffs byte-identically. NASA-level in-process suites (vitest + xUnit) cover the full operator × kind × value-shape Cartesian on each backend independently.
```

Adjust formatting to match the exact existing style in CHANGELOG.md. If a `## Unreleased` section already exists, append the new `### Added` bullets under it (or add the `### Added` sub-header if it's not there yet).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full green-tree gate (11 commands) and stage CHANGELOG.md Unreleased entry</name>
  <files>viewmodel-shell/CHANGELOG.md</files>
  <read_first>
    - viewmodel-shell/CHANGELOG.md — READ the first 60 lines to understand the current Unreleased section structure and the most recent versioned entry's formatting. The new entry must match the existing style precisely.
    - AGENTS.md — READ the "NEVER PUBLISH OR PUSH ANYTHING BROKEN" working agreement and the exact gate command list. Read the "CHANGELOG.md + MIGRATION.md are release-gated, not HEAD-synced" convention: Unreleased entries ARE allowed here; the versioned header + MIGRATION.md wait for Phase 33.
    - .planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-SPEC.md — READ the full Acceptance Criteria section (lines ~120-133) to confirm every checkbox is satisfied before writing the CHANGELOG entry.
  </read_first>
  <action>
    STEP 1 — Run the green-tree gate. Execute each of the 11 commands from the interfaces block IN ORDER. For each command: capture exit code and last 5 lines of output. If any command exits non-zero, HALT immediately and report the failure to the operator. Do NOT write the CHANGELOG entry until ALL 11 commands exit 0.

    Gate commands (run from repo root /home/thenasty/ViewModelShell):
    1. `cd viewmodel-shell && npm run build`
    2. `cd viewmodel-shell && npm run check:test-types`
    3. `cd viewmodel-shell && npm run check:core-globals`
    4. `cd viewmodel-shell && npm run check:aa-contrast`
    5. `cd viewmodel-shell && npm run check:no-demo-style`
    6. `cd viewmodel-shell && npm run check:demo-types`
    7. `cd viewmodel-shell && npx vitest run`
    8. `export PATH="$HOME/.dotnet:$PATH" && dotnet test viewmodel-shell-dotnet/Tests --nologo`
    9. `for p in $(find /home/thenasty/ViewModelShell/demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo; done`
    10. `export PATH="$HOME/.dotnet:$PATH" && dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal`
    11. `export PATH="$HOME/.dotnet:$PATH" && bun run parity/run.ts`

    STEP 2 — AFTER all 11 commands exit 0, read viewmodel-shell/CHANGELOG.md. Locate the `## Unreleased` section (or the top of the file if none exists). Insert the CHANGELOG entry from the interfaces block under `## Unreleased` / `### Added`. Match the existing bullet-point style and spacing exactly.

    STEP 3 — Verify the CHANGELOG entry did not accidentally break any existing content below it (read the file after writing to confirm the insertion point is correct and no prior entry was overwritten).

    Do NOT bump any version numbers. Do NOT write to MIGRATION.md. Do NOT run npm publish or dotnet nuget push. Do NOT create a git tag.
  </action>
  <verify>
    <automated>grep -c "matchesFilter" viewmodel-shell/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - All 11 gate commands exit 0. Gate failures captured in SUMMARY with command + output.
    - `grep -c "matchesFilter" viewmodel-shell/CHANGELOG.md` returns >= 1 (entry present).
    - `grep -c "FilterSpec" viewmodel-shell/CHANGELOG.md` returns >= 1.
    - `grep -c "filterDescriptorBinds" viewmodel-shell/CHANGELOG.md` returns >= 1.
    - `grep -c "ignore-punctuation" viewmodel-shell/CHANGELOG.md` returns >= 1.
    - `grep -c "## Unreleased" viewmodel-shell/CHANGELOG.md` returns >= 1 (section exists).
    - `grep -n "version" viewmodel-shell/package.json | head -1` — version field is UNCHANGED from what it was before this plan (no version bump).
    - MIGRATION.md is UNCHANGED (no new entry).
    - No `npm publish` was run. No `dotnet nuget push` was run. No `git tag` was created.
    - SPEC Req 3 acceptance criterion confirmed: parity fixture at column-filter-wire-shape.json passed in gate step 11.
    - SPEC Req 7 acceptance criterion confirmed: parity fixture at column-filter-helper.json passed in gate step 11.
    - SPEC Req 8 mutation-verify documented in the SUMMARY for this plan (cross-referencing the mutation-verify entries from 32-01 and 32-02 SUMMARYs).
  </acceptance_criteria>
  <done>
    Phase 32 is complete. All 13 SPEC acceptance criteria are satisfied. Green-tree gate green. CHANGELOG staged. No publish, no tag, no announce — Phase 33 owns the release ritual.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate commands → local filesystem | The gate runs local test + build commands. No network calls except parity/run.ts (which starts local backend processes). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04-01 | Tampering | CHANGELOG.md insertion breaking existing entries | mitigate | Read the file before writing; the Write tool overwrites, so read-then-write ensures the existing content is preserved. Verify step reads the file after write to confirm insertion point is correct. |
| T-32-04-02 | Elevation of privilege | Running gate commands might accidentally trigger publish | mitigate | Gate commands explicitly listed in plan — none include `npm publish`, `dotnet nuget push`, or `git tag`. The task action explicitly forbids these. |
| T-32-04-SC | Tampering | package installs | accept | No new package installs in this plan. Gate only runs existing test + build commands. |
</threat_model>

<verification>
Phase 32 completion checklist (all must be true before SUMMARY is written):
- [ ] All 11 gate commands exit 0 (captured in SUMMARY with exit code + tail output).
- [ ] CHANGELOG.md staged entry present under ## Unreleased.
- [ ] package.json version unchanged.
- [ ] MIGRATION.md unchanged.
- [ ] Old wire fields (filterable, filterValue, filterBinds, filterAction) still present in index.ts and ViewModels.cs (grep confirms).
- [ ] No HelpDesk / Showcase / FeatureProbe tests broken (covered by gate step 9).
- [ ] Both parity fixtures green (gate step 11).
- [ ] Mutation-verify results from Plans 32-01 and 32-02 referenced in this SUMMARY.
</verification>

<success_criteria>
All 11 green-tree gate commands exit 0. CHANGELOG.md has a staged ## Unreleased entry documenting the additive wire vocabulary, the reference truth function, and the parity coverage. No version bump, no publish, no tag, no MIGRATION.md entry. Phase 32 is complete; Phase 33 owns the release ritual.
</success_criteria>

<output>
Create `.planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-04-SUMMARY.md`. Record: exit code + tail output for each of the 11 gate commands; CHANGELOG.md diff (before/after for the Unreleased section); cross-references to the mutation-verify results from Plans 32-01 and 32-02; confirmation that all 13 SPEC acceptance criteria are satisfied with pointers to which plan/file satisfies each.

**Commit intent:** `docs(32): stage CHANGELOG Unreleased entry for Phase 32 column-filter wire additions + green-tree gate green`
</output>
