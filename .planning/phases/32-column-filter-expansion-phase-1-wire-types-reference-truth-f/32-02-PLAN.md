---
phase: 32-column-filter-expansion-phase-1-wire-types-reference-truth-f
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell-dotnet/ViewModels.cs
  - viewmodel-shell-dotnet/FilterHelper.cs
  - viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs
autonomous: true
requirements:
  - REQ-CF1-01
  - REQ-CF1-02
  - REQ-CF1-03
  - REQ-CF1-04
  - REQ-CF1-05
  - REQ-CF1-06
  - REQ-CF1-08

must_haves:
  truths:
    - "TableColumn record in ViewModels.cs gains a new optional positional parameter `FilterSpec? Filter = null` appended LAST in the primary constructor, carrying `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` per AGENTS.md gotcha #8. The field is placed after the existing Tooltip parameter. All existing positional parameters remain UNCHANGED in order and count."
    - "TableNode record in ViewModels.cs gains a new optional positional parameter `Dictionary<string, string>? FilterDescriptorBinds = null` appended LAST, carrying `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]`. Parallel to the existing FilterBinds parameter (which remains untouched)."
    - "New .NET record types in ViewModels.cs (byte-parallel to TS twins): FilterSpec record (Kind string, Options string[]? null, MatchingHints string[]? null — all nullable carry WhenWritingNull); FilterRule record (Operator string, Value object? null — Value carries WhenWritingNull, never emits null on the wire); FilterDescriptor record (Rules IReadOnlyList<FilterRule>, Joiner string). String-typed for all closed-union fields per AGENTS.md 'closed unions enforced on ONE side only' invariant."
    - "New file viewmodel-shell-dotnet/FilterHelper.cs exports a public static class FilterHelper in the ViewModelShell namespace with a public static method `MatchesFilter(FilterDescriptor descriptor, object? rawValue, string displayString, string kind, IReadOnlyList<string>? matchingHints = null)` returning bool. Every operator x applicable value-kind is implemented with semantics byte-identical to the TS matchesFilter in Plan 32-01."
    - "New xUnit test file viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs covers the same NASA Cartesian product as the TS suite: 1-rule exhaustive per operator x kind x value shape, 2-rule representative both joiners, 3-rule diagonal both joiners, four D-03 ISO date shapes, the ignore-punctuation hint, whitespace-only is NOT empty, negative integers, decimals, and negative decimals for number kind per SPEC Req 8. Per-case names are auto-generated from the tuple using xUnit [Theory] / [InlineData] or [MemberData]. Suite is byte-parallel with Plan 32-01's vitest suite (same cases, same expected results)."
    - "Mutation-verify session: reverting each operator in FilterHelper.MatchesFilter to a constant stub (return false) causes at least one corresponding test in ColumnFilterTests.cs to go red. Documented in SUMMARY."
    - "Old wire fields (Filterable bool, FilterValue string?, FilterBinds Dictionary<string,string>?, FilterAction ActionDescriptor?) remain present and UNTOUCHED on TableColumn and TableNode. No existing .csproj files changed — ColumnFilterTests.cs is auto-discovered by the Tests project."
    - "Every new nullable wire field carries WhenWritingNull per gotcha #8: FilterSpec? Filter, Dictionary<string,string>? FilterDescriptorBinds, FilterSpec.Options string[]?, FilterSpec.MatchingHints string[]?, FilterRule.Value object?. No exception."
    - "The Markdown companion package still compiles against the updated ViewModels.cs. No ctor arity changes on records the Markdown companion touches (TableColumn and TableNode are NOT touched by the Markdown companion — verified: MarkdownConverter.cs does not construct TableColumn or TableNode). Adding positional params to TableColumn/TableNode is safe here — the companion binary-compat gate concern from Phase 31 applies to TextNode (which Markdown DOES construct); Markdown does NOT construct TableColumn/TableNode."
  artifacts:
    - path: viewmodel-shell-dotnet/ViewModels.cs
      provides: "FilterSpec, FilterDescriptor, FilterRule .NET records + TableColumn.Filter? + TableNode.FilterDescriptorBinds? — every nullable carries WhenWritingNull per gotcha #8"
      contains: "FilterSpec"
    - path: viewmodel-shell-dotnet/FilterHelper.cs
      provides: "FilterHelper.MatchesFilter public static method — byte-identical behavior to TS matchesFilter"
      contains: "MatchesFilter"
    - path: viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs
      provides: "xUnit NASA-level test suite: 1-rule exhaustive x operator x kind x value-shape, 2-rule representative, 3-rule diagonal, both joiners"
      contains: "MatchesFilter"
  key_links:
    - from: "FilterSpec? Filter on TableColumn"
      to: "WhenWritingNull attribute"
      via: "[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]"
      pattern: "WhenWritingNull"
    - from: "FilterRule.Value object? = null"
      to: "absent-not-null wire contract"
      via: "WhenWritingNull on Value — never emits \"value\": null when operator carries no value"
      pattern: "FilterRule.*Value"
    - from: "FilterHelper.MatchesFilter"
      to: "byte-parallel with TS matchesFilter"
      via: "same operator semantics, same joiner logic, same isEmpty definition"
      pattern: "MatchesFilter"
---

<objective>
Add the .NET wire twins (FilterSpec, FilterDescriptor, FilterRule records in ViewModels.cs; TableColumn.Filter? and TableNode.FilterDescriptorBinds?) and the reference truth function (FilterHelper.MatchesFilter in a new FilterHelper.cs), plus the byte-parallel xUnit NASA test suite in Tests/ColumnFilterTests.cs.

Purpose: This is the .NET half of Phase 32 wire + logic work. Runs in parallel with Plan 32-01 (TS). Both plans are wave 1 — they can execute simultaneously since they touch disjoint files. Byte-identical wire with Plan 32-01 is the whole point; Plan 32-03 (parity fixture, wave 2) will prove it. AGENTS.md gotcha #8 posture is non-negotiable on every new nullable field.

Output: FilterSpec/FilterDescriptor/FilterRule records on ViewModels.cs; FilterHelper.cs with MatchesFilter; xUnit suite in Tests/ColumnFilterTests.cs proving every operator × type × value-shape; `dotnet test viewmodel-shell-dotnet/Tests` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-SPEC.md
@.planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-CONTEXT.md
@AGENTS.md
@.planning/phases/31-textnode-maxlines-axis-closed-enum-line-cap-primitive-route-/31-02-PLAN.md
</context>

<interfaces>
**Existing TableColumn record (ViewModels.cs lines ~2004-2018):**

```csharp
public record TableColumn(
    string Key,
    string Label,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool Sortable = false,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool Filterable = false,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? FilterValue = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? LinkLabel = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool LinkExternal = false,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Tooltip = null
);
```

Append `FilterSpec? Filter = null` with WhenWritingNull as the LAST positional parameter.

**Existing TableNode record (ViewModels.cs lines ~2073-2094):**
Key parameters: Columns, Rows, SortBind, FilterBinds, PaginationBind, SortActions, FilterAction, Pagination, Selection.
Append `Dictionary<string, string>? FilterDescriptorBinds = null` with WhenWritingNull as the LAST positional parameter.

**New records to define (before or after TableColumn — keep wire-type order):**

FilterSpec record:
```csharp
public record FilterSpec(
    string Kind,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] IReadOnlyList<string>? Options = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] IReadOnlyList<string>? MatchingHints = null
);
```

FilterRule record:
```csharp
public record FilterRule(
    string Operator,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] object? Value = null
);
```

FilterDescriptor record:
```csharp
public record FilterDescriptor(
    IReadOnlyList<FilterRule> Rules,
    string Joiner
);
```

**FilterHelper.cs structure (new file in viewmodel-shell-dotnet/):**
```csharp
namespace ViewModelShell;
public static class FilterHelper
{
    public static bool MatchesFilter(
        FilterDescriptor descriptor,
        object? rawValue,
        string displayString,
        string kind,
        IReadOnlyList<string>? matchingHints = null) { ... }

    private static bool EvaluateRule(FilterRule rule, object? rawValue, string displayString, string kind, IReadOnlyList<string>? matchingHints) { ... }
    private static bool IsEmpty(object? v) => v is null || (v is string s && s == "");
    private static bool ApplyContains(string displayString, object? ruleValue, IReadOnlyList<string>? matchingHints) { ... }
}
```

**xUnit test file pattern:** `viewmodel-shell-dotnet/Tests/TextWeightSerializationTests.cs` or `CompositeStateAxisSerializationTests.cs` for file structure. For parameterized cases, use `[Theory]` + `[MemberData]` with a `public static IEnumerable<object[]>` method returning per-case tuples: `(string caseName, FilterDescriptor descriptor, object? rawValue, string displayString, string kind, string[]? matchingHints, bool expected)`.

**Operator strings (byte-parallel with TS):** Same kebab-case strings as TypeScript (e.g. "starts-with", "does-not-equal", "greater-than-or-equal", "is-empty", "is-not-empty", "is-true", "is-false", "in-range", "fixed-set", "yes-no"). Typed as string in .NET (not an enum) per closed-unions-on-one-side invariant.

**between / in-range value shape on .NET:** Rule.Value for between/in-range is a JSON array when deserialized. Deserialize via `JsonSerializer.Deserialize<double[]>(jsonValue.ToString()!)` (or cast to JsonElement and read). Use a robust approach that handles both pre-deserialized JsonElement (from round-tripped state) and already-deserialized arrays.

**Date string comparison:** Use `string.Compare(a, b, StringComparison.Ordinal)` — ISO-8601 strings sort chronologically by ordinal comparison. Same semantics as the TS string comparison in Plan 32-01.

**Number comparison:** Convert rawValue to double via `Convert.ToDouble(rawValue)` (handles int, long, double, string-numeric, JsonElement.GetDouble()). Convert ruleValue similarly. This handles negative integers (e.g. -5), decimals (e.g. 3.14), and negative decimals (e.g. -2.5) correctly via Convert.ToDouble.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add .NET wire types to ViewModels.cs (FilterSpec, FilterDescriptor, FilterRule records + TableColumn.Filter? + TableNode.FilterDescriptorBinds?)</name>
  <files>viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell-dotnet/ViewModels.cs — READ lines ~2004-2094 (TableColumn record, TableRow record, TablePagination record, TableNode record). Note the exact positional parameter lists; append new params LAST.
    - .planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-SPEC.md — READ Requirements 1-4 for exact field names and shapes.
    - .planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-CONTEXT.md — READ D-01 (FilterSpec shape — kind + options? + matchingHints?).
    - AGENTS.md — READ gotcha #8 fully: every new nullable field carries WhenWritingNull; the state record exception (not this plan — ViewModels.cs IS the framework's type file); "closed unions enforced on ONE side only" invariant (Kind, Operator, Joiner are all string-typed, not enum).
    - viewmodel-shell-dotnet/ViewModels.cs — READ the file header comment block (lines 1-50 approx) for the existing WhenWritingNull convention comment; any new record should include a brief comment citing the same rule.
  </read_first>
  <action>
    Three steps in ViewModels.cs:

    STEP 1 — Add new records before the TableColumn declaration. Insert FilterSpec, FilterRule, FilterDescriptor in that order. Each carries XML documentation comments explaining: closed-union fields are string-typed (not enum) per AGENTS.md invariant; FilterRule.Value is absent (null) when the operator carries no value (is-empty, is-not-empty, is-true, is-false). WhenWritingNull is MANDATORY on every nullable field.

    FilterSpec constructor parameters: `string Kind`, `IReadOnlyList<string>? Options = null` with WhenWritingNull, `IReadOnlyList<string>? MatchingHints = null` with WhenWritingNull.

    FilterRule constructor parameters: `string Operator`, `object? Value = null` with WhenWritingNull. XML doc: "Value is ABSENT (WhenWritingNull) when the operator carries no value — absent is not null, per the VMS wire contract. Never set Value to null explicitly — omit it. Byte-parallel with TS FilterRule.value? which is structurally absent when not set."

    FilterDescriptor constructor parameters: `IReadOnlyList<FilterRule> Rules`, `string Joiner`.

    STEP 2 — Append to TableColumn: add `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] FilterSpec? Filter = null` as the LAST positional parameter. XML doc: "Phase 32 typed-filter schema for this column. Declares the value-kind (Kind), optional fixed-set options (Options), and optional matching hints (MatchingHints). The current filter instance (the ordered rules + joiner) lives in state at the path declared by TableNode.FilterDescriptorBinds. Coexists with the legacy Filterable/FilterValue/FilterBinds/FilterAction shape at Phase 32 close."

    STEP 3 — Append to TableNode: add `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] Dictionary<string, string>? FilterDescriptorBinds = null` as the LAST positional parameter. XML doc: "Phase 32 typed-filter bind paths, keyed by column key. Each value is a state path where the column's current FilterDescriptor lives. Parallel to FilterBinds for the legacy shape. Both fields remain valid at Phase 32 close."

    Do NOT touch the body of either record. Do NOT reorder existing parameters. Do NOT change the FilterBinds parameter.
  </action>
  <verify>
    <automated>cd viewmodel-shell-dotnet && dotnet build AshleyShrok.ViewModelShell.csproj --nologo -v minimal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FilterSpec" viewmodel-shell-dotnet/ViewModels.cs` returns >= 2 (record definition + TableColumn usage).
    - `grep -c "FilterDescriptor" viewmodel-shell-dotnet/ViewModels.cs` returns >= 1.
    - `grep -c "FilterRule" viewmodel-shell-dotnet/ViewModels.cs` returns >= 1.
    - `grep -c "FilterDescriptorBinds" viewmodel-shell-dotnet/ViewModels.cs` returns >= 1.
    - `grep -c "WhenWritingNull.*Filter" viewmodel-shell-dotnet/ViewModels.cs` returns >= 3 (FilterSpec.Options, FilterSpec.MatchingHints, FilterRule.Value, TableColumn.Filter?, TableNode.FilterDescriptorBinds? — at least 3 distinct WhenWritingNull attributes on new fields).
    - `grep -c "Filterable" viewmodel-shell-dotnet/ViewModels.cs` returns >= 1 (old Filterable bool field STILL present).
    - `grep -c "FilterBinds" viewmodel-shell-dotnet/ViewModels.cs` returns >= 1 (old FilterBinds field STILL present).
    - `cd viewmodel-shell-dotnet && dotnet build AshleyShrok.ViewModelShell.csproj --nologo -v minimal 2>&1 | tail -5` exits 0, no errors.
  </acceptance_criteria>
  <done>
    .NET wire type additions landed in ViewModels.cs. FilterSpec/FilterDescriptor/FilterRule records present with WhenWritingNull on every nullable. Old TableColumn/TableNode fields untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New FilterHelper.cs truth function + xUnit NASA test suite in Tests/ColumnFilterTests.cs</name>
  <files>viewmodel-shell-dotnet/FilterHelper.cs, viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs</files>
  <read_first>
    - viewmodel-shell-dotnet/Tests/TextWeightSerializationTests.cs — READ the full file for the xUnit test project file structure, usings, JsonSerializerOptions pattern, and MemberData test case pattern.
    - viewmodel-shell-dotnet/ViewModels.cs — READ the FilterSpec, FilterDescriptor, FilterRule records just added (Task 1) for the exact constructor parameter names.
    - .planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-SPEC.md — READ Requirement 6 (every operator's semantics) and Requirement 8 (NASA test bar: 1-rule exhaustive, 2-rule representative, 3-rule diagonal, both joiners, mutation-verify; specifically "numeric formatting variants including negative and decimal for number").
    - .planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-CONTEXT.md — READ D-03 (four ISO date shapes), D-05 (FilterHelper.cs location in main package).
    - AGENTS.md — READ gotcha #8 (object? Value in FilterRule is absent not null); READ "test suites are non-negotiable" + "NEVER PUSH ANYTHING BROKEN" for the gate commands.
    - .planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-01-PLAN.md — READ Task 2's behavior block for the full enumeration of test cases to implement (byte-parallel with .NET suite), including the negative-integer, decimal, and negative-decimal NUMBER cases added in revision 1.
  </read_first>
  <behavior>
    Write ColumnFilterTests.cs first (TDD: RED), then implement FilterHelper.cs (GREEN).

    Test cases are byte-parallel with the TS vitest suite from Plan 32-01 — same operators, same kinds, same value shapes, same expected booleans. Per-case names use the same format: "{kind}/{operator}/{valueShape}". Use `[Theory]` with `[MemberData(nameof(Cases))]` where Cases is a `public static IEnumerable<object?[]>` property.

    Each test-case element: `(string caseName, FilterDescriptor descriptor, object? rawValue, string displayString, string kind, string[]? matchingHints, bool expected)`.

    Critical cases to include (same as Plan 32-01 behavior block):
    - All text operators (contains case-insensitive, contains with ignore-punctuation, equals case-sensitive, starts-with, ends-with, is-empty with null/empty/""/whitespace, is-not-empty).
    - All number operators (equals, does-not-equal, gt, gte, lt, lte, between inclusive, is-empty with null vs 0, is-not-empty with 0).
    - Negative integer cases (SPEC Req 8 "numeric formatting variants including negative"):
      - less-than: rawValue=-5, ruleValue=0 → match: true; rawValue=-5, ruleValue=-10 → match: false.
      - does-not-equal: rawValue=-5, ruleValue=0 → match: true.
      - greater-than: rawValue=-3, ruleValue=-5 → match: true.
      - is-empty: rawValue=-5 → match: false (-5 is not empty; non-null numeric value).
    - Decimal cases (SPEC Req 8 "numeric formatting variants including decimal"):
      - equals: rawValue=3.14, ruleValue=3.14 → match: true; rawValue=3.14, ruleValue=3.15 → match: false.
      - between: rawValue=3.14, ruleValue=[3.0, 4.0] → match: true (inclusive).
      - greater-than: rawValue=3.14, ruleValue=3.0 → match: true.
    - Negative-decimal cases:
      - greater-than: rawValue=-2.5, ruleValue=-5 → match: true.
      - less-than: rawValue=-2.5, ruleValue=0 → match: true.
      - equals: rawValue=-2.5, ruleValue=-2.5 → match: true.
    - All date operators with all four D-03 ISO shapes: "2026-08-15", "2026-08-15T09:00:00-04:00", "2026-08-15T13:00:00Z", "2026-08-15T09:00:00". String comparison semantics (no parsing).
    - All fixed-set operators (contains case-insensitive, is string-equal, is-not, is-empty/not-empty).
    - All yes-no operators (contains on display string, is-true boolean true/false/null, is-false, is-empty null/undefined vs false).
    - Multi-rule all-of where second rule fails → no match.
    - Multi-rule any-of where second rule passes → match.
    - 3-rule all-of diagonal: [match, match, no-match] → false.
    - 3-rule any-of diagonal: [no-match, no-match, match] → true.
  </behavior>
  <action>
    STEP 1 — viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs (new file).

    File header: `global using Xunit;` (the test project already has GlobalUsings.cs — check first). Usings: `using ViewModelShell;` + `using System.Text.Json;` if needed for complex Value types. No `using static` — be explicit.

    The test method: `[Theory] [MemberData(nameof(Cases))] public void MatchesFilter_ReturnsExpected(string caseName, FilterDescriptor descriptor, object? rawValue, string displayString, string kind, string[]? matchingHints, bool expected) { Assert.Equal(expected, FilterHelper.MatchesFilter(descriptor, rawValue, displayString, kind, matchingHints)); }`.

    Helper to build a single-rule all-of descriptor: `private static FilterDescriptor OneRule(string op, object? value = null) => new(new[] { new FilterRule(op, value) }, "all-of");`.

    The Cases static property returns tuples for all cases enumerated in the behavior block above. For between/in-range, pass the value as a `double[]` or `string[]` array directly (test constructs it as a typed array; FilterHelper should handle both JsonElement and typed arrays as the Value field — more robust).

    Target: >= 120 test cases covering the NASA bar (same as TS suite, including the added negative-integer, decimal, and negative-decimal NUMBER cases).

    STEP 2 — viewmodel-shell-dotnet/FilterHelper.cs (new file).

    Namespace: `using System.Text.Json; namespace ViewModelShell;`. Class: `public static class FilterHelper`.

    Main entry: `public static bool MatchesFilter(FilterDescriptor descriptor, object? rawValue, string displayString, string kind, IReadOnlyList<string>? matchingHints = null)`. Loop over rules, call EvaluateRule for each, combine. No short-circuit — collect all booleans, apply joiner at the end. Joiner: "all-of" = results.All(x => x); "any-of" = results.Any(x => x). Unknown joiner: treat as all-of (fail-safe).

    Private EvaluateRule: switch on kind, then on rule.Operator within each kind. For unknown operators, return false (fail-safe — matches TS behavior).

    Private isEmpty: `private static bool IsEmpty(object? v) => v is null || (v is string s && s.Length == 0) || (v is JsonElement je && (je.ValueKind == JsonValueKind.Null || (je.ValueKind == JsonValueKind.String && je.GetString()!.Length == 0)));`.

    Private ApplyContains: `private static bool ApplyContains(string displayString, object? ruleValue, IReadOnlyList<string>? matchingHints)`. Get the rule value as string. If ignore-punctuation in matchingHints, strip `[$£€,.]` via regex from both display and rule strings before comparison. Then case-insensitive string contains.

    Number value extraction: helper `TryGetDouble(object? v, out double d)` using `Convert.ToDouble` inside try-catch, also handles `JsonElement.GetDouble()`. Convert.ToDouble handles negative integers (-5), decimals (3.14), and negative decimals (-2.5) correctly — no special casing needed.

    Date string comparison: helper `CompareIso(string? a, string? b)` using `string.Compare(a, b, StringComparison.Ordinal)`.

    For between (number): rule.Value is either `JsonElement` (array), `double[]`, or `long[]`. Deserialize via `JsonSerializer.Deserialize<double[]>(JsonSerializer.Serialize(rule.Value))` as a robust approach. Low = [0], high = [1].

    For in-range (date): same pattern — deserialize rule.Value as `string[]`. Low = [0], high = [1].

    TSDoc (XML summary): "Reference truth function for column filtering. Evaluates a FilterDescriptor against a row's cell value. Call from your action handler after reading the current FilterDescriptor from state. Kind must match the column's declared FilterSpec.Kind. MatchingHints optionally specifies matching-behavior modifiers (e.g. ignore-punctuation for currency columns)."

    STEP 3 — Mutation-verify session (document in SUMMARY):
    Temporarily return `false` from each operator's case arm in turn (e.g., comment out the contains implementation, return false). Run `dotnet test viewmodel-shell-dotnet/Tests --filter ColumnFilter` and confirm at least one test fails. Restore and verify all tests pass. Run for at least: contains, equals, is-empty, greater-than, is (date), is (fixed-set), is-true (yes-no).
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && dotnet test viewmodel-shell-dotnet/Tests --nologo --filter ColumnFilter 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell-dotnet/FilterHelper.cs` exists.
    - `grep -c "MatchesFilter" viewmodel-shell-dotnet/FilterHelper.cs` returns >= 1 (public static method).
    - `grep -c "public static class FilterHelper" viewmodel-shell-dotnet/FilterHelper.cs` returns >= 1.
    - `viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs` exists.
    - `grep -c "2026-08-15T09:00:00-04:00" viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs` returns >= 1 (D-03 datetime with offset case).
    - `grep -c "ignore-punctuation" viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs` returns >= 1 (matchingHint case).
    - `grep -c "\-2\.5" viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs` returns >= 1 (negative-decimal case present).
    - `grep -c "3\.14" viewmodel-shell-dotnet/Tests/ColumnFilterTests.cs` returns >= 1 (decimal case present).
    - `cd /home/thenasty/ViewModelShell && dotnet test viewmodel-shell-dotnet/Tests --nologo --filter ColumnFilter 2>&1 | tail -5` — all ColumnFilter tests PASS (target: >= 120 cases).
    - `cd /home/thenasty/ViewModelShell && dotnet test viewmodel-shell-dotnet/Tests --nologo 2>&1 | tail -5` — full .NET test suite passes (zero regressions).
    - `cd /home/thenasty/ViewModelShell && dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal 2>&1 | tail -5` exits 0 (Markdown companion still compiles against updated ViewModels.cs).
    - Mutation-verify session completed and documented: each stubbed operator causes at least one test to go red.
  </acceptance_criteria>
  <done>
    .NET reference truth function ships with NASA-level xUnit coverage. Every operator x type x value-shape combination proven byte-parallel with the TS suite, including negative integers, decimals, and negative decimals for number kind per SPEC Req 8. Markdown companion compiles. Mutation-verify documented. Ready for Plan 32-03 (parity fixture, wave 2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer action handler → FilterHelper.MatchesFilter | Consumer calls MatchesFilter with a FilterDescriptor deserialized from round-tripped state (app-owned) and cell values from app data. |
| FilterRule.Value deserialization | Value is typed as object? in .NET. Between/in-range values arrive as JsonElement array. FilterHelper must robustly coerce them without throwing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Tampering | FilterDescriptor from client state (malformed Value on between/in-range) | mitigate | FilterHelper's between/in-range deserialization wraps in try-catch; returns false on parse failure. Matches TS behavior (unknown operator / bad value shape = no match). No security impact — filter determines row visibility, not data access. Documented in XML summary. |
| T-32-02-02 | Tampering | .NET nullable wire fields emitting "null" on the wire (gotcha #8) | mitigate | Every new nullable field carries WhenWritingNull. xUnit tests assert absence of null fields when unset (byte-parallel with TS tests). |
| T-32-02-03 | Tampering | npm/NuGet installs | accept | No new package dependencies in this plan. xUnit and the test project already exist. |
| T-32-02-SC | Tampering | package installs | accept | No new package installs — PACKAGE_LEGITIMACY_GATE N/A. |
</threat_model>

<verification>
Full .NET gate at plan close:
- `dotnet test viewmodel-shell-dotnet/Tests --nologo` — all tests pass including new ColumnFilterTests.
- `dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal` — exits 0.
- `grep -c "Filterable" viewmodel-shell-dotnet/ViewModels.cs` — returns >= 1 (old field intact).
- `grep -c "FilterBinds" viewmodel-shell-dotnet/ViewModels.cs` — returns >= 1 (old field intact).
</verification>

<success_criteria>
- FilterSpec, FilterDescriptor, FilterRule records in ViewModels.cs; every nullable carries WhenWritingNull.
- TableColumn.Filter? and TableNode.FilterDescriptorBinds? present; old fields (Filterable, FilterValue, FilterBinds, FilterAction) untouched.
- FilterHelper.cs exists with public static MatchesFilter implementing all operators x kinds.
- NASA xUnit suite in Tests/ColumnFilterTests.cs covers all operators x all kinds x all required value shapes including four D-03 ISO date formats and negative integers, decimals, and negative decimals for number kind per SPEC Req 8; byte-parallel case names with TS suite.
- Mutation-verify session documented in SUMMARY.
- `dotnet test viewmodel-shell-dotnet/Tests` full suite green. Markdown companion compiles.
</success_criteria>

<output>
Create `.planning/phases/32-column-filter-expansion-phase-1-wire-types-reference-truth-f/32-02-SUMMARY.md`. Record: files created/modified with line ranges, xUnit test count, verify-command exit codes, mutation-verify session results (which operator stubbed → which test went red), Markdown companion build result, any deviation from CONTEXT-locked design + rationale.

**Commit intent:** `feat(32-02): .NET wire types + FilterHelper truth function + NASA xUnit suite`
</output>
