---
phase: 02-upload-progress-milestone-closeout
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - viewmodel-shell/package.json
  - MIGRATION.md
  - README.md
autonomous: true
requirements: [MIGRATE-01]
user_setup: []

must_haves:
  truths:
    - "npm package version is 0.3.13 (patch from 0.3.12), NOT 0.4.0"
    - "NuGet AshleyShrok.ViewModelShell stays 0.3.9 — unchanged, no .NET action"
    - "The AGENTS.md 'two packages share major.minor' versioning rule is byte-unchanged"
    - "A copy-pasteable MIGRATION.md exists at repo root containing all of D-13 items 1-5 including the why-patch rationale, the NOT-breaking list, and the two silent-behavior caveats"
    - "README.md points to MIGRATION.md"
    - "The full parity suite (parity/run.ts, 7 fixtures) stays 100% green, confirming the shared response path is unchanged"
  artifacts:
    - path: "viewmodel-shell/package.json"
      provides: "npm version 0.3.13 (patch)"
      contains: "\"version\": \"0.3.13\""
    - path: "MIGRATION.md"
      provides: "MIGRATE-01 copy-pasteable consumer-maintainer migration blurb (D-13 items 1-5)"
      contains: "onUploadProgress"
    - path: "README.md"
      provides: "pointer to MIGRATION.md"
      contains: "MIGRATION.md"
  key_links:
    - from: "README.md"
      to: "MIGRATION.md"
      via: "a markdown link/pointer to the migration blurb"
      pattern: "MIGRATION\\.md"
    - from: "MIGRATION.md"
      to: "consumer divide-by-zero guard"
      via: "explicit total > 0 / indeterminate-sentinel caveat (D-05a / D-13.5b)"
      pattern: "total > 0"
---

<objective>
Deliver MIGRATE-01 milestone closeout: bump the npm package to `0.3.13` (PATCH — locked by explicit user decision D-10/D-10a, NOT `0.4.0`), keep NuGet at `0.3.9` (D-11), leave the `AGENTS.md` versioning rule untouched (D-10a), author a dedicated copy-pasteable `MIGRATION.md` at the repo root containing all of D-13 items 1-5, add a short `README.md` pointer to it, and run the full parity suite as the milestone gate (ROADMAP success criterion 3).

Purpose: The migration blurb is a first-class milestone deliverable (PROJECT.md) — downstream app maintainers across multiple apps must know exactly what (if anything) to update and what is explicitly NOT breaking and why. Both packages stay on the `0.3` major.minor so there is no divergence; the blurb must still say *why it is a patch* because consumers tracking the patch cadence will notice the number.

Output: Modified `viewmodel-shell/package.json` (`0.3.12` → `0.3.13`), new `MIGRATION.md` at repo root, modified `README.md` (one pointer). Parity suite confirmed 100% green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-upload-progress-milestone-closeout/02-CONTEXT.md
@.planning/phases/02-upload-progress-milestone-closeout/02-01-SUMMARY.md

<interfaces>
<!-- The shipped API surface this blurb documents (from plan 02-01) and the locked version facts. -->

The single public-API addition (viewmodel-shell/src/index.ts, ShellOptions, from 02-01):
```typescript
onUploadProgress?: (sent: number, total: number) => void;
```

Locked version facts (D-10 / D-10a / D-11 — NOT discretionary):
- npm @ashley-shrok/viewmodel-shell: 0.3.12 → 0.3.13  (PATCH — never 0.4.0)
- NuGet AshleyShrok.ViewModelShell: 0.3.9  (UNCHANGED — no .NET action)
- AGENTS.md line containing "The two packages share major.minor" : MUST NOT be modified

Current package.json version line (viewmodel-shell/package.json:3):
```json
  "version": "0.3.12",
```

Current csproj version (viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj:13) — read-only, do NOT edit:
```xml
    <Version>0.3.9</Version>
```

Parity command (from .github/workflows/parity.yml + parity/run.ts): `cd parity && bun run run.ts` (spins up all backends, runs all 7 fixtures, diffs normalized responses).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump npm version to 0.3.13 (PATCH) — NuGet and AGENTS.md versioning rule untouched</name>
  <read_first>
    - viewmodel-shell/package.json (the file being modified — current `"version": "0.3.12"` at line 3)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (read-only confirmation: `<Version>0.3.9</Version>` line 13 must stay)
    - AGENTS.md (read-only confirmation: the line "The two packages share major.minor — bumping a `ViewNode` type or wire-format change bumps both sides." ~line 13 must stay byte-identical)
    - .planning/phases/02-upload-progress-milestone-closeout/02-CONTEXT.md (D-10 why patch, D-10a the rejected minor alternative + "AGENTS.md rule NOT changed", D-11 NuGet no-op)
  </read_first>
  <files>viewmodel-shell/package.json</files>
  <action>
Single-line edit to `viewmodel-shell/package.json`: change `"version": "0.3.12",` to `"version": "0.3.13",`. This is a PATCH bump — locked by explicit user decision (D-10/D-10a). Do NOT bump to `0.4.0` or any minor: this milestone has zero wire-format/ViewNode change (Phase 1 relocated bindings; Phase 2 is pure client-side transport), so by the project's own `AGENTS.md:13` major.minor-alignment rule the minor stays fixed, and the established cadence is patch (redirect v0.3.4, side-effects v0.3.5, polling/push v0.3.6, npm-only 0.3.10/0.3.11/0.3.12).

Do NOT modify any other file in this task. Specifically:
- `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` stays at `<Version>0.3.9</Version>` (D-11 — no .NET delta; both packages remain on the 0.3 major.minor, preserving alignment).
- `AGENTS.md` — do NOT touch the versioning rule line ("The two packages share major.minor …"). The user explicitly considered and DECLINED adopting "minor = feature" (D-10a). Do not "improve" versioning policy as a side effect.
  </action>
  <verify>
    <automated>node -e "const p=require('./viewmodel-shell/package.json'); if(p.version!=='0.3.13'){console.error('version is '+p.version);process.exit(1)}" && node -e "const s=require('fs').readFileSync('viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj','utf8'); process.exit(/<Version>0\.3\.9<\/Version>/.test(s)?0:1)" && node -e "const s=require('fs').readFileSync('AGENTS.md','utf8'); process.exit(/The two packages share major\.minor/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/package.json` contains `"version": "0.3.13"` and does NOT contain `"version": "0.4.0"` or `"version": "0.3.12"`
    - `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` still contains `<Version>0.3.9</Version>`
    - `git diff AGENTS.md` shows NO change to the line containing `share major.minor` (versioning rule byte-unchanged, D-10a)
    - `git diff --name-only` for this task lists ONLY `viewmodel-shell/package.json`
    - `cd viewmodel-shell && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (package.json still valid JSON)
  </acceptance_criteria>
  <done>`viewmodel-shell/package.json` version is exactly `0.3.13` (patch); csproj unchanged at `0.3.9`; the `AGENTS.md` "share major.minor" rule is byte-identical; no other file touched in this task; package.json is valid JSON.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author MIGRATION.md (D-13 items 1-5, copy-pasteable) + README.md pointer</name>
  <read_first>
    - README.md (the file being modified — current structure; the "Capability seam" section ~lines 9-21 and "Docs" section ~lines 61-63 are natural pointer locations)
    - .planning/phases/02-upload-progress-milestone-closeout/02-CONTEXT.md (D-12 dedicated MIGRATION.md at repo root + README pointer; D-13 items 1-5 verbatim incl. 5a/5b silent-behavior caveats; D-05a divide-by-zero rationale)
    - .planning/phases/02-upload-progress-milestone-closeout/02-01-SUMMARY.md (confirm the shipped API addition is exactly `onUploadProgress?: (sent: number, total: number) => void` and the D-02 silent-fallback behavior)
    - AGENTS.md (read-only: the `AGENTS.md:13` "share major.minor" rule — quote its rationale in MIGRATION.md item 1; do NOT edit AGENTS.md)
    - .planning/REQUIREMENTS.md (MIGRATE-01 acceptance wording — npm/NuGet versions, API additions, what is NOT breaking and why, recommended upgrade steps)
  </read_first>
  <files>MIGRATION.md, README.md</files>
  <action>
Create `MIGRATION.md` at the repository ROOT (`C:\requests\mvc-agent-framework\MIGRATION.md`). It must be copy-pasteable and contain ALL of D-13 items 1-5 concretely. Prose/headings are Claude's discretion but every item below is mandatory content (the divide-by-zero caveat is NOT optional — only its phrasing is):

1. **Exact versions + why patch.** State: npm `@ashley-shrok/viewmodel-shell` `0.3.12 → 0.3.13`; NuGet `AshleyShrok.ViewModelShell` unchanged at `0.3.9`. Include the brief why-patch rationale: the documented `AGENTS.md` rule *"The two packages share major.minor — bumping a `ViewNode` type or wire-format change bumps both sides"* + this milestone has zero wire-format/ViewNode change (Phase 1 relocated bindings, Phase 2 is pure client-side transport), and the established patch cadence (redirect 0.3.4, side-effects 0.3.5, polling/push 0.3.6, npm-only 0.3.10/0.3.11/0.3.12). Also state the why-no-NuGet-bump rationale: no wire-format or .NET API delta, both packages stay on the `0.3` major.minor so this *preserves* alignment (it is not divergence).
2. **The single public-API addition** — exactly: `ShellOptions.onUploadProgress?: (sent: number, total: number) => void`.
3. **Explicit NOT-breaking list with reasons** — wire format, server-initiated redirect, client side-effects (set-local/session-storage), polling/push, every existing ViewNode type, AND existing custom `Adapter` implementations (`transport?` is and remains optional — adapters that don't implement it still compile and behave exactly as before). Give the one-line reason per item (Phase 1 relocated *where* bindings run, not *what*; Phase 2 only adds an optional client-side send path).
4. **Recommended upgrade steps** — `npm update @ashley-shrok/viewmodel-shell` (or pin `^0.3.13`); optionally set `onUploadProgress` in `ShellOptions` if you want byte progress on file uploads; .NET consumers: NO action.
5. **The two non-obvious silent behaviors, called out explicitly:**
   - **(5a)** `onUploadProgress` fires ONLY if the plugged-in adapter implements `transport`. The default `BrowserAdapter` does; a custom adapter without `transport` silently falls back to the core `fetch` path — the upload still succeeds but NO progress events fire (per D-02, intentional graceful degradation, NOT an error).
   - **(5b)** The `total` argument may be `0`, meaning "indeterminate" (server/stream did not report a content length). Consumers MUST guard `total > 0` before computing `sent / total`, or a percentage calculation yields `NaN`/`Infinity` (per D-05a). Include a tiny copy-pasteable guard snippet, e.g. `const pct = total > 0 ? Math.round((sent / total) * 100) : null;`.

Then add a SHORT pointer from `README.md` to `MIGRATION.md` (D-12). Add one line in the "Capability seam" section or "Docs" section, e.g. a sentence like: *Upgrading from a previous version? See [MIGRATION.md](./MIGRATION.md) for exact versions, the one new option, and what is explicitly not breaking.* Keep the README change minimal (one line / one short sentence) and additive — do NOT restructure README or remove existing content. Do NOT add the pointer to AGENTS.md (D-12 specifies README).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('MIGRATION.md','utf8'); for(const t of ['0.3.13','0.3.9','onUploadProgress','transport','total > 0','NOT breaking','npm update']){ if(!s.includes(t)){ console.error('MIGRATION.md missing: '+t); process.exit(1);} }" && node -e "const s=require('fs').readFileSync('README.md','utf8'); process.exit(/MIGRATION\.md/.test(s)?0:1)" && node -e "const s=require('fs').readFileSync('AGENTS.md','utf8'); process.exit(/The two packages share major\.minor/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `MIGRATION.md` exists at repo root (`C:\requests\mvc-agent-framework\MIGRATION.md`)
    - `MIGRATION.md` contains all of: `0.3.13`, `0.3.9`, `onUploadProgress`, `(sent: number, total: number) => void`, `transport`, `total > 0` (or equivalent explicit divide-by-zero guard wording), the string `NOT breaking` (or a clearly-labelled not-breaking section), and `npm update`
    - `MIGRATION.md` contains both silent-behavior caveats: a 5a sentence about progress only firing when the adapter implements `transport` (and silent fetch fallback otherwise), and a 5b sentence about `total` possibly being `0`/indeterminate with the `total > 0` guard
    - `MIGRATION.md` explicitly lists existing custom `Adapter` implementations among the NOT-breaking items (transport? stays optional)
    - `README.md` contains a link or pointer string `MIGRATION.md`
    - `git diff AGENTS.md` shows NO change (AGENTS.md not edited at all in this task)
    - `git diff --name-only` for this task lists ONLY `MIGRATION.md` and `README.md`
  </acceptance_criteria>
  <done>`MIGRATION.md` at repo root is copy-pasteable and contains D-13 items 1 (exact versions + why-patch + why-no-NuGet rationale), 2 (the one API addition with exact signature), 3 (NOT-breaking list incl. custom Adapter impls), 4 (upgrade steps incl. ".NET: no action"), and 5 (both 5a transport-silent-fallback and 5b total-may-be-0 / `total > 0` guard caveats); `README.md` has a one-line pointer to it; AGENTS.md untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run the full milestone verification gate — parity 100% green + core-globals + full vitest</name>
  <read_first>
    - .github/workflows/parity.yml (the exact CI gate sequence: check:core-globals → vitest → .NET build/test → parity run)
    - parity/run.ts (the parity harness — `cd parity && bun run run.ts` spins up all backends and diffs all 7 fixtures; ROADMAP success criterion 3)
    - .planning/phases/02-upload-progress-milestone-closeout/02-CONTEXT.md (D-09: upload progress has NO parity surface — parity's role here is to prove the shared response path / wire contract is unchanged; D-15 no FeatureProbe/fixture change)
    - .planning/ROADMAP.md (Phase 2 success criteria 2 and 3 — the no-XMLHttpRequest-in-core grep and parity 100% green)
  </read_first>
  <files>(verification only — no files modified)</files>
  <action>
This task modifies NO files. It runs the milestone gate and confirms green, then records results in the SUMMARY. Run, in order, from the repo root:

1. `cd viewmodel-shell && npm install && npm run build && npm run check:core-globals` — confirms the core-globals guard passes with the Phase 2 changes (ROADMAP success criterion 2: zero `XMLHttpRequest` in `src/index.ts`).
2. `cd viewmodel-shell && npx vitest run` — confirms the full framework jsdom/vitest suite is green, including the existing `adapter-seam.test.ts` AND the new `upload-progress.test.ts` from plan 02-02.
3. `cd parity && bun install --frozen-lockfile && bun run run.ts` — the full cross-backend parity suite, all 7 fixtures. MUST be 100% green (ROADMAP success criterion 3). This proves the D-08 shared `processResponse()` path and the wire contract are byte-unchanged by Phase 2 (upload progress itself has no parity surface — D-09).

If parity is NOT 100% green: STOP and report — this is a milestone-blocking regression (the shared response path or wire contract drifted, contradicting D-08). Do not paper over a parity failure. If the parity harness cannot run in this environment (missing bun/.NET toolchain), record that explicitly in the SUMMARY as an environment limitation and note that the CI `.github/workflows/parity.yml` "Run cross-backend parity tests" step is the authoritative gate — do NOT mark the criterion satisfied without evidence.

Record in `02-03-SUMMARY.md`: the exact command outputs / exit codes for all three gates, and an explicit statement of whether ROADMAP Phase 2 success criteria 1-4 are all satisfied (criterion 1 → plan 02-02's tests; criterion 2 → core-globals + grep; criterion 3 → parity green; criterion 4 → MIGRATION.md exists with required contents).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npm run check:core-globals && npx vitest run && cd ../parity && bun install --frozen-lockfile && bun run run.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd viewmodel-shell && npm run check:core-globals` exits 0 (ROADMAP success criterion 2)
    - `cd viewmodel-shell && npx vitest run` exits 0 (all framework tests green incl. new upload-progress.test.ts and existing adapter-seam.test.ts)
    - `cd parity && bun run run.ts` exits 0 with all 7 fixtures green (ROADMAP success criterion 3) — OR the SUMMARY explicitly records an environment toolchain limitation and defers the authoritative check to CI parity.yml (criterion NOT silently marked satisfied)
    - `git status --porcelain parity/` shows NO modification to any parity fixture or FeatureProbe (D-15)
    - `02-03-SUMMARY.md` records exit codes for all three gates and an explicit pass/fail statement for each of ROADMAP Phase 2 success criteria 1-4
  </acceptance_criteria>
  <done>core-globals guard exits 0; full vitest suite (incl. 02-02's upload-progress.test.ts) exits 0; parity suite is 100% green across all 7 fixtures (or an environment limitation is explicitly recorded and deferred to CI, never silently passed); no parity fixture/FeatureProbe modified; SUMMARY records all four ROADMAP success criteria with explicit pass/fail and command evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → downstream consumers (MIGRATION.md) | The migration blurb is the security-relevant artifact: if it omits the `total === 0` divide-by-zero caveat or the silent-fallback caveat, consumers ship broken progress UIs or assume progress fired when it did not. |
| published package version → consumer dependency resolution | An incorrect version bump (e.g. minor `0.4.0`) would break the documented npm/NuGet major.minor alignment invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure (omission) | MIGRATION.md omitting the `total === 0` divide-by-zero hazard (D-05a) | mitigate | Acceptance criteria hard-require the `total > 0` guard string and a 5b caveat sentence + copy-pasteable guard snippet in MIGRATION.md; a missing caveat fails the task's automated verify. |
| T-02-08 | Tampering (false reassurance) | MIGRATION.md NOT-breaking list overclaiming (e.g. silently implying progress always fires) | mitigate | D-13.5a caveat is mandatory content + acceptance-checked: the blurb must state progress fires ONLY if the adapter implements `transport` and silently falls back otherwise — preventing a "looks fine but uploads aren't reported" false-reassurance. |
| T-02-09 | Tampering / supply-chain semantics | wrong version number breaking npm/NuGet alignment invariant | mitigate | Automated verify asserts `package.json` is exactly `0.3.13` (not `0.4.0`/`0.3.12`), csproj stays `0.3.9`, and the AGENTS.md alignment rule is byte-unchanged — the locked D-10/D-10a/D-11 facts are enforced, not trusted. |
| T-02-10 | Repudiation | claiming parity green without running it | mitigate | Task 3 requires recorded exit codes; if the toolchain is unavailable the SUMMARY must explicitly defer to CI parity.yml and NOT mark the criterion satisfied — no silent pass. |

**HIGH findings:** None. This plan ships no runtime code (one version string + two docs files + a verification run). The realistic risk is *documentation omission* (a consumer tripping on the silent `total===0` / silent-fallback behaviors), which is mitigated by mandatory, acceptance-checked MIGRATION.md content (D-13.5a/5b).
</threat_model>

<verification>
- ROADMAP success criterion 2 (no XMLHttpRequest in core): re-confirmed by Task 3's `npm run check:core-globals` exit 0.
- ROADMAP success criterion 3 (parity 100% green — shared response path unchanged): Task 3's `cd parity && bun run run.ts` exit 0 across all 7 fixtures (D-08/D-09).
- ROADMAP success criterion 4 (copy-pasteable migration blurb with exact versions, API additions, NOT-breaking list, upgrade steps): Task 2's MIGRATION.md acceptance criteria.
- Locked D-10/D-10a/D-11: package.json == 0.3.13, csproj == 0.3.9, AGENTS.md "share major.minor" byte-unchanged — automated-verified in Task 1.
- `git status --porcelain parity/` empty (D-15 — no fixture/FeatureProbe change).
</verification>

<success_criteria>
- npm version is exactly `0.3.13` (patch); NuGet stays `0.3.9`; the AGENTS.md versioning rule is byte-unchanged.
- `MIGRATION.md` exists at repo root, copy-pasteable, containing D-13 items 1-5 (versions + why-patch + why-no-NuGet, the one API addition, NOT-breaking list incl. custom Adapter impls, upgrade steps, and BOTH silent-behavior caveats 5a/5b with a `total > 0` guard).
- `README.md` has a short pointer to `MIGRATION.md`; AGENTS.md untouched.
- Full milestone gate green: `npm run check:core-globals` exit 0, full `npx vitest run` exit 0 (incl. 02-02's test), parity 100% green across all 7 fixtures (or environment limitation explicitly recorded + deferred to CI, never silently passed).
- No parity fixture / FeatureProbe / demo modified (D-15).
</success_criteria>

<output>
After completion, create `.planning/phases/02-upload-progress-milestone-closeout/02-03-SUMMARY.md`
</output>
