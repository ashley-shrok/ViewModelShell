---
phase: 02-upload-progress-milestone-closeout
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - viewmodel-shell/test/upload-progress.test.ts
autonomous: true
requirements: [UPLOAD-01]
user_setup: []

must_haves:
  truths:
    - "A net-new jsdom/vitest mock-XHR test proves onUploadProgress fires with (sent,total) on a files-bearing dispatch when the option is set"
    - "The test proves onUploadProgress is never invoked when no files are present or the option is unset"
    - "The test proves a render-only adapter (no transport) still succeeds via fetch fallback AND onUploadProgress never fires"
    - "The test proves the response still flows through processResponse() (rendered VM updates), confirming D-08"
    - "The test proves an indeterminate (lengthComputable:false) completion emits (finalLoaded,finalLoaded), never (0,0)"
  artifacts:
    - path: "viewmodel-shell/test/upload-progress.test.ts"
      provides: "D-14 (a)-(e) mock-XHR verification of UPLOAD-01"
      contains: "onUploadProgress"
  key_links:
    - from: "viewmodel-shell/test/upload-progress.test.ts"
      to: "ViewModelShell.dispatch() + BrowserAdapter.transport"
      via: "mock XMLHttpRequest driving xhr.upload.onprogress under jsdom"
      pattern: "onUploadProgress"
    - from: "viewmodel-shell/test/upload-progress.test.ts"
      to: "npm test (vitest/jsdom) + CI parity.yml adapter-seam step"
      via: "test/**/*.test.ts include glob in vitest.config.ts"
      pattern: "upload-progress\\.test\\.ts"
---

<objective>
Verify UPLOAD-01 with a net-new framework-level jsdom/vitest mock-XHR unit test (D-14), extending the established `viewmodel-shell/test/adapter-seam.test.ts` harness pattern (the Phase 1 D-12.2 pattern). The test asserts D-14 (a)-(e), including the two highest-risk rules: (c) the fetch-fallback path fires no progress, and (e) an indeterminate completion is `(finalLoaded, finalLoaded)` and never `(0, 0)`.

Purpose: Upload progress is browser-runtime-only and has no parity surface (STATE.md architectural notes). The framework's "no real browser for tests" promise means a mock-XHR jsdom unit test is the established and only way to verify the byte-level XHR binding. Without this test the feature ships unverified.

Output: A new test file `viewmodel-shell/test/upload-progress.test.ts` that runs under the existing `npm test` (vitest/jsdom) and the existing `.github/workflows/parity.yml` "Framework adapter-seam tests" step (which runs `npx vitest run` — picks up `test/**/*.test.ts` automatically). NO new demo app, NO FeatureProbe change, NO parity-fixture change (D-15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-upload-progress-milestone-closeout/02-CONTEXT.md
@.planning/phases/02-upload-progress-milestone-closeout/02-01-SUMMARY.md

<interfaces>
<!-- The shipped contract this test exercises (from plan 02-01). -->

ShellOptions.onUploadProgress (viewmodel-shell/src/index.ts, additive in 02-01):
```typescript
onUploadProgress?: (sent: number, total: number) => void;
```

dispatch() routing condition (viewmodel-shell/src/index.ts, 02-01):
```typescript
if (action.files && this.options.onUploadProgress && adapter.transport) {
  res = await adapter.transport(actionEndpoint, init, { onUploadProgress: this.options.onUploadProgress });
} else {
  res = await fetch(actionEndpoint, init);
}
```

BrowserAdapter.transport XHR branch (viewmodel-shell/src/browser.ts, 02-01) — taken only when hooks.onUploadProgress is set; binds xhr.upload.onprogress; on load resolves new Response(xhr.responseText, { status, statusText }); terminal emit is (knownTotal,knownTotal) or (lastLoaded,lastLoaded), never (0,0).

Existing harness pattern (viewmodel-shell/test/adapter-seam.test.ts:11-33) — import from "../src/index.js" and "../src/browser.js" (NodeNext .js specifiers, NOT the published package); freshContainer() helper; beforeEach clears storage; afterEach vi.restoreAllMocks().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Net-new mock-XHR jsdom/vitest test asserting D-14 (a)-(e)</name>
  <read_first>
    - viewmodel-shell/test/adapter-seam.test.ts (the harness pattern to extend — imports via "../src/index.js"/"../src/browser.js", freshContainer(), beforeEach/afterEach, ViewModelShell construction, vi.spyOn on BrowserAdapter.prototype)
    - viewmodel-shell/src/index.ts (shipped: ShellOptions.onUploadProgress, dispatch() routing condition, processResponse() — the render path the test observes)
    - viewmodel-shell/src/browser.ts (shipped: BrowserAdapter.transport XHR branch — what the mock XHR drives)
    - viewmodel-shell/vitest.config.ts (environment: jsdom; include: test/**/*.test.ts — confirms a sibling *.test.ts is auto-picked-up)
    - .planning/phases/02-upload-progress-milestone-closeout/02-CONTEXT.md (D-14 the five required assertions (a)-(e); D-15 no demo / no FeatureProbe / no parity-fixture change; D-02 fallback; D-05 indeterminate-completion rule; D-08 shared processResponse)
  </read_first>
  <files>viewmodel-shell/test/upload-progress.test.ts</files>
  <behavior>
RED→GREEN: write the failing assertions first (the source is already shipped by 02-01, so "RED" here means the test must be authored to genuinely exercise the binding and would fail if the 02-01 wiring were wrong — verify by temporarily reasoning about each branch, not by reverting source).

The test file MUST contain these five behaviors as separate `it(...)` cases (D-14 a-e):

- (a) FIRES on files + option: a dispatch with `action.files` set and `ShellOptions.onUploadProgress` set, using a real `BrowserAdapter`, drives a mock `XMLHttpRequest` whose `xhr.upload.onprogress` emits a computable progress event then `onload`. Assert `onUploadProgress` was called with `(sent, total)` byte numbers during transfer (e.g. `(50, 100)`) and a terminal `(100, 100)`.
- (b) NEVER fires without files OR without the option: (b1) dispatch WITHOUT `action.files` but WITH `onUploadProgress` set → callback never called, and the mock XHR's `upload.onprogress` path is never taken (fetch path used). (b2) dispatch WITH `action.files` but WITHOUT `onUploadProgress` set → callback never called.
- (c) MISSING adapter.transport → fetch fallback succeeds AND no progress: a render-only adapter `{ render() {} }` (no `transport`) with `onUploadProgress` set and `action.files` present. Stub global `fetch` to resolve a real `Response` JSON body. Assert: dispatch resolves, `onUploadProgress` is NEVER called, and no error surfaces via `onError` (D-02 silent graceful fallback, NOT fail-loud).
- (d) Response flows through processResponse(): after a successful files-bearing dispatch via the mock XHR (resolving a `Response` whose JSON is `{ vm: {type:"text", value:"updated"}, state:{} }`), assert `shell.getCurrentVm()` reflects the new vm (proves D-08 — the XHR branch's reconstructed `Response` traverses the unchanged `processResponse()` exactly like fetch).
- (e) Indeterminate completion is (finalLoaded, finalLoaded), NEVER (0,0): drive the mock XHR with `xhr.upload.onprogress` emitting an event where `lengthComputable === false` and `loaded === 73`, then `onload`. Assert the final `onUploadProgress` call is `(73, 73)` and assert it is NOT `(0, 0)` (explicit `expect(lastCall).toEqual([73,73])` AND `expect(lastCall).not.toEqual([0,0])`).
  </behavior>
  <action>
Create `viewmodel-shell/test/upload-progress.test.ts`. Mirror the import + harness style of `viewmodel-shell/test/adapter-seam.test.ts` exactly: `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";`, `import { ViewModelShell, type ShellResponse, type ViewNode, type Adapter } from "../src/index.js";`, `import { BrowserAdapter } from "../src/browser.js";`, a `freshContainer()` helper, `afterEach(() => vi.restoreAllMocks())`.

Define a controllable mock `XMLHttpRequest` class and install it via `vi.stubGlobal("XMLHttpRequest", MockXHR)` (jsdom has no real network; the mock lets the test drive `upload.onprogress`/`onload`/`onerror` deterministically). The mock must:
- expose `upload = { onprogress: null as ((e: any) => void) | null }`, plus `onload`, `onerror`, `ontimeout`, `onabort` slots, and `status`/`statusText`/`responseText` fields the test sets,
- implement `open(method, url)` (record them), `setRequestHeader()` (no-op or record), and `send(body)` that, on a microtask/`queueMicrotask` or `setTimeout(...,0)`, fires the test-scripted sequence: zero or more `this.upload.onprogress?.({ lengthComputable, loaded, total })` calls then `this.onload?.()` (with `this.status=200, this.statusText="OK", this.responseText=JSON.stringify(...)` set before `onload`).

For (a)/(d)/(e) use a real `new BrowserAdapter(freshContainer())` so the actual shipped `BrowserAdapter.transport` XHR branch executes against the mock. For (c) use a plain `{ render() {} }` adapter and `vi.stubGlobal("fetch", vi.fn().mockResolvedValue(new Response(JSON.stringify({ vm:{type:"text",value:"x"}, state:{} }), { status:200 })))`.

Dispatch via `shell.dispatch({ name: "upload", context: {}, files: { file: new File(["data"], "f.txt") } })` (jsdom provides `File`). Capture progress calls with `const calls: Array<[number,number]> = []; const onUploadProgress = (s:number,t:number)=>calls.push([s,t]);` and pass it in `ShellOptions`. `await` the dispatch (it's async; ensure the mock fires its sequence and the promise resolves before assertions — use `await shell.dispatch(...)` and, if needed, an extra `await new Promise(r => setTimeout(r, 0))` to flush the scheduled mock events).

Required assertions per the five `<behavior>` cases — use `calls` to assert exact tuples, e.g. (a) `expect(calls).toContainEqual([50,100])` and `expect(calls.at(-1)).toEqual([100,100])`; (e) `expect(calls.at(-1)).toEqual([73,73])` and `expect(calls.at(-1)).not.toEqual([0,0])`; (b)/(c) `expect(calls).toHaveLength(0)`; (c) also `expect(onError).not.toHaveBeenCalled()`; (d) `expect(shell.getCurrentVm()).toEqual({ type:"text", value:"updated" })`.

Do NOT modify `src/index.ts`, `src/browser.ts`, `adapter-seam.test.ts`, any parity fixture, FeatureProbe, or add a demo app (D-15). The test file is the only artifact.
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx vitest run test/upload-progress.test.ts && npm test && npm run check:core-globals</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/test/upload-progress.test.ts` exists and contains `onUploadProgress`, `XMLHttpRequest`, `lengthComputable`, and `getCurrentVm`
    - `viewmodel-shell/test/upload-progress.test.ts` contains five distinct `it(` cases covering D-14 (a) fires, (b) never-fires-without-files-or-option, (c) fetch-fallback-no-progress, (d) processResponse rendered-vm, (e) indeterminate `(finalLoaded,finalLoaded)` never `(0,0)`
    - the (e) case contains both an `expect(...).toEqual([73, 73])` (or chosen finalLoaded value) AND an `expect(...).not.toEqual([0, 0])` assertion
    - the (c) case asserts `onUploadProgress` length 0 AND `onError` not called (silent fallback, not fail-loud)
    - `cd viewmodel-shell && npx vitest run test/upload-progress.test.ts` exits 0 (all five cases pass)
    - `cd viewmodel-shell && npm test` exits 0 (full vitest/jsdom suite incl. existing adapter-seam.test.ts still green)
    - `cd viewmodel-shell && npm run check:core-globals` exits 0 (test file did not provoke a core change)
    - `git status --porcelain` shows NO modification to `viewmodel-shell/src/index.ts`, `viewmodel-shell/src/browser.ts`, `viewmodel-shell/test/adapter-seam.test.ts`, or any `parity/` file (D-15)
  </acceptance_criteria>
  <done>`viewmodel-shell/test/upload-progress.test.ts` exists with five `it` cases asserting D-14 (a)-(e); a controllable mock `XMLHttpRequest` drives the shipped `BrowserAdapter.transport` for (a)/(d)/(e) and a render-only adapter + stubbed fetch for (c); `npx vitest run` and full `npm test` exit 0; no source/parity/demo file modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → shipped source | This plan adds only a test file; it executes the already-shipped 02-01 source under jsdom. No new runtime trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering (test fidelity) | mock `XMLHttpRequest` could mask a real defect by being more permissive than a browser XHR | mitigate | The mock drives the *shipped* `BrowserAdapter.transport` (real source via `../src/browser.js`) for cases (a)/(d)/(e), not a reimplementation; case (e) asserts the exact `(finalLoaded,finalLoaded)` / not-`(0,0)` rule and (d) asserts the real `processResponse()` render output, so the mock cannot trivially pass a broken binding. |
| T-02-06 | Repudiation / coverage gap | a missing assertion lets a regression ship silently | accept | D-14's five mandatory cases are encoded as explicit grep-checkable acceptance criteria; the CI `parity.yml` adapter-seam step runs `npx vitest run` so the test is gating, not advisory. |

**HIGH findings:** None. This plan is test-only; it adds no runtime surface and weakens no security posture. It exists to *prove* the 02-01 implementation honors D-02/D-05/D-08.
</threat_model>

<verification>
- ROADMAP success criterion 1 (callback fires with (sent,total) on files+option; never when no files / option unset): directly proven by cases (a) and (b).
- D-02 silent fallback (no progress, no error on missing transport): proven by case (c).
- D-08 shared response path: proven by case (d) asserting the rendered vm round-trips through the unchanged `processResponse()`.
- D-05 indeterminate-completion rule (never (0,0)): proven by case (e).
- `cd viewmodel-shell && npm test` exits 0 (new test + existing adapter-seam.test.ts both green).
- `git status --porcelain` shows no source/parity/demo modification (D-15 boundary held).
</verification>

<success_criteria>
- `viewmodel-shell/test/upload-progress.test.ts` exists with five passing `it` cases mapping 1:1 to D-14 (a)-(e).
- The test drives the real shipped `BrowserAdapter.transport` (cases a/d/e) and exercises the fetch fallback with a render-only adapter (case c).
- `npx vitest run test/upload-progress.test.ts` and full `npm test` exit 0; existing `adapter-seam.test.ts` remains green.
- No `src/`, `parity/`, FeatureProbe, or demo file modified (D-15).
</success_criteria>

<output>
After completion, create `.planning/phases/02-upload-progress-milestone-closeout/02-02-SUMMARY.md`
</output>
