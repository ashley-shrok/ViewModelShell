---
phase: 05-canonical-examples-0-4-0-release-closeout
plan: 05
type: execute
wave: 3
depends_on: [01]
files_modified:
  - viewmodel-shell/package.json
  - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
  - CHANGELOG.md
  - MIGRATION.md
  - README.md
autonomous: true
requirements: [RELEASE-01, RELEASE-03]
must_haves:
  truths:
    - "npm package.json version is 0.4.0 and NuGet .csproj <Version> is 0.4.0 (aligned)"
    - "Every source-of-truth version-string location is enumerated and confirmed bumped (no missed location)"
    - "CHANGELOG has one consolidated ## 0.4.0 milestone entry; MIGRATION has one ## Upgrading to 0.4.0 section"
    - "The dark→light flip is framed as an intentional default change, NOT a wire/API/ViewNode break, with the one-line restore"
    - "The AGENTS.md major.minor-alignment rule TEXT is byte-unchanged; only version NUMBERS change"
  artifacts:
    - path: "viewmodel-shell/package.json"
      provides: "npm version 0.4.0"
      contains: '"version": "0.4.0"'
    - path: "viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj"
      provides: "NuGet version 0.4.0"
      contains: "<Version>0.4.0</Version>"
    - path: "CHANGELOG.md"
      provides: "Consolidated 0.4.0 milestone entry"
      contains: "## 0.4.0"
    - path: "MIGRATION.md"
      provides: "Upgrading to 0.4.0 section"
      contains: "Upgrading to 0.4.0"
  key_links:
    - from: "CHANGELOG.md ## 0.4.0"
      to: "the one-line dark restore"
      via: "import themes/dark-purple.css"
      pattern: "themes/dark-purple.css"
---

<objective>
0.4.0 release closeout (RELEASE-01/03): bump npm `@ashley-shrok/viewmodel-shell` `0.3.14`→`0.4.0` and NuGet `AshleyShrok.ViewModelShell` `0.3.10`→`0.4.0` ALIGNED (the layout wire-format change forces the minor per the AGENTS.md major.minor rule, D-26). Write ONE consolidated CHANGELOG `## 0.4.0` + ONE MIGRATION `## Upgrading to 0.4.0` covering the whole milestone (theme/density/card + layout enum + palette re-baseline + de-chrome), framing the dark→light flip as an intentional default change NOT a wire/API break with the one-line restore (D-05/D-26). Includes research item 3: enumerate every version-string source-of-truth location before bumping and confirm the AGENTS.md major.minor RULE TEXT stays byte-unchanged.

Purpose: A missed version-string location is a classic closeout failure; the milestone is incoherent if npm/NuGet are not aligned at 0.4.0 with the migration story honestly framed. Depends on Plan 01 (the `dark-purple.css` restore path the migration doc references must exist).
Output: Bumped package.json + .csproj, consolidated CHANGELOG/MIGRATION 0.4.0 entries, README accuracy fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-01-SUMMARY.md
@CHANGELOG.md
@MIGRATION.md

<interfaces>
Authoritative version-string SOURCE-OF-TRUTH locations (verified by repo sweep):
  viewmodel-shell/package.json                              line 3:  "version": "0.3.14"  -> 0.4.0
  viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj  line 13: &lt;Version&gt;0.3.10&lt;/Version&gt; -> 0.4.0
  CHANGELOG.md                                              new "## 0.4.0" ABOVE the "## 0.3.14" entry
  MIGRATION.md                                              new "## Upgrading to 0.4.0" ABOVE "## Upgrading to npm 0.3.13"
  README.md                                                 line ~54 "The base stylesheet ships a dark-purple theme" (now FALSE) + theme list line ~61 (missing dark-purple)

NOT version source-of-truth (DO NOT hand-edit — build-generated, regenerated by dotnet restore/build):
  demo/**/bin/**, demo/**/obj/** (*.deps.json, project.assets.json, *.nuget.dgspec.json) carry the OLD
  NuGet version as restore artifacts; they refresh when Plan 06 rebuilds the .NET demos. The in-repo demos
  use a local ProjectReference, so the bin/obj numbers are not a release surface.

Existing CHANGELOG entry format (copy this exact one-section-per-release shape):
  ## <version> — <title>
  **npm:** `x` (KIND) · **NuGet:** `y` (KIND)
  ### Added / ### Changed / ### Consumer action
  --- (hr between entries)

The 0.3.13 MIGRATION "Why this is a PATCH" reasoning is the honest-framing TEMPLATE to mirror
symmetrically for the 0.4.0 "Why this is a MINOR (aligned)" explanation (layout wire-format change
per the major.minor rule).

AGENTS.md line ~13 major.minor-alignment RULE TEXT stays byte-unchanged; only numbers change
(research item 3 cross-check; Plan 04 verifies the AGENTS.md text byte-identity).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every version-string source-of-truth location + confirm the major.minor rule text byte-unchanged (RESEARCH ITEM — RELEASE-01)</name>
  <read_first>
    - viewmodel-shell/package.json (line 3 version)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (line 13 Version element)
    - README.md (every line — scan for version strings, badges, the "ships a dark-purple theme" claim ~line 54, the theme list ~line 61)
    - AGENTS.md (line ~13 major.minor rule text; any version-number references in the header/wire-format sections)
    - CHANGELOG.md, MIGRATION.md (the version tables + existing entries)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (Specifics RESEARCH ITEM — version-string completeness; D-26)
  </read_first>
  <action>
    Research item 3: produce the COMPLETE enumeration of version-string locations BEFORE any bump so no location is missed.

    Run a repo-wide sweep for `0.3.14`, `0.3.10`, and version-shaped strings in source-of-truth files (exclude `node_modules/`, `.planning/`, `.claude/`, and build-generated `bin/`/`obj/` — they refresh on rebuild and are NOT a release surface). Classify EVERY hit into exactly one of:
    - MUST bump (npm 0.3.14 -> 0.4.0): `viewmodel-shell/package.json` line 3.
    - MUST bump (NuGet 0.3.10 -> 0.4.0): `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` Version element.
    - MUST add a NEW entry (not bump): CHANGELOG.md (new `## 0.4.0`), MIGRATION.md (new `## Upgrading to 0.4.0`).
    - Accuracy-touch: README.md — the "The base stylesheet ships a dark-purple theme" claim (FALSE after Plan 01: shipped default is now the light-purple value set) and the theme list (missing `dark-purple`); plus any README badge/version reference if one exists.
    - Historical — DO NOT touch: prior CHANGELOG/MIGRATION 0.3.x entries (historical record; D-26: intermediate 0.3.x dev states are non-events, not re-enumerated).
    - Build-generated — DO NOT hand-edit: any `demo/**/bin/**` or `demo/**/obj/**` JSON carrying `0.3.x` — note "refreshes on Plan 06 .NET rebuild", exclude from the bump list.

    CRITICAL cross-check (research item 3, second half): confirm the AGENTS.md major.minor-alignment RULE TEXT (line ~13) is the governing rule and that an ALIGNED 0.4.0 bump is what the rule REQUIRES (the layout enum is a wire-format change -> minor -> both sides move together, symmetric to the 0.3.13 "why PATCH"). Record that the rule TEXT itself stays byte-unchanged — only the version NUMBERS change.

    OUTPUT (mandatory, in SUMMARY): the complete classified enumeration table — every source-of-truth location, current value, target value (or new-entry / accuracy-touch / historical-untouched / build-generated-refresh). This table is the checklist Task 2 executes against.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pj=JSON.parse(fs.readFileSync('viewmodel-shell/package.json','utf8')).version; const cs=fs.readFileSync('viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj','utf8'); const m=cs.match(/<Version>([^<]+)<\/Version>/); console.log('pre-bump baseline npm='+pj+' nuget='+(m&&m[1]))"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a complete classified version-string enumeration table covering package.json, .csproj, CHANGELOG, MIGRATION, README, and the build-generated bin/obj exclusion note.
    - The enumeration explicitly records the AGENTS.md major.minor rule as governing and that the rule TEXT stays byte-unchanged (only numbers change).
    - No edits are made in this task (enumeration/research only — Task 2 executes the bump).
  </acceptance_criteria>
  <done>Complete version-string enumeration table produced; major.minor rule confirmed as governing with rule text byte-unchanged; ready for Task 2 to execute.</done>
</task>

<task type="auto">
  <name>Task 2: Bump npm + NuGet to aligned 0.4.0; write the consolidated CHANGELOG/MIGRATION 0.4.0 entries; README accuracy fixes (RELEASE-01/03, D-05/D-26)</name>
  <read_first>
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-05-SUMMARY.md (the Task-1 enumeration checklist — execute against it; if SUMMARY not yet written, the enumeration is in this plan's <interfaces>)
    - viewmodel-shell/package.json (line 3)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (line 13)
    - CHANGELOG.md (lines 1-25 — the format + the 0.3.14 entry the new 0.4.0 entry goes ABOVE)
    - MIGRATION.md (lines 1-60 — the format + the 0.3.13 "Why this is a PATCH" honest-framing template)
    - README.md (line ~54 dark-purple claim, line ~61 theme list)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-01-SUMMARY.md (the palette re-baseline + dark-purple.css restore path the migration doc cites)
  </read_first>
  <action>
    (A) npm bump: in `viewmodel-shell/package.json` change `"version": "0.3.14"` -> `"version": "0.4.0"` (line 3 only; keep JSON valid).

    (B) NuGet bump: in `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` change the `<Version>0.3.10</Version>` element -> `<Version>0.4.0</Version>` (line 13 only). Do NOT touch the worktree copies under `.claude/worktrees/` (not a release surface).

    (C) CHANGELOG.md — add ONE consolidated entry directly ABOVE the `## 0.3.14 — CopyButtonNode` line, matching the existing one-section-per-release format exactly:
      `## 0.4.0 — Design system: theme + layout + canonical examples`
      `**npm:** \`0.4.0\` (MINOR) · **NuGet:** \`0.4.0\` (MINOR — wire-format change, aligned)`
      `### Added` — the shipped default stylesheet/page shell + spacing/type scale + `density` (comfortable/compact) + `section variant:"card"` (Phase 3); the `layout` preset enum (`stack`/`split`/`cards`) on `page`/`section` (Phase 4); the new `themes/dark-purple.css`; the Showcase canonical reference set (Dashboard/Form-heavy/List-detail).
      `### Changed` — the shipped `:root` default re-based dark->light onto the `light-purple` value set (intentional DEFAULT change, NOT a wire/API/ViewNode break); demos de-chromed onto the shipped stylesheet.
      `### Consumer action` — "**None required for the wire contract** — the `layout` enum is additive and omitted = byte-identical prior behavior; existing apps render unchanged unless they opt in. The shipped default look changed dark->light: if you relied on the prior dark default and set no theme, restore it with one line: `import \"@ashley-shrok/viewmodel-shell/themes/dark-purple.css\";`. The npm `0.3.14`->`0.4.0` / NuGet `0.3.10`->`0.4.0` jump is a MINOR because the `layout` enum is a wire-format change — by the AGENTS.md major.minor rule both packages move together (symmetric to the 0.3.13 'why PATCH' explanation)." Keep the `---` hr between entries.

    (D) MIGRATION.md — add ONE new `## Upgrading to 0.4.0` section directly ABOVE the existing `## Upgrading to npm \`0.3.13\`` section, mirroring its structure (an "Exact versions" table: npm `0.3.14`->`0.4.0`, NuGet `0.3.10`->`0.4.0`; a "Why this is a MINOR (aligned)" subsection symmetric to the 0.3.13 "Why this is a PATCH" reasoning — the layout enum is a wire-format change, the major.minor rule requires both sides bump together; the honest dark->light default-change caveat framed as intentional, NOT breaking, with the exact one-line restore `import "@ashley-shrok/viewmodel-shell/themes/dark-purple.css";`; "existing apps render unchanged unless they opt in"). One consolidated section for the whole milestone (D-26 — do not separately enumerate intermediate 0.3.x dev states).

    (E) README.md accuracy-touch (D-22): line ~54 "The base stylesheet ships a dark-purple theme. To override, import a theme file on top:" is now FALSE — the shipped default is the light-purple value set (Plan 01 D-01). Correct it to state the shipped default is light (purple accent) and the prior dark default is `themes/dark-purple.css` (one import away). Add `dark-purple` to the available-themes list (line ~61) so all 12 are listed. Accuracy-only — do NOT rewrite unrelated README content; do NOT touch the MIGRATION/CHANGELOG reference sentence beyond what is factually stale.

    DO NOT modify the AGENTS.md major.minor rule text or any AGENTS.md version reference (Plan 04 owns AGENTS.md; the rule text is byte-unchanged). DO NOT hand-edit any `bin/`/`obj/` build artifact.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pj=JSON.parse(fs.readFileSync('viewmodel-shell/package.json','utf8')).version; const cs=fs.readFileSync('viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj','utf8'); const cl=fs.readFileSync('CHANGELOG.md','utf8'); const mg=fs.readFileSync('MIGRATION.md','utf8'); const rm=fs.readFileSync('README.md','utf8'); const ok=pj==='0.4.0'&&/<Version>0\.4\.0<\/Version>/.test(cs)&&/^## 0\.4\.0/m.test(cl)&&cl.indexOf('## 0.4.0')<cl.indexOf('## 0.3.14')&&/Upgrading to 0\.4\.0/.test(mg)&&/themes\/dark-purple\.css/.test(cl)&&/themes\/dark-purple\.css/.test(mg)&&/dark-purple/.test(rm)&&!/ships a dark-purple theme/.test(rm); if(!ok){console.error('FAIL release bump/docs');process.exit(1)} console.log('OK 0.4.0 aligned')"</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/package.json` `"version"` is exactly `"0.4.0"`; valid JSON.
    - `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` contains `<Version>0.4.0</Version>` (the non-worktree copy).
    - CHANGELOG.md has one new `## 0.4.0` entry positioned ABOVE `## 0.3.14`, in the existing format, containing the one-line `import "@ashley-shrok/viewmodel-shell/themes/dark-purple.css";` restore and the "MINOR because layout is a wire-format change, aligned" rationale.
    - MIGRATION.md has one new `## Upgrading to 0.4.0` section ABOVE `## Upgrading to npm 0.3.13`, with an exact-versions table (npm 0.3.14->0.4.0, NuGet 0.3.10->0.4.0), the "why MINOR (aligned)" reasoning, and the dark->light intentional-not-breaking framing + one-line restore.
    - README.md no longer claims "ships a dark-purple theme"; the theme list includes `dark-purple` (all 12 listed); no unrelated README content rewritten.
    - No `bin/`/`obj/` artifact hand-edited; AGENTS.md untouched by this plan.
  </acceptance_criteria>
  <done>npm + NuGet aligned at 0.4.0; consolidated CHANGELOG/MIGRATION 0.4.0 entries with the honest dark->light framing + one-line restore; README accuracy fixed; no missed location.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits version strings and release documentation. No code logic, no runtime surface, no input/secret/auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | (N/A) | package.json / .csproj / CHANGELOG / MIGRATION / README | accept | Realistic threat surface is N/A — version-number + release-doc edits. Hygiene only: no secrets/tokens introduced in the version or doc edits (verifiable: `git diff` is a version-number change + Markdown release notes + a README accuracy fix — no secret-shaped strings; no new dependency, no install script change). |
</threat_model>

<verification>
- npm `package.json` and NuGet `.csproj` both at `0.4.0` (aligned, RELEASE-01).
- Complete version-string enumeration recorded (research item 3) — no source-of-truth location missed; build-generated bin/obj excluded with a refresh note.
- One consolidated CHANGELOG `## 0.4.0` + one MIGRATION `## Upgrading to 0.4.0`, dark->light framed as intentional-not-breaking with the one-line restore (RELEASE-03, D-05/D-26).
- README accuracy-fixed (no false "ships a dark-purple theme"; all 12 themes listed); AGENTS.md major.minor rule text byte-unchanged (Plan 04 verifies the AGENTS.md side).
</verification>

<success_criteria>
0.4.0 ships with npm + NuGet aligned, every version-string source-of-truth location confirmed bumped (no classic missed-location closeout failure), and the milestone's migration story honestly framed (the dark->light flip is an intentional default change, not a wire/API break; prior look is one import away). RELEASE-01 + RELEASE-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-05-SUMMARY.md` recording the complete classified version-string enumeration table (research item 3), the final bumped values, and confirmation the major.minor rule text is byte-unchanged.
</output>
