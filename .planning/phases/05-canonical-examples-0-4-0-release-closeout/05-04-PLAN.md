---
phase: 05-canonical-examples-0-4-0-release-closeout
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - AGENTS.md
autonomous: true
requirements: [EXAMPLES-03]
must_haves:
  truths:
    - "AGENTS.md has one focused Design system section: import styles.css + a theme → serviceable look"
    - "It documents the --vms-* token override seam (--vms-page-max / --vms-font-* / colors)"
    - "It gives a when-to-use guide for stack / split / cards + density + section variant:card"
    - "The preset→archetype mapping is exactly D-10/D-13's and points to the live Showcase as the worked example"
    - "The now-false 'app owns the CSS' / dark-theme-stylesheet lines are corrected (accuracy-only)"
  artifacts:
    - path: "AGENTS.md"
      provides: "Design system section + bounded accuracy pass"
      contains: "Design system"
  key_links:
    - from: "AGENTS.md Design system section"
      to: "the live Showcase canonical set"
      via: "single-source-of-truth pointer (D-23)"
      pattern: "Showcase"
---

<objective>
Document the design system in AGENTS.md so an agent can use presets/density/card from docs alone (EXAMPLES-03). Add ONE focused, tight "Design system" section (D-20) and perform a BOUNDED accuracy-only pass fixing exactly the statements the Phase 3-5 design-system work invalidated (D-21). The preset→archetype mapping stated is EXACTLY D-10/D-13's and points to the live Showcase as the single-source-of-truth worked example (D-23 — docs and Showcase cannot drift). AGENTS.md only; no new standalone doc (D-22).

Purpose: AGENTS.md is the normative few-shot surface — a bare class list does not meet "usable from docs alone" for an agent deciding WHEN to use cards vs split. Depends on Plan 02 (Showcase archetypes) + Plan 03 (de-chromed demos) so the docs mirror the FINISHED reality (D-23).
Output: An updated AGENTS.md with the Design system section + the bounded accuracy fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-02-SUMMARY.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-03-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Now-false AGENTS.md statements to fix (accuracy-only — verified line refs from the repo): -->
Line ~158 (footer of §"CSS classes emitted by BrowserAdapter"):
  "The framework emits class names; the app owns the CSS. Reference dark-theme
   stylesheets: `demo/Tasks/frontend/index.html` and `demo/HelpDesk/frontend/requester.html`."
  → FALSE after Plan 03: demos no longer hand-roll CSS; Tasks now pins dark-purple via main.ts;
    HelpDesk/requester pins light-teal via requester.ts; there are no per-demo dark-theme <style> blocks.
Recurring "the app owns the CSS" framing appears in: §"CSS classes emitted by BrowserAdapter" footer,
  §"Demo apps" table descriptions, §"Frontend wiring", §"Testing" — fix ONLY where it now mis-states reality.
§"Frontend wiring" code block (~line 437): `onLoading: (loading) => document.body.classList.toggle("is-loading", loading)`
  — the is-loading dim CSS is dropped (D-15); the callback example is still valid JS but the doc should not
    imply a shipped dim affordance. Accuracy-touch only if it asserts a visual behavior.

Locked preset→archetype→Bootstrap mapping to state VERBATIM (D-10/D-13):
  Dashboard  = `cards`  (stat/summary card grid + section variant:"card") → Bootstrap "Dashboard"
  Form-heavy = `stack`  (default vertical; or split-with-aside)            → Bootstrap "Checkout"
  List/detail= `split`  (list ↔ detail, collapses on narrow)              → Bootstrap "Album"

Sanctioned override seam tokens (the ONLY allowed per-app deviation, D-17/D-19):
  --vms-page-max  (page width retune), --vms-font-body/head/mono (branded fonts), :root color vars (full reskin)
  applied as a tiny per-app stylesheet imported AFTER the theme — never an HTML <style>.
The major.minor-alignment RULE TEXT (AGENTS.md line ~13) stays BYTE-UNCHANGED (Plan 05 owns version NUMBERS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the focused "Design system" section to AGENTS.md (D-20/D-23)</name>
  <read_first>
    - AGENTS.md (entire file — to choose the correct insertion point and match the doc's existing voice/format; note §"Node types" ~line 101, §"CSS classes emitted by BrowserAdapter" ~line 137, §"Patterns" ~line 175)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md (§AGENTS.md Design-System Doc Contract — the exact scope; §Archetype Composition Contract — the locked mapping)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-02-SUMMARY.md (the FINAL Showcase archetype composition + Form-heavy preset choice — docs must mirror what was actually built, D-23)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (D-20 tight not sprawling; D-23 single source of truth)
  </read_first>
  <action>
    Add ONE new `## Design system` section to AGENTS.md (place it after §"CSS classes emitted by BrowserAdapter" and before §"Non-obvious framework behaviors" — adjacent to the class table it complements, matching the doc's existing terse markdown voice). Keep it TIGHT (D-20: not a sprawling guide — target ~40-70 lines). Cover exactly these four things:

    1. **Serviceable by default.** Import the shipped stylesheet + one theme; the framework owns reset/centering/width/background/font (the `.vms-page` shell + body rule). Show the exact two-line import (the real-app pattern the demos now use):
       `import "@ashley-shrok/viewmodel-shell/styles.css";`
       `import "@ashley-shrok/viewmodel-shell/themes/<name>.css";`
       List the 12 shipped theme names INCLUDING the new `dark-purple` (dark-blue, dark-green, dark-rose, dark-amber, dark-teal, dark-purple, light-purple, light-blue, light-green, light-rose, light-amber, light-teal). State the shipped DEFAULT (no theme import) is now `light-purple`'s value set and the prior dark default is `themes/dark-purple.css` (one import away).

    2. **The `--vms-*` override seam.** The ONLY sanctioned per-app deviation: a tiny per-app stylesheet with a single `:root{}` setting `--vms-page-max` (page width) and/or `--vms-font-body`/`--vms-font-head`/`--vms-font-mono` (branded fonts), or any `--vms-*` color var for a full reskin — imported AFTER the theme, NEVER an HTML `<style>`. "Override the token, don't hand-roll." Reference RetroBoard's `src/app-tokens.css` (the `--vms-page-max` retune) as the live example (use the actual path from the Plan 03 SUMMARY).

    3. **When to use which layout preset / density / card** (the agent-decision guide — this is what makes it "usable from docs alone"):
       - `stack` (default, omitted): vertical flow — forms, single-column content.
       - `split`: two-column on wide, collapses to stacked on narrow, zero app breakpoints — list↔detail, content+aside.
       - `cards`: auto-fit grid from `--vms-card-min`, collapses to one column — dashboards, tile/summary grids.
       - `density: "compact"` on `page`: tightens the rhythm tokens globally (no app CSS).
       - `section variant:"card"`: a grouped surface (bg/border/padding/radius) — dashboard tiles, detail panes.

    4. **The canonical worked example (single source of truth, D-23).** State the LOCKED preset→archetype→Bootstrap mapping VERBATIM from the `<interfaces>` block (Dashboard=`cards`→Bootstrap "Dashboard"; Form-heavy=`stack`→Bootstrap "Checkout" — use the ACTUAL Form-heavy preset choice recorded in the Plan 02 SUMMARY; List/detail=`split`→Bootstrap "Album"). Point to the live Showcase (`demo/Showcase/`) as the worked example rather than inventing separate doc snippets — docs and Showcase reinforce each other and cannot drift (D-23).

    HARD CONSTRAINTS: this is the ONLY new section. Do NOT rewrite the node table or class table (already accurate Phases 3/4). Do NOT add a standalone doc (D-22 — AGENTS.md only). Mirror exactly what Plans 02/03 actually built (read their SUMMARYs) — do not invent capabilities.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('AGENTS.md','utf8'); const sec=s.split(/^## /m).find(x=>x.startsWith('Design system')); if(!sec){console.error('no Design system section');process.exit(1)} const ok=/dark-purple/.test(sec)&&/--vms-page-max/.test(sec)&&/stack/.test(sec)&&/split/.test(sec)&&/cards/.test(sec)&&/variant.*card|card.*variant/.test(sec)&&/Showcase/.test(sec)&&/Bootstrap/.test(sec); if(!ok){console.error('Design system section missing required content');process.exit(1)} console.log('OK len='+sec.split(/\n/).length)"</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains exactly one new `## Design system` section, placed after the CSS-class table section.
    - It states the two-line styles.css + theme import, lists all 12 themes including `dark-purple`, and states the new shipped default = light-purple value set with `dark-purple.css` as the prior-default restore.
    - It documents the `--vms-page-max` / `--vms-font-*` / color override seam (after-theme per-app stylesheet, never `<style>`) and references the actual RetroBoard token-file path from the Plan 03 SUMMARY.
    - It has a when-to-use guide for `stack`/`split`/`cards` + `density:compact` + `section variant:"card"`.
    - It states the LOCKED preset→archetype→Bootstrap mapping verbatim and points to `demo/Showcase/` as the single-source-of-truth worked example; the Form-heavy preset stated matches the Plan 02 SUMMARY's actual choice.
    - The section is ≤ ~70 lines (tight, not sprawling — D-20).
    - No new standalone doc file created; only AGENTS.md modified by this task.
  </acceptance_criteria>
  <done>One tight Design system section added: serviceable-by-default + override seam + when-to-use guide + the locked mapping pointing at the live Showcase.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded accuracy-only pass over the now-false design-system statements (D-21/D-22)</name>
  <read_first>
    - AGENTS.md (§"CSS classes emitted by BrowserAdapter" footer line ~158; §"Non-obvious framework behaviors"; §"Patterns"→"Frontend wiring" ~435; §"Testing" ~473; §"Demo apps" ~508)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-03-SUMMARY.md (the FINAL per-demo theme mapping — the corrected reference points)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (D-21 bounded accuracy-only, no rewriting unrelated content; D-22 AGENTS.md only)
  </read_first>
  <action>
    BOUNDED accuracy-only pass — change ONLY statements the Phase 3-5 design-system work invalidated. Do NOT rewrite unrelated content (Phase 3/4 "don't improve adjacent surfaces" discipline).

    (A) Line ~158 footer of §"CSS classes emitted by BrowserAdapter": the sentence `The framework emits class names; the app owns the CSS. Reference dark-theme stylesheets: \`demo/Tasks/frontend/index.html\` and \`demo/HelpDesk/frontend/requester.html\`.` is now FALSE (demos no longer hand-roll CSS; no per-demo dark-theme `<style>`). Replace it with an accurate statement: the framework emits semantic class names styled by the shipped `viewmodel-shell/styles/default.css`; apps import `styles.css` + optionally one theme and author zero CSS (point to the new `## Design system` section). Use the actual de-chromed demos as the reference (e.g. Tasks pins `dark-purple` via `main.ts`, HelpDesk roles pin distinct themes via their entrypoints — use the Plan 03 SUMMARY's final mapping).

    (B) Recurring "the app owns the CSS" framing: scan §"Demo apps" table, §"Frontend wiring", §"Testing". Fix ONLY occurrences that now mis-state reality (i.e. claim apps hand-roll page CSS). Where "Frontend wiring" or "Testing" reference per-demo hand-rolled CSS or `is-loading` as a shipped visual affordance, correct to the de-chromed reality (theme via import; `is-loading` is an app-level JS hook with no shipped dim styling — the dim affordance was dropped/deferred, D-15). Leave all non-styling content (state record, controller pattern, action payloads, redirect/polling, MSBuild, testing commands) BYTE-UNCHANGED.

    (C) CRITICAL: the major.minor-alignment RULE TEXT at AGENTS.md line ~13 ("The two packages share major.minor — bumping a `ViewNode` type or wire-format change bumps both sides...") stays BYTE-UNCHANGED in THIS plan. Plan 05 owns version-NUMBER references; this plan must NOT touch any version string or the rule text. Verify (do not edit) that the rule text is byte-identical to its pre-phase state and record the byte-unchanged confirmation in the SUMMARY (this is part of research item 3's cross-check; Plan 05 owns the enumeration).

    Keep edits minimal and surgical — diff should show only the false-statement corrections, nothing else.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('AGENTS.md','utf8'); const bad=/the app owns the CSS\. Reference dark-theme stylesheets/.test(s); const ruleIntact=/The two packages share major\.minor — bumping a .ViewNode. type or wire-format change bumps both sides/.test(s); if(bad){console.error('stale owns-the-CSS/dark-theme line still present');process.exit(1)} if(!ruleIntact){console.error('major.minor rule text altered — MUST stay byte-unchanged');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The stale `the app owns the CSS. Reference dark-theme stylesheets: demo/Tasks/frontend/index.html and demo/HelpDesk/frontend/requester.html` sentence no longer exists; replaced with an accurate statement pointing to the Design system section and the de-chromed demos.
    - "App owns the CSS" framing corrected ONLY where it now mis-states reality; all non-styling sections (controller pattern, action payloads, redirect/polling, MSBuild, testing commands) are byte-unchanged (`git diff AGENTS.md` shows only styling-accuracy corrections + the new Design system section from Task 1).
    - The major.minor-alignment rule text (line ~13) is byte-identical to its pre-phase state (no version number or rule wording changed by this plan); confirmation recorded in SUMMARY.
    - No README or other doc modified (D-22 — AGENTS.md only; Plan 05 owns README accuracy + versions).
  </acceptance_criteria>
  <done>Bounded accuracy pass complete: false design-system statements corrected, unrelated content untouched, major.minor rule text verified byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits documentation prose in AGENTS.md only. No code, no config, no runtime surface, no input/secret/auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | (N/A) | AGENTS.md | accept | Realistic threat surface is N/A — documentation-only edit. Hygiene only: no secrets/tokens introduced in the doc text (verifiable: `git diff AGENTS.md` is prose + import-example snippets, no secret-shaped strings). |
</threat_model>

<verification>
- One tight `## Design system` section: serviceable-by-default + `--vms-*` override seam + when-to-use guide + the LOCKED preset/archetype/Bootstrap mapping pointing at the live Showcase (D-20/D-23).
- Bounded accuracy pass corrects only the now-false design-system statements (D-21); unrelated content byte-unchanged.
- The major.minor-alignment rule text is byte-unchanged (Plan 05 owns version numbers); AGENTS.md only (D-22).
</verification>

<success_criteria>
An agent can decide WHEN to use cards vs split vs stack, how to retune via the token seam, and which theme to import — from AGENTS.md alone — and the docs mirror the live Showcase (single source of truth, cannot drift). EXAMPLES-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-04-SUMMARY.md` recording the Design system section outline, the itemized accuracy fixes made, and the explicit confirmation that the major.minor-alignment rule text is byte-unchanged.
</output>
