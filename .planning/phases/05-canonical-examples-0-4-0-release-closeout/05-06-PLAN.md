---
phase: 05-canonical-examples-0-4-0-release-closeout
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - viewmodel-shell/scripts/check-no-demo-style.mjs
  - viewmodel-shell/scripts/check-theme-byte-identity.mjs
  - viewmodel-shell/package.json
  - .github/workflows/parity.yml
autonomous: false
requirements: [RELEASE-02, RELEASE-04]
must_haves:
  truths:
    - "The existing 7-fixture cross-backend parity suite is byte-identical green (.NET/Bun/Node), zero new parity surface"
    - "All existing Phase 3/4 jsdom class-emission tests stay green; no new jsdom behavior test added"
    - "A static repo-scan CI guard asserts zero <style> in demo/Showcase frontend HTML + .vms-*-only archetypes"
    - "A static CI guard asserts the 11 pre-existing theme files byte-identical + dark-purple.css byte-exact"
    - "The AA-contrast guard (Plan 01) + the new repo-scan guards all gate in parity.yml"
  artifacts:
    - path: "viewmodel-shell/scripts/check-no-demo-style.mjs"
      provides: "Static repo-scan: zero <style> in demo/Showcase frontend HTML"
      min_lines: 20
    - path: "viewmodel-shell/scripts/check-theme-byte-identity.mjs"
      provides: "Static guard: 11 theme files byte-identical + dark-purple.css byte-exact capture"
      min_lines: 25
  key_links:
    - from: ".github/workflows/parity.yml"
      to: "the two new static guards + the existing parity run"
      via: "gating steps beside core-globals + AA"
      pattern: "check:no-demo-style|check:theme-byte-identity"
---

<objective>
The concluding 0.4.0 closeout gate (RELEASE-02/04, D-24/D-25): prove the existing 7-fixture cross-backend parity suite is byte-identical green with ZERO new parity surface (no new fixture, no `backends.json` change — the re-based default + Showcase are CSS/client-only, parity diffs wire JSON not CSS); prove all existing Phase 3/4 jsdom class-emission tests stay green WITHOUT adding any new jsdom behavior test; and add the two static repo-scan CI guards (zero-`<style>` / `.vms-*`-only demos+archetypes, and the 11-theme byte-identity + `dark-purple.css` byte-exact capture) gated in parity.yml beside core-globals and the Plan 01 AA guard.

Purpose: This is the milestone's final falsifiable gate. Depends on ALL prior plans (it verifies their combined output). Phase 5 adds no wire/DOM behavior — only CSS values + repo hygiene — so verification is parity-regression + inherited-jsdom + static guards (D-25), never new jsdom behavior tests.
Output: Two new static guard scripts, package.json scripts, parity.yml gating steps, and a human-verified green gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-01-SUMMARY.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-02-SUMMARY.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-03-SUMMARY.md

<interfaces>
The parity suite (UNCHANGED — D-24, regression-only): 7 fixtures
  contacts, expenses, feature-probe, helpdesk, reorder, retro, tasks
  feature-probe = the Phase-4-widened FeatureProbe already exercising layout/density/card across
  .NET/Bun/Node. Run: `cd parity && bun install --frozen-lockfile && bun run run.ts`. NO new fixture,
  NO parity/backends.json change.

Existing jsdom tests (RELEASE-04 inherited-green — add NONE): viewmodel-shell/test/*.test.ts
  (theme-modifiers.test.ts = the Phase 3/4 layout/density/card class-emission tests). Run:
  `cd viewmodel-shell && npx vitest run` (jsdom env per vitest.config.ts).

CI-guard pattern to mirror (standalone Node ESM, read targets, scan/compare, console.log + process.exit):
  viewmodel-shell/scripts/check-core-platform-globals.mjs
  package.json scripts already has: check:core-globals, and (Plan 01) check:aa-contrast
parity.yml already gates: "Enforce core platform-agnosticism (AGNOSTIC-03)" then (Plan 01)
  "Enforce WCAG-AA on shipped default (D-07)" then framework vitest + .NET + parity steps.

Static-scan targets:
  Zero-<style>/.vms-*-only: the 7 non-bun frontend HTML files (demo/{ContactManager,ExpenseTracker,
    RetroBoard,Tasks,Showcase}/frontend/index.html + demo/HelpDesk/frontend/{index,agent,requester}.html)
    and demo/Showcase/frontend/src/main.ts (no <style>, no raw-HTML content nodes). Bun mirrors are pure
    server.ts (no HTML) — not in scope. Build-generated demo/**/AspNetCore/wwwroot/*.html are Vite output
    of the de-chromed frontend; either scan them too OR exclude them and rely on a wwwroot rebuild — the
    executor decides and records it (the source frontend HTML is the authoritative scan surface).
  Theme byte-identity: the 11 pre-existing theme files
    (dark-{blue,green,rose,amber,teal}, light-{purple,blue,green,rose,amber,teal}) byte-identical to their
    pre-phase git blob; themes/dark-purple.css byte-exact == the prior default.css :root dark color block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two static repo-scan CI guards + gate them in parity.yml (D-25)</name>
  <files>viewmodel-shell/scripts/check-no-demo-style.mjs, viewmodel-shell/scripts/check-theme-byte-identity.mjs, viewmodel-shell/package.json, .github/workflows/parity.yml</files>
  <read_first>
    - viewmodel-shell/scripts/check-core-platform-globals.mjs (the exact standalone-Node guard pattern to mirror)
    - viewmodel-shell/package.json (scripts block — mirror check:core-globals / check:aa-contrast entries)
    - .github/workflows/parity.yml (the gating step pattern; the Plan 01 AA step — add the two new steps beside, weaken nothing)
    - viewmodel-shell/styles/themes/dark-purple.css + viewmodel-shell/styles/default.css (the byte-exact-capture relationship the theme guard asserts; the prior dark block is recoverable from git history of default.css)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-03-SUMMARY.md (the final de-chromed HTML file list + HelpDesk landing approach — the scan surface)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (D-24/D-25 — static guards, not jsdom; D-03 the byte-identity assertions)
  </read_first>
  <action>
    Create TWO standalone Node ESM guards mirroring `check-core-platform-globals.mjs` structure exactly (read targets, scan/compare, `console.error`+`process.exit(1)` on violation, `console.log`+`process.exit(0)` on pass — zero deps, zero jsdom):

    (A) `viewmodel-shell/scripts/check-no-demo-style.mjs` — the falsifiable structural proxy (D-12/D-15). Scan the 7 non-bun frontend HTML files (`demo/ContactManager/frontend/index.html`, `demo/ExpenseTracker/frontend/index.html`, `demo/RetroBoard/frontend/index.html`, `demo/Tasks/frontend/index.html`, `demo/HelpDesk/frontend/index.html`, `demo/HelpDesk/frontend/agent.html`, `demo/HelpDesk/frontend/requester.html`) + `demo/Showcase/frontend/index.html`: assert each contains ZERO `<style` (case-insensitive). Additionally scan `demo/Showcase/frontend/src/main.ts`: assert NO `<style` substring and NO raw-HTML content construction (the Showcase archetypes must be `.vms-*` nodes only — assert no `innerHTML =`/`insertAdjacentHTML` of markup AND no `document.createElement("div"|"section"|"table"|...)` of content elements; the existing single `themeStyle = document.createElement("style")` runtime switcher element is the ONE allowed exception — match it precisely by its `id="vms-showcase-theme"` usage so the guard does not false-positive on the legitimate switcher, OR scope the main.ts assertion to "no literal `<style>` HTML and no markup-string innerHTML" and leave the JS-created switcher element alone; pick the precise rule and document it in the script header). On any violation list every offending file:reason and exit 1. Decide and record whether the build-generated `demo/**/AspNetCore/wwwroot/*.html` are also scanned (if yes, the executor must rebuild wwwroot from the de-chromed frontend first so the committed output matches; if excluded, document that the source frontend HTML is the authoritative surface and wwwroot is regenerated output).

    (B) `viewmodel-shell/scripts/check-theme-byte-identity.mjs` — the D-03 regression guard. Assert: (1) each of the 11 pre-existing theme files (`dark-blue/green/rose/amber/teal`, `light-purple/blue/green/rose/amber/teal`) is byte-identical to its pre-Phase-5 git blob — implement via `git show <pre-phase-ref>:viewmodel-shell/styles/themes/<file>` compared to the working file, OR (simpler, CI-stable) compare each theme file's bytes to a checked-in expected SHA-256 manifest the script carries (compute the 11 SHAs from the current files, since Plan 01 left them byte-unchanged, and embed them as the expected manifest with a header comment explaining they are the frozen-by-D-03 hashes); (2) `themes/dark-purple.css` `:root` color declarations are byte-exact equal to the prior `default.css` `:root` dark color block (the values recorded in the Plan 01 SUMMARY / recoverable via `git show` of pre-Plan-01 default.css) — normalize only trailing whitespace/newline, assert the declaration set + values match exactly. On mismatch print the diff and exit 1.

    Add to `viewmodel-shell/package.json` scripts, after `check:aa-contrast`:
      `"check:no-demo-style": "node scripts/check-no-demo-style.mjs",`
      `"check:theme-byte-identity": "node scripts/check-theme-byte-identity.mjs",`
    (keep JSON valid).

    Add two new gating steps to `.github/workflows/parity.yml` immediately AFTER the Plan 01 `Enforce WCAG-AA on shipped default (D-07)` step, named `Enforce zero per-demo <style> (D-12/D-15)` and `Enforce theme byte-identity (D-03)`, each running `cd viewmodel-shell && npm run check:no-demo-style` / `npm run check:theme-byte-identity`. Do NOT modify or weaken any existing step (D-25 — add steps, weaken nothing). Do NOT add a new parity fixture, do NOT change `parity/backends.json` (D-24). Do NOT add any new jsdom test (D-25).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npm run check:no-demo-style && npm run check:theme-byte-identity && node -e "const y=require('fs').readFileSync('../.github/workflows/parity.yml','utf8'); const ok=y.includes('check:no-demo-style')&&y.includes('check:theme-byte-identity')&&y.includes('check:aa-contrast')&&y.includes('check:core-globals'); if(!ok){console.error('parity.yml missing a guard step');process.exit(1)} console.log('all 4 guards gated')"</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/scripts/check-no-demo-style.mjs` exists, standalone Node (no jsdom — `grep -L jsdom` returns it), `npm run check:no-demo-style` exits 0 against the Plan-03 de-chromed demos + Plan-02 Showcase.
    - `viewmodel-shell/scripts/check-theme-byte-identity.mjs` exists, standalone Node, `npm run check:theme-byte-identity` exits 0 (11 theme files byte-identical, dark-purple.css byte-exact capture).
    - `package.json` scripts has `check:no-demo-style` and `check:theme-byte-identity`; valid JSON.
    - `.github/workflows/parity.yml` gates BOTH new guards (steps named per D-12/D-15 and D-03) after the AA step; `git diff parity.yml` shows only added steps — no existing step removed/weakened; no new parity fixture; `parity/backends.json` unchanged.
    - No new file under `viewmodel-shell/test/` (no new jsdom behavior test — D-25); `git status` shows zero new `test/*.test.ts`.
  </acceptance_criteria>
  <done>Two static repo-scan guards added + scripted + gated in parity.yml beside core-globals/AA; zero new fixture, zero backends.json change, zero new jsdom test.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full regression gate — parity 7/7 byte-identical + inherited jsdom green + all 4 static guards (RELEASE-02/04, D-24/D-25)</name>
  <files>(verification-only — no source files modified; SUMMARY records results)</files>
  <read_first>
    - parity/run.ts + parity/backends.json (the 7-fixture harness — confirm unchanged)
    - viewmodel-shell/vitest.config.ts + viewmodel-shell/test/theme-modifiers.test.ts (the inherited Phase 3/4 jsdom class-emission tests)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-03-SUMMARY.md (whether wwwroot needs a rebuild before the .NET parity backends start / before the no-demo-style scan if wwwroot is in scope)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-05-SUMMARY.md (the bumped versions — confirm the .NET build picks up the local ProjectReference, not a stale NuGet)
  </read_first>
  <action>
    Execute the concluding regression gate against the COMBINED output of Plans 01-05. Do NOT add/modify any fixture, backend, or jsdom test — this is pure regression verification (D-24/D-25).

    (1) If Plan 03's SUMMARY indicates wwwroot is in the no-demo-style scan scope OR the .NET parity backends serve wwwroot: rebuild the affected demo frontends so the committed `wwwroot/*.html` are the de-chromed Vite output (e.g. `cd demo/<demo>/frontend && npm install && npm run build` for each de-chromed demo, and `demo/Showcase/frontend` for the archetype set). Otherwise note wwwroot is regenerated build output and out of the authoritative scan surface.

    (2) Build the framework: `cd viewmodel-shell && npm install && npm run build`.

    (3) Run ALL 4 static guards (must all exit 0): `npm run check:core-globals`, `npm run check:aa-contrast`, `npm run check:no-demo-style`, `npm run check:theme-byte-identity`.

    (4) Run the inherited jsdom suite (must stay green, zero new tests): `cd viewmodel-shell && npx vitest run` — confirm `test/theme-modifiers.test.ts` (Phase 3/4 layout/density/card class-emission) + the other existing tests all pass.

    (5) Run the .NET test suites + build the .NET demos (parity backends): `dotnet build` each demo AspNetCore project, `dotnet test` each AspNetCore.Tests (mirroring parity.yml's existing steps).

    (6) Run the full cross-backend parity suite UNCHANGED: `cd parity && bun install --frozen-lockfile && bun run run.ts`. Assert all 7 fixtures (contacts, expenses, feature-probe, helpdesk, reorder, retro, tasks) are byte-identical green across .NET / Bun / Node — the Phase-4-widened feature-probe already covers layout/density/card; RELEASE-02 inherits it (D-24). ANY non-byte-identical diff is a phase blocker — investigate and fix the regression (do NOT modify a fixture or normalize.ts to mask a real wire drift; the re-based default + Showcase are CSS/client-only and MUST have zero parity impact — if parity drifts, a non-CSS change leaked and must be found).

    Record in the SUMMARY: the parity result (7/7 byte-identical green), the jsdom result (all existing pass, zero added), and all 4 static-guard exit codes. If everything is green, proceed to the human-verify checkpoint. If anything fails, STOP and report the failure as a phase blocker.
  </action>
  <verify>
    <automated>cd parity && bun install --frozen-lockfile >/dev/null 2>&1; bun run run.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd parity && bun run run.ts` exits 0 with all 7 fixtures byte-identical green across all backends; `parity/backends.json` and `parity/fixtures/*` are git-unchanged (D-24 zero new parity surface).
    - `cd viewmodel-shell && npx vitest run` exits 0; `git status` shows no new `viewmodel-shell/test/*.test.ts` (RELEASE-04 inherited-green, no new jsdom behavior test — D-25).
    - All 4 static guards exit 0: `check:core-globals`, `check:aa-contrast`, `check:no-demo-style`, `check:theme-byte-identity`.
    - .NET demo builds + AspNetCore.Tests all pass.
    - The SUMMARY records the parity 7/7 result, the jsdom inherited-green result, and the 4 guard exit codes.
  </acceptance_criteria>
  <done>Full regression gate green: parity 7/7 byte-identical, inherited jsdom green with zero new tests, all 4 static guards passing — RELEASE-02/04 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reviewer sign-off — visual-quality benchmark + closeout confirmation (D-12)</name>
  <files>.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md (§Falsifiability contract — the reviewer-sign-off acceptance row; the LOCKED Bootstrap benchmark mapping D-13)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-02-SUMMARY.md (the final archetype composition + Form-heavy preset choice)
  </read_first>
  <action>
    This is a BLOCKING human-verify checkpoint (NOT autonomous): pause execution and present the
    visual-quality benchmark to the user. After all mechanical gates (Tasks 1-2) are green, ask the
    user to run the Showcase and benchmark each archetype against its LOCKED Bootstrap page, then
    record their explicit owned+dated sign-off (or issue list) in the 05-06-SUMMARY.md. Do NOT
    auto-approve; do NOT claim this as automated (D-12: visual quality cannot be browser-unit-tested,
    by framework design). The sign-off is a first-class acceptance artifact — present and explicit,
    never silently absent, never pretended-automated.
  </action>
  <what-built>
    The full 0.4.0 milestone is mechanically green: parity 7/7 byte-identical, inherited jsdom green, all 4 static CI guards passing (core-globals, WCAG-AA on the new light default, zero-per-demo-<style>/.vms-*-only, 11-theme byte-identity + dark-purple.css byte-exact). The Showcase is a navigable canonical set (gallery + Dashboard/Form-heavy/List-detail on the LOCKED cards/stack/split mapping, on the new light default); every demo is de-chromed onto the shipped stylesheet with its pinned theme; AGENTS.md documents the design system pointing at the Showcase; npm+NuGet aligned at 0.4.0 with MIGRATION/CHANGELOG written.
  </what-built>
  <how-to-verify>
    Visual quality cannot be browser-unit-tested (the framework's no-browser-test promise) — D-12 splits it into the mechanically-checkable proxies (now all green) PLUS this irreducibly-subjective, explicitly-owned reviewer sign-off (NOT pretended-automated, NOT silently skipped).
    1. Run the Showcase: `cd demo/Showcase/frontend && npm install && npm run dev`; open the served URL.
    2. Switch through the top-level nav: Components (gallery + theme switcher), Dashboard, Form, List / detail.
    3. For each archetype, compare "visually serviceable" against its LOCKED Bootstrap benchmark page (open the named Bootstrap example for side-by-side reference): Dashboard ↔ Bootstrap "Dashboard"; Form ↔ Bootstrap "Checkout"; List / detail ↔ Bootstrap "Album". Confirm the archetypes look comparable-quality on the new light default (not pixel-identical — comparable serviceability).
    4. Optionally spot-run one or two de-chromed demos (e.g. Tasks on dark-purple, HelpDesk agent vs requester distinct themes) to confirm the de-chrome reads correctly with zero hand-rolled CSS.
    5. In the verification/SUMMARY doc, record an EXPLICIT reviewer sign-off line (owned, dated) stating the canonical set is visually serviceable vs the Bootstrap benchmarks — or list specific issues to fix.
  </how-to-verify>
  <verify>
    Human-verify checkpoint: the user runs the Showcase, benchmarks the three archetypes against Bootstrap "Dashboard"/"Checkout"/"Album", and either types "approved" (the executor then records the explicit dated reviewer sign-off line in 05-06-SUMMARY.md) or supplies a specific visual-issue list to fix. Not automatable by design (D-12).
  </verify>
  <resume-signal>Type "approved" (records the explicit reviewer sign-off in the SUMMARY) or describe the visual issues to fix.</resume-signal>
  <done>An explicit, owned, dated reviewer sign-off line is recorded in 05-06-SUMMARY.md confirming the canonical set is visually serviceable vs the LOCKED Bootstrap benchmarks (or the listed issues are resolved and re-signed). Sign-off present and explicit, never automated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan adds static Node CI scripts, parity.yml steps, and runs the existing regression suite. No runtime input, network, secret, or auth surface is introduced. The static guards only read repo files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | (N/A) | check-no-demo-style.mjs / check-theme-byte-identity.mjs / parity.yml | accept | Realistic threat surface is N/A — static repo-scan scripts + CI workflow steps + running the existing regression suite. Hygiene only: no secrets/tokens introduced in the scripts or workflow steps (verifiable: `git diff` is Node file-scan logic + parity.yml step additions — no secret-shaped strings; no new dependency, no network call in the guards). |
</threat_model>

<verification>
- Parity 7/7 byte-identical green across .NET/Bun/Node, zero new fixture, `backends.json` unchanged (RELEASE-02, D-24).
- Existing jsdom class-emission tests green; zero new jsdom behavior test (RELEASE-04, D-25).
- All 4 static guards gated in parity.yml beside core-globals (D-25): core-globals, AA-contrast (Plan 01), zero-demo-<style>, theme-byte-identity.
- Explicit owned reviewer sign-off recorded for the irreducibly-subjective visual-quality benchmark (D-12) — present, not automated, not skipped.
</verification>

<success_criteria>
0.4.0 is mechanically falsifiably done: the wire contract is regression-proven (parity 7/7), behavior is inherited-green (jsdom), the milestone invariants are CI-enforced (4 static guards), and the irreducibly-subjective visual-quality dimension has an explicit owned reviewer sign-off benchmarked against Bootstrap. RELEASE-02 + RELEASE-04 satisfied; the phase (and the 0.4.0 milestone) is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-06-SUMMARY.md` recording the parity 7/7 result, the inherited-jsdom result, the 4 static-guard exit codes, and the explicit dated reviewer sign-off line (D-12).
</output>
