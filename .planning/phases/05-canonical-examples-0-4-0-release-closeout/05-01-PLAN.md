---
phase: 05-canonical-examples-0-4-0-release-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/styles/default.css
  - viewmodel-shell/styles/themes/dark-purple.css
  - viewmodel-shell/package.json
  - viewmodel-shell/scripts/check-aa-contrast.mjs
  - .github/workflows/parity.yml
autonomous: true
requirements: [EXAMPLES-01, RELEASE-04]
must_haves:
  truths:
    - "Importing only styles.css renders a light page (#f7f7f9 bg, #fff surface, #5a4ad7 accent)"
    - "Importing themes/dark-purple.css reproduces the prior dark default pixel-for-pixel"
    - "The new light default passes WCAG-AA for body + muted text and semantic colors, CI-enforced"
    - "Every --vms-* variable name still exists in default.css :root (none removed/renamed)"
  artifacts:
    - path: "viewmodel-shell/styles/themes/dark-purple.css"
      provides: "Byte-exact capture of the prior default :root dark block"
      contains: "--vms-bg:          #0f0f11"
    - path: "viewmodel-shell/scripts/check-aa-contrast.mjs"
      provides: "Standalone Node WCAG-AA contrast CI guard over default.css :root"
      min_lines: 40
    - path: "viewmodel-shell/styles/default.css"
      provides: "Re-based light :root (light-purple value set)"
      contains: "--vms-bg:          #f7f7f9"
  key_links:
    - from: ".github/workflows/parity.yml"
      to: "viewmodel-shell/scripts/check-aa-contrast.mjs"
      via: "npm run check:aa-contrast gating step"
      pattern: "check:aa-contrast"
    - from: "viewmodel-shell/package.json"
      to: "viewmodel-shell/styles/themes/dark-purple.css"
      via: "exports map entry"
      pattern: "./themes/dark-purple.css"
---

<objective>
Execute the Phase 3 D-16 deferred decision: re-base the shipped `default.css` `:root` from dark→light onto the existing `light-purple.css` value set (D-01), capture the prior dark default byte-exact into the new `themes/dark-purple.css` (D-02), and make "serviceable" mechanically falsifiable for the blind pipeline by adding a standalone WCAG-AA contrast CI guard gated in parity.yml (D-07/D-25). Includes the two foundation research items: the `default.css` rule-body color-literal audit (D-16-research) and the WCAG-AA re-verification of the new light default (D-07-research).

Purpose: The new light default must be correct and AA-verified BEFORE the Showcase and demos render against it (downstream Wave 2). This is the dependency root of the entire phase.
Output: Re-based `default.css`, new `themes/dark-purple.css`, new `check-aa-contrast.mjs`, package.json export entry, parity.yml gating step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md
@.planning/phases/03-default-design-system/03-CONTEXT.md

<interfaces>
<!-- Current default.css :root dark block (lines 17-61) — to be re-based to light.
     The light value set (verbatim, from light-purple.css lines 5-24) the executor copies in: -->
NEW light :root color block (D-01, verbatim from themes/light-purple.css):
  --vms-bg:           #f7f7f9;
  --vms-surface:      #ffffff;
  --vms-surface-2:    #f0f0f4;
  --vms-border:       #d8d8e0;
  --vms-accent:       #5a4ad7;
  --vms-accent-glow:  rgba(90, 74, 215, 0.10);
  --vms-accent-dim:   rgba(90, 74, 215, 0.30);
  --vms-text:         #1a1a22;
  --vms-text-muted:   #6c6c80;
  --vms-done-bg:      #ececf2;
  --vms-done-text:    #9090a0;
  --vms-error:        #c2453d;
  --vms-error-glow:   rgba(194, 69, 61, 0.10);
  --vms-warning:      #c89610;
  --vms-priority-high:#d76410;
  --vms-success:      #2da359;
  --vms-info:         #2277dd;
  --vms-color-scheme: light;

PRIOR dark color block (current default.css :root lines 18-34+41) to capture verbatim into dark-purple.css:
  --vms-bg:          #0f0f11;
  --vms-surface:     #18181c;
  --vms-surface-2:   #222228;
  --vms-border:      #2e2e38;
  --vms-accent:      #7c6af7;
  --vms-accent-glow: rgba(124, 106, 247, 0.18);
  --vms-accent-dim:  rgba(124, 106, 247, 0.35);
  --vms-text:        #e8e8f0;
  --vms-text-muted:  #9090a8;
  --vms-done-bg:     #3a3a48;
  --vms-done-text:   #5a5a6e;
  --vms-error:       #e05a5a;
  --vms-error-glow:  rgba(224, 90, 90, 0.12);
  --vms-warning:       #e0a823;
  --vms-priority-high: #e07015;
  --vms-success:       #4dd17a;
  --vms-info:          #4a9eff;
  --vms-color-scheme: dark;

CI-guard pattern to mirror: viewmodel-shell/scripts/check-core-platform-globals.mjs
  (standalone Node ESM, reads a target file, computes/scans, console.log + process.exit(0|1))
package.json scripts already has: "check:core-globals": "node scripts/check-core-platform-globals.mjs"
package.json exports already has: "./themes/light-purple.css": "./styles/themes/light-purple.css" (and 10 siblings, NO dark-purple)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color-literal audit of default.css rule bodies (RESEARCH ITEM — D-16)</name>
  <read_first>
    - viewmodel-shell/styles/default.css (entire file — current state, lines 1-537)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (§Specifics RESEARCH ITEM — color literals; §code_context "Sanctioned default-value change ≠ seam behavior change")
    - .planning/phases/03-default-design-system/03-CONTEXT.md (D-08 literal normalization principle)
  </read_first>
  <action>
    This is research item 1: determine whether the dark→light re-baseline is a PURE `:root` value swap or also needs localized rule-body fixes.

    Scan EVERY rule body in `viewmodel-shell/styles/default.css` BELOW the `:root` block (i.e. line 63 onward, the `body` rule through `.vms-error button`) for any hardcoded color literal — hex (`#xxx`/`#xxxxxx`), `rgb(`/`rgba(`, or named colors — that is NOT a `var(--vms-*)` reference.

    Known pre-existing intentional literals to CLASSIFY (do not assume; verify each against light vs dark):
    - Line ~254: `.vms-checkbox__input:checked + .vms-checkbox__mark::after { border: 2px solid #fff; }` — the checkmark glyph; sits on `--vms-accent` (#5a4ad7 purple) in the new light default → white check on purple still passes contrast → KEEP, but record the reasoning.
    - Line ~279: `.vms-button--primary { color: #fff; }` — white text on `--vms-accent` (#5a4ad7) → KEEP (still AA, verified by Task 4 script).
    - Line ~354: `.vms-list-item--critical { background: rgba(224, 90, 90, 0.04); }` — a DARK-tuned translucent red tint (224,90,90 = the OLD dark `--vms-error`). On the new light surface this is a faint pinkish wash; the new light `--vms-error` is `#c2453d` = rgb(194,69,61).
    - Line ~508-509: `.vms-table__row--warning { background: rgba(212, 130, 26, 0.06); }` and `.vms-table__row--critical { background: rgba(224, 90, 90, 0.06); }` — same: dark-era literal RGB triplets at low alpha.

    For each non-var literal found, decide its disposition and RECORD it in the SUMMARY's research-findings section:
    - If it is a foreground-on-accent literal (`#fff` checkmark / primary-button text): KEEP unchanged (accent is brand-purple in both themes; AA covered by Task 4).
    - If it is a dark-era translucent tint literal that visibly assumes a dark backdrop (`.vms-list-item--critical` bg, `.vms-table__row--warning/--critical` bg): this is accuracy-scoped collateral of the re-baseline. Replace the hardcoded triplet with the matching `--vms-error`/`--vms-warning`/`--vms-priority-high` semantic var at the SAME alpha using a modern relative-color or keep the literal ONLY if it still reads correctly on `#f7f7f9`/`#fff`. Preferred: change `.vms-list-item--critical { background: rgba(224, 90, 90, 0.04); }` → keep the visual intent (faint danger wash) but note explicitly in the SUMMARY which literals were touched and why (still accuracy-scoped, NOT a redesign — do NOT add new rules, do NOT introduce new tokens).
    - This task makes NO `:root` change (Task 2 owns that). It only documents the audit verdict and performs the minimal localized literal fixes if-and-only-if the audit proves a literal visibly assumes dark. If the audit finds every literal is either a var-reference or a still-correct foreground-on-accent literal, the verdict is "pure :root swap, zero rule-body changes" and NO edit is made in this task.

    Output the explicit audit verdict paragraph for the SUMMARY: either "default.css rule bodies reference only --vms-* color vars; D-16 is a pure :root value swap" OR a precise list of every literal touched + why.
  </action>
  <verify>
    <automated>cd viewmodel-shell && node -e "const c=require('fs').readFileSync('styles/default.css','utf8').split(/\r?\n/).slice(62).join('\n'); const m=c.match(/#[0-9a-fA-F]{3,6}|rgba?\(/g)||[]; console.log('non-:root color literals remaining:', m.length, m.join(' '))"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains an explicit audit verdict paragraph (either "pure :root swap" or an itemized list of touched literals with rationale).
    - Any literal touched is a translucent tint that demonstrably assumed dark; foreground-on-accent literals (`#fff` checkmark, primary-button `color:#fff`) are KEPT and that decision is recorded.
    - `git diff viewmodel-shell/styles/default.css` shows EITHER zero changes (pure-swap verdict) OR only translucent-tint background literal changes below line 62 — NO new CSS rules, NO new selectors, NO `:root` change in this task.
    - No new `--vms-*` token introduced anywhere.
  </acceptance_criteria>
  <done>Audit verdict recorded; minimal localized literal fixes applied only if the audit proved a dark-assuming literal; no new rules/tokens; no :root edit in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Re-base default.css :root to light + capture prior dark default into themes/dark-purple.css + package.json export (D-01/D-02/D-03)</name>
  <read_first>
    - viewmodel-shell/styles/default.css (lines 1-61 — the :root block being replaced)
    - viewmodel-shell/styles/themes/light-purple.css (lines 1-24 — the value set being adopted; stays byte-unchanged)
    - viewmodel-shell/styles/themes/dark-blue.css (a sibling theme file — replicate its exact header-comment + :root formatting style for the new dark-purple.css)
    - viewmodel-shell/package.json (lines 51-63 — the exports map; dark-purple entry is missing)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md (§Color — the new shipped default table; D-01/D-02/D-03)
  </read_first>
  <action>
    Three edits, all CSS-value/config only — NO new tokens, NO new rules, NO renamed variables (D-03: every `--vms-*` name must still exist after).

    (A) Re-base `viewmodel-shell/styles/default.css` `:root` (D-01). In the `:root` block (lines 17-61), replace ONLY the COLOR + color-scheme declarations with the light-purple value set verbatim from the `<interfaces>` block above. The exact replacements (keep each variable name, keep declaration order, keep the surrounding comment lines, keep all NON-color declarations — `--vms-radius`, `--vms-radius-sm`, `--vms-font-*`, `--vms-t`, the spacing scale block, the type scale block, `--vms-page-max`, `--vms-card-min` — byte-unchanged):
      `--vms-bg: #0f0f11` → `#f7f7f9`
      `--vms-surface: #18181c` → `#ffffff`
      `--vms-surface-2: #222228` → `#f0f0f4`
      `--vms-border: #2e2e38` → `#d8d8e0`
      `--vms-accent: #7c6af7` → `#5a4ad7`
      `--vms-accent-glow: rgba(124, 106, 247, 0.18)` → `rgba(90, 74, 215, 0.10)`
      `--vms-accent-dim: rgba(124, 106, 247, 0.35)` → `rgba(90, 74, 215, 0.30)`
      `--vms-text: #e8e8f0` → `#1a1a22`
      `--vms-text-muted: #9090a8` → `#6c6c80`
      `--vms-done-bg: #3a3a48` → `#ececf2`
      `--vms-done-text: #5a5a6e` → `#9090a0`
      `--vms-error: #e05a5a` → `#c2453d`
      `--vms-error-glow: rgba(224, 90, 90, 0.12)` → `rgba(194, 69, 61, 0.10)`
      `--vms-warning: #e0a823` → `#c89610`
      `--vms-priority-high: #e07015` → `#d76410`
      `--vms-success: #4dd17a` → `#2da359`
      `--vms-info: #4a9eff` → `#2277dd`
      `--vms-color-scheme: dark` → `light`
    Also update the file's top doc-comment example (lines ~8-10) if it shows a dark `--vms-bg: #fff` placeholder — leave the comment example accurate but do not invent new comment content (accuracy-only).

    (B) Create NEW file `viewmodel-shell/styles/themes/dark-purple.css` (D-02) as a BYTE-EXACT capture of today's `default.css` `:root` dark color block. Use the EXACT same file structure/format as the sibling `dark-blue.css` (header comment + a single `:root { ... }` containing only the color + color-scheme declarations). Body, verbatim from the PRIOR dark block in `<interfaces>` (the values that were in default.css :root BEFORE edit A). Header comment first line: `/* Dark-purple theme — the pre-0.4.0 shipped default, preserved verbatim.` second line `   import "@ashley-shrok/viewmodel-shell/styles.css";` third line `   import "@ashley-shrok/viewmodel-shell/themes/dark-purple.css";` then `*/` then the `:root { ... }`. Match the indentation/alignment convention of dark-blue.css exactly.

    (C) Add the export entry to `viewmodel-shell/package.json`. In the `exports` map, immediately AFTER the `"./themes/dark-teal.css": "./styles/themes/dark-teal.css",` line and BEFORE `"./themes/light-purple.css"`, insert: `"./themes/dark-purple.css": "./styles/themes/dark-purple.css",`. Keep JSON valid (trailing comma correctness — the inserted line ends with a comma; the line before it already ends with a comma).

    Do NOT touch `light-purple.css` or any of the other 10 theme files (D-02/D-03 — they stay byte-identical; editing light-purple.css would be a THEME-05 seam behavior change).
  </action>
  <verify>
    <automated>cd viewmodel-shell && node -e "const fs=require('fs'); const d=fs.readFileSync('styles/default.css','utf8'); const dp=fs.readFileSync('styles/themes/dark-purple.css','utf8'); const lp=fs.readFileSync('styles/themes/light-purple.css','utf8'); const ok=d.includes('--vms-bg:          #f7f7f9')&&d.includes('--vms-accent:      #5a4ad7')&&d.includes('--vms-color-scheme: light')&&dp.includes('#0f0f11')&&dp.includes('#7c6af7')&&dp.includes('--vms-color-scheme: dark')&&JSON.parse(fs.readFileSync('package.json','utf8')).exports['./themes/dark-purple.css']==='./styles/themes/dark-purple.css'; if(!ok){console.error('FAIL'); process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `default.css` `:root` contains `--vms-bg:          #f7f7f9`, `--vms-surface:     #ffffff`, `--vms-accent:      #5a4ad7`, `--vms-color-scheme: light` (light value set adopted).
    - `default.css` `:root` STILL contains every non-color declaration unchanged: `--vms-radius`, `--vms-radius-sm`, `--vms-font-body/head/mono`, `--vms-t`, `--vms-space-2xs..xl`, `--vms-text-xs..2xl`, `--vms-page-max:   1080px`, `--vms-card-min:   16rem` (verify via grep — no spacing/type/font token changed).
    - Every `--vms-*` variable name that existed before still exists in `default.css` :root after (D-03(a): none removed/renamed) — `grep -o "\-\-vms-[a-z0-9-]*" styles/default.css | sort -u` count unchanged vs prior.
    - `viewmodel-shell/styles/themes/dark-purple.css` exists, contains `--vms-bg:          #0f0f11`, `--vms-accent:      #7c6af7`, `--vms-color-scheme: dark`, and matches the header-comment + `:root`-only structure of `dark-blue.css`.
    - `git diff --stat viewmodel-shell/styles/themes/` shows ONLY `dark-purple.css` added (mode A) — the 11 pre-existing theme files including `light-purple.css` are byte-identical (zero modified theme files).
    - `package.json` is valid JSON and `exports["./themes/dark-purple.css"] === "./styles/themes/dark-purple.css"`.
  </acceptance_criteria>
  <done>default.css :root re-based to light; dark-purple.css is a byte-exact capture of the prior dark default; package.json exports it; 11 prior theme files byte-unchanged; every --vms-* name preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WCAG-AA contrast CI guard over the new light default + parity.yml gating (RESEARCH ITEM — D-07/D-25)</name>
  <read_first>
    - viewmodel-shell/scripts/check-core-platform-globals.mjs (the standalone-Node CI-guard pattern to mirror: ESM, read target, compute, console.log, process.exit(0|1))
    - viewmodel-shell/styles/default.css (the re-based :root from Task 2 — the script's input)
    - viewmodel-shell/package.json (lines 27-32 — scripts block; mirror the check:core-globals entry)
    - .github/workflows/parity.yml (the "Enforce core platform-agnosticism" step — the gating pattern to mirror; add a new step beside it, weaken nothing)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md (§Falsifiability contract — the AA proxy row; §Design Decisions Recorded — the minimum AA color-pair list)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (D-07, D-25; §Specifics RESEARCH ITEM — re-confirm WCAG-AA)
  </read_first>
  <behavior>
    - Parses `viewmodel-shell/styles/default.css` `:root` for the hex values of: `--vms-bg`, `--vms-surface`, `--vms-surface-2`, `--vms-text`, `--vms-text-muted`, `--vms-error`, `--vms-success`, `--vms-warning`, `--vms-info`, `--vms-priority-high`.
    - Computes the WCAG 2.x relative-luminance contrast ratio for each required pair (the minimum set from UI-SPEC §Design Decisions Recorded):
      - `--vms-text` on `--vms-bg`, on `--vms-surface`, on `--vms-surface-2` (body text — must be >= 4.5:1, AA normal text).
      - `--vms-text-muted` on `--vms-bg`, on `--vms-surface`, on `--vms-surface-2` (muted/secondary text — must be >= 4.5:1; muted text is still text, not large; if a pair lands between 3.0 and 4.5 the script must FAIL and report the exact ratio so the milestone's "serviceable" claim is not silently violated).
      - `--vms-error`, `--vms-success`, `--vms-warning`, `--vms-info`, `--vms-priority-high` each on `--vms-surface` (semantic colors used as text/accent on the dominant surface — must be >= 3.0:1, AA non-text/large-or-graphical floor; pick the documented threshold and state it in the script header).
    - Exit 0 with a per-pair ratio table to stdout when ALL pairs meet their threshold; exit 1 listing every failing pair with its measured ratio + required threshold otherwise.
    - Pure Node, zero deps, zero jsdom, zero DOM (D-25: this is a static invariant, not DOM behavior).
  </behavior>
  <action>
    This is research item 2: verify-and-CI-enforce WCAG-AA on the new light default (Phase 3 D-17 only verified the dark default; the light-purple values now becoming the default were never AA-audited).

    Create `viewmodel-shell/scripts/check-aa-contrast.mjs` mirroring the structure of `check-core-platform-globals.mjs` exactly: a `#!/usr/bin/env node` ESM script that reads `../styles/default.css`, extracts the `:root` hex values listed in <behavior>, implements sRGB→linear relative luminance + the `(L1+0.05)/(L2+0.05)` contrast formula, asserts each pair against its threshold (body/muted text pairs: 4.5:1; semantic-on-surface pairs: 3.0:1 — state these thresholds in the script header comment with the WCAG 2.x citation, mirroring how core-globals documents its rule). On any failure: `console.error` each failing pair (`<fg> on <bg>: 3.92:1 (need 4.5:1)`) and `process.exit(1)`. On success: print the full ratio table and `process.exit(0)`.

    Add to `viewmodel-shell/package.json` `scripts`, immediately after the `"check:core-globals"` line: `"check:aa-contrast": "node scripts/check-aa-contrast.mjs",` (keep JSON valid).

    Add a new gating step to `.github/workflows/parity.yml` immediately AFTER the existing "Enforce core platform-agnosticism (AGNOSTIC-03)" step, named `Enforce WCAG-AA on shipped default (D-07)`, body:
    ```
          - name: Enforce WCAG-AA on shipped default (D-07)
            run: |
              cd viewmodel-shell
              npm run check:aa-contrast
    ```
    Do NOT modify or weaken any existing parity.yml step (D-25: add steps, weaken nothing). Place the new step so it runs after `npm install`/`npm run build` has happened in the prior step's job (same job, sequential steps — `cd viewmodel-shell` + `npm run check:aa-contrast` needs no build, it only reads a CSS file, so it is self-sufficient).

    RESEARCH OUTPUT (mandatory): run `npm run check:aa-contrast` and record in the SUMMARY the full measured ratio table and the explicit verdict: "the new light default passes WCAG-AA on all required pairs" OR, if any pair fails, STOP and report it as a blocker (the milestone's falsifiable-serviceable claim depends on this — a failing default is a phase blocker, NOT a silent pass; do not adjust :root values to force a pass without escalating, since :root values are locked to the light-purple set by D-01).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npm run check:aa-contrast && node -e "const y=require('fs').readFileSync('../.github/workflows/parity.yml','utf8'); if(!y.includes('check:aa-contrast')){console.error('parity.yml missing AA step');process.exit(1)} console.log('parity.yml gates AA')"</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/scripts/check-aa-contrast.mjs` exists, is standalone Node ESM, has zero imports beyond `node:fs`/`node:url`/`node:path` (no jsdom, no DOM — `grep -L "jsdom\|document\|window" scripts/check-aa-contrast.mjs` returns the file).
    - `npm run check:aa-contrast` exits 0 against the Task-2 re-based default.css and prints a per-pair ratio table.
    - `package.json` scripts contains `"check:aa-contrast": "node scripts/check-aa-contrast.mjs"` and remains valid JSON.
    - `.github/workflows/parity.yml` contains a step named `Enforce WCAG-AA on shipped default (D-07)` running `npm run check:aa-contrast`, placed after the AGNOSTIC-03 step; `git diff .github/workflows/parity.yml` shows ONLY an added step (no existing step removed/weakened).
    - The SUMMARY records the full measured ratio table and the explicit AA verdict (research item 2 output).
  </acceptance_criteria>
  <done>Standalone AA-contrast CI guard created, passes on the new light default, scripted into package.json, gated in parity.yml beside core-globals; AA verdict recorded; no jsdom, no weakened existing steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies CSS values, adds a static Node CI script, and edits a CI workflow + package.json config. No runtime input, no network, no user data, no auth, no parser of untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information disclosure) | package.json / parity.yml / dark-purple.css edits | accept | Realistic threat surface is N/A for CSS-value + CI-config edits. Standard hygiene only: no secrets, tokens, or credentials are introduced in the version/config/CSS changes (verifiable: `git diff` of this plan's files contains only CSS hex values, a Node math script, a package.json export/script line, and a parity.yml step — no secret-shaped strings). |
</threat_model>

<verification>
- `viewmodel-shell/styles/default.css` `:root` is the light-purple value set; every non-color token byte-unchanged; every `--vms-*` name preserved (D-03(a)).
- `themes/dark-purple.css` is a byte-exact capture of the prior dark default; the other 11 theme files byte-identical (D-03(b)); `package.json` exports it.
- `npm run check:aa-contrast` exits 0; parity.yml gates it beside core-globals (D-07/D-25); AA verdict + literal-audit verdict recorded in the SUMMARY (the 2 research items).
</verification>

<success_criteria>
The shipped default is light and AA-verified, the prior dark default is one import away (`themes/dark-purple.css`, byte-exact), the override seam mechanism is intact (every `--vms-*` name present, 11 theme files byte-identical), and "serviceable" is mechanically falsifiable via a CI-gated AA guard. Downstream Wave-2 plans can now render against a correct, verified light default.
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-01-SUMMARY.md` including: the color-literal audit verdict (research item 1), the full WCAG-AA ratio table + verdict (research item 2), and the list of files changed.
</output>
