---
phase: 05-canonical-examples-0-4-0-release-closeout
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - demo/Showcase/frontend/src/main.ts
autonomous: true
requirements: [EXAMPLES-01]
must_haves:
  truths:
    - "A top-level nav switches between the component gallery and Dashboard / Form-heavy / List-detail archetype views"
    - "Dashboard renders a card-grid (cards preset + section variant:card) with stat-bar, small table, primary CTA 'New report'"
    - "Form-heavy renders a realistic multi-section form with submit CTA 'Place order'"
    - "List/detail renders a split preset: list of rows + a detail card; collapses on narrow with zero app breakpoints"
    - "Archetype views render the fixed light-purple (new default) theme; the switcher is scoped to the gallery view only"
    - "Every archetype view uses only .vms-* nodes; no raw HTML; no per-view <style>"
  artifacts:
    - path: "demo/Showcase/frontend/src/main.ts"
      provides: "Navigable canonical reference set (gallery + 3 archetypes), switcher remapped + scoped"
      contains: "New report"
  key_links:
    - from: "demo/Showcase/frontend/src/main.ts"
      to: "the top-level archetype nav"
      via: "tabs node switching the rendered view"
      pattern: "Dashboard|List.?detail|Form.?heavy"
    - from: "demo/Showcase/frontend/src/main.ts themeFiles map"
      to: "themes/dark-purple.css"
      via: "real dark-purple entry replacing the implicit empty slot"
      pattern: "dark-purple"
---

<objective>
Turn the Showcase into the canonical few-shot agent surface (EXAMPLES-01): keep the existing kitchen-sink component gallery as one view (preserves the every-node few-shot value) and add Dashboard / Form-heavy / List-detail sibling archetype views, switched by a top-level `tabs` nav (D-09). Apply the LOCKED archetype→preset→Bootstrap mapping (D-10/D-13). Remap the theme switcher's implicit empty slot to `light-purple` and add a real `dark-purple` entry pointing at the new `themes/dark-purple.css`; boot in light-purple; scope the switcher to the gallery view only (D-06/D-14).

Purpose: The canonical set is the highest-leverage quality lever — it teaches the blind agent WHEN to reach for `cards`/`split`/`stack`, benchmarked against named Bootstrap pages. Depends on Plan 01 (the new light default must be correct + AA-verified before the archetypes render against it).
Output: A rebuilt `demo/Showcase/frontend/src/main.ts` with the navigable reference set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md
@.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-01-SUMMARY.md
@demo/Showcase/frontend/src/main.ts

<interfaces>
<!-- The shipped wire node shapes the archetypes compose (ALL exist in Phases 3-4 — add NO new node/field).
     page/section accept optional `layout?: "stack" | "split" | "cards"` and section accepts `variant?: "card"`,
     page accepts `density?: "comfortable" | "compact"`. The Showcase already uses these node types: -->
page:       { type:"page", title, layout?, density?, children:[] }
section:    { type:"section", heading?, variant?:"card", layout?, children:[] }
stat-bar:   { type:"stat-bar", stats:[{label,value}] }
table:      { type:"table", columns:[...], rows:[...] }   // see existing Showcase table section
list:       { type:"list", children:[ {type:"list-item", id, variant?, children:[]} ] }
form:       { type:"form", submitAction:{name}, submitLabel, children:[ {type:"field",...} ] }
button:     { type:"button", label, action:{name}, variant?:"primary"|"secondary"|"danger" }
text:       { type:"text", value, style?:"heading"|"subheading"|"body"|"muted"|... }
tabs:       { type:"tabs", selected, action:{name}, tabs:[{value,label}] }
modal:      { type:"modal", ... }  // existing Showcase modal pattern — archetypes reuse, do not re-author

Existing Showcase structure (main.ts): single buildVm(): ViewNode returning one `page` whose children are
the Theme switcher section + gallery sections; State has selectedTab/mode/accent; handle(ev) switches on
action names incl. "theme:mode"/"theme:accent" (calls applyTheme()), "tab:set"; render via adapter.render(buildVm(), handle).
themeFiles map currently: "dark-purple": "" (implicit empty slot) + 11 ?inline imports; NO light-purple?inline default boot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the top-level archetype nav + Dashboard, Form-heavy, List/detail archetype views (D-09/D-10/D-13/D-12)</name>
  <read_first>
    - demo/Showcase/frontend/src/main.ts (entire file 1-324 — current State, buildVm, handle, render wiring)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-UI-SPEC.md (§Archetype Composition Contract — the LOCKED preset/Bootstrap mapping table; §Copywriting Contract — exact CTA + empty/error/destructive copy; §Falsifiability contract)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (D-09 augment-navigable; D-10 fixed teaching mapping; D-13 locked Bootstrap mapping; D-12 falsifiability; D-14 switcher scope)
    - viewmodel-shell/styles/default.css (the shipped .vms-page--cards / .vms-page--split / .vms-section--card rules — confirm class behavior the archetypes rely on; do NOT edit this file)
  </read_first>
  <action>
    Restructure `demo/Showcase/frontend/src/main.ts` into a navigable canonical reference set. Add a `view` field to `State` (type `"components" | "dashboard" | "form" | "list-detail"`, default `"components"`). Add a top-level `tabs` nav as the FIRST child of the returned `page` (action name `view:set`, selected = `state.view`, tabs: `{value:"components",label:"Components"}`, `{value:"dashboard",label:"Dashboard"}`, `{value:"form",label:"Form"}`, `{value:"list-detail",label:"List / detail"}`). In `buildVm()`, branch on `state.view`: render the EXISTING gallery sections when `view==="components"` (move the current section children into a `componentsView()` helper unchanged — preserve every existing gallery section verbatim, including the Theme switcher section which stays ONLY in the components view per D-14); render one of three new archetype builders otherwise. Add the `view:set` case to `handle()` (set `state.view`, re-render).

    Build the three archetype views using ONLY `.vms-*` wire nodes (no raw HTML, no `<style>`), per the LOCKED mapping (D-10/D-13 — not discretionary):

    DASHBOARD view → `cards` preset, benchmark Bootstrap **"Dashboard"**: a `page` (or a wrapping `section`) carrying `layout:"cards"` whose children are 3-4 `section variant:"card"` stat tiles (each: a `text` subheading label + a large `stat-bar` or `text` value, realistic dashboard metrics — e.g. "Revenue", "Active users", "Conversion", "Open issues" with concrete numbers), PLUS a `stat-bar` summary row, PLUS a small `table` (≥3 columns, ≥4 rows of realistic recent-activity data), PLUS a primary CTA `button` labelled exactly `New report` (variant `primary`). The `cards` preset auto-fits the tiles from `--vms-card-min`.

    FORM-HEAVY view → `stack` preset (the default vertical flow — the safest few-shot exemplar; matches Bootstrap "Checkout"'s single-column form), benchmark Bootstrap **"Checkout"**: a realistic multi-section checkout-style form — group fields into 2-3 `section`s (e.g. "Contact", "Shipping address", "Payment") each containing a `form` (or one `form` with sectioned fieldsets via sections) with concrete `field` nodes (text/email/select/number inputType as appropriate), ending with a primary submit `button` labelled exactly `Place order`. (Per D-10 the executor MAY instead use `split` with a static help/summary aside; default to `stack` per UI-SPEC §Design Decisions Recorded — if `split` is chosen the aside MUST be a static help/summary panel that collapses on narrow via the preset, zero app breakpoints.)

    LIST/DETAIL view → `split` preset on the page, benchmark Bootstrap **"Album"** (list half; the detail pane is our own composition): a `page`/`section` with `layout:"split"` and exactly two children — LEFT a `list` of ≥5 `list-item`s (realistic catalog rows: title + meta `text` + a `View details` `button` per row), RIGHT a `section variant:"card"` detail pane showing the selected item's fields + a detail action `button` labelled exactly `Add to cart`. Track selection in `State` (e.g. `selectedItemId`, default the first item) and a `list-detail:select` action in `handle()`. The `split` preset collapses to stacked on narrow with ZERO app-specified breakpoints (preset-driven).

    Use the exact copy from UI-SPEC §Copywriting Contract: empty-state heading `Nothing here yet` via `text style:"subheading"`, empty-state body `No items to show. Use the form above to add the first one.` via `text style:"muted"` where an empty state is plausible (e.g. an empty filter result); the destructive-confirm `Delete forever` modal pattern ONLY if an archetype needs a destructive action — reuse the EXISTING Showcase modal pattern, do not re-author it.

    HARD CONSTRAINTS: add NO new wire field, NO new node type, NO new CSS, NO new design token, NO raw HTML element, NO per-view `<style>`. Compose strictly from the shipped node set + shipped `layout`/`variant`/`density` fields (D-16 phase-nature). The preset→archetype mapping is LOCKED (Dashboard=cards, Form-heavy=stack, List/detail=split) — it is NOT Claude's discretion; only the archetype CONTENT within that mapping is discretionary.
  </action>
  <verify>
    <automated>cd demo/Showcase/frontend && npx tsc --noEmit -p tsconfig.json 2>/dev/null; node -e "const s=require('fs').readFileSync('src/main.ts','utf8'); const ok=s.includes('New report')&&s.includes('Place order')&&s.includes('Add to cart')&&/layout:\s*[\"']cards[\"']/.test(s)&&/layout:\s*[\"']split[\"']/.test(s)&&/view:set/.test(s)&&!/<style/.test(s)&&!/document\.createElement\(\s*[\"']div[\"']/.test(s); if(!ok){console.error('FAIL archetype/mapping/style check');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `main.ts` `State` has a `view` field defaulting to the components/gallery view; a top-level `tabs` nav (action `view:set`) is the first child of the page; `handle()` has a `view:set` case.
    - The existing gallery sections are preserved verbatim in a components view (every prior section still present — `grep` for "Text styles", "Stat bar", "Buttons", "Form inputs", "Copy button" still all present).
    - Dashboard view uses `layout:"cards"` + `section variant:"card"` tiles + a `stat-bar` + a `table` + a `button` labelled exactly `New report` (variant primary).
    - Form-heavy view uses `stack` (default) with grouped `section`s + a submit `button` labelled exactly `Place order`.
    - List/detail view uses `layout:"split"` with a `list` (≥5 items, each with a `View details` button) + a `section variant:"card"` detail pane + a detail `button` labelled exactly `Add to cart`; selection state + a `list-detail:select` action exist.
    - `grep -c "<style" demo/Showcase/frontend/src/main.ts` == 0 AND no raw-HTML node construction (`.vms-*` nodes only — no `document.createElement` of content elements, no innerHTML of markup).
    - `npx tsc --noEmit` reports no new type errors introduced by the archetype code.
  </acceptance_criteria>
  <done>Showcase is a navigable canonical set: gallery + 3 archetypes on the LOCKED preset/Bootstrap mapping, .vms-*-only, zero per-view style, exact CTAs.</done>
</task>

<task type="auto">
  <name>Task 2: Remap + scope the theme switcher (D-06/D-14)</name>
  <read_first>
    - demo/Showcase/frontend/src/main.ts (the themeFiles map ~lines 50-63, applyTheme() ~65-68, the Theme switcher section ~99-113, boot state ~33-42)
    - viewmodel-shell/styles/themes/dark-purple.css (the NEW file from Plan 01 — confirm it exists and the import path)
    - .planning/phases/05-canonical-examples-0-4-0-release-closeout/05-CONTEXT.md (D-06 implicit slot → light-purple + real dark-purple entry; D-14 switcher scoped to gallery, archetypes fixed light-purple)
  </read_first>
  <action>
    (A) D-06: In `main.ts`, add a `?inline` import for the new dark-purple theme: `import darkPurpleCss from "@ashley-shrok/viewmodel-shell/themes/dark-purple.css?inline";` (alongside the existing 11 `?inline` imports). In the `themeFiles` map, change the implicit-default entry: `"dark-purple": ""` → `"dark-purple": darkPurpleCss` (it is no longer the implicit empty default — the shipped default is now light-purple per Plan 01 D-01). Add/confirm `"light-purple": lightPurpleCss` exists in the map (it already does). Change the boot State so the Showcase opens in the new light default: set `mode: "light"` and `accent: "purple"` in the initial `state` (so `applyTheme()`'s `${mode}-${accent}` key = `light-purple` on first render — the canonical set benchmarks light-on-Bootstrap-light, D-06). Since the shipped `default.css` is now light-purple (Plan 01), `applyTheme()` applying the `light-purple` override on top is a harmless no-op equivalent — acceptable; do NOT special-case it.

    (B) D-14: Scope the runtime theme switcher to the gallery view ONLY. The Theme switcher `section` (mode tabs + accent tabs + the explanatory muted text) must render ONLY when `state.view === "components"`. The three archetype views render with NO theme switcher and on the fixed `light-purple` (new default) theme — i.e. when entering an archetype view, the switcher is absent and the theme stays the fixed light-purple default (do NOT reset the user's gallery theme selection persistently, but archetype views must visually present the fixed light-purple default — simplest correct approach: keep the switcher section gated to the components view and ensure archetype views are authored/benchmarked against the light-purple default; if the user changed the gallery theme then navigated to an archetype, that is acceptable as the switcher is a gallery-scoped reference per D-14 — the LOCKED requirement is structural: no switcher control on archetype views).
  </action>
  <verify>
    <automated>cd demo/Showcase/frontend && node -e "const s=require('fs').readFileSync('src/main.ts','utf8'); const ok=/dark-purple\.css\?inline/.test(s)&&/[\"']dark-purple[\"']\s*:\s*darkPurpleCss/.test(s)&&!/[\"']dark-purple[\"']\s*:\s*[\"'][\"']/.test(s)&&/mode:\s*[\"']light[\"']/.test(s)&&/accent:\s*[\"']purple[\"']/.test(s)&&/state\.view\s*===\s*[\"']components[\"']/.test(s); if(!ok){console.error('FAIL switcher remap/scope');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `main.ts` imports `darkPurpleCss` from `@ashley-shrok/viewmodel-shell/themes/dark-purple.css?inline`.
    - `themeFiles` map has `"dark-purple": darkPurpleCss` (a real entry) and NO `"dark-purple": ""` empty-string slot remains.
    - Boot `state` is `mode: "light"`, `accent: "purple"` (Showcase opens in the new light default).
    - The Theme switcher section renders only under `state.view === "components"` (gated condition present in buildVm/componentsView); archetype views have no switcher control.
    - `npx tsc --noEmit` passes; no other theme map entry changed (the 11 existing `?inline` imports + their map entries are unchanged besides the dark-purple slot).
  </acceptance_criteria>
  <done>Switcher remapped (real dark-purple entry, light-purple boot) and scoped to the gallery view only; archetype views present the fixed light default with no switcher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits a single client-only demo TypeScript file (`demo/Showcase/frontend/src/main.ts`). The Showcase is client-only with no backend (D-11), composes static demo content from shipped wire nodes, takes no untrusted input, touches no secrets/auth/network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | (N/A) | demo/Showcase/frontend/src/main.ts | accept | Realistic threat surface is N/A — client-only demo scaffolding composing shipped UI nodes, no input/secret/network surface. Hygiene only: no secrets or tokens introduced in the demo content (verifiable: `git diff` is demo view code + theme-map entries, no secret-shaped strings). |
</threat_model>

<verification>
- Top-level nav switches gallery ↔ Dashboard / Form-heavy / List-detail; LOCKED preset/Bootstrap mapping honored (D-10/D-13).
- Exact CTAs present: `New report`, `Place order`, `Add to cart`; UI-SPEC empty/error/destructive copy used where applicable.
- `.vms-*`-only, zero per-view `<style>`, no raw HTML (D-12 falsifiable proxies — enforced by Plan 06's repo-scan guard).
- Switcher remapped (real `dark-purple` entry, `light-purple` boot) and scoped to the gallery view only (D-06/D-14).
- No new wire field / node type / CSS / token introduced (D-16 phase-nature).
</verification>

<success_criteria>
The Showcase is the canonical few-shot agent surface EXAMPLES-01 requires: a navigable reference set teaching WHEN to use `cards`/`split`/`stack`, benchmarked against Bootstrap "Dashboard"/"Checkout"/"Album", on the fixed new light default, with the swappable-themes switcher preserved as the gallery reference. AGENTS.md docs (Plan 04) can now point to this as the single-source-of-truth worked example (D-23).
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-examples-0-4-0-release-closeout/05-02-SUMMARY.md` recording the final archetype composition, the locked preset/Bootstrap mapping confirmation, and the Form-heavy preset choice (stack vs split-with-aside).
</output>
