---
phase: 30-chat-composer-primitive-chatcomposernode-route-b-composite
plan: 11
type: execute
wave: 9
depends_on: ["30-06", "30-07", "30-08", "30-09", "30-10"]
files_modified: []
autonomous: true
requirements: [CHAT-18]

must_haves:
  truths:
    - "Full green-tree gate per AGENTS.md 🚨 NEVER PUBLISH OR PUSH ANYTHING BROKEN — the EXACT set CI runs. The gate below matches vms-map.md §'The local green-tree gate' verbatim. Machine-verifiable; NO theatrical human checkpoint per banked 2026-07-31 Ashley directive."
    - "TS-side gate (in `viewmodel-shell/`): `npm run build`, `npm run check:test-types`, `npm run check:core-globals`, `npm run check:aa-contrast`, `npm run check:no-demo-style`, `npm run check:demo-types`, `npx vitest run` — ALL exit 0. `check:test-types` is CI-only-catches per banked lesson; do NOT skip. `check:aa-contrast` is load-bearing for CHAT-18 (send-button accent-bg × icon-fg pair; stop-icon during streaming; disabled state; chip X-remove — all NEW fg/bg pairs from Plans 30-03/04/05)."
    - "PATH-export gate: `export PATH=\"$HOME/.dotnet:$PATH\"` before any .NET or parity command — the parity harness spawns dotnet as a child and dies with a bare `spawn` trace if it's not on PATH."
    - ".NET-side gate: `dotnet test viewmodel-shell-dotnet/Tests` (framework's OWN test project — historically forgotten; 3.0.0-3.3.0 uncompilable stretch is why it's now in the gate) — exit 0."
    - "Every demo .NET tests project runs: `for p in $(find demo -name '*.Tests.csproj'); do dotnet test \"$p\"; done` — every project exits 0. IF any demo test project shows a PRE-EXISTING failure that is not related to Phase 30's work: HALT per AGENTS.md working-agreement rule ('It was already failing on main is NOT an exception') and report the failure to the operator; do NOT close the phase until fixed OR explicitly waived."
    - "Markdown companion compile: `dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj` — exit 0."
    - "Parity: `bun run parity/run.ts` — exit 0. Plan 30-07 landed the fixture; this run is the last cross-backend byte-check before phase closes."
    - "Additionally: `npm run check:theme-byte-identity` + `npm run check:theme-function` — theme consistency gates; historically added; run for defense-in-depth. `npm run check:no-demo-style` — the demo-CSS-hygiene gate; Plans 30-03/04/05 added CSS to the shipped stylesheet, this catches drift."
    - "AA-contrast gate (CHAT-18 explicit): `npm run check:aa-contrast` is the machine gate. If any new fg/bg pair from this phase fails contrast in any theme, the plan HALTS + reports the failing pair(s) + theme(s) to the operator. Common remediation: tone-token adjustment on the failing theme's `.css` file, re-run gate — OR a rare adjustment to the composer's shipped color values (e.g. force higher accent-vs-bg contrast on the send-button). Do NOT approve a contrast failure silently."
    - "SUMMARY records the exact exit code + last-few-lines of output for EACH gate command, so any future audit can point to concrete evidence green-tree was verified."
    - "IF ANY gate fails: HALT. Do NOT create a SUMMARY marking Phase 30 green. The failing gate's output is recorded in a FAILURE SUMMARY; the executor fixes the failure OR reports it to the operator; the plan reopens only after the failure is genuinely green."
    - "IF gate passes: SUMMARY marks PHASE 30 GREEN. ROADMAP.md's Phase 30 checkbox flips to `[x]`. NO release (per CONTEXT §Non-negotiables item 9); no version bump; no publish; no tag. `## Unreleased` CHANGELOG entry from Plan 30-08 accumulates; a SEPARATE downstream closeout phase promotes it + runs the release ritual (matches Phase 26 shape)."
  artifacts:
    - path: ".planning/phases/30-chat-composer-primitive-chatcomposernode-route-b-composite/30-11-SUMMARY.md"
      provides: "Per-gate exit code + tail output evidence + timing + final GREEN or FAILURE verdict."
      contains: "GREEN"
  key_links:
    - from: "AGENTS.md 🚨 gate + vms-map.md §The local green-tree gate"
      to: "This plan's task list"
      via: "The command set is byte-copied from vms-map.md so a future gate-adds-to-CI change gets mirrored here"
      pattern: "check:test-types|check:core-globals|check:aa-contrast"
---

<objective>
Run the FULL green-tree gate — the exact machine-verifiable set CI runs — as the final phase-closing task. This is the load-bearing prerequisite for phase closure per AGENTS.md 🚨 NEVER PUBLISH OR PUSH ANYTHING BROKEN rule. Autonomous; no theatrical human checkpoint per banked 2026-07-31 Ashley directive (green/red is objective — no reason to ask).

Purpose: The gate catches classes of defect that unit tests + adopter validation both miss: (a) a plan added a test but forgot to include it in tsconfig.test.json's coverage (check:test-types); (b) a plan added a CSS rule that fails AA contrast (check:aa-contrast — LOAD-BEARING FOR CHAT-18); (c) a plan added a file that a demo tsconfig doesn't pick up (check:demo-types); (d) a pre-existing framework .NET test failure that everyone assumed was someone else's problem (the 3.0.0-3.3.0 uncompilable stretch). Running the ENTIRE gate before phase-closure means the phase ships green.

Output: A SUMMARY marking every gate GREEN + phase closed (ROADMAP checkbox flipped). If ANY gate fails, HALT with a FAILURE SUMMARY naming the failing gate — the downstream closeout phase is blocked until fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
</context>

<interfaces>
The gate command set is copied verbatim from `~/.claude/identities/vicky/vms-map.md §'The local green-tree gate'` and `AGENTS.md §'NEVER PUBLISH OR PUSH ANYTHING BROKEN'`. If either doc updates the command set, THIS plan's task must be updated in the same commit — drift is silently unsafe.

The gate commands (batch A — TS-side, run from `viewmodel-shell/`):
```
npm run build && npm run check:test-types && npm run check:core-globals \
  && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types \
  && npm run check:theme-byte-identity && npm run check:theme-function \
  && npx vitest run
```

Batch B — .NET + parity (repo root, PATH exported):
```
export PATH="$HOME/.dotnet:$PATH"
dotnet test viewmodel-shell-dotnet/Tests
for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p"; done
dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj
bun run parity/run.ts
bash parity/check-companion-binary-compat.sh   # plan-checker L-1 (2026-08-02) — CI runs this; keep the local gate byte-parallel
```

Pre-existing failure handling (from AGENTS.md working agreement):
> `It was already failing on main / it's unrelated to my change / it's just a demo test` are **NOT** exceptions — a red suite is a red suite. If a pre-existing failure surfaces mid-phase-close, STOP, surface it, and fix or explicitly waive before proceeding.

The plan MUST NOT insert a `checkpoint:human-verify` for "confirm the gate is green" per banked Ashley 2026-07-31 directive (theatrical checkpoint class). The gate's exit codes are objective; if all green, autonomous proceed to phase-close; if any red, halt.

The AA-contrast gate specifically checks numeric contrast ratios in the shipped tokens across every theme. Plans 30-03/04/05 added shipped CSS with new fg/bg pairs (send-button, chip strip, error banner, drag-drop affordance). Ashley's Plan 30-09 sign-off caught SUBJECTIVE readability issues; this gate catches OBJECTIVE numeric contrast failures — both dimensions matter.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full TS-side green-tree gate (viewmodel-shell/)</name>
  <files></files>
  <read_first>
    - viewmodel-shell/package.json (verify every `check:*` script exists — a missing script means the interface has drifted; halt + report to operator)
  </read_first>
  <action>
    From `viewmodel-shell/`, run the TS-side gate command-by-command (capture each exit code + tail):

    ```bash
    cd viewmodel-shell
    npm run build 2>&1 | tail -5              && echo "build OK"          || (echo "build FAIL"; exit 1)
    npm run check:test-types 2>&1 | tail -5   && echo "test-types OK"     || (echo "test-types FAIL"; exit 1)
    npm run check:core-globals 2>&1 | tail -5 && echo "core-globals OK"   || (echo "core-globals FAIL"; exit 1)
    npm run check:aa-contrast 2>&1 | tail -20 && echo "aa-contrast OK"    || (echo "aa-contrast FAIL"; exit 1)
    npm run check:no-demo-style 2>&1 | tail -5 && echo "no-demo-style OK" || (echo "no-demo-style FAIL"; exit 1)
    npm run check:demo-types 2>&1 | tail -5   && echo "demo-types OK"     || (echo "demo-types FAIL"; exit 1)
    npm run check:theme-byte-identity 2>&1 | tail -5 && echo "theme-byte OK" || (echo "theme-byte FAIL"; exit 1)
    npm run check:theme-function 2>&1 | tail -5 && echo "theme-fn OK"     || (echo "theme-fn FAIL"; exit 1)
    npx vitest run 2>&1 | tail -20            && echo "vitest OK"         || (echo "vitest FAIL"; exit 1)
    ```

    Record every command's exit code + last 5-20 lines in SUMMARY.

    IF `check:aa-contrast` FAILS: capture the FULL error output (which pairs/themes failed); halt + surface to operator. **Remediation preference order (plan-checker M-3, 2026-08-02) — try LEAST-BLAST-RADIUS first:** (1) ship a **per-composer color token** (e.g. `--vms-chat-composer-send-fg`, `--vms-chat-composer-send-bg`) in default.css + per-theme overrides — isolates the composer from other accent-bearing components (Button, Alert, etc.) that shipped fine; (2) adjust the composer's shipped color values directly (force higher accent-vs-bg contrast); (3) adjust global tone tokens ONLY if the failing pair is genuinely a global palette issue AND every other accent-carrying component still passes AA after the adjustment (re-run `check:aa-contrast` after each tweak — collateral is silent).

    IF ANY script is MISSING from package.json: halt + surface to operator (interface drift — one of the check scripts got renamed / removed; adopt the new name if reasonable, else escalate).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npm run build && npm run check:test-types && npm run check:core-globals && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types && npm run check:theme-byte-identity && npm run check:theme-function && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - ALL 9 TS-side commands exit 0.
    - AA-contrast output shows 0 failing pairs across all themes.
    - vitest output shows the Plan 30-06 tests PASSING + no regressions in existing tests.
  </acceptance_criteria>
  <done>
    TS-side of the green-tree gate is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the .NET-side + parity green-tree gate</name>
  <files></files>
  <read_first>
    - AGENTS.md §Working agreement (verify the exact command set + the pre-existing-failure rule)
  </read_first>
  <action>
    Batch B (repo root):
    ```bash
    export PATH="$HOME/.dotnet:$PATH"
    dotnet test viewmodel-shell-dotnet/Tests 2>&1 | tail -20 && echo "framework .NET tests OK" || (echo "framework .NET tests FAIL"; exit 1)

    for p in $(find demo -name '*.Tests.csproj'); do
      echo "=== Running $p ==="
      dotnet test "$p" 2>&1 | tail -10 || (echo "FAIL: $p"; exit 1)
    done

    dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj 2>&1 | tail -5 && echo "Markdown OK" || (echo "Markdown FAIL"; exit 1)

    bun run parity/run.ts 2>&1 | tail -30 && echo "parity OK" || (echo "parity FAIL"; exit 1)
    ```

    Record every command's exit code + last 5-30 lines in SUMMARY.

    IF ANY demo .NET Tests project fails with a pre-existing (not-Phase-30-related) failure: HALT per AGENTS.md working agreement. Report to operator with the exact failure output; wait for either (a) an operator-authored fix / waiver, or (b) direction to add a follow-up plan to fix.

    IF parity FAILS: capture the diff output; identify which fixture (probably `chat-composer` from Plan 30-07 if the failure is Phase-30-related); halt + surface. Parity failures on the `chat-composer` fixture likely indicate a byte-parallel drift between the TS + .NET renders — investigate the wire JSON output for the mismatched fields.
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" && dotnet test viewmodel-shell-dotnet/Tests 2>&1 | tail -5 && (for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" 2>&1 | tail -3; done) && dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj 2>&1 | tail -3 && bun run parity/run.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test viewmodel-shell-dotnet/Tests` exits 0.
    - EVERY demo `.Tests.csproj` exits 0.
    - Markdown companion builds exit 0.
    - `bun run parity/run.ts` exits 0 (all fixtures, all backends).
  </acceptance_criteria>
  <done>
    .NET-side + parity are green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 30 GREEN + flip ROADMAP checkbox + finalize SUMMARY</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (find the Phase 30 entry + the `Plans: 0/N` checkbox tracker)
    - .planning/phases/30-*/30-01-SUMMARY.md .. 30-10-SUMMARY.md (verify each plan's SUMMARY exists + is marked complete)
  </read_first>
  <action>
    (a) Verify every prior plan's SUMMARY exists (30-01 through 30-10). If any is missing, halt — the phase is not actually complete.

    (b) Update `.planning/ROADMAP.md` Phase 30 entry: flip the `Plans: 0/N` tracker to `Plans: 11/11 plans executed`. Flip every `[ ]` checkbox for CHAT-01..20 requirements to `[x]` (verify against the Plans they were assigned to).

    (c) Finalize this plan's SUMMARY as PHASE 30 GREEN:
    ```markdown
    # Phase 30 — Chat Composer Primitive: GREEN

    All 11 plans executed. Full green-tree gate passed at <timestamp>.

    ## Gate exit codes

    | Command | Exit | Tail |
    |---|---|---|
    | npm run build | 0 | ... |
    | npm run check:test-types | 0 | ... |
    | (etc. — one row per gate command) | | |

    ## Requirements closed

    CHAT-01..20 — all present in prior plan SUMMARYs.

    ## Release status

    NOT SHIPPED. `## Unreleased` in CHANGELOG.md accumulates Plan 30-08's entry. A SEPARATE downstream closeout phase (v9.1.0 aligned npm + NuGet publish + tag + advance-main + announce) promotes + ships. This matches Phase 26's shape.

    ## Angel-adopter disposition

    (From Plan 30-10 SUMMARY: CLOSED / HALTED / CONDITIONAL — quote verbatim.)
    ```

    (d) Do NOT run any release commands. Do NOT bump versions. Do NOT publish. Do NOT tag. Do NOT push. Per CONTEXT §Non-negotiables item 9 + AGENTS.md §Working agreement §Git is operator-driven.
  </action>
  <verify>
    <automated>grep -c 'Plans:.*11/11\|Plans:.*11 plans executed' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All prior plan SUMMARYs exist.
    - ROADMAP Phase 30 tracker updated to reflect completion.
    - SUMMARY marks PHASE 30 GREEN with gate evidence.
    - No release ritual commands run.
  </acceptance_criteria>
  <done>
    Phase 30 closes green. Downstream closeout phase (separate plan set) can promote `## Unreleased` → v9.1.0 + run the release ritual when the operator is ready.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate command outputs ↔ SUMMARY evidence | Exit codes + tail output recorded verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11-01 | Repudiation | Gate silently passes on a broken tree | mitigate | Every gate command's exit code recorded + verified. HALT on ANY red. Pre-existing failures NOT waived without operator's explicit approval. |
| T-30-11-02 | Tampering | Someone edits SUMMARY to claim GREEN without gates running | mitigate | Gate commands are re-runnable at any time; a future audit re-runs them against the same commit and detects drift. |
</threat_model>

<verification>
Tasks 1-2 machine-verified via the gate exit codes. Task 3 verifies via ROADMAP checkbox + SUMMARY presence.
</verification>

<success_criteria>
- All gate commands exit 0.
- Phase 30 marked GREEN in SUMMARY + ROADMAP.
- All 20 CHAT-XX requirements closed.
- NO release ritual commands run (per CONTEXT).
</success_criteria>

<output>
Create `.planning/phases/30-chat-composer-primitive-chatcomposernode-route-b-composite/30-11-SUMMARY.md`. Record: gate exit code + tail per command, CHAT-XX requirement closure table, Angel-adopter disposition summary (quoted from 30-10-SUMMARY), release status ("NOT SHIPPED — pending closeout phase").
</output>

**Commit intent:** `chore(30-11): Phase 30 green-tree gate passed; phase closes without release (CHAT-18)`
