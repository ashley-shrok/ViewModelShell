---
phase: 30-chat-composer-primitive-chatcomposernode-route-b-composite
plan: 07
type: execute
wave: 6
depends_on: ["30-02", "30-03", "30-04", "30-05"]
files_modified:
  - parity/fixtures/chat-composer.json
  - parity/backends.json
  - demo/FeatureProbe/AspNetCore/ProbeController.cs
  - demo/FeatureProbe-bun/server.ts
autonomous: true
requirements: [CHAT-15]

must_haves:
  truths:
    - "New parity fixture `parity/fixtures/chat-composer.json` covers 5 distinct branches of the ChatComposerNode wire, each with its OWN `expectBodyContains` substring assertion per AGENTS.md gotcha #9 class-3 lesson (branches the fixture never runs are invisible to a diff — every step exercising a documented branch MUST have a distinctive substring assertion, per Vicky's banked lesson from v6.0.0)."
    - "The 5 branches, each with a distinct expectBodyContains: (a) IDLE — `\"data-composer-status=\\\"idle\\\"\"` present; sendAction wired. (b) STREAMING — `\"data-composer-status=\\\"streaming\\\"\"` present; stopAction wired; verify the `square` icon path emitted (per Plan 30-01's icon addition). (c) ATTACH-CLICKED — after simulating an attach-action dispatch (state carries attachedFiles count > 0 in some app-modelable form; the fixture's server serializes it via a state slot the FeatureProbeState grew), the response body contains `\"vms-chat-composer__chip\"`. (d) DROPSCOPE-GLOBAL — the emitted composer node carries `data-drop-scope=\"global\"` (or the JSON wire carries `\"dropScope\":\"global\"`); assert on the JSON `\"dropScope\":\"global\"` substring, which is more diff-robust than the DOM attribute (the wire is what parity diffs). (e) SUBMITMODE-CTRLENTER — response JSON body carries `\"submitMode\":\"ctrl-enter\"`."
    - "Both bun + .NET FeatureProbe backends grow a minimal `ChatComposerNode` render that exercises each branch, keyed off state fields that the fixture's action steps mutate via `stateMutations`. The FeatureProbe pattern — grep the existing `parity/fixtures/feature-probe.json` — is: buildVm renders EVERY new feature as static view-shape by default (bare closed-enum sweeps), so a byte-diff across backends covers additions with no dedicated action step; per-branch action steps only exist when a branch requires DIFFERENT state (e.g. attachedFiles.length > 0 requires a state field to be set)."
    - "Fixture is registered in `parity/backends.json` under the two FeatureProbe backends' `fixtures` array (`bun-feature-probe` + `dotnet-feature-probe` + `node-feature-probe` per grep — verify all three FeatureProbe backend names). Fixture reference addition is ONE line per backend."
    - "`bun run parity/run.ts` runs the fixture across all FeatureProbe backends and DIFFS byte-for-byte. All backends must emit the identical wire JSON. If ANY diff surfaces, the plan halts + investigates."
    - "Every expectBodyContains assertion for each of the 5 branches is verified independently (not via diff — via the per-response invariant machinery in parity/run.ts described in AGENTS.md gotcha #9). If a substring is absent, the harness fails LOUDLY — this is the class-3 tripwire."
    - "The fixture also asserts `findNulls: false` per response — the null-omission contract from gotcha #8 (nullable optionals must be ABSENT from the wire, not present as `null`). This catches the class-2 defect where a shared normalization would filter nulls before diffing and hide the violation."
    - "`$comment` field at the top of the fixture explains: what each branch tests, why the substring was chosen for each expectBodyContains, and cites AGENTS.md gotcha #9 class-3 lesson explicitly (following the shipped `helpdesk-seeded.json` documentation style)."
    - "The FeatureProbeState record grows minimal fields to drive the branches (e.g. `bool ChatComposerAttached` — toggled by an action step; `ChatComposerStatus? ChatComposerStatus` — controlled the same way). Additions are byte-parallel on both backends (bun's TS type + .NET record) — per gotcha #8 the nullable additions must carry the correct JsonIgnore attributes."
    - "The action set in the fixture: (1) initial GET → renders default composer (branch A: idle). (2) POST `chat-composer-set-streaming` → sets state.ChatComposerStatus=\"streaming\" + state.ChatComposerHasStopAction=true → response renders branch B. (3) POST `chat-composer-toggle-attached` → sets state.ChatComposerAttached=true → renders branch C. (4) POST `chat-composer-set-dropscope-global` → sets state.ChatComposerDropScope=\"global\" → renders branch D. (5) POST `chat-composer-set-submitmode-ctrlenter` → sets state.ChatComposerSubmitMode=\"ctrl-enter\" → renders branch E."
    - "The FeatureProbe demos still pass their own existing test suites after the additions. The additions are ADDITIVE to the state record + buildVm — no existing action removed, no existing branch changed."
    - "`bun run parity/run.ts` exits 0 for ALL backends and ALL fixtures (not just the new one — the full parity suite must remain green)."
  artifacts:
    - path: "parity/fixtures/chat-composer.json"
      provides: "New parity fixture covering 5 ChatComposerNode branches with per-branch expectBodyContains tripwires."
      contains: "chat-composer"
    - path: "parity/backends.json"
      provides: "Fixture registration on FeatureProbe backends."
      contains: "chat-composer"
    - path: "demo/FeatureProbe/AspNetCore/ProbeController.cs"
      provides: "State record growth + buildVm branches for the 5 test cases + 5 new action handlers."
      contains: "ChatComposerNode"
    - path: "demo/FeatureProbe-bun/server.ts"
      provides: "Byte-parallel state + buildVm on the bun side."
      contains: "chat-composer"
  key_links:
    - from: "parity/fixtures/chat-composer.json (expectBodyContains per branch)"
      to: "AGENTS.md gotcha #9 class-3 lesson"
      via: "each documented branch has a distinctive substring — the SEEDED-helpdesk precedent applied to a new composite"
      pattern: "expectBodyContains"
    - from: "FeatureProbe demos (both backends)"
      to: "parity/backends.json fixture registration"
      via: "the fixture name matches the JSON file's `name` field + the backends.json `fixtures` array entry"
      pattern: "chat-composer"
---

<objective>
Land the cross-backend parity fixture proving the ChatComposerNode wire is byte-identical between the bun + .NET (+ Node if a Node backend of FeatureProbe exists) implementations across all 5 documented wire branches. Each branch carries its own `expectBodyContains` substring tripwire per AGENTS.md gotcha #9 class-3 lesson — a branch that stops firing fails LOUDLY instead of silently going vacuous, and `findNulls: false` catches the class-2 defect where nulls would slip past a normalize-then-diff pipeline.

Purpose: Both banked lessons from Vicky's identity (class-2 nulls filtered pre-diff, class-3 branches not exercised) apply to this composite directly. Without per-branch tripwires, a future refactor could silently break streaming or dropScope="global" and the diff would still print "all backends agree" because the fixture never runs that branch. This plan closes both gaps at fixture-authoring time.

Output: Fixture green across all FeatureProbe backends; 5 distinct branches proven exercised; full parity suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-chat-composer-primitive-chatcomposernode-route-b-composite/CONTEXT.md
@AGENTS.md
</context>

<interfaces>
**Existing fixture patterns to model against** — grep + read:
- `parity/fixtures/feature-probe.json` — the shipped generic-feature-probe fixture; latest additions (arrange/align, switcher, fits) show how a wire-shape addition rides existing GET steps as static view-shape.
- `parity/fixtures/helpdesk-seeded.json` — the shipped precedent for `expectBodyContains` tripwires per branch + the class-3 lesson's authoritative documentation ($comment block at the top explains the pattern verbatim; adopt the same tone).
- `parity/fixtures/feature-probe-envelope.json` — the shipped precedent for `findNulls` invariants.

**FeatureProbe pattern** — grep the FeatureProbe demos (bun + AspNetCore):
- `demo/FeatureProbe-bun/server.ts` — FeatureProbeState + buildVm; every new wire-shape addition renders as static VM tree branches.
- `demo/FeatureProbe/AspNetCore/ProbeController.cs` — byte-parallel .NET side.

Both demos already have precedent for growing state + adding action handlers to test new wire fields. Follow the shipped pattern exactly — do not invent a new demo.

**parity/run.ts invariants** — grep for `expectBodyContains` implementation. The harness reads the fixture's per-step `expectBodyContains: string[]` and asserts each substring is present in the raw response body. If absent → step fails with a clear message.

Also grep for `findNulls` implementation — the harness scans the response body for `"foo":null` patterns and fails if `findNulls: false` is set but any null is found.

**Fixture JSON schema** — the shipped fixtures use:
```json
{
  "name": "chat-composer",
  "endpoint": "/api/probe",
  "actionEndpoint": "/api/probe/action",
  "$comment": "...",
  "steps": [
    { "id": "initial", "method": "GET", "expectBodyContains": ["..."], "findNulls": false },
    { "id": "streaming", "method": "POST", "action": {"name": "..."}, "stateMutations": {"...": "..."}, "expectBodyContains": ["..."], "findNulls": false }
  ]
}
```

Verify the exact fixture schema by reading feature-probe.json's structure.

**Kebab-case discriminator in wire** — the JSON emission for ChatComposerNode has `"type":"chat-composer"` (verified in Plan 30-02's smoke test). Every branch's expectBodyContains includes `"chat-composer"` implicitly through this discriminator; the per-branch substrings are additional (`"submitMode":"ctrl-enter"`, `"dropScope":"global"`, etc.).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Grow FeatureProbeState + buildVm on both backends to render 5 ChatComposerNode branches</name>
  <files>demo/FeatureProbe/AspNetCore/ProbeController.cs, demo/FeatureProbe-bun/server.ts</files>
  <read_first>
    - demo/FeatureProbe/AspNetCore/ProbeController.cs (READ FeatureProbeState record; the buildVm function; the existing action handlers)
    - demo/FeatureProbe-bun/server.ts (byte-parallel TS structure)
    - .planning/phases/30-chat-composer-primitive-chatcomposernode-route-b-composite/CONTEXT.md (§Wire shape — the 18 fields; verify field name spellings)
  </read_first>
  <action>
    On BOTH backends (byte-parallel):

    (a) Grow FeatureProbeState with 5 new fields:
    ```csharp
    // AspNetCore ProbeController.cs — inside FeatureProbeState record:
    string ChatComposerStatus,      // "idle" (default) | "sending" | "streaming"
    bool ChatComposerHasStopAction, // when Status can reach "streaming", set true so tree is valid
    bool ChatComposerAttached,      // when true, buildVm emits a chat-composer with a headerSlot chip-strip preview
    string ChatComposerDropScope,   // "composer" (default) | "global"
    string ChatComposerSubmitMode   // "enter" (default) | "ctrl-enter"
    ```
    Byte-parallel additions to bun's `FeatureProbeState` TS interface + defaults.

    ⚠️ Every new nullable-optional wire field on .NET must carry the JsonIgnore attribute per gotcha #8 IF it's used in a ViewNode. Here the STATE record itself has the same rule (per gotcha #8 second paragraph — the state record is NOT covered by the intrinsic JsonIgnore, only the ViewNode types are. Explicit attribute needed if the state field can be null). Since we're using non-nullable string / bool defaults, no JsonIgnore needed on the state fields — the empty string defaults are fine.

    (b) Extend buildVm to render a ChatComposerNode inside the FeatureProbe view:
    ```csharp
    new ChatComposerNode(
        Bind: "chatComposerDraft",
        SendAction: new ActionEvent("chat-composer-send"),
        Status: state.ChatComposerStatus switch {
            "sending" => AshleyShrok.ViewModelShell.ChatComposerStatus.Sending,
            "streaming" => AshleyShrok.ViewModelShell.ChatComposerStatus.Streaming,
            _ => null,  // absent → idle default
        },
        StopAction: state.ChatComposerHasStopAction ? new ActionEvent("chat-composer-stop") : null,
        AttachAction: new ActionEvent("chat-composer-attach"),  // always present so attach branches render
        DropScope: state.ChatComposerDropScope switch {
            "global" => AshleyShrok.ViewModelShell.ChatComposerDropScope.Global,
            _ => null,  // absent → composer default
        },
        SubmitMode: state.ChatComposerSubmitMode switch {
            "ctrl-enter" => AshleyShrok.ViewModelShell.ChatComposerSubmitMode.CtrlEnter,
            _ => null,  // absent → enter default
        },
        // HeaderSlot: for branch C (attached chip preview). If state.ChatComposerAttached,
        // render a minimal fake preview chip subtree so the fixture can assert
        // "vms-chat-composer__chip" or an equivalent JSON substring.
        // (The real chip renders in the browser adapter from client-side registry;
        //  the server-side WIRE doesn't include chip DOM. So instead, use a TextNode
        //  that names the attached-state signal on the wire — a distinct substring
        //  the branch-C step can assert on.)
        HeaderSlot: state.ChatComposerAttached
            ? new TextNode("Attachment: draft.txt (0 KB) [ChatComposerAttached=true tripwire]")
            : null
    )
    ```

    (c) Add 5 new action handlers on both backends:
    - `chat-composer-set-streaming`: state = state with { ChatComposerStatus = "streaming", ChatComposerHasStopAction = true }.
    - `chat-composer-toggle-attached`: state = state with { ChatComposerAttached = !ChatComposerAttached }.
    - `chat-composer-set-dropscope-global`: state = state with { ChatComposerDropScope = "global" }.
    - `chat-composer-set-submitmode-ctrlenter`: state = state with { ChatComposerSubmitMode = "ctrl-enter" }.
    - `chat-composer-send` / `chat-composer-stop` / `chat-composer-attach`: no-op state passthrough (they exist so the tree validator accepts the ActionEvent, but the fixture doesn't exercise them).

    (d) Run each demo locally briefly (`cd demo/FeatureProbe-bun && bun run server.ts` + curl `/api/probe`; `cd demo/FeatureProbe/AspNetCore && dotnet run` + curl `/api/probe`) to confirm the ChatComposerNode appears in the returned JSON. Kill both servers after.

    ⚠️ Byte-parallel: any wire-value spelling difference between bun + .NET will fail the parity diff. Verify each enum's camelCase emission matches between the two backends (Plan 30-02 smoke test already confirmed .NET; verify bun with a curl).
  </action>
  <verify>
    <automated>grep -c 'ChatComposerNode\|chat-composer' demo/FeatureProbe/AspNetCore/ProbeController.cs && grep -c 'chat-composer\|ChatComposerNode' demo/FeatureProbe-bun/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both backends' FeatureProbeState grows the 5 new fields (byte-parallel).
    - Both backends' buildVm renders a ChatComposerNode.
    - Both backends have 5 new action handlers (state mutators; plus 3 no-op passthroughs for the ActionEvents the composer expects).
    - `grep -c 'chat-composer' demo/FeatureProbe/AspNetCore/ProbeController.cs` >= 4 (composer type + 3 action names).
    - `grep -c 'chat-composer' demo/FeatureProbe-bun/server.ts` >= 4.
    - Each demo starts + responds to GET /api/probe with a body containing `"type":"chat-composer"`.
  </acceptance_criteria>
  <done>
    FeatureProbe demos render ChatComposerNode + all 5 wire branches are state-driven from the fixture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write parity/fixtures/chat-composer.json with per-branch expectBodyContains</name>
  <files>parity/fixtures/chat-composer.json</files>
  <read_first>
    - parity/fixtures/feature-probe.json (structural template)
    - parity/fixtures/helpdesk-seeded.json (the class-3 lesson $comment block; adopt the tone)
    - parity/fixtures/feature-probe-envelope.json (findNulls invariant example)
  </read_first>
  <action>
    Write the fixture:
    ```json
    {
      "name": "chat-composer",
      "endpoint": "/api/probe",
      "actionEndpoint": "/api/probe/action",
      "$comment": "CHAT-15 parity fixture for the v9.x ChatComposerNode composite. EVERY branch documented in .planning/phases/30-*/CONTEXT.md §Wire shape has a distinctive expectBodyContains tripwire per AGENTS.md gotcha #9 class-3 lesson (branches the fixture never runs are invisible to the diff). Additionally, `findNulls: false` on every step catches the class-2 defect (nulls filtered pre-diff by normalize.ts would hide a gotcha #8 null-omission violation). If a future refactor stops rendering `data-composer-status` OR drops the JSON `submitMode` field OR emits `null` for a nullable that should be absent, the corresponding step fails LOUDLY. Do NOT weaken these tripwires without a plan-authored rationale — they are the class-3 tripwire that stops the SEEDED-helpdesk-style pattern from recurring.",
      "steps": [
        {
          "id": "idle-initial",
          "method": "GET",
          "expectBodyContains": ["\"type\":\"chat-composer\"", "\"bind\":\"chatComposerDraft\""],
          "findNulls": false
        },
        {
          "id": "streaming-swap",
          "method": "POST",
          "action": {"name": "chat-composer-set-streaming"},
          "expectBodyContains": ["\"status\":\"streaming\"", "\"stopAction\""],
          "findNulls": false
        },
        {
          "id": "attach-toggled",
          "method": "POST",
          "action": {"name": "chat-composer-toggle-attached"},
          "expectBodyContains": ["ChatComposerAttached=true tripwire"],
          "findNulls": false
        },
        {
          "id": "dropscope-global",
          "method": "POST",
          "action": {"name": "chat-composer-set-dropscope-global"},
          "expectBodyContains": ["\"dropScope\":\"global\""],
          "findNulls": false
        },
        {
          "id": "submitmode-ctrlenter",
          "method": "POST",
          "action": {"name": "chat-composer-set-submitmode-ctrlenter"},
          "expectBodyContains": ["\"submitMode\":\"ctrl-enter\""],
          "findNulls": false
        }
      ]
    }
    ```

    ⚠️ Every JSON string inside `expectBodyContains` must be exact-substring-present in the response body. Adjust the exact escaping / naming based on:
    - The actual bind name used in Task 1 (`chatComposerDraft` — verify).
    - The actual attached-tripwire text used in Task 1's HeaderSlot (`ChatComposerAttached=true tripwire` — verify byte-identical spelling).
    - The exact JSON emission for enums — camelCase (`streaming`, `global`, `ctrl-enter`) matches Plan 30-02's smoke-test output.

    ⚠️ The step actions run SEQUENTIALLY. Because each POST mutates state (via the demo's state passthrough), subsequent GETs/POSTs see the mutated state. If a step's assertion depends on ONLY that step's mutation (not carry-over from previous), the fixture will work; if carry-over affects it (e.g. step 3's `attach-toggled` is called AFTER step 2's `streaming-swap` so the response body contains BOTH streaming AND attached tripwires), the assertions must be strictly-independent substrings. Verify by mentally running through the state chain.
  </action>
  <verify>
    <automated>test -f parity/fixtures/chat-composer.json && python3 -c 'import json; d = json.load(open("parity/fixtures/chat-composer.json")); assert d["name"] == "chat-composer"; assert len(d["steps"]) == 5; assert all("expectBodyContains" in s and s.get("findNulls") == False for s in d["steps"]); print("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `parity/fixtures/chat-composer.json` exists + parses as JSON.
    - Has exactly 5 steps.
    - Every step carries `expectBodyContains` (non-empty array) + `findNulls: false`.
    - The `$comment` block cites AGENTS.md gotcha #9 class-3 lesson + gotcha #8 null-omission contract.
    - Each step's expectBodyContains substring uniquely identifies its branch (no two steps rely on the same substring).
  </acceptance_criteria>
  <done>
    Fixture written with tripwires per branch + null-omission invariant per step.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register chat-composer fixture on FeatureProbe backends in parity/backends.json</name>
  <files>parity/backends.json</files>
  <read_first>
    - parity/backends.json (find the FeatureProbe backends — grep for `feature-probe`; note their names + current `fixtures` array)
  </read_first>
  <action>
    For each FeatureProbe backend (`bun-feature-probe`, `dotnet-feature-probe`, `node-feature-probe` — grep confirms which exist), add `"chat-composer"` to the `fixtures` array. One-line addition per backend (delimited by comma; JSON syntax).

    Do NOT touch any other backend or any other fixture registration.
  </action>
  <verify>
    <automated>python3 -c 'import json; d = json.load(open("parity/backends.json")); probes = [b for b in d["backends"] if "feature-probe" in b["name"]]; assert all("chat-composer" in b.get("fixtures", []) for b in probes), f"chat-composer missing from: {[b[\"name\"] for b in probes if \"chat-composer\" not in b.get(\"fixtures\", [])]}"; print(f"Registered on {len(probes)} FeatureProbe backends")'</automated>
  </verify>
  <acceptance_criteria>
    - Every FeatureProbe backend's `fixtures` array now contains `"chat-composer"`.
    - No other backend touched.
    - JSON still valid.
  </acceptance_criteria>
  <done>
    Fixture is registered; parity harness will run it against every FeatureProbe backend.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run parity + confirm the fixture is green + per-branch tripwires all fire</name>
  <files></files>
  <read_first>
    - parity/run.ts (READ to understand: how failures are reported; how expectBodyContains failures are surfaced; how the `--fixture chat-composer` filter works if any)
  </read_first>
  <action>
    Run the full parity suite with the new fixture:
    ```bash
    export PATH="$HOME/.dotnet:$PATH"
    bun run parity/run.ts 2>&1 | tee /tmp/parity-30-07-run.log
    ```
    Expected: exits 0. Every backend agrees on every step. Every expectBodyContains substring found. Every findNulls check passes.

    Additionally, ADVERSARIALLY verify the tripwires actually work — temporarily break ONE substring assertion (add a nonsense char to one step's expectBodyContains) and re-run; confirm the harness FAILS LOUDLY. Revert the break; re-run; confirm green.

    (This is the class-3 lesson operationalized — proving the tripwire fires when it should, not just that it's silent when it doesn't need to.)

    Record in SUMMARY: the parity exit code, the tail of the run log, the adversarial-break test result.
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" && bun run parity/run.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `bun run parity/run.ts` exits 0.
    - Log shows the `chat-composer` fixture ran against all FeatureProbe backends.
    - Log shows all 5 steps passed on all backends (per-step OK indicator).
    - Adversarial-break test confirmed the tripwire fires on failure.
    - `git status --short` shows only: `M parity/backends.json`, `A parity/fixtures/chat-composer.json`, `M demo/FeatureProbe/AspNetCore/ProbeController.cs`, `M demo/FeatureProbe-bun/server.ts`.
  </acceptance_criteria>
  <done>
    Cross-backend byte-parallel wire proven for ChatComposerNode across 5 branches + null-omission invariant + adversarial-break confirmation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parity fixture ↔ two shipped backends | The class-2 + class-3 gotchas from AGENTS.md #9 documented in the fixture $comment. Every branch tripwired, every null-omission invariant enforced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07-01 | Repudiation | Silent wire drift between backends | mitigate | Byte-diff on every response; per-step expectBodyContains tripwires ensure branches actually execute; findNulls invariant catches null-vs-absent drift. |
| T-30-07-02 | Tampering | Fixture author accidentally weakens a tripwire | mitigate | $comment block explicitly warns against weakening; adversarial-break test in Task 4 proves the tripwire fires. |
</threat_model>

<verification>
Task-scoped parity + adversarial-break checks above. Full green-tree gate deferred to Plan 30-11.
</verification>

<success_criteria>
- `parity/fixtures/chat-composer.json` exists with 5 branches + per-branch expectBodyContains + findNulls invariants.
- FeatureProbe demos on both backends render the composer with state-driven branch selection.
- `bun run parity/run.ts` green across all fixtures (not just chat-composer).
- Adversarial-break test proves the tripwire fires on regression.
</success_criteria>

<output>
Create `.planning/phases/30-chat-composer-primitive-chatcomposernode-route-b-composite/30-07-SUMMARY.md` recording: line-count added to each demo's state + buildVm, exact expectBodyContains substrings used per branch, parity run's exit code + tail, adversarial-break test result, `git status --short`.
</output>

**Commit intent:** `test(30-07): parity fixture chat-composer with per-branch tripwires + null-omission invariant (CHAT-15)`
