---
phase: 21-v5-2-lookup-field-lookup-lookup-multiple-the-live-query-lane
plan: 09
type: execute
wave: 7
depends_on: [21-05, 21-07, 21-08]
files_modified:
  - demo/LookupVerification-bun/server.ts
  - demo/LookupVerification-bun/index.html
  - viewmodel-shell/styles/default.css
autonomous: false
requirements: [LOOK-03, LOOK-06, LOOK-07]

must_haves:
  truths:
    - "The FULL green-tree gate passes: vitest, core-globals, aa-contrast, theme-byte-identity, parity, the framework .NET Tests, and EVERY demo/**/*.Tests.csproj — with no exception for pre-existing, unrelated, or just-a-demo failures"
    - "The chip fill's fg/bg pair is HAND-computed for AA contrast across the default + all 12 themes — the fixed 13-pair check:aa-contrast gate does NOT auto-cover a new pair, so a green gate proves nothing about the chip"
    - "A tailnet verification page drives the REAL shipped bundle + default.css + themes, and its fetch-shim runs buildVm output through the REAL tree validator (a shim that skips it accepts trees the real server rejects)"
    - "Ashley reviews the tailnet page and signs off BEFORE any publish — the lookup is a visual + interactive change, so the publish is the in-question path"
  artifacts:
    - path: "demo/LookupVerification-bun/server.ts"
      provides: "a real-bundle verification app: preselected-value, single, multi chips, allowCustom tags, live search, error state — theme-toggleable, validator-gated shim"
      contains: "lookup"
  key_links:
    - from: "demo/LookupVerification-bun/server.ts (fetch shim)"
      to: "the REAL tree validator"
      via: "buildVm output passed through the shipped validator before being returned, so a duplicate action name fails HERE not in production"
      pattern: "validate|collectActions"
---

<objective>
Prove the whole tree is green, hand-verify the chip's AA contrast across every theme, and get Ashley's sign-off on the real rendered control BEFORE any publish.

Purpose: this is the gate between "built" and "released." The lookup is a visual AND interactive change, so the publish is the in-question path and must not proceed until she confirms. Two gates here are specifically NOT covered by the automated suite and would silently pass without this plan:
1. **The chip fill is a NEW fg/bg pair.** `check:aa-contrast` is a FIXED 13-pair gate — it does **not** auto-cover a new pair. A green run proves nothing about the chip. And the ratio inherited from `.vms-badge` was tuned for a small badge on a PAGE surface; the chip sits on the FIELD's input background — a different backdrop.
2. **The verification shim must run the REAL validator.** Banked lesson: a shim that returns `buildVm` output directly bypasses server-side validation and **accepts trees the real server rejects** — it 500'd on a real controller after sailing through a mock. **A duplicate action name anywhere in one tree is a hard validator failure**, and this phase adds a whole new action-bearing field (`searchAction`) plus two validator descents, which is exactly the surface that lesson covers.

Output: `demo/LookupVerification-bun/` driving the REAL shipped bundle; a passing full green-tree gate; a recorded 13-theme AA hand-check; a recorded operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/lookup-field.md
@.planning/phases/21-v5-2-lookup-field-lookup-lookup-multiple-the-live-query-lane/21-CONTEXT.md
@.planning/phases/21-v5-2-lookup-field-lookup-lookup-multiple-the-live-query-lane/21-06-SUMMARY.md

<interfaces>
**The full green-tree gate (AGENTS.md — run ALL, no "pre-existing/unrelated/just-a-demo" exceptions):**
- `cd viewmodel-shell && npx vitest run` (NOT `bun test`)
- `cd viewmodel-shell && npm run check:core-globals`
- `cd viewmodel-shell && node scripts/check-aa-contrast.mjs`
- `cd viewmodel-shell && node scripts/check-theme-byte-identity.mjs`
- `cd parity && bun run run.ts`
- `export PATH="$HOME/.dotnet:$PATH" && dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj`
- `for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p"; done`
⚠️ The framework `viewmodel-shell-dotnet/Tests` project is the easy one to forget — it was uncompilable from 3.0.0 to 3.3.0 precisely because nobody ran it.

**Verification-page precedent:** Phase 20's `demo/NavVerification-bun/` (and Phase 19's chart page) — a frontend `*-bun` demo with an `index.html` + a built `dist/` serving the REAL shipped browser bundle over `Bun.serve`. The `demo/NonBlockingStaleness-bun` siblings are the closest template for a page that must exercise dispatch behavior. ⚠️ **NOT** `demo/FeatureProbe-bun`, which is a headless JSON backend with no frontend/bundle — it renders no browser view. **Rebuild the bundle (`npm run build` in `viewmodel-shell`) before serving**, or you verify the OLD renderer and learn nothing.

**Tailnet serving convention:** bind on the box, reach via `http://thenasty:<port>/`.

**Agent discoverability convention:** this page mounts a real VMS shell with real endpoints ⇒ it MUST carry the one-line HTML comment + `<meta name="viewmodel-shell">` in `<head>` (unlike the pure-frontend Showcase, which has no endpoint to advertise). `grep -L 'viewmodel-shell"' demo/**/*.html` should return nothing among backend-bearing pages.

**The AA hand-check.** `viewmodel-shell/scripts/check-aa-contrast.mjs` is the existing tool — read it to reuse its contrast math rather than re-deriving WCAG formulas by hand. The 13 targets are the default + the 12 files under `viewmodel-shell/styles/themes/`. The chip's fill is `color-mix(in srgb, var(--_chip-tone) N%, var(--vms-surface))` with text at `var(--_chip-tone)` (Plan 21-06). The `--vms-surface` knockout is polarity-adaptive by construction (it mixes toward the theme's own surface, so it adapts to light AND dark with zero per-theme deepening) — the CONTEXT notes the same technique cleared ≥3:1 on all 13 targets for the steps marker. **But "the technique usually works" is not a measurement.** Measure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full green-tree gate — every suite green</name>
  <read_first>
    - ./CLAUDE.md then AGENTS.md (the GREEN-TREE GATE + "NEVER PUBLISH OR PUSH ANYTHING BROKEN" sections — the exact suite list)
    - viewmodel-shell/package.json (the check:core-globals / check:aa-contrast script names)
  </read_first>
  <action>Run the FULL gate in order and make it green: vitest, core-globals, aa-contrast, theme-byte-identity, parity, the framework `viewmodel-shell-dotnet/Tests`, and EVERY `demo/**/*.Tests.csproj` in a loop. If ANY suite is red — **including a pre-existing, unrelated, or demo-only failure** — STOP and fix it, or surface it to the operator for an explicit waiver, before proceeding. "It was already failing" is not an exception: a red suite is a red suite, and shipping on top of one buries this phase's own regressions in the noise. Record each suite's result in the SUMMARY.</action>
  <acceptance_criteria>
    - Every listed suite exits 0, and the SUMMARY records each result individually.
    - Any pre-existing failure found is either fixed (and noted) or explicitly waived by the operator (and noted).
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH"; cd /home/thenasty/ViewModelShell/viewmodel-shell && npx vitest run && npm run check:core-globals && node scripts/check-aa-contrast.mjs && node scripts/check-theme-byte-identity.mjs && cd ../parity && bun run run.ts && cd .. && dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj --nologo -v minimal && for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done && echo GREEN_TREE</automated>
  </verify>
  <done>The full green-tree gate is green across all seven suite groups, each result recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-compute the chip's AA contrast across the default + all 12 themes, then RE-RUN the gate</name>
  <read_first>
    - viewmodel-shell/scripts/check-aa-contrast.mjs (the existing contrast math + how it resolves theme tokens — REUSE this, do not re-derive WCAG formulas)
    - viewmodel-shell/styles/default.css (the .vms-field__chip rules from Plan 21-06 + the .vms-badge 16% precedent at 1141-1185)
    - viewmodel-shell/styles/themes/ (the 12 theme files = 12 of the 13 targets; the shipped light default is the 13th)
    - .planning/phases/21-.../21-06-SUMMARY.md (the provisional mix ratio recorded there)
  </read_first>
  <behavior>
    - The chip's text-on-fill pair is measured against **all 13 targets** (the default + each of the 12 themes) and each result is RECORDED as a number, not asserted as "should be fine".
    - Every target meets the project's AA bar for the chip's text (matching the bar `check-aa-contrast.mjs` enforces for comparable text pairs).
    - The remove button's focus indicator meets the ≥3:1 non-text bar on all 13.
    - If any target fails: **tune the mix ratio (or the tone var) — do NOT add per-theme deepening rules.** The whole point of the `color-mix(… , var(--vms-surface))` knockout is that it is polarity-adaptive with zero per-theme special-casing; a per-theme override would be a regression in technique, not a fix.
    - `node scripts/check-aa-contrast.mjs` and `node scripts/check-theme-byte-identity.mjs` both stay green.
    - 🚨 **If this task changed `default.css` at all, the FULL green-tree gate is RE-RUN and green afterwards.** Task 1's gate ran against the PRE-tuning CSS; a ratio change is a real source change, and per AGENTS.md the sign-off gate must never be taken on an ungated tree. A CSS tweak can break a snapshot/structural test, and discovering that in Plan 21-10 would mean Ashley's sign-off (Task 4) was taken on a tree nobody had gated.
  </behavior>
  <action>🚨 **The fixed 13-pair `check:aa-contrast` gate does NOT auto-cover the chip's new fg/bg pair — a green gate run says NOTHING about the chip.** Measure it by hand.

Reuse the contrast math and theme-token resolution in `scripts/check-aa-contrast.mjs` (a throwaway script that imports/copies its helpers is fine and is far better than re-deriving WCAG formulas). Compute the chip's text-on-fill ratio for the default + each of the 12 themes and **record all 13 numbers in the SUMMARY as a table.** Do the same for the remove button's focus indicator against the ≥3:1 non-text bar.

Do NOT assume the badge's `16%` ratio carries over: it was tuned for a small badge on a **page surface**, and the chip sits on the **field's input background** — a different backdrop. Tune the ratio (or the tone var) until all 13 pass, keeping the single `color-mix(… , var(--vms-surface))` formulation. **No per-theme deepening rules** — if you find yourself writing one, the formulation is wrong, not the theme.

**Consider adding the chip pair to `check-aa-contrast.mjs`'s target set** so it stops being a hand-check next time — judge whether the script's structure admits a new pair cleanly; if it does, add it and say so; if it does not (the "fixed 13-pair" framing suggests it may be structurally fixed), record why not, and note it as a candidate improvement. Do not force it.

🚨 **If you changed `default.css` here, RE-RUN the full green-tree gate before finishing** (the `<verify>` below does exactly that). Task 1's gate ran against the pre-tuning CSS; Ashley's sign-off in Task 4 must not be taken on an ungated tree.</action>
  <acceptance_criteria>
    - The SUMMARY contains a 13-row table: theme → chip text-on-fill contrast ratio → pass/fail.
    - All 13 pass the AA bar; the focus indicator passes ≥3:1 on all 13.
    - No per-theme deepening rule was added (`git diff viewmodel-shell/styles/themes/` is empty).
    - `node scripts/check-aa-contrast.mjs` and `node scripts/check-theme-byte-identity.mjs` both exit 0.
    - **If `default.css` changed: the full green-tree gate was re-run and is green** (recorded in the SUMMARY as a second, post-tuning run — do NOT reuse Task 1's result).
    - The SUMMARY records whether the chip pair was added to the automated gate, and why/why not.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH"; test -z "$(git diff --name-only -- viewmodel-shell/styles/themes/)" && cd viewmodel-shell && node scripts/check-aa-contrast.mjs && node scripts/check-theme-byte-identity.mjs && npx vitest run && npm run check:core-globals && cd ../parity && bun run run.ts && cd .. && dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj --nologo -v minimal && for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done && echo CONTRAST_AND_REGATE_OK</automated>
  </verify>
  <done>The chip's fg/bg pair is hand-measured across all 13 targets with every ratio recorded, all pass AA (and the focus ring passes 3:1), the single surface-knockout formulation is preserved with zero per-theme deepening, the full green-tree gate is re-run green against the post-tuning CSS, and the automated-gate question is answered.</done>
</task>

<task type="auto">
  <name>Task 3: The tailnet verification page — real bundle, REAL-validator fetch shim</name>
  <read_first>
    - demo/NavVerification-bun/ (the Phase 20 precedent: index.html + a built dist/ served over Bun.serve, driving the REAL shipped bundle)
    - demo/NonBlockingStaleness-bun/ (the closest template for a page that must exercise DISPATCH behavior — the lookup's search is the point)
    - viewmodel-shell/src/server.ts (the REAL tree validator the shim must call — collectActions + the uniqueness check)
    - AGENTS.md "Agent discoverability" (the mandatory meta tag for backend-bearing pages)
  </read_first>
  <behavior>
    - The page serves the REAL shipped browser bundle + the REAL `default.css` + the REAL themes (rebuilt from source before serving — not a mockup, not a stale dist).
    - 🚨 **The fetch-shim/handler runs `buildVm` output through the REAL tree validator before returning it.** A tree the real server would reject (e.g. a duplicate action name) must fail HERE, loudly, at build/serve time. **This is the banked lesson**: a shim that skips validation accepts trees the real server rejects, and it 500'd on a real controller after sailing through a mock. This phase adds `searchAction` — a whole new action-bearing field — plus two validator descents, so it is squarely in that blast radius.
    - The page exercises, in the real browser: **(a) the preselected-value case — a form that loads with a reference already set, rendering its label with NO search having occurred (the headline);** (b) single-select search-as-you-type against a directory large enough that the debounce is perceptible; (c) `lookup-multiple` chips — add, remove, keyboard traversal; (d) `allowCustom: true` free-form tags; (e) a search-error state (a query that makes the server fail) proving a dead backend is DISTINGUISHABLE from "no results" (the OPEN-5 differentiator — react-select swallows this); (f) light AND dark themes.
    - The page carries the `<meta name="viewmodel-shell">` discoverability tag + the one-line HTML comment (it mounts a real shell with real endpoints).
    - It is reachable over the tailnet at `http://thenasty:<port>/`.
  </behavior>
  <action>Build `demo/LookupVerification-bun/` on the `NavVerification-bun` / `NonBlockingStaleness-bun` pattern: `index.html` + a built `dist/` served over `Bun.serve`, driving the REAL shipped bundle. **Run `npm run build` in `viewmodel-shell` FIRST** so the new lookup renderer is actually in the bundle — otherwise you verify the old renderer and learn nothing.

🚨 **Route `buildVm`'s output through the REAL tree validator** (`viewmodel-shell/src/server.ts`'s action-name collection + uniqueness check) before returning any response. Do not hand-roll a lookalike check; call the shipped one. Comment it with the banked lesson so it is not "simplified away" later. Seed the directory with enough rows that the debounce is perceptible and the "refine your filter" cap (D7) can be demonstrated as an app-rendered `TextNode`.

Cover all six scenarios above, with the preselected-value case FIRST and visibly labelled — it is the phase's headline proof and the case that kills naive designs. Include the search-error scenario deliberately: it is a free differentiator and the one thing no surveyed library does.

Add the `<meta name="viewmodel-shell">` tag + the one-line comment (backend-bearing page). Serve it on the tailnet and record the URL in the SUMMARY.</action>
  <acceptance_criteria>
    - `demo/LookupVerification-bun/` serves the freshly-rebuilt real bundle + shipped CSS/themes.
    - The response path calls the REAL validator (grep confirms an import from the shipped server module, not a local lookalike).
    - All six scenarios are present and reachable; the preselected-value case is first and labelled.
    - `grep -c 'viewmodel-shell"' demo/LookupVerification-bun/index.html` >= 1 (the meta tag).
    - The page loads over the tailnet; the URL is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && grep -q 'name="viewmodel-shell"' demo/LookupVerification-bun/index.html && grep -qE "collectActions|validate" demo/LookupVerification-bun/server.ts && echo VERIFICATION_PAGE_OK</automated>
  </verify>
  <done>A tailnet verification page drives the real shipped bundle with a REAL-validator-gated response path, exercising the preselected-value headline, live search, chips, allowCustom tags, the search-error differentiator, and light+dark themes, and carrying the discoverability meta.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ashley's sign-off on the rendered lookup</name>
  <what-built>The full green-tree gate is green; the chip's AA contrast is hand-verified across the default + all 12 themes; a tailnet verification page drives the REAL shipped bundle (real renderer, real default.css, real themes) with its response path gated by the REAL tree validator. The lookup is a visual AND interactive change, so the publish is the in-question path — it does not proceed without this confirmation.</what-built>
  <how-to-verify>
    Visit the tailnet page (URL recorded in the SUMMARY; `http://thenasty:<port>/`) and confirm:
    1. 🚨 **The headline:** the preselected lookup shows its LABEL (e.g. "Sally Omer") on page load, with no search performed — NOT a raw id, not empty. This is the case that kills naive designs.
    2. **Live search:** typing feels responsive; results update after you stop typing (~300ms), not per keystroke; the page never freezes or greys out while you type (no busy-lock).
    3. **Type fast, then stop:** the results you land on match what you actually typed — not a stale earlier query.
    4. **Mid-search, the selection holds:** with a filter applied that excludes your current selection, the selected label is still shown (it never falls back to a raw id).
    5. **Chips (multi):** add several; remove one with the mouse and one with the keyboard; focus lands somewhere sensible (the next chip's remove, or the previous, or the text input) — never jumping to the top of the page.
    6. **Free-form tags** (`allowCustom`): typing a new value and pressing Enter creates a tag.
    7. **Search error:** trigger the error scenario — a failed search is clearly DISTINGUISHABLE from "no results" (this is the differentiator; every surveyed library gets this wrong).
    8. **Themes:** switch between light and dark — the chips remain readable in both; nothing is washed out or invisible.
  </how-to-verify>
  <resume-signal>Ashley types "approved" (or describes what looks wrong). The publish (Plan 21-10) does NOT proceed without this.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification shim → real server semantics | A shim more permissive than the real validator gives false confidence and ships a tree that 500s in production |
| built artifact → what Ashley reviews | A stale `dist/` means she signs off on code that is not what ships |
| repo state → publish gate | This plan is the only thing standing between a red/unreviewed tree and the registries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-31 | Repudiation | verification shim accepting a tree the real server rejects (the banked lesson) | mitigate | The response path calls the SHIPPED validator, not a lookalike; a duplicate action name — including the new `searchAction` — fails at the page, loudly, before publish |
| T-21-32 | Repudiation | signing off on a stale bundle | mitigate | `npm run build` is re-run before serving; the page serves the real shipped bundle + CSS + themes, never a mockup |
| T-21-33 | Information disclosure | unreadable chips on some theme (a new fg/bg pair the fixed gate does not cover) | mitigate | All 13 targets hand-measured with the ratios RECORDED; the single surface-knockout formulation is preserved with no per-theme deepening |
| T-21-34 | Tampering | publishing on a red or unreviewed tree | mitigate | The full gate (including the easily-forgotten framework .NET Tests and every demo Tests project) runs here with no pre-existing/unrelated/demo exceptions, and the blocking human checkpoint gates Plan 21-10 |

No third-party package installs in this plan (the verification demo reuses the existing bun/demo toolchain; if a NEW dependency is required, the Package Legitimacy Gate applies and a blocking human checkpoint must precede the install) → no legitimacy gate needed as planned.
</threat_model>

<verification>
- Every green-tree suite exits 0, each recorded individually in the SUMMARY — and re-run green AFTER any AA tuning of `default.css`, so the sign-off is taken on a gated tree.
- The SUMMARY carries the 13-row chip AA table, all passing, with no per-theme deepening added.
- The verification page serves the freshly-built real bundle and gates responses through the real validator.
- Ashley's sign-off is recorded (the resume signal), including anything she flagged.
</verification>

<success_criteria>
The tree is fully green (all seven suite groups, no exceptions), the chip's new fg/bg pair is hand-verified AA across the default + all 12 themes with every ratio recorded and the polarity-adaptive knockout preserved, a tailnet page drives the REAL shipped bundle through the REAL tree validator exercising the preselected-value headline + live search + chips + allowCustom + the search-error differentiator + light/dark, and Ashley has signed off — the precondition for Plan 21-10's publish.
</success_criteria>

<output>
Create `.planning/phases/21-v5-2-lookup-field-lookup-lookup-multiple-the-live-query-lane/21-09-SUMMARY.md` when done (record: each suite's result; the 13-row AA table; the tailnet URL; Ashley's sign-off and any flagged items).
</output>
