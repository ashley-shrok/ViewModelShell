---
phase: 28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b
plan: 11
type: execute
wave: 9
depends_on: ["28-10"]
files_modified:
  - CHANGELOG.md
  - MIGRATION.md
  - AGENTS.md
  - .planning/design/composite-nodes-layer.md
autonomous: true
requirements: [RICH-08]

must_haves:
  truths:
    - "CHANGELOG.md gains a new `## 8.2.0 — <YYYY-MM-DD> (npm + NuGet aligned)` section at the top of the Added/Notes stream, following the Phase 27 v8.1.0 entry as byte-aligned template. The date placeholder stays as `<YYYY-MM-DD>` — Plan 28-12 replaces it with the actual date at bump time (banked lesson from Phase 27's 27-10 → 27-11 handoff)."
    - "CHANGELOG entry documents: (a) the milestone framing — Rich text WYSIWYG input primitive; (b) RichTextFieldNode leaf-input primitive (name, bind, label, placeholder, toolbar, required, disabled, state — Markdown-string wire value); (c) RichTextToolbarNode Route B composite (tools, size, tone, state — approved via before/after tasting page served on tailnet, Ashley signed off <YYYY-MM-DD>); (d) TipTap 2.x + turndown bundled into main @ashley-shrok/viewmodel-shell package — lazy-imported from browser.ts (Chart.js precedent); consumers who never render a RichTextFieldNode ship ZERO TipTap/turndown bytes, verified by adapter test; (e) IF Plan 28-06 landed the shipped markdown scheme sanitizer: a note on the shipped whitelist + `linkHrefRewrite` composition; (f) Wire protocol token stays viewmodel-shell/1.0; MINOR bump; no consumer required to change code."
    - "MIGRATION.md gains a new `## Upgrading to v8.2.0` section at the top of the upgrade-notes stream. Content: 'NO CODE CHANGE required — v8.2.0 is additive wire fields + bundled TipTap. Consumers who don't render a RichTextFieldNode see zero bytes shipped and zero behavior change.' Then a bump-both-packages code block per Phase 27 template. Then a 'New capability: RichTextFieldNode + RichTextToolbarNode' section with a minimal usage example (both TS and .NET snippets, mirroring the existing per-version entries). Then a 'Note on bundling' subsection explaining the D-04 lazy-import posture and the fail-loud behavior on TipTap load failure. IF the sanitizer landed in Plan 28-06: a 'Note on link sanitization' subsection describing the shipped whitelist + how consumers compose `linkHrefRewrite` on top."
    - "AGENTS.md gains a new addendum under §Route B composite-nodes layer's inventory section: a Phase 28-style entry (mirror Phase 27's addendum at the end of that section) noting that v8.2.0 adds RichTextFieldNode (leaf-input primitive) + RichTextToolbarNode (Route B composite) + the shipped default sanitizer contract (if it landed). Reference the design doc's §4 inventory table (updated below) + Phase 28 in ROADMAP.md."
    - "`.planning/design/composite-nodes-layer.md` §4 shipped-recipe inventory table gains a new row for RichTextToolbarNode per Analog J in 28-PATTERNS.md:\n      `| RichTextToolbarNode | tools[], size, tone, state | 28 (RICH-02) | \"rich-text-toolbar\" | RichTextToolbarNode | ButtonNode styling; framework-owned TipTap chain wiring via the enclosing RichTextFieldNode |`\n      Only RichTextToolbarNode goes in the inventory — RichTextFieldNode is a LEAF-INPUT PRIMITIVE per D-01, not a Route B composite. Also add a §5 addendum-style note describing the Phase 28 addition (mirror Phase 27's addendum inserted into AGENTS.md for state axis uniformity)."
    - "IF Plan 28-06 landed the shipped markdown sanitizer, AGENTS.md ALSO gains a gotcha or reference note documenting the shipped whitelist contract (per Plan 28-06's SUMMARY text — the plan produced the exact text; this plan lands it)."
    - "All 4 doc edits STAGE the file changes but do NOT bump versions or commit. Version bumps happen in Plan 28-12 (release) atomically with the release commit."
    - "The green-tree gate from Plan 28-10 remains green after these doc edits — no code changes here, so gates should stay stable, but a doc edit that accidentally modifies a code file would surface via a re-run. Verify by re-running `cd viewmodel-shell && npm run build && npm run check:test-types` briefly at the end (guardrail; if a doc-line-numbered command was misread and a code file got touched, this catches it)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v8.2.0 Added/Notes entry with date placeholder for Plan 28-12 to finalize."
      contains: "8.2.0"
    - path: "MIGRATION.md"
      provides: "v8.2.0 upgrade section with additive-only guidance + usage examples + bundling + sanitization notes."
      contains: "v8.2.0"
    - path: "AGENTS.md"
      provides: "Route B inventory addendum for v8.2.0 + shipped-sanitizer contract note (if applicable)."
      contains: "RichTextFieldNode"
    - path: ".planning/design/composite-nodes-layer.md"
      provides: "§4 table row for RichTextToolbarNode + §5 addendum note."
      contains: "RichTextToolbarNode"
  key_links:
    - from: "CHANGELOG.md + MIGRATION.md v8.2.0 sections"
      to: "Plan 28-12 (release — finalizes the date placeholder + commits atomically)"
      via: "date placeholder <YYYY-MM-DD> handoff pattern from Phase 27"
      pattern: "<YYYY-MM-DD>"
    - from: "composite-nodes-layer.md §4 inventory addition"
      to: "the shipped RichTextToolbarNode composite (Plan 28-05)"
      via: "docs do not race ahead of code — this row lands AFTER the composite SUMMARY lands on main"
      pattern: "RichTextToolbarNode.*rich-text-toolbar"
---

<objective>
Stage all doc edits for the v8.2.0 release: CHANGELOG.md, MIGRATION.md, AGENTS.md addendum, composite-nodes-layer.md §4 inventory row. Follow Phase 27's Plan 27-10 → Plan 27-11 template — this plan stages the docs with a `<YYYY-MM-DD>` date placeholder, and Plan 28-12 (release) finalizes the date + commits atomically with the version bump.

Purpose: Docs land in the SAME commit as the version bump per AGENTS.md release rule; staging separately (this plan) means we know exactly what to bump-commit in Plan 28-12 without confusion or accidental doc-only commits.

Output: 4 files with staged v8.2.0 content; date placeholders intact for Plan 28-12; no version bumps; no commits (git is operator-driven per AGENTS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-CONTEXT.md
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md
@.planning/design/composite-nodes-layer.md
@AGENTS.md
</context>

<interfaces>
Template: Phase 27's v8.1.0 entries.
- `CHANGELOG.md:9-28` — the v8.1.0 CHANGELOG entry shape (heading date placeholder + Added subsection + Note subsection).
- `MIGRATION.md:9-60` — the v8.1.0 MIGRATION shape (Upgrading to header + no-code-change lede + code block + capability description).

Analog K in 28-PATTERNS.md provides the exact template text to adapt.

Analog J in 28-PATTERNS.md provides the exact §4 inventory row for composite-nodes-layer.md.

AGENTS.md addendum: mirror the shape of the existing addendum for Phase 27 state axis uniformity — look for the paragraph starting "Phase 27 (v8.1.0, state axis uniformity):" in AGENTS.md and add a parallel "Phase 28 (v8.2.0, rich text WYSIWYG):" block adjacent.

For the sanitization note (if Plan 28-06 landed the sanitizer): Plan 28-06's SUMMARY produced the exact AGENTS.md note text. Copy it into AGENTS.md's gotchas / references section adjacent to gotcha #4 (which discusses validation-in-state — the sanitizer's semantic neighbor).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add CHANGELOG.md + MIGRATION.md v8.2.0 sections</name>
  <files>CHANGELOG.md, MIGRATION.md</files>
  <read_first>
    - CHANGELOG.md (READ the current head of the file — the most recent entry is Phase 27's 8.1.0; the new 8.2.0 entry goes IMMEDIATELY ABOVE it, following the newest-first convention grep-verify from the existing file structure)
    - MIGRATION.md (READ the current head — Phase 27's Upgrading to v8.1.0 sits at ~lines 9-60; the new Upgrading to v8.2.0 goes IMMEDIATELY ABOVE it)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-04-SUMMARY.md (extract Ashley's tasting sign-off date for the CHANGELOG text: "Ashley signed off <YYYY-MM-DD>" — use the actual date the sign-off happened)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-06-SUMMARY.md (extract the sanitizer disposition — if CLEAN, omit the sanitization notes; if a sanitizer was added, include the notes)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog K — the CHANGELOG + MIGRATION template)
  </read_first>
  <action>
    Insert a NEW `## 8.2.0 — <YYYY-MM-DD> (npm + NuGet aligned)` section at the head of `CHANGELOG.md` (immediately above the v8.1.0 entry). Content follows Analog K's template — the executor copies the template + fills in the actual Ashley-sign-off date from 28-04-SUMMARY.md + conditionally includes the sanitization note if 28-06 landed one.

    Insert a NEW `## Upgrading to v8.2.0` section at the head of `MIGRATION.md` (immediately above the Upgrading to v8.1.0 entry). Content follows Analog K's template.

    DO NOT modify the DATE placeholder `<YYYY-MM-DD>` in the CHANGELOG heading — Plan 28-12 substitutes it at bump time (banked Phase 27 handoff pattern). MIGRATION heading may or may not carry a date — mirror the existing v8.1.0 shape.

    IF Plan 28-06 SUMMARY reports the sanitization audit was CLEAN (no mitigation added): OMIT the sanitization notes from CHANGELOG + MIGRATION. IF it added a whitelist sanitizer: include the sanitization notes per Analog K.
  </action>
  <verify>
    <automated>grep -c '## 8.2.0' CHANGELOG.md &amp;&amp; grep -c '## Upgrading to v8.2.0' MIGRATION.md &amp;&amp; grep -c 'RichTextFieldNode' CHANGELOG.md &amp;&amp; grep -c 'RichTextToolbarNode' CHANGELOG.md &amp;&amp; grep -c 'TipTap' CHANGELOG.md &amp;&amp; grep -c '<YYYY-MM-DD>' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `## 8.2.0 — <YYYY-MM-DD>` (placeholder preserved for Plan 28-12).
    - MIGRATION.md contains `## Upgrading to v8.2.0`.
    - Both files reference RichTextFieldNode, RichTextToolbarNode, TipTap, and (if applicable) the sanitizer.
    - Existing 8.1.0 (and earlier) content preserved unchanged.
    - `grep -c '<YYYY-MM-DD>' CHANGELOG.md` returns >= 1 (the new placeholder — Plan 28-12 will replace it).
  </acceptance_criteria>
  <done>
    v8.2.0 CHANGELOG + MIGRATION sections staged with date placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AGENTS.md addendum for Phase 28 v8.2.0 + shipped-sanitizer note (if applicable)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md §"Route B composite-nodes layer (v8.0.0)" — find the shipped-recipes inventory paragraph and the Phase 27 (v8.1.0, state axis uniformity): addendum block; the new Phase 28 addendum lands ADJACENT (mirror the shape)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-06-SUMMARY.md (the sanitizer disposition + the exact AGENTS.md note text 28-06 produced)
  </read_first>
  <action>
    Insert a `Phase 28 (v8.2.0, rich text WYSIWYG):` block immediately after the existing `Phase 27 (v8.1.0, state axis uniformity):` block in AGENTS.md's §Route B composite-nodes layer section. Content:

    - RichTextFieldNode (leaf-input primitive per D-01) + RichTextToolbarNode Route B composite (per D-02, approved via before/after tasting page served on tailnet, Ashley signed off).
    - Wire value is a markdown STRING on the field's bind path (per D-06); zero XSS surface on the wire; display flows through the existing markdown.ts → InlineRuns pipeline (no new render code).
    - TipTap 2.x + turndown bundled into main package (per D-04), lazy-imported from browser.ts per Chart.js precedent — consumers who never render one ship zero bytes; verified by adapter test asserting the modules are NOT in the initial bundle graph.
    - RichTextToolbarNode is the fifth composite that adds a `state?: string` field to the inventory per Phase 27's uniformity rule; the axis is a matter of course for row/list-shaped composites now.
    - Feature-surface floor is the D-08 Slack/GitHub level (bold, italic, link, ordered/unordered lists, headings h1-h3, inline code, code block, blockquote). Everything else deferred.

    IF the sanitizer landed in Plan 28-06: also insert the exact AGENTS.md note text 28-06 produced — locate the gotchas / references section adjacent to gotcha #4 (validation-in-state — the sanitizer's semantic neighbor) and add a new note documenting the shipped whitelist + linkHrefRewrite composition.

    Preserve all other AGENTS.md content byte-for-byte.
  </action>
  <verify>
    <automated>grep -c 'Phase 28 (v8.2.0' AGENTS.md &amp;&amp; grep -c 'RichTextFieldNode' AGENTS.md &amp;&amp; grep -c 'RichTextToolbarNode' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `Phase 28 (v8.2.0` addendum block exists.
    - RichTextFieldNode + RichTextToolbarNode both referenced.
    - No unrelated AGENTS.md content changed.
    - If sanitizer landed: the sanitizer note is present adjacent to gotcha #4.
  </acceptance_criteria>
  <done>
    AGENTS.md addendum landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add composite-nodes-layer.md §4 inventory row + §5 addendum</name>
  <files>.planning/design/composite-nodes-layer.md</files>
  <read_first>
    - .planning/design/composite-nodes-layer.md (READ §4 shipped-recipe inventory table; grep for `| RichTextToolbarNode` — should NOT already exist; locate the alphabetical / phase-ordered insertion point)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog J — the exact row shape)
  </read_first>
  <action>
    (a) Insert into §4 shipped-recipe inventory table:
    ```markdown
    | `RichTextToolbarNode` | tools[], size, tone, state | 28 (RICH-02) | `"rich-text-toolbar"` | `RichTextToolbarNode` | ButtonNode styling; framework-owned TipTap chain wiring via the enclosing RichTextFieldNode |
    ```

    (b) Append a §5-style addendum (mirroring how Phase 27's state-axis uniformity addendum reads in AGENTS.md — locate the equivalent in this design doc if a §5 exists, else append a Phase 28 note at the end).

    Only RichTextToolbarNode gets an inventory row — RichTextFieldNode is a leaf-input primitive per D-01, not a Route B composite. Per the convention in composite-nodes-layer.md ("Docs do not race ahead of code"), this row lands ONLY after Plan 28-05's SUMMARY exists (which it does by the time this plan runs).
  </action>
  <verify>
    <automated>grep -c 'RichTextToolbarNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'rich-text-toolbar' .planning/design/composite-nodes-layer.md</automated>
  </verify>
  <acceptance_criteria>
    - `RichTextToolbarNode` row present in §4 table.
    - `rich-text-toolbar` wire discriminator present.
    - RichTextFieldNode NOT in the composite inventory (leaf-input primitives don't earn a Route B row).
  </acceptance_criteria>
  <done>
    Design doc updated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify green-tree gates still pass after doc edits</name>
  <files></files>
  <read_first>
    - Plan 28-10's SUMMARY (the green verdict from Plan 28-10 must still hold)
  </read_first>
  <action>
    Doc edits should not touch code; re-run the FAST subset of the gate as guardrail:
    ```bash
    cd viewmodel-shell && npm run build && npm run check:test-types
    ```
    Both must exit 0.

    If either fails: a doc-line-numbered command was misread and a code file got touched — HALT and inspect the diff.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build 2&gt;&amp;1 | tail -3 &amp;&amp; npm run check:test-types 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Both commands exit 0.
    - No code file was accidentally modified — verify by `git status --short` showing only doc files modified.
  </acceptance_criteria>
  <done>
    Doc-only changes confirmed; ready for release.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc entry ↔ shipped behavior | Docs promise behavior; shipped code delivers. Drift is invisible to test gates but visible to consumers. Task 1's ext-mm to Plan 28-06 SUMMARY handling means the CHANGELOG accurately reflects whether the sanitizer shipped. |
| Date placeholder ↔ Plan 28-12 handoff | The `<YYYY-MM-DD>` placeholder is the load-bearing handoff signal per Phase 27. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-28 | Repudiation | CHANGELOG claims sanitizer shipped when it didn't (or vice versa) | mitigate | Task 1's conditional-on-28-06-SUMMARY logic: read the SUMMARY, only include sanitizer notes if it actually landed. |
| T-28-29 | Repudiation | Date placeholder accidentally replaced | mitigate | Task 1 acceptance criterion: `<YYYY-MM-DD>` still present in CHANGELOG at plan close. Plan 28-12 is the ONLY task authorized to replace it. |
</threat_model>

<verification>
Per-task verifications above. Task 4's guardrail catches accidental code touches.
</verification>

<success_criteria>
- 4 files staged for v8.2.0.
- Date placeholder preserved.
- Green-tree gate re-verification passes.
- No code changes.
</success_criteria>

<output>
Create `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-11-SUMMARY.md`. Record: diff summary for each of the 4 files, the sanitizer disposition (baked into CHANGELOG or not), Ashley's tasting date extracted from 28-04-SUMMARY, and the guardrail gate re-verification result.
</output>
