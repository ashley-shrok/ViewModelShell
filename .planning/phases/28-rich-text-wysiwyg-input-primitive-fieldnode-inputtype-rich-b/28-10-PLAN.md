---
phase: 28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b
plan: 10
type: execute
wave: 8
depends_on: ["28-06", "28-07", "28-08", "28-09"]
files_modified: []
autonomous: true
requirements: [RICH-07]

must_haves:
  truths:
    - "Full green-tree gate per AGENTS.md 🚨 NEVER PUBLISH OR PUSH ANYTHING BROKEN — the EXACT set CI runs. The gate below matches vms-map.md §'The local green-tree gate' verbatim. Machine-verifiable; NO theatrical human checkpoint per banked 2026-07-31 Ashley directive."
    - "TS-side gate (in `viewmodel-shell/`): `npm run build`, `npm run check:test-types`, `npm run check:core-globals`, `npm run check:aa-contrast`, `npm run check:no-demo-style`, `npm run check:demo-types`, `npx vitest run` — ALL exit 0. check:test-types is CI-only-catches per banked lesson; do not skip."
    - "PATH-export gate: `export PATH=\"$HOME/.dotnet:$PATH\"` before any .NET or parity command — the parity harness spawns dotnet as a child and dies with a bare `spawn` trace if it's not on PATH."
    - ".NET-side gate: `dotnet test viewmodel-shell-dotnet/Tests` (the framework's OWN test project — historically forgotten; the 3.0.0-3.3.0 uncompilable stretch is why it's now in the gate) — exit 0."
    - "Every demo .NET tests project runs: `for p in $(find demo -name '*.Tests.csproj'); do dotnet test \"$p\"; done` — every project exits 0. IF any demo test project shows a PRE-EXISTING failure that is not related to Phase 28's work: HALT per AGENTS.md working-agreement rule ('It was already failing on main is NOT an exception') and report the failure to the operator; do NOT proceed to Plan 28-11 until the pre-existing failure is fixed OR explicitly waived by the operator."
    - "Markdown companion compile: `dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj` — exit 0. If Plan 28-06 added a sanitizer to the Markdown companion, this test is where it lands green."
    - "Parity: `bun run parity/run.ts` — exit 0. Plan 28-07 landed the fixture; this run is the last cross-backend byte-check before release."
    - "Additionally: `npm run check:theme-byte-identity` + `npm run check:theme-function` — theme consistency gates; historically added; run for defense-in-depth (some phases add / drop themes without a full-audit — this catches drift)."
    - "SUMMARY records the exact exit code + last-few-lines of output for EACH gate command, so 28-11's release plan can point to concrete evidence that green-tree was verified."
    - "IF ANY gate fails: HALT. Do NOT create a SUMMARY marking Phase 28 green. The failing gate's output is recorded in a FAILURE SUMMARY; the executor fixes the failure OR reports it to the operator; the plan reopens only after the failure is genuinely green."
  artifacts:
    - path: ".planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-10-SUMMARY.md"
      provides: "Per-gate exit-code + tail output evidence + timing + a final GREEN or FAILURE verdict."
      contains: "GREEN"
  key_links:
    - from: "AGENTS.md 🚨 gate + vms-map.md §The local green-tree gate"
      to: "This plan's task list"
      via: "The command set is byte-copied from vms-map.md so a future gate-adds-to-CI change gets mirrored here"
      pattern: "check:test-types|check:core-globals|check:aa-contrast"
---

<objective>
Run the FULL green-tree gate — the exact machine-verifiable set CI runs — before the release plan touches version files. This is the load-bearing prerequisite for Plan 28-11 (release) per AGENTS.md 🚨 NEVER PUBLISH OR PUSH ANYTHING BROKEN rule. Autonomous; no theatrical human checkpoint per banked 2026-07-31 Ashley directive.

Purpose: The gate exists to catch the class of defect that slipped through: (a) a plan added a test but forgot to include it in tsconfig.test.json's coverage (check:test-types); (b) a plan added a CSS rule that fails AA contrast (check:aa-contrast); (c) a plan added a file that a demo tsconfig doesn't pick up (check:demo-types); (d) a pre-existing framework .NET test failure that everyone assumed was someone else's problem (the 3.0.0-3.3.0 uncompilable stretch). Running the entire gate before release means we ship green.

Output: A SUMMARY marking every gate GREEN, or a HALT with a FAILURE SUMMARY naming the failing gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
</context>

<interfaces>
The gate is copied verbatim from `~/.claude/identities/vicky/vms-map.md §'The local green-tree gate'` and `AGENTS.md §'NEVER PUBLISH OR PUSH ANYTHING BROKEN'`. If either doc updates the command set, THIS plan's task must be updated in the same commit — the rule is that the gate matches what CI runs, so a drift is silently unsafe.

The gate commands (batch A — TS-side, run from `viewmodel-shell/`):
```
npm run build && npm run check:test-types && npm run check:core-globals \
  && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types \
  && npm run check:theme-byte-identity && npm run check:theme-function \
  && npx vitest run
```

Batch B — .NET + parity (repo root, PATH exported):
```
export PATH="$HOME/.dotnet:$PATH"
dotnet test viewmodel-shell-dotnet/Tests
for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p"; done
dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj
bun run parity/run.ts
```

⚠️ Pre-existing failure handling (from AGENTS.md working agreement):
> `It was already failing on main / it's unrelated to my change / it's just a demo test` are **NOT** exceptions — a red suite is a red suite. If a pre-existing failure surfaces mid-release, STOP, surface it, and fix or explicitly waive before proceeding.

The plan MUST NOT insert a `checkpoint:human-verify` for "confirm the gate is green" per banked Ashley 2026-07-31 directive (the theatrical checkpoint class). The gate's exit codes are objective; if all green, autonomous proceed; if any red, halt.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full TS-side gate (viewmodel-shell/)</name>
  <files></files>
  <read_first>
    - AGENTS.md §"Working agreement for agents" (the 🚨 gate rule + the exact command set)
    - viewmodel-shell/package.json (READ the scripts block to confirm every check:* target exists; a script name change since gate-copying would silently break the gate)
  </read_first>
  <action>
    Run the TS gate:
    ```bash
    cd viewmodel-shell
    npm run build && \
    npm run check:test-types && \
    npm run check:core-globals && \
    npm run check:aa-contrast && \
    npm run check:no-demo-style && \
    npm run check:demo-types && \
    npm run check:theme-byte-identity && \
    npm run check:theme-function && \
    npx vitest run
    ```
    Capture: exit code + last 10 lines of output for EACH command. If any command exits non-zero, HALT and report to SUMMARY as FAILURE with the failing command name + output. Do NOT proceed to Task 2 until Task 1 is fully green.

    Special handling: if `check:aa-contrast` fails on a Phase 28-introduced fg/bg pair (a toolbar tone variance), the fix is to deepen the pair via `color-mix` per the banked Phase 27 pattern — but that's a code fix in Plan 28-05, not a workaround here. HALT and report; do NOT skip the gate.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; npm run check:test-types &amp;&amp; npm run check:core-globals &amp;&amp; npm run check:aa-contrast &amp;&amp; npm run check:no-demo-style &amp;&amp; npm run check:demo-types &amp;&amp; npm run check:theme-byte-identity &amp;&amp; npm run check:theme-function &amp;&amp; npx vitest run 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EVERY command in the sequence exits 0.
    - `npx vitest run` reports ALL passing (0 failed, 0 skipped except intentional skips).
    - SUMMARY records per-command exit code + tail output.
  </acceptance_criteria>
  <done>
    TS gate green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the .NET + parity gate (repo root, PATH exported)</name>
  <files></files>
  <read_first>
    - AGENTS.md working-agreement rule about pre-existing failures NOT being an exception
    - vms-map.md §"The local green-tree gate" for the exact .NET + parity command set
  </read_first>
  <action>
    Run the .NET + parity gate:
    ```bash
    export PATH="$HOME/.dotnet:$PATH"

    # Framework's OWN .NET tests (historically forgotten):
    dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal

    # Every demo test project (Plan 28-02's new tests + Plan 28-06's Markdown sanitization tests are inside these):
    for p in $(find demo -name '*.Tests.csproj') viewmodel-shell-dotnet/Markdown/Tests/*.csproj; do
      echo "=== $p ===";
      dotnet test "$p" --nologo -v minimal;
    done

    # Markdown companion compile (in case sanitizer landed):
    dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal

    # Cross-backend parity:
    bun run parity/run.ts
    ```

    Any failure: HALT. Follow the AGENTS.md working-agreement rule — pre-existing failure is NOT an exception. Report to SUMMARY as FAILURE + surface to the operator.

    Include the RichTextSerializationTests (Plan 28-02) + MarkdownSanitizationTests (Plan 28-06) in the counts — these must pass on their first run under the gate; if they don't, Plan 28-02 or Plan 28-06 has a bug that the individual plan's verification missed.
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal &amp;&amp; for p in $(find demo -name '*.Tests.csproj') viewmodel-shell-dotnet/Markdown/Tests/*.csproj; do echo "=== $p ==="; dotnet test "$p" --nologo -v minimal || exit 1; done &amp;&amp; dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal &amp;&amp; bun run parity/run.ts 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the sequence exits 0.
    - `dotnet test viewmodel-shell-dotnet/Tests` reports all passing.
    - Every `demo/**/*.Tests.csproj` reports all passing.
    - Markdown companion builds green.
    - `bun run parity/run.ts` reports all backends green + all tripwires present.
    - SUMMARY records per-command exit codes + tail output.
  </acceptance_criteria>
  <done>
    Full green-tree gate green. Phase 28 is ready for the release plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Green-tree gate ↔ CI | The local gate MUST match CI. If CI adds a new check (e.g., a new lint), this gate's task list must be updated in the same commit — or CI fails after local passes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-26 | Repudiation | "Green" verdict without actually running every gate | mitigate | Task acceptance criteria enumerates EVERY command; SUMMARY records per-command evidence. A missing command in the SUMMARY is visible drift from the AGENTS.md rule. |
| T-28-27 | Denial of Service | Pre-existing failure blocks release | accept | Per AGENTS.md working agreement — pre-existing failure is NOT an exception. The release halts until the failure is fixed OR explicitly waived by operator. |
</threat_model>

<verification>
Task-level exit codes are the verification. Green = proceed to Plan 28-11. Any red = halt.
</verification>

<success_criteria>
- Every gate in the sequence exits 0.
- SUMMARY records per-command evidence + a final GREEN verdict.
- Ready for Plan 28-11 (release).
</success_criteria>

<output>
Create `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-10-SUMMARY.md`. Record: per-command exit codes, tail output, timing, and a final GREEN verdict OR a FAILURE verdict naming the failing command.
</output>
