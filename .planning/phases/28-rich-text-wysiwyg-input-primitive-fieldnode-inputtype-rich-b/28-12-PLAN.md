---
phase: 28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b
plan: 12
type: execute
wave: 10
depends_on: ["28-11"]
files_modified:
  - viewmodel-shell/package.json
  - viewmodel-shell/package-lock.json
  - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
  - CHANGELOG.md
  - MIGRATION.md
autonomous: false
requirements: [RICH-09]

must_haves:
  truths:
    - "OPERATOR-GATED per AGENTS.md 'A version bump is NOT a release' rule: every registry-touching command runs under operator credentials via the `.env` sync ritual per vms-map.md §Publishing runbook. The agent runs the mechanical auth precheck INLINE (banked Ashley 2026-07-30 directive: STRIP GSD-generated `checkpoint:human-action` auth-precheck tasks; the agent executes the precheck itself and reports halt-on-failure). Publishing itself (npm publish, dotnet nuget push) runs under the operator's shell where the credentials are loaded."
    - "Version bump surgical: `viewmodel-shell/package.json` `\"version\"` bumped from `8.1.0` → `8.2.0`; `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` `<Version>` bumped from `8.1.0` → `8.2.0`; `viewmodel-shell/package-lock.json` self-version bumped in EXACTLY TWO spots per gotcha (root `\"version\"` ~L3, `packages.\"\".version` ~L9) — assert count==2 with a scripted check, python3 -c 'import json; json.load(open(...))' to validate JSON, then the delta is 2 lines. IF package-lock.json's current self-version is 6.12.1 (pre-Phase-28 stale — Plan 28-03's npm install may or may not have refreshed it), take current-value → 8.2.0; the surgical count still applies."
    - "CHANGELOG.md v8.2.0 heading date placeholder `<YYYY-MM-DD>` REPLACED with today's actual ISO date. MIGRATION.md heading finalized similarly if it carries a date placeholder."
    - "AUTH PRECHECK executed INLINE by the agent (NOT a human checkpoint): source `.env` at repo root; verify `NPM_TOKEN` + `NUGET_API_KEY` are present; sync `~/.npmrc` from `NPM_TOKEN` per the vms-map ritual; run `npm whoami` — expect `ashley-shrok`. On any failure: HALT and report to operator with the specific failure (e.g., `NPM_TOKEN missing from .env` OR `npm whoami E401 — token expired, mint new one at settings/tokens`). Do NOT bump versions until precheck passes."
    - "PRE-BUMP full green-tree gate re-run: the gate from Plan 28-10 may have staled during Plan 28-11's doc edits (unlikely but the AGENTS.md rule 'NEVER PUBLISH ANYTHING BROKEN' is absolute). Re-run the full gate (TS batch + .NET batch + parity) BEFORE the bump commit. On any failure: HALT and report; do NOT bump."
    - "npm publish command (operator runs this in their own shell where NPM_TOKEN is active): `cd viewmodel-shell && npm publish` (prepublishOnly hook rebuilds dist/). Verify registry-latest via `curl -s https://registry.npmjs.org/@ashley-shrok/viewmodel-shell | python3 -c \"import sys,json;print(json.load(sys.stdin)['dist-tags']['latest'])\"` returns `8.2.0`. NEVER `npm view` (cached per AGENTS.md)."
    - "NuGet publish command (operator runs): `cd viewmodel-shell-dotnet && dotnet pack -c Release && dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.8.2.0.nupkg --api-key \"$NUGET_API_KEY\" --source https://api.nuget.org/v3/index.json`. Registry-latest verified via curl to flat-container index. NuGet indexer is EVENTUALLY CONSISTENT — 404s for ~5-15 min after push are NORMAL; the push receipt is authoritative."
    - "Annotated tag `v8.2.0` created at the release-commit SHA + pushed: `git tag -a v8.2.0 <sha> -m 'viewmodel-shell 8.2.0'` + `git push origin v8.2.0`. Use the SHA recorded from the bump-commit, NOT HEAD (HEAD may drift)."
    - "Main-advance verified: `git merge-base --is-ancestor v8.2.0 main` exits 0 — the load-bearing 'release must be reachable from main' gate (the missed-2-releases-2026-06-14 lesson). If STRANDED: reconcile per AGENTS.md — rebase main onto v8.2.0 OR merge the tag in, then push origin main; re-run the ancestor check."
    - "🚨 ANNOUNCE ROOM IS `#vms-announcements` (`!QvlInhfVNZRUxQPtcR:thenasty.taild9b663.ts.net`) where vicky is OWNER — NOT `#vms-changelog` (that's where Phase 27 shipped by mistake; corrected 2026-07-31 per CONTEXT §Ashley banked directives). VERIFY the room ID against Nelly's `/joined_rooms` output BEFORE posting — if the room ID has drifted or Nelly no longer owns it, HALT and confirm with operator (banked pre-flight check from CONTEXT)."
    - "DM Angel that rich text ships (his `/ai` composition-swap surface may adopt this in a future iteration — CONTEXT §Deferred cross-references his chat-composer-primitive bounty; the DM is a courtesy that he's now unblocked to prototype). DM Molly that the mention-picker-primitive bounty's future consumer (RichTextFieldNode.mentionsProvider — a DEFERRED slot per CONTEXT §Deferred that the slot shape does not preclude) is now shipping the field it will attach to. Both DMs are courtesies, not blockers — if either agent is unreachable, note in SUMMARY but do not halt."
    - "The Plan 28-04 tasting server (PID in `demo/RichTextTasting-bun/server.pid`) AND the Plan 28-09 verification server (PID in `demo/RichTextVerification-bun/server.pid`) are killed at plan close: `kill $(cat demo/RichTextTasting-bun/server.pid) 2>/dev/null; kill $(cat demo/RichTextVerification-bun/server.pid) 2>/dev/null; echo cleaned`. Servers stay running through release ceremony (Ashley can re-verify at will); killed after the announce."
    - "The plan SUMMARY records: precheck outputs (npm whoami result + $NUGET_API_KEY presence check WITHOUT leaking the key), pre-bump gate exit codes, bump-commit SHA, npm publish output, NuGet push output, both curl-registry-latest verifications, tag creation output, main-ancestor verification (STRANDED reconciliation if it happened), Ashley's announce message text + Matrix ref, Angel/Molly DM confirmation refs (or unreachable notes), and both PID cleanup outputs."
  artifacts:
    - path: "viewmodel-shell/package.json"
      provides: "version bumped to 8.2.0 (was 8.1.0)"
      contains: "8.2.0"
    - path: "viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj"
      provides: "<Version> bumped to 8.2.0 (was 8.1.0)"
      contains: "8.2.0"
    - path: ".planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-12-SUMMARY.md"
      provides: "Full release evidence — commit SHA, both registry publish verifications, tag + main-advance verification, announce message text, DM refs, PID cleanup."
      contains: "8.2.0"
  key_links:
    - from: "All prior Phase 28 plans (shipped wire + renderer + tests + parity + docs + verification sign-off)"
      to: "npm @ashley-shrok/viewmodel-shell 8.2.0 + NuGet AshleyShrok.ViewModelShell 8.2.0"
      via: "This plan's operator-gated commit + publish + tag + advance-main"
      pattern: "v?8\\.2\\.0"
---

<objective>
Aligned MINOR release of v8.2.0 on both npm + NuGet. Operator-gated per AGENTS.md publishing runbook + vms-map.md §Publishing runbook. Mirrors Plan 27-11's structure verbatim with 8.2.0 substitutions + the corrected announce room (`#vms-announcements` per CONTEXT banked directive) + the inline auth precheck (NO theatrical human-action checkpoint per Ashley banked 2026-07-30 directive).

Purpose: Publishing is the ONLY way v8.2.0 reaches consumers. A version bump in git without the registry push is stranded silence (the AGENTS.md loophole that left npm stuck at 1.0.1 through 3 releases). This plan enforces the runbook step-for-step + the missed-2-releases-2026-06-14 main-advance mitigation.

Output: Both registries serve 8.2.0; tag v8.2.0 exists + pushed; main advanced; #vms-announcements posted; Angel + Molly DM'd; verification/tasting servers cleaned up; SUMMARY records every verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-10-SUMMARY.md
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-11-SUMMARY.md
</context>

<interfaces>
Reference plan (READ before executing): `.planning/phases/27-composite-state-axis-uniformity-close-the-state-gap-across-a/27-11-PLAN.md` — the byte-aligned template with 8.1.0 substituted for 8.2.0.

AGENTS.md rules governing this plan:
- "A version bump is NOT a release" — publish in the same operator session.
- "Publishing is operator-gated" — `autonomous: false`; every registry command runs under operator credentials.
- "Credential precheck" — verify auth BEFORE bump-commit; halt on failure. INLINE, not human-action (banked directive).
- "Advance main" — verify with `git merge-base --is-ancestor v8.2.0 main`; reconcile if failing.
- "Companion NuGet compat" — MINOR bump does NOT require companion republish; Markdown companion's compile check ran in Plan 28-10 gate.
- "Never publish or push anything broken" — Plan 28-10's GREEN verdict is the prerequisite.

vms-map.md §Publishing runbook — the exact `.env` sync ritual (source `.env` at repo root, verify NPM_TOKEN + NUGET_API_KEY, write `~/.npmrc` from NPM_TOKEN, run `npm whoami`).

Version bump summary:
- npm: viewmodel-shell/package.json `"version": "8.1.0"` → `"8.2.0"`
- NuGet: viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj `<Version>8.1.0</Version>` → `<Version>8.2.0</Version>`
- package-lock.json: 2 surgical spots per gotcha (root `"version"` + `packages."".version`)

Announce room ID (VERIFY against `/joined_rooms` before posting):
- `#vms-announcements` — `!QvlInhfVNZRUxQPtcR:thenasty.taild9b663.ts.net` (vicky is OWNER; confirmed 2026-07-31)
- NOT `#vms-changelog` (Phase 27 shipped there by mistake)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inline auth precheck + pre-bump green-tree re-run</name>
  <files></files>
  <read_first>
    - vms-map.md §Publishing runbook (READ the exact `.env` sync ritual + auth precheck commands)
    - AGENTS.md §Working agreement (the 🚨 gate rule + operator-driven git rule)
    - Plan 28-10's SUMMARY (the last-recorded green verdict)
  </read_first>
  <action>
    (a) Auth precheck — run INLINE (NOT a human-action checkpoint per Ashley banked 2026-07-30):
    ```bash
    ENV="$(git rev-parse --show-toplevel)/.env"
    if [ ! -f "$ENV" ]; then echo "HALT: .env not found at $ENV — operator must create with NPM_TOKEN + NUGET_API_KEY"; exit 1; fi
    grep -qE '^NPM_TOKEN=' "$ENV"     || { echo "HALT: NPM_TOKEN missing from .env"; exit 1; }
    grep -qE '^NUGET_API_KEY=' "$ENV" || { echo "HALT: NUGET_API_KEY missing from .env"; exit 1; }

    NPM_TOKEN_VAL=$(grep '^NPM_TOKEN=' "$ENV" | cut -d= -f2- | sed "s/^['\"]//; s/['\"]\$//")
    printf '//registry.npmjs.org/:_authToken=%s\n' "$NPM_TOKEN_VAL" > ~/.npmrc
    chmod 600 ~/.npmrc

    NPM_USER=$(npm whoami 2>&1)
    if [ "$NPM_USER" != "ashley-shrok" ]; then echo "HALT: npm whoami returned '$NPM_USER' (expected ashley-shrok) — token likely expired; operator mints new one at https://www.npmjs.com/settings/ashley-shrok/tokens with 'Bypass 2FA' TICKED"; exit 1; fi

    set -a; source "$ENV"; set +a
    if [ -z "$NUGET_API_KEY" ]; then echo "HALT: $NUGET_API_KEY not exported after source .env"; exit 1; fi
    echo "PRECHECK OK: npm=ashley-shrok, NUGET_API_KEY=<present, ${#NUGET_API_KEY} chars>"
    ```
    Record the PRECHECK OK line (WITHOUT the key value) to SUMMARY.

    (b) Pre-bump green-tree re-run — verify Plan 28-10's verdict still holds:
    ```bash
    cd viewmodel-shell && npm run build && npm run check:test-types && npm run check:core-globals \
      && npm run check:aa-contrast && npm run check:no-demo-style && npm run check:demo-types \
      && npm run check:theme-byte-identity && npm run check:theme-function \
      && npx vitest run
    cd .. && export PATH="$HOME/.dotnet:$PATH" \
      && dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal \
      && for p in $(find demo -name '*.Tests.csproj') viewmodel-shell-dotnet/Markdown/Tests/*.csproj; do dotnet test "$p" --nologo -v minimal || exit 1; done \
      && dotnet build viewmodel-shell-dotnet/Markdown/AshleyShrok.ViewModelShell.Markdown.csproj --nologo -v minimal \
      && bun run parity/run.ts
    ```
    Any failure: HALT and report. Do NOT proceed to Task 2.
  </action>
  <verify>
    <automated>test -f "$(git rev-parse --show-toplevel)/.env" &amp;&amp; grep -qE '^NPM_TOKEN=' "$(git rev-parse --show-toplevel)/.env" &amp;&amp; grep -qE '^NUGET_API_KEY=' "$(git rev-parse --show-toplevel)/.env" &amp;&amp; echo "PRECHECK: .env + tokens present"</automated>
  </verify>
  <acceptance_criteria>
    - `.env` exists at repo root with NPM_TOKEN + NUGET_API_KEY.
    - `npm whoami` returns `ashley-shrok`.
    - Full green-tree gate green (all commands exit 0).
    - SUMMARY records PRECHECK OK + gate re-run evidence.
  </acceptance_criteria>
  <done>
    Auth confirmed; green tree confirmed. Ready to bump versions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump versions + finalize CHANGELOG/MIGRATION dates + commit</name>
  <files>viewmodel-shell/package.json, viewmodel-shell/package-lock.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj, CHANGELOG.md, MIGRATION.md</files>
  <read_first>
    - viewmodel-shell/package.json (READ current version — expect `"version": "8.1.0"`)
    - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (READ `<Version>8.1.0</Version>`)
    - viewmodel-shell/package-lock.json (READ the top-of-file for the 2 surgical spots — root `"version"` at ~L3 and `packages."".version` at ~L9)
    - CHANGELOG.md (locate the `<YYYY-MM-DD>` placeholder in the v8.2.0 heading from Plan 28-11)
    - MIGRATION.md (locate any date placeholder from Plan 28-11)
  </read_first>
  <action>
    Perform surgical file edits in sequence:

    (a) `viewmodel-shell/package.json`: change `"version": "8.1.0"` → `"version": "8.2.0"`. Single-line edit.

    (b) `viewmodel-shell/package-lock.json`: SURGICAL 2-spot edit per gotcha. Root `"version"` (~L3) + `packages."".version` (~L9). Both go from current-value to `"8.2.0"`. Assert count == 2 via `grep -cE '"version": "8.2.0"' viewmodel-shell/package-lock.json` (this returns >= 2; verify that the count matches exactly the 2 surgical spots — any additional match is a blind replace clobbering a dependency at the same version, which is a bug). Then validate JSON: `python3 -c "import json; json.load(open('viewmodel-shell/package-lock.json'))"`.

    (c) `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj`: change `<Version>8.1.0</Version>` → `<Version>8.2.0</Version>`. Single-line edit.

    (d) `CHANGELOG.md`: replace `<YYYY-MM-DD>` in the v8.2.0 heading with today's actual date (ISO format from `date -u +%Y-%m-%d`).

    (e) `MIGRATION.md`: if the v8.2.0 heading has a date placeholder, replace similarly.

    Post-bump build check:
    ```bash
    cd viewmodel-shell && npm run build
    cd .. && export PATH="$HOME/.dotnet:$PATH" && dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal
    ```
    Both must exit 0.

    Commit:
    ```bash
    git add CHANGELOG.md MIGRATION.md viewmodel-shell/package.json viewmodel-shell/package-lock.json viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj AGENTS.md .planning/design/composite-nodes-layer.md
    git commit -m "$(cat <<'EOF'
    release: npm 8.2.0 + NuGet 8.2.0 — rich text WYSIWYG input primitive

    Milestone: RichTextFieldNode leaf-input primitive + RichTextToolbarNode
    Route B composite. Wire value is a markdown string on the field's bind
    path (via TipTap 2.x + turndown, bundled + lazy-imported per Chart.js
    precedent — consumers who never render one ship zero TipTap/turndown
    bytes, verified by adapter test). Toolbar composite approved via
    before/after tasting served on tailnet; Ashley signed off.

    Additive on the wire; MINOR bump; wire protocol token stays viewmodel-shell/1.0.

    See CHANGELOG.md v8.2.0 + MIGRATION.md upgrade section.
    EOF
    )"
    ```
    Record commit SHA to SUMMARY.
  </action>
  <verify>
    <automated>grep -c '"version": "8.2.0"' viewmodel-shell/package.json &amp;&amp; grep -cE '"version": "8.2.0"' viewmodel-shell/package-lock.json &amp;&amp; grep -c '<Version>8.2.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj &amp;&amp; grep -c '<YYYY-MM-DD>' CHANGELOG.md &amp;&amp; cd viewmodel-shell &amp;&amp; npm run build 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"version": "8.2.0"' viewmodel-shell/package.json` returns exactly 1.
    - `grep -cE '"version": "8.2.0"' viewmodel-shell/package-lock.json` returns exactly 2 (the 2 surgical spots per gotcha).
    - `grep -c '<Version>8.2.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns exactly 1.
    - `grep -c '<YYYY-MM-DD>' CHANGELOG.md` returns 0 (placeholder replaced).
    - `python3 -c "import json; json.load(open('viewmodel-shell/package-lock.json'))"` no error (valid JSON post-edit).
    - TS + .NET builds green.
    - Commit exists; SHA recorded.
  </acceptance_criteria>
  <done>
    Versions bumped, dates finalized, release commit staged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Publish to npm + publish to NuGet + verify both registries via curl</name>
  <files></files>
  <read_first>
    - vms-map.md §Publishing runbook (READ the exact publish commands)
    - AGENTS.md "publishing is operator-gated" (operator's shell runs the publish; the agent may drive them via the same session with the operator's explicit go-ahead per this plan's autonomous:false posture)
  </read_first>
  <action>
    (a) npm publish (uses ~/.npmrc from Task 1's precheck):
    ```bash
    cd viewmodel-shell && npm publish
    ```
    Record full stdout to SUMMARY. On failure: HALT.

    Verify registry-latest via direct curl (NEVER `npm view` — cached per AGENTS.md):
    ```bash
    curl -s https://registry.npmjs.org/@ashley-shrok/viewmodel-shell \
      | python3 -c "import sys,json;print(json.load(sys.stdin)['dist-tags']['latest'])"
    ```
    Expected: `8.2.0`.

    (b) NuGet publish:
    ```bash
    cd viewmodel-shell-dotnet && dotnet pack -c Release
    dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.8.2.0.nupkg --api-key "$NUGET_API_KEY" --source https://api.nuget.org/v3/index.json
    ```
    Record `Your package was pushed` receipt to SUMMARY (this is authoritative per vms-map).

    Verify registry-latest via curl (may take 5-15 min to appear per NuGet indexer eventual consistency):
    ```bash
    curl -s https://api.nuget.org/v3-flatcontainer/ashleyshrok.viewmodelshell/index.json \
      | python3 -c "import sys,json;print(json.load(sys.stdin)['versions'][-1])"
    ```
    Expected: `8.2.0` (a 404 for up to 15 min is NORMAL and NOT a failure — the push receipt is the authoritative signal).

    If EITHER publish fails: HALT and report to operator.
  </action>
  <verify>
    <automated>echo "Task 3 verifies via publish receipts + curl-to-registry — outputs go to SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - `npm publish` succeeded (stdout captured; no error).
    - `curl` to npm registry returns `8.2.0` as `dist-tags.latest`.
    - `dotnet nuget push` succeeded with "Your package was pushed" receipt.
    - `curl` to NuGet flat-container returns `8.2.0` at some point (may be delayed by indexer; the push receipt is authoritative).
  </acceptance_criteria>
  <done>
    Both registries serve 8.2.0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Tag v8.2.0 + push tag + verify main advance</name>
  <files></files>
  <read_first>
    - AGENTS.md "Tag the release after a successful publish" + "🚨 Advance main to the release commit" sections
    - vms-map.md §Publishing runbook tag pattern
  </read_first>
  <action>
    (a) Create annotated tag at Task 2's release-commit SHA:
    ```bash
    git tag -a v8.2.0 <release-commit-sha> -m "viewmodel-shell 8.2.0"
    git push origin v8.2.0
    ```
    (Use the SHA from Task 2; NOT HEAD.)

    (b) Verify tag pushed:
    ```bash
    git ls-remote --tags origin | grep 'refs/tags/v8.2.0$'
    ```
    Expected: one match.

    (c) Push main to origin (release commit is on the local main branch; per AGENTS.md working-agreement rule, the operator drives this):
    ```bash
    git push origin main
    ```

    (d) Verify main-advance:
    ```bash
    git merge-base --is-ancestor v8.2.0 main && echo "on main" || echo "STRANDED"
    ```

    (e) If STRANDED: reconcile per AGENTS.md — options:
    - `git rebase v8.2.0 main` (replay any main-only commits onto v8.2.0), then `git push origin main`.
    - OR `git checkout main && git merge v8.2.0 --ff-only`, then `git push origin main`.
    Re-run the ancestor check.

    Record: tag creation output, tag push output, main push output, ancestor verdict (+ reconciliation if needed).
  </action>
  <verify>
    <automated>git tag -l 'v8.2.0' &amp;&amp; git ls-remote --tags origin | grep -q 'refs/tags/v8.2.0' &amp;&amp; git merge-base --is-ancestor v8.2.0 main &amp;&amp; echo "MAIN ADVANCED"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l 'v8.2.0'` returns exactly `v8.2.0`.
    - `git ls-remote --tags origin | grep 'refs/tags/v8.2.0$'` returns one match.
    - `git merge-base --is-ancestor v8.2.0 main` exits 0.
    - If reconciliation happened, `git ls-remote --heads origin main` reveals the reconciled tip.
  </acceptance_criteria>
  <done>
    Tag pushed; main advanced; release reachable via both `main` and `v8.2.0`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Announce v8.2.0 + notify Angel/Molly + cleanup PIDs</name>
  <action>See <how-to-verify> below — Ashley verifies room ID, posts announcement, sends DMs, kills tasting/verification servers. Agent waits for the resume-signal.</action>
  <what-built>
    - npm registry serves 8.2.0.
    - NuGet registry serves 8.2.0 (or receipt captured; indexer may lag ~5-15 min).
    - Tag `v8.2.0` at release commit; pushed to origin.
    - `main` contains the release (`git merge-base --is-ancestor v8.2.0 main` green).

    OUTSTANDING (for Ashley to complete):
    - Room-ID verification against Nelly's /joined_rooms output for `#vms-announcements`.
    - Post the release announcement to `#vms-announcements`.
    - Send DMs to Angel + Molly.
    - Confirm both PID cleanups.
  </what-built>
  <how-to-verify>
    Ashley, please complete the release announcement + notifications:

    **(0) Room-ID verification (banked pre-flight check from CONTEXT §Ashley banked directives).** Verify with Nelly that `#vms-announcements` room ID is still `!QvlInhfVNZRUxQPtcR:thenasty.taild9b663.ts.net` and that she still owns/can post to it. If the ID drifted or she can no longer post, respond `announce-blocked: <reason>` and we halt for reconciliation.

    **(1) Announce on `#vms-announcements`** (`!QvlInhfVNZRUxQPtcR:thenasty.taild9b663.ts.net`).

    Suggested message text (edit as you prefer):
    ```
    v8.2.0 shipped — rich text WYSIWYG input primitive 📝

    - `RichTextFieldNode` (leaf-input primitive) — wire value is a markdown string on the field's `bind` path; the editor is a bundled TipTap 2.x, lazy-imported from browser.ts (Chart.js precedent). Consumers who never render one ship ZERO TipTap/turndown bytes.
    - `RichTextToolbarNode` (Route B composite) — typed slot on RichTextField OR standalone. Closed-enum variance axes: size (compact/expanded), tone (danger/warning/success/info), state (Phase 27 uniformity). Approved via before/after tasting served on tailnet.
    - Feature-surface floor (Slack/GitHub level): bold, italic, link, ordered/unordered lists, headings h1-h3, inline code, code block, blockquote.
    - Display on the read side flows through the existing `markdown.ts` → InlineRuns pipeline (no new render code).
    - Additive on the wire; MINOR bump; wire protocol token stays `viewmodel-shell/1.0`.

    npm: https://www.npmjs.com/package/@ashley-shrok/viewmodel-shell
    NuGet: https://www.nuget.org/packages/AshleyShrok.ViewModelShell/8.2.0
    CHANGELOG: see repo
    ```

    **(2) DM Angel** — rich text is now in the framework; his `/ai` composition-swap surface can consider adopting it in a future iteration. Suggested DM:
    ```
    Angel — v8.2.0 shipped rich text WYSIWYG (RichTextFieldNode + RichTextToolbarNode). Not folded into /ai this phase per your ask, but the primitive is there for you to reach for whenever chat-composer wants it.
    ```

    **(3) DM Molly** — the mention-picker-primitive bounty's future consumer (RichTextFieldNode.mentionsProvider) is now shipping the field it will attach to. Suggested DM:
    ```
    Molly — v8.2.0 shipped rich text (RichTextFieldNode). The mentionsProvider slot is DEFERRED to a follow-up phase per plan boundary, but the field it'll attach to now ships. When mention-picker-primitive is ready, we'll add the slot.
    ```

    **(4) PID cleanup** — kill the tasting + verification servers (leaves the box clean):
    ```bash
    kill $(cat demo/RichTextTasting-bun/server.pid) 2>/dev/null
    kill $(cat demo/RichTextVerification-bun/server.pid) 2>/dev/null
    echo "cleaned"
    ```

    After (1)-(4) are done, respond with these signals on one message:
    - `announced: {message URL or note}`
    - `dm-angel-sent: {ref or note}` (or `dm-angel-unreachable: {reason}` if he's genuinely offline)
    - `dm-molly-sent: {ref or note}` (or `dm-molly-unreachable: {reason}`)
    - `cleanup-ok`

    Room-ID reconciliation (if step 0 flagged a drift) is handled BEFORE (1).
  </how-to-verify>
  <resume-signal>Respond with all of: `announced: {ref}`, `dm-angel-sent: {ref}` (or unreachable), `dm-molly-sent: {ref}` (or unreachable), `cleanup-ok`. Any `announce-blocked: {reason}` from step 0 HALTS the plan for room-ID reconciliation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator credentials → npm registry | Uses `~/.npmrc` synced from `.env`'s NPM_TOKEN; no automation. Same trust boundary as every prior release. |
| Operator credentials → NuGet registry | `dotnet nuget push` uses `$NUGET_API_KEY` sourced from `.env`; no automation. |
| Git remote push | Runs under operator's git credential. |
| Announcement room | Room-ID pre-flight check protects against sending to a stale room ID (Phase 27's mistake corrected). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-30 | Elevation of Privilege | Registry publish credentials | mitigate | Operator-gated at task boundaries; agent never sees `$NUGET_API_KEY` value (only presence check). Credentials live in `.env` outside version control. |
| T-28-31 | Repudiation | Missed publish / stranded main | mitigate | Task 3's curl verification + Task 4's `merge-base --is-ancestor` gate. The reconciliation path handles STRANDED cases. |
| T-28-32 | Repudiation | Announcement to wrong room | mitigate | Task 5 step 0 asks Ashley to verify the room ID against `/joined_rooms` BEFORE posting. Banked pre-flight check from CONTEXT explicitly for this class of defect (Phase 27's misroute is what triggered the banking). |
| T-28-33 | Tampering | Rushed publish with green-tree stale | mitigate | Task 1's pre-bump green-tree re-run explicitly verifies Plan 28-10's verdict still holds AT BUMP TIME. Any drift halts. |
</threat_model>

<verification>
Per-task verifications above. Task 5 is the operator sign-off — the plan intentionally halts for the announcement + DM signals.
</verification>

<success_criteria>
- Both registries serve 8.2.0.
- Tag v8.2.0 pushed + main-ancestor green.
- #vms-announcements posted + Angel/Molly DM'd + PID cleanups done.
- Plan SUMMARY records every verification.
</success_criteria>

<output>
Create `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-12-SUMMARY.md`. Record: precheck outputs, pre-bump gate re-run verdict, bump-commit SHA, npm publish output + curl verification, NuGet push receipt + curl verification, tag creation + push output, main-ancestor verification (+ reconciliation if any), Ashley's announcement message text + Matrix ref, both DM refs (or unreachable notes), and both PID cleanup outputs.
</output>
