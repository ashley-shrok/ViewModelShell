---
phase: 28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/server.ts
autonomous: true
requirements: [RICH-01, RICH-04]

must_haves:
  truths:
    - "Two new interfaces added to `viewmodel-shell/src/index.ts`: `RichTextFieldNode` (leaf-input primitive, per D-01) and `RichTextToolbarNode` (Route B composite, per D-02), each with TSDoc that names the phase/decision (`v8.2.0 (RICH-01)` / `v8.2.0 (RICH-02)`) and the anticipated-customization-surface note explaining why the primitive earns its own shape (per D-01/D-02 rationale in 28-CONTEXT.md)."
    - "`RichTextFieldNode` interface exposes: `type: \"rich-text-field\"`, `name: string` (REQUIRED), `bind: string` (REQUIRED — markdown-string round-trip target per D-06), `label?: string`, `placeholder?: string`, `toolbar?: RichTextToolbarNode` (typed slot per Route B pattern — omitted = framework default toolbar), `required?: boolean`, `disabled?: boolean`, `state?: string` (Phase 27 uniform lifecycle axis)."
    - "`RichTextToolbarNode` interface exposes: `type: \"rich-text-toolbar\"`, `tools: RichTextTool[]` (REQUIRED — the composite's semantically-primary slot per typed-slots §3), `size?: \"compact\" | \"expanded\"` (closed enum — D-02 anticipated axis), `tone?: \"danger\" | \"warning\" | \"success\" | \"info\"` (framework-wide tone axis), `state?: string` (Phase 27 uniformity). NO raw CSS/style fields — variance is closed-enum only per §3."
    - "`RichTextTool` closed union exported alongside — 11 values matching the D-08 Slack/GitHub floor: `\"bold\" | \"italic\" | \"link\" | \"bullet-list\" | \"ordered-list\" | \"heading-1\" | \"heading-2\" | \"heading-3\" | \"inline-code\" | \"code-block\" | \"blockquote\"`. Widening later is additive."
    - "Both interfaces added to the `ViewNode` discriminated union arms — `| RichTextFieldNode` and `| RichTextToolbarNode` appear in the union declaration."
    - "Tree validator arms added in `viewmodel-shell/src/server.ts`: `case \"rich-text-field\":` (leaf — no action-bearing descendants; the `toolbar` slot's inner `tools[]` are enum tokens, NOT dispatches) and `case \"rich-text-toolbar\":` (leaf — the `tools[]` are enum tokens naming built-in TipTap chain commands, resolved CLIENT-SIDE; no framework-side dispatch). Both are exhaustive-switch defense-in-depth per gotcha #9."
    - "Every optional field uses `?:` (never emits `null` on the TS twin — intrinsic side of gotcha #8)."
    - "`npm run build` (in `viewmodel-shell/`) succeeds — additive; no existing consumer breaks."
    - "`npm run check:test-types` succeeds (mandatory per banked check:test-types-easy-to-forget lesson from vms-map)."
    - "`grep -c '\"rich-text-field\"\\|\"rich-text-toolbar\"' viewmodel-shell/src/index.ts` returns at least 4 (two interfaces, two type discriminators; discounting comment-hits by NOT filtering on `^` — a bare `grep -c` is fine because these token strings only appear in code sites within this file at this stage)."
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "RichTextFieldNode + RichTextToolbarNode + RichTextTool exports; ViewNode union grown by 2 arms."
      contains: "rich-text-field"
    - path: "viewmodel-shell/src/server.ts"
      provides: "Two case arms in the collectActions walker for defense-in-depth exhaustive-switch discipline."
      contains: "rich-text-field"
  key_links:
    - from: "viewmodel-shell/src/index.ts (2 new interfaces)"
      to: "viewmodel-shell-dotnet/ViewModels.cs (Plan 28-02 .NET byte-identical twin)"
      via: "shared wire discriminator strings (\"rich-text-field\", \"rich-text-toolbar\") + optional-fields rule"
      pattern: "rich-text-(field|toolbar)"
    - from: "viewmodel-shell/src/index.ts (RichTextFieldNode)"
      to: "viewmodel-shell/src/browser.ts (Plan 28-03 renderer)"
      via: "type discriminator dispatch switch"
      pattern: "case \"rich-text-field\""
---

<objective>
Add the two new wire type interfaces (`RichTextFieldNode` leaf-input, `RichTextToolbarNode` Route B composite) + the `RichTextTool` closed union to `viewmodel-shell/src/index.ts`, and add the two exhaustive-switch defense-in-depth walker arms to `viewmodel-shell/src/server.ts`. This is Wave 1 of Phase 28 — establishes the TS wire shape that Plans 28-02 (.NET), 28-03 (renderer), 28-05 (composite renderer + tasting-gated CSS), 28-07 (parity), and 28-08 (agent-skill) all depend on.

Purpose: The design of record (per D-01, D-02, D-06, D-08 in 28-CONTEXT.md; Analog B in 28-PATTERNS.md) is fully locked. Landing the TS types first unblocks parallel work — 28-02 can mirror on .NET, and Wave 2 renderer/tests can proceed against a committed contract.

Output: TS wire shape complete for both nodes; both walkers descend (leaf arms); TS build + type-check green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-CONTEXT.md
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md
@.planning/design/composite-nodes-layer.md
@AGENTS.md
</context>

<interfaces>
<!-- Golden templates for the two new interfaces + the closed union.
     Extracted verbatim from 28-PATTERNS.md Analog B (§Wire type + validator arms).
     Every field's TSDoc must land as specified — the executor should NOT paraphrase. -->

RichTextFieldNode analog: `AvatarNode` at index.ts:1554-1586 (leaf primitive shape) + `FieldNode(inputType:"textarea")` at index.ts (write-back-on-input plumbing conceptually — for the .bind path documentation).

RichTextToolbarNode analog: `MessageNode` at index.ts:1687-1723 (composite with typed slots + `state?` axis).

RichTextTool analog: `ChartNode.kind` at index.ts:2462-2465 (closed union pattern; widening is additive).

Existing state? occurrences AFTER Phase 27 (grep-verified from Phase 27's SUMMARY):
- ListItemNode, TableRow, ListRowNode, UserRowNode, MessageNode, DetailRowNode, TimelineEntryNode, SettingRowNode, ChipNode — 9 total.
- Phase 28 adds 1 MORE: RichTextFieldNode.state — total becomes 10. RichTextToolbarNode.state adds an 11th.

Existing case arm shape reference for the walker (server.ts:500-506, `case "avatar"`):
```typescript
case "avatar": {
  // v8.0.0 (COMP-04) — AvatarNode is a leaf (no children, no action). Same
  // terminal-leaf posture as IconNode. The arm exists so a future refactor
  // that promotes it to a container fails the TypeScript exhaustiveness
  // check here first.
  return;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add RichTextFieldNode + RichTextToolbarNode interfaces + RichTextTool union + union arms in index.ts</name>
  <files>viewmodel-shell/src/index.ts</files>
  <read_first>
    - viewmodel-shell/src/index.ts (READ index.ts:1554-1586 for the AvatarNode leaf-node TSDoc pattern; READ index.ts:1687-1723 for the MessageNode composite TSDoc pattern; READ index.ts:2462-2465 for the ChartNode.kind closed-union pattern; grep for the `ViewNode` union declaration to locate the arm insertion point — typically near the bottom of the primary union block)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-CONTEXT.md (§Decisions D-01, D-02, D-06, D-08 — the four decisions this task implements; §Deferred — the customization surface the interfaces MUST NOT preclude)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog B — the exact interface bodies to synthesize; read the full block starting at "Shape the planner should synthesize:")
  </read_first>
  <action>
    Add three new exports to `viewmodel-shell/src/index.ts` in sequence, immediately after the existing MessageNode block (which lives in the Route B composites section, ~line 1687-1723):

    (a) The closed union type `RichTextTool` — export as a plain type alias:
    ```typescript
    export type RichTextTool =
      | "bold"
      | "italic"
      | "link"
      | "bullet-list"
      | "ordered-list"
      | "heading-1"
      | "heading-2"
      | "heading-3"
      | "inline-code"
      | "code-block"
      | "blockquote";
    ```
    With a full TSDoc block above it explaining: (i) this is the D-08 Slack/GitHub floor; (ii) widening later is additive per gotcha #9's forward-compat posture; (iii) consumers/agents key off the string, never assume a fixed set (mirrors ChartNode.kind posture at :2462-2465).

    (b) The `RichTextToolbarNode` interface — the Route B composite. Copy the exact body from 28-PATTERNS.md Analog B (the "Shape the planner should synthesize:" block, second interface). The TSDoc must include: (i) `v8.2.0 (RICH-02)`; (ii) the D-03 tasting-before-ship note referencing `.planning/design/composite-nodes-layer.md §2`; (iii) the typed-slots §3 rules; (iv) the "ANTICIPATED CUSTOMIZATION" block enumerating deferred fields per D-02 rationale (visibleTools, headings-dropdown, position variants, compact/expanded variants, overflow-to-kebab) with the note that the shape below MUST NOT preclude them. Fields exactly as specified in Analog B: `type`, `tools` (REQUIRED), `size?`, `tone?`, `state?`.

    (c) The `RichTextFieldNode` interface — the leaf-input primitive. Copy the exact body from 28-PATTERNS.md Analog B (the "Shape the planner should synthesize:" block, first interface). The TSDoc must include: (i) `v8.2.0 (RICH-01)`; (ii) the wire-format-is-markdown-string note per D-06 (zero XSS surface on the wire; zero new render infrastructure since display flows through existing markdown.ts → InlineRuns); (iii) the bundling note per D-04 (bundled TipTap + turndown, lazy-imported from browser.ts per Chart.js precedent — consumers who never render one ship zero bytes); (iv) the D-08 feature-surface floor enumeration; (v) the D-Q4 sanitization note; (vi) the "ANTICIPATED CUSTOMIZATION SURFACE (deferred per CONTEXT §Deferred; slot shape MUST not preclude)" block: allowedMarks/allowedNodes, mentionsProvider, plainTextValueBind, heightMin/heightMax, sanitizeConfig, imageUpload, comment-only mode. Fields exactly as specified in Analog B: `type`, `name` (REQUIRED), `bind` (REQUIRED), `label?`, `placeholder?`, `toolbar?` (typed `RichTextToolbarNode`), `required?`, `disabled?`, `state?`.

    (d) Add both nodes to the `ViewNode` discriminated union (grep for `export type ViewNode =` or scan the union arms — the pattern is one `| InterfaceName` per line). Add exactly two new arms in alphabetical position where the neighbors sit (adjacent to the other composites — near MessageNode / TimelineEntryNode / SettingRowNode; place `| RichTextFieldNode` and `| RichTextToolbarNode` on separate lines, together).

    Do NOT use `null` on any optional field (all optionals use `?:` per intrinsic side of gotcha #8 for the TS twin).

    Do NOT add any field beyond those listed — the deferred fields (mentionsProvider, plainTextValueBind, heightMin/heightMax, sanitizeConfig, imageUpload, allowedMarks/allowedNodes) are called out in CONTEXT §Deferred; adding them here violates the Phase 28 scope boundary.

    Verify with `cd viewmodel-shell && npm run build` — TypeScript must compile clean.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build 2&gt;&amp;1 | tail -5 &amp;&amp; grep -c 'export interface RichTextFieldNode' src/index.ts &amp;&amp; grep -c 'export interface RichTextToolbarNode' src/index.ts &amp;&amp; grep -c 'export type RichTextTool' src/index.ts &amp;&amp; grep -c '| RichTextFieldNode' src/index.ts &amp;&amp; grep -c '| RichTextToolbarNode' src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export interface RichTextFieldNode' viewmodel-shell/src/index.ts` returns exactly 1.
    - `grep -c 'export interface RichTextToolbarNode' viewmodel-shell/src/index.ts` returns exactly 1.
    - `grep -c 'export type RichTextTool' viewmodel-shell/src/index.ts` returns exactly 1.
    - `grep -c '| RichTextFieldNode' viewmodel-shell/src/index.ts` returns exactly 1 (union arm).
    - `grep -c '| RichTextToolbarNode' viewmodel-shell/src/index.ts` returns exactly 1 (union arm).
    - `grep -cE '"rich-text-field"|"rich-text-toolbar"' viewmodel-shell/src/index.ts` returns at least 2 (the two `type: "..."` discriminators).
    - `cd viewmodel-shell && npm run build` — exit 0.
    - No occurrence of `mentionsProvider` / `plainTextValueBind` / `allowedMarks` / `heightMin` in the new interfaces (deferred fields must not sneak in): `grep -cE 'mentionsProvider|plainTextValueBind|allowedMarks|heightMin' viewmodel-shell/src/index.ts | grep -q '^0$'` OR the pre-existing count is unchanged.
  </acceptance_criteria>
  <done>
    Two new interfaces + one closed union added to `viewmodel-shell/src/index.ts`. `ViewNode` union grown by exactly 2 arms. TypeScript build clean. Contract is committed for Plans 28-02 (.NET twin) and 28-03 (renderer) to build against.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add exhaustive-switch defense-in-depth walker arms in server.ts</name>
  <files>viewmodel-shell/src/server.ts</files>
  <read_first>
    - viewmodel-shell/src/server.ts (READ the `case "avatar":` block at ~line 500-506 for the leaf arm template; grep for `case "message":` at ~line 219-231 for the composite descend pattern — but the RichText nodes are BOTH leaves per Analog D rationale in 28-PATTERNS.md; grep for `switch` to find both `collectActions` and any second walker that needs the arms)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog D — reads the full rationale for treating BOTH new nodes as leaves in the walker: RichTextField's `toolbar` slot's inner `tools[]` are enum tokens NOT dispatches; RichTextToolbar's `tools[]` are enum tokens naming client-side TipTap chain commands)
  </read_first>
  <action>
    Add two `case` arms to EACH walker switch in `viewmodel-shell/src/server.ts` that has an existing `case "avatar":` no-op arm (there will be at least the `collectActions` walker; grep for every function containing `switch (node.type)` and add arms to each — the exhaustive-switch discipline mandates coverage in every walker or TypeScript flags an unhandled `ViewNode` union arm).

    For each walker, add both arms immediately after the existing `case "avatar":` no-op arm. Use the exact leaf-arm shape (per Analog D template):

    ```typescript
    case "rich-text-field": {
      // v8.2.0 (RICH-01) — RichTextFieldNode is a leaf (no action-bearing
      // descendants; its `toolbar?` slot's `tools[]` are enum tokens naming
      // built-in TipTap chain commands, resolved CLIENT-SIDE — never a
      // dispatch that could collide with another action's name). The arm
      // exists so a future refactor that adds a dispatch-bearing slot fails
      // the TypeScript exhaustiveness check here first.
      return;
    }
    case "rich-text-toolbar": {
      // v8.2.0 (RICH-02) — RichTextToolbarNode is a leaf (tools[] are enum
      // tokens naming built-in TipTap chain commands, resolved CLIENT-SIDE —
      // never a framework-side dispatch that could collide with another
      // action's name). Same exhaustive-switch defense-in-depth as
      // rich-text-field above.
      return;
    }
    ```

    Do NOT add arms to walkers whose switch does NOT already carry the `case "avatar":` no-op (they exist for SectionNode-specific rules; the two new nodes have no SectionNode descendants).

    Verify `npm run build` in `viewmodel-shell/` remains green.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; grep -c 'case "rich-text-field"' src/server.ts &amp;&amp; grep -c 'case "rich-text-toolbar"' src/server.ts &amp;&amp; npm run build 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "rich-text-field"' viewmodel-shell/src/server.ts` matches the count of walker switches that carry `case "avatar":` — verify by first running `grep -c 'case "avatar"' viewmodel-shell/src/server.ts` and asserting the two counts are equal.
    - `grep -c 'case "rich-text-toolbar"' viewmodel-shell/src/server.ts` also matches that same count.
    - `cd viewmodel-shell && npm run build` — exit 0.
    - `cd viewmodel-shell && npm run check:test-types` — exit 0 (this is the CI-only-catches gate; runs `tsc -p tsconfig.test.json --noEmit`).
    - No `return; return;` typos or duplicate case blocks (`grep -c 'case "rich-text-field"'` returns exactly N, not 2N — verify against the walker count).
  </acceptance_criteria>
  <done>
    Every TS walker that switches on `node.type` handles both new nodes with a leaf-arm no-op. Exhaustive-switch discipline preserved. Downstream plans (28-03 renderer, 28-07 parity) can safely add non-leaf usage without silently skipping the walk.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS wire types → downstream serializers | Adding an interface widens the ViewNode contract every backend must honor; a shape drift between TS + .NET would silently fail both backends' walkers (class-1 defect from gotcha #9). Plan 28-02 mirrors on .NET; parity gate in Plan 28-07 diffs the serialized wire. |
| Union arms → tree walker exhaustiveness | Failing to add a walker arm for a new ViewNode discriminator silently skips descent — hides duplicate-action-name bugs (banked 2026-07-16 Nelly finding on TrackerNode's .NET-side gap). Task 2 above addresses defense-in-depth on the TS side; Plan 28-02 mirrors on .NET. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | TS wire type shape | mitigate | Both nodes fully documented in TSDoc with `v8.2.0 (RICH-01/02)` provenance markers; deferred customization fields explicitly enumerated in code comments (mentionsProvider, plainTextValueBind, etc.) so a future refactor cannot silently re-add them under the wrong name. |
| T-28-02 | Repudiation | Missing walker descent | mitigate | Task 2 adds defense-in-depth leaf arms to EVERY walker with an existing `case "avatar":` arm. `check:test-types` gate in Task 2's verify catches any newly-added walker in the codebase that omits the arm (the TypeScript exhaustiveness check flags an unhandled `ViewNode` union member at compile time). |
| T-28-03 | Tampering | XSS via user-supplied HTML on wire (Phase 28 T1) | accept | MITIGATED BY DESIGN per D-06: the wire is a markdown STRING on the field's `bind`, NEVER HTML. Zero HTML crosses this interface. This threat is CLOSED at the type contract layer; downstream sanitization concerns (T2 in the phase-level threat model) belong to Plan 28-06 (markdown.ts audit). |
</threat_model>

<verification>
Per-task verifications above. Downstream cross-check: Plan 28-02's .NET Serialization Test will assert that the JSON round-trip of `RichTextFieldNode` + `RichTextToolbarNode` produces byte-identical `"type": "rich-text-field"` / `"type": "rich-text-toolbar"` payloads against the TS side.
</verification>

<success_criteria>
- Both interfaces exported from `viewmodel-shell/src/index.ts`.
- `RichTextTool` closed union exported alongside.
- `ViewNode` union grown by exactly 2 arms.
- Both new discriminators handled as leaf arms in every walker with an `case "avatar":` counterpart.
- `npm run build` + `npm run check:test-types` both exit 0.
</success_criteria>

<output>
Create `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-01-SUMMARY.md` when done. Record: the exact insertion line numbers for the three new exports in `index.ts`, the count of walker arms added to `server.ts`, and the exit status of `npm run build` + `npm run check:test-types`.
</output>
