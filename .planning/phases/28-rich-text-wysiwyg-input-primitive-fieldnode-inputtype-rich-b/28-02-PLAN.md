---
phase: 28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell-dotnet/ViewModels.cs
  - viewmodel-shell-dotnet/Tests/RichTextSerializationTests.cs
autonomous: true
requirements: [RICH-01, RICH-04]

must_haves:
  truths:
    - "Two new records added to `viewmodel-shell-dotnet/ViewModels.cs`: `RichTextFieldNode` (leaf-input primitive) and `RichTextToolbarNode` (Route B composite), each derived from `ViewNode`, byte-aligned with the TS twin from Plan 28-01. Field ordering + optionality mirrors Analog C in 28-PATTERNS.md verbatim."
    - "Two new `[JsonDerivedType]` discriminator attributes added to the ViewNode polymorphism block (~line 791-833): `[JsonDerivedType(typeof(RichTextFieldNode), \"rich-text-field\")]` and `[JsonDerivedType(typeof(RichTextToolbarNode), \"rich-text-toolbar\")]`."
    - "Two new C# enums added: `RichTextTool` (11 members matching D-08 floor: Bold, Italic, Link, BulletList, OrderedList, Heading1, Heading2, Heading3, InlineCode, CodeBlock, Blockquote) and `RichTextToolbarSize` (Compact, Expanded). Each enum uses the shipped `KebabEnum<T>` converter (grep the file for existing precedent — `ChartKind`, `MessageRole`, `StatusKind` all follow this pattern). Per closed-union-must-be-enum discipline (composite-nodes-layer.md §6); string? would silently accept invalid values on the .NET side (class-1 defect from gotcha #9 audit)."
    - "EVERY nullable property (`T?`) on both new records carries `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` per gotcha #8. Applies to: RichTextFieldNode's Label, Placeholder, Toolbar, State; RichTextToolbarNode's Size, Tone, State."
    - "EVERY optional non-nullable bool whose `false` means \"absent\" carries `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)]` per gotcha #8. Applies to: RichTextFieldNode's Required (default false), Disabled (default false). Matches the shipped FieldNode.Required / FieldNode.Disabled posture."
    - "REQUIRED fields carry NO ignore condition: RichTextFieldNode's Name + Bind; RichTextToolbarNode's Tools (IReadOnlyList<RichTextTool>). Bind is `string` (not `string?`) because a bindless rich text field has no value round-trip and is meaningless."
    - "Tree validator arms added to both walker switches in ViewModels.cs (`Collect` at ~line 3499 and `WalkForSectionAction` at ~line 3112). Both new records handled as leaf arms (`case RichTextFieldNode: break;` and `case RichTextToolbarNode: break;`) — mirrors AvatarNode's terminal-leaf posture (Analog D template)."
    - "Serialization test file `viewmodel-shell-dotnet/Tests/RichTextSerializationTests.cs` exists and asserts: (a) minimum-shape RichTextFieldNode (only Name + Bind) emits `{\"type\":\"rich-text-field\",\"name\":\"foo\",\"bind\":\"draft\"}` with NO null fields; (b) full-shape RichTextFieldNode with Toolbar populated emits all set fields + omits every unset optional; (c) RichTextToolbarNode with just Tools emits `{\"type\":\"rich-text-toolbar\",\"tools\":[\"bold\",\"italic\",...]}` in kebab-case; (d) `Required = false` and `Disabled = false` are ABSENT from the emitted JSON (WhenWritingDefault gate); (e) `Required = true` and `Disabled = true` ARE present; (f) a findNulls scan across every fixture-emitted JSON asserts zero literal `null` values (defense against the ~130 violations gotcha #8 documents)."
    - "`dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` succeeds. `dotnet test viewmodel-shell-dotnet/Tests` succeeds with the new RichTextSerializationTests passing."
  artifacts:
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "RichTextFieldNode + RichTextToolbarNode records; RichTextTool + RichTextToolbarSize enums; 2 [JsonDerivedType] discriminators; 2 leaf walker arms in each of the 2 walker switches."
      contains: "record RichTextFieldNode"
    - path: "viewmodel-shell-dotnet/Tests/RichTextSerializationTests.cs"
      provides: "Serialization tests covering shape, kebab-case enum values, null-omission, WhenWritingDefault gate on Required/Disabled, findNulls defense."
      contains: "RichTextSerializationTests"
  key_links:
    - from: "viewmodel-shell-dotnet/ViewModels.cs (2 new records)"
      to: "viewmodel-shell/src/index.ts (Plan 28-01 TS twin)"
      via: "shared wire discriminator strings + optional-fields absence rule; parity gate in Plan 28-07 enforces byte-equality"
      pattern: "rich-text-(field|toolbar)"
    - from: "viewmodel-shell-dotnet/ViewModels.cs (walker arms)"
      to: "Tree validation on the .NET server side (BuildVm → response.Validate())"
      via: "case RichTextFieldNode: / case RichTextToolbarNode: — leaf arm defense-in-depth"
      pattern: "case RichText(Field|Toolbar)Node"
---

<objective>
Mirror Plan 28-01's TS types on the .NET side: add `RichTextFieldNode` + `RichTextToolbarNode` records + `RichTextTool` + `RichTextToolbarSize` enums + 2 `[JsonDerivedType]` discriminators + 2 leaf walker arms in EACH of the two walker switches in `viewmodel-shell-dotnet/ViewModels.cs`, and land a `RichTextSerializationTests.cs` test file that proves byte-alignment with the TS twin. Runs in parallel with Plan 28-01 (file-disjoint).

Purpose: gotcha #8's null-omission + gotcha #9's class-1 defect (both backends agreeing on the wrong thing) are only checkable if the .NET twin exists with the correct `[JsonIgnore]` attributes and the closed-union enums. Landing this in Wave 1 unblocks Plan 28-07 (parity fixture) which requires both backends to emit the new nodes.

Output: .NET wire types complete + tested. Parity fixture in Plan 28-07 can safely emit the new nodes from the .NET FeatureProbeController.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-CONTEXT.md
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md
@AGENTS.md
</context>

<interfaces>
<!-- Reference: 28-PATTERNS.md Analog C — the exact shape of both records + both enums +
     the placement of every JsonIgnore attribute. Do NOT paraphrase the analog. -->

Analog for the composite record: `MessageNode` at `viewmodel-shell-dotnet/ViewModels.cs:2222-2252`.
Analog for the leaf-input record: `AvatarNode` at `viewmodel-shell-dotnet/ViewModels.cs:2111-2130`.
Analog for the closed-enum + KebabEnum converter: `MessageRole`, `ChartKind`, `StatusKind` — grep for `KebabEnum<` in ViewModels.cs for the exact converter application pattern.

The two walker switches to update:
- `Collect` at ~ViewModels.cs:3206 (composite descend) + ~ViewModels.cs:3499 (leaf terminals — `case AvatarNode:` sits at 3499-3502).
- `WalkForSectionAction` at ~ViewModels.cs:2953 (composite) + ~ViewModels.cs:3112 (leaf terminals — `case AvatarNode:` sits at 3112-3116).

`[JsonDerivedType]` block: the file's ViewNode polymorphism attribute list sits at ~ViewModels.cs:791-833. Add both new discriminators there (typically alphabetized against the neighbor set; place adjacent to the other Route B composite discriminators).

Exact record synthesis (from 28-PATTERNS.md Analog C, "Shape the planner should synthesize"):

```csharp
public enum RichTextTool { Bold, Italic, Link, BulletList, OrderedList,
                            Heading1, Heading2, Heading3, InlineCode,
                            CodeBlock, Blockquote }
public enum RichTextToolbarSize { Compact, Expanded }

public record RichTextFieldNode(
    string Name,
    string Bind,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Label = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Placeholder = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] RichTextToolbarNode? Toolbar = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool Required = false,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool Disabled = false,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? State = null
) : ViewNode;

public record RichTextToolbarNode(
    IReadOnlyList<RichTextTool> Tools,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] RichTextToolbarSize? Size = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] Tone? Tone = null,
    [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? State = null
) : ViewNode;
```

TSDoc/comments on each record MUST mirror the TS twin's TSDoc content (D-01/D-02 rationale, anticipated customization surface note, D-06 markdown-string wire note, D-08 floor). Use C# doc-comments (`///`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add RichTextFieldNode + RichTextToolbarNode records + enums + 2 JsonDerivedType discriminators in ViewModels.cs</name>
  <files>viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell-dotnet/ViewModels.cs (READ the [JsonDerivedType] attribute block at ~lines 791-833; READ MessageNode at ~lines 2222-2252 for the composite record shape template; READ AvatarNode at ~lines 2111-2130 for the leaf-node record shape template; grep for `KebabEnum<` to see how the existing enums use the shipped converter — MessageRole is the closest peer)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog C — the exact record + enum bodies to synthesize)
    - AGENTS.md gotcha #8 (the JsonIgnore attribute discipline — do NOT rely on host JsonSerializerOptions; every nullable MUST carry the attribute regardless)
  </read_first>
  <action>
    Perform 4 edits to `viewmodel-shell-dotnet/ViewModels.cs`:

    (a) Add two `[JsonDerivedType]` attributes to the ViewNode polymorphism block (~lines 791-833). Place adjacent to other Route B composite discriminators (near MessageNode/UserRowNode/SettingRowNode entries):
    ```csharp
    [JsonDerivedType(typeof(RichTextFieldNode),   "rich-text-field")]
    [JsonDerivedType(typeof(RichTextToolbarNode), "rich-text-toolbar")]
    ```

    (b) Add the two new enums adjacent to the other closed-union enums (grep for `public enum MessageRole` or `public enum ChartKind` to locate the enum cluster):
    ```csharp
    public enum RichTextTool { Bold, Italic, Link, BulletList, OrderedList,
                                Heading1, Heading2, Heading3, InlineCode,
                                CodeBlock, Blockquote }
    public enum RichTextToolbarSize { Compact, Expanded }
    ```
    IF the existing enum cluster uses a `[JsonConverter(typeof(KebabEnum<...>))]` attribute (grep to verify — the file's precedent is that closed-union enums crossing the wire declare the converter at the enum site), apply the same attribute pattern to both new enums so the wire values emit as kebab-case (`bold`, `bullet-list`, `heading-1`, `compact`, etc.). If the file uses a different converter mechanism (e.g. a global `JsonNamingPolicy`), MIRROR that mechanism exactly — do not invent a new one. VERIFY the kebab-case output in Task 2's serialization test (this is where a wrong converter would fail loudly).

    (c) Add the two new records adjacent to the other Route B composite records (near MessageNode / UserRowNode / SettingRowNode). Copy the exact bodies from the `<interfaces>` block above. Add C# doc comments (`///`) that mirror the TS twin's TSDoc content:
       - RichTextFieldNode doc: `v8.2.0 (RICH-01)`; D-01 dedicated-node rationale; D-06 markdown-string wire; D-04 lazy-bundling; D-08 floor; D-Q4 sanitization; deferred customization surface enumeration.
       - RichTextToolbarNode doc: `v8.2.0 (RICH-02)`; D-03 tasting-before-ship gate; typed-slots §3 rules; D-02 anticipated axes (visibleTools, headings-dropdown, position, compact/expanded, overflow-to-kebab) that the shape MUST not preclude.

    (d) Verify build: `export PATH="$HOME/.dotnet:$PATH" && dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal`. Must exit 0.

    HARD CONSTRAINTS (from gotcha #8 + Analog C):
    - Do NOT type `Toolbar` as `ViewNode?` — use the concrete `RichTextToolbarNode?` (a consumer would never legitimately pass a random ViewNode as a toolbar). Contrast is the `IReadOnlyList<ViewNode>? Actions` pattern on MessageNode which IS polymorphic on purpose; the toolbar slot is not.
    - Do NOT type `Tools` as `string[]` or `List<string>` — use `IReadOnlyList<RichTextTool>` (the closed enum). `string[]` reverts to the class-1 defect gotcha #9 documents.
    - Do NOT omit the `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` on ANY nullable — the intrinsic-null-omission rule from gotcha #8 covers ViewNode's OWN nullable properties, but every new attribute placement is explicit per that gotcha's `Maintainer rule` section.
    - Do NOT omit the `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)]` on `Required = false` and `Disabled = false` — matching the shipped FieldNode.Required / FieldNode.Disabled / AlertNode.Dismissible posture.
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal 2&gt;&amp;1 | tail -5 &amp;&amp; grep -c 'public record RichTextFieldNode' viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c 'public record RichTextToolbarNode' viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c 'public enum RichTextTool' viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c 'public enum RichTextToolbarSize' viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c 'JsonDerivedType(typeof(RichTextFieldNode)' viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c 'JsonDerivedType(typeof(RichTextToolbarNode)' viewmodel-shell-dotnet/ViewModels.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'public record RichTextFieldNode' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 1.
    - `grep -c 'public record RichTextToolbarNode' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 1.
    - `grep -c 'public enum RichTextTool' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 1.
    - `grep -c 'public enum RichTextToolbarSize' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 1.
    - `grep -c 'JsonDerivedType(typeof(RichTextFieldNode)' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 1.
    - `grep -c 'JsonDerivedType(typeof(RichTextToolbarNode)' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 1.
    - Around each nullable property (`? Label`, `? Placeholder`, `? Toolbar`, `? Size`, `? Tone`, `? State`), the preceding attribute is `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` — verify with a scoped grep: `grep -B1 '? Label = null' viewmodel-shell-dotnet/ViewModels.cs | grep -c 'WhenWritingNull'` returns exactly 1 (repeat conceptually for each).
    - Around each `bool Required = false` and `bool Disabled = false` on the RichTextFieldNode record, the preceding attribute is `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)]`.
    - `dotnet build` exit 0.
    - No literal `string?` used where a closed-union `RichTextTool` / `RichTextToolbarSize` / `Tone` should be (grep the new records for `string?` — the only permitted `string?` is `Label`, `Placeholder`, `State`).
  </acceptance_criteria>
  <done>
    .NET wire types complete. Both records byte-aligned with the TS twin. Both closed unions are real C# enums (not string?). Every nullable carries the correct JsonIgnore attribute. Build clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add leaf-arm walker cases in Collect + WalkForSectionAction + RichTextSerializationTests.cs</name>
  <files>viewmodel-shell-dotnet/ViewModels.cs, viewmodel-shell-dotnet/Tests/RichTextSerializationTests.cs</files>
  <read_first>
    - viewmodel-shell-dotnet/ViewModels.cs (READ `case AvatarNode:` in Collect at ~line 3499-3502 for the leaf-arm template; READ `case AvatarNode:` in WalkForSectionAction at ~line 3112-3116 for the second walker's leaf-arm template; grep for `case MessageNode message:` at ~line 3206 to see the composite descend counterpart — but per Analog D, both new nodes are LEAVES in the walker)
    - Existing test files under `viewmodel-shell-dotnet/Tests/` — read one existing serialization test file (grep for `Fact` / `Assert.Contains(` / `JsonSerializer.Serialize`) for the shipped test structure + JsonSerializerOptions configuration pattern; a good peer is `CompositeStateAxisSerializationTests.cs` (Phase 27) if it exists, or any file matching `*SerializationTests.cs`.
    - AGENTS.md gotcha #7 (test project needs `global using Xunit;` in GlobalUsings.cs + `<FrameworkReference Include="Microsoft.AspNetCore.App" />` — verify the test project's `GlobalUsings.cs` already covers this, otherwise the test file additions will not compile)
  </read_first>
  <action>
    Two logical edits:

    **(a) Add walker leaf arms in ViewModels.cs.**

    In `Collect` at ~line 3499 (the leaf-terminals cluster containing `case AvatarNode:`), add immediately after the `case AvatarNode:` arm:
    ```csharp
    case RichTextFieldNode:
        // v8.2.0 (RICH-01) — RichTextFieldNode is a leaf (no action-bearing
        // descendants; the `Toolbar` slot's inner `Tools` are enum tokens
        // naming built-in TipTap chain commands, resolved CLIENT-SIDE).
        // Mirrors the TS twin `case "rich-text-field"` arm in server.ts.
        break;
    case RichTextToolbarNode:
        // v8.2.0 (RICH-02) — RichTextToolbarNode is a leaf (Tools are enum
        // tokens naming built-in TipTap chain commands, resolved
        // CLIENT-SIDE). Mirrors the TS twin arm.
        break;
    ```

    In `WalkForSectionAction` at ~line 3112, add the same two `case … break;` arms after the `case AvatarNode:` arm — same shape, same comment provenance.

    **(b) Create `viewmodel-shell-dotnet/Tests/RichTextSerializationTests.cs`.**

    File contents: an xUnit test class with these tests (each `[Fact]`), matching the shape of the existing serialization test peers:

    1. `RichTextFieldNode_MinimumShape_EmitsExactly_TypeNameBind` — construct `new RichTextFieldNode(Name: "note", Bind: "draft")`; serialize with `JsonSerializerOptions { WriteIndented = false, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull }`; assert output equals (order-independent JSON compare — `JsonDocument.Parse` + property enumeration): exactly `{"type":"rich-text-field","name":"note","bind":"draft"}`. NO null keys. NO absent-when-default `required` / `disabled` keys.

    2. `RichTextFieldNode_FullShape_EmitsAllSetFields_OmitsUnset` — construct with all fields set including `Toolbar: new RichTextToolbarNode(Tools: [Bold, Italic], Size: Expanded, Tone: Info, State: "active")`; assert the serialized string CONTAINS each set field's kebab-case key + value AND that no field is emitted as `null`. Use `findNulls`-style scan: `!json.Contains("\":null")` && `!json.Contains("\": null")`.

    3. `RichTextFieldNode_Required_False_IsAbsent` — construct with `Required: false`; assert `!json.Contains("\"required\":")`. Then construct with `Required: true`; assert `json.Contains("\"required\":true")`. Same for `Disabled`.

    4. `RichTextToolbarNode_Tools_EmitAsKebabCase` — construct `new RichTextToolbarNode(Tools: [Bold, BulletList, Heading1, InlineCode, CodeBlock, Blockquote, Link, OrderedList, Heading2, Heading3, Italic])`; assert the serialized `tools` array contains the substrings `"bold"`, `"bullet-list"`, `"heading-1"`, `"inline-code"`, `"code-block"`, `"blockquote"`, `"link"`, `"ordered-list"`, `"heading-2"`, `"heading-3"`, `"italic"` — proves the enum converter emits kebab-case.

    5. `RichTextToolbarNode_Size_EmitAsKebabCase` — construct with `Size: Compact`; assert the serialized output contains `"size":"compact"`. Then `Size: Expanded` → `"size":"expanded"`.

    6. `RichTextFieldNode_UsesPolymorphicDiscriminator_ThroughViewNode` — serialize the node AS a `ViewNode` (not the concrete type): `JsonSerializer.Serialize<ViewNode>(node, options)`. Assert the output contains `"type":"rich-text-field"` (verifies the `[JsonDerivedType]` discriminator emits). This is the load-bearing check that protects against gotcha #1 (missing "type" field when serialized through the base type).

    7. `RichTextFieldNode_FindNulls_ScanAllShapes` — a defense-against-gotcha-#8 scan: construct all-permutations shape (each field either set-or-null, each bool either true-or-false), serialize each, aggregate all output strings, assert `combined.IndexOf("\":null") < 0` — zero literal `null` values in ANY shape. Mirrors the findNulls invariant per gotcha #9's class-2 defect protection.

    Verify: `export PATH="$HOME/.dotnet:$PATH" && dotnet test viewmodel-shell-dotnet/Tests --filter FullyQualifiedName~RichTextSerializationTests -v minimal`. All 7 tests must pass.
  </action>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --filter 'FullyQualifiedName~RichTextSerializationTests' --nologo -v minimal 2&gt;&amp;1 | tail -15 &amp;&amp; grep -c 'case RichTextFieldNode:' viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c 'case RichTextToolbarNode:' viewmodel-shell-dotnet/ViewModels.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case RichTextFieldNode:' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 2 (one arm in each of the 2 walkers).
    - `grep -c 'case RichTextToolbarNode:' viewmodel-shell-dotnet/ViewModels.cs` returns exactly 2.
    - `grep -c 'case AvatarNode:' viewmodel-shell-dotnet/ViewModels.cs` also returns exactly 2 — verify the two counts are equal (cross-check the walker count).
    - File `viewmodel-shell-dotnet/Tests/RichTextSerializationTests.cs` exists.
    - `dotnet test viewmodel-shell-dotnet/Tests --filter FullyQualifiedName~RichTextSerializationTests` reports 7 passing tests, 0 failing.
    - The full `dotnet test viewmodel-shell-dotnet/Tests` suite still reports 0 failing tests (adding these does not break neighbors).
  </acceptance_criteria>
  <done>
    Both .NET walkers descend into (via no-op leaf arm) both new nodes. Serialization tests prove: byte-alignment shape, kebab-case enum emission, WhenWritingDefault gate on Required/Disabled, WhenWritingNull gate on all nullables, polymorphic-discriminator emission, findNulls defense.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .NET wire types → JSON serialization | Missing `[JsonIgnore(WhenWritingNull)]` on any nullable silently emits `"field":null` and breaks byte-alignment with the TS twin — the exact class-2 defect gotcha #8 exists to prevent (the 2026-07-16 audit found ~130 violations). Task 2's findNulls scan defends against this. |
| Closed-union enum → wire value | Using `string?` where a closed enum belongs (RichTextTool, RichTextToolbarSize) silently accepts invalid values on the .NET side — the exact class-1 defect gotcha #9's audit documents (37 of 37 closed unions were open on .NET as of 2026-07-16). Task 1's enum + KebabEnum converter closes this. |
| Test project compile | Test project needs `global using Xunit;` + `<FrameworkReference Include="Microsoft.AspNetCore.App" />` per gotcha #7. Read_first step in Task 2 verifies the project's GlobalUsings.cs already provides these. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | .NET null emission drift | mitigate | Task 2 test #7 (`RichTextFieldNode_FindNulls_ScanAllShapes`) asserts zero literal `null` across every-permutation shape — invariant defense per gotcha #9 class-2 (the exact protection style parity/normalize.ts's null-drop was meant to provide but doesn't). |
| T-28-05 | Tampering | .NET closed-union open-string drift | mitigate | Task 1 uses real C# enums (`RichTextTool`, `RichTextToolbarSize`), not `string?` — closed at the type layer; the compiler rejects an invalid value. Task 2 test #4/#5 verifies the wire emits kebab-case (the shape agents/consumers key off). |
| T-28-06 | Repudiation | Polymorphic discriminator omission | mitigate | Task 2 test #6 serializes the node AS a `ViewNode` (base type) and asserts `"type":"rich-text-field"` appears in the output — protects against gotcha #1 (the "Ok() drops DeclaredType" hazard) at the type level. |
</threat_model>

<verification>
Per-task verifications above. Downstream: Plan 28-07 (parity) will run the full cross-backend byte-diff which is where a TS-vs-.NET drift on either new node would surface. Task 2's tests are the .NET-only guardrails that fire BEFORE parity does.
</verification>

<success_criteria>
- Both records + both enums + 2 discriminators added to ViewModels.cs.
- Both walker switches carry both new leaf arms.
- Serialization test file exists with 7 passing tests.
- `dotnet build` + `dotnet test viewmodel-shell-dotnet/Tests` both green.
</success_criteria>

<output>
Create `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-02-SUMMARY.md`. Record: line numbers of the 2 new records + 2 new enums + 2 new [JsonDerivedType] attributes + 4 new walker case arms; the test file's absolute path; the `dotnet test` output showing 7 passing.
</output>
