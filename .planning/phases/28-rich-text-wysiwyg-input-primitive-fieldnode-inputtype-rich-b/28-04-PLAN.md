---
phase: 28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - demo/RichTextTasting-bun/package.json
  - demo/RichTextTasting-bun/tsconfig.json
  - demo/RichTextTasting-bun/vite.config.ts
  - demo/RichTextTasting-bun/server.ts
  - demo/RichTextTasting-bun/index.html
  - demo/RichTextTasting-bun/src/main.ts
  - demo/RichTextTasting-bun/src/panel-primitives.html
  - demo/RichTextTasting-bun/src/panel-composite.html
  - demo/RichTextTasting-bun/src/panel-primitives.ts
  - demo/RichTextTasting-bun/src/panel-composite.ts
autonomous: false
requirements: [RICH-02, RICH-06]

must_haves:
  truths:
    - "New demo `demo/RichTextTasting-bun/` exists and mirrors the Phase 27 `demo/StateAxisVerification-bun/` structure: Bun.serve process serving `/vms/default.css`, `/vms/themes/<name>.css`, `/api/themes`; Vite for `index.html` + `/src/main.ts` bundling; standard Bun + Vite trio (`package.json`, `tsconfig.json`, `vite.config.ts`)."
    - "The tasting page presents the SIDE-BY-SIDE BEFORE/AFTER comparison the D-03 governance rule requires: LEFT panel = a rich text input hand-composed from EXISTING primitives (SectionNode(layout:row) header + ButtonNode toolbar + FieldNode(inputType:textarea) editor host — the pretty-bad-approximation baseline); RIGHT panel = the proposed RichTextFieldNode + RichTextToolbarNode composite rendering as it WILL ship in Plan 28-05."
    - "IFRAME-SCOPED per banked v8.0.3 lesson (2026-07-30) — MANDATORY. Panels sit in SEPARATE iframe documents (src/panel-primitives.html, src/panel-composite.html), each with its OWN link/script includes. A shared top-level asset would cross-contaminate the comparison. Parent index.html contains ONLY the iframe scaffolding (grid layout, theme switcher chrome, framework attribution) — no VMS mount, no shared CSS/JS."
    - "The BEFORE panel renders through the REAL VMS renderer + REAL default.css with a VMS tree composing primitives: a SectionNode(layout:row) containing ButtonNodes labelled B, I, Link, Bullets, Numbers, H1, H2, H3, Code, Block, Quote above a FieldNode(inputType:textarea, bind:draft). Buttons are functionally dead — they exist to demonstrate the visual/UX bar the primitive-composed shape hits."
    - "The AFTER panel renders a REAL RichTextFieldNode using Plan 28-03's renderer WITH an explicit toolbar: RichTextToolbarNode { tools: [D-08 floor], size: expanded } slot so the composite's rendering path is exercised. Since Plan 28-05's real body has not landed, the AFTER panel uses Plan 28-03's INTERIM richTextToolbar() placeholder — Ashley eyeballs (a) the CSS + layout of the composite's toolbar strip when composed alongside a real editor, (b) the semantic HTML the composite emits, and (c) how it feels relative to the primitive-composed baseline. Feedback drives the composite's final shape in Plan 28-05."
    - "Served on the tailnet via `bun run demo/RichTextTasting-bun/server.ts` on port 3021 (or first available >= 3021 if 3021 collides). PID recorded to demo/RichTextTasting-bun/server.pid for later cleanup by the release plan."
    - "Ashley eyeballs both panels and signs off with `taste ok — proceed to composite` OR responds with concrete visual adjustments to bake into Plan 28-05."
    - "Plan HALTS at the sign-off checkpoint. Plan 28-05 does NOT begin until this plan's SUMMARY records `taste ok` (or equivalent unambiguous sign-off text)."
    - "SUMMARY records: tasting URL, server PID, Ashley's verbatim sign-off response (or the list of adjustments), any screenshots she provides."
  artifacts:
    - path: "demo/RichTextTasting-bun/server.ts"
      provides: "Bun.serve process that serves the tasting page + REAL shipped default.css/themes + REAL VMS bundle from viewmodel-shell/dist."
      contains: "Bun.serve"
    - path: "demo/RichTextTasting-bun/index.html"
      provides: "Parent-page iframe scaffolding + theme switcher chrome; no VMS mount."
      contains: "iframe"
    - path: "demo/RichTextTasting-bun/src/panel-primitives.html"
      provides: "BEFORE panel — primitives-composed rich text approximation."
      contains: "panel-primitives"
    - path: "demo/RichTextTasting-bun/src/panel-composite.html"
      provides: "AFTER panel — RichTextFieldNode + RichTextToolbarNode composite."
      contains: "panel-composite"
    - path: ".planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-04-SUMMARY.md"
      provides: "Ashley's verbatim sign-off text OR the list of adjustments to bake into Plan 28-05."
      contains: "taste"
  key_links:
    - from: "demo/RichTextTasting-bun/ (tasting page)"
      to: "Plan 28-05 (composite implementation)"
      via: "Ashley's sign-off is Plan 28-05's blocking depends_on"
      pattern: "taste ok|proceed to composite"
    - from: "Route B governance rule (.planning/design/composite-nodes-layer.md §2)"
      to: "demo/RichTextTasting-bun/ (tasting served BEFORE composite lands)"
      via: "D-03 mandate + AGENTS.md 'earned-a-composite' section"
      pattern: "earned.*composite|tasting"
---

<objective>
Serve the D-03 Route B tasting page on the tailnet — side-by-side before/after per the earned-a-composite governance rule (.planning/design/composite-nodes-layer.md §2; AGENTS.md §Route B composite-nodes layer). Ashley visually signs off on the composite's shape BEFORE Plan 28-05 lands the wire type + renderer changes. This plan carries `autonomous: false` because that visual sign-off is legitimate design taste — the one class of checkpoint the banked directive (2026-07-31) explicitly protects. Everything upstream (Plans 28-01/02/03) is machine-verifiable and lands autonomously.

Purpose: Every prior Route B composite (COMP-05..13a, Phases 24-25) followed this ritual, and the ritual has caught real design issues (Ashley's Phase 24 feedback on typography weight/spacing shaped the final MessageNode slot). Skipping the tasting means either the composite ships in the wrong shape and needs a v8.2.1 CSS retune, or (worse) the shape lands and consumers work around it — the exact loop Route B governance exists to prevent.

Output: A live tasting URL Ashley visits; a verbatim sign-off (or a list of adjustments) recorded to 28-04-SUMMARY.md; Plan 28-05 unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-CONTEXT.md
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md
@.planning/design/composite-nodes-layer.md
@AGENTS.md
</context>

<interfaces>
Analog: demo/StateAxisVerification-bun/ (Phase 27) — the exact structural template. Read that demo end-to-end before scaffolding this one.

Structural files to mirror (from demo/StateAxisVerification-bun/):
- package.json — Bun + Vite dev deps + dev/serve scripts.
- tsconfig.json — TypeScript config for the Vite bundle.
- vite.config.ts — Vite config.
- server.ts — Bun.serve handler that serves theme routes.
- index.html — the parent page with theme-switcher chrome + iframe scaffolding.
- src/main.ts — client-side bootstrap for the parent page.

Per-panel files (NEW to this tasting demo — no direct Phase 27 analog because that demo was single-panel):
- src/panel-primitives.html — the BEFORE iframe document; includes /vms/default.css + #theme-css link + script src=/src/panel-primitives.ts; contains a div id=app.
- src/panel-primitives.ts — mounts a ViewModelShell + BrowserAdapter and calls .load() with a fake initial state producing the primitives-composed tree.
- src/panel-composite.html — the AFTER iframe document; parallel structure.
- src/panel-composite.ts — same mount pattern but the tree contains one RichTextFieldNode with an explicit toolbar slot.

The iframe-scoping load-bearing rule (banked v8.0.3 2026-07-30): "A/B panels that hinge on different asset versions MUST be scoped via iframes. A shared script/link at the parent-page level defeats the comparison." Even when both panels use the SAME shipped CSS + SAME renderer bundle, iframes preserve the property that Ashley's feedback on ONE panel cannot bleed into the OTHER via a shared global (e.g. a JS module singleton, a CSS custom property override, a shared theme-css link swap).

Port selection: demo/StateAxisVerification-bun/ uses port 3020 (grep-verify). Use port 3021 (or the first free port). Record the choice in server.ts + the SUMMARY.

Renderer coverage: since Plan 28-05 has not landed, the AFTER panel exercises Plan 28-03's INTERIM richTextToolbar() placeholder + the RichTextFieldNode's DEFAULT-TOOLBAR floor path. Ashley's sign-off shape-checks the WIRE + the field's default toolbar rendering; the composite's own final CSS lands in Plan 28-05. If Ashley requires the composite renderer's final CSS to be visible before sign-off, Plan 28-05 lands a preview branch and Ashley re-eyeballs.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold demo/RichTextTasting-bun/ mirroring StateAxisVerification-bun structure</name>
  <files>demo/RichTextTasting-bun/package.json, demo/RichTextTasting-bun/tsconfig.json, demo/RichTextTasting-bun/vite.config.ts, demo/RichTextTasting-bun/server.ts, demo/RichTextTasting-bun/index.html, demo/RichTextTasting-bun/src/main.ts</files>
  <read_first>
    - demo/StateAxisVerification-bun/package.json + tsconfig.json + vite.config.ts + server.ts + index.html + src/main.ts (READ EACH in a single pass; extract EVERY structural detail — port pattern, script names, tsconfig extends, vite root/server, Bun.serve routing, theme-switcher wiring)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog G — the exact iframe scaffolding shape for index.html)
  </read_first>
  <action>
    Copy the 6 files above from demo/StateAxisVerification-bun/ to demo/RichTextTasting-bun/, then EDIT:

    (a) package.json: change "name" to demo-rich-text-tasting-bun; if scripts reference 3020, sub 3021 uniformly.

    (b) server.ts: change the port to 3021. Preserve theme-serve routes (/vms/default.css, /vms/themes/*, /api/themes). Add fall-through to Vite for /src/panel-primitives.html and /src/panel-composite.html. Write server PID to demo/RichTextTasting-bun/server.pid on start.

    (c) tsconfig.json + vite.config.ts: mirror Phase 27's shape. vite.config.ts needs build.rollupOptions.input listing all three HTML entrypoints (index.html, src/panel-primitives.html, src/panel-composite.html) so each iframe document bundles its own JS entry.

    (d) index.html: parent page. Copy top of Phase 27's index.html (charset, viewport, page meta content=rich-text-tasting, link href=/vms/default.css, #theme-css dynamic link). REPLACE the shipped div id=app mount block with a header + two iframes side-by-side (grid-template-columns: 1fr 1fr, iframe src=/src/panel-primitives.html and /src/panel-composite.html). Keep the theme switcher chrome from Phase 27 verbatim, but ADD listener code that, on theme change, re-broadcasts the theme name to BOTH iframes via postMessage({ type: vms-theme, name: picked }); the iframe children each need a listener to swap their own theme-css link.

    (e) src/main.ts: parent-page bootstrap. Theme switcher wiring + postMessage broadcast to iframes. NO VMS shell mount at the parent level.

    Verify: cd demo/RichTextTasting-bun && bun install; run bun run dev in background; curl -s http://localhost:3021/ returns 200 + parent HTML; curl -s http://localhost:3021/vms/default.css | head -3 returns CSS. Kill the dev server after the check.
  </action>
  <verify>
    <automated>test -f demo/RichTextTasting-bun/package.json &amp;&amp; test -f demo/RichTextTasting-bun/server.ts &amp;&amp; test -f demo/RichTextTasting-bun/index.html &amp;&amp; grep -c 'iframe' demo/RichTextTasting-bun/index.html &amp;&amp; grep -c 'panel-primitives.html' demo/RichTextTasting-bun/index.html &amp;&amp; grep -c 'panel-composite.html' demo/RichTextTasting-bun/index.html &amp;&amp; grep -q '3021' demo/RichTextTasting-bun/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 base files exist under demo/RichTextTasting-bun/.
    - index.html contains at least 2 iframe elements.
    - Both /src/panel-primitives.html and /src/panel-composite.html are referenced as iframe src in index.html.
    - server.ts references port 3021 (or the chosen alternative recorded in SUMMARY).
    - No VMS mount at parent level (grep for `new ViewModelShell(` in main.ts returns 0; parent only handles theme chrome).
  </acceptance_criteria>
  <done>
    Tasting demo scaffolded; parent page shows iframe grid + theme chrome; server serves the shipped CSS + themes; ready for the two per-panel iframe documents in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create panel-primitives + panel-composite iframe documents + client-side mounts</name>
  <files>demo/RichTextTasting-bun/src/panel-primitives.html, demo/RichTextTasting-bun/src/panel-composite.html, demo/RichTextTasting-bun/src/panel-primitives.ts, demo/RichTextTasting-bun/src/panel-composite.ts</files>
  <read_first>
    - viewmodel-shell/src/index.ts (grep for SectionNode, ButtonNode, FieldNode, RichTextFieldNode, RichTextToolbarNode — the type shapes both panels build)
    - demo/StateAxisVerification-bun/src/main.ts (READ the full ViewModelShell + BrowserAdapter mount pattern for the fetch-shim that returns a canned initial VM tree; both panels use this pattern)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog G — the panel structural shape)
  </read_first>
  <action>
    Create 4 files:

    (a) src/panel-primitives.html — minimal HTML: head with link href=/vms/default.css + #theme-css dynamic link (initially data:text/css,) + script type=module src=/src/panel-primitives.ts; body contains only a div id=app + a small header ("BEFORE — primitives composition"). Include the postMessage theme-swap listener inline (a tiny script that listens for message events with type=vms-theme and updates the #theme-css href accordingly).

    (b) src/panel-primitives.ts — mount a ViewModelShell + BrowserAdapter targeting div#app. Use a fetch-shim (the same pattern as demo/StateAxisVerification-bun/src/main.ts) that returns a canned VM tree on GET and echoes state on POST. The tree is a PageNode containing a SectionNode(layout:row, arrange:start, gap: small) with 11 ButtonNodes (labels: B/I/Link/Bullets/Numbers/H1/H2/H3/Code/Block/Quote — each dispatching a dead-action name like `toolbar-bold`; the controller acknowledges but does not transform), followed by a FieldNode { inputType: "textarea", bind: "draft", label: "Notes" }. Initial state: { draft: "# Welcome\n\nType something with **bold** or `code`." }. On dispatch of a toolbar action, echo the same state back (buttons are visually functional — hover/click work — but functionally dead).

    (c) src/panel-composite.html — mirror (a) but src=/src/panel-composite.ts and header "AFTER — RichTextFieldNode + RichTextToolbarNode composite".

    (d) src/panel-composite.ts — mount ViewModelShell + BrowserAdapter. Fetch-shim returns a tree with a PageNode containing exactly ONE RichTextFieldNode:
    ```typescript
    {
      type: "rich-text-field",
      name: "notes",
      bind: "draft",
      label: "Notes",
      toolbar: {
        type: "rich-text-toolbar",
        tools: ["bold","italic","link","bullet-list","ordered-list","heading-1","heading-2","heading-3","inline-code","code-block","blockquote"],
        size: "expanded",
      },
    }
    ```
    Initial state matches Panel A ({ draft: "# Welcome\n\nType something with **bold** or `code`." }). On dispatch, echo state back.

    Both panel .ts files must include the same postMessage listener for theme swaps (parent broadcasts theme; each panel swaps its own #theme-css).

    Verify: bun run dev serves both panels; curl -s http://localhost:3021/src/panel-primitives.html returns 200; curl -s http://localhost:3021/src/panel-composite.html returns 200. Load the parent URL in a browser and confirm BOTH panels render (Ashley can accept a screenshot in lieu of a live check for the plan's own verification — but the tasting checkpoint below is the load-bearing sign-off).
  </action>
  <verify>
    <automated>test -f demo/RichTextTasting-bun/src/panel-primitives.html &amp;&amp; test -f demo/RichTextTasting-bun/src/panel-primitives.ts &amp;&amp; test -f demo/RichTextTasting-bun/src/panel-composite.html &amp;&amp; test -f demo/RichTextTasting-bun/src/panel-composite.ts &amp;&amp; grep -c 'inputType.*textarea' demo/RichTextTasting-bun/src/panel-primitives.ts &amp;&amp; grep -c 'rich-text-field' demo/RichTextTasting-bun/src/panel-composite.ts &amp;&amp; grep -c 'rich-text-toolbar' demo/RichTextTasting-bun/src/panel-composite.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 panel files exist.
    - panel-primitives.ts contains a FieldNode with inputType=textarea (grep returns >= 1) and 11 ButtonNodes (grep for `type: "button"` returns exactly 11).
    - panel-composite.ts contains a RichTextFieldNode with a nested RichTextToolbarNode carrying all 11 D-08 tool names.
    - No top-level import of @tiptap/core or turndown in the panel .ts files — the lazy-load in browser.ts drives loading.
  </acceptance_criteria>
  <done>
    Both iframes render their respective panels through the SAME shipped CSS + SAME renderer bundle; only the wire tree differs. Ready for tailnet serve + Ashley's visual sign-off in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Serve tasting on tailnet + record URL + PID</name>
  <files></files>
  <read_first>
    - demo/StateAxisVerification-bun/README.md OR the equivalent doc if present (the tailnet-serve pattern Phase 27 used — reachable via the box's tailnet hostname on the chosen port)
    - AGENTS.md §"Working agreement for agents" (do NOT push; the server is served locally and reachable over the tailnet — no branch creation, no commit needed to serve)
  </read_first>
  <action>
    Start the tasting server in the background:
    ```bash
    cd demo/RichTextTasting-bun && bun run dev &
    echo $! > server.pid
    sleep 3
    curl -sf http://localhost:3021/ | head -20
    ```

    Confirm both panel URLs return 200:
    ```bash
    curl -sf -o /dev/null -w '%{http_code}\n' http://localhost:3021/src/panel-primitives.html
    curl -sf -o /dev/null -w '%{http_code}\n' http://localhost:3021/src/panel-composite.html
    ```
    Both must print `200`.

    Determine the tailnet hostname of this box (`hostname` or check the tailscale IP). The URL Ashley visits is `http://<tailnet-hostname>:3021/` (e.g. `http://thenasty:3021/`).

    Record in the plan's SUMMARY:
    - The chosen port (3021 or fallback).
    - The exact URL to visit.
    - The server PID (from server.pid).
    - A brief note that the server stays running until Plan 28-11 (release) cleans it up in its final task, per the Phase 27 pattern.
  </action>
  <verify>
    <automated>test -f demo/RichTextTasting-bun/server.pid &amp;&amp; ps -p $(cat demo/RichTextTasting-bun/server.pid) &gt;/dev/null &amp;&amp; curl -sf -o /dev/null -w '%{http_code}' http://localhost:3021/ | grep -q '200' &amp;&amp; curl -sf -o /dev/null -w '%{http_code}' http://localhost:3021/src/panel-primitives.html | grep -q '200' &amp;&amp; curl -sf -o /dev/null -w '%{http_code}' http://localhost:3021/src/panel-composite.html | grep -q '200'</automated>
  </verify>
  <acceptance_criteria>
    - server.pid file exists + the recorded PID is a running process.
    - curl to http://localhost:3021/ returns 200.
    - curl to both panel URLs returns 200.
    - Tasting URL recorded to SUMMARY (the exact string Ashley will visit).
  </acceptance_criteria>
  <done>
    Tasting page live on the tailnet; URL delivered; ready for Ashley's visual sign-off in Task 4.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Route B tasting — Ashley visual sign-off</name>
  <action>See <how-to-verify> below — Ashley eyeballs both panels and signs off. Agent presents the URL + waits for the resume-signal.</action>
  <what-built>
    - Live tasting page at http://&lt;tailnet-hostname&gt;:3021/ (exact URL in Task 3's SUMMARY entry).
    - LEFT panel: rich text input composed from EXISTING primitives (SectionNode(layout:row) + ButtonNodes + FieldNode textarea) — the pretty-bad-approximation baseline.
    - RIGHT panel: RichTextFieldNode + explicit RichTextToolbarNode slot rendering through the just-shipped Plan 28-03 renderer.
    - Iframes are scoped per the banked v8.0.3 lesson — no cross-contamination possible.
    - Same initial content ("# Welcome / bold / code") in both panels for fair comparison.
    - Theme switcher chrome at parent level broadcasts to both iframes for theme comparison.

    This is a LEGITIMATE `autonomous: false` per Ashley's banked 2026-07-31 directive: the Route B tasting is the one class of visual-taste checkpoint that must NOT be stripped.
  </what-built>
  <how-to-verify>
    Ashley, please open the tasting URL in a browser (the URL was recorded in the SUMMARY by Task 3) and eyeball the two panels side-by-side:

    1. **Toolbar visual weight** — does the RichTextToolbarNode's shape (right panel) improve over the primitives-composed baseline (left panel)? Is the button size/spacing appropriate for a rich text editor?

    2. **Editor host** — does the right panel's editor have a legible focus ring, adequate min-height, correct border/padding? Compare to the textarea in the left panel.

    3. **State axis** — try applying `state:"active"` on the composite (by having the plan expose a state toggle if convenient, or just note that STYLE-3 unification is present in the CSS). Does it look right relative to the Phase 27 STYLE-3 rendering on other composites (e.g., in a mental comparison with a UserRow--active)?

    4. **Theme switch** — cycle through 2-3 themes via the parent chrome. Do BOTH panels adapt? Does the composite's toolbar look reasonable under the darker themes?

    5. **Any layout/spacing anomalies** — anything that catches your eye as "off" that would ship as a v8.2.0 regression.

    Then respond with ONE of:
    - `taste ok` — proceed to Plan 28-05 as-designed.
    - `taste ok — with: <list of adjustments>` — proceed to Plan 28-05 BAKING IN those adjustments (list the CSS/wire tweaks explicitly).
    - `taste retry` — the shape needs a re-eyeball; Plan 28-05 lands a preview branch of the composite renderer, and this plan reopens for another sign-off pass.
    - `taste blocked: <reason>` — a genuine design blocker; a follow-up discussion phase is warranted before continuing.

    Per D-03 governance: no matter which response, the composite plan lands only after the sign-off signal is recorded to this plan's SUMMARY.
  </how-to-verify>
  <resume-signal>Type `taste ok` OR `taste ok — with: {list}` to unblock Plan 28-05. Any other response (`taste retry` / `taste blocked`) HALTS the phase; the executor records the response verbatim to SUMMARY and does not proceed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tasting URL → Ashley's browser | Standard local/tailnet HTTP; no auth surface; served from this box's local network. Same posture as Phase 27's tasting/verification pages. |
| Iframe scoping | The iframe boundary prevents JS module singletons or CSS custom-property overrides from cross-contaminating the two panels. Banked v8.0.3 lesson protects the comparison's validity — a defect this class was previously invisible before the rule was banked. |
| Bun.serve process | Runs locally as the operator's user; no persistent state; the PID is captured for later kill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-11 | Tampering | Cross-panel bleed via shared globals | mitigate | Iframe scoping — each panel loads its own `<script>` + `<link>`. The banked v8.0.3 rule (2026-07-30) makes this MANDATORY; a defect in a shared top-level asset is structurally impossible under iframe scoping. |
| T-28-12 | Repudiation | Ashley's sign-off not recorded | mitigate | Task 4's resume-signal + SUMMARY explicitly records the verbatim response text. Plan 28-05's depends_on: ["28-04"] means execute-phase's atomic gate cannot open 28-05 without this plan's SUMMARY landing. |
</threat_model>

<verification>
Task 1-3 machine-verified. Task 4 is the operator sign-off — the plan intentionally halts there. The tasting server keeps running (per Phase 27 pattern) until the release plan (28-11) cleans it up.
</verification>

<success_criteria>
- demo/RichTextTasting-bun/ scaffolded with iframe-scoped panels.
- Tailnet-served on port 3021.
- Ashley's sign-off recorded verbatim in 28-04-SUMMARY.md.
- Plan 28-05 unblocked (or explicitly gated by Ashley's `taste retry` / `taste blocked` response).
</success_criteria>

<output>
Create `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-04-SUMMARY.md`. Record: the chosen port, the tailnet URL, the server PID, Ashley's verbatim sign-off response, and (if she provided adjustments) the concrete list Plan 28-05 must incorporate.
</output>
