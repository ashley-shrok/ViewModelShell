---
phase: 28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b
plan: 08
type: execute
wave: 6
depends_on: ["28-05"]
files_modified:
  - viewmodel-shell/agent-skill.md
  - viewmodel-shell-dotnet/AgentSkill.md
autonomous: true
requirements: [RICH-08]

must_haves:
  truths:
    - "viewmodel-shell/agent-skill.md (the CANONICAL source) gains a new section describing the RichTextFieldNode + RichTextToolbarNode wire shape for agents driving the API cold. Documents: wire discriminators; wire VALUE format (markdown string on bind path per D-06); RichTextFieldNode field list; RichTextToolbarNode field list; the closed RichTextTool union (11 D-08 values); example wire JSON showing initial VM containing a rich text field; example agent flow — read markdown from state[bindPath], POST an action with updated markdown, receive next VM."
    - "Section titled `## Rich text fields (RICH-01, RICH-02)` — placement adjacent to the existing Fields section."
    - "viewmodel-shell-dotnet/AgentSkill.md is BYTE-IDENTICAL to the TS source after the edit — the parity gate (parity/check-skill.ts) diffs both source files AND the served HTTP bodies on the HelpDesk twins."
    - "bun run parity/check-skill.ts (or the runner it ships in) exits 0."
    - "Skill entry accurately reflects the shipped wire — field names, defaults, tool names, and enum casing come FROM the shipped code in Plans 28-01/02/05, not from CONTEXT/PATTERNS docs. Grep audit at the end of Task 1 confirms every field name in the skill matches the exact type declaration in viewmodel-shell/src/index.ts."
  artifacts:
    - path: "viewmodel-shell/agent-skill.md"
      provides: "New section documenting RichTextFieldNode + RichTextToolbarNode + RichTextTool for agents driving the wire cold."
      contains: "rich-text-field"
    - path: "viewmodel-shell-dotnet/AgentSkill.md"
      provides: "Byte-identical copy of the edited agent-skill.md."
      contains: "rich-text-field"
  key_links:
    - from: "viewmodel-shell/agent-skill.md (new rich text section)"
      to: "viewmodel-shell-dotnet/AgentSkill.md"
      via: "parity/check-skill.ts diffs both files + served HTTP bodies"
      pattern: "byte-identical"
    - from: "Skill contract (field names + tool enum values)"
      to: "viewmodel-shell/src/index.ts (Plan 28-01 wire types)"
      via: "the shipped types are the ground truth — the skill mirrors them"
      pattern: "rich-text-(field|toolbar)"
---

<objective>
Add the RICH-01/RICH-02 wire section to `viewmodel-shell/agent-skill.md` (the canonical source), then byte-copy to `viewmodel-shell-dotnet/AgentSkill.md`. Verify parity check-skill green.

Purpose: Per AGENTS.md §"Agent discoverability" maintainer rule, any wire-shape change MUST update the skill in the same change. Rich text adds two new nodes; agents driving VMS apps that render rich text must know how to read/write the markdown-string bind and what tool names populate the toolbar.

Output: Both skill files updated + byte-identical; parity check-skill green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-CONTEXT.md
@.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md
@AGENTS.md
</context>

<interfaces>
Analog: Analog I in 28-PATTERNS.md — the shape of the agent-skill entry.

Reference structure: viewmodel-shell/agent-skill.md already documents Fields, Composites, per-node wire shapes. Read the existing sections top-to-bottom before drafting so tone/structure matches.

Byte-copy discipline (from AGENTS.md maintainer rule):
- Edit ONLY viewmodel-shell/agent-skill.md.
- Then: cp viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md.
- NEVER edit viewmodel-shell-dotnet/AgentSkill.md directly (it is a mirror).

Parity check (parity/check-skill.ts OR the gate baked into parity/run.ts) verifies both source files byte-equal + served HTTP bodies from the HelpDesk twins match the source. If either check fails the parity build fails.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Draft + insert the RICH-01/RICH-02 section into viewmodel-shell/agent-skill.md</name>
  <files>viewmodel-shell/agent-skill.md</files>
  <read_first>
    - viewmodel-shell/agent-skill.md (READ FULLY — 251 lines; extract tone + structure; locate insertion point after the Fields / FormNode section)
    - viewmodel-shell/src/index.ts (READ the FINAL RichTextFieldNode + RichTextToolbarNode + RichTextTool declarations as shipped in Plans 28-01/05 — the ground truth)
    - .planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-PATTERNS.md (Analog I)
  </read_first>
  <action>
    Append a new section anchor `## Rich text fields (RICH-01, RICH-02)` to `viewmodel-shell/agent-skill.md`. Placement: after the existing Fields section, or at the natural per-node reference cluster location — study the file's structure and slot naturally.

    Section content mirrors the tone of the existing sections — brief, operational, no marketing. The content covers:

    1. Two new node types (v8.2.0): rich-text-field (leaf-input primitive) and rich-text-toolbar (composite for the editor's tool strip).

    2. Wire value: the user's rich text serialized as a MARKDOWN STRING on the field's bind path — never HTML. Read via state[bindPath]. Submit via action with updated string at the same path.

    3. `rich-text-field` field reference: name (REQUIRED), bind (REQUIRED), label?, placeholder?, toolbar?, required?, disabled?, state? — with a JSON example.

    4. `rich-text-toolbar` field reference: tools (REQUIRED, closed union), size?, tone?, state? — with a JSON example. Explicitly note that providing this as the `toolbar` slot on a `rich-text-field` REPLACES the framework's default toolbar.

    5. The 11 tool names from D-08 floor listed once (bold, italic, link, bullet-list, ordered-list, heading-1, heading-2, heading-3, inline-code, code-block, blockquote). Note that widening later is additive; agents that don't recognize a new tool name should ignore it, not fail.

    6. Driving-cold section (5 steps): GET, locate rich-text-field in vm, read state[bindPath], update state[bindPath], POST the action envelope with updated state. Close with the note that the bind wire is a plain string — an agent never renders the editor, never dispatches toolbar clicks, and never needs TipTap. Toolbar buttons are CLIENT-SIDE UX; their effect is to mutate the markdown string, which any agent can produce directly.

    Do NOT touch anything OUTSIDE the new section. Preserve the existing 251 lines byte-for-byte.

    Final audit: grep the new section for every field name (name, bind, label, placeholder, toolbar, required, disabled, state, tools, size, tone) and every tool name; grep the SHIPPED viewmodel-shell/src/index.ts for the same set; verify the two lists match byte-for-byte. Any drift is a bug in the skill entry — fix by reading the shipped types.
  </action>
  <verify>
    <automated>grep -c '## Rich text fields' viewmodel-shell/agent-skill.md &amp;&amp; grep -c 'rich-text-field' viewmodel-shell/agent-skill.md &amp;&amp; grep -c 'rich-text-toolbar' viewmodel-shell/agent-skill.md &amp;&amp; grep -c 'markdown string' viewmodel-shell/agent-skill.md &amp;&amp; grep -c 'RICH-01' viewmodel-shell/agent-skill.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Rich text fields` heading exists.
    - `rich-text-field` and `rich-text-toolbar` discriminators are documented (grep >= 3 each).
    - All 11 D-08 tool names listed in the tools example.
    - Wire-value-is-markdown note is explicit (`markdown string` appears >= 1).
    - No changes to sections outside the new one — verify by counting existing anchor headings before/after the edit; they must all still exist unmodified.
  </acceptance_criteria>
  <done>
    Skill entry drafted + inserted; agents have a durable reference for the rich text wire.
  </done>
</task>

<task type="auto">
  <name>Task 2: Byte-copy agent-skill.md to .NET AgentSkill.md + run parity check-skill</name>
  <files>viewmodel-shell-dotnet/AgentSkill.md</files>
  <read_first>
    - AGENTS.md §"Agent skill mount" (canonical source + byte-copy discipline)
    - parity/check-skill.ts (READ FULLY — understand what the check verifies; may be inline in parity/run.ts if not a standalone file)
  </read_first>
  <action>
    (a) Byte-copy: `cp viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md`

    (b) Verify byte-identity: `diff -q viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` returns empty.

    (c) Run parity's skill check. Either standalone (`bun run parity/check-skill.ts`) or as part of `bun run parity/run.ts` — Task 2's read_first identifies which. Must exit 0. Requires PATH exported to reach dotnet.

    (d) If check-skill diffs the SERVED HTTP body from a demo (HelpDesk) using appPreamble, verify no drift from that path either.
  </action>
  <verify>
    <automated>cp viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md &amp;&amp; diff -q viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; (bun run parity/check-skill.ts 2&gt;&amp;1 || bun run parity/run.ts 2&gt;&amp;1) | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `diff -q viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` returns empty (byte-identical).
    - `wc -l viewmodel-shell/agent-skill.md == wc -l viewmodel-shell-dotnet/AgentSkill.md` — line count matches.
    - Parity check-skill (whichever runner invokes it) exits 0.
    - No standalone edits to `viewmodel-shell-dotnet/AgentSkill.md` were made (all changes come from the cp).
  </acceptance_criteria>
  <done>
    .NET skill mirror is byte-identical to the canonical source. Parity gate green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill markdown ↔ shipped wire types | The skill is a promise to agents. Drift between the skill and the shipped types silently breaks agents driving VMS apps cold. Task 1's field-name grep audit catches drift at the source. |
| Skill source ↔ served HTTP body | The demo backends serve the skill markdown at `/.well-known/vms-skill.md` (per AGENTS.md §Agent skill mount). Parity's HTTP diff catches drift between the source file and the served body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-22 | Repudiation | Skill drift from shipped wire | mitigate | Task 1's final grep audit compares the skill's field names against the shipped types byte-for-byte; any drift is fixed by reading the shipped types (not the CONTEXT docs). |
| T-28-23 | Repudiation | .NET copy drift from canonical | mitigate | Task 2's `diff -q` + parity check-skill gate. The rule against editing the .NET copy directly is documented + machine-checked. |
</threat_model>

<verification>
Task 2's parity run is the load-bearing gate.
</verification>

<success_criteria>
- Both skill files updated + byte-identical.
- Parity check-skill green.
- Skill field names/tool names match shipped types byte-for-byte.
</success_criteria>

<output>
Create `.planning/phases/28-rich-text-wysiwyg-input-primitive-fieldnode-inputtype-rich-b/28-08-SUMMARY.md`. Record: line range of the new section, diff between source + copy is empty, parity check-skill exit status.
</output>
