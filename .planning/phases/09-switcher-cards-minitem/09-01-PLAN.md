---
phase: 09-switcher-cards-minitem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/browser.ts
  - viewmodel-shell/styles/default.css
  - viewmodel-shell/test/theme-modifiers.test.ts
  - viewmodel-shell-dotnet/ViewModels.cs
  - demo/FeatureProbe-bun/handler.ts
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
  - parity/fixtures/feature-probe.json
autonomous: true
requirements: [SWITCH-01, SWITCH-02, SWITCH-03]

must_haves:
  truths:
    - "A switcher layout with N equal-weight children sits in ONE row above its content-width threshold and stacks ALL children below it — an atomic flip with no intermediate partial-wrap state, zero @media"
    - "A switcher with threshold set to sm|md|lg|xl flips at 20rem|30rem|40rem|48rem respectively (vms-switch--{token} sets --vms-switch-threshold)"
    - "A switcher with limit set to 2..8 forces every child onto its own full-width line once the child count exceeds limit, regardless of container width"
    - "An omitted threshold emits no class and falls back to the 30rem CSS default (var(--vms-switch-threshold,30rem)); an omitted limit emits no class and applies no count cap — both byte-identical to having no token"
    - "switcher/threshold/limit land byte-identically in TS and .NET (.NET Threshold/Align string?, Limit int?, all [JsonIgnore(WhenWritingNull)]); bun run parity/run.ts is byte-identical green across .NET / Bun / Node"
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "layout union extended with switcher on PageNode+SectionNode; threshold?/limit? closed-union/bounded fields on both"
      contains: "switcher"
    - path: "viewmodel-shell/src/browser.ts"
      provides: "vms-switch--{threshold} / vms-switch-limit--{limit} modifier-class emission in all 5 page/section className builders"
      contains: "vms-switch--"
    - path: "viewmodel-shell/styles/default.css"
      provides: "negative-flex-basis switcher block + .vms-switch--{token} threshold vars + per-n .vms-switch-limit--{n} quantity-query rules"
      contains: "vms-switch-threshold"
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "Threshold string? + Limit int? [JsonIgnore(WhenWritingNull)] fields on PageNode + SectionNode"
      contains: "int? Limit = null"
    - path: "parity/fixtures/feature-probe.json"
      provides: "fixture coverage: bare switcher (no token), switcher+threshold per value, switcher+limit"
      contains: "switcher"
  key_links:
    - from: "demo/FeatureProbe-bun/handler.ts buildVm"
      to: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs BuildVm"
      via: "byte-identical switcher sections carrying threshold/limit"
      pattern: "switcher|Threshold|Limit"
    - from: "viewmodel-shell/src/browser.ts switcher emission"
      to: "viewmodel-shell/styles/default.css .vms-switch--/.vms-switch-limit-- rules"
      via: "modifier class names emitted by the renderer and styled by the CSS"
      pattern: "vms-switch"
---

<objective>
Add the `switcher` layout value (the one completeness primitive a grid provably cannot express — an atomic all-row ↔ all-stack flip via negative-`flex-basis`, zero `@media`) to the `layout` union on `PageNode`/`SectionNode`, plus its bounded `threshold` token (flip width) and bounded `limit` (max-per-row count cap), across both the TS and .NET backends, with the CSS switcher block, renderer modifier-class emission, unit-test coverage of class emission and the byte-identical-when-omitted property, and a widened FeatureProbe parity fixture exercising the new vocabulary across all three backends.

Purpose: Deliver SWITCH-01/02/03 — the framework gains the negative-flex-basis Switcher, the genuine flexbox idiom that earns its own layout value (distinct from `cards` auto-fit, which passes through intermediate column counts; the Switcher flips atomically with no "2-then-1" state). This is the wire + CSS + renderer + parity layer.

Output: Modified framework source (`index.ts`, `browser.ts`, `default.css`), .NET records (`ViewModels.cs`), a widened vitest suite (`theme-modifiers.test.ts`), both FeatureProbe backends rendering the new fields, and the widened `feature-probe.json` parity fixture. `bun run parity/run.ts` byte-identical green. NO version bump, NO publish (release deferred to Phase 11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-switcher-cards-minitem/09-CONTEXT.md
@.planning/design/layout-system-research.md
@.planning/ROADMAP.md
@.planning/phases/08-alignment-enums-layout-policy/08-01-SUMMARY.md

<interfaces>
<!-- Phase 8 is the EXACT template. The arrange/align fields (just landed) are the idiom to mirror for threshold/limit. The layout UNION must additionally gain "switcher". -->

<!-- index.ts PageNode (~L114-127) + SectionNode (~L130-141): both already carry
     layout?: "stack"|"split"|"cards"|"sidebar"|"row" and the new arrange?/align? closed unions.
     ADD "switcher" to the layout union on BOTH nodes, and ADD threshold?/limit? right after align?. -->
PageNode:    layout?: "stack"|"split"|"cards"|"sidebar"|"row"|"switcher"; arrange?; align?; threshold?: "sm"|"md"|"lg"|"xl"; limit?: 2|3|4|5|6|7|8; children;
SectionNode: layout?: "stack"|"split"|"cards"|"sidebar"|"row"|"switcher"; arrange?; align?; threshold?; limit?; id?; collapsible?; flyout?; link?; action?; children;

<!-- ViewModels.cs PageNode record (~L266-290) ends with Align (1.12.0). SectionNode record (~L302-378) ends with Align (1.12.0).
     ADD Threshold (string?) + Limit (int?) trailing optional params after Align on BOTH records, each
     [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]. Layout stays free-form string? (already accepts "switcher"; only the doc-comment needs the mention). -->
PageNode(... string? Align = null, string? Threshold = null, int? Limit = null) : ViewNode
SectionNode(... string? Align = null, string? Threshold = null, int? Limit = null) : ViewNode

<!-- browser.ts: page() ~L263-278 and the FOUR section() className builders — collapsible ~L291-296, flyout ~L323-327, link ~L351-355, base section ~L400-404 — each already chain `${n.arrange ? ...}${n.align ? ...}`. APPEND
     `${n.threshold ? ` vms-switch--${n.threshold}` : ""}${n.limit ? ` vms-switch-limit--${n.limit}` : ""}`
     on the SAME element, in ALL FIVE builders. The `vms-page--{layout}`/`vms-section--{layout}` class already
     emits "switcher" automatically (it's `n.layout && n.layout !== "stack" ? ...`) — no change needed there. -->

<!-- default.css: the .vms-page--sidebar/.vms-section--sidebar flex-wrap block (~L190-214) is the STRUCTURAL ANALOG.
     The arrange/align modifier rules end ~L249. ADD the switcher block + threshold-var rules + per-n limit rules
     AFTER the arrange/align rules (~L250), BEFORE the `.vms-section--card` variant rule (~L251). -->

<!-- FeatureProbe precedent (1.11.0 row / 1.12.0 arrange-align): NEW fields render as STATIC view-shape in
     buildVm/BuildVm, captured by existing GET steps — NO new action arm. bun handler.ts buildVm ends with the
     arrange/align sections + the page children list ~L294-305; .NET BuildVm ends ~L355-366. Mirror that exactly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add switcher layout value + threshold/limit wire fields to both backends</name>
  <files>viewmodel-shell/src/index.ts, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    viewmodel-shell/src/index.ts (PageNode ~L114-128 + SectionNode ~L130-141 — the layout union and the just-landed arrange?/align? closed-union + doc-comment style);
    viewmodel-shell-dotnet/ViewModels.cs (PageNode record ~L266-290 + SectionNode record ~L302-378 — the trailing `[JsonIgnore(WhenWritingNull)] string? Arrange = null` / `Align = null` pattern + the Layout free-form-string field's doc-comment style)
  </read_first>
  <action>
    In `viewmodel-shell/src/index.ts`:
    1. Extend the `layout?` closed union on BOTH the `PageNode` and `SectionNode` interfaces from `"stack" | "split" | "cards" | "sidebar" | "row"` to `"stack" | "split" | "cards" | "sidebar" | "row" | "switcher"`. Update the layout field's doc-comment on both nodes to add the `switcher` description in the existing terse idiom: "switcher" (N equal items flipping all-row ↔ all-stack atomically at a content-width `threshold` — the negative-flex-basis primitive a grid cannot express; distinct from `cards` auto-fit which passes through intermediate column counts).
    2. Add two optional fields to BOTH the `PageNode` interface (after `align?`) and the `SectionNode` interface (after `align?`):
       `threshold?` typed as the closed union `"sm" | "md" | "lg" | "xl"`, and
       `limit?` typed as the closed numeric union `2 | 3 | 4 | 5 | 6 | 7 | 8`.
       Write the doc-comments in the EXACT style of the adjacent `arrange`/`align`/`width` comments: state both are for `layout:"switcher"`; that `threshold` is the content-width FLIP point — a CLOSED size scale (sm→20rem, md→30rem, lg→40rem, xl→48rem) emitted as `.vms-switch--{token}` setting `--vms-switch-threshold`, and an OMITTED `threshold` emits no class so the `var(--vms-switch-threshold, 30rem)` CSS default (30rem) holds = well-defined; that `limit` is the OPTIONAL max-items-per-row count cap — once child count exceeds `limit` every child goes full-width regardless of container width — emitted as `.vms-switch-limit--{n}`, and an OMITTED `limit` emits no class = no count cap. Note `limit` is a bounded numeric union (2..8) per P2 (bounded scalar, not raw CSS). Do NOT edit `src/server.ts` (it re-exports automatically).

    In `viewmodel-shell-dotnet/ViewModels.cs`, add to the `PageNode` record (after `Align`) and the `SectionNode` record (after `Align`) two trailing optional parameters each, mirroring the existing nullable-field pattern:
    `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Threshold = null` and
    `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] int? Limit = null`.
    Write explanatory comments matching the existing `Arrange`/`Align` comment style: `Threshold` is free-form `string?` mirroring the TS closed union `"sm"|"md"|"lg"|"xl"` (closed union enforced TS-side + validated by parity, matching the Layout field's pattern), the switcher flip width; omitted = no class → 30rem CSS default; emits `.vms-switch--{value}`. `Limit` is `int?` mirroring the TS bounded union 2..8, the switcher max-per-row count cap; omitted = no class → no cap; emits `.vms-switch-limit--{n}`. Also append a one-line note to the .NET `Layout` field's doc-comment that `"switcher"` is now an accepted value (1.13.0 — atomic row↔stack flip). `JsonIgnore`-on-null per the file-header rule. Because these are appended as trailing optional params with defaults, existing positional callers keep compiling — Task 3 sets them by NAME.
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && cd ../viewmodel-shell-dotnet && PATH="$HOME/.dotnet:$PATH" dotnet build --nologo -v minimal</automated>
  </verify>
  <done>
    `PageNode`/`SectionNode` `layout` unions include `"switcher"` and both carry `threshold?` (`"sm"|"md"|"lg"|"xl"`) + `limit?` (`2..8`) in TS; .NET records carry `Threshold` `[JsonIgnore(WhenWritingNull)] string?` + `Limit` `[JsonIgnore(WhenWritingNull)] int?`; both backends compile.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit switcher modifier classes + add the negative-flex-basis CSS block</name>
  <files>viewmodel-shell/src/browser.ts, viewmodel-shell/styles/default.css, viewmodel-shell/test/theme-modifiers.test.ts</files>
  <read_first>
    viewmodel-shell/src/browser.ts (page() ~L263-278 className builder; the FOUR section() className builders — collapsible ~L291-296, flyout ~L323-327, link ~L351-355, base ~L400-404 — each already chaining the arrange/align ternaries);
    viewmodel-shell/styles/default.css (the .vms-page--sidebar/.vms-section--sidebar flex-wrap block ~L190-214 as the structural analog; the .vms-page--row/.vms-section--row block ~L216-229; the arrange/align modifier rules ~L231-249; the .vms-section--card variant rule ~L251 as the insertion boundary);
    viewmodel-shell/test/theme-modifiers.test.ts (the ALIGN-01/02/03 describe block — presence-when-set, byte-identical-when-omitted `=== "vms-page vms-page--row"`, and non-base-branch emission cases — plus the renderPage/renderSection helpers ~L43-57)
  </read_first>
  <behavior>
    - page with layout:"switcher" + threshold:"md" ⇒ root className contains "vms-page--switcher" AND "vms-switch--md"
    - section with layout:"switcher" + threshold:"xl" ⇒ section className contains "vms-section--switcher" AND "vms-switch--xl"
    - section with layout:"switcher" + limit:4 ⇒ section className contains "vms-switch-limit--4"
    - page with layout:"switcher" + limit:8 ⇒ root className contains "vms-switch-limit--8"
    - page/section with layout:"switcher" but NEITHER threshold nor limit ⇒ className is EXACTLY "vms-page vms-page--switcher" / "vms-section vms-section--switcher" — contains NO "vms-switch--" and NO "vms-switch-limit--" token (byte-identical-when-omitted)
    - a switcher section with threshold/limit set inside a collapsible/flyout/link branch still carries the modifier classes (emission is present in every section branch)
  </behavior>
  <action>
    In `viewmodel-shell/src/browser.ts`, extend the className string in `page()` and in EVERY `section()` className builder (collapsible, flyout, link, and base-section branches) to APPEND, immediately after the existing `${n.align ? ...}` ternary, on the same element that carries the `vms-page--{layout}` / `vms-section--{layout}` class:
    `${n.threshold ? ` vms-switch--${n.threshold}` : ""}` and `${n.limit ? ` vms-switch-limit--${n.limit}` : ""}`.
    Mirror the existing bare-presence-check ternary idiom exactly. Omitting a field MUST emit no token (the byte-identical guarantee). Do NOT touch the existing `vms-page--{layout}` emission — it already renders `switcher` because `n.layout !== "stack"`.

    In `viewmodel-shell/styles/default.css`, add the switcher rules AFTER the arrange/align modifier rules (~L249) and BEFORE the `.vms-section--card` variant rule (~L251). Add a leading comment block in the existing idiom (mirror the sidebar block's comment style) explaining: the Every-Layout Switcher (zero `@media`) — N equal items flip all-row ↔ all-stack atomically via a negative `flex-basis`; above the threshold the basis goes hugely negative → clamped to 0 → all children share one row; below it the basis goes hugely positive → each child takes a full line → all stack; no partial-wrap state (this is the distinction from `cards` auto-fit). Title/heading keeps its own full-width row like the other presets.
    Emit these rules:
    - `.vms-page--switcher, .vms-section--switcher { display:flex; flex-wrap:wrap; }`
    - `.vms-page--switcher > *, .vms-section--switcher > * { flex-grow:1; flex-basis: calc((var(--vms-switch-threshold, 30rem) - 100%) * 999); }`
    - `.vms-page--switcher > .vms-page__title, .vms-section--switcher > .vms-section__heading { flex: 0 0 100%; }` (title/heading full-width row, mirroring the row/sidebar presets)
    - The threshold-token rules (each sets the custom property the basis calc reads): `.vms-switch--sm { --vms-switch-threshold: 20rem; } .vms-switch--md { --vms-switch-threshold: 30rem; } .vms-switch--lg { --vms-switch-threshold: 40rem; } .vms-switch--xl { --vms-switch-threshold: 48rem; }`
    - The per-`n` limit quantity-query rules — one static rule per allowed n (2..8), forcing every child full-width when the count exceeds n. For each n in 2..8 emit (with m = n+1):
      `.vms-switch-limit--{n} > :nth-last-child(n+{m}), .vms-switch-limit--{n} > :nth-last-child(n+{m}) ~ * { flex-basis: 100%; }`
      i.e. limit--2 uses `:nth-last-child(n+3)`, limit--3 uses `n+4`, … limit--8 uses `n+9`. Add a short comment noting the quantity-query selects all children only when there are MORE than n of them (so a switcher with exactly `limit` children stays on one row; the cap engages on overflow), and that the rule sets `flex-basis:100%` which overrides the calc basis to force vertical regardless of width.
    Add a short comment that an OMITTED threshold/limit emits no class so the 30rem default + no-cap behavior holds = byte-identical.

    In `viewmodel-shell/test/theme-modifiers.test.ts`, add a `SWITCH-01/02` describe block covering the `<behavior>` cases above using the existing renderPage/renderSection helpers and `classList.contains(...)` assertion style: layout:"switcher" + each threshold value ⇒ `vms-switch--{token}` present; layout:"switcher" + a couple of limit values ⇒ `vms-switch-limit--{n}` present; a bare switcher page AND section (layout:"switcher", no threshold, no limit) ⇒ `className === "vms-page vms-page--switcher"` / `"vms-section vms-section--switcher"` (byte-identical-when-omitted — assert exact equality, mirroring the ALIGN bare-row assertion); and a switcher section with threshold/limit inside a collapsible (and a flyout) branch ⇒ both modifier classes present (non-base-branch emission). Follow the existing assertion idioms exactly.
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && npx vitest run test/theme-modifiers.test.ts && npm run check:core-globals</automated>
  </verify>
  <done>
    Renderer emits `vms-switch--{threshold}`/`vms-switch-limit--{limit}` on the layout element in all 5 page/section branches; default.css has the negative-flex-basis `.vms-*--switcher` block, the four `.vms-switch--{token}` threshold-var rules, and seven per-`n` `.vms-switch-limit--{n}` quantity-query rules; the sidebar/row/arrange/align rules are untouched; theme-modifiers.test.ts asserts presence-when-set, exact byte-identical-when-omitted, and non-base-branch emission; vitest + core-globals guard pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Widen FeatureProbe (both backends) + parity fixture for switcher/threshold/limit</name>
  <files>demo/FeatureProbe-bun/handler.ts, demo/FeatureProbe/AspNetCore/FeatureProbeController.cs, parity/fixtures/feature-probe.json</files>
  <read_first>
    demo/FeatureProbe-bun/handler.ts (buildVm arrange/align sections + the page children list ~L294-305 — the 1.12.0 static-view-shape precedent);
    demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (BuildVm arrange/align sections + the pageChildren list / PageNode return ~L345-366);
    parity/fixtures/feature-probe.json (the top-level $comment ~L5 documenting 1.11.0/1.12.0 static coverage; GET steps ~L6-7 — new static sections are captured by existing GETs);
    parity/run.ts and parity/normalize.ts (how GET responses are diffed — property order normalized, values + presence must match)
  </read_first>
  <action>
    Mirror the 1.12.0 arrange/align precedent EXACTLY: render the new fields as STATIC view-shape in both `buildVm`/`BuildVm`, captured by the existing GET steps (NO new action arm).

    In `demo/FeatureProbe-bun/handler.ts` `buildVm`, add new `layout:"switcher"` sections and append them to the page `children` array (after the align sections, before `buildTableSection(state)`). Cover, at minimum:
    (a) a BARE switcher section — `layout:"switcher"` with NEITHER threshold nor limit, ~3-4 equal `LinkNode` children (`external:false` explicit) — proves omitted = no class on the wire;
    (b) one switcher section per `threshold` value (`sm`, `md`, `lg`, `xl`) — same minimal children;
    (c) one switcher section with a `limit` value (e.g. `limit:4`) and >4 children (e.g. 6 small `LinkNode`s) — exercises the count-cap field on the wire.
    Keep children minimal (LinkNodes) so the wire stays small. Build the threshold set with a `["sm","md","lg","xl"] as const` map exactly like the existing `arrangeValues`/`alignValues` maps.

    In `demo/FeatureProbe/AspNetCore/FeatureProbeController.cs` `BuildVm`, add the BYTE-IDENTICAL equivalents using the `SectionNode(...)` constructor with NAMED `Layout:"switcher"`, `Threshold:`, `Limit:` args, `LinkNode` children matching the TS exactly (same labels/hrefs, same order, same headings), and append them to the `pageChildren` list in the SAME position/order as the TS side (after the align sections, before `BuildTableSection(state)`). Build the threshold set with a `new[] { "sm","md","lg","xl" }` `.Select(...)` exactly like the existing `arrangeValues`/`alignValues`. The serialized JSON of every new section MUST match the bun side field-for-field — omitted threshold/limit absent on the wire, set ones present; `Limit:4` serializes as the JSON number `4`.

    In `parity/fixtures/feature-probe.json`: extend the top-level `$comment` to note (mirroring the existing 1.11.0/1.12.0 sentences) that buildVm additionally renders the `switcher` vocabulary as static view-shape — a bare `layout:"switcher"` (no token, proving omitted = absent), one switcher per `threshold` value (sm/md/lg/xl), and a switcher with `limit` set — all captured by the existing GET steps so the byte-identical diff covers `PageNode`/`SectionNode` `layout:"switcher"` + `threshold` + `limit` with no new step. Do NOT add a new fixture FILE (so `parity/backends.json` needs no edit).
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
  </verify>
  <done>
    Both FeatureProbe backends render a bare `switcher` (no token), one switcher per `threshold` value, and a `switcher` with `limit` set — byte-identically; `bun run parity/run.ts` is byte-identical green across dotnet-probe / bun-probe / node-probe (other backends unaffected); the fixture `$comment` documents the new static switcher coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client wire | The `layout`/`threshold`/`limit` values cross the wire and the renderer interpolates them into CSS class names (`vms-switch--${n.threshold}`, `vms-switch-limit--${n.limit}`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | browser.ts class-name interpolation of `threshold`/`limit` | accept | The TS types are a closed union (`"sm"|"md"|"lg"|"xl"`) and a bounded numeric union (2..8); a value outside them is a compile error on every honest backend. A hostile server already fully controls the view tree (it IS the view), so injecting a class string is not a new capability and cannot escape the className attribute into script. No runtime sanitization added — consistent with the Phase-8 `vms-arrange--`/`vms-align--` and the existing `vms-page--{layout}`/`{width}` interpolations (T-08-01 precedent). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure source edits to existing files; no new dependencies. |
</threat_model>

<verification>
- `cd viewmodel-shell && npx tsc --noEmit` — TS compiles with the extended union + threshold/limit fields.
- `cd viewmodel-shell && npx vitest run` — full vitest suite green (theme-modifiers SWITCH cases + no regressions).
- `cd viewmodel-shell && npm run check:core-globals` — core platform-agnosticism guard green (browser.ts excluded; no core change).
- `cd viewmodel-shell-dotnet && PATH="$HOME/.dotnet:$PATH" dotnet build --nologo -v minimal` — .NET compiles with the new records.
- `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` — byte-identical green across .NET / Bun / Node, including the new switcher sections.
</verification>

<success_criteria>
- The `layout` closed union on `PageNode`/`SectionNode` includes `"switcher"` (TS) and the `.NET` `Layout` doc-comment notes it; `threshold?` (`"sm"|"md"|"lg"|"xl"`) + `limit?` (`2..8`) exist on both nodes (TS) and as `[JsonIgnore(WhenWritingNull)]` `string? Threshold` + `int? Limit` (.NET).
- The renderer emits `vms-switch--{threshold}`/`vms-switch-limit--{limit}` on the layout element in all 5 branches; default.css has the negative-flex-basis switcher block (atomic flip, zero `@media`), the four threshold-var rules, and seven per-`n` limit quantity-query rules; sidebar/row/arrange/align rules untouched.
- Omitting `threshold`/`limit` emits no class — proven by a theme-modifiers exact-equality test AND a bare-`switcher` section in the parity fixture (omitted = absent, 30rem default holds).
- `bun run parity/run.ts` byte-identical green; vitest green; both backends build. NO version bump / publish.
</success_criteria>

<output>
Create `.planning/phases/09-switcher-cards-minitem/09-01-SUMMARY.md` when done.
</output>
