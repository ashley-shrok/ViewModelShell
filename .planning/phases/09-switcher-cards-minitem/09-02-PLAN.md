---
phase: 09-switcher-cards-minitem
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/browser.ts
  - viewmodel-shell/styles/default.css
  - viewmodel-shell/test/theme-modifiers.test.ts
  - viewmodel-shell-dotnet/ViewModels.cs
  - demo/FeatureProbe-bun/handler.ts
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
  - parity/fixtures/feature-probe.json
  - demo/Showcase/frontend/src/main.ts
  - AGENTS.md
  - CHANGELOG.md
autonomous: true
requirements: [GRID-01, GRID-02]

must_haves:
  truths:
    - "A cards layout with minItem set to xs|sm|md|lg|xl sets the auto-fit minimum track width to 10rem|13rem|16rem|20rem|24rem (vms-cards-min--{token} sets --vms-card-min on that element)"
    - "An omitted minItem emits no class and renders byte-identical to today's inherited --vms-card-min: 16rem default"
    - "minItem lands byte-identically in TS (closed union) and .NET (MinItem string? [JsonIgnore(WhenWritingNull)]); bun run parity/run.ts is byte-identical green across .NET / Bun / Node"
    - "The Showcase renders a switcher row of ~4 equal cards (resize → atomic flip) and a cards block showing 2-3 minItem values, built only from ViewNodes (zero <style>)"
    - "AGENTS.md Layout policy says switcher is shipped (the Phase-9 forward-reference caveat is dropped); CHANGELOG Unreleased gains a Phase 9 subsection; NO version bump, NO publish"
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "minItem? closed-union field on PageNode + SectionNode"
      contains: "minItem"
    - path: "viewmodel-shell/src/browser.ts"
      provides: "vms-cards-min--{minItem} modifier-class emission in all 5 page/section className builders"
      contains: "vms-cards-min--"
    - path: "viewmodel-shell/styles/default.css"
      provides: "five .vms-cards-min--{token} rules setting --vms-card-min"
      contains: "vms-cards-min--md"
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "MinItem string? [JsonIgnore(WhenWritingNull)] field on PageNode + SectionNode"
      contains: "string? MinItem = null"
    - path: "demo/Showcase/frontend/src/main.ts"
      provides: "a switcher demo row + a cards/minItem demo block (zero app CSS)"
      contains: "switcher"
    - path: "AGENTS.md"
      provides: "Layout policy: switcher marked shipped (forward-reference caveat dropped)"
      contains: "switcher"
    - path: "CHANGELOG.md"
      provides: "Unreleased Phase 9 subsection (switcher + cards minItem)"
      contains: "minItem"
  key_links:
    - from: "demo/FeatureProbe-bun/handler.ts buildVm"
      to: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs BuildVm"
      via: "byte-identical cards sections carrying minItem"
      pattern: "minItem|MinItem"
    - from: "viewmodel-shell/src/browser.ts minItem emission"
      to: "viewmodel-shell/styles/default.css .vms-cards-min--{token} rules"
      via: "modifier class names emitted by the renderer and styled by the CSS"
      pattern: "vms-cards-min"
---

<objective>
Promote the CSS-only `--vms-card-min` token to an explicit bounded `minItem` wire field on the `cards` layout — a closed size scale (`xs|sm|md|lg|xl` → 10/13/16/20/24rem) that overrides the auto-fit minimum track width as declared server intent — across both backends, with CSS, renderer emission, unit tests, and parity coverage; then close out the phase's non-code deliverables: the minimal Showcase demos for `switcher` (from Plan 01) and `cards`/`minItem`, the AGENTS.md Layout-policy update marking `switcher` shipped, and the Unreleased CHANGELOG Phase-9 subsection.

Purpose: Deliver GRID-01/02 (the converged auto-fit grid becomes declared server intent, not a hidden CSS variable) plus the phase's POLICY/DEMO/CHANGELOG follow-through. RELEASE IS DEFERRED to Phase 11 — accumulate CHANGELOG notes under the existing `## Unreleased` heading; do NOT bump `package.json`/`.csproj` and do NOT publish/tag.

Output: Modified framework source + .NET records + tests + both FeatureProbe backends + parity fixture (for `minItem`), plus `demo/Showcase/frontend/src/main.ts` (switcher + minItem demos), `AGENTS.md` (Layout policy), and `CHANGELOG.md` (Unreleased Phase-9 subsection). `bun run parity/run.ts` byte-identical green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-switcher-cards-minitem/09-CONTEXT.md
@.planning/design/layout-system-research.md
@.planning/ROADMAP.md
@.planning/phases/09-switcher-cards-minitem/09-01-PLAN.md
@.planning/phases/08-alignment-enums-layout-policy/08-01-SUMMARY.md

<interfaces>
<!-- Depends on Plan 09-01 (switcher/threshold/limit already landed): minItem is added alongside, same files.
     After 09-01, PageNode/SectionNode already carry threshold?/limit? — ADD minItem? after limit?. -->

<!-- index.ts: ADD minItem?: "xs"|"sm"|"md"|"lg"|"xl" after limit? on BOTH PageNode and SectionNode. -->
PageNode:    ... arrange?; align?; threshold?; limit?; minItem?: "xs"|"sm"|"md"|"lg"|"xl"; children;
SectionNode: ... arrange?; align?; threshold?; limit?; minItem?; id?; collapsible?; ...

<!-- ViewModels.cs: ADD `[property: JsonIgnore(WhenWritingNull)] string? MinItem = null` after Limit on BOTH records. -->
PageNode(... int? Limit = null, string? MinItem = null) : ViewNode
SectionNode(... int? Limit = null, string? MinItem = null) : ViewNode

<!-- browser.ts: in page() + the 4 section() builders, APPEND `${n.minItem ? ` vms-cards-min--${n.minItem}` : ""}`
     after the `${n.limit ? ...}` ternary (added in 09-01), on the same element. -->

<!-- default.css: --vms-card-min token (L72, =16rem); the .vms-page--cards/.vms-section--cards auto-fit rule
     (L153-157) reads `minmax(min(var(--vms-card-min),100%),1fr)`. minItem just sets --vms-card-min on the
     element via a modifier class, so the existing auto-fit rule picks it up — NO change to the cards rule itself.
     Add the five .vms-cards-min--{token} rules near the cards rules. -->

<!-- Showcase main.ts: the 1.12.0 arrange/align demo block (~L240-277) is the insertion neighborhood — add the
     switcher + minItem demo sections right after it (zero <style>; built only from ViewNodes). -->

<!-- AGENTS.md ~L167: "VMS ships `sidebar`; `switcher` arrives in Phase 9 (the forward reference is intentional)."
     → change to "VMS ships `sidebar` and `switcher`." (drop the forward-reference caveat). Keep the
     concern→source convention (do NOT enumerate the threshold/limit/minItem token scales — point at the type source). -->

<!-- CHANGELOG.md: the `## Unreleased — Layout System Completeness` heading already has a Phase-8 (arrange/align)
     subsection. ADD a `### Switcher + cards minItem — Phase 9 (on `main`, unpublished)` subsection BELOW it,
     mirroring the Phase-8 subsection's Added / Not changed / Demo+tests / Migration structure. Do NOT bump versions. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cards minItem field (both backends) + emit class + CSS + tests + parity</name>
  <files>viewmodel-shell/src/index.ts, viewmodel-shell-dotnet/ViewModels.cs, viewmodel-shell/src/browser.ts, viewmodel-shell/styles/default.css, viewmodel-shell/test/theme-modifiers.test.ts, demo/FeatureProbe-bun/handler.ts, demo/FeatureProbe/AspNetCore/FeatureProbeController.cs, parity/fixtures/feature-probe.json</files>
  <read_first>
    viewmodel-shell/src/index.ts (PageNode + SectionNode — the threshold?/limit? fields from 09-01 + the closed-union doc-comment idiom);
    viewmodel-shell-dotnet/ViewModels.cs (PageNode + SectionNode records — the Threshold/Limit trailing params from 09-01);
    viewmodel-shell/src/browser.ts (page() + the 4 section() className builders — the `${n.limit ? ...}` ternary from 09-01 is the anchor to append after);
    viewmodel-shell/styles/default.css (--vms-card-min token L72; the .vms-page--cards/.vms-section--cards auto-fit rule L153-157; the .vms-page--cards/.vms-section--cards > min-width:0 rule ~L185-188);
    viewmodel-shell/test/theme-modifiers.test.ts (the SWITCH describe block from 09-01 + the renderPage/renderSection helpers);
    demo/FeatureProbe-bun/handler.ts + demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (the switcher sections from 09-01 + the page children lists);
    parity/fixtures/feature-probe.json ($comment ~L5)
  </read_first>
  <behavior>
    - page with layout:"cards" + minItem:"sm" ⇒ root className contains "vms-cards-min--sm"
    - section with layout:"cards" + minItem:"xl" ⇒ section className contains "vms-cards-min--xl"
    - each minItem value (xs/sm/md/lg/xl) on a cards section ⇒ the matching "vms-cards-min--{token}" class present
    - page/section with layout:"cards" but NO minItem ⇒ className is EXACTLY "vms-page vms-page--cards" / "vms-section vms-section--cards" — NO "vms-cards-min--" token (byte-identical-when-omitted)
    - a cards section with minItem inside a collapsible/flyout/link branch still carries the modifier class
  </behavior>
  <action>
    Wire field (TS): in `viewmodel-shell/src/index.ts`, add `minItem?` typed as the closed union `"xs" | "sm" | "md" | "lg" | "xl"` to BOTH the `PageNode` interface (after `limit?`) and the `SectionNode` interface (after `limit?`). Doc-comment in the adjacent idiom: for `layout:"cards"` — overrides the auto-fit minimum track width (today's fixed `--vms-card-min: 16rem`); a CLOSED size scale (xs→10rem, sm→13rem, md→16rem [= today's default], lg→20rem, xl→24rem) emitted as `.vms-cards-min--{token}` setting `--vms-card-min` on that element (the existing `repeat(auto-fit, minmax(min(var(--vms-card-min),100%),1fr))` rule reads it); OMITTED `minItem` emits no class → the inherited 16rem default holds = byte-identical to today; intended for `cards` but harmless elsewhere (it only sets a variable the cards rule reads). Do NOT edit `src/server.ts`.

    Wire field (.NET): in `viewmodel-shell-dotnet/ViewModels.cs`, add `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? MinItem = null` to the `PageNode` record (after `Limit`) and the `SectionNode` record (after `Limit`), with a comment matching the `Threshold` style: free-form `string?` mirroring the TS closed union `"xs"|"sm"|"md"|"lg"|"xl"` (enforced TS-side + validated by parity); the `cards` auto-fit min track width; omitted = no class → 16rem default; emits `.vms-cards-min--{value}`. `JsonIgnore`-on-null per the file-header rule.

    Renderer (browser.ts): in `page()` and EVERY `section()` className builder (collapsible, flyout, link, base), APPEND after the `${n.limit ? ...}` ternary (from 09-01): `${n.minItem ? ` vms-cards-min--${n.minItem}` : ""}`. Bare-presence-check idiom; omitting emits no token.

    CSS (default.css): add the five `.vms-cards-min--{token}` rules near the cards rules (e.g. immediately after the `.vms-page--cards > .vms-page__title, .vms-section--cards > .vms-section__heading { grid-column: 1 / -1; }` rule ~L158-161, or grouped with the switcher rules — pick the clearer placement and note it). Each sets the existing custom property: `.vms-cards-min--xs { --vms-card-min: 10rem; } .vms-cards-min--sm { --vms-card-min: 13rem; } .vms-cards-min--md { --vms-card-min: 16rem; } .vms-cards-min--lg { --vms-card-min: 20rem; } .vms-cards-min--xl { --vms-card-min: 24rem; }`. Add a short comment: promotes the CSS-only `--vms-card-min` (D-05 seam) to declared server intent; the existing auto-fit cards rule reads the variable unchanged, so an omitted `minItem` keeps the 16rem default = byte-identical. Do NOT modify the `.vms-page--cards`/`.vms-section--cards` auto-fit rule itself.

    Tests (theme-modifiers.test.ts): add a `GRID-01` describe block covering the `<behavior>` cases — each minItem value on a cards page/section ⇒ `vms-cards-min--{token}` present; a bare cards page AND section (no minItem) ⇒ `className === "vms-page vms-page--cards"` / `"vms-section vms-section--cards"` (exact byte-identical-when-omitted assertion); a cards section with minItem inside a collapsible (and flyout) branch ⇒ the class present. Existing `classList.contains` / exact-equality idioms.

    FeatureProbe + parity: in BOTH `buildVm`/`BuildVm`, add `layout:"cards"` sections exercising minItem as STATIC view-shape (captured by existing GETs, NO new action arm), appended to the page children list after the switcher sections (from 09-01) and before the table section. Cover: a bare cards section (no minItem, proves omitted = absent — NOTE the page root is already `layout:"cards"` but a dedicated SECTION-level bare-cards proves it at the section level) and one cards section per minItem value (xs/sm/md/lg/xl), each with ~3-4 minimal `LinkNode`/`TextNode` children, byte-identical across backends (build the set with a `["xs","sm","md","lg","xl"] as const` map / `new[]{...}.Select(...)`, like the arrange/align maps). In `parity/fixtures/feature-probe.json`, extend the `$comment` to note the new `cards`/`minItem` static coverage (one section per minItem value + a bare cards section proving omitted = absent), captured by existing GET steps — no new fixture FILE, so `parity/backends.json` is unchanged.
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && npx vitest run test/theme-modifiers.test.ts && npm run check:core-globals && cd ../viewmodel-shell-dotnet && PATH="$HOME/.dotnet:$PATH" dotnet build --nologo -v minimal && cd .. && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
  </verify>
  <done>
    `minItem?` (`"xs"|"sm"|"md"|"lg"|"xl"`) exists on `PageNode`/`SectionNode` (TS) and as `[JsonIgnore(WhenWritingNull)] string? MinItem` (.NET); the renderer emits `vms-cards-min--{minItem}` in all 5 branches; default.css has the five `.vms-cards-min--{token}` rules and the auto-fit cards rule is untouched; theme-modifiers asserts presence + exact byte-identical-when-omitted + non-base-branch emission; both FeatureProbe backends render the minItem matrix + a bare cards section byte-identically; tsc / vitest / core-globals / dotnet build / `bun run parity/run.ts` all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Showcase demos (switcher + cards/minItem) + AGENTS.md policy + CHANGELOG Phase-9 subsection</name>
  <files>demo/Showcase/frontend/src/main.ts, AGENTS.md, CHANGELOG.md</files>
  <read_first>
    demo/Showcase/frontend/src/main.ts (the 1.12.0 arrange/align demo block ~L240-277 — the insertion neighborhood + the `flatMap` / `as const` ViewNode idioms; the cards-preset usage ~L430+);
    AGENTS.md (the Layout policy "Two layouts a grid provably cannot express" paragraph ~L167 — the `switcher arrives in Phase 9` forward reference; the concern→source convention ~L169);
    CHANGELOG.md (the `## Unreleased — Layout System Completeness` heading + the Phase-8 `### Alignment enums` subsection ~L10-46 — the structure to mirror; the `## 1.11.0` heading below it as the lower boundary)
  </read_first>
  <action>
    Showcase (demo/Showcase/frontend/src/main.ts): add two new sections immediately after the 1.12.0 arrange/align demo block (~L277), built ONLY from ViewNodes (zero `<style>`, zero app CSS):
    (1) a `switcher` demo — a heading section ("Switcher (atomic row ↔ stack flip)") with a muted `TextNode` explaining "resize the window: ~4 equal cards sit in one row above the threshold and ALL stack below it — no 2-then-1 state (the distinction from `cards` auto-fit)", then a `layout:"switcher"` section of ~4 equal children (use `variant:"card"` sub-sections or simple TextNode/LinkNode cards labeled "One".."Four" so the flip is visible). Optionally show a `threshold:"sm"` variant alongside to demonstrate the token. Keep it minimal — the comprehensive spread is Phase 11.
    (2) a `cards`/`minItem` demo — a heading section ("Cards minItem (auto-fit min track width)") with a muted `TextNode`, then 2-3 `layout:"cards"` sub-sections each with a different `minItem` (e.g. `"sm"` → more, smaller columns; `"md"` → default; `"xl"` → fewer, wider), each containing ~4-6 minimal card children, so the reviewer sees the column-width difference. All still collapse to one column intrinsically.
    Mirror the existing demo block's comment style (a leading `// 1.13.0 — …` comment naming what it demonstrates). Use `as const` on inline node literals where the existing block does, so `tsc` is happy.

    AGENTS.md: in the "Two layouts a grid provably cannot express" paragraph (~L167), change "VMS ships `sidebar`; `switcher` arrives in Phase 9 (the forward reference is intentional)." to state both are now shipped — e.g. "VMS ships both `sidebar` and `switcher`." Drop the forward-reference caveat entirely. Do NOT enumerate the `threshold`/`limit`/`minItem` token scales here — keep the concern→source convention (the paragraph ~L169 already points at the type source as authoritative). Make no other AGENTS.md changes (the node-table / Design-system doc refresh is Phase 11).

    CHANGELOG.md: under the existing `## Unreleased — Layout System Completeness` heading, ADD a new subsection BELOW the Phase-8 `### Alignment enums` block and ABOVE the `## 1.11.0` heading:
    `### Switcher + cards minItem — Phase 9 (on `main`, unpublished)`
    Mirror the Phase-8 subsection's structure (a short intro paragraph + `### Added` / `### Not changed` / `### Demo + tests` / `### Migration` sub-blocks). Content to capture: the `switcher` layout value (atomic all-row ↔ all-stack flip via negative-`flex-basis`, zero `@media`; the one completeness primitive a grid cannot express — distinct from `cards` auto-fit's intermediate column counts) + its bounded `threshold` (`sm|md|lg|xl` → 20/30/40/48rem) and bounded `limit` (`2..8` count cap); the `cards` `minItem` (`xs|sm|md|lg|xl` → 10/13/16/20/24rem) promoting the CSS-only `--vms-card-min` to declared server intent. Note: `.NET` `Threshold`/`MinItem` are free-form `string?`, `Limit` is `int?`, all `[JsonIgnore(WhenWritingNull)]`; protocol token stays `viewmodel-shell/1.0` (additive optional fields; `agent-skill.md`/`AgentSkill.md` untouched); **omitting any of them is byte-identical to today** (no class → 30rem switcher default / 16rem card default / no cap hold); FeatureProbe + theme-modifiers cover them with cross-backend parity; the Showcase demonstrates the switcher flip + the minItem matrix; Migration: none (purely additive). **Do NOT bump `package.json` or `.csproj` versions and do NOT add a `## 1.13.0` released heading** — these notes stay under `## Unreleased` (release is the consolidated Phase-11 bump+publish). Keep the existing top-of-Unreleased note about batched release intact.
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && cd ../demo/Showcase/frontend && npx tsc --noEmit 2>/dev/null || npx vite build --logLevel error; cd /home/thenasty/ViewModelShell && grep -q "ships both" AGENTS.md && grep -q "Switcher + cards minItem — Phase 9" CHANGELOG.md && ! grep -q "switcher` arrives in Phase 9" AGENTS.md && echo DOC-CHECKS-PASS</automated>
  </verify>
  <done>
    The Showcase renders a `switcher` demo row (~4 equal cards → atomic flip on resize) and a `cards`/`minItem` demo (2-3 minItem values side-by-side), built only from ViewNodes (no `<style>`); the Showcase frontend type-checks/builds. AGENTS.md Layout policy marks `switcher` shipped (forward-reference caveat removed, concern→source convention preserved). CHANGELOG.md has a `### Switcher + cards minItem — Phase 9` subsection under `## Unreleased` with Added/Not-changed/Demo+tests/Migration; NO version bump, NO new released heading, NO publish/tag.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client wire | The `minItem` value crosses the wire and the renderer interpolates it into a CSS class name (`vms-cards-min--${n.minItem}`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | browser.ts class-name interpolation of `minItem` | accept | The TS type is a closed union (`"xs"|"sm"|"md"|"lg"|"xl"`); a value outside it is a compile error on every honest backend. A hostile server already fully owns the view tree, so injecting a class string is not a new capability and cannot escape the className attribute into script. No runtime sanitization — consistent with the Phase-8/09-01 modifier-class interpolations (T-08-01 / T-09-01 precedent). |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure source + doc edits to existing files; no new dependencies. |
</threat_model>

<verification>
- `cd viewmodel-shell && npx tsc --noEmit` — TS compiles with the `minItem` field.
- `cd viewmodel-shell && npx vitest run` — full vitest suite green (theme-modifiers GRID cases + no regressions).
- `cd viewmodel-shell && npm run check:core-globals` — core platform-agnosticism guard green.
- `cd viewmodel-shell-dotnet && PATH="$HOME/.dotnet:$PATH" dotnet build --nologo -v minimal` — .NET compiles.
- `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` — byte-identical green across .NET / Bun / Node, including the new minItem sections.
- Showcase frontend type-checks/builds; AGENTS.md says `switcher` is shipped; CHANGELOG has the Phase-9 Unreleased subsection; no version bump in `package.json`/`.csproj`.
</verification>

<success_criteria>
- `minItem?` (`"xs"|"sm"|"md"|"lg"|"xl"`) exists on `PageNode`/`SectionNode` (TS) and as `[JsonIgnore(WhenWritingNull)] string? MinItem` (.NET); the renderer emits `vms-cards-min--{minItem}` in all 5 branches; default.css maps every token to `--vms-card-min`; the auto-fit cards rule is untouched.
- Omitting `minItem` emits no class — proven by a theme-modifiers exact-equality test AND a bare-`cards` section in the parity fixture (16rem default holds).
- The Showcase demonstrates the `switcher` atomic flip + the `cards`/`minItem` width matrix with zero app CSS; AGENTS.md marks `switcher` shipped; CHANGELOG has the Phase-9 Unreleased subsection.
- `bun run parity/run.ts` byte-identical green; vitest green; both backends build. NO version bump / publish / tag (deferred to Phase 11).
</success_criteria>

<output>
Create `.planning/phases/09-switcher-cards-minitem/09-02-SUMMARY.md` when done.
</output>
