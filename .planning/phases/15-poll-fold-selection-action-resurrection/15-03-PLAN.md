---
phase: 15-poll-fold-selection-action-resurrection
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified: []
autonomous: true
requirements: [NBA-05, NBA-06, NBA-07]

must_haves:
  truths:
    - "The full green-tree gate (vitest, core-globals guard, cross-backend parity, .NET framework tests, all 5 demo .Tests.csproj) passes with zero failures after both 15-01 and 15-02 land."
    - "Every ROADMAP Phase 15 requirement (NBA-05, NBA-06, NBA-07) has a concrete, grep-verifiable artifact on disk."
  artifacts:
    - path: ".planning/phases/15-poll-fold-selection-action-resurrection/15-03-SUMMARY.md"
      provides: "recorded pass/fail counts for every gate command, run in THIS session (not copied from a prior report)"
  key_links: []
---

<objective>
Close Phase 15 the same way Phase 14 closed (14-03's Task 2 pattern): after both wave-1 plans
(15-01 the TS dispatch-loop fix + tests, 15-02 the agent-skill.md docs) have landed, run the complete
green-tree gate required before any phase can be considered done, and cross-check every phase
requirement ID against a concrete on-disk artifact. No code changes in this plan — verification only.

Purpose: per AGENTS.md's "NEVER PUBLISH OR PUSH ANYTHING BROKEN" rule, a phase touching the core
dispatch loop (used by every demo app) must prove the WHOLE tree — not just the new test files — is
green before being marked complete.
Output: a recorded, in-session verification pass; no version bump, no publish, no git push (explicitly excluded from this phase per the ROADMAP's hard exclusions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-non-blocking-dispatch-core/14-03-SUMMARY.md
@.planning/phases/14-non-blocking-dispatch-core/14-04-GAP-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: full green-tree gate re-run</name>
  <files></files>
  <read_first>
    @.planning/phases/14-non-blocking-dispatch-core/14-04-GAP-SUMMARY.md ("Green-tree gate — full re-run, all green" table — reuse this EXACT command list and table shape for the SUMMARY of this task)
  </read_first>
  <action>
    Run, in order, and record pass/fail + counts for each:
    1. `cd viewmodel-shell && npx vitest run` (expect: more files/tests than the Phase-14 baseline of 44 files / 524 passed / 1 skipped — 15-01 added `poll-fold.test.ts` and `checkbox-rapid-toggle.test.ts` as new files plus new tests appended to `nonblocking-dispatch.test.ts`).
    2. `cd viewmodel-shell && npm run check:core-globals` (AGNOSTIC-03 clean).
    3. `cd viewmodel-shell && npm run build` (tsc -b, exit 0).
    4. `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` (repo root) — full fixture suite green, "✓ all backends agree", skill source + HTTP twin parity green (15-02's addition).
    5. `dotnet test viewmodel-shell-dotnet/Tests` (expect 102/102, unchanged — this plan touches no .NET code).
    6. Every `demo/**/*.Tests.csproj`: `for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p"; done` (expect 181/181 total across the 5 projects, unchanged — matching the 14-04-GAP-SUMMARY.md baseline, since no demo app or ViewModels.cs changed in this phase).
    If ANY command fails, STOP, diagnose, fix (in the appropriate wave-1 plan's files, not ad hoc here), and re-run the full gate before proceeding — per AGENTS.md's green-tree gate rule, "it was already failing" is not an exception.
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx vitest run</automated>
    <automated>cd viewmodel-shell && npm run check:core-globals</automated>
    <automated>cd viewmodel-shell && npm run build</automated>
    <automated>cd /home/thenasty/ViewModelShell && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet test /home/thenasty/ViewModelShell/viewmodel-shell-dotnet/Tests</automated>
  </verify>
  <acceptance_criteria>
    <automated>cd viewmodel-shell && npx vitest run</automated>
    <automated>cd viewmodel-shell && npm run check:core-globals</automated>
    <automated>cd viewmodel-shell && npm run build</automated>
    <automated>cd /home/thenasty/ViewModelShell && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
    <automated>PATH="$HOME/.dotnet:$PATH" dotnet test /home/thenasty/ViewModelShell/viewmodel-shell-dotnet/Tests</automated>
    - All five commands above exit 0.
    - All 5 `demo/**/*.Tests.csproj` projects individually exit 0 (`dotnet test` on each path returned by `find demo -name '*.Tests.csproj'`).
  </acceptance_criteria>
  <done>Every command in the green-tree gate passes in this session, with counts recorded in the SUMMARY (not copied from a prior report).</done>
</task>

<task type="auto">
  <name>Task 2: requirement-to-artifact cross-check (NBA-05/06/07)</name>
  <files></files>
  <read_first>
    @.planning/REQUIREMENTS.md (NBA-05, NBA-06, NBA-07 exact wording)
    @.planning/ROADMAP.md (Phase 15 Success Criteria, exact wording)
  </read_first>
  <action>
    For each requirement, grep for its concrete artifact and confirm it's present:
    - NBA-05: `grep -n "NBA-05" viewmodel-shell/src/index.ts` (doc comment from 15-01 Task 1) AND `grep -n "pollInterval" viewmodel-shell/test/poll-fold.test.ts` (real-timer test from 15-01 Task 2).
    - NBA-06: `grep -n "pendingNonBlockingRefire === null" viewmodel-shell/src/index.ts` (the fix from 15-01 Task 1) AND `grep -c "checked).toBe(false)" viewmodel-shell/test/checkbox-rapid-toggle.test.ts` (>= 2, from 15-01 Task 3) AND `grep -n "NBA-06" viewmodel-shell/test/nonblocking-dispatch.test.ts` (the regression test from 15-01 Task 2).
    - NBA-07: `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` (empty, from 15-02) AND `grep -n "Non-blocking actions" viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` (present in BOTH, byte-identically).
    Record each grep's result in the SUMMARY as explicit FOUND/NOT-FOUND evidence, matching the
    Self-Check convention used in the Phase 14 plan SUMMARYs.
  </action>
  <verify>
    <human-check>See <acceptance_criteria> below for concrete grep/diff checks.</human-check>
  </verify>
  <acceptance_criteria>
    - Every grep listed above returns a match (or, for the `diff`, empty output) — recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <done>Every Phase 15 requirement ID (NBA-05, NBA-06, NBA-07) has a verified, grep-confirmed on-disk artifact, recorded in the SUMMARY's Self-Check section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Verification-only plan; no files modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | n/a | n/a | accept | No new component or trust boundary; this plan only runs existing test/build/parity commands. |
</threat_model>

<verification>
All six gate commands in Task 1 pass; all grep cross-checks in Task 2 confirm a match for every requirement ID.
</verification>

<success_criteria>
- The complete green-tree gate (vitest, core-globals, build, parity, .NET framework tests, all demo tests) passes after both 15-01 and 15-02.
- NBA-05, NBA-06, NBA-07 each have a verified on-disk artifact.
- No version bump, no publish, no git push performed (deferred to Phase 16 per the ROADMAP).
</success_criteria>

<output>
Create `.planning/phases/15-poll-fold-selection-action-resurrection/15-03-SUMMARY.md` when done
</output>
