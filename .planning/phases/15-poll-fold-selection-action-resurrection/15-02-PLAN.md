---
phase: 15-poll-fold-selection-action-resurrection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/agent-skill.md
  - viewmodel-shell-dotnet/AgentSkill.md
autonomous: true
requirements: [NBA-07]

must_haves:
  truths:
    - "A wire-driving agent reading agent-skill.md understands that a node action descriptor's `blocking:false` is a client-side (browser ViewModelShell) scheduling hint that never rides the `_action` POST payload, and that it can safely ignore the field and dispatch the action normally."
    - "viewmodel-shell/agent-skill.md and viewmodel-shell-dotnet/AgentSkill.md are byte-identical."
  artifacts:
    - path: "viewmodel-shell/agent-skill.md"
      provides: "a new '## Non-blocking actions (blocking:false)' section documenting the field for wire-driving agents"
    - path: "viewmodel-shell-dotnet/AgentSkill.md"
      provides: "byte-identical copy of the above"
  key_links:
    - from: "viewmodel-shell/agent-skill.md"
      to: "viewmodel-shell-dotnet/AgentSkill.md"
      via: "parity/check-skill.ts's checkSourceTwins() byte-diff"
      pattern: "checkSourceTwins"
---

<objective>
Document `blocking:false` (the Phase 14 `ActionEvent.blocking` / `.NET ActionDescriptor.Blocking`
field, unchanged by this phase's other plan) in the canonical agent operating manual, so a
wire-driving agent (curl/JSON, no browser, no `ViewModelShell` instance) that encounters a node whose
action descriptor carries `"blocking": false` in a `vm` response tree knows what it means and that it
does NOT affect how the agent itself should dispatch the action.

Purpose: NBA-07 — close the documentation gap the ROADMAP names explicitly. The wire-level semantics
of `blocking` were already finalized in Phase 14 and are NOT changing in this phase (Plan 15-01's fix
is a purely internal client dispatch-loop refinement invisible to a raw wire consumer) — this plan is
additive documentation only.
Output: one new section in `viewmodel-shell/agent-skill.md`, byte-copied to the `.NET` twin, with the
parity skill-diff gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/design/non-blocking-actions.md
@viewmodel-shell/agent-skill.md
@parity/check-skill.ts
</context>

<interfaces>
<!-- The exact byte-diff mechanism this plan's Task 2 must satisfy. -->

`parity/check-skill.ts`'s `checkSourceTwins()` (already exists, unmodified by this plan):
```typescript
const TS_SKILL_PATH = resolve(__dirname, "..", "viewmodel-shell", "agent-skill.md");
const DOTNET_SKILL_PATH = resolve(__dirname, "..", "viewmodel-shell-dotnet", "AgentSkill.md");
// throws with message "Fix: cp <TS_SKILL_PATH> <DOTNET_SKILL_PATH>" on any byte mismatch
```

Current `agent-skill.md` section order (for placement — insert the new section between these two):
`## Polling` (ends ~line 136, "Polls run silently — they are not user-initiated.") then
`## Files` (starts ~line 138).

Reference field (unchanged by this phase, already shipped in Phase 14):
- TS: `ActionEvent.blocking?: boolean` (`viewmodel-shell/src/index.ts`) — never rides the `_action`
  POST payload; lives only on a node's `action`/`dismissAction`/`sortActions[...]`/etc. descriptor
  inside the `vm` RESPONSE tree.
- .NET: `ActionDescriptor(string Name, bool? Blocking = null)` (`viewmodel-shell-dotnet/ViewModels.cs`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: document `blocking:false` for wire-driving agents in agent-skill.md</name>
  <files>viewmodel-shell/agent-skill.md</files>
  <read_first>
    @viewmodel-shell/agent-skill.md (the whole file — it is short, 146 lines; read it in full to match its exact prose register: "operational, not historical", second-person imperative, terse tables)
    @.planning/design/non-blocking-actions.md (the "Wire / API surface (minimal)" section: confirms `blocking` never touches the `_action` payload, and the "Poll = a non-blocking action on a timer" framing)
    @viewmodel-shell/src/index.ts (the `ActionEvent.blocking?` doc comment, ~line 6-19: the authoritative statement of what does/doesn't ride the wire)
  </read_first>
  <action>
    Insert a new "## Non-blocking actions (`blocking:false`)" section into
    `viewmodel-shell/agent-skill.md`, positioned immediately after the existing "## Polling" section
    and before "## Files" (matching the file's existing section order and heading style — `##`,
    sentence-style body, a table where useful). The section must state, for a wire-driving agent:

    1. Some node action descriptors in the `vm` tree (a `CheckboxNode.action`, `ButtonNode.action`,
       a `TableRow.action`, etc.) may carry `"blocking": false`. This is a CLIENT-SIDE scheduling
       hint for the browser `ViewModelShell` instance (it selects a non-blocking dispatch lane that
       coexists with an in-flight blocking action instead of queuing behind it, and coalesces rapid
       repeated triggers to "latest wins").
    2. It NEVER appears in the `_action` POST payload you send — the request body shape stays
       `{"name": "<action-name>"}` (JSON form) or `{"name":"<action-name>"}` (multipart `_action`
       field) regardless of whether the descriptor you read it from said `blocking:true` (default,
       omitted) or `blocking:false`.
    3. **For you (a wire-driving agent with no client-side dispatch loop): `blocking` is
       INFORMATIONAL ONLY. Dispatch the action exactly the same way regardless of its value** — POST
       `_action`/`_state` (or the JSON form) as normal and read the response per the existing rules
       in this manual (`ok`, `rejected`, `errors[]`). You do not need to implement coalescing, an
       epoch, or any dispatch-lane concept to drive the wire correctly.
    4. One sentence connecting it to the existing "## Polling" section immediately above: the
       `{"name":"poll"}` dispatch this manual already documents is itself an instance of a
       non-blocking action (poll always rides the non-blocking lane client-side) — so nothing about
       how YOU send a poll dispatch changes either.

    Do not restructure any existing section; this is a pure insertion.
  </action>
  <verify>
    <human-check>See <acceptance_criteria> below for concrete grep/diff checks.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Non-blocking actions" viewmodel-shell/agent-skill.md` matches exactly once.
    - `grep -n "^## Non-blocking actions" viewmodel-shell/agent-skill.md` reports a line number greater than the line number of `grep -n "^## Polling" viewmodel-shell/agent-skill.md` and less than `grep -n "^## Files" viewmodel-shell/agent-skill.md` (confirms correct placement).
    - `grep -n "INFORMATIONAL ONLY\|informational only" viewmodel-shell/agent-skill.md` matches (confirms the "ignore it and dispatch normally" guidance is present in some form).
    - `grep -n "_action.*POST payload\|POST payload" viewmodel-shell/agent-skill.md` still finds language confirming `blocking` never rides the request.
  </acceptance_criteria>
  <done>viewmodel-shell/agent-skill.md has a new section, correctly placed, explaining `blocking:false` to a wire-driving agent with no client-side dispatch loop.</done>
</task>

<task type="auto">
  <name>Task 2: byte-copy to the .NET twin + parity skill-diff verification</name>
  <files>viewmodel-shell-dotnet/AgentSkill.md</files>
  <read_first>
    @parity/check-skill.ts (the exact `checkSourceTwins()` byte-diff check and its printed fix command)
    @.planning/phases/14-non-blocking-dispatch-core/14-01-SUMMARY.md (confirms this is the SAME maintainer rule already followed for the `blocking` field's introduction in Phase 14 — no new process, just repeating it)
  </read_first>
  <action>
    Run `cp viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` (the exact fix
    command `check-skill.ts` prints on a mismatch) so the two files are byte-identical. Then run the
    parity harness's skill checks to confirm: `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` from
    the repo root (this runs the full parity suite, which includes the skill source-twin diff and the
    skill HTTP-twin check against the live HelpDesk backends per the 14-03-SUMMARY.md precedent).
  </action>
  <verify>
    <automated>diff /home/thenasty/ViewModelShell/viewmodel-shell/agent-skill.md /home/thenasty/ViewModelShell/viewmodel-shell-dotnet/AgentSkill.md</automated>
    <automated>cd /home/thenasty/ViewModelShell && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
  </verify>
  <acceptance_criteria>
    <automated>diff /home/thenasty/ViewModelShell/viewmodel-shell/agent-skill.md /home/thenasty/ViewModelShell/viewmodel-shell-dotnet/AgentSkill.md</automated>
    - The diff command above produces NO output (byte-identical).
    <automated>cd /home/thenasty/ViewModelShell && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
    - Exit code 0; console output contains "✓ skill source files byte-identical" and "✓ skill HTTP twins byte-identical" (or the harness's equivalent success lines for the skill-diff phase).
  </acceptance_criteria>
  <done>viewmodel-shell-dotnet/AgentSkill.md is byte-identical to viewmodel-shell/agent-skill.md; the full parity suite (including the skill source + HTTP twin checks) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure markdown documentation change; no code path, no new input parsing, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | agent-skill.md content | accept | The manual documents an already-public, already-shipped wire behavior (Phase 14's `blocking` field); no secret, credential, or internal-only detail is added. |
</threat_model>

<verification>
- `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` produces no output.
- `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` from the repo root exits 0 with the skill source + HTTP twin checks reported green.
</verification>

<success_criteria>
- ROADMAP Phase 15 Success Criterion #3 (NBA-07): `agent-skill.md` documents `blocking:false` and is byte-identical to `.NET AgentSkill.md`, parity gate green.
</success_criteria>

<output>
Create `.planning/phases/15-poll-fold-selection-action-resurrection/15-02-SUMMARY.md` when done
</output>
