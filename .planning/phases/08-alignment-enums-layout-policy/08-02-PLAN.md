---
phase: 08-alignment-enums-layout-policy
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - AGENTS.md
  - demo/Showcase/frontend/src/main.ts
  - CHANGELOG.md
  - viewmodel-shell/package.json
  - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
autonomous: false
requirements: [ALIGN-04, POLICY-01]

must_haves:
  truths:
    - "AGENTS.md has a Layout policy section stating P1 (intrinsic / zero-viewport-breakpoint) and P2 (closed-enum / bounded-scalar) as the test a field must pass to join the layout vocabulary"
    - "The Layout policy section names sidebar and switcher as the two flexbox idioms a grid cannot express, and points at .planning/design/layout-system-research.md as the rationale of record"
    - "The Showcase demonstrates the canonical header-bar (row + arrange:space-between + heading TextNode first child + nav cluster) with zero app CSS"
    - "The Showcase demonstrates a small matrix of the align values"
    - "A lockstep npm 1.12.0 + NuGet 1.10.0 minor is published (version bump + CHANGELOG + publish ritual + annotated v1.12.0 tag + main advanced) — operator-gated"
  artifacts:
    - path: "AGENTS.md"
      provides: "Layout policy section (P1/P2, sidebar+switcher named, research-doc pointer)"
      contains: "Layout policy"
    - path: "demo/Showcase/frontend/src/main.ts"
      provides: "header-bar + align-matrix demo using arrange/align, zero <style>"
      contains: "space-between"
    - path: "CHANGELOG.md"
      provides: "1.12.0 / 1.10.0 entry describing the additive arrange/align fields"
      contains: "arrange"
    - path: "viewmodel-shell/package.json"
      provides: "version bumped to 1.12.0"
      contains: "1.12.0"
    - path: "viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj"
      provides: "Version bumped to 1.10.0"
      contains: "1.10.0"
  key_links:
    - from: "demo/Showcase/frontend/src/main.ts header-bar"
      to: "viewmodel-shell/styles/default.css .vms-arrange--space-between"
      via: "arrange:space-between on a layout:row section"
      pattern: "arrange.*space-between"
---

<objective>
Codify the two standing layout principles in AGENTS.md (POLICY-01), prove the alignment vocabulary in the Showcase with the canonical header-bar and an `align` matrix (ALIGN-04), write the CHANGELOG entry, bump both package versions in lockstep, then STOP at the operator-gated release task (git commit, npm + NuGet publish, annotated `v1.12.0` tag, advance `main`).

Purpose: Turn the shipped primitive (Plan 01) into governing policy + a verifiable demo, and ship it lockstep. ALIGN-04 is satisfied by the general `arrange:"space-between"` primitive serving the PBMInvoices header-bar request, demonstrated in the Showcase.

Output: AGENTS.md "Layout policy" section; Showcase demo additions; CHANGELOG `1.12.0 / 1.10.0` entry; version bumps in `package.json` (1.12.0) and `.csproj` (1.10.0); and a final operator-gated release sequence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-alignment-enums-layout-policy/08-CONTEXT.md
@.planning/design/layout-system-research.md
@.planning/phases/08-alignment-enums-layout-policy/08-01-SUMMARY.md

<interfaces>
<!-- AGENTS.md anchors: "## Design system" (~L119) with "### When to use which layout preset / density / card" (~L146) and "### The canonical worked example" (~L157). The new "### Layout policy" subsection fits inside the Design system section, near the layout-preset guidance — OR as a sibling top-level "## Layout policy". CONTEXT.md says add a "Layout policy" section; place it where it reads as the governing test for layout changes (a subsection under Design system, or just after it). Existing closed-union fields documented: stack/split/cards/sidebar/row layout, density, width, variant. -->

<!-- Showcase: demo/Showcase/frontend/src/main.ts has NO backend — it drives BrowserAdapter directly (~L580-617). Views are built by componentsView()/dashboardView()/formView()/listDetailView() returning ViewNode[] and switched in viewChildren() (~L553-561). buildVm() (~L563-577) wraps them. The shipped styles.css + the new default.css arrange/align rules are imported at top. ZERO per-view <style> is enforced by scripts/check-no-demo-style.mjs — add only ViewNodes. -->

<!-- The new arrange?/align? fields (from Plan 01) are now on PageNode/SectionNode in @ashley-shrok/viewmodel-shell — the Showcase imports the local source via the Vite alias, so the fields are available. -->

<!-- Version state at plan time: npm package.json "version": "1.11.0" → 1.12.0; NuGet .csproj <Version>1.9.0</Version> → 1.10.0. Wire protocol token stays viewmodel-shell/1.0 (additive). CHANGELOG head is the 1.11.0 / 1.9.0 entry — prepend the new entry above it. Release ritual + .env credential precheck + tag + advance-main rules: see AGENTS.md "Conventions for evolving the framework" (~L597+) — the canonical release procedure. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Layout policy section to AGENTS.md (POLICY-01)</name>
  <files>AGENTS.md</files>
  <read_first>
    AGENTS.md ("## Design system" ~L119, "### When to use which layout preset / density / card" ~L146-155, "### The canonical worked example" ~L157-167, "## Conventions for evolving the framework" ~L597+);
    .planning/design/layout-system-research.md (the P1/P2 articulation + the sidebar/switcher grid-inexpressibility argument + the enum values rationale)
  </read_first>
  <action>
    Add a "Layout policy" section to AGENTS.md (the repo-root framework AGENTS.md — NOT the home one). Place it inside or immediately adjacent to the "## Design system" section so it reads as the governing test for all future layout changes (a `### Layout policy` subsection right after "### When to use which layout preset / density / card", or a `## Layout policy` section just after the Design system block — pick whichever flows best with the surrounding headings).

    State the two principles as the governing test a layout field MUST pass to join the wire vocabulary:
    - **P1 — intrinsic / container-relative responsiveness, ZERO viewport breakpoints.** Collapse must come from auto-fit / `minmax`, flex-wrap, negative-flex-basis, or container queries. Container queries are the ONLY escape hatch; a viewport `@media` rule is never acceptable.
    - **P2 — closed enum or bounded scalar, never raw CSS.** Every layout knob crossing the wire is a closed union or bounded token. No CSS values, no spans/tracks/areas, no breakpoint objects.
    State the rule explicitly: a layout field joins the vocabulary IFF it passes BOTH P1 and P2.
    Name `sidebar` and `switcher` as the two flexbox idioms a grid provably cannot express (note `switcher` arrives in Phase 9 — the forward reference is intentional). Point at `.planning/design/layout-system-research.md` as the rationale of record.

    Write it in the dense, declarative AGENTS.md house style (matching the surrounding Design-system prose). Do NOT enumerate the arrange/align enum values in a drift-prone way here — point at the type source per the existing concern→source convention (the node-table update is Phase 11 / POLICY-02; this task is only the policy statement).
  </action>
  <verify>
    <automated>grep -q 'Layout policy' AGENTS.md && grep -q 'switcher' AGENTS.md && grep -q 'layout-system-research.md' AGENTS.md && grep -v '^#' AGENTS.md | grep -c -E 'P1|P2'</automated>
  </verify>
  <done>
    AGENTS.md has a "Layout policy" section stating P1 + P2 as the join-the-vocabulary test, naming `sidebar` and `switcher` as the grid-inexpressible flexbox idioms, and pointing at `.planning/design/layout-system-research.md`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Demonstrate the header-bar + align matrix in the Showcase (ALIGN-04)</name>
  <files>demo/Showcase/frontend/src/main.ts</files>
  <read_first>
    demo/Showcase/frontend/src/main.ts (componentsView() ~L?, dashboardView() ~L387, viewChildren() ~L553, buildVm() ~L563; the top imports of styles.css);
    viewmodel-shell/styles/default.css (the new .vms-arrange--*/.vms-align--* rules from Plan 01);
    viewmodel-shell/scripts/check-no-demo-style.mjs (the zero-demo-CSS guard this must not trip)
  </read_first>
  <action>
    Add a minimal demonstration of the new capability to the Showcase, built ENTIRELY from ViewNodes (zero `<style>`, zero app CSS — the `check-no-demo-style` guard must stay green).

    Add a small "Alignment" block (a labeled `section` group, or appended to the existing components/gallery view) containing:
    (a) The canonical header-bar (ALIGN-04): a `section` with `layout:"row"` + `arrange:"space-between"` whose FIRST child is a heading `TextNode` (`{ type:"text", value:"Acme Console", style:"heading" }`) — NOT the page title / section heading — followed by a nested `section` with `layout:"row"` of `LinkNode`s (Dashboard / Reports / Settings) as the nav cluster. This renders title-left / nav-right with zero app CSS, proving the PBMInvoices pattern served by the general primitive.
    (b) A small matrix of the `align` values: several `layout:"row"` sections, each with a different `align` value (`start`, `center`, `end`, `stretch`, `baseline`) and a couple of differently-sized children (e.g. a heading `TextNode` next to a `LinkNode` to make `baseline` and `stretch` visible), each labeled with a muted `TextNode` naming the value so a human reviewer can see what each does.
    Optionally also show one or two `arrange` values (e.g. `center`, `space-evenly`) for completeness — keep it minimal per CONTEXT.md (the comprehensive spread is Phase 11).

    Wire the block in so it renders: either append to `componentsView()`'s returned array, or add a dedicated entry — but do NOT add app CSS and do NOT introduce a `<style>` tag. Keep `buildVm()`/`viewChildren()` structure intact (if adding a new tab/view, follow the existing tabs pattern; appending to an existing view is simpler and acceptable).
  </action>
  <verify>
    <automated>cd viewmodel-shell && node scripts/check-no-demo-style.mjs && cd ../demo/Showcase/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    The Showcase renders the header-bar (`row` + `arrange:"space-between"` + heading TextNode first child + nav cluster) and an `align`-value matrix, built only from ViewNodes; `check-no-demo-style` stays green and the Showcase frontend typechecks.
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG entry + lockstep version bumps (staged, not published)</name>
  <files>CHANGELOG.md, viewmodel-shell/package.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj</files>
  <read_first>
    CHANGELOG.md (head — the "## 1.11.0 / 1.9.0" entry format + the package-moved/consumers-must-do convention ~L7);
    viewmodel-shell/package.json ("version": "1.11.0");
    viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (<Version>1.9.0</Version>);
    AGENTS.md ("## Conventions for evolving the framework" — the CHANGELOG-on-bump + major.minor-aligned rules)
  </read_first>
  <action>
    Prepend a new CHANGELOG.md entry ABOVE the current `## 1.11.0 / 1.9.0` entry, titled `## 1.12.0 / 1.10.0 — Alignment enums (arrange / align) on the row layout (npm + NuGet)`. Note: npm `1.12.0` (MINOR — additive `arrange`/`align` closed-union fields on `PageNode`/`SectionNode`); NuGet `1.10.0` (MINOR — additive `Arrange`/`Align` nullable string fields; both carry `[JsonIgnore(WhenWritingNull)]`). Describe in the existing house style:
    - **Added:** `arrange?` (`start|center|end|space-between|space-around|space-evenly` → `justify-content`) and `align?` (`start|center|end|stretch|baseline` → `align-items`) on `PageNode`/`SectionNode`, intended for `layout:"row"` (the cluster primitive). Emits `.vms-arrange--{value}` / `.vms-align--{value}`. Serves the PBMInvoices header-bar request (`arrange:"space-between"` + a heading `TextNode` first child = title-left / nav-right) via the general primitive — no bespoke node.
    - **Not changed:** no wire-shape change; protocol token stays `viewmodel-shell/1.0` (additive optional fields); `agent-skill.md` / `AgentSkill.md` untouched. Omitting `arrange`/`align` is byte-identical to today (no class emitted).
    - **Demo + tests:** FeatureProbe twins exercise every enum value + a bare `row` (parity byte-identical across .NET/Bun/Node); `theme-modifiers.test.ts` asserts class emission + the omitted-is-absent property; the Showcase demonstrates the header-bar + an `align` matrix.
    - State a MIGRATION note is NOT needed (purely additive).
    Also reference the new AGENTS.md "Layout policy" section in the entry.

    Bump `viewmodel-shell/package.json` `"version"` from `1.11.0` to `1.12.0`. Bump `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` `<Version>` from `1.9.0` to `1.10.0`. Do NOT publish, commit, or tag in this task — that is the operator-gated Task 4.
  </action>
  <verify>
    <automated>grep -q '1.12.0 / 1.10.0' CHANGELOG.md && grep -q '"version": "1.12.0"' viewmodel-shell/package.json && grep -q '<Version>1.10.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj</automated>
  </verify>
  <done>
    CHANGELOG has the `1.12.0 / 1.10.0` additive-alignment entry (referencing the Layout policy section); `package.json` is at `1.12.0` and the `.csproj` at `1.10.0`; nothing published/committed/tagged yet.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: OPERATOR-GATED release — full gate, commit, npm + NuGet publish, tag, advance main</name>
  <files>viewmodel-shell/package.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj, CHANGELOG.md (release/publish artifacts — no new file edits; this task commits/publishes/tags the staged tree)</files>
  <what-built>
    All code, CSS, both-backend parity, vitest, the Showcase demo, the AGENTS.md Layout policy section, the CHANGELOG entry, and the version bumps (npm 1.12.0 / NuGet 1.10.0) are staged in the working tree. Nothing has been committed, published, or tagged.
  </what-built>
  <how-to-verify>
    This is the OPERATOR-GATED release step (repo git policy + outward-facing publish). The operator runs / approves the following, in order, per the AGENTS.md "Conventions for evolving the framework" release ritual:

    1. **Green gate (run first, must all pass):**
       - `bun run parity/run.ts` (byte-identical across .NET / Bun / Node)
       - `cd viewmodel-shell && npx vitest run`
       - `cd viewmodel-shell && node scripts/check-core-globals.mjs && node scripts/check-aa-contrast.mjs && node scripts/check-no-demo-style.mjs`
       - `cd viewmodel-shell-dotnet && dotnet test` (and the .NET demo builds the parity harness already exercised)
    2. **Credential precheck** (surface gaps BEFORE publishing): `ENV="$(git rev-parse --show-toplevel)/.env"; grep -E '^(NPM_TOKEN|NUGET_API_KEY)=' "$ENV"`; sync `~/.npmrc` from `NPM_TOKEN` per the AGENTS.md ritual; `npm whoami` (expect `ashley-shrok`, not E401); `set -a; source "$ENV"; set +a` to load `NUGET_API_KEY`. **Never `npm login`.**
    3. **Commit** to the current branch (do NOT auto-branch) — a single release commit for the staged code/test/doc/CHANGELOG/version work.
    4. **npm publish:** `cd "$(git rev-parse --show-toplevel)/viewmodel-shell" && npm publish` (prepublishOnly builds). Confirm via the curl-to-registry read (`dist-tags.latest` == `1.12.0`).
    5. **NuGet publish:** `cd "$(git rev-parse --show-toplevel)/viewmodel-shell-dotnet" && dotnet pack -c Release && dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.1.10.0.nupkg --api-key "$NUGET_API_KEY" --source https://api.nuget.org/v3/index.json`. Confirm via the flatcontainer index read (last version == `1.10.0`).
    6. **Tag:** annotated `v1.12.0` at the release commit (`git tag -a v1.12.0 <sha> -m "viewmodel-shell 1.12.0"; git push origin v1.12.0`). (Tag naming follows the existing `v<semver>` convention; the NuGet side is 1.10.0 but the repo tag tracks the npm semver per existing v1.x tags.)
    7. **Advance main:** ensure `main` contains the release commit — `git merge-base --is-ancestor v1.12.0 main && echo "on main"` MUST print `on main`; if not, reconcile and push `main` (per the AGENTS.md "advance main — a tag is NOT enough" rule).

    Do NOT proceed past any failing gate. The publish/commit/tag/push are the gated actions — wait for the operator's explicit go.
  </how-to-verify>
  <resume-signal>Operator confirms the green gate passed, the commit landed, npm 1.12.0 + NuGet 1.10.0 are live (registry-confirmed), the `v1.12.0` tag is pushed, and `main` contains the release commit — or describes what to fix.</resume-signal>
  <action>This is a blocking operator-gated checkpoint — do NOT auto-run it. Pause and hand off to the operator the ordered release ritual described in <how-to-verify> above (green gate → credential precheck → commit → npm publish → NuGet publish → annotated v1.12.0 tag → advance main), per the AGENTS.md "Conventions for evolving the framework" procedure. The agent stages nothing new here; all code/test/doc/CHANGELOG/version work is already complete from Tasks 1–3. Wait for the operator's explicit go before any commit/publish/tag/push, and do not proceed past any failing gate.</action>
  <verify>Operator-run: `bun run parity/run.ts` byte-identical green; `cd viewmodel-shell && npx vitest run` green; the static guards (check-core-globals, check-aa-contrast, check-no-demo-style) green; `cd viewmodel-shell-dotnet && dotnet test` green; then registry reads confirm npm dist-tags.latest == 1.12.0 and the NuGet flatcontainer last version == 1.10.0; and `git merge-base --is-ancestor v1.12.0 main && echo "on main"` prints `on main`.</verify>
  <done>The full green gate passed; a single release commit landed on the current branch; npm 1.12.0 and NuGet 1.10.0 are live and registry-confirmed; the annotated `v1.12.0` tag is pushed; and `main` contains the release commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public registries | The release task pushes the built packages to npm + NuGet (outward-facing, irreversible-ish). Operator-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | publish credentials in `.env` | mitigate | The release ritual sources the repo-root `.env` (gitignored) and syncs `~/.npmrc` from `NPM_TOKEN` rather than trusting a stale `~/.npmrc`; the credential precheck surfaces an expired/missing token before any publish. Operator-gated step. |
| T-08-03 | Repudiation | a version bump without a registry publish (drift) | mitigate | The operator-gated task makes publish + registry-read confirmation + tag + advance-`main` a single mandatory sequence, preventing the bumped-but-unpublished drift documented in AGENTS.md. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency installs; `npm publish` ships only the existing package. `dotnet pack` builds from source. No third-party package added. |
</threat_model>

<verification>
- `grep 'Layout policy' AGENTS.md` — policy section present (P1/P2, sidebar+switcher, research-doc pointer).
- `cd viewmodel-shell && node scripts/check-no-demo-style.mjs` — Showcase adds zero app CSS.
- `cd demo/Showcase/frontend && npx tsc --noEmit` — Showcase typechecks with the new arrange/align usage.
- `grep '1.12.0 / 1.10.0' CHANGELOG.md` + version greps — CHANGELOG + bumps staged.
- (Operator-gated) full green gate: `bun run parity/run.ts`, `vitest run`, the static guards, `dotnet test`; then publish/tag/advance-main.
</verification>

<success_criteria>
- AGENTS.md "Layout policy" section states P1 + P2 as the join-the-vocabulary test, names `sidebar` + `switcher`, and points at the research doc.
- The Showcase demonstrates the header-bar (`arrange:"space-between"` + heading TextNode first child + nav cluster) and an `align` matrix with zero app CSS.
- CHANGELOG has the additive `1.12.0 / 1.10.0` entry; `package.json` = 1.12.0, `.csproj` = 1.10.0.
- The release (commit + npm 1.12.0 + NuGet 1.10.0 publish + `v1.12.0` tag + `main` advanced) is executed only on operator go, with the full green gate passing first.
</success_criteria>

<output>
Create `.planning/phases/08-alignment-enums-layout-policy/08-02-SUMMARY.md` when done.
</output>
