---
phase: 25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/browser.ts
  - viewmodel-shell/src/server.ts
  - viewmodel-shell/styles/default.css
  - viewmodel-shell-dotnet/ViewModels.cs
  - viewmodel-shell/test/user-row.test.ts
  - viewmodel-shell-dotnet/Tests/UserRowNodeSerializationTests.cs
autonomous: true
requirements: [COMP-09]

must_haves:
  truths:
    - "`UserRowNode` ships with wire type `\"user-row\"` on both backends. Slots: `avatar?` (ViewNode, typically AvatarNode from COMP-04), `name` (required, `string | ViewNode` — trained: TextNode body + weight:\"medium\"), `meta?` (`string | ViewNode` — trained: TextNode muted), `status?` (small typed sub-record `{label: string; kind: StatusKind}` — NOT a ViewNode slot), `trailing?` (ViewNode), `action?` (whole-row click, member-picker pattern)."
    - "`StatusKind` closed 4-value enum `\"online\" | \"away\" | \"offline\" | \"busy\"` — controls status-dot color via CSS class mapping: `online`→`--vms-success`, `away`→`--vms-warning`, `offline`→muted, `busy`→`--vms-error`."
    - "Renderer emits container-detected element: parent has `.vms-user-row-list` class → `<li>`; else standalone `<div>`. Class-name composition: `vms-user-row` + `vms-user-row--clickable` (when action). Grid layout `[avatar | content | status]` via CSS."
    - "Renderer wraps string `name` in `TextNode { style: \"body\", weight: \"medium\" }`; string `meta` in `TextNode { style: \"muted\" }`. status renders `<span class=\"vms-user-row__status\"><span class=\"vms-status-dot vms-status-dot--{kind}\"></span>{label}</span>` — dot BEFORE label."
    - "Whole-row `action` follows ListRowNode.action shape (`role=\"button\"`, `tabIndex=0`, aria-label from name+meta text, Enter+Space dispatch with Space preventDefault, stopPropagation on interactive descendants `.vms-button, .vms-checkbox__input, .vms-checkbox, .vms-field__input, a[href]`)."
    - "Tree-validator descends into `avatar`, `name` (if ViewNode), `meta` (if ViewNode), `trailing`; records `action` for name-uniqueness. status is a LEAF sub-record — NO walker descent (no ViewNode content)."
    - "`.NET` `UserRowNode` record types every ViewNode-typed slot as `ViewNode?` per Phase 24 §5 Analog C — consumers wrap strings explicitly (`new TextNode(\"Foo\", Style: TextStyle.Body, Weight: TextWeight.Medium)`). `Name` non-nullable ViewNode (required). `status` typed as new `UserRowStatus` sub-record (`Label` string + `Kind` StatusKind non-nullable enum). `StatusKind` .NET enum with `KebabEnum` converter."
    - "vitest (`test/user-row.test.ts`) covers every slot combination (bare `{name}`, all-slots-set, status × 4 kinds, action present/absent, string-vs-ViewNode name/meta); asserts `.vms-user-row__status` + `.vms-status-dot--{kind}` class emission per kind; asserts `role=\"button\"` + Enter/Space when action set. Mutation-testable — swap `--online` → `--away` in browser.ts, the kind=online test fails."
    - ".NET (`Tests/UserRowNodeSerializationTests.cs`) asserts discriminator `\"type\":\"user-row\"`, WhenWritingNull on every optional, KebabEnum roundtrip for StatusKind × 4, status-sub-record shape `{\"label\":\"…\",\"kind\":\"…\"}` byte-identical to bun."
    - "AA hand-check for `.vms-status-dot--{kind}` × 4 kinds × 13 themes = 52 pair-checks recorded in test-file header. Kind→color mapping reuses shipped Phase 23 tone palette; verify no regression (graphical UI-state threshold 3:1)."
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "`UserRowNode` interface + `StatusKind` type + ViewNode union entry."
      contains: "\"user-row\""
    - path: "viewmodel-shell/src/browser.ts"
      provides: "`private userRow()` renderer + `renderNode` switch arm."
      contains: "vms-user-row"
    - path: "viewmodel-shell/src/server.ts"
      provides: "Walker arms for `\"user-row\"` (collectActions descent + walkForSectionAction passthrough)."
      contains: "user-row"
    - path: "viewmodel-shell/styles/default.css"
      provides: "`.vms-user-row*` + `.vms-user-row-list` + `.vms-status-dot*` blocks."
      contains: ".vms-user-row"
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "`UserRowNode` record + `UserRowStatus` sub-record + `StatusKind` enum + `[JsonDerivedType]` + walker arms."
      contains: "UserRowNode"
    - path: "viewmodel-shell/test/user-row.test.ts"
      provides: "jsdom render + a11y + mutation coverage + AA hand-check header (52 pair-checks)."
    - path: "viewmodel-shell-dotnet/Tests/UserRowNodeSerializationTests.cs"
      provides: ".NET wire tests including StatusKind KebabEnum + status sub-record shape."
  key_links:
    - from: "viewmodel-shell/src/index.ts"
      to: "viewmodel-shell-dotnet/ViewModels.cs"
      via: "byte-identical wire shape for user-row + status sub-record"
      pattern: "user-row"
    - from: "viewmodel-shell/src/browser.ts (private userRow())"
      to: "viewmodel-shell/styles/default.css"
      via: ".vms-user-row* + .vms-status-dot--{kind} class emission"
      pattern: "vms-status-dot"
---

<objective>
Land `UserRowNode` (COMP-09) end-to-end. Both backends byte-identical. Renderer emits status-dot palette via closed `StatusKind` enum; whole-row `action` mirrors ListRowNode.action a11y/stopPropagation shape (Phase 24 pattern). Ships with vitest + .NET tests + AA hand-check for the 52-pair status-dot matrix.

Purpose: The person-entity display recipe — member pickers, user lists, contact rows. Route B recipe with typed semantic slots; the small `status` sub-record is the ONE new mechanism (a leaf typed sub-record — same posture as LookupItem, NOT a ViewNode slot).

Output: `UserRowNode` shipped both backends; status-dot palette baked in default.css; full test coverage; AA hand-check documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md
@.planning/design/composite-nodes-layer.md
@AGENTS.md
</context>

<interfaces>
<!-- Pulled from 25-PATTERNS.md §1, §2b, §3, §4a, §5. -->

Direct analog: ListRowNode (Phase 24) at index.ts:1617-1649 + browser.ts:1145-1253 + server.ts:189-209 + ViewModels.cs:2123-2152 — same grid + string-lift + action-a11y + stopPropagation shape. UserRowNode adds ONE new mechanism: the `status: {label, kind}` sub-record with StatusKind-driven CSS class palette.

Phase 24 listRow() action-a11y block (browser.ts:1220-1250 per 25-PATTERNS.md §2b) — the exact block userRow() copies for whole-row action:

```typescript
if (n.action) {
  const action = n.action;
  el.tabIndex = 0;
  el.setAttribute("role", "button");
  el.setAttribute("aria-label", flattenedText.slice(0, 200));
  el.addEventListener("click", () => { on(action); });
  el.addEventListener("keydown", (e) => {
    if (e.key === "Enter") on(action);
    else if (e.key === " " || e.key === "Spacebar") { e.preventDefault(); on(action); }
  });
  el.querySelectorAll<HTMLElement>(
    ".vms-button, .vms-checkbox__input, .vms-checkbox, .vms-field__input, a[href]"
  ).forEach(ctrl => {
    ctrl.addEventListener("click", (e) => { e.stopPropagation(); });
  });
}
```

Status-dot render (25-PATTERNS.md §2b new machinery — 8 lines) — the ONLY genuinely new adapter code:

```typescript
if (n.status) {
  const statusEl = document.createElement("span");
  statusEl.className = "vms-user-row__status";
  const dot = document.createElement("span");
  dot.className = `vms-status-dot vms-status-dot--${n.status.kind}`;
  statusEl.appendChild(dot);
  statusEl.appendChild(document.createTextNode(n.status.label));
  el.appendChild(statusEl);
}
```

`.NET` UserRowStatus sub-record shape (25-PATTERNS.md §5) — NOT a ViewNode, so NO `[JsonDerivedType]`. Same posture as LookupItem/FieldOption. WhenWritingNull still applies to any nullable field (Label + Kind are both required here).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add UserRowNode + StatusKind + UserRowStatus wire types (TS + .NET) + CSS</name>
  <files>viewmodel-shell/src/index.ts, viewmodel-shell/styles/default.css, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell/src/index.ts (read ViewNode union near :180-214; read `interface ListRowNode` from Phase 24 for the direct template — the Phase-24-shipped shape with typed slots + closed tone union)
    - viewmodel-shell-dotnet/ViewModels.cs (grep `public record ListRowNode` — the Phase 24 record template for the exact `[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] ViewNode? …` shape; grep `[JsonPolymorphic]` block near :801-804 for JsonDerivedType registration site; grep for `LookupItem` — the sub-record template UserRowStatus mirrors)
    - viewmodel-shell/styles/default.css (grep `.vms-list-row-list` OR `.vms-list--rows` for the single-bordered-surface container pattern; grep `--vms-success\|--vms-warning\|--vms-error\|--vms-text-muted` for the tone palette the status-dot classes reference)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md §1 (LOCKED UserRowNode schema)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §1 (index.ts adds — exact code block lines 101-124 of PATTERNS.md), §4a (default.css block — verbatim lines 855-913 of PATTERNS.md), §5 (ViewModels.cs record lines 1188-1204 of PATTERNS.md)
  </read_first>
  <action>
    In `viewmodel-shell/src/index.ts`:

    (a) Add `UserRowNode` interface verbatim from 25-PATTERNS.md §1 (lines 103-124). Slots: `avatar?: ViewNode`, `name: string | ViewNode` (required), `meta?: string | ViewNode`, `status?: { label: string; kind: "online" | "away" | "offline" | "busy" }`, `trailing?: ViewNode`, `action?: ActionEvent`. Include TSDoc — every slot documented including string-lift trained typography.

    (b) Add `UserRowNode` to the ViewNode union (append near existing v8.0 additions).

    In `viewmodel-shell-dotnet/ViewModels.cs`:

    (c) Add `StatusKind` enum:
    ```csharp
    [JsonConverter(typeof(KebabEnum<StatusKind>))]
    public enum StatusKind { Online, Away, Offline, Busy }
    ```

    (d) Add `UserRowStatus` sub-record (NOT a ViewNode — no [JsonDerivedType], same posture as LookupItem):
    ```csharp
    public record UserRowStatus(string Label, StatusKind Kind);
    ```

    (e) Add `UserRowNode` record per 25-PATTERNS.md §5:
    ```csharp
    public record UserRowNode(
        ViewNode Name,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] ViewNode? Avatar = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] ViewNode? Meta = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] UserRowStatus? Status = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] ViewNode? Trailing = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] ActionDescriptor? Action = null
    ) : ViewNode;
    ```
    Slot-typing policy locked per Phase 24 §5 Analog C: every ViewNode slot is `ViewNode?` (not narrow), consumers wrap strings explicitly.

    (f) Add `[JsonDerivedType(typeof(UserRowNode), "user-row")]` to the ViewNode `[JsonPolymorphic]` block near :801-804.

    In `viewmodel-shell/styles/default.css`:

    (g) Append the full UserRowNode + status-dot CSS block verbatim from 25-PATTERNS.md §4a (lines 861-913 of PATTERNS.md — starts with `.vms-user-row-list`, ends with `.vms-status-dot--busy`). Insert AFTER Phase 24's `.vms-list-row*` block (grep `.vms-list-row` to locate). Key blocks: `.vms-user-row-list` (single-bordered-surface container, `<ul>` styling), `.vms-user-row` (grid `[auto | 1fr | auto]`, per-row divider), `.vms-user-row__content` / `__name` / `__meta` / `__status` (typography + layout), `.vms-status-dot` (base) + `.vms-status-dot--online` / `--away` / `--offline` / `--busy` (kind→color mapping to `--vms-success` / `--vms-warning` / muted / `--vms-error`).

    NO renderer changes yet (Task 2). NO server-side walker changes yet (Task 3).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c '"user-row"' src/index.ts &amp;&amp; grep -c 'export interface UserRowNode' src/index.ts &amp;&amp; grep -c 'UserRowNode\|UserRowStatus\|StatusKind' ../viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c '\.vms-user-row' styles/default.css &amp;&amp; grep -c '\.vms-status-dot' styles/default.css &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet build ../viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"user-row"' viewmodel-shell/src/index.ts` >= 1.
    - `grep -n 'export interface UserRowNode' viewmodel-shell/src/index.ts` returns exactly one match with fields `avatar?`, `name`, `meta?`, `status?`, `trailing?`, `action?`.
    - StatusKind union `"online" | "away" | "offline" | "busy"` present in the interface's `status.kind` type.
    - `viewmodel-shell/src/index.ts` ViewNode union contains `| UserRowNode`.
    - `grep -c 'public record UserRowNode' viewmodel-shell-dotnet/ViewModels.cs` == 1.
    - `grep -c 'public record UserRowStatus' viewmodel-shell-dotnet/ViewModels.cs` == 1 (sub-record, NOT `: ViewNode`).
    - `grep -c 'public enum StatusKind' viewmodel-shell-dotnet/ViewModels.cs` == 1 with KebabEnum converter.
    - `grep -c 'JsonDerivedType(typeof(UserRowNode), "user-row")' viewmodel-shell-dotnet/ViewModels.cs` == 1.
    - Every ViewNode-typed slot on `UserRowNode` .NET record is `ViewNode?` (not narrow) with `WhenWritingNull`.
    - `Status` field typed `UserRowStatus?` with `WhenWritingNull`.
    - `grep -c '\.vms-user-row' viewmodel-shell/styles/default.css` >= 5 (list container + row + __content + __name + __meta + __status).
    - `grep -c '\.vms-status-dot--\(online\|away\|offline\|busy\)' viewmodel-shell/styles/default.css` >= 4.
    - `cd viewmodel-shell && npm run build` — TypeScript compiles.
    - `~/.dotnet/dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` succeeds.
  </acceptance_criteria>
  <done>
    Wire types shipped byte-identical; CSS block installed; builds green. Renderer + walker in Tasks 2/3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire UserRowNode renderer in browser.ts</name>
  <files>viewmodel-shell/src/browser.ts</files>
  <read_first>
    - viewmodel-shell/src/browser.ts (grep `case "list-row"` for the switch-arm insertion pattern; grep `private listRow` for the byte-aligned template — Phase 24 shipped; grep `renderIconSvg` if userRow needs to render an icon-in-avatar path — but avatar is a full ViewNode slot so `this.node()` handles it)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §2a (switch arm addition — line 294), §2b (full `private userRow()` code shape — lines 306-361 of PATTERNS.md; adapt listRow's leading/content/trailing shape, insert status-dot rendering as new machinery)
  </read_first>
  <action>
    In `viewmodel-shell/src/browser.ts`:

    (a) Add `case "user-row": return this.userRow(n, parent, on);` to the `renderNode` switch. Placement: near the Phase 24 case arms.

    (b) Add `private userRow(n: UserRowNode, parent: HTMLElement, on: (a: ActionEvent) => void): void` — direct copy of `listRow()` shape (browser.ts:1145-1253) with the following adjustments:

      - **Container detection**: `const isInList = parent.classList.contains("vms-user-row-list");`. `<li>` in list; else standalone `<div>`.
      - **Class-name composition**: `vms-user-row` base; `vms-user-row--clickable` when action present. (No `--{tone}` or `--{state}` for UserRow.)
      - **Avatar slot** (leading grid col): `<div class="vms-user-row__avatar">` — `this.node(n.avatar, avatarEl, on)`.
      - **Content stack** (middle grid col): `<div class="vms-user-row__content">`.
        - **Name row**: `<div class="vms-user-row__name">`. String → `this.node({ type: "text", value: n.name, style: "body", weight: "medium" }, nameEl, on)`; ViewNode → `this.node(n.name, nameEl, on)`.
        - **Meta row** (optional): `<div class="vms-user-row__meta">`. String → `this.node({ type: "text", value: n.meta, style: "muted" }, metaEl, on)`; ViewNode → `this.node(n.meta, metaEl, on)`.
      - **Status slot** (right grid col — 25-PATTERNS.md §2b new machinery, 8 lines):
        ```typescript
        if (n.status) {
          const statusEl = document.createElement("span");
          statusEl.className = "vms-user-row__status";
          const dot = document.createElement("span");
          dot.className = `vms-status-dot vms-status-dot--${n.status.kind}`;
          statusEl.appendChild(dot);
          statusEl.appendChild(document.createTextNode(n.status.label));
          el.appendChild(statusEl);
        }
        ```
      - **Trailing slot** (optional; between content and status, or appended after status per grid): `<div class="vms-user-row__trailing">`; `this.node(n.trailing, trailingEl, on)`.
      - **Whole-row action**: copy the block from listRow() at browser.ts:1220-1250 verbatim. `el.tabIndex = 0`, `el.setAttribute("role", "button")`, aria-label derived from `el.textContent.slice(0, 200)` (collapse whitespace), click + keydown (Enter + Space with preventDefault). stopPropagation selectors: `.vms-button, .vms-checkbox__input, .vms-checkbox, .vms-field__input, a[href]` — exact string from listRow.

    Use ONLY class names from Task 1's CSS block: `vms-user-row`, `vms-user-row__avatar`, `vms-user-row__content`, `vms-user-row__name`, `vms-user-row__meta`, `vms-user-row__status`, `vms-user-row__trailing`, `vms-user-row--clickable`, `vms-status-dot`, `vms-status-dot--{kind}`.

    NO app-level `<style>` block or inline `style=` — all styling from `.vms-user-row*` in default.css.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'case "user-row"' src/browser.ts &amp;&amp; grep -c 'private userRow' src/browser.ts &amp;&amp; grep -c 'vms-user-row__' src/browser.ts &amp;&amp; grep -c 'vms-status-dot--' src/browser.ts &amp;&amp; grep -c 'stopPropagation' src/browser.ts &amp;&amp; npm run check:core-globals</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "user-row"' viewmodel-shell/src/browser.ts` == 1 (switch arm).
    - `grep -c 'private userRow(' viewmodel-shell/src/browser.ts` == 1.
    - Renderer wraps string `name` in `TextNode { style: "body", weight: "medium" }` (grep `style: "body", weight: "medium"` inside userRow function body).
    - Renderer wraps string `meta` in `TextNode { style: "muted" }`.
    - Standalone detection uses `parent.classList.contains("vms-user-row-list")` (grep).
    - Status dot rendered with class `vms-status-dot--${n.status.kind}` (grep for template literal).
    - Whole-row action block: `role="button"`, `tabIndex = 0`, click + keydown (Enter + Space, Space `preventDefault`).
    - stopPropagation selectors: `.vms-button, .vms-checkbox__input, .vms-checkbox, .vms-field__input, a[href]` (all 5 selectors present).
    - `cd viewmodel-shell && npm run build` succeeds.
    - `cd viewmodel-shell && npm run check:core-globals` green.
  </acceptance_criteria>
  <done>
    Renderer wired; status-dot rendered from closed enum; core-globals guard green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire tree-validator walker arms for user-row in server.ts + ViewModels.cs</name>
  <files>viewmodel-shell/src/server.ts, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell/src/server.ts (grep `case "list-row"` for the Phase 24 shipped template — the exact typed-slots-with-typeof-guard shape; grep `walkForSectionAction` for the passthrough site)
    - viewmodel-shell-dotnet/ViewModels.cs (grep `ListRowNode` inside Collect method — the .NET-side Phase 24 template; grep WalkForSectionAction for the .NET passthrough site)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §3 (server.ts walker arms — the exact `case "user-row"` code block at lines 682-695 of PATTERNS.md)
  </read_first>
  <action>
    In `viewmodel-shell/src/server.ts`:

    (a) Import `UserRowNode` at the top.

    (b) Add `case "user-row"` to `collectActions` walker per 25-PATTERNS.md §3:
    ```typescript
    case "user-row": {
      const ur = node as UserRowNode;
      if (ur.avatar) collectActions(ur.avatar, enclosingForm, out);
      if (typeof ur.name !== "string") collectActions(ur.name, enclosingForm, out);
      if (ur.meta != null && typeof ur.meta !== "string") {
        collectActions(ur.meta, enclosingForm, out);
      }
      if (ur.trailing) collectActions(ur.trailing, enclosingForm, out);
      if (ur.action) recordAction(ur.action, enclosingForm, out);
      return;
    }
    ```
    NOTE: status is a leaf sub-record — NO walker descent. Placement: near other v8.0 composite arms.

    (c) Add passthrough `case "user-row"` to `walkForSectionAction` — descend into avatar/name/meta/trailing ViewNode slots (mirror Phase 24 list-row passthrough shape).

    In `viewmodel-shell-dotnet/ViewModels.cs`:

    (d) Add `UserRowNode` arm to `Collect` (mirror ListRowNode Phase 24 shape):
    ```csharp
    case UserRowNode userRow:
        if (userRow.Avatar != null) Collect(userRow.Avatar, enclosingForm, sink);
        Collect(userRow.Name, enclosingForm, sink);
        if (userRow.Meta != null) Collect(userRow.Meta, enclosingForm, sink);
        if (userRow.Trailing != null) Collect(userRow.Trailing, enclosingForm, sink);
        if (userRow.Action != null) RecordAction(userRow.Action, enclosingForm, sink);
        break;
    ```
    (Adjust the exact method names — `RecordAction` etc. — to match the Phase 24-shipped C# names; grep the existing ListRowNode arm for the exact idiom.)

    (e) Add `UserRowNode` passthrough arm to `WalkForSectionAction` — descend into avatar/name/meta/trailing.

    NOTE: since .NET's `Name` is non-nullable `ViewNode`, no typeof-guard — always call `Collect(userRow.Name, …)`. Meta/Avatar/Trailing are nullable — guard with `!= null`.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'case "user-row"' src/server.ts &amp;&amp; grep -c 'UserRowNode' src/server.ts &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; grep -c 'UserRowNode' ../viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; dotnet build ../viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "user-row"' viewmodel-shell/src/server.ts` >= 2 (collectActions + walkForSectionAction).
    - Walker descends into every ViewNode slot: avatar, name (typeof-guarded), meta (null + typeof-guarded), trailing. Records action.
    - Walker does NOT descend into `status` (leaf sub-record — verify by grep that no `ur.status` appears inside collectActions).
    - .NET Collect method contains `UserRowNode` case arm descending into every ViewNode slot AND recording Action.
    - .NET WalkForSectionAction contains UserRowNode passthrough arm.
    - `cd viewmodel-shell && npm run build` succeeds.
    - `~/.dotnet/dotnet build viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` succeeds.
  </acceptance_criteria>
  <done>
    Tree-validator descent wired on both backends; status sub-record correctly treated as leaf; builds green.
  </done>
</task>

<task type="auto">
  <name>Task 4: vitest render + a11y tests + AA hand-check header + .NET serialization tests</name>
  <files>viewmodel-shell/test/user-row.test.ts, viewmodel-shell-dotnet/Tests/UserRowNodeSerializationTests.cs</files>
  <read_first>
    - viewmodel-shell/test/list-row.test.ts (Phase 24 shipped — the direct byte-aligned template; mimic every it() block + stopPropagation test + AA hand-check header format)
    - viewmodel-shell/test/avatar-render.test.ts (the DOM/a11y assertion + stylesheet-load pattern)
    - viewmodel-shell/test/text-caption.test.ts (AA-contrast hand-check header format at :23-52 + stylesheet-load pattern at :64-76)
    - viewmodel-shell-dotnet/Tests/ListRowNodeSerializationTests.cs (Phase 24 shipped — byte-aligned .NET template with WhenWritingNull + KebabEnum + discriminator tests)
    - viewmodel-shell/styles/themes/*.css (12 theme files — AA hand-check spans default + 12 = 13 themes)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md §8 (AA-contrast checklist — UserRow status-dots × 4 kinds × 13 themes = 52 pair-checks)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §9 (vitest coverage lines 1483) + §10 (.NET serialization tests lines 1533-1549)
  </read_first>
  <action>
    Create `viewmodel-shell/test/user-row.test.ts`:

    (a) File header AA-contrast hand-check block (mirror `test/text-caption.test.ts:23-52`): "AA-contrast hand-check for UserRowNode status-dots (COMP-09, v8.0.0)". 52 pair-checks — `.vms-status-dot--{kind}` colors × 4 kinds (`online`→success, `away`→warning, `offline`→muted, `busy`→error) × 13 themes (default + 12 in `viewmodel-shell/styles/themes/`) against `--vms-surface`. Target: UI-state 3:1 (graphical). Colors reuse Phase 23 shipped palette — should pass by construction; verify no regression. Where any theme drops, document deepen-via-color-mix mitigation.

    (b) Stylesheet-load setup: reuse `test/text-caption.test.ts:64-76` pattern (read `../styles/default.css` from disk; inject as `<style data-vms-default>` via beforeAll).

    (c) Tests in `describe("UserRowNode (COMP-09)", () => { ... })`:
      - `it("emits <li> when in .vms-user-row-list container", ...)`.
      - `it("emits <div> standalone when parent lacks .vms-user-row-list", ...)`.
      - `it("wraps string name in TextNode with style:body weight:medium", ...)` — assert `.vms-user-row__name > .vms-text.vms-text--body.vms-text--medium`.
      - `it("wraps string meta in TextNode with style:muted", ...)`.
      - `it("passes ViewNode name through without wrapping", ...)` — pass a `TextNode` or `SectionNode` as name.
      - `it("passes ViewNode meta through without wrapping", ...)`.
      - `it("omits meta element when meta is absent", ...)`.
      - `it("renders avatar slot through node dispatch (AvatarNode)", ...)` — pass an AvatarNode; assert `.vms-user-row__avatar > .vms-avatar`.
      - `it("status kind=online emits vms-status-dot--online", ...)`.
      - `it("status kind=away emits vms-status-dot--away", ...)`.
      - `it("status kind=offline emits vms-status-dot--offline", ...)`.
      - `it("status kind=busy emits vms-status-dot--busy", ...)`.
      - `it("status label renders AFTER the dot", ...)` — assert DOM child order: dot span first, text node second.
      - `it("omits status element when status absent", ...)`.
      - `it("with action, emits role=button + tabindex=0 + aria-label + vms-user-row--clickable", ...)` — aria-label contains name + meta text.
      - `it("dispatches action on Enter", ...)` — spy call.
      - `it("dispatches action on Space + preventDefault", ...)`.
      - `it("Tab does NOT dispatch", ...)`.
      - `it("stopPropagation on nested .vms-button click", ...)` — spy proves nested-button-click fires only button's action, not row's.
      - `it("renders trailing slot through node dispatch", ...)`.
      - Mutation-test proof block (comment) — name exact reverts: swap `--online` → `--away` breaks kind=online test; remove weight:medium wrap breaks name-typography test.

    Create `viewmodel-shell-dotnet/Tests/UserRowNodeSerializationTests.cs`:

    (d) Ensure `global using Xunit;` present in `Tests/GlobalUsings.cs`.

    (e) Tests mirroring `Tests/ListRowNodeSerializationTests.cs` structure line-for-line:
      - `Type_SerializesAsUserRow()` — asserts `"type":"user-row"`.
      - `BareNode_MinimalShape_OnlyRequiredFields()` — `new UserRowNode(Name: new TextNode("X"))` → JSON has only `type` + `name`; every optional ABSENT.
      - `Avatar_OmittedIsAbsent()`, `Meta_OmittedIsAbsent()`, `Status_OmittedIsAbsent()`, `Trailing_OmittedIsAbsent()`, `Action_OmittedIsAbsent()`.
      - `Avatar_SetSerializesPolymorphically()` — set to AvatarNode; assert nested `"type":"avatar"`.
      - `StatusKind_Online_SerializesAsKebabOnline()`, `StatusKind_Away_SerializesAsKebabAway()`, `StatusKind_Offline_SerializesAsKebabOffline()`, `StatusKind_Busy_SerializesAsKebabBusy()`.
      - `Status_SerializesAsSubRecord()` — assert JSON contains `"status":{"label":"…","kind":"…"}` (sub-record shape, not polymorphic `"type":"…"`).
      - `AllFieldsSet_AllPresent()` — full serialize proof.

    Every test uses the exact `JsonSerializerOptions` from `ListRowNodeSerializationTests.cs` (`PropertyNamingPolicy = JsonNamingPolicy.CamelCase`).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npx vitest run test/user-row.test.ts &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --filter 'UserRowNodeSerializationTests' --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/test/user-row.test.ts` exists.
    - `grep -c '^\s*it(' viewmodel-shell/test/user-row.test.ts` >= 15.
    - File header contains "AA-contrast hand-check" with 52-pair or per-theme summary (4 kinds × 13 themes).
    - `grep -c 'vms-status-dot--online\|vms-status-dot--away\|vms-status-dot--offline\|vms-status-dot--busy' viewmodel-shell/test/user-row.test.ts` >= 4 (one it() per kind).
    - `grep -c 'weight: "medium"\|vms-text--medium' viewmodel-shell/test/user-row.test.ts` >= 1.
    - `grep -c 'role.*"button"' viewmodel-shell/test/user-row.test.ts` >= 1.
    - `grep -c 'preventDefault\|defaultPrevented' viewmodel-shell/test/user-row.test.ts` >= 1.
    - `grep -c 'stopPropagation\|nested' viewmodel-shell/test/user-row.test.ts` >= 1.
    - `viewmodel-shell-dotnet/Tests/UserRowNodeSerializationTests.cs` exists with >= 10 `[Fact]` methods.
    - `grep -c 'StatusKind\.\(Online\|Away\|Offline\|Busy\)' viewmodel-shell-dotnet/Tests/UserRowNodeSerializationTests.cs` >= 4.
    - `grep -c 'Status_SerializesAsSubRecord\|"status":\{' viewmodel-shell-dotnet/Tests/UserRowNodeSerializationTests.cs` >= 1.
    - `cd viewmodel-shell && npx vitest run test/user-row.test.ts` — green.
    - `~/.dotnet/dotnet test viewmodel-shell-dotnet/Tests --filter 'UserRowNodeSerializationTests'` — green.
    - Mutation-verified: temporarily swap `--online` → `--away` in browser.ts, run vitest, the kind=online test fails; revert.
    - AA hand-check header shows per-theme ratio >= 3:1 for each kind OR documented deepening.
  </acceptance_criteria>
  <done>
    Tests exist and pass; mutation-verified; AA hand-check recorded for 13 themes × 4 kinds; .NET wire tests green including status sub-record shape.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This addition introduces no new trust boundary. Wire-shape addition of a passive rendering node with an `action?` slot that follows the shipped `ListRowNode.action` pattern (already gated at the same trust boundary as every other action-bearing node). Status is a leaf sub-record with a closed enum — no user-input processing, no security surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | (none) | `UserRowNode` — rendering-only structured node with optional whole-row `action` | accept | No new input parsing, no external data flow, no auth interaction, no new URL. `action` slot is an `ActionEvent` (same as every other action-bearing node); action-name uniqueness enforced by the shipped walker. |
| T-25-01-02 | Tampering | `stopPropagation` on nested interactive descendants (whole-row action + nested Button in `trailing`) | accept | Selectors (`.vms-button, .vms-checkbox__input, .vms-checkbox, .vms-field__input, a[href]`) copied verbatim from listRow() at browser.ts:1220-1250 (Phase 24 shipped). No new machinery; no delta from Phase 24's disposition. |
| T-25-01-03 | Information Disclosure | `status.label` renders via `document.createTextNode(n.status.label)` — safe (textContent, not innerHTML) | accept | Framework already uses `createTextNode` for every user-facing string; the label follows the same discipline. Even a malicious server-supplied label cannot inject markup. |
| T-25-01-04 | Denial of Service | Deeply nested UserRowNode in `avatar`/`name`/`meta`/`trailing` slots | accept | Same recursion depth ceiling as every other typed-slot composite; existing tree walker guards depth. No new machinery. |
</threat_model>

<verification>
Task-scoped verifications above. Full green-tree gate deferred to Plan 25-10.
</verification>

<success_criteria>
- `UserRowNode` wire type shipped both backends byte-identical (interface + .NET record + JsonDerivedType + StatusKind enum + UserRowStatus sub-record).
- Renderer emits correct DOM per CONTEXT §1 (grid layout, string-lift to trained typography, status-dot palette from closed enum, whole-row action a11y).
- Tree-validators descend into every ViewNode slot; status treated as leaf sub-record; action name uniqueness participates.
- vitest coverage >= 15 tests including status × 4 kinds, string-vs-ViewNode name/meta, Enter/Space dispatch, stopPropagation, mutation-verified.
- .NET serialization tests cover discriminator + WhenWritingNull + KebabEnum × 4 + status sub-record shape.
- AA hand-check recorded for status-dot × 4 kinds × 13 themes = 52 pair-checks.
- Wire protocol token stays `viewmodel-shell/1.0` (additive change, no wire break).
</success_criteria>

<output>
Create `.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-01-SUMMARY.md` recording: artifacts touched, mutation-test evidence, AA hand-check verdict per theme × per kind, and confirmation that the shipped `UserRowNode` behavior matches CONTEXT §1 exactly.
</output>
