---
phase: 25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/design/composite-nodes-layer.md
  - AGENTS.md
autonomous: true
requirements: [COMP-09, COMP-10, COMP-10a, COMP-11, COMP-11a, COMP-12, COMP-12a, COMP-13, COMP-13a]

must_haves:
  truths:
    - "`.planning/design/composite-nodes-layer.md` **Shipped Recipe Inventory** table grows with 5 secondary composite rows (Phase 24 populated 4 primaries; Phase 25 completes the shipped set with 5 secondaries — 9 composites total + primitives)."
    - "Each secondary inventory row records: composite name, slot summary, phase (25 with COMP-XX), wire type discriminator, .NET record name, Phase 23/24 foundations consumed, + any key mechanism reuse OR new-mechanism callout. Two critical callouts: (a) **TimelineNode's ::before rail+dot CSS mechanism is the ONE genuinely new CSS pattern in Phase 25** — apps CANNOT compose this from primitives; (b) **ChipNode.dismissAction is a caller-supplied ActionEvent slot** (mirrors ModalNode, NOT AlertNode.dismissible)."
    - "`AGENTS.md` \"Currently shipped recipes\" section grows from 4 primaries (Phase 24) to 9 composites (4 primaries + 5 secondaries). Replace the \"Phase 25 ... TBD\" line with 5 populated entries. Preserve the append-only convention: docs describe SHIPPED recipes only."
    - "Both documents flag the TimelineNode ::before mechanism as the ONE genuinely new CSS pattern in Phase 25 — the composite exists specifically because \"apps describe, never decorate\" precludes app-CSS for a rail."
    - "Both documents flag ChipNode.dismissAction as intentionally distinct from AlertNode.dismissible — chips need identity-carrying dispatch."
    - "Phase 26 release ritual (aligned v8.0.0 npm + NuGet publish; tailnet verification page across all 10 composites) noted as \"TBD in Phase 26\" in both docs — no premature detail."
  artifacts:
    - path: ".planning/design/composite-nodes-layer.md"
      provides: "Shipped Recipe Inventory table filled in for 5 secondaries + Phase 26 TBD placeholder."
      contains: "UserRowNode"
    - path: "AGENTS.md"
      provides: "Currently shipped recipes list populated with 5 secondaries + reuse + new-mechanism callouts (Timeline rail + Chip dismissAction distinction)."
      contains: "TimelineNode"
  key_links:
    - from: ".planning/design/composite-nodes-layer.md"
      to: "AGENTS.md"
      via: "same 5-composite secondary inventory referenced across both docs"
      pattern: "UserRowNode\\|DetailListNode\\|TimelineNode\\|SettingListNode\\|ChipListNode"
---

<objective>
Fill the Shipped Recipe Inventory table in `.planning/design/composite-nodes-layer.md` (design-of-record) and replace the "Phase 25 ... TBD" line in `AGENTS.md` "Currently shipped recipes" section — both with the 5 secondaries. Both docs flag the TimelineNode ::before rail-and-dot mechanism as the ONE genuinely new CSS pattern in Phase 25 AND flag ChipNode.dismissAction as intentionally distinct from AlertNode.dismissible.

Purpose: Both docs are the operator's + future-agents' mental model of the composite-nodes layer's growth. Phase 24 landed the 4 primaries; Phase 25 completes the shipped set of 9 composite recipes. Two properties MUST be visible in the doctrine: (a) the Timeline rail mechanism is new + apps cannot compose it, (b) Chip dismissAction is caller-supplied.

Output: Both docs updated in same commit run; grep-verifiable content for each of the 5 secondaries; Timeline-new-CSS + Chip-dismissAction callouts prominent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grow the Shipped Recipe Inventory table in .planning/design/composite-nodes-layer.md with 5 secondaries</name>
  <files>.planning/design/composite-nodes-layer.md</files>
  <read_first>
    - .planning/design/composite-nodes-layer.md (full read; find the "Shipped recipe inventory" table populated by Phase 24 plan 24-05 with 4 primary rows around line 80 per 25-PATTERNS.md §12; find §4d "Composites (Phase 25 — secondary)" free-form section if one exists — the doc landed Phase 23 with Phase 25 as free-form design; Phase 25 lands the actual recipes and this task confirms the free-form matches shipped reality)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md §11 (design-doc + AGENTS.md update mandate)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §12 (design-doc table content — the exact rows to insert at lines 1657-1662)
  </read_first>
  <action>
    In `.planning/design/composite-nodes-layer.md`:

    (a) Locate the "Shipped recipe inventory" section populated by Phase 24 plan 24-05 with 4 rows (ListRowNode, MessageNode+MessageListNode, AlertNode, EmptyStateNode).

    (b) APPEND 5 new rows for the secondaries per 25-PATTERNS.md §12:

    ```
    | UserRowNode | avatar, name, meta, status:{label,kind}, trailing, action | 25 (COMP-09) | "user-row" | UserRowNode | AvatarNode (COMP-04), TextNode caption/weight (COMP-01/02), status-dot palette (baked-in CSS) |
    | DetailRowNode + DetailListNode | label, value, tone, icon; labelWidth on list | 25 (COMP-10 + 10a) | "detail-row" / "detail-list" | DetailRowNode / DetailListNode | IconName (v7.0), TextNode body (COMP-01); dl/dt/dd semantic HTML |
    | TimelineEntryNode + TimelineNode | time, description, tone, icon | 25 (COMP-11 + 11a) | "timeline-entry" / "timeline" | TimelineEntryNode / TimelineNode | TextNode caption/body (COMP-01); NEW baked-in CSS mechanism: rail via ::before on container, dot via ::before on entry — apps CANNOT compose this from primitives |
    | SettingRowNode + SettingListNode | icon, label, description, trailing, action; heading on list | 25 (COMP-12 + 12a) | "setting-row" / "setting-list" | SettingRowNode / SettingListNode | TextNode weight (COMP-02), CheckboxNode(variant:"switch") (COMP-03) as natural trailing pairing |
    | ChipNode + ChipListNode | label, tone, icon, dismissAction, action | 25 (COMP-13 + 13a) | "chip" / "chip-list" | ChipNode / ChipListNode | IconName (v7.0); dismissAction is a caller-supplied ActionEvent SLOT (identity-carrying names like remove-filter-42), NOT a fixed-name bool like AlertNode.dismissible |
    ```

    (c) If the doc has a §4d "Composites (Phase 25 — secondary)" free-form section from Phase 23 planning, verify each entry now matches shipped reality. If any free-form entry contradicts what Plans 25-01..05 shipped (e.g. free-form promised a `dismissible: bool` on Chip but the shipped shape is `dismissAction: ActionEvent`), UPDATE the free-form to match reality.

    (d) Add or extend §5 "Phase 26 release ritual" — mark it "TBD in Phase 26". Do NOT populate.

    (e) The inventory table now shows 9 shipped composites + 10 including EmptyStateNode + 11 including the AvatarNode primitive it consumed. Update any prose that mentions the running count (e.g. "the shipped set of X recipes").
  </action>
  <verify>
    <automated>test -f .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'UserRowNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'DetailRowNode\|DetailListNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'TimelineNode\|TimelineEntryNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'SettingRowNode\|SettingListNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'ChipNode\|ChipListNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'apps CANNOT\|apps describe' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'dismissAction' .planning/design/composite-nodes-layer.md</automated>
  </verify>
  <acceptance_criteria>
    - Design doc contains rows for all 5 secondaries: UserRowNode, DetailRowNode + DetailListNode, TimelineEntryNode + TimelineNode, SettingRowNode + SettingListNode, ChipNode + ChipListNode.
    - `grep -c 'UserRowNode' .planning/design/composite-nodes-layer.md` >= 1.
    - `grep -c 'TimelineNode\|TimelineEntryNode' .planning/design/composite-nodes-layer.md` >= 1.
    - `grep -c 'ChipNode\|ChipListNode' .planning/design/composite-nodes-layer.md` >= 1.
    - **Timeline ::before rail-and-dot callout present**: `grep -c 'apps CANNOT\|apps describe\|::before.*rail\|::before.*dot\|NEW baked-in' .planning/design/composite-nodes-layer.md` >= 1.
    - **Chip dismissAction vs Alert.dismissible callout present**: `grep -c 'dismissAction.*ActionEvent\|dismissAction.*caller\|NOT.*AlertNode.dismissible\|distinct from AlertNode' .planning/design/composite-nodes-layer.md` >= 1.
    - Phase 26 TBD preserved (docs don't race ahead).
    - Existing Phase 24 primary rows unchanged (verify by git diff — only the Phase 25 rows added).
  </acceptance_criteria>
  <done>
    Design doc inventory table grown with 5 secondaries; Timeline rail + Chip dismissAction callouts prominent; Phase 26 TBD preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace AGENTS.md "Phase 25 ... TBD" line with populated inventory</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md around :744-752 (grep `Currently shipped recipes` — Phase 24 populated with 4 primary bullets; the "Phase 25 composites ... TBD" line at the tail is what this task replaces)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §13 (AGENTS.md content — exact suggested replacement text lines 1689-1697)
  </read_first>
  <action>
    In `AGENTS.md`:

    (a) Locate the "Phase 25 composites ... TBD in `/gsd:plan-phase 25`" line at the tail of the "Currently shipped recipes" section (per 25-PATTERNS.md §13).

    (b) REPLACE the "TBD" line with 5 populated bullet entries per 25-PATTERNS.md §13 (lines 1689-1697):

    ```
    Phase 25 (v8.0.0, secondary composites):
    - UserRowNode (COMP-09) — person entity display: avatar + name + meta + right-aligned status dot. status?: {label, kind} uses a closed 4-value StatusKind enum (online→success, away→warning, offline→muted, busy→danger). Whole-row action? for member-picker pattern.
    - DetailRowNode + DetailListNode (COMP-10 + 10a) — key-value with aligned label column via CSS grid on dl/dt/dd (proper screen-reader term/definition semantics). labelWidth?: "sm"|"md"|"lg" closed enum on the list (8/10/12rem). Trained typography: label = text-xs uppercase weight:500 muted; value = body.
    - TimelineEntryNode + TimelineNode (COMP-11 + 11a) — activity feed with baked-in rail-and-dot CSS mechanism. ::before rail on the container + ::before dot per entry with tone-encoded border — apps CANNOT compose this from primitives (the composite exists specifically to bake it in, per "apps describe, never decorate"). Trained typography: time = caption; description = body (string or ViewNode).
    - SettingRowNode + SettingListNode (COMP-12 + 12a) — label + description + trailing control. Grid: [body | control] = 1fr auto, align-items: center. Natural pairing with CheckboxNode(variant:"switch") from COMP-03 in the trailing slot; also common: ButtonNode, LinkNode. Optional list heading?: string.
    - ChipNode + ChipListNode (COMP-13 + 13a) — tinted-pill cluster (filter chips, selected tags, categories). dismissAction?: ActionEvent DEVIATES from AlertNode.dismissible — it's a caller-supplied ActionEvent slot (identity-carrying: remove-filter-42), not a fixed-name boolean. Chip needs the app to name the action because chips typically operate on specific identities. If BOTH action and dismissAction are set, the X's click does stopPropagation so it doesn't double-fire the whole-chip click.

    Phase 26 release ritual: TBD (aligned v8.0.0 npm + NuGet publish; see .planning/design/composite-nodes-layer.md §5).
    ```

    (c) The rest of the AGENTS.md "Route B composite-nodes layer" governance section is UNCHANGED — only the "Phase 25 ... TBD" line gets replaced.

    (d) Also grow the "genuine composites we shipped" narrative if any prose elsewhere in the Route B section references the count (was 4 primaries + 1 primitive = 5; now 9 composites + 1 primitive = 10).
  </action>
  <verify>
    <automated>grep -c 'Phase 25 (v8\.0\.0, secondary composites)\|Phase 25.*secondary composites' AGENTS.md &amp;&amp; grep -c 'UserRowNode\|DetailListNode\|TimelineNode\|SettingListNode\|ChipListNode' AGENTS.md &amp;&amp; grep -c 'apps CANNOT\|apps describe' AGENTS.md &amp;&amp; grep -c 'DEVIATES from AlertNode\|deviates from AlertNode\|caller-supplied ActionEvent' AGENTS.md &amp;&amp; grep -c 'CheckboxNode.*switch\|natural pairing' AGENTS.md &amp;&amp; grep -c 'Phase 26 release ritual\|Phase 26.*TBD' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Old "TBD in /gsd:plan-phase 25" line REMOVED — verify: `grep -c 'TBD in.*plan-phase 25' AGENTS.md` == 0.
    - New Phase 25 heading present: `grep -c 'Phase 25 (v8\.0\.0, secondary composites)' AGENTS.md` >= 1.
    - All 5 secondaries named: UserRowNode, DetailListNode, TimelineNode, SettingListNode, ChipListNode each grep-matched.
    - **Timeline apps-CANNOT-compose callout present**: `grep -c 'apps CANNOT\|apps describe' AGENTS.md` >= 1.
    - **Chip dismissAction DEVIATES from AlertNode.dismissible callout present**: `grep -c 'DEVIATES from AlertNode\|caller-supplied ActionEvent' AGENTS.md` >= 1.
    - CheckboxNode(variant:"switch") natural pairing mentioned.
    - Phase 26 TBD preserved.
    - Rest of Route B governance section unchanged (verify by git diff — only the "Phase 25 ... TBD" line region changed).
  </acceptance_criteria>
  <done>
    AGENTS.md "Currently shipped recipes" section populated with 5 secondaries; Timeline + Chip callouts prominent; Route B governance section preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan. No code, no wire change, no runtime surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-01 | (none) | Design doc + AGENTS.md inventory update | accept | No new input parsing, no external data flow, no auth interaction, no new URL. Documentation update; zero runtime surface change. |
| T-25-06-02 | Information Disclosure | Doc could drift ahead of what ships — future agents rely on it as truth (e.g. an inventory row for a composite not actually landed) | mitigate | Wave 1 file-disjoint means this plan runs alongside Plan 25-01 (UserRow) NOT depending on it — but by convention docs describe SHIPPED recipes only. Plan 25-10's cross-check verifies every inventory row has a landed composite. If a plan gets deferred, this doc's Phase 25 row would need reverting — Plan 25-10's requirement-to-artifact cross-check catches that mismatch. |
| T-25-06-03 | Information Disclosure | The Timeline "apps CANNOT compose from primitives" claim could mislead future contributors into thinking OTHER composites' visual mechanisms also can't be composed | mitigate | The doc explicitly narrows the claim to Timeline's rail-and-dot mechanism: "apps describe, never decorate" precludes app-CSS FOR A RAIL specifically. Other composites use existing primitives (grid, flexbox, border, color-mix) — those apps CAN compose. The doc phrasing must not overreach. |
</threat_model>

<verification>
Task-scoped verifications above. Both files pure markdown; no build/test to run beyond grep checks.
</verification>

<success_criteria>
- Design doc Shipped Recipe Inventory table grew with 5 secondaries.
- AGENTS.md "Currently shipped recipes" populated with 5 secondaries (4 primaries preserved).
- TimelineNode "apps CANNOT compose from primitives" callout prominent in both docs.
- ChipNode.dismissAction "DEVIATES from AlertNode.dismissible" callout prominent in both docs.
- Phase 26 release ritual TBD preserved.
- Both files remain the doctrine-of-record for the Route B composite-nodes layer.
</success_criteria>

<output>
Create `.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-06-SUMMARY.md` recording: both files updated, grep-verified inventory content for each of the 5 secondaries, and the Timeline-new-CSS + Chip-dismissAction distinction callouts.
</output>
