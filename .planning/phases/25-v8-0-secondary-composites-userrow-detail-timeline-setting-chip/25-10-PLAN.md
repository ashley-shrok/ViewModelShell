---
phase: 25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip
plan: 10
type: execute
wave: 7
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05", "25-06", "25-07", "25-08", "25-09"]
files_modified: []
autonomous: false
requirements: [COMP-09, COMP-10, COMP-10a, COMP-11, COMP-11a, COMP-12, COMP-12a, COMP-13, COMP-13a]

must_haves:
  truths:
    - "The FULL green-tree gate runs GREEN: `npm run build && check:test-types && check:core-globals && check:aa-contrast && check:no-demo-style && check:demo-types && npx vitest run` in `viewmodel-shell/`, PLUS `dotnet test viewmodel-shell-dotnet/Tests`, PLUS every `demo/**/*.Tests.csproj`, PLUS `bun run parity/run.ts`."
    - "The AA-contrast hand-check is complete and documented across all 5 composite test files: UserRowNode status-dots (52 pair-checks), DetailRowNode label + tone-accent value (65+ pair-checks), TimelineEntryNode dot-borders (52 pair-checks), SettingRowNode (0 new — reuses Phase 23 + 24 shipped pairs, documented), ChipNode tinted-pill (52 pair-checks — HIGHEST risk). Total NEW pair-checks: ~221 documented in test-file headers."
    - "**Ashley (operator) has done a visual smoke** on the Showcase Secondary Composites section served over the tailnet: UserRowNode status dots visibly render correct colors for all 4 kinds (online=green, away=yellow, offline=gray, busy=red); DetailListNode label column visibly aligned via labelWidth (sm/md/lg proves the closed-enum crossing); **TimelineNode rail-and-dot visually renders** (the ONE new CSS mechanism — vertical rail with tone-encoded dot borders); SettingRowNode + CheckboxNode(variant:'switch') natural pairing renders switch in trailing slot correctly; ChipNode tinted-pill colors render correctly across all 4 tones; dismissible chip's X visibly present + click dismisses (fires the caller-supplied dispatch)."
    - "**The Timeline ::before rail-and-dot visual mechanism is verified working end-to-end** — the ONE genuinely new CSS mechanism in Phase 25. Ashley confirms via live browser inspection that the rail spans the container top-to-bottom and each entry has a dot with tone-encoded border color."
    - "**The ChipNode dismissAction posture** is verified end-to-end: dismissible chip in Showcase dispatches the CALLER-SUPPLIED name (verified either by dev-tools inspection of the dispatch action name OR by watching the Showcase app-handler receive the caller-supplied name)."
    - "Every COMP-XX requirement is CLOSED: COMP-09 (UserRowNode), COMP-10 + 10a (DetailRowNode + DetailListNode), COMP-11 + 11a (TimelineEntryNode + TimelineNode), COMP-12 + 12a (SettingRowNode + SettingListNode), COMP-13 + 13a (ChipNode + ChipListNode). Each has: code shipped + tests green + parity tripwire green + demo adoption + CHANGELOG entry + design-doc inventory row + AGENTS.md entry."
    - "NO release ships in this plan (batch-then-ship LOCKED per CONTEXT §10). No `npm publish`, no `dotnet nuget push`, no version bump, no tag. `package.json` stays 7.1.0; `.csproj` stays 7.0.0."
    - "Phase 25 SUMMARY records a requirement-to-artifact cross-check: for each of COMP-09..13a the exact files, tests, tripwires, demo instances, CHANGELOG/MIGRATION entries."
  artifacts:
    - path: ".planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md"
      provides: "Records green-tree gate results + AA hand-check aggregate + Ashley's visual sign-off (including Timeline ::before + Chip dismissAction end-to-end verification) + COMP-XX requirement-to-artifact cross-check."
      contains: "green"
  key_links:
    - from: "All prior plans (25-01..09)"
      to: "The green-tree gate + Ashley sign-off"
      via: "Phase 25 landing verification"
      pattern: "green"
---

<objective>
Run the FULL local green-tree gate one final time with everything from Plans 25-01..09 in place, hand-check aggregate AA-contrast across all 5 secondary composites (~221 total new pair-checks), run parity green across all backends, get Ashley's visual sign-off on the Showcase Secondary Composites section served over the tailnet — with particular attention to (a) the TimelineNode ::before rail-and-dot visual mechanism (the ONE new CSS pattern in Phase 25), (b) the ChipNode dismissAction end-to-end posture verification, and (c) the SettingRowNode + CheckboxNode(variant:"switch") natural pairing. Record the requirement-to-artifact cross-check for COMP-09..13a.

Purpose: Phase 25 does NOT ship a release (batch-then-ship LOCKED per CONTEXT §10). But every AGENTS.md gate must be green before commit lands, the AA-contrast per-new-pair discipline is non-negotiable (banked lesson), the fleet-adoption discipline requires operator eyeballing for the NEW visual mechanism (Timeline rail+dot), and the ChipNode dismissAction posture end-to-end verification proves the divergence-from-AlertNode is live. This plan is the landing verification — it doesn't add code, it proves the code from Plans 25-01..09 is correct + coherent.

Output: SUMMARY.md documenting green-tree evidence, per-composite AA aggregate, Ashley's sign-off note (with Timeline + Chip specific verification), COMP-XX cross-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full green-tree gate + parity + every .NET test project</name>
  <files></files>
  <read_first>
    - AGENTS.md Working Agreement — the exact green-tree gate command list (npm run build + check:test-types + check:core-globals + check:aa-contrast + check:no-demo-style + check:demo-types + npx vitest run + .NET tests + parity + every `demo/**/*.Tests.csproj`)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md §Specifics dotnet PATH: `export PATH="$HOME/.dotnet:$PATH"` before parity harness AND .NET tests
  </read_first>
  <action>
    Ensure `~/.dotnet` is on PATH first. Then run each gate command in sequence, capturing output. Any RED result blocks Phase 25 closeout — either fix the underlying issue OR (if pre-existing per AGENTS.md rule) surface to operator with an explicit waiver request.

    **Gate command sequence:**

    (a) Framework build + all `check:*` scripts + vitest, in `viewmodel-shell/`:
    ```
    cd viewmodel-shell
    npm run build
    npm run check:test-types
    npm run check:core-globals
    npm run check:aa-contrast
    npm run check:no-demo-style
    npm run check:demo-types
    npx vitest run
    ```

    (b) Framework .NET Tests project:
    ```
    export PATH="$HOME/.dotnet:$PATH"
    dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal
    ```

    (c) Every `demo/**/*.Tests.csproj`:
    ```
    for p in $(find demo -name '*.Tests.csproj'); do
      echo "=== $p ==="
      dotnet test "$p" --nologo -v minimal
    done
    ```

    (d) Cross-backend parity:
    ```
    cd parity
    bun run run.ts
    ```

    RECORD every gate's result in the phase SUMMARY. Any RED result STOPS Phase 25 closeout.

    **Special checks for this phase:**

    - **TimelineNode ::before CSS mechanism installed correctly**: `grep -c '\.vms-timeline::before\|\.vms-timeline-entry::before' viewmodel-shell/styles/default.css` >= 2 (rail + dot). If missing, Plan 25-03 was incomplete.
    - **ChipNode.dismissAction posture proved at code level**: `grep -c 'on(dismissAction)\|on(chipDismissAction)' viewmodel-shell/src/browser.ts` >= 1 for the chip() function; `grep -c 'on({name: "dismiss"})' viewmodel-shell/src/browser.ts` should NOT be inside the chip() function (only inside alert() from Phase 24).
    - **SettingRow extended stopPropagation includes switch classes**: `grep -c 'vms-checkbox__input\|vms-field--switch' viewmodel-shell/src/browser.ts` >= 1 (from settingRow function).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; npm run check:test-types &amp;&amp; npm run check:core-globals &amp;&amp; npm run check:aa-contrast &amp;&amp; npm run check:no-demo-style &amp;&amp; npm run check:demo-types &amp;&amp; npx vitest run &amp;&amp; cd .. &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal &amp;&amp; (for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done) &amp;&amp; cd parity &amp;&amp; bun run run.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `viewmodel-shell/` succeeds.
    - `npm run check:test-types` green.
    - `npm run check:core-globals` green (core index.ts has ZERO platform globals; new composite renderers legitimately live in browser.ts).
    - `npm run check:aa-contrast` green (fixed 13-pair gate — the new Phase 25 pairs are hand-checked in test-file headers).
    - `npm run check:no-demo-style` green.
    - `npm run check:demo-types` green (Showcase Secondary Composites section compiles under strict tsconfig).
    - `npx vitest run` green — all shipped tests + the new Phase 25 files (user-row.test.ts, detail-row.test.ts, timeline.test.ts, setting-row.test.ts, chip.test.ts) pass.
    - `dotnet test viewmodel-shell-dotnet/Tests` green — all shipped .NET tests + the new Phase 25 files (UserRowNodeSerializationTests.cs, DetailRowNodeSerializationTests.cs, DetailListNodeSerializationTests.cs, TimelineEntryNodeSerializationTests.cs, TimelineNodeSerializationTests.cs, SettingRowNodeSerializationTests.cs, SettingListNodeSerializationTests.cs, ChipNodeSerializationTests.cs, ChipListNodeSerializationTests.cs) pass.
    - Every `demo/**/*.Tests.csproj` green.
    - `bun run parity/run.ts` byte-identical GREEN across all backends AND all `expectBodyContains` tripwires match (foundations from Phase 23 + primary composites from Phase 24 + secondary composites from Plan 25-08).
    - **Timeline ::before mechanism verification**: `grep -c '\.vms-timeline::before\|\.vms-timeline-entry::before' viewmodel-shell/styles/default.css` >= 2.
    - **Chip dismissAction posture verification at code level**: `grep -c 'on({name: "dismiss"})' viewmodel-shell/src/browser.ts` inside the chip() function body == 0 (must NOT be there — that's the Alert posture).
    - Every gate's output captured in the phase SUMMARY with pass/fail state.
  </acceptance_criteria>
  <done>
    Every AGENTS.md gate green; no red results anywhere; parity + all .NET test projects included; Timeline ::before mechanism + Chip dismissAction posture verified at code level; ready for the visual smoke.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Everything from Plans 25-01..09:
    - UserRowNode (COMP-09) — person entity display with avatar + name + meta + status dot (StatusKind × 4 kinds).
    - DetailRowNode + DetailListNode (COMP-10 + 10a) — key-value with `<dl>/<dt>/<dd>` semantic HTML + labelWidth closed enum (sm/md/lg).
    - TimelineEntryNode + TimelineNode (COMP-11 + 11a) — activity feed with the NEW baked-in ::before rail-and-dot CSS mechanism.
    - SettingRowNode + SettingListNode (COMP-12 + 12a) — label + description + trailing control; natural pairing with CheckboxNode(variant:"switch"); extended stopPropagation.
    - ChipNode + ChipListNode (COMP-13 + 13a) — tinted-pill cluster with dismissAction (caller-supplied ActionEvent, distinct from AlertNode.dismissible) + action; both may coexist.
    - Design doc `.planning/design/composite-nodes-layer.md` shipped-recipe-inventory table extended to 9 composites total.
    - AGENTS.md "Currently shipped recipes" section grown to 9 composites.
    - Showcase demo Secondary Composites section demonstrating all 5 in situ (including 3+ SettingRow + switch pairings).
    - FeatureProbe parity extended across all backends with ~15 new tripwires — parity GREEN.
    - CHANGELOG.md Unreleased — v8.0.0 (in progress) gained 5 additive Added entries. No BREAKING.
    - MIGRATION.md v8.0.0 section grew with additive-only note + Chip.dismissAction paragraph + Timeline ::before paragraph.
    - Full green-tree gate + parity + every .NET test project all green (from Task 1).
  </what-built>
  <how-to-verify>
    (1) Serve the Showcase demo over the tailnet:
    ```
    cd demo/Showcase/frontend
    npm run dev -- --host 0.0.0.0 --port <a-free-port>
    ```
    (Or grep `demo/Showcase/package.json` scripts for the actual build+serve incantation.)

    (2) Navigate to the Showcase URL on the tailnet in a real browser. Scroll to the "v8.0.0 Secondary Composites" section (added by Plan 25-07). Verify VISUALLY:

    - **UserRowNode status dots — all 4 kinds visibly colored correctly.** Online = green dot (shipped success color); Away = yellow dot (shipped warning color); Offline = gray/muted dot; Busy = red dot (shipped error color). The dot renders BEFORE the label text (dot-then-label order). Tab-focus a clickable user row — focus ring shown. Press Enter — action fires (dev-tools console or Showcase's onError/onAction hook can confirm).

    - **DetailListNode label column visibly aligned.** The `labelWidth: "md"` value in the Showcase (default) renders with a 10rem label column consistent across every DetailRow. Labels are uppercase text-xs muted; values are body text. The `tone:"danger"` row's value text renders in the shipped danger color.

    - **🚨 TIMELINE ::before rail-and-dot — THE NEW CSS MECHANISM.** Inspect a Timeline entry with browser dev-tools. Verify:
      * The `<ol class="vms-timeline">` container has a `::before` pseudo-element rendering a vertical 2px-wide line (the rail) — spanning from top to bottom of the container, positioned at `left: 0.375rem`, background `--vms-border`.
      * Each `<li class="vms-timeline-entry">` has a `::before` pseudo-element rendering a filled circle (the dot) — positioned at `left: -1.5rem` (against the rail), 0.875rem diameter, background `--vms-surface`, border `2px solid` with tone-encoded color (`--vms-error` for danger tone, `--vms-warning` for warning, etc.).
      * Toggle a couple of themes (dark/light) and verify the rail + dots render correctly across themes.
      * **This is the ONE genuinely new CSS mechanism in Phase 25.** If the rail is missing OR the dots are the wrong color OR positioned incorrectly, Plan 25-03's CSS block needs a fix.

    - **SettingRowNode + CheckboxNode(variant:"switch") natural pairing.** Each SettingRow in the Showcase renders `[body | control]` grid layout with the switch vertically centered in the control column. Toggle a switch — the checkbox state changes (bind updates locally). Tab-focus the switch — focus ring visible. Verify the ButtonNode-trailing row (Configure) renders with the button in the trailing slot, not swallowed by the row.

    - **ChipListNode tinted-pill colors + dismissAction end-to-end.** Chips render as rounded pills with tinted-tone backgrounds. Each of the 4 tones + neutral renders visibly differently. Click the X on the `active` chip (dismissAction: `showcase-secondary-chip-remove-active`). **Verify via dev-tools console or the app's onAction hook that the dispatched action name is `showcase-secondary-chip-remove-active`** — NOT `dismiss`. This proves the ChipNode.dismissAction is a caller-supplied ActionEvent, NOT AlertNode.dismissible's fixed name. Click on the `both` chip (has BOTH action + dismissAction) — the whole-chip click fires `showcase-secondary-chip-toggle-both`. Click the X on the `both` chip — the X's dismissAction fires `showcase-secondary-chip-remove-both` and does NOT fire the whole-chip action (stopPropagation worked).

    (3) Toggle the theme picker (Showcase has a theme switcher) — verify the Secondary Composites section looks right on default + at least 2-3 dark themes + at least 2-3 light themes. Focus on:
      * Timeline rail + dot visibility on tinted-background themes.
      * Chip tinted-pill text still passes AA-normal 4.5:1 (the AA-contrast hand-check in Plan 25-05 covered the numeric ratios; this is a live sanity check — the highest AA risk in Phase 25).
      * UserRow status dots visible on light + dark surfaces.

    (4) Re-verify the AA-contrast hand-check aggregate. Inspect the SUMMARY.md files from Plans 25-01..05 (records for UserRow status-dots, DetailRow tone-accent + label, Timeline dot-borders, SettingRow reused prior coverage, Chip tinted-pill highest-risk). Any theme documented as failing should have a deepening or KNOCKOUT mitigation recorded in default.css. If a theme was skipped, name it now.

    (5) Confirm all 5 secondaries "look right" per your (Ashley's) judgment. If any look wrong (e.g. the Timeline rail is invisible on dark theme, the Chip tinted-pill text fails contrast on a specific theme, the SettingRow switch overlaps the label), name specifically what's wrong so a follow-up task can fix it.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 secondary composites look right on default + representative themes AND the Timeline ::before rail-and-dot visibly renders AND the ChipNode dismissAction dispatches the caller-supplied name AND the AA hand-check aggregate is complete. Type "fix: {description}" if any composite needs a visual fix before Phase 26 opens. Type "AA-gap: {description}" if an AA pair was missed and needs a hand-check re-run.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Requirement-to-artifact cross-check + write SUMMARY.md + update ROADMAP</name>
  <files>.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md, .planning/ROADMAP.md</files>
  <read_first>
    - All 9 prior plan SUMMARY files (25-01-SUMMARY.md through 25-09-SUMMARY.md) if they exist
    - .planning/REQUIREMENTS.md — COMP-09..13a definitions
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md (Locked Decisions — each has a landing artifact)
    - .planning/ROADMAP.md — locate Phase 25 plan-list block (currently `**Plans:** 0 plans (to be created by /gsd:plan-phase 25)`) and tick boxes
  </read_first>
  <action>
    (a) Write `.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md`. Sections:

    (i) **Green-tree gate results** — table with columns `Gate | Result | Command | Notes`. Every command from Task 1 gets a row.

    (ii) **Requirement-to-artifact cross-check** — table with COMP-09, COMP-10, COMP-10a, COMP-11, COMP-11a, COMP-12, COMP-12a, COMP-13, COMP-13a as rows. Columns: `Requirement | Wire type shipped | Renderer | CSS | .NET twin | Parity tripwire | Tests (vitest + .NET) | Demo adoption | CHANGELOG | Design-doc inventory row`. Every cell filled with exact file+line evidence.

    Example row (COMP-09):
    | COMP-09 UserRowNode | `interface UserRowNode` in `src/index.ts` | `private userRow()` in `src/browser.ts` | `.vms-user-row*` + `.vms-status-dot*` in `styles/default.css` | `public record UserRowNode` + `UserRowStatus` + `StatusKind` in `ViewModels.cs` | `"type":"user-row"` + `"kind":"online"` + `user-row-open-jd` in `parity/fixtures/feature-probe.json` | `test/user-row.test.ts` + `Tests/UserRowNodeSerializationTests.cs` | Showcase Secondary Composites section + FeatureProbe secondaryCompositesSection | CHANGELOG.md Unreleased v8.0.0 Added | composite-nodes-layer.md Shipped Recipe Inventory row for user-row |

    (iii) **AA-contrast hand-check aggregate** — restate per-composite totals: UserRow 52 pair-checks (4 kinds × 13 themes), DetailRow 65+ (label 13 + tone-accent 4×13), Timeline 52 (4 tones × 13), SettingRow 0 new (reuses Phase 23 + 24), Chip 52 (4 tones × 13, HIGHEST risk). Total: ~221 new pair-checks. Anywhere any theme required deepening or KNOCKOUT mitigation, name the mitigation.

    (iv) **The 🚨 NEW ::before rail-and-dot CSS mechanism (Timeline) — end-to-end verification** — three-way proof:
      (a) code proof: `default.css` contains `.vms-timeline::before` (rail) + `.vms-timeline-entry::before` (dot) + 4 tone-encoded modifiers;
      (b) test proof: `test/timeline.test.ts` asserts class-emission (jsdom cannot compute pseudo-elements, but the CSS installation is verified);
      (c) visual proof: Ashley's checkpoint sign-off confirms the rail + dots render correctly on default + dark themes + light themes.

    (v) **The 🚨 ChipNode.dismissAction posture — end-to-end verification** — three-way proof:
      (a) code proof: `browser.ts` chip() calls `on(dismissAction)` (caller's action), NOT `on({name: "dismiss"})`;
      (b) test proof: `test/chip.test.ts` KEY MUTATION TEST asserts caller-supplied name dispatched;
      (c) parity proof: `"dismissAction":{` tripwire in feature-probe.json proves ActionEvent-slot serializes as object across backends;
      (d) visual proof: Ashley's checkpoint sign-off confirms dismissible chip fires the caller-supplied name via dev-tools inspection.

    (vi) **Design-doc + AGENTS.md landing** — one line each:
      - `.planning/design/composite-nodes-layer.md` shipped-recipe-inventory table grew to 9 composites (Plan 25-06).
      - AGENTS.md "Currently shipped recipes" grew to 9 composites (Plan 25-06).
      - CHANGELOG.md Unreleased v8.0.0 gained 5 additive Added entries (Plan 25-09).
      - MIGRATION.md v8.0.0 grew with additive note + Chip.dismissAction paragraph + Timeline ::before paragraph (Plan 25-09).
      - Showcase Secondary Composites section landed (Plan 25-07) — exercises SettingRow + switch pairing 3+ rows.
      - FeatureProbe parity extended with 15 tripwires across all backends (Plan 25-08) — parity GREEN.

    (vii) **Ashley's visual sign-off** — transcribe the exact resume-signal text from Task 2.

    (viii) **NO release ship** — explicitly note: no version bump (package.json still 7.1.0, .csproj still 7.0.0), no npm publish, no dotnet nuget push, no git tag. Batch-then-ship LOCKED per CONTEXT §10; v8.0.0 releases at Phase 26 closeout.

    (ix) **Ready for Phase 26** — explicit "green light to run `/gsd:plan-phase 26`" if all gates + sign-off are green. Phase 26 will: bump versions to 8.0.0 on both packages, finalize CHANGELOG (rename "Unreleased" → "v8.0.0"), publish npm + NuGet, tag `v8.0.0`, verify with a comprehensive tailnet page across all 10 composites.

    (b) Update `.planning/ROADMAP.md` Phase 25 plan-list block. Replace `**Plans:** 0 plans (to be created by /gsd:plan-phase 25)` with:

    ```markdown
    **Plans:** 10 plans complete

    Plans:
    - [x] 25-01-PLAN.md — UserRowNode (COMP-09) end-to-end (wave 1)
    - [x] 25-02-PLAN.md — DetailRowNode + DetailListNode (COMP-10 + 10a) end-to-end (wave 2, depends on 25-01)
    - [x] 25-03-PLAN.md — TimelineEntryNode + TimelineNode (COMP-11 + 11a) end-to-end with NEW ::before rail+dot CSS mechanism (wave 3, depends on 25-02)
    - [x] 25-04-PLAN.md — SettingRowNode + SettingListNode (COMP-12 + 12a) end-to-end with CheckboxNode(variant:"switch") natural pairing (wave 4, depends on 25-03)
    - [x] 25-05-PLAN.md — ChipNode + ChipListNode (COMP-13 + 13a) end-to-end with caller-supplied dismissAction posture (wave 5, depends on 25-04)
    - [x] 25-06-PLAN.md — design/composite-nodes-layer.md + AGENTS.md inventory growth (wave 1, file-disjoint)
    - [x] 25-07-PLAN.md — Showcase Secondary Composites section (wave 6, depends on 25-05)
    - [x] 25-08-PLAN.md — FeatureProbe parity extension + 15 tripwires (wave 6, depends on 25-05)
    - [x] 25-09-PLAN.md — CHANGELOG.md + MIGRATION.md v8.0.0 additive entries (wave 6, depends on 25-05)
    - [x] 25-10-PLAN.md — Full green-tree gate + Ashley visual sign-off + Timeline + Chip end-to-end verification (wave 7, depends on all prior)
    ```

    (c) Verify version files STILL UNTOUCHED: `grep '"version":' viewmodel-shell/package.json` returns `7.1.0`; `grep '<Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns `7.0.0`. Final defensive check against accidental release.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md &amp;&amp; grep -c 'COMP-09\|COMP-10\|COMP-11\|COMP-12\|COMP-13' .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md &amp;&amp; grep -c 'green' .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md &amp;&amp; grep -c 'Timeline.*::before\|rail.*dot' .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md &amp;&amp; grep -c 'dismissAction\|Chip.*dismiss' .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md &amp;&amp; grep -c 'batch-then-ship\|Batch-then-ship' .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md &amp;&amp; grep -c 'Phase 26' .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md &amp;&amp; grep '"version": "7\.1\.0"' viewmodel-shell/package.json &amp;&amp; grep '<Version>7\.0\.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj &amp;&amp; grep -c '25-01-PLAN\|25-10-PLAN' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `25-10-SUMMARY.md` exists.
    - Contains green-tree gate results table with every gate command from Task 1.
    - Contains requirement-to-artifact table with 9 COMP-XX rows (09, 10, 10a, 11, 11a, 12, 12a, 13, 13a).
    - `grep -c 'COMP-09' 25-10-SUMMARY.md` >= 1.
    - `grep -c 'COMP-10' 25-10-SUMMARY.md` >= 2 (COMP-10 + COMP-10a).
    - `grep -c 'COMP-11' 25-10-SUMMARY.md` >= 2 (COMP-11 + COMP-11a).
    - `grep -c 'COMP-12' 25-10-SUMMARY.md` >= 2 (COMP-12 + COMP-12a).
    - `grep -c 'COMP-13' 25-10-SUMMARY.md` >= 2 (COMP-13 + COMP-13a).
    - `grep -c 'AA' 25-10-SUMMARY.md` >= 1 (AA-contrast aggregate section).
    - `grep -c '221 pair\|total.*221\|~221' 25-10-SUMMARY.md` >= 1 (or the aggregate count shown by summing per-composite).
    - **Timeline ::before end-to-end verification section present** — 3-way proof (code + test + visual).
    - **Chip dismissAction end-to-end verification section present** — 4-way proof (code + test + parity + visual).
    - `grep -c 'Ashley' 25-10-SUMMARY.md` >= 1 (sign-off transcribed).
    - `grep -c 'batch-then-ship\|Batch-then-ship' 25-10-SUMMARY.md` >= 1.
    - `grep -c 'Phase 26' 25-10-SUMMARY.md` >= 1 (next-phase readiness for the release).
    - Version files UNCHANGED (package.json = 7.1.0; .csproj = 7.0.0).
    - ROADMAP.md Phase 25 plan-list block updated with 10 plans + tick boxes.
  </acceptance_criteria>
  <done>
    Phase 25 SUMMARY records green-tree + AA aggregate + Timeline ::before end-to-end verification + Chip dismissAction end-to-end verification + Ashley sign-off + no-release confirmation + Phase 26 readiness. ROADMAP updated. Phase 25 is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan. No code, no wire change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-10-01 | (none) | Green-tree verification + operator sign-off | accept | This plan verifies that the code shipped in Plans 25-01..09 is correct + coherent. It doesn't add new code. Attack surface delta: zero. |
| T-25-10-02 | Operational | Accidental publish (operator sees v8.0.0 CHANGELOG + reflexes to publish v8.0.0) | mitigate | Three documented mitigations: (a) CHANGELOG Unreleased heading contains the batch-then-ship reminder, (b) Task 3 SUMMARY explicitly states no-release-in-this-phase + version-file-unchanged verification, (c) Ashley's checkpoint asks for "approved" as the resume signal, not "publish" — the sign-off boundary and the publish boundary are separate rituals. Phase 26 is the explicit release ritual. |
| T-25-10-03 | Information Disclosure | Chip.dismissAction end-to-end posture confusion during visual sign-off — Ashley might not spot that the dispatched name is the caller's, not "dismiss" | mitigate | Task 2's `how-to-verify` (5) explicitly directs Ashley to inspect the dispatched action name via dev-tools console OR the Showcase's onError/onAction hook — the test isn't "does the X visually work" but "is the dispatched name what the caller supplied". The test name specifically calls out `showcase-secondary-chip-remove-active` as the expected dispatched name. |
| T-25-10-04 | Information Disclosure | Timeline ::before rail-and-dot visual mechanism could look correct on default theme but broken on a dark theme | mitigate | Task 2's `how-to-verify` (3) explicitly requires toggling themes and inspecting the rail + dot rendering on 2-3 dark + 2-3 light themes. The Plan 25-03 AA hand-check header enumerated dot-border tones × 13 themes; this task re-verifies live. |
</threat_model>

<verification>
Task-scoped verifications inside each task. Overall plan complete when Task 3's SUMMARY is written + Ashley's sign-off transcribed + ROADMAP updated + version files verified untouched.
</verification>

<success_criteria>
- Full green-tree gate green across `viewmodel-shell`, `viewmodel-shell-dotnet/Tests`, every `demo/**/*.Tests.csproj`, and `parity/run.ts`.
- AA-contrast hand-check aggregate (~221 NEW pair-checks) documented across default + 12 themes for every new fg/bg pair.
- Ashley's visual sign-off recorded with specific verification of (a) Timeline ::before rail-and-dot mechanism, (b) Chip dismissAction caller-supplied dispatch, (c) SettingRow + switch natural pairing.
- Requirement-to-artifact cross-check complete for COMP-09..13a (9 requirements).
- No release, no version bump — batch-then-ship LOCKED. Version files (package.json = 7.1.0; .csproj = 7.0.0) UNCHANGED.
- Phase 26 unblocked; explicit readiness statement in SUMMARY + ROADMAP updated with 10 completed plans.
</success_criteria>

<output>
`.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-10-SUMMARY.md` is the phase closeout artifact.
</output>
