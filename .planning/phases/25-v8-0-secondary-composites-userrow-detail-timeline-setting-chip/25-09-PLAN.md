---
phase: 25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip
plan: 09
type: execute
wave: 6
depends_on: ["25-05"]
files_modified:
  - CHANGELOG.md
  - MIGRATION.md
autonomous: true
requirements: [COMP-09, COMP-10, COMP-10a, COMP-11, COMP-11a, COMP-12, COMP-12a, COMP-13, COMP-13a]

must_haves:
  truths:
    - "`CHANGELOG.md` **Unreleased — v8.0.0 (in progress)** section gains **5 more Added entries** (no BREAKING in Phase 25 — all additive). Existing Phase 23 + Phase 24 entries UNCHANGED."
    - "Added: UserRowNode (COMP-09). Names status-dot palette + StatusKind closed enum."
    - "Added: DetailRowNode + DetailListNode (COMP-10 + 10a). Names `<dl>/<dt>/<dd>` semantic HTML choice + DetailLabelWidth closed enum + labelWidth CSS-var pattern."
    - "Added: TimelineEntryNode + TimelineNode (COMP-11 + 11a). Names the **NEW ::before rail-and-dot CSS mechanism** as a load-bearing property: apps CANNOT compose this from primitives; composite exists SPECIFICALLY to bake it in."
    - "Added: SettingRowNode + SettingListNode (COMP-12 + 12a). Names the CheckboxNode(variant:'switch') natural pairing per CONTEXT §9."
    - "Added: ChipNode + ChipListNode (COMP-13 + 13a). Names the **dismissAction-as-caller-supplied-ActionEvent** posture as a load-bearing property: **DEVIATES from AlertNode.dismissible** which emits a fixed local name; Chip needs identity-carrying dispatch. Both action + dismissAction may coexist; X click stopPropagates."
    - "No Changed / no BREAKING entries in Phase 25 — all 5 secondaries are additive."
    - "`MIGRATION.md` v8.0.0 (in progress) section grows with a single note covering all 5 secondaries — additive-only + a one-paragraph callout for the ChipNode.dismissAction posture (consumers building filter-chip UIs need to model per-chip identity-carrying dispatch names)."
    - "Batch-then-ship reminder at the top of the Unreleased CHANGELOG section (preserved from Phase 23 plan 23-08) reiterated — Phase 25 does NOT release; v8.0.0 publishes at Phase 26 closeout."
    - "No version bump in `viewmodel-shell/package.json` (stays at 7.1.0 per CONTEXT §10) or `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` (stays at 7.0.0). This plan touches CHANGELOG + MIGRATION only."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "5 Added entries under Unreleased — v8.0.0 (in progress). No BREAKING entries."
      contains: "UserRowNode"
    - path: "MIGRATION.md"
      provides: "v8.0.0 (in progress) section additive-only note covering all 5 secondaries + Chip.dismissAction posture callout."
      contains: "UserRowNode\\|Chip"
  key_links:
    - from: "CHANGELOG.md (Chip entry naming dismissAction posture)"
      to: "MIGRATION.md (Chip.dismissAction callout)"
      via: "cross-reference: CHANGELOG names the posture; MIGRATION expands on it for chip-building consumers"
      pattern: "dismissAction\\|Chip"
---

<objective>
Update `CHANGELOG.md` Unreleased-v8.0.0-in-progress section with 5 more Added entries (no BREAKING — all additive). Update `MIGRATION.md` with an additive note covering all 5 secondaries + a callout for the Chip.dismissAction identity-carrying posture. No version bump; batch-then-ship discipline preserves the Unreleased heading.

Purpose: CHANGELOG + MIGRATION are the operator's + external consumer's mental model of what changed between releases. Phase 23 added 4 foundations; Phase 24 added 4 primaries + 1 BREAKING (EmptyStateNode rename); Phase 25 adds 5 secondaries — all additive. Two load-bearing properties MUST be named in the CHANGELOG for future consumers reading it cold: (a) the Timeline ::before rail-and-dot mechanism, and (b) the Chip dismissAction-as-caller-supplied-ActionEvent (distinct from Alert.dismissible).

Output: Both docs updated; no release fired; the Unreleased heading preserved; every entry mirrors what shipped in Plans 25-01..05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-CONTEXT.md
@.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 5 Added entries to CHANGELOG Unreleased v8.0.0 (no BREAKING)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (read the Unreleased — v8.0.0 (in progress) section around :9-25 per 25-PATTERNS.md §14; the Phase 23 + Phase 24 Added + Changed entries stay UNCHANGED)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §14 (exact text for the 5 Added entries — lines 1717-1721)
  </read_first>
  <action>
    In `CHANGELOG.md`:

    (a) Locate the `## Unreleased — v8.0.0 (in progress)` section. It has an `### Added` sub-section with Phase 23 entries (TextNode.style:caption, TextNode.weight, CheckboxNode.variant:switch, AvatarNode) + Phase 24 entries (ListRowNode, MessageNode/MessageListNode, AlertNode) + a `### Changed (BREAKING)` sub-section with EmptyStateNode. Leave those UNCHANGED.

    (b) APPEND to the `### Added` sub-section 5 new entries per 25-PATTERNS.md §14:

    ```markdown
    - `UserRowNode` (`"user-row"`) — person entity display with avatar + name + meta + right-aligned status dot. Wire shape: `{ type, avatar?, name, meta?, status?, trailing?, action? }`. `name` and `meta` are `string | ViewNode` (TS ergonomic convenience; .NET wraps explicitly). `status: {label, kind}` uses a closed `StatusKind` enum (`"online"|"away"|"offline"|"busy"`) mapped to shipped tone colors (online→success, away→warning, offline→muted, busy→danger). Whole-row `action?` follows `ListRowNode.action` shape. (COMP-09)
    - `DetailRowNode` (`"detail-row"`) + `DetailListNode` (`"detail-list"`) — key-value display with aligned label column via `<dl>/<dt>/<dd>` semantic HTML. Wire shapes: `{ type, label, value, tone?, icon? }` and `{ type, children[], labelWidth?: "sm"|"md"|"lg" }`. `labelWidth` sets a fixed CSS grid label column via `--vms-detail-label` CSS-var (8/10/12rem); omitted = md default. Tree-validator rejects non-DetailRowNode children with byte-identical error across TS + .NET. Trained typography: label = text-xs uppercase weight:500 muted (baked in CSS, NOT TextNode-wrapped); value = body. (COMP-10 + 10a)
    - `TimelineEntryNode` (`"timeline-entry"`) + `TimelineNode` (`"timeline"`) — activity feed with baked-in CSS rail-and-dot mechanism. Wire shapes: `{ type, time, description, tone?, icon? }` and `{ type, children[] }`. **🚨 NEW CSS MECHANISM**: rail via `::before` on the `<ol>` container; dot per entry via `::before` with tone-encoded border. **This composite exists specifically to bake this in — apps CANNOT compose this from primitives per "apps describe, never decorate."** Trained typography: time = caption; description = body (string or ViewNode). (COMP-11 + 11a)
    - `SettingRowNode` (`"setting-row"`) + `SettingListNode` (`"setting-list"`) — label + description + trailing control (settings pages, feature-flag toggles, notification preferences). Wire shapes: `{ type, icon?, label, description?, trailing?, action? }` and `{ type, children[], heading? }`. Grid: `[body | control]` = `1fr auto`, `align-items: center`. **Natural pairing with `CheckboxNode(variant:"switch")` from COMP-03** in the trailing slot; also common: ButtonNode, LinkNode. Whole-row action's stopPropagation is extended to include `.vms-checkbox__input` + `.vms-field--switch` so the natural switch pairing does NOT double-fire the row action. Tree-validator rejects non-SettingRowNode children. (COMP-12 + 12a)
    - `ChipNode` (`"chip"`) + `ChipListNode` (`"chip-list"`) — tinted-pill cluster for filter chips, selected tags, categories. Wire shapes: `{ type, label, tone?, icon?, dismissAction?, action? }` and `{ type, children[] }`. **🚨 `dismissAction?: ActionEvent` is a CALLER-SUPPLIED slot** (identity-carrying: `remove-filter-42`), **distinct from `AlertNode.dismissible`** which emits a fixed local name. Chip needs the app to name the action because chips typically operate on specific identities. Both `action` and `dismissAction` may coexist on the same chip; the X's click `stopPropagation`s so it doesn't double-fire the whole-chip action. Absent `dismissAction` = no X rendered (respects "no dead UI"). Tree-validator rejects non-ChipNode children in ChipListNode. (COMP-13 + 13a)
    ```

    (c) **No new `### Changed (BREAKING)` sub-section** — Phase 25 adds no breaking changes. Existing Phase 24 `### Changed (BREAKING)` entry (EmptyStateNode rename) stays UNCHANGED.

    (d) The "Batch-then-ship" reminder at the top of the Unreleased section STAYS — this plan does NOT bump the version, publish, or tag. Verify still present + accurate: "v8.0.0 publishes at Phase 26 closeout with all 10 composites + 3 wire tweaks + 4 foundations in one aligned release. Do NOT publish Phase 25 as its own release."

    (e) Verify version files UNCHANGED: `viewmodel-shell/package.json` still 7.1.0; `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` still 7.0.0.
  </action>
  <verify>
    <automated>grep -c 'Unreleased — v8\.0\.0' CHANGELOG.md &amp;&amp; grep -c 'UserRowNode' CHANGELOG.md &amp;&amp; grep -c 'DetailRowNode\|DetailListNode' CHANGELOG.md &amp;&amp; grep -c 'TimelineEntryNode\|TimelineNode' CHANGELOG.md &amp;&amp; grep -c 'SettingRowNode\|SettingListNode' CHANGELOG.md &amp;&amp; grep -c 'ChipNode\|ChipListNode' CHANGELOG.md &amp;&amp; grep -c 'apps CANNOT compose\|apps describe' CHANGELOG.md &amp;&amp; grep -c 'DEVIATES from AlertNode\|distinct from.*AlertNode.*dismissible\|caller-supplied.*ActionEvent' CHANGELOG.md &amp;&amp; grep -c 'batch-then-ship\|Batch-then-ship' CHANGELOG.md &amp;&amp; grep '"version": "7\.1\.0"' viewmodel-shell/package.json &amp;&amp; grep '<Version>7\.0\.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Unreleased — v8\.0\.0' CHANGELOG.md` >= 1 (section heading preserved).
    - All 5 secondaries named in Added entries: UserRowNode, DetailRowNode/DetailListNode, TimelineEntryNode/TimelineNode, SettingRowNode/SettingListNode, ChipNode/ChipListNode.
    - **Timeline ::before rail-and-dot NEW MECHANISM callout in CHANGELOG**: `grep -c 'apps CANNOT compose\|apps describe\|NEW CSS MECHANISM\|::before.*rail' CHANGELOG.md` >= 1.
    - **Chip dismissAction-vs-Alert.dismissible callout in CHANGELOG**: `grep -c 'DEVIATES from AlertNode\|distinct from.*AlertNode\|caller-supplied.*ActionEvent\|identity-carrying' CHANGELOG.md` >= 1.
    - SettingRow + CheckboxNode(variant:"switch") natural pairing mentioned.
    - No new `### Changed (BREAKING)` sub-section in Phase 25 (verify by git diff — only Added entries appended).
    - Phase 24 `### Changed (BREAKING)` EmptyStateNode entry UNCHANGED (verify by git diff).
    - Batch-then-ship reminder still present.
    - `viewmodel-shell/package.json` version still `7.1.0` (NOT bumped).
    - `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` `<Version>` still `7.0.0` (NOT bumped).
  </acceptance_criteria>
  <done>
    CHANGELOG grown with 5 additive Added entries; no BREAKING; version files untouched; batch-then-ship discipline preserved. Timeline + Chip callouts prominent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append additive-only note + Chip.dismissAction callout to MIGRATION.md v8.0.0 section</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md (read entirely; find the v8.0.0 (in progress) section landed by Phase 23 plan 23-08 + grown by Phase 24 plan 24-08 with the EmptyStateNode rewrite; Phase 25 appends below the existing v8.0.0 content)
    - .planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-PATTERNS.md §15 (exact text for the MIGRATION.md additions lines 1745-1752)
  </read_first>
  <action>
    In `MIGRATION.md`:

    (a) Locate the `## v8.0.0 (in progress)` section. Phase 24 populated it with the EmptyStateNode BREAKING rewrite + a Phase 24 additive-only note for ListRow/Message/Alert. Phase 25 appends BELOW those, NOT replacing anything.

    (b) APPEND per 25-PATTERNS.md §15 (lines 1745-1752):

    ```markdown
    ### UserRowNode, DetailRowNode + DetailListNode, TimelineEntryNode + TimelineNode, SettingRowNode + SettingListNode, ChipNode + ChipListNode (COMP-09..COMP-13) — new node types

    **Additive; no consumer changes required.** Five secondary composite pairs with `[JsonDerivedType]` discriminators; older adapters silently emit nothing for unknown types (existing behavior for forward compatibility). No wire renames, no field removals — every existing v7.x + Phase 24 consumer keeps working untouched.

    See the `Unreleased — v8.0.0 (in progress)` section of `CHANGELOG.md` for the full wire-level detail per composite.

    **One deliberate deviation to know about**: `ChipNode.dismissAction` is a CALLER-SUPPLIED `ActionEvent` slot (identity-carrying name like `remove-filter-42`), distinct from `AlertNode.dismissible` (Phase 24) which emits a fixed-name `{name: "dismiss"}` locally. Consumers building filter-chip UIs should model each chip's dismiss dispatch with a unique per-chip name in state — the framework does not auto-name. If both `action` and `dismissAction` are set on the same chip, the X click stops propagation so the whole-chip click does not double-fire.

    **Two ::before CSS mechanisms baked in**: TimelineNode ships a vertical rail via `::before` on the `<ol>` container + a per-entry dot via `::before` on each `<li>` (tone-encoded border color). Apps cannot compose this from primitives — the composite exists specifically to bake it in per "apps describe, never decorate." UserRowNode ships a status-dot palette (`.vms-status-dot--{online|away|offline|busy}`) — reuses the shipped tone-color palette. Neither requires any consumer action.
    ```

    (c) MIGRATION.md's Phase 23 + Phase 24 sections UNCHANGED.
  </action>
  <verify>
    <automated>grep -c '## v8\.0\.0' MIGRATION.md &amp;&amp; grep -c 'UserRowNode.*DetailRowNode.*TimelineEntryNode\|COMP-09\.\.COMP-13\|COMP-09..COMP-13' MIGRATION.md &amp;&amp; grep -c 'Additive\|additive' MIGRATION.md &amp;&amp; grep -c 'ChipNode\.dismissAction\|dismissAction.*CALLER-SUPPLIED\|caller-supplied.*ActionEvent' MIGRATION.md &amp;&amp; grep -c 'distinct from.*AlertNode\|distinct from AlertNode\|deviate\|DEVIATES' MIGRATION.md &amp;&amp; grep -c 'remove-filter-42\|identity-carrying' MIGRATION.md &amp;&amp; grep -c 'apps.*cannot.*compose\|apps describe\|::before' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## v8\.0\.0' MIGRATION.md` >= 1 (section heading preserved).
    - MIGRATION v8.0.0 section now contains an entry naming all 5 secondaries (COMP-09..COMP-13) as additive.
    - `grep -c 'Additive' MIGRATION.md` >= 1 (additive-only note explicit).
    - **Chip.dismissAction posture callout present**: `grep -c 'ChipNode\.dismissAction\|dismissAction.*CALLER\|caller-supplied.*ActionEvent' MIGRATION.md` >= 1.
    - **Chip vs Alert.dismissible distinction documented**: `grep -c 'distinct from.*AlertNode\|deviate\|DEVIATES' MIGRATION.md` >= 1.
    - Identity-carrying name example (`remove-filter-42`) present.
    - Timeline ::before rail-and-dot mechanism documented as apps-cannot-compose.
    - Phase 23 + Phase 24 sections (foundations Added notes + EmptyStateNode BREAKING rewrite + Phase 24 additive note) UNCHANGED (verify by git diff — only the new Phase 25 additive note appended).
  </acceptance_criteria>
  <done>
    MIGRATION.md v8.0.0 section grown with a Phase 25 additive-only note + Chip.dismissAction posture callout + Timeline ::before mechanism callout; prior sections unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan. No code, no wire change, no runtime surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09-01 | (none) | CHANGELOG + MIGRATION addition | accept | No new input parsing, no external data flow, no auth interaction. Documentation update; zero runtime surface change. |
| T-25-09-02 | Operational | Accidental publish (operator sees v8.0.0 CHANGELOG + reflexes to publish) | mitigate | Three documented mitigations: (a) CHANGELOG Unreleased heading contains the batch-then-ship reminder, (b) Task 1 verification checks version files unchanged, (c) Plan 25-10 (final gate + Ashley checkpoint) also names no-release-in-this-phase as a success criterion. |
| T-25-09-03 | Information Disclosure | Chip.dismissAction posture confusion — future consumers or agents could think it's a bool like AlertNode.dismissible | mitigate | Three-layer defense: (a) ChipNode TSDoc in index.ts explicitly documents divergence (Plan 25-05 task 1), (b) CHANGELOG entry for ChipNode names the divergence explicitly, (c) MIGRATION.md includes a dedicated "one deliberate deviation" paragraph. Cross-referenced. |
</threat_model>

<verification>
Task-scoped verifications above. Both files pure markdown; no build/test to run beyond grep + version-file untouched checks.
</verification>

<success_criteria>
- CHANGELOG.md Unreleased — v8.0.0 (in progress) section grew with 5 Added entries. No BREAKING.
- Timeline ::before rail-and-dot NEW mechanism callout in CHANGELOG.
- ChipNode.dismissAction-vs-Alert.dismissible divergence callout in CHANGELOG.
- MIGRATION.md v8.0.0 section grew with additive-only note + Chip.dismissAction paragraph + Timeline ::before paragraph.
- Version files UNCHANGED (package.json = 7.1.0; .csproj = 7.0.0).
- Batch-then-ship reminder preserved.
- Every entry mirrors what actually shipped in Plans 25-01..05.
</success_criteria>

<output>
Create `.planning/phases/25-v8-0-secondary-composites-userrow-detail-timeline-setting-chip/25-09-SUMMARY.md` recording: CHANGELOG diff summary (5 additive entries), MIGRATION diff summary (additive note + two callouts), version-file-untouched verification, no-release confirmation.
</output>
