---
phase: 20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-
plan: 06
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03, 20-04, 20-05]
files_modified:
  - demo/NavVerification-bun/server.ts
  - demo/NavVerification-bun/index.html
autonomous: false
requirements: [NAV-04]

must_haves:
  truths:
    - "The FULL green-tree gate passes: vitest, core-globals, aa-contrast, theme-byte-identity, parity, the framework .NET Tests, and every demo Tests project"
    - "A tailnet-served verification view renders both nodes + both step orientations in light AND dark + a clickable-row cursor check, driving the real shipped bundle"
    - "Ashley reviews the tailnet page and signs off BEFORE any publish (visual change = in-question path)"
  artifacts:
    - path: "demo/NavVerification-bun/server.ts"
      provides: "a real-bundle verification app: breadcrumb + steps (both orientations) + a clickable-row table, theme-toggleable"
      contains: "steps"
  key_links:
    - from: "demo/NavVerification-bun"
      to: "the shipped browser bundle + default.css + themes"
      via: "serves the real renderer/CSS (not a mockup) over the tailnet"
      pattern: "viewmodel-shell"
---

<objective>
Prove the whole tree is green and get Ashley's visual sign-off on the two new nodes BEFORE any publish. Nav is a visual change, so the publish is the in-question path and must NOT proceed until she confirms.

Purpose: this is the gate between "built" and "released." The green-tree gate catches any regression across both backends + all demos; the verification page lets a human confirm the rendered breadcrumb + both step orientations look right in light AND dark and that clickable rows show the pointer cursor.

Output: a `demo/NavVerification-bun/` app driving the REAL shipped bundle + `default.css` + themes, served over the tailnet; a passing full green-tree gate; a recorded operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/nav-primitives.md
@.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-CONTEXT.md

<interfaces>
The full green-tree gate (from AGENTS.md — run ALL, no "pre-existing/unrelated/just-a-demo" exceptions):
- `cd viewmodel-shell && npx vitest run` (NOT `bun test`)
- `cd viewmodel-shell && npm run check:core-globals`
- `cd viewmodel-shell && node scripts/check-aa-contrast.mjs`
- `cd viewmodel-shell && node scripts/check-theme-byte-identity.mjs`
- `cd parity && bun run run.ts`
- `export PATH="$HOME/.dotnet:$PATH" && dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj`
- `for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p"; done`

Verification-page precedent: Phase 19 built a tailnet chart verification page (real shipped CSS + real renderer/bundle) for Ashley's sign-off. The `demo/NonBlockingStaleness-bun` (and its NonBlocking* siblings) is the correct template — a frontend `*-bun` demo with an `index.html` + a built `dist/` that serves the real shipped browser bundle over Bun.serve. (⚠️ NOT `demo/FeatureProbe-bun`, which is a headless JSON backend with no frontend/bundle — it renders no browser view.) Rebuild the bundle (`npm run build` in viewmodel-shell) before serving so the new breadcrumb/steps renderers are included. The Showcase (extended in Plan 20-05) already has both-orientation steps + breadcrumb gallery sections and a 12-theme switcher (light + dark) — the verification view may reuse/serve that content plus a clickable-row table, or be a purpose-built page; discretion.

Tailnet serving convention (AGENTS.md/home): bind on the box, reach via `http://thenasty:<port>/` on the tailnet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full green-tree gate — all suites green</name>
  <read_first>
    - ./CLAUDE.md then AGENTS.md (the GREEN-TREE GATE + "NEVER PUBLISH OR PUSH ANYTHING BROKEN" sections — the exact suite list)
    - viewmodel-shell/package.json (the check:core-globals / check:aa-contrast script names)
  </read_first>
  <action>Run the FULL gate in order and make it green: vitest (`npx vitest run`), core-globals (`npm run check:core-globals`), aa-contrast (`node scripts/check-aa-contrast.mjs`), theme-byte-identity (`node scripts/check-theme-byte-identity.mjs`), parity (`bun run parity/run.ts`), the framework Tests (`dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj`), and EVERY `demo/**/*.Tests.csproj` in a loop. If ANY suite is red — including a pre-existing/unrelated/demo failure — STOP and fix it (or surface to the operator for an explicit waiver) before proceeding; never advance on a broken tree. Record each suite's result in the SUMMARY.</action>
  <acceptance_criteria>
    - `npx vitest run` exits 0.
    - `npm run check:core-globals` exits 0.
    - `node scripts/check-aa-contrast.mjs` exits 0.
    - `node scripts/check-theme-byte-identity.mjs` exits 0.
    - `bun run parity/run.ts` exits 0 (byte-identical).
    - `dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj` exits 0.
    - every `demo/**/*.Tests.csproj` `dotnet test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH"; cd /home/thenasty/ViewModelShell/viewmodel-shell && npx vitest run && npm run check:core-globals && node scripts/check-aa-contrast.mjs && node scripts/check-theme-byte-identity.mjs && cd ../parity && bun run run.ts && cd .. && dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj --nologo -v minimal && for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done && echo ALL_GREEN</automated>
  </verify>
  <done>Every suite in the green-tree gate passes with no exceptions; each pass recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Combined tailnet verification view (real bundle, light + dark, clickable-row check)</name>
  <read_first>
    - demo/NonBlockingStaleness-bun/{server.ts,index.html} (the CORRECT real-bundle-in-a-browser template: a frontend demo with an index.html + a built dist/ that loads the shipped browser bundle over Bun.serve — NOT FeatureProbe-bun, which is a headless JSON backend with no frontend)
    - demo/Showcase/frontend/src/main.ts (the breadcrumb + both-orientation steps gallery sections from Plan 20-05) and its theme switcher
    - viewmodel-shell/styles/default.css + styles/themes/ (the real CSS the page must load)
  </read_first>
  <action>First REBUILD the browser bundle (`cd viewmodel-shell && npm run build`) so the freshly-added breadcrumb/steps renderers from Plan 20-03 are IN the shipped dist/browser.js the page will load — a stale bundle renders nothing new. Then create `demo/NavVerification-bun/` (a Bun.serve app + index.html, mirroring demo/NonBlockingStaleness-bun's frontend-serving structure) that loads the REAL shipped viewmodel-shell browser bundle + `default.css` + a light theme + a dark theme (a toggle or two side-by-side frames), and renders: a BreadcrumbNode (href crumbs + an action crumb + a current last item), a horizontal StepsNode inside a deliberately narrow container so the intrinsic narrow-to-vertical collapse is visible, a deliberate `orientation:"vertical"` StepsNode with per-step descriptions, and a small TableNode with a `TableRow.action` so the reviewer can confirm the already-shipped `cursor:pointer` on hover. Bind it so it is reachable at `http://thenasty:<port>/` (bind the tailnet IP `100.113.23.63`) on the tailnet. Do NOT hand-mock the CSS — it must be the shipped renderer/CSS so the sign-off is meaningful. Include an on-page checklist naming what to look at (breadcrumb trail + current styling; step markers/connectors; both orientations; narrow-collapse; light + dark; clickable-row pointer).</action>
  <acceptance_criteria>
    - The bundle was rebuilt (`viewmodel-shell/dist/browser.js` postdates the Plan 20-03 renderer edits) so the served page carries the new breadcrumb/steps renderers.
    - `demo/NavVerification-bun/server.ts` + `index.html` exist and reference the real viewmodel-shell bundle + `default.css` + a theme.
    - The rendered view includes a breadcrumb, a horizontal steps, a vertical steps, and a clickable-row table.
    - The app starts and returns HTTP 200 on `/`.
    - A light/dark affordance (toggle or dual frame) is present.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell/demo/NavVerification-bun && bun run server.ts & SRV=$!; sleep 2; curl -s -o /dev/null -w 'HTTP:%{http_code}\n' "http://127.0.0.1:${PORT:-3011}/"; kill $SRV</automated>
  </verify>
  <done>A real-bundle verification view rendering both nodes, both step orientations, and a clickable-row table is served over the tailnet with a light/dark affordance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ashley signs off on the rendered nav primitives (pre-publish gate)</name>
  <action>Pause for the operator to open the tailnet verification page and review the rendered nodes; block Plan 20-07 until Ashley approves. The agent takes no implementation action in this step.</action>
  <what-built>BreadcrumbNode + StepsNode render via the real shipped bundle + default.css/themes on the tailnet verification page, in both step orientations (horizontal with intrinsic narrow-collapse + deliberate vertical), light + dark, alongside a clickable-row table.</what-built>
  <how-to-verify>
    1. Start `demo/NavVerification-bun` on the box and open `http://thenasty:<port>/` on the tailnet.
    2. Confirm the breadcrumb: a trail with the last item as the current page (bold, non-clickable), earlier items as links + the action crumb.
    3. Confirm the horizontal steps: numbered markers, a check on done steps, the current step highlighted with its ring, connectors reading bubble-edge to bubble-edge; then narrow the window and confirm it auto-collapses to vertical with NO awkward mid-break.
    4. Confirm the deliberate vertical wizard: markers down the left, connector running down, descriptions beside each step.
    5. Toggle light and dark (or view both frames) and confirm marker/connector/text contrast is legible in both.
    6. Hover a clickable table row and confirm the pointer cursor.
  </how-to-verify>
  <resume-signal>Ashley types "approved" (or describes any visual issue to fix before publish). Do NOT proceed to the release plan (20-07) until approved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built artifact → published release | The gate between an unverified build and an irreversible npm/NuGet publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | Tampering | shipping a regressed/red tree | mitigate | The full green-tree gate (both backends + all demos) must pass before the sign-off; no "pre-existing/unrelated" waiver without explicit operator approval |
| T-20-12 | Repudiation | publishing an unreviewed visual change | mitigate | A blocking human-verify checkpoint on the real-bundle tailnet page gates the publish (Plan 20-07) — a visual change cannot ship unreviewed |

No package-manager installs in this plan → no legitimacy gate needed.
</threat_model>

<verification>
- All green-tree suites exit 0 (recorded in SUMMARY).
- The verification app serves HTTP 200 with both nodes, both orientations, and a clickable-row table.
- Operator sign-off recorded; publish plan (20-07) is blocked until "approved".
</verification>

<success_criteria>
The full green-tree gate is green across both backends and all demos; a real-bundle tailnet verification page renders both nodes in both step orientations (with the intrinsic narrow-collapse) in light + dark plus a clickable-row cursor check; Ashley reviews it and signs off before any publish.
</success_criteria>

<output>
Create `.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-06-SUMMARY.md` when done (record every green-tree suite result + the sign-off outcome).
</output>
