---
phase: 20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-
plan: 04
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - viewmodel-shell/src/tui.tsx
autonomous: true
requirements: [NAV-03]

must_haves:
  truths:
    - "The TUI renders a legible text degradation for BreadcrumbNode (an inline trail) and StepsNode (a per-step list with state markers) instead of hitting UnsupportedView"
  artifacts:
    - path: "viewmodel-shell/src/tui.tsx"
      provides: "case 'breadcrumb' + case 'steps' in the renderNode switch"
      contains: "case \"breadcrumb\""
  key_links:
    - from: "viewmodel-shell/src/tui.tsx (steps case)"
      to: "per-step state text marker"
      via: "derive done/current/upcoming from current"
      pattern: "current"
---

<objective>
Give the `@experimental` TUI adapter a legible text degradation for both new nodes so the non-browser target doesn't fall through to `UnsupportedView`.

Purpose: the phase's cross-backend rule requires every target to at least degrade sensibly. The bar (per the `fits` case contract) is "doesn't break + degrades sensibly," not full fidelity.

Output: `case "breadcrumb"` and `case "steps"` in the `renderNode` switch of `tui.tsx`, rendering an inline trail (e.g. `Home › Products › Widget`) and a per-step list with state markers (e.g. `✓ Cart / ▸ Shipping / Payment`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/nav-primitives.md
@.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-PATTERNS.md

<interfaces>
From viewmodel-shell/src/tui.tsx (renderNode switch — the degrade template, lines 948-981):
```tsx
case "divider": return <text key={key} fg="#555555">{node.orientation === "vertical" ? "│" : "─".repeat(40)}</text>;
case "fits": { /* render the guaranteed-fits LAST candidate; @experimental degrade */ }
default: return <UnsupportedView key={key} type={...} />;
```
BreadcrumbNode/StepsNode types are in viewmodel-shell/src/index.ts (Plan 20-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TUI degradation cases for breadcrumb + steps</name>
  <read_first>
    - viewmodel-shell/src/tui.tsx (renderNode switch 948-981; the `divider` inline case + the `fits` deliberate-degrade case + `UnsupportedView`; how other text nodes render label text)
    - viewmodel-shell/src/index.ts (BreadcrumbNode/StepsNode from Plan 20-01)
    - .planning/phases/20-.../20-PATTERNS.md file #7 (the exact degrade adaptation)
  </read_first>
  <behavior>
    - A breadcrumb renders its item labels joined by a separator glyph on one line (last item is the current page — render it plainly, no special interactivity).
    - A steps node renders each step on a line (or joined) with a leading state marker derived from `current`: done = a check, current = a pointer/arrow, upcoming = plain; the description (if present) may be appended.
    - Neither node reaches `UnsupportedView`.
  </behavior>
  <action>Add `case "breadcrumb"` to the `renderNode` switch: render a single `<text>` of `node.items.map(i => i.label).join(" › ")` (or the tui's text primitive), muted styling consistent with the `divider` fg convention. Add `case "steps"`: derive each step's state from `node.current` (`i<current`→done marker, `i===current`→current marker, `i>current`→upcoming) and render the steps as text lines/segments with the state marker + label (append `description` when present). Keep it minimal — this is an `@experimental` degrade (the bar is "doesn't break," per the `fits` case comment). Do not import the browser renderer or add DOM.</action>
  <acceptance_criteria>
    - `grep -c 'case "breadcrumb"' viewmodel-shell/src/tui.tsx` == 1 and `case "steps"` == 1.
    - The steps case references `current` to derive per-step markers.
    - `cd viewmodel-shell && npx tsc --noEmit` exits 0.
    - `cd viewmodel-shell && npx vitest run` exits 0 (any existing TUI tests still pass; add a minimal assertion if the TUI has a test harness, otherwise tsc + no-UnsupportedView is sufficient).
  </acceptance_criteria>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && grep -q 'case "breadcrumb"' src/tui.tsx && grep -q 'case "steps"' src/tui.tsx</automated>
  </verify>
  <done>Both nodes degrade to legible text in the TUI (trail line + per-step state list), neither hits UnsupportedView, and TS compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-supplied strings → terminal text | Labels rendered as terminal text (no markup interpretation in a TUI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Denial of Service | TUI renderNode cases | accept | `@experimental` text-only degrade; labels render as plain terminal text with no markup/escape interpretation, no new external boundary |

No package-manager installs in this plan → no legitimacy gate needed.
</threat_model>

<verification>
- `cd viewmodel-shell && npx tsc --noEmit` clean.
- Both switch cases present via grep; neither node reaches UnsupportedView.
- `npx vitest run` green.
</verification>

<success_criteria>
The TUI adapter renders a defined legible degradation for BreadcrumbNode (inline trail) and StepsNode (per-step state list) instead of crashing/UnsupportedView; TS compiles and the suite is green.
</success_criteria>

<output>
Create `.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-04-SUMMARY.md` when done.
</output>
