---
phase: 20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/server.ts
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "index.ts declares a BreadcrumbNode and a StepsNode, both registered in the ViewNode union"
    - "A breadcrumb crumb can carry an optional action, and collectActions records that action so its name is uniqueness-checked"
    - "StepsNode carries no per-step status field — done/current/upcoming derives from a single 0-based current"
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "BreadcrumbNode + BreadcrumbItem + StepsNode + StepItem interfaces and union entries"
      contains: "interface BreadcrumbNode"
    - path: "viewmodel-shell/src/server.ts"
      provides: "collectActions breadcrumb arm + documented action-free/section-free leaf status for both nodes in both TS walks"
      contains: "case \"breadcrumb\""
  key_links:
    - from: "viewmodel-shell/src/server.ts"
      to: "BreadcrumbItem.action"
      via: "collectActions iterating items and recordAction-ing each item.action"
      pattern: "recordAction\\(item.action"
---

<objective>
Declare the two new node wire types in the TypeScript backend (the source of truth the .NET twin mirrors and the renderer consumes) and teach BOTH TypeScript tree-validators about them.

Purpose: BreadcrumbNode + StepsNode must exist as typed wire shapes before the renderer, TUI, parity fixtures, and .NET twin can be built against them. The action-name walk must descend into dispatch-bearing crumbs or we reintroduce the "silently-exempt dispatch-bearing descendant" bug the empty-state arm exists to prevent.

Output: `BreadcrumbNode`, `BreadcrumbItem`, `StepsNode`, `StepItem` interfaces + two `ViewNode` union entries in `index.ts`; a `case "breadcrumb"` in `collectActions` and both nodes named in the leaf comments of `collectActions` and `walkForSectionAction`; a vitest proving the crumb-action descent.

LOCKED DECISION (settles the design's open point): the crumb shape is `{ label: string; href?: string; external?: boolean; action?: ActionEvent }` — `href` = browser nav (external ⇒ new tab, exactly LinkNode), `action` = server dispatch. There is NO per-item "current" flag; the LAST item is auto-current. This is the minimal shape aligned with how LinkNode/ButtonNode already model nav-vs-dispatch, per PATTERNS.md file #1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/nav-primitives.md
@.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-CONTEXT.md
@.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-PATTERNS.md

<interfaces>
Analog contracts the executor uses directly (extracted; no exploration needed).

From viewmodel-shell/src/index.ts (LinkNode, the crumb model — lines 465-476):
```typescript
export interface LinkNode {
  type: "link";
  label: string;
  href: string;
  external?: boolean;   // true = new tab + noopener
  active?: boolean;     // "you are here"
}
```
ActionEvent lives at index.ts:3 (the dispatch payload type used for crumb actions).

From viewmodel-shell/src/index.ts (ChartNode/ChartSeries — the steps model, lines 734-763):
```typescript
export interface ChartSeries { name: string; data: number[]; tone?: "danger"|"warning"|"success"|"info"; }
export interface ChartNode {
  type: "chart";
  /** CLOSED union; OMITTED = "bar". The renderer treats an absent `kind` as "bar". */
  kind?: "bar" | "line" | "area" | "pie" | "donut";
  labels: string[];
  series: ChartSeries[];
}
```

ViewNode union tail (index.ts:132-154): `...| DividerNode | BadgeNode | ChartNode;` — append the two new entries here.

From viewmodel-shell/src/server.ts (collectActions tabs arm — the descent template, lines 175-178):
```typescript
case "tabs": {
  const tabs = node as TabsNode;
  for (const tab of tabs.tabs) recordAction(tab.action, enclosingForm, out);
  return;
}
```
The leaf `default: return` + its "no dispatch-bearing actions" comment is at server.ts:249-256 (currently lists `text, link, image, stat-bar, progress, copy-button, badge, chart`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare BreadcrumbNode + StepsNode wire types in index.ts</name>
  <read_first>
    - viewmodel-shell/src/index.ts (lines 1-10 for ActionEvent; 132-154 for the ViewNode union; 465-476 for LinkNode; 734-763 for ChartNode/ChartSeries — the two analogs)
    - .planning/phases/20-.../20-PATTERNS.md file #1 (the exact adaptation of LinkNode→crumb and ChartNode→steps)
    - .planning/design/nav-primitives.md sections 1 + 2 (the locked wire shapes + framework-owned/off-the-wire rules)
  </read_first>
  <behavior>
    - A `BreadcrumbNode` type-checks as `{ type: "breadcrumb"; items: BreadcrumbItem[] }`.
    - `BreadcrumbItem` type-checks as `{ label: string; href?: string; external?: boolean; action?: ActionEvent }` — no "current" field.
    - A `StepsNode` type-checks as `{ type: "steps"; steps: StepItem[]; current: number; orientation?: "horizontal" | "vertical" }`; `StepItem` = `{ label: string; description?: string }`.
    - `current` is a required (non-optional) `number`.
    - Both types are members of the `ViewNode` union (a `ViewNode` value of `{type:"breadcrumb",...}` / `{type:"steps",...}` type-checks).
  </behavior>
  <action>Add `export interface BreadcrumbItem` (`label` required string; `href?` string; `external?` boolean; `action?` ActionEvent) and `export interface BreadcrumbNode { type: "breadcrumb"; items: BreadcrumbItem[] }` near the LinkNode declaration. Add `export interface StepItem` (`label` required string; `description?` string) and `export interface StepsNode { type: "steps"; steps: StepItem[]; current: number; orientation?: "horizontal" | "vertical" }` near ChartNode. Append `| BreadcrumbNode` and `| StepsNode` to the `ViewNode` union at the end (after `| ChartNode`). TSDoc each: state that the LAST breadcrumb item is auto-current/non-clickable and the separator is framework-drawn/off-the-wire (mirror DividerNode's framework-drawn-separator doc); state that StepsNode per-step done/current/upcoming DERIVES from `current` (index<current=done, ==current=current, >current=upcoming), that OMITTED `orientation` = `"horizontal"` (mirror ChartNode's "renderer treats absent kind as bar" phrasing), and that the framework owns markers/connectors/reflow/a11y (never on the wire). Do NOT add per-step status, icons, maxItems, or custom-separator fields (deferred per CONTEXT out-of-scope).</action>
  <acceptance_criteria>
    - `grep -c "interface BreadcrumbNode" viewmodel-shell/src/index.ts` == 1 and `interface StepsNode`, `interface BreadcrumbItem`, `interface StepItem` each == 1.
    - The `ViewNode` union contains `| BreadcrumbNode` and `| StepsNode` (grep `-v '^ *//' viewmodel-shell/src/index.ts | grep -c "| BreadcrumbNode"` == 1).
    - `BreadcrumbItem.action` is typed `ActionEvent` (grep confirms `action?: ActionEvent`).
    - `StepsNode.current` is a non-optional `number` (grep confirms `current: number` with no `?`).
    - `cd viewmodel-shell && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && grep -q "interface BreadcrumbNode" src/index.ts && grep -q "interface StepsNode" src/index.ts</automated>
  </verify>
  <done>Both node interfaces + their sub-record interfaces exist, are in the ViewNode union, `current` is a required number, `action?: ActionEvent` is on the crumb, and TS compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teach both TS tree-validators about the new nodes</name>
  <read_first>
    - viewmodel-shell/src/server.ts (collectActions at 125; tabs arm 175-178; empty-state arm 240-247; leaf comment + default 249-256; walkForSectionAction at 309; its empty-state arm 423)
    - viewmodel-shell/src/index.ts (the BreadcrumbNode/StepsNode types from Task 1)
    - .planning/phases/20-.../20-PATTERNS.md file #5 (the exact validator adaptations + the "no fits-style blind spot" rationale)
  </read_first>
  <behavior>
    - collectActions on a `{type:"breadcrumb", items:[{label,action:{name:"go-home"}}, {label,href:"/x"}, {label /* current */}]}` records exactly the crumb action names (here `go-home`) into the action sink.
    - collectActions on a `{type:"steps",...}` records NOTHING (steps carry no actions) and does not throw.
    - Two breadcrumb crumbs sharing the same action name are flagged as a duplicate by the existing uniqueness machinery.
  </behavior>
  <action>In `collectActions`, add a `case "breadcrumb"` that iterates `items` and, for each item where `item.action` is set, calls `recordAction(item.action, enclosingForm, out)` — modeled exactly on the `tabs` arm (175-178) with an optional guard like the empty-state arm (240-247). Add `breadcrumb (crumb actions recorded above)` and `steps` to the `default:` leaf comment (249-256) so the deliberate action-free status of steps is explicit (no fits-style blind spot). In `walkForSectionAction`, add both `breadcrumb` and `steps` to the "leaf-like nodes carry no SectionNode descendants — no recursion needed" comment (do NOT add a recursion arm — neither node holds `ViewNode` children). Add/extend a vitest asserting (a) a breadcrumb crumb action name is collected and (b) duplicate crumb action names are rejected by the tree validator.</action>
  <acceptance_criteria>
    - `grep -c 'case "breadcrumb"' viewmodel-shell/src/server.ts` == 1, inside `collectActions`.
    - server.ts leaf comments name both `breadcrumb` and `steps` (grep both appear in comment lines near the two `default:` arms).
    - A vitest exists asserting the crumb-action descent + duplicate-name rejection.
    - `cd viewmodel-shell && npx vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd viewmodel-shell && grep -q 'case "breadcrumb"' src/server.ts && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>collectActions descends into breadcrumb crumb actions; both nodes documented as action-free/section-free leaves in both TS walks; a vitest proves the descent + duplicate rejection; vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-authored view tree → validators | An app (already-trusted per VMS model) submits a ViewNode tree; the validators are a correctness gate, not a security gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | collectActions breadcrumb arm | mitigate | Descend into crumb `action` so duplicate/ambiguous action names are caught at build (invalid_tree), not silently dispatched — the exact miss the empty-state arm exists to prevent |
| T-20-02 | Denial of Service | steps/breadcrumb leaf handling | accept | Type-only + validator-comment change; no runtime input parsing, no new external boundary; steps falls through the existing `default: return` |

No package-manager installs in this plan (no new dependencies) → no legitimacy gate needed.
</threat_model>

<verification>
- `cd viewmodel-shell && npx tsc --noEmit` clean.
- `cd viewmodel-shell && npx vitest run` green (includes the new validator test).
- Both nodes appear in the ViewNode union and both TS walks reference them (as an arm or a named leaf).
</verification>

<success_criteria>
BreadcrumbNode + StepsNode + their sub-records are declared and union-registered in TS; the crumb-action shape is locked to `{label,href?,external?,action?}`; both TS tree-validators handle the new nodes (breadcrumb action descent + documented leaves); TS compiles and vitest is green.
</success_criteria>

<output>
Create `.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-01-SUMMARY.md` when done.
</output>
