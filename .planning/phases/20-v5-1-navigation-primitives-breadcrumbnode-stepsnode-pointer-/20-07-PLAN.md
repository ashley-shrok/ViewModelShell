---
phase: 20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-
plan: 07
type: execute
wave: 4
depends_on: [20-06]
files_modified:
  - CHANGELOG.md
  - MIGRATION.md
  - viewmodel-shell/package.json
  - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
autonomous: false
requirements: [NAV-04]

must_haves:
  truths:
    - "agent-skill.md needs NO change (new node types, not new wire verbs/side-effects) — confirmed, and the .NET AgentSkill.md stays byte-identical"
    - "CHANGELOG + MIGRATION document the two new nodes (MIGRATION: new optional nodes, no action required)"
    - "npm + NuGet are both bumped to 5.1.0 (npm 5.0.1 to 5.1.0, .NET 5.0.0 to 5.1.0) and published; tag v5.1.0; main advanced; CI green; #vms-changelog announced"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "5.1.0 entry naming both moving packages + both new nodes"
      contains: "5.1.0"
    - path: "viewmodel-shell/package.json"
      provides: "version 5.1.0"
      contains: "5.1.0"
  key_links:
    - from: "git tag v5.1.0"
      to: "main"
      via: "git merge-base --is-ancestor v5.1.0 main"
      pattern: "merge-base"
---

<objective>
After Ashley's sign-off, ship the aligned npm + NuGet `5.1.0` release: docs, version bumps, the operator-gated publish ritual, tag, advance `main`, CI green, and the `#vms-changelog` announcement.

Purpose: a version bump is NOT a release — the registries are. This plan does the docs + the full publishing ritual per AGENTS.md, which is operator-gated (credentials held by the maintainer, never in the repo). The additive nodes keep the wire token `viewmodel-shell/1.0`; MIGRATION only needs the "new optional nodes, no action required" note.

Output: CHANGELOG + MIGRATION `5.1.0` entries; `package.json` and `.csproj` both at `5.1.0`; a confirmed no-change to `agent-skill.md`/`AgentSkill.md`; the operator-run publish/tag/advance-main/announce.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/nav-primitives.md
@.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-CONTEXT.md

<interfaces>
Current versions (from repo): npm `viewmodel-shell/package.json` = `5.0.1`; .NET `AshleyShrok.ViewModelShell.csproj <Version>` = `5.0.0`. Both go to `5.1.0` (asymmetric bump is legal; CHANGELOG names both moving packages).

agent-skill.md rule (AGENTS.md + design of record): these are new NODE types, not new wire-protocol verbs / side-effects — the skill enumerates the PROTOCOL, not the node catalog. So NO agent-skill.md change is expected (contrast toasts, which DID require a skill update). Confirm by diffing: `viewmodel-shell/agent-skill.md` must stay byte-identical to `viewmodel-shell-dotnet/AgentSkill.md` (the parity gate `parity/check-skill.ts` diffs both) — the correct outcome is that neither file changes.

The publishing ritual (AGENTS.md — operator-gated; credentials are NOT in the repo; the maintainer runs an auth precheck first, never `npm login`):
- npm: `cd viewmodel-shell && npm publish` (prepublishOnly runs the build), then confirm via the registry curl.
- NuGet: `cd viewmodel-shell-dotnet && dotnet pack -c Release && dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.5.1.0.nupkg --api-key "$NUGET_API_KEY" --source https://api.nuget.org/v3/index.json`, then confirm via the flatcontainer curl.
- Tag: `git tag -a v5.1.0 <release-sha> -m "viewmodel-shell 5.1.0" && git push origin v5.1.0`.
- Advance main: ensure `git merge-base --is-ancestor v5.1.0 main` (rebase/merge if the release commit is not on main), push main.
- Announce `#vms-changelog`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm agent-skill.md unchanged + write CHANGELOG/MIGRATION + bump both versions to 5.1.0</name>
  <read_first>
    - CHANGELOG.md (the latest 5.0.0/5.0.1 entries — the additive-node entry format + the "names both moving packages" convention)
    - MIGRATION.md (a prior additive-node "no action required" note)
    - viewmodel-shell/agent-skill.md + viewmodel-shell-dotnet/AgentSkill.md (to confirm NO change is needed and they stay byte-identical)
    - viewmodel-shell/package.json + viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj (the version fields)
  </read_first>
  <action>Confirm `agent-skill.md` requires NO edit (new node types, not new wire verbs/side-effects) and that `viewmodel-shell/agent-skill.md` is byte-identical to `viewmodel-shell-dotnet/AgentSkill.md` (diff them; expect zero diff — do NOT edit either). Add a CHANGELOG `5.1.0` entry naming BOTH moving packages (npm 5.0.1 to 5.1.0, NuGet 5.0.0 to 5.1.0) and describing BreadcrumbNode (nav trail, last item auto-current, framework-drawn separator, href/external/action crumbs) + StepsNode (0-based `current` deriving done/current/upcoming, A+C orientation with intrinsic horizontal-to-vertical collapse, framework-drawn markers/connectors + full a11y); note additive, wire token stays `viewmodel-shell/1.0`. Add a MIGRATION `5.1.0` note: "new optional nodes (breadcrumb, steps) — no action required; existing apps and agents are byte-unchanged." Set `package.json` `"version": "5.1.0"` and `.csproj` `<Version>5.1.0</Version>`.</action>
  <acceptance_criteria>
    - `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` produces NO output (byte-identical, both unchanged).
    - CHANGELOG.md has a `5.1.0` entry mentioning `breadcrumb`, `steps`, and both package versions.
    - MIGRATION.md has a `5.1.0` "no action required" note.
    - `grep '"version": "5.1.0"' viewmodel-shell/package.json` and `grep '<Version>5.1.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` both match.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md && grep -q '"version": "5.1.0"' viewmodel-shell/package.json && grep -q '<Version>5.1.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj && grep -q '5.1.0' CHANGELOG.md && echo DOCS_OK</automated>
  </verify>
  <done>agent-skill.md confirmed unchanged + byte-identical across backends; CHANGELOG + MIGRATION document the two nodes; both packages bumped to 5.1.0.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full green-tree gate at release version (release-time gate)</name>
  <read_first>
    - ./CLAUDE.md then AGENTS.md (the release-time green-tree gate requirement)
    - viewmodel-shell/Tests/VersioningTests (if a .NET version test asserts the version string — it will now expect 5.1.0)
  </read_first>
  <action>With the versions bumped, re-run the FULL green-tree gate (vitest, core-globals, aa-contrast, theme-byte-identity, parity, framework .NET Tests, every demo Tests). Fix any version-assertion test that pins the old version (e.g. a VersioningTests expecting 5.0.x → 5.1.0). Confirm the skill parity check (`parity/check-skill.ts`, run as part of parity) is green with the unchanged skill. Do NOT publish on a red tree.</action>
  <acceptance_criteria>
    - Every green-tree suite exits 0 at version 5.1.0 (vitest, core-globals, aa-contrast, theme-byte-identity, parity, framework Tests, all demo Tests).
    - Any version-pinning test now asserts 5.1.0 and passes.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH"; cd /home/thenasty/ViewModelShell/viewmodel-shell && npx vitest run && npm run check:core-globals && node scripts/check-aa-contrast.mjs && node scripts/check-theme-byte-identity.mjs && cd ../parity && bun run run.ts && cd .. && dotnet test viewmodel-shell-dotnet/Tests/Tests.csproj --nologo -v minimal && for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done && echo GREEN_AT_5_1_0</automated>
  </verify>
  <done>The full green-tree gate is green at 5.1.0, including the skill parity check on the unchanged agent-skill.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator publish ritual — npm + NuGet 5.1.0, tag, advance main, CI, announce</name>
  <action>Pause for the maintainer to run the credentialed publish ritual (auth precheck, commit docs+bumps, npm publish, dotnet pack + nuget push, tag v5.1.0, advance main, watch CI, announce #vms-changelog). The agent does not hold the registry credentials and does not publish.</action>
  <what-built>Docs + version bumps are committed at 5.1.0 and the full green-tree gate is green; Ashley has signed off on the rendered nodes (Plan 20-06). The registries are the release — this step publishes.</what-built>
  <how-to-verify>
    The maintainer (operator) performs the publishing ritual per AGENTS.md — the registry credentials are NOT in the repo, so an autonomous agent cannot do this:
    1. Run the auth precheck (activate the npm token so `npm whoami` succeeds; make `$NUGET_API_KEY` available). If auth is broken, STOP.
    2. Commit the docs + version bumps to the current branch (operator-driven git, per project rules).
    3. npm: `cd viewmodel-shell && npm publish`; confirm `latest` == `5.1.0` via the registry curl.
    4. NuGet: `cd viewmodel-shell-dotnet && dotnet pack -c Release && dotnet nuget push bin/Release/AshleyShrok.ViewModelShell.5.1.0.nupkg --api-key "$NUGET_API_KEY" --source https://api.nuget.org/v3/index.json`; confirm the flatcontainer curl shows `5.1.0`.
    5. Tag: `git tag -a v5.1.0 <release-sha> -m "viewmodel-shell 5.1.0" && git push origin v5.1.0`.
    6. Advance main: confirm `git merge-base --is-ancestor v5.1.0 main` (rebase/merge the release commit onto main if needed) and push main.
    7. Watch CI green.
    8. Post the release line to `#vms-changelog`.
  </how-to-verify>
  <resume-signal>Operator confirms: npm 5.1.0 + NuGet 5.1.0 both live on the registries, tag v5.1.0 pushed, `git merge-base --is-ancestor v5.1.0 main` passes, CI green, #vms-changelog announced.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo state → public registries | npm + NuGet publish is irreversible; a mismatched/broken publish drifts the registry from the repo |
| maintainer credentials → publish | npm/NuGet tokens held by the operator, never in the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Elevation of Privilege | publish credentials | mitigate | Publish is an operator-gated checkpoint (`checkpoint:human-action`); credentials are never in the repo and the agent never runs the publish — the maintainer does, after an auth precheck |
| T-20-14 | Tampering | main stranded behind the tag | mitigate | Explicitly verify `git merge-base --is-ancestor v5.1.0 main` (the exact failure that stranded v1.5.0/v1.6.0) so a fresh clone gets the released code |
| T-20-15 | Repudiation | version/registry drift | mitigate | Both packages bumped + both published + confirmed via registry curls; CHANGELOG names both moving packages |

No third-party package installs — this publishes OUR OWN packages; no legitimacy gate needed. (The `T-{phase}-SC` supply-chain slot does not apply: no npm/pip/cargo dependency installs occur in this phase.)
</threat_model>

<verification>
- `diff` of the two skill files is empty (unchanged, byte-identical).
- Both versions read 5.1.0; CHANGELOG + MIGRATION entries present.
- Full green-tree gate green at 5.1.0.
- (Operator) npm 5.1.0 + NuGet 5.1.0 live; tag v5.1.0 pushed; `git merge-base --is-ancestor v5.1.0 main` passes; CI green; #vms-changelog announced.
</verification>

<success_criteria>
v5.1.0 ships as an aligned additive npm + NuGet release: agent-skill.md confirmed unchanged/byte-identical, CHANGELOG + MIGRATION document the two nodes, both packages published + tagged `v5.1.0`, `main` advanced to the release commit, CI green, and `#vms-changelog` announced — the wire token stays `viewmodel-shell/1.0`.
</success_criteria>

<output>
Create `.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-07-SUMMARY.md` when done (record the published versions, tag, main-advance verification, and announcement).
</output>
