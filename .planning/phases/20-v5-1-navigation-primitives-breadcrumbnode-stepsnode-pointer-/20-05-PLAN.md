---
phase: 20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-
plan: 05
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - demo/FeatureProbe-bun/handler.ts
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
  - parity/fixtures/feature-probe.json
  - demo/Showcase/frontend/src/main.ts
autonomous: true
requirements: [NAV-03, NAV-04]

must_haves:
  truths:
    - "A byte-identical nav section (breadcrumb + steps in both orientations, with omitted-vs-present optional fields, and an action crumb) is added to BOTH FeatureProbe backends"
    - "bun run parity/run.ts is byte-identical green across every backend"
    - "Showcase has a Breadcrumb and a Steps gallery section (steps in both orientations) for the visual reference"
  artifacts:
    - path: "demo/FeatureProbe-bun/handler.ts"
      provides: "navSection with breadcrumb + steps exercising the full omitted/present wire matrix (TS)"
      contains: "breadcrumb"
    - path: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs"
      provides: "byte-identical .NET twin of navSection"
      contains: "BreadcrumbNode"
    - path: "demo/Showcase/frontend/src/main.ts"
      provides: "Breadcrumb + Steps gallery sections (both step orientations)"
      contains: "steps"
  key_links:
    - from: "parity/run.ts"
      to: "the nav section wire"
      via: "existing GET steps capture the static view-shape; byte-diff across backends"
      pattern: "feature-probe"
---

<objective>
Add the parity coverage that PROVES BreadcrumbNode + StepsNode round-trip byte-identically across the TS + .NET backends, and add the Showcase gallery entries that serve as the visual reference for Ashley's verification page.

Purpose: byte-identity across backends is the phase's non-negotiable cross-cutting requirement; parity is the CI-gated proof. The FeatureProbe twins must be constructed identically (same labels/hrefs/orientation-omit-choices/current) — that is exactly what `bun run parity/run.ts` diffs.

Output: a `navSection` (or two) appended to both FeatureProbe backends exercising the full omitted-vs-present matrix; a `$comment` addition to the fixture spec; a Breadcrumb + Steps gallery section in Showcase; a green parity run.

The nav section is STATIC view-shape captured by the existing GET steps (like feedbackSection/chartSection) — NO new POST step is needed. Give the action crumb a UNIQUE action name so the action-name walk descent (Plans 20-01/20-02) is exercised in a real tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/nav-primitives.md
@.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-PATTERNS.md

<interfaces>
Analog contracts (extracted).

From demo/FeatureProbe-bun/handler.ts (feedbackSection — the static-section template, 609-625; appended into buildVm's page children 682-704):
```typescript
const feedbackSection: ViewNode = {
  type: "section", heading: "Feedback primitives", variant: "card",
  children: [
    { type: "badge", label: "New" },
    { type: "badge", label: "3", tone: "danger" },
    { type: "empty-state", heading: "Nothing here", message: "...",
      action: { type: "button", label: "Add item", action: { name: "feedback-cta" }, emphasis: "primary" } },
  ],
};
// ... return { ... children: [ ..., feedbackSection ] }
```

From demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (the .NET twin — 675-688):
```csharp
pageChildren.Add(new SectionNode(Heading: "Feedback primitives", Variant: "card",
    Children: new ViewNode[] {
        new BadgeNode("New"),
        new EmptyStateNode("Nothing here", Message: "Add the first item.",
            Action: new ButtonNode("Add item", new ActionDescriptor("feedback-cta"), Emphasis: "primary")),
    }));
```

parity/fixtures/feature-probe.json `$comment` (line 3) is the running-doc convention — every additive node appended a sentence describing what its section proves (badge/divider/chart/blocking all ride the existing GET steps with NO new POST step).

Showcase gallery section shape (demo/Showcase/frontend/src/main.ts, e.g. lines 224/235/248): `{ type: "section", heading: "...", children: [ ...nodes... ] }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: byte-identical navSection in both FeatureProbe backends</name>
  <read_first>
    - demo/FeatureProbe-bun/handler.ts (feedbackSection 609-625; the buildVm return page.children array 682-704; the "byte-identical to the .NET twin" comment convention on every section)
    - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (the feedbackSection twin 675-688; how BadgeNode/EmptyStateNode/ButtonNode are constructed)
    - viewmodel-shell/src/index.ts + viewmodel-shell-dotnet/ViewModels.cs (the BreadcrumbNode/StepsNode shapes from Plans 20-01/20-02)
    - .planning/phases/20-.../20-PATTERNS.md files #8-10 (the exact fixture adaptation + omitted-vs-present matrix)
  </read_first>
  <behavior>
    - The nav section, serialized, is byte-identical between the bun backend and the .NET backend (that is what parity diffs).
    - The breadcrumb exercises: an href-only crumb (external omitted → absent), an external:true crumb (present), an action crumb with a UNIQUE action name (proving the action-name descent), and the auto-current LAST item.
    - The steps exercise: `orientation` OMITTED (proves absent = default horizontal), a second steps with `orientation:"vertical"` (proves the literal string crosses), a step with `description` omitted vs present, and a mid `current` index.
  </behavior>
  <action>In `handler.ts`, build a `navSection: ViewNode` (`type:"section"`, a heading like "Navigation primitives", `variant:"card"`) containing: a `{type:"breadcrumb", items:[...]}` with an href crumb (no external), an `external:true` crumb, an `{label, action:{name:"nav-crumb-probe"}}` action crumb (UNIQUE name — not reused elsewhere in the fixture), and a final label-only current crumb; a `{type:"steps", steps:[{label,description}, {label}, {label}], current:1}` with `orientation` OMITTED; and a second `{type:"steps", ..., current:1, orientation:"vertical"}`. Append `navSection` to the buildVm return `children` array. Add the standing `// byte-identical to the .NET twin` comment. In `FeatureProbeController.cs`, construct the SAME section with `new BreadcrumbNode(...)`, `new BreadcrumbItem(...)`, `new StepsNode(...)`, `new StepItem(...)` — EXACT same labels/hrefs/external choices/action name/step labels/descriptions/current/orientation-omit choices, appended to `pageChildren` at the mirror position. Any value that is omitted in one backend MUST be omitted in the other.</action>
  <acceptance_criteria>
    - `grep -q 'breadcrumb' demo/FeatureProbe-bun/handler.ts && grep -q 'orientation: "vertical"' demo/FeatureProbe-bun/handler.ts` true.
    - `grep -q 'new BreadcrumbNode' demo/FeatureProbe/AspNetCore/FeatureProbeController.cs && grep -q 'Orientation: "vertical"' demo/FeatureProbe/AspNetCore/FeatureProbeController.cs` true.
    - The action crumb name `nav-crumb-probe` (or the chosen unique name) appears in BOTH backends and nowhere else in the fixture (no duplicate-action collision).
    - `cd viewmodel-shell && cd ../demo/FeatureProbe-bun && bun build handler.ts --target=bun >/dev/null` (or `bunx tsc --noEmit`) succeeds; `export PATH="$HOME/.dotnet:$PATH" && cd demo/FeatureProbe/AspNetCore && dotnet build --nologo -v minimal` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>export PATH="$HOME/.dotnet:$PATH" && cd demo/FeatureProbe/AspNetCore && dotnet build --nologo -v minimal 2>&1 | tail -3 && grep -q 'new BreadcrumbNode' FeatureProbeController.cs</automated>
  </verify>
  <done>A byte-aligned nav section exists in both FeatureProbe backends exercising the full omitted/present wire matrix + an action crumb with a unique name; both backends build.</done>
</task>

<task type="auto">
  <name>Task 2: fixture $comment + run parity green</name>
  <read_first>
    - parity/fixtures/feature-probe.json ($comment line 3 — the running-doc convention; confirm the nav section rides existing GET steps, no new POST step)
    - parity/backends.json (the feature-probe backends 109-135 — confirm which backends run this fixture)
    - .planning/phases/20-.../20-PATTERNS.md file #10 (the $comment adaptation + "static wire-shape rides GET steps" precedent)
  </read_first>
  <action>Append a sentence to the `feature-probe.json` `$comment` describing what the nav section proves: breadcrumb crumbs with omitted-vs-present `href`/`external`/`action` (and the auto-current last item), the crumb action-name uniqueness descent (`nav-crumb-probe`), and steps with `orientation` omitted (default horizontal) vs `"vertical"`, `description` omitted vs present, all captured by the existing GET steps with no new POST step. Do NOT add a POST step. Then run `bun run parity/run.ts` and confirm byte-identical green across all backends. If parity flags a diff, the fix is to make the two FeatureProbe twins byte-identical (a value present in one and omitted in the other), NOT to loosen the diff.</action>
  <acceptance_criteria>
    - `feature-probe.json` `$comment` mentions the nav section (grep `breadcrumb` / `steps` / `orientation` in the comment).
    - No new POST step added to `feature-probe.json` (step count for the nav coverage unchanged — it rides GET).
    - `cd parity && bun run run.ts` exits 0 with a byte-identical result across every backend.
  </acceptance_criteria>
  <verify>
    <automated>cd parity && bun run run.ts 2>&1 | tail -12</automated>
  </verify>
  <done>The fixture doc names the nav coverage; `bun run parity/run.ts` is byte-identical green, proving both nodes serialize identically across TS + .NET.</done>
</task>

<task type="auto">
  <name>Task 3: Showcase gallery sections (breadcrumb + steps, both orientations)</name>
  <read_first>
    - demo/Showcase/frontend/src/main.ts (existing gallery per-primitive sections, e.g. 224/235/248 — the `{type:"section", heading, children}` pattern)
    - viewmodel-shell/src/index.ts (BreadcrumbNode/StepsNode shapes)
    - .planning/phases/20-.../20-PATTERNS.md file #11 (Showcase is the discretionary host — the visual reference the verification page draws from)
  </read_first>
  <action>Add a "Breadcrumb" gallery section rendering a representative breadcrumb (a few href crumbs + a current last item, and one action crumb) and a "Steps" gallery section rendering a horizontal steps (default orientation, a mid `current`) AND a vertical steps (`orientation:"vertical"`, with per-step descriptions) so both orientations are visible. Follow the existing gallery section shape exactly; no custom CSS.</action>
  <acceptance_criteria>
    - `grep -q 'type: "breadcrumb"' demo/Showcase/frontend/src/main.ts && grep -c 'type: "steps"' demo/Showcase/frontend/src/main.ts` shows breadcrumb present and ≥2 steps nodes (both orientations).
    - `cd demo/Showcase/frontend && npx tsc --noEmit` (or the Showcase build) succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd demo/Showcase/frontend && npx tsc --noEmit 2>&1 | tail -3 && grep -q 'type: "breadcrumb"' src/main.ts</automated>
  </verify>
  <done>Showcase has a Breadcrumb section and a Steps section covering both orientations; the Showcase frontend compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-backend wire | The two FeatureProbe backends must emit byte-identical JSON for the same declared tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering | FeatureProbe twin drift | mitigate | `bun run parity/run.ts` byte-diffs the two backends step-for-step; a value present in one and omitted in the other fails CI, so cross-backend wire drift cannot ship |
| T-20-10 | Tampering | duplicate crumb action name in fixture | mitigate | The action crumb uses a unique name (`nav-crumb-probe`) not reused elsewhere, so it exercises (not trips) the action-name uniqueness validator |

No package-manager installs in this plan → no legitimacy gate needed.
</threat_model>

<verification>
- Both FeatureProbe backends build; the nav section is byte-aligned.
- `cd parity && bun run run.ts` byte-identical green.
- Showcase frontend compiles with breadcrumb + both-orientation steps sections.
</verification>

<success_criteria>
Both nodes are proven byte-identical across TS + .NET by a green `bun run parity/run.ts` (new FeatureProbe nav section exercising the full omitted/present matrix + an action-crumb descent), and Showcase has the visual-reference gallery sections (both step orientations).
</success_criteria>

<output>
Create `.planning/phases/20-v5-1-navigation-primitives-breadcrumbnode-stepsnode-pointer-/20-05-SUMMARY.md` when done.
</output>
