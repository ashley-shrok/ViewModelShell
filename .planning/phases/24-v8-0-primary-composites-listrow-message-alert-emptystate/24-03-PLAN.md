---
phase: 24-v8-0-primary-composites-listrow-message-alert-emptystate
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/browser.ts
  - viewmodel-shell/src/server.ts
  - viewmodel-shell/styles/default.css
  - viewmodel-shell-dotnet/ViewModels.cs
  - viewmodel-shell/test/alert.test.ts
  - viewmodel-shell-dotnet/Tests/AlertNodeSerializationTests.cs
autonomous: true
requirements: [COMP-07]

must_haves:
  truths:
    - "`AlertNode` ships with wire type `\"alert\"` on both backends. Slots: `tone` (REQUIRED, closed 4-way `\"danger\" | \"warning\" | \"success\" | \"info\"` — this IS the point of the node), `title?` (string), `message` (required, `string | ViewNode`), `icon?` (IconName override), `actions?` (`ButtonNode[]`), `dismissible?` (bool with `WhenWritingDefault` posture on .NET)."
    - "Tone→icon default mapping is BAKED into the browser renderer as `ALERT_TONE_ICON` static: `danger` → `x-circle`, `warning` → `alert-triangle`, `success` → `check-circle`, `info` → `info`. All four are LUCIDE names shipped in the Phase 22 curated set. `n.icon?` overrides the default."
    - "Renderer emits `<div class=\"vms-alert vms-alert--{tone}\">` with grid `[icon | body | actions]`. Icon column via `renderIconSvg(iconName, \"md\")`. Body: `.vms-alert__title` (string→TextNode style:body weight:medium) + `.vms-alert__message` (string→TextNode style:muted). Actions column: right-aligned ButtonNode[]; if `dismissible: true`, a `<button class=\"vms-alert__dismiss\" aria-label=\"Dismiss\">✕</button>` that dispatches `{ name: \"dismiss\" }` (fixed action name, NOT a caller-supplied ActionEvent — deviates from ModalNode.dismissAction per PATTERNS.md §Headline point 4)."
    - "Tree-validator descends into `message` (if ViewNode), each `actions[i]`. `dismissible:true` emits `{name:\"dismiss\"}` client-side only — the server tree carries NO ActionEvent, so nothing is recorded server-side for it. `tone` required at the type level (both TS union and .NET non-nullable enum)."
    - "`.NET` `AlertNode.Tone` typed non-nullable `Tone` (required); `Message` typed non-nullable `ViewNode` (required, polymorphic). `Dismissible` typed `bool` with `[JsonIgnore(WhenWritingDefault)]` — false is ABSENT on the wire. Icon typed `IconName?`."
    - "vitest (`test/alert.test.ts`) asserts DOM per tone × 4 + custom-icon override + tone-default-icon default + dismissible-emits-dismiss + string-vs-ViewNode message + title present/absent. Dismissible action-name is exactly `\"dismiss\"` (not `\"alert-dismiss\"` or app-supplied) — verified by spy call."
    - ".NET (`Tests/AlertNodeSerializationTests.cs`) asserts discriminator, tone required + KebabEnum roundtrip, Dismissible WhenWritingDefault (false ABSENT / true present), IconName kebab roundtrip for override."
    - "AA hand-check for AlertNode's 4 tones × 4 elements (title text, message text, icon glyph, action button) × 13 themes = 208 pair-checks. WCAG AA-normal 4.5:1 on title + message text; WCAG UI-state 3:1 on icon glyph + action button border. Deepen via color-mix or KNOCKOUT per v3.5.0 pattern where any drops below target."
    - "Documented in the composite's docstring: `dismissible:true` uses the reserved action name `\"dismiss\"`. Apps that need a more-specific name compose their own dismiss button in `actions[]` and set `dismissible:false`."
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "`AlertNode` interface + ViewNode union entry."
      contains: "\"alert\""
    - path: "viewmodel-shell/src/browser.ts"
      provides: "`private alert()` renderer + `ALERT_TONE_ICON` static tone→icon default map + `renderNode` switch arm."
      contains: "vms-alert"
    - path: "viewmodel-shell/src/server.ts"
      provides: "Walker arm for `\"alert\"` (descend into message + actions; do NOT record dismiss)."
      contains: "case \"alert\""
    - path: "viewmodel-shell/styles/default.css"
      provides: "`.vms-alert*` class block including tinted-surface tones + `__title`/`__message`/`__icon`/`__actions`/`__dismiss`."
      contains: ".vms-alert"
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "`AlertNode` record + `[JsonDerivedType]` + walker arms. Tone required (non-nullable); Message required (non-nullable ViewNode); Dismissible WhenWritingDefault."
      contains: "AlertNode"
    - path: "viewmodel-shell/test/alert.test.ts"
      provides: "jsdom render tests + a11y + mutation coverage + AA hand-check header (208 pair-checks)."
    - path: "viewmodel-shell-dotnet/Tests/AlertNodeSerializationTests.cs"
      provides: ".NET wire tests including Tone-required + Dismissible-WhenWritingDefault."
  key_links:
    - from: "viewmodel-shell/src/index.ts"
      to: "viewmodel-shell-dotnet/ViewModels.cs"
      via: "byte-identical wire shape — Tone required (union in TS, non-nullable enum in .NET)"
      pattern: "alert"
    - from: "viewmodel-shell/src/browser.ts (ALERT_TONE_ICON static)"
      to: "viewmodel-shell/src/browser.ts (private alert())"
      via: "n.icon ?? ALERT_TONE_ICON[n.tone] default resolution"
      pattern: "ALERT_TONE_ICON"
---

<objective>
Land `AlertNode` (COMP-07) end-to-end. Both backends byte-identical. Renderer bakes in the tone→icon default map (danger→x-circle, warning→alert-triangle, success→check-circle, info→info) with n.icon override. Dismissible variant emits a close-X that dispatches the RESERVED fixed action name `"dismiss"` client-side (deviates from ModalNode.dismissAction — Alert emits the ActionEvent locally rather than accepting a caller-supplied slot per CONTEXT §5). Ships with vitest + .NET tests + AA hand-check for the 208-pair matrix (4 tones × 4 elements × 13 themes).

Purpose: The prominent status-message primitive — the shape Moxie's discoverability-miss + every "warning banner" app needs. Route B recipe with framework-baked tone semantics: the tone axis controls color palette + default icon + border color simultaneously (typed-slots pattern).

Output: `AlertNode` shipped both backends; tone→icon default mapping baked in browser; dismissible reserved-action documented; full test coverage; AA hand-check documented for 208 pairs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md
@.planning/design/composite-nodes-layer.md
@AGENTS.md
</context>

<interfaces>
<!-- Pulled from PATTERNS.md §1, §2f, §4, §5. The dismissible deviation from ModalNode.dismissAction is the key design intent to preserve. -->

Current ModalNode.dismissAction close-X template (browser.ts:3471-3479 per PATTERNS.md §2f Analog E):
```typescript
if (n.dismissAction) {
  const action = n.dismissAction;
  const closeBtn = document.createElement("button");
  closeBtn.type = "button";
  closeBtn.className = "vms-modal__close";
  closeBtn.textContent = "✕";
  closeBtn.addEventListener("click", () => on(action));
  header.appendChild(closeBtn);
}
```

AlertNode DIFFERS: `dismissible?: boolean` (not `dismissAction?: ActionEvent`). The renderer builds `{ name: "dismiss" }` locally at click time. Rationale per CONTEXT §5: "No `dismissAction: ActionEvent` slot — that would be redundant; the composite emits `{name: "dismiss"}`."

Current SectionNode tinted-surface CSS (default.css:443-446 per PATTERNS.md §4 Analog B):
```css
.vms-section--danger  { background: color-mix(in srgb, var(--vms-error) 14%, var(--vms-surface));   border: 1px solid var(--vms-error); }
.vms-section--warning { background: color-mix(in srgb, var(--vms-warning) 14%, var(--vms-surface)); border: 1px solid var(--vms-warning); }
.vms-section--success { background: color-mix(in srgb, var(--vms-success) 14%, var(--vms-surface)); border: 1px solid var(--vms-success); }
.vms-section--info    { background: color-mix(in srgb, var(--vms-info) 14%, var(--vms-surface));    border: 1px solid var(--vms-info); }
```

AlertNode uses a SIMILAR pattern with different percentages (per PATTERNS.md §4 lines 929-932 — danger/success 8%, warning 10%, info 8%; borders at 30% mix).

renderIconSvg helper signature (Phase 22 shipped — check browser.ts around the icon() renderer): `this.renderIconSvg(iconName, size, tone?, ariaLabel?)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add AlertNode wire type (TS + .NET) + CSS block</name>
  <files>viewmodel-shell/src/index.ts, viewmodel-shell/styles/default.css, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell/src/index.ts (read the ViewNode union at :180-210; find IconName type — grep `export type IconName` or `export const ICON_NAMES` — the closed union AlertNode.icon references; find ButtonNode)
    - viewmodel-shell-dotnet/ViewModels.cs (grep for `public enum Tone` — the tone enum that AlertNode.Tone reuses; `IconName` enum; `[JsonPolymorphic]` block at :751-782)
    - viewmodel-shell/styles/default.css (read `.vms-section--danger` .. `--info` at :443-446 for the color-mix template; grep for `--vms-error`, `--vms-warning`, `--vms-success`, `--vms-info` to know the tone tokens)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md §5 (LOCKED AlertNode schema + tone→icon default mapping)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §1 (AlertNode interface to copy verbatim, lines 146-163) + §4 (default.css block for `.vms-alert*` verbatim, lines 916-943) + §5 (.NET record)
  </read_first>
  <action>
    In `viewmodel-shell/src/index.ts`:

    (a) Add `AlertNode` interface per PATTERNS.md §1 (lines 146-163 of PATTERNS.md):
    ```
    export interface AlertNode {
      type: "alert";
      tone: "danger" | "warning" | "success" | "info";    // REQUIRED
      title?: string;
      message: string | ViewNode;                           // REQUIRED
      icon?: IconName;                                      // overrides tone default
      actions?: ButtonNode[];
      dismissible?: boolean;
    }
    ```
    TSDoc from PATTERNS.md — every slot documented. `tone` TSDoc explicitly notes: "REQUIRED — the point of the node. Drives surface tint + border color + default icon." `dismissible` TSDoc: "true → close-X button dispatches `{name: 'dismiss'}` (fixed reserved name; distinct from `ModalNode.dismissAction` which takes a caller-supplied ActionEvent slot). Apps needing a more-specific name compose their own dismiss button in `actions[]` and set `dismissible:false`."

    (b) Add `AlertNode` to the ViewNode union.

    In `viewmodel-shell-dotnet/ViewModels.cs`:

    (c) Add `[JsonDerivedType(typeof(AlertNode), "alert")]` to the `[JsonPolymorphic]` block at :751-782.

    (d) Add `AlertNode` record per PATTERNS.md §5:
    ```
    public record AlertNode(
        Tone Tone,                              // REQUIRED — non-nullable
        ViewNode Message,                        // REQUIRED — non-nullable, polymorphic
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Title = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] IconName? Icon = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] IReadOnlyList<ButtonNode>? Actions = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool Dismissible = false
    ) : ViewNode;
    ```
    `Tone` is non-nullable (required by C# type system — cannot construct AlertNode without providing it, mirroring the required union in TS). `Message` is `ViewNode` (not `ViewNode?`) — required, polymorphic. `Dismissible` uses `WhenWritingDefault` matching the SectionNode.FollowTail posture from Plan 24-02.

    In `viewmodel-shell/styles/default.css`:

    (e) Append the AlertNode CSS block verbatim from PATTERNS.md §4 (lines 916-943 of PATTERNS.md — starts with `/* ── AlertNode (COMP-07, v8.0.0) ─── */`). Insert after the MessageNode block from Plan 24-02. Includes:
      - `.vms-alert` — grid `auto 1fr auto` with gap `var(--vms-space-md)`, padding, border-radius, border 1px transparent.
      - `.vms-alert--{danger|warning|success|info}` — tinted background via `color-mix(in srgb, var(--vms-{error|warning|success|info}) {8%|10%|8%|8%}, var(--vms-surface))` and border-color at 30% mix.
      - `.vms-alert__icon` — 1.25rem × 1.25rem with tone-colored `color`.
      - `.vms-alert--{tone} .vms-alert__icon` — `color: var(--vms-{tone-token})` for each tone.
      - `.vms-alert__body` (min-width: 0), `.vms-alert__title` (text-md weight:600), `.vms-alert__message` (text-sm muted).
      - `.vms-alert__actions` (right-aligned flex, gap), `.vms-alert__dismiss` (transparent bg, border:none, cursor:pointer, focus outline).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'export interface AlertNode' src/index.ts &amp;&amp; grep -c '"alert"' src/index.ts &amp;&amp; grep -c 'public record AlertNode' ../viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c 'JsonDerivedType(typeof(AlertNode)' ../viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c '\.vms-alert' styles/default.css &amp;&amp; grep -c 'vms-alert--danger\|vms-alert--warning\|vms-alert--success\|vms-alert--info' styles/default.css &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet build ../viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export interface AlertNode' viewmodel-shell/src/index.ts` == 1.
    - AlertNode interface: `tone` is REQUIRED (no `?`), `message` REQUIRED (no `?`); `title/icon/actions/dismissible` optional.
    - `viewmodel-shell/src/index.ts` ViewNode union contains `| AlertNode`.
    - `grep -c 'public record AlertNode' viewmodel-shell-dotnet/ViewModels.cs` == 1.
    - .NET `AlertNode` record: `Tone Tone` (non-nullable), `ViewNode Message` (non-nullable); `Dismissible = false` positional with `WhenWritingDefault` attribute.
    - `grep -c 'JsonDerivedType(typeof(AlertNode), "alert")' viewmodel-shell-dotnet/ViewModels.cs` == 1.
    - `grep -c '\.vms-alert' viewmodel-shell/styles/default.css` >= 8 (base + 4 tones + __icon + __body + __title + __message + __actions + __dismiss).
    - Every tone rule uses `color-mix` (grep pattern: `color-mix.*var\(--vms-(error|warning|success|info)\)`) — 8 matches expected (4 tones × background + border).
    - TS + .NET builds green.
  </acceptance_criteria>
  <done>
    Wire type shipped byte-identical (tone + message required on both sides); CSS block installed; builds green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire AlertNode renderer + tone→icon default map in browser.ts</name>
  <files>viewmodel-shell/src/browser.ts</files>
  <read_first>
    - viewmodel-shell/src/browser.ts (read `case "icon"` and `private icon()` — find via grep `private icon(` — for the renderIconSvg call pattern; read `renderIconSvg` method — grep `renderIconSvg` — signature `(name, size, tone?, ariaLabel?)`; read `private modal()` around :3471-3479 for the close-X template; read the `renderNode` switch statement around :535-580)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §2a (switch arm) + §2f (full `private alert()` code block including ALERT_TONE_ICON static — verbatim to copy, lines 592-653 of PATTERNS.md)
  </read_first>
  <action>
    In `viewmodel-shell/src/browser.ts`:

    (a) Add `case "alert": return this.alert(n, parent, on);` to `renderNode` switch. Alphabetical placement.

    (b) Add the `ALERT_TONE_ICON` static class member (near the top of `BrowserAdapter` class, adjacent to other static tables — grep for `private static` to find existing statics):
    ```
    private static readonly ALERT_TONE_ICON: Record<
      "danger"|"warning"|"success"|"info", IconName
    > = {
      danger: "x-circle",
      warning: "alert-triangle",
      success: "check-circle",
      info: "info",
    };
    ```
    Verify all four icon names exist in the Phase 22 IconName closed union (grep `export const ICON_NAMES` or `export type IconName` for the shipped set) — if any name doesn't exist, STOP and surface to operator (planner assumed all four are shipped per CONTEXT §5).

    (c) Add `private alert(n: AlertNode, parent: HTMLElement, on: (a: ActionEvent) => void): void` per PATTERNS.md §2f code block:
      - Wrap `<div class="vms-alert vms-alert--{n.tone}">`.
      - Icon column: `<div class="vms-alert__icon">` containing `this.renderIconSvg(iconName, "md", undefined, undefined)`. Icon resolution: `const iconName = n.icon ?? BrowserAdapter.ALERT_TONE_ICON[n.tone];`.
      - Body: `<div class="vms-alert__body">` containing:
        - If `n.title`: `<div class="vms-alert__title">` with `this.node({ type: "text", value: n.title, style: "body", weight: "medium" }, title, on)`.
        - `<div class="vms-alert__message">`. If string message: `this.node({ type: "text", value: n.message, style: "muted" }, message, on)`. If ViewNode message: `this.node(n.message, message, on)`.
      - Actions column: only if `(n.actions && n.actions.length > 0) || n.dismissible === true`. `<div class="vms-alert__actions">` with:
        - Each action button via `this.button(btn, actionsEl, on)`.
        - If `n.dismissible === true`: `<button type="button" class="vms-alert__dismiss" aria-label="Dismiss">✕</button>` with click listener firing `on({ name: "dismiss" })`. **The action name is the FIXED string `"dismiss"` — NOT customizable per composite instance.** This is the deliberate deviation from `ModalNode.dismissAction`.

    Do NOT emit any other class names; DO NOT accept a `dismissAction` slot on the interface.

    Verify `check:core-globals` — the renderer uses `document.createElement` + event listeners only (all permitted in browser.ts).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'case "alert"' src/browser.ts &amp;&amp; grep -c 'private alert(' src/browser.ts &amp;&amp; grep -c 'ALERT_TONE_ICON' src/browser.ts &amp;&amp; grep -c '"x-circle"\|"alert-triangle"\|"check-circle"' src/browser.ts &amp;&amp; grep -c 'name: "dismiss"' src/browser.ts &amp;&amp; grep -c 'aria-label.*Dismiss' src/browser.ts &amp;&amp; npm run check:core-globals</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "alert"' viewmodel-shell/src/browser.ts` >= 1 (switch arm).
    - `grep -c 'private alert(' viewmodel-shell/src/browser.ts` == 1.
    - `grep -c 'ALERT_TONE_ICON' viewmodel-shell/src/browser.ts` >= 2 (static declaration + reference in the renderer).
    - Static map contains exact icon name strings: `"x-circle"`, `"alert-triangle"`, `"check-circle"`, `"info"`.
    - `grep -c 'n\.icon ?? BrowserAdapter\.ALERT_TONE_ICON\[n\.tone\]' viewmodel-shell/src/browser.ts` >= 1 (icon override precedence).
    - Renderer wraps string title in `TextNode { style: "body", weight: "medium" }`; string message in `TextNode { style: "muted" }`.
    - Dismissible action name is EXACTLY the string `"dismiss"` (grep pattern: `on\(\{ name: "dismiss" \}\)` — the literal fixed action).
    - `grep -c 'dismissAction' viewmodel-shell/src/browser.ts` did NOT increase (this task must NOT introduce a dismissAction slot; verify via git diff — new occurrences of `dismissAction` are ONLY in code comments referencing ModalNode).
    - `check:core-globals` green.
  </acceptance_criteria>
  <done>
    Renderer wired; ALERT_TONE_ICON baked; dismissible emits fixed "dismiss" action; core-globals green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire tree-validator walker arm for alert in server.ts + ViewModels.cs</name>
  <files>viewmodel-shell/src/server.ts, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell/src/server.ts (the ListRowNode arm added in Plan 24-01 + MessageNode arm added in Plan 24-02 as templates for this new arm; find `walkForSectionAction`)
    - viewmodel-shell-dotnet/ViewModels.cs (`Collect` + `WalkForSectionAction` methods around :2267-2415 and :2149-2264 — the ListRowNode + MessageNode arms added in prior plans as the direct templates)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §3 (server.ts walker code block for `case "alert"` — note the design point: `dismissible: true` emits `{name:"dismiss"}` client-side, so the SERVER tree carries no ActionEvent for it — walker records nothing for dismiss)
  </read_first>
  <action>
    In `viewmodel-shell/src/server.ts`:

    (a) Add `case "alert"` to `collectActions` per PATTERNS.md §3:
    ```
    case "alert": {
      const a = node as AlertNode;
      if (typeof a.message !== "string") collectActions(a.message, enclosingForm, out);
      for (const btn of a.actions ?? []) collectActions(btn, enclosingForm, out);
      // dismissible: true emits {name:"dismiss"} client-side — the server tree
      // does NOT carry an ActionEvent, so nothing to record here.
      return;
    }
    ```

    (b) Add passthrough `case "alert"` to `walkForSectionAction`.

    (c) Import `AlertNode` type at the top of server.ts.

    In `viewmodel-shell-dotnet/ViewModels.cs`:

    (d) Add .NET twin walker arm for `AlertNode` in `Collect` (descend into `Message` when ViewNode, each `Actions[i]`; do NOT record anything for `Dismissible`).

    (e) Add passthrough arm in `WalkForSectionAction`.

    No new tree-invariant rules for AlertNode (tone is enforced at the type level — TS union + .NET non-nullable enum).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'case "alert"' src/server.ts &amp;&amp; grep -c 'AlertNode' src/server.ts &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; grep -c 'AlertNode' ../viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; dotnet build ../viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "alert"' viewmodel-shell/src/server.ts` >= 2 (collectActions + walkForSectionAction).
    - Walker descends into `message` (typeof-guarded to skip strings), each `actions[i]`.
    - Walker does NOT record anything for `dismissible` (verify by inspection — the walker arm has NO `recordAction` call related to dismiss).
    - .NET twin walker (`Collect`) contains an `AlertNode` arm with the same descent pattern.
    - .NET twin `WalkForSectionAction` contains a passthrough arm.
    - TS + .NET builds green.
  </acceptance_criteria>
  <done>
    Tree-validator descent wired both backends; dismiss action-name is client-only (not walker-recorded); builds green.
  </done>
</task>

<task type="auto">
  <name>Task 4: vitest render + a11y + dismiss + AA hand-check + .NET serialization tests</name>
  <files>viewmodel-shell/test/alert.test.ts, viewmodel-shell-dotnet/Tests/AlertNodeSerializationTests.cs</files>
  <read_first>
    - viewmodel-shell/test/avatar-render.test.ts (byte-aligned DOM/a11y template)
    - viewmodel-shell/test/text-caption.test.ts (stylesheet-load + AA-header format)
    - viewmodel-shell-dotnet/Tests/AvatarNodeSerializationTests.cs (byte-aligned .NET template)
    - viewmodel-shell/styles/themes/*.css (13 themes for AA hand-check)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md §9 (AA-contrast checklist — AlertNode = 208 pair-checks)
  </read_first>
  <action>
    Create `viewmodel-shell/test/alert.test.ts`:

    (a) AA-contrast hand-check header: 208 pair-checks in a tabular format. Structure the header as: for each `tone` in `[danger, warning, success, info]` × each `theme` in `[default, dark-*, light-*, ...]` (13 themes) × each `element` in `[title-text, message-text, icon-glyph, action-button-border]`. Target ratios: 4.5:1 for text (WCAG AA-normal); 3:1 for icon glyph + action border (WCAG UI-state). Where any pair drops below, document deepening mitigation (raise the color-mix percentage in `default.css` or use KNOCKOUT).

    (b) Stylesheet-load setup: reuse the pattern.

    (c) Tests in `describe("AlertNode (COMP-07)", () => { ... })`:
      - For each tone `["danger", "warning", "success", "info"]`:
        - `it("tone={tone}: emits .vms-alert.vms-alert--{tone}", ...)`.
        - `it("tone={tone}: uses default icon {default-name} when n.icon omitted", ...)` — assert the icon SVG's data-icon-name (or class-name — grep `IconNode`/`renderIconSvg` in browser.ts to find how icons expose their name in the DOM) matches the tone default (`x-circle`/`alert-triangle`/`check-circle`/`info`).
      - `it("n.icon override wins over tone default", ...)` — set `icon: "user"` on a `tone: "danger"` alert; assert the emitted icon SVG identifies as `user`, not `x-circle`.
      - `it("wraps string title in TextNode with style:body weight:medium", ...)`.
      - `it("wraps string message in TextNode with style:muted", ...)`.
      - `it("passes ViewNode message through without wrapping", ...)`.
      - `it("title absent hides .vms-alert__title element", ...)`.
      - `it("actions[] renders each button", ...)`.
      - `it("dismissible:true renders .vms-alert__dismiss with ✕ and aria-label=Dismiss", ...)`.
      - `it("dismissible:false/omitted hides .vms-alert__dismiss entirely", ...)`.
      - `it("dismissible click dispatches EXACTLY {name: 'dismiss'}", ...)` — spy on `on`, click the dismiss button, assert the dispatched ActionEvent is deeply equal to `{ name: "dismiss" }` (no other fields).
      - `it("dismissible action name is NOT customizable", ...)` — code-search test: `readFileSync("src/browser.ts")` inside the `alert()` function region contains the literal string `name: "dismiss"` (verifying the fixed constant, not a `n.dismissAction` reference).
      - `it("actions[] + dismissible:true both render — dismiss appears after actions", ...)`.
      - Mutation-test proof block.

    Create `viewmodel-shell-dotnet/Tests/AlertNodeSerializationTests.cs`:

    (d) Ensure `global using Xunit;` and framework reference in Tests project.

    (e) Tests following AvatarNodeSerializationTests.cs structure:
      - `Type_SerializesAsAlert()`.
      - `Tone_Required_AlwaysPresent()` — parametrize over `[Tone.Danger, Tone.Warning, Tone.Success, Tone.Info]`; assert `"tone":"danger"` etc.
      - `Message_Required_SerializesPolymorphically()` — pass a `TextNode`; assert nested `"type":"text"`.
      - `BareRequiredNode_MinimalShape()` — `new AlertNode(Tone: Tone.Danger, Message: new TextNode("X"))` → JSON contains ONLY `type`, `tone`, `message` (every optional ABSENT).
      - `Title_OmittedIsAbsent()`, `Icon_OmittedIsAbsent()`, `Actions_OmittedIsAbsent()`.
      - `Dismissible_False_SerializedAsAbsent()` — proves WhenWritingDefault posture: `new AlertNode(Tone: Tone.Info, Message: ..., Dismissible: false)` JSON does NOT contain `"dismissible"`.
      - `Dismissible_True_SerializedAsTrue()`.
      - `Icon_SerializesAsKebab()` — `Icon: IconName.XCircle` → `"icon":"x-circle"` (validates KebabEnum on IconName round-trip).
      - `AllFieldsSet_AllPresent()`.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npx vitest run test/alert.test.ts &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --filter 'AlertNodeSerializationTests' --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/test/alert.test.ts` exists with >= 15 `it()` blocks.
    - AA-contrast header enumerates 208 pair-checks (planner-chosen format — tabular row-per-tone-theme-element or aggregated per-theme with 4 columns; either shape acceptable as long as 4 × 13 × 4 pair-checks are documented).
    - Test for `dismissible click dispatches EXACTLY {name: "dismiss"}` present.
    - Test for tone-default-icon-map coverage: 4 tones × 4 default icon assertions (16 tests OR 4 parametrized tests).
    - Test for n.icon override wins over tone default.
    - `viewmodel-shell-dotnet/Tests/AlertNodeSerializationTests.cs` exists with >= 9 `[Fact]` methods including `Tone_Required`, `Dismissible_False_SerializedAsAbsent`, `Dismissible_True_SerializedAsTrue`.
    - `cd viewmodel-shell && npx vitest run test/alert.test.ts` — green.
    - `~/.dotnet/dotnet test viewmodel-shell-dotnet/Tests --filter 'AlertNodeSerializationTests'` — green.
    - Mutation-verified: swap `"x-circle"` → `"info"` in the `ALERT_TONE_ICON` static; run alert.test.ts; the tone=danger default icon test fails; revert.
  </acceptance_criteria>
  <done>
    Tests exist and pass; mutation-verified; AA hand-check recorded for 208 pairs; .NET wire tests including Tone-required + Dismissible-WhenWritingDefault + IconName kebab roundtrip.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client action dispatch | `dismissible:true` introduces a new client-emitted action with the RESERVED fixed name `"dismiss"`. Apps that already handle a `"dismiss"` action for a different purpose will observe conflated dispatches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Spoofing | `dismissible:true` dispatches `{name:"dismiss"}` — an app with its own `dismiss` action handler for a different purpose could conflate the two | mitigate | The AlertNode interface's TSDoc + CHANGELOG entry (Plan 24-09) BOTH document `"dismiss"` as a RESERVED reserved action name when `dismissible:true` is used. Apps needing a specific name compose their own dismiss button in `actions[]` and set `dismissible:false`. The design deliberately trades composite ergonomics for a fixed convention; the escape hatch (custom action in `actions[]`) exists for consumers who need distinct semantics. |
| T-24-03-02 | (none) | `AlertNode` rendering-only structured node | accept | No new input parsing, no external data flow, no auth interaction. `actions[]` are `ButtonNode[]` (same shape as every other action-bearing surface). |
| T-24-03-03 | Denial of Service | Deeply nested ViewNode in `message` slot | accept | Same recursion depth ceiling as every other typed-slot composite. |
| T-24-03-04 | Information Disclosure | Tone-driven color could leak semantic info (e.g. an alert `tone:"danger"` visually signals "sensitive") | accept | This is a rendering INTENT — the whole point of the tone axis. Not a leak; not user-controllable at runtime. |
</threat_model>

<verification>
Task-scoped verifications above. Full green-tree gate deferred to Plan 24-10.
</verification>

<success_criteria>
- `AlertNode` wire type shipped both backends byte-identical; `tone` REQUIRED on both sides (TS union + .NET non-nullable enum); `message` REQUIRED.
- Tone→icon default mapping baked in `ALERT_TONE_ICON` static; `n.icon` overrides.
- `dismissible:true` dispatches the RESERVED fixed action name `"dismiss"` (documented in TSDoc + CHANGELOG).
- Tree-validators descend into `message` (when ViewNode) + `actions[i]`; walker records nothing for the client-only dismiss.
- vitest coverage: >= 15 tests including all 4 tones × default-icon check, override precedence, dismissible dispatch exactness, mutation-verified.
- .NET serialization tests: Tone-required + Dismissible-WhenWritingDefault (false-ABSENT/true-present) + IconName KebabEnum + polymorphic Message.
- AA hand-check recorded for 208 pairs (4 tones × 4 elements × 13 themes).
- Wire protocol token stays `viewmodel-shell/1.0`.
</success_criteria>

<output>
Create `.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-03-SUMMARY.md` recording: artifacts touched, tone→icon default map (verify all 4 icon names exist in the Phase 22 shipped set), dismiss reserved-name convention, mutation-test evidence, and AA hand-check verdicts for 208 pairs.
</output>
