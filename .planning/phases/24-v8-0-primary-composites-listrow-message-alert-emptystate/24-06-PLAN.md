---
phase: 24-v8-0-primary-composites-listrow-message-alert-emptystate
plan: 06
type: execute
wave: 5
depends_on: ["24-04"]
files_modified:
  - demo/Showcase/frontend/src/main.ts
autonomous: true
requirements: [COMP-05, COMP-05a, COMP-06, COMP-06a, COMP-07, COMP-08]

must_haves:
  truths:
    - "Showcase demo (`demo/Showcase/frontend/src/main.ts`) grows a **v8.0.0 Primary Composites** section AFTER the existing v8.0.0 Foundations section landed in Phase 23 plan 23-07."
    - "The new section demonstrates ALL 4 primaries in situ: ListRowNode (inside a ListNode variant:'rows' container with 2-3 example rows), MessageNode + MessageListNode (a small conversation with different roles, followTail:true), AlertNode (one per tone × 4 including one dismissible:true), EmptyStateNode with icon + title + description + action."
    - "Each composite section has a preceding `TextNode { style: \"subheading\" }` naming the composite + a `TextNode { style: \"muted\" }` blurb explaining what shipped."
    - "Fleet-adoption discipline per AGENTS.md UseVmsShellStaticFiles 6.7.0 banked lesson: composites ship WITH demo adoption in same batch. This plan closes that loop for Phase 24."
    - "Showcase demo type-checks under strict tsconfig (`npm run check:demo-types` green) — the demo consumes the strictest form of the wire types, so any drift between the interface and the demo's construction call fails the build."
    - "Every demonstrated ListRowNode.action / MessageNode-actions.action / AlertNode dismiss / EmptyStateNode.action name is UNIQUE across the tree (banked from Nelly's action-name-uniqueness lesson) — planner-picked names include e.g. `showcase-open-user-ada`, `showcase-message-ok`, `showcase-empty-state-cta`, and the framework-baked `dismiss` for the dismissible alert."
  artifacts:
    - path: "demo/Showcase/frontend/src/main.ts"
      provides: "v8.0.0 Primary Composites section demonstrating all 4 composites in situ."
      contains: "Primary Composites"
  key_links:
    - from: "demo/Showcase/frontend/src/main.ts"
      to: "viewmodel-shell/src/index.ts"
      via: "consumes the shipped ListRowNode/MessageNode/MessageListNode/AlertNode/EmptyStateNode interfaces (strict tsconfig type-checks the composition)"
      pattern: "list-row\\|message\\|alert\\|empty-state"
---

<objective>
Extend the Showcase demo (`demo/Showcase/frontend/src/main.ts`) with a **v8.0.0 Primary Composites** section immediately after the shipped Foundations section (Phase 23 plan 23-07). Each of the 4 primaries demonstrated in situ (fleet-adoption discipline per AGENTS.md — composites ship WITH demo adoption in same batch). Passes `check:demo-types` under strict tsconfig.

Purpose: Ashley + external agents visually verify each composite via the tailnet-served Showcase. The tasting-page approved mockups are the visual truth; the Showcase adoption proves the wire types + renderer emit that truth end-to-end. Fleet-adoption discipline prevents the composites from shipping unused (banked from UseVmsShellStaticFiles 6.7.0).

Output: Showcase updated with 4 primaries in situ; strict tsconfig green; action names unique across the tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v8.0.0 Primary Composites section to Showcase main.ts</name>
  <files>demo/Showcase/frontend/src/main.ts</files>
  <read_first>
    - demo/Showcase/frontend/src/main.ts (read the full buildVm; find the v8.0.0 Foundations section around :248-320 per PATTERNS.md §10 — the direct-neighbor template; the insertion point for the new section is right AFTER Foundations)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §10 (Showcase adoption — the exact code block for the Primary Composites section is documented there, lines 1387-1442)
    - viewmodel-shell/src/index.ts (verify each composite's TSDoc + slot shape — the Showcase must consume the shipped interface exactly)
  </read_first>
  <action>
    In `demo/Showcase/frontend/src/main.ts`:

    Insert a new SectionNode entry into the page tree IMMEDIATELY AFTER the v8.0.0 Foundations section. Copy the code block from PATTERNS.md §10 (lines 1387-1442) verbatim; adapt any type imports as needed (grep the file's top-of-file imports to ensure ListRowNode/MessageNode/MessageListNode/AlertNode/EmptyStateNode types are imported — they're all in the ViewNode union so import via ViewNode may suffice).

    Structure per PATTERNS.md §10:

    ```typescript
    { type: "section", heading: "v8.0.0 Primary Composites", children: [
      { type: "text", value: "Four high-frequency composites — the shapes VMS consumers reach for first. Each ships as a typed-slot recipe: the framework owns layout / typography / spacing / a11y, the app hands it content via named slots.", style: "muted" },

      // ── COMP-05 ListRowNode + ListNode(variant:"rows") ───
      { type: "text", value: "ListRowNode — dense list row with 3-tier typography", style: "subheading" },
      { type: "list", variant: "rows", children: [
        { type: "list-row",
          leading: { type: "avatar", initials: "AL", tone: "success" },
          primary: "Ada Lovelace",
          secondary: "ada@analytical.example",
          meta: ["Last active 2h ago", "role: admin"],
          trailing: { type: "badge", label: "ACTIVE", tone: "success" },
          tone: "success",
          action: { name: "showcase-open-user-ada" },
        },
        { type: "list-row",
          leading: { type: "avatar", initials: "GH", tone: "info" },
          primary: "Grace Hopper",
          secondary: "grace@compilers.example",
          meta: ["Last active 6h ago", "role: engineer"],
          state: "active",
          action: { name: "showcase-open-user-grace" },
        },
        { type: "list-row",
          primary: "Payment declined",
          secondary: "Order #42 · card ending 4321",
          meta: ["3m ago", "issuer decline"],
          tone: "danger",
          state: "high",
        },
      ]},

      // ── COMP-06 MessageNode + MessageListNode ───────────
      { type: "text", value: "MessageNode — chat / comment thread with follow-tail", style: "subheading" },
      { type: "message-list", followTail: true, children: [
        { type: "message", author: "Ada Lovelace", timestamp: "2:14 PM",
          content: "Can we ship v8 this week?",
          avatar: { type: "avatar", initials: "AL", tone: "success" },
          role: "user",
        },
        { type: "message", author: "VMS", timestamp: "2:15 PM",
          content: "Green tree. Ready to publish.",
          avatar: { type: "avatar", icon: "sparkles", tone: "info" },
          role: "assistant",
          actions: [{ type: "button", label: "OK", action: { name: "showcase-message-ok" } }],
        },
      ]},

      // ── COMP-07 AlertNode — one per tone ────────────────
      { type: "text", value: "AlertNode — every tone, with the default tone→icon mapping", style: "subheading" },
      { type: "alert", tone: "danger",  title: "Payment declined", message: "Your card was refused." },
      { type: "alert", tone: "warning", title: "Storage almost full", message: "You've used 92% of your quota.", dismissible: true },
      { type: "alert", tone: "success", title: "Refund processed", message: "Refund of $124 issued." },
      { type: "alert", tone: "info",    title: "New version",     message: "v8.0.0 is available." },

      // ── COMP-08 EmptyStateNode ──────────────────────────
      { type: "text", value: "EmptyStateNode — the friendly \"nothing here\" recipe", style: "subheading" },
      { type: "empty-state",
        icon: "inbox",
        title: "No orders yet",
        description: "Once customers place orders they'll show up here.",
        action: { type: "button", label: "Learn more", action: { name: "showcase-empty-state-cta" } },
      },
    ]},
    ```

    Adapt as needed:
    - If `type: "badge"` isn't shipped as a leaf node in this codebase, replace the `trailing` slot with a shipped alternative (e.g. `{ type: "text", value: "ACTIVE", style: "caption", tone: "success" }` — planner picks based on what actually ships; grep `viewmodel-shell/src/index.ts` for `BadgeNode` / `Badge` to confirm).
    - If any Phase 22 icon name in this block doesn't exist (`sparkles`, `inbox`), swap for one that does — grep `viewmodel-shell/src/index.ts` for the `IconName` union to see the shipped set. `inbox` is expected to exist per CONTEXT §6; `sparkles` per §5 tasting mockups; but verify.
    - Every action name must be UNIQUE across the entire Showcase tree (the walker enforces this cross-tree at framework level per Nelly's TODO discovery). Planner-picked names above are pre-namespaced with `showcase-` — verify no existing Showcase action collides. If any collides, rename with a more-specific prefix (e.g. `showcase-primaries-...`).

    Verify Showcase demo type-checks: `cd viewmodel-shell && npm run check:demo-types` — the demo strict tsconfig will fail if any slot access is wrong (a `list-row` without `primary`, an `alert` without `tone`, etc).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run check:demo-types &amp;&amp; grep -c 'v8\.0\.0 Primary Composites\|Primary Composites' ../demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'type: "list-row"' ../demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'type: "message"' ../demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'type: "message-list"' ../demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'type: "alert"' ../demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'type: "empty-state"' ../demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'followTail: true' ../demo/Showcase/frontend/src/main.ts &amp;&amp; grep -c 'showcase-' ../demo/Showcase/frontend/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Primary Composites' demo/Showcase/frontend/src/main.ts` >= 1 (section heading).
    - `grep -c 'type: "list-row"' demo/Showcase/frontend/src/main.ts` >= 2 (at least 2 list-rows in the demo).
    - `grep -c 'type: "message-list"' demo/Showcase/frontend/src/main.ts` >= 1 with `followTail: true`.
    - `grep -c 'type: "message"' demo/Showcase/frontend/src/main.ts` >= 2 (a user + an assistant message).
    - `grep -c 'type: "alert"' demo/Showcase/frontend/src/main.ts` >= 4 (one per tone).
    - `grep -c 'dismissible: true' demo/Showcase/frontend/src/main.ts` >= 1 (dismissible variant demonstrated).
    - `grep -c 'type: "empty-state"' demo/Showcase/frontend/src/main.ts` >= 1 with `icon:`, `title:`, `description:` present.
    - Action names are unique — `grep -o 'name: "showcase[^"]*"' demo/Showcase/frontend/src/main.ts | sort -u | wc -l` matches the total count (unique = total).
    - `cd viewmodel-shell && npm run check:demo-types` — GREEN (proves the composite consumption matches the shipped wire types under strict tsconfig).
  </acceptance_criteria>
  <done>
    Showcase adopts all 4 primaries in situ; strict tsconfig green; action names unique; fleet-adoption discipline satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Demo-only plan. No framework code change; no wire change. The demo consumes shipped wire types under strict tsconfig — the type-check is the sole safety gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06-01 | (none) | Showcase demo section addition | accept | No new input parsing, no auth interaction. The demo is a static tree of ViewNodes rendered client-side; users can click buttons that dispatch to demo actions, but the demo's controller handles them locally (no server round trip in the strictest Showcase build — or if there is a server, it's a demo-only endpoint). |
| T-24-06-02 | Tampering | Action name collisions with other Showcase sections | mitigate | Task 1 acceptance criteria include a unique-count grep for `showcase-*` action names. The framework's action-name-uniqueness walker (banked from Nelly's TODO discovery) will fail the build if a duplicate slips through — belt-and-braces at both the demo and framework level. |
</threat_model>

<verification>
Task-scoped verifications above. `check:demo-types` is the strict tsconfig proof. Ashley's visual sign-off happens in Plan 24-10 (final checkpoint).
</verification>

<success_criteria>
- Showcase demo `main.ts` grows a Primary Composites section with all 4 primaries in situ.
- Each composite has a labeled subheading + muted blurb.
- `check:demo-types` GREEN (strict tsconfig proves wire-type consumption is byte-correct).
- All action names unique across the tree.
- Fleet-adoption discipline satisfied.
</success_criteria>

<output>
Create `.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-06-SUMMARY.md` recording: main.ts diff summary, action-name uniqueness verification, strict tsconfig verdict, and any icon-name-not-shipped substitution decisions.
</output>
