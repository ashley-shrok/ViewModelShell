---
phase: 24-v8-0-primary-composites-listrow-message-alert-emptystate
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/design/composite-nodes-layer.md
  - AGENTS.md
autonomous: true
requirements: [COMP-05, COMP-05a, COMP-06, COMP-06a, COMP-07, COMP-08]

must_haves:
  truths:
    - "`.planning/design/composite-nodes-layer.md` grows a **Shipped Recipe Inventory** table (or fills the existing §4c placeholder) with 4 primary rows: ListRowNode, MessageNode+MessageListNode, AlertNode, EmptyStateNode (v8.0-renamed)."
    - "Each inventory row records: composite name, slot summary, phase (24 with COMP-XX), wire type discriminator, .NET record name, Phase 23 foundations consumed (COMP-01 caption, COMP-02 weight, COMP-04 AvatarNode), + any key mechanism reuse (MessageListNode.followTail REUSES SectionNode.followTail; EmptyStateNode is a BREAKING rename from v7.x, not a new node)."
    - "`AGENTS.md` \"Currently shipped recipes\" placeholder at :744 is REPLACED with a populated list. Content mirrors the design-doc inventory (or narrows to the essentials); the same 4 primaries + the followTail reuse note + the EmptyStateNode BREAKING rename note appear."
    - "Both documents remain the doctrine-of-record for the Route B composite-nodes layer — they describe SHIPPED recipes only (per AGENTS.md convention: docs should not race ahead of what actually landed)."
    - "The 4 Phase 25 composites (UserRow, DetailRow, Timeline, SettingRow, Chip+ChipList) are noted as \"TBD in Phase 25\" in both docs — no premature detail."
    - "The Phase 26 release ritual (aligned v8.0.0 npm + NuGet publish; tailnet verification page) is noted as \"TBD in Phase 26\" in both docs."
  artifacts:
    - path: ".planning/design/composite-nodes-layer.md"
      provides: "Shipped Recipe Inventory table filled in for 4 primaries."
      contains: "ListRowNode"
    - path: "AGENTS.md"
      provides: "Currently shipped recipes list populated with 4 primaries + reuse + rename notes."
      contains: "MessageListNode"
  key_links:
    - from: ".planning/design/composite-nodes-layer.md"
      to: "AGENTS.md"
      via: "same 4-composite inventory referenced across both docs"
      pattern: "ListRowNode\\|MessageNode\\|AlertNode\\|EmptyStateNode"
---

<objective>
Fill the Shipped Recipe Inventory table in `.planning/design/composite-nodes-layer.md` (design-of-record) and replace the placeholder in `AGENTS.md` "Currently shipped recipes" section — both with the 4 primaries landed in Plans 24-01..04. Both docs preserve the append-only, "docs describe SHIPPED recipes only" convention (per AGENTS.md rule against docs racing ahead of code).

Purpose: Both docs are the operator's mental model of the composite-nodes layer's growth. Phase 23 landed the frame with empty inventory + placeholder in AGENTS.md; Phase 24 fills these in with the 4 primaries. Future agents reading either doc get an accurate picture of what shipped without needing to spelunk in the code.

Output: Both docs updated in same commit run; grep-verifiable content for each of the 4 primaries; the MessageListNode.followTail-REUSES-SectionNode.followTail note called out prominently; the EmptyStateNode BREAKING RENAME flagged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grow the Shipped Recipe Inventory table in .planning/design/composite-nodes-layer.md</name>
  <files>.planning/design/composite-nodes-layer.md</files>
  <read_first>
    - .planning/design/composite-nodes-layer.md (full read; find the existing §4c "Composites (Phase 24 — primary)" section OR wherever the Phase-24 inventory placeholder lives; the doc landed in Phase 23 plan 23-05 with an empty inventory)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md §12 (design-doc + AGENTS.md update mandate)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §11 (design-doc table content — the exact table rows to insert are documented there, lines 1461-1466)
  </read_first>
  <action>
    In `.planning/design/composite-nodes-layer.md`:

    (a) Locate the "Shipped recipe inventory" section (or §4c "Composites (Phase 24 — primary)" — whichever placeholder Phase 23 plan 23-05 left). If no explicit "Shipped Recipe Inventory" heading exists, add one (recommend near the top of §4 or as a new §5 after the composite descriptions — planner picks placement based on the doc's actual structure at read-time).

    (b) Populate the inventory as a markdown table per PATTERNS.md §11 (lines 1461-1466). Columns: `Composite | Slot summary | Phase | Wire type | .NET record | Consumes`. Row content:

    ```
    | Composite | Slot summary | Phase | Wire type | .NET record | Consumes |
    |---|---|---|---|---|---|
    | `ListRowNode` | leading, primary, secondary, meta[], trailing, tone, state, action | 24 (COMP-05) | `"list-row"` | `ListRowNode` | TextNode caption + weight (COMP-01/02); TableRow.action a11y pattern |
    | `ListNode.variant:"rows"` | (extension) rows container for ListRowNode-only children | 24 (COMP-05a) | `"list"` (existing) | `ListNode.Variant` | Existing ListNode + new closed-union enum |
    | `MessageNode` | avatar, author, timestamp, content, role, actions[] | 24 (COMP-06) | `"message"` | `MessageNode` | AvatarNode (COMP-04); TextNode caption (COMP-01); TextNode weight (COMP-02) |
    | `MessageListNode` | children (MessageNode[]), followTail | 24 (COMP-06a) | `"message-list"` | `MessageListNode` | SectionNode.followTail mechanism REUSED verbatim (browser.ts:227-372) — one-line addition `el.dataset.followTail = ""` |
    | `AlertNode` | tone (required), title, message, icon, actions[], dismissible | 24 (COMP-07) | `"alert"` | `AlertNode` | IconName (v7.0); TextNode weight (COMP-02); tone→icon default map (danger→x-circle, warning→alert-triangle, success→check-circle, info→info) BAKED into browser.ts:ALERT_TONE_ICON; `dismissible:true` dispatches RESERVED `{name:"dismiss"}` |
    | `EmptyStateNode` (v8.0 rename) | icon, title (was heading), description (was message), action | 24 (COMP-08) | `"empty-state"` (unchanged) | `EmptyStateNode` | IconName (v7.0); **BREAKING wire rename** from v7.x `heading`/`message` — see CHANGELOG.md v8.0.0 + MIGRATION.md |
    | `UserRowNode` | (Phase 25) | 25 (COMP-09) | TBD | TBD | TBD |
    | `DetailRowNode` | (Phase 25) | 25 (COMP-10) | TBD | TBD | TBD |
    | `TimelineEntryNode` | (Phase 25) | 25 | TBD | TBD | TBD |
    | `SettingRowNode` | (Phase 25) | 25 | TBD | TBD | TBD |
    | `ChipListNode` + `ChipNode` | (Phase 25) | 25 | TBD | TBD | TBD |
    ```

    (c) Above the table, add a one-paragraph header: "This inventory tracks recipes that have landed on `main` and shipped byte-identical across both backends. Rows for phases that haven't landed yet are marked TBD; they're populated as `/gsd:plan-phase 25` and `/gsd:plan-phase 26` land."

    (d) If the doc's §4c "Composites (Phase 24 — primary)" free-form section repeats content that the table now captures, leave the free-form section (it holds design rationale + slot-schema justifications) — the table augments it, doesn't replace it. If any free-form entry in §4c contradicts what actually shipped (e.g. the doc mentioned a `dismissAction: ActionEvent` slot on AlertNode when the final decision was `dismissible: boolean`), UPDATE the free-form to match reality.

    (e) If the doc references EmptyStateNode's old field names (`heading`/`message`) anywhere, update to the new names (`title`/`description`); flag the rename as BREAKING v8.0.
  </action>
  <verify>
    <automated>test -f .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'ListRowNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'MessageNode\|MessageListNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'AlertNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'EmptyStateNode' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'Shipped recipe inventory\|Shipped Recipe Inventory' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'REUSE\|Reuses\|reuses' .planning/design/composite-nodes-layer.md &amp;&amp; grep -c 'BREAKING\|breaking wire' .planning/design/composite-nodes-layer.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Shipped recipe inventory\|Shipped Recipe Inventory' .planning/design/composite-nodes-layer.md` >= 1 (section heading exists).
    - `grep -c 'ListRowNode' .planning/design/composite-nodes-layer.md` >= 2 (row in table + probably referenced elsewhere).
    - `grep -c 'MessageListNode' .planning/design/composite-nodes-layer.md` >= 1.
    - `grep -c 'followTail\|data-follow-tail' .planning/design/composite-nodes-layer.md` >= 1 (the reuse callout).
    - `grep -c 'BREAKING\|breaking wire' .planning/design/composite-nodes-layer.md` >= 1 (EmptyStateNode rename flag).
    - Table has rows for all 4 primaries: `list-row`, `message`, `message-list`, `alert`, `empty-state` wire types.
    - Phase 25 composites listed as TBD (not populated).
    - No mention of old EmptyStateNode field names `heading`/`message` in prose (grep-scan; where they appear, verify context — historical references OK if flagged as "v7.x").
    - `ALERT_TONE_ICON` OR the tone→icon default map is referenced in the AlertNode row's Consumes column.
  </acceptance_criteria>
  <done>
    Design doc inventory table populated with 4 primaries; reuse and rename callouts prominent; Phase 25/26 TBD placeholders preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace AGENTS.md "Currently shipped recipes" placeholder with populated inventory</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md around :744 (grep `Currently shipped recipes` OR `To be populated as Phases 24-26` — the placeholder line the Phase 23 plan 23-06 left)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §12 (AGENTS.md content — lines 1484-1493 have the exact suggested replacement text)
  </read_first>
  <action>
    In `AGENTS.md`:

    (a) Locate the placeholder near :744 (grep for `Currently shipped recipes` or `To be populated`). Read the surrounding paragraph — the Phase 23 plan 23-06 explicitly said the initial version omits the inventory table so it doesn't drift ahead of what actually ships.

    (b) REPLACE the placeholder with the populated inventory per PATTERNS.md §12:

    ```
    **Currently shipped recipes.** *(Updated as Phase 24-26 land.)*

    Phase 24 (v8.0.0, primary composites):
    - `ListRowNode` (COMP-05) + `ListNode.variant:"rows"` (COMP-05a) — dense list row with 3-tier typography (primary body-weight-medium, secondary muted, meta caption); single-surface `rows` container with per-row dividers. Whole-row `action?` follows TableRow.action shape (role="button", Enter/Space, aria-label from flattened text, stopPropagation on interactive descendants).
    - `MessageNode` (COMP-06) + `MessageListNode` (COMP-06a) — chat/comment message with follow-tail transcript. **MessageListNode.followTail REUSES SectionNode.followTail's shipped mechanism** — the pre-render snapshot / post-render restore at `browser.ts:227-246 + 362-372` walks EVERY `[data-follow-tail]` element, so a new consumer just sets `el.dataset.followTail = ""` (no new adapter code, ever). Same posture will apply to any future "growing feed" composite (activity feed, live log, notification stream).
    - `AlertNode` (COMP-07) — prominent status message with tone-appropriate icon (default map baked in browser: `danger`→`x-circle`, `warning`→`alert-triangle`, `success`→`check-circle`, `info`→`info`; overridable via `icon?`). `dismissible:true` dispatches RESERVED `{name:"dismiss"}` — apps needing a distinct name compose their own dismiss button in `actions[]`.
    - `EmptyStateNode` (COMP-08) — friendly "nothing here" recipe. **v8.0 WIRE BREAKING:** field rename `heading→title`, `message→description`; new `icon?` slot; .NET `Tooltip` field removed for byte-alignment. See `MIGRATION.md`.

    Phase 25 composites (UserRow, DetailRow, Timeline, SettingRow, Chip+ChipList): TBD in `/gsd:plan-phase 25`.
    Phase 26 release ritual: TBD (aligned v8.0.0 npm + NuGet publish; comprehensive tailnet verification page across all 10 composites; see `.planning/design/composite-nodes-layer.md` §5).
    ```

    (c) The rest of the AGENTS.md "Route B composite-nodes layer" governance section (earn-a-composite rule, typed-slots pattern, multi-phase milestone plan) is UNCHANGED — only the "Currently shipped recipes" placeholder gets replaced.
  </action>
  <verify>
    <automated>grep -c 'Currently shipped recipes\|currently shipped recipes' AGENTS.md &amp;&amp; grep -c 'To be populated' AGENTS.md &amp;&amp; grep -c 'ListRowNode\|MessageNode\|AlertNode\|EmptyStateNode' AGENTS.md &amp;&amp; grep -c 'REUSES SectionNode.followTail\|reuses SectionNode.followTail' AGENTS.md &amp;&amp; grep -c 'BREAKING\|WIRE BREAKING' AGENTS.md &amp;&amp; grep -c 'data-follow-tail' AGENTS.md &amp;&amp; grep -c 'dismiss' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Currently shipped recipes\|currently shipped recipes' AGENTS.md` >= 1 (heading preserved).
    - `grep -c 'To be populated' AGENTS.md` == 0 (placeholder deleted — verify the old text is gone).
    - All 4 primaries named in the section: `grep -c 'ListRowNode' AGENTS.md` >= 1, same for MessageNode, MessageListNode, AlertNode, EmptyStateNode.
    - MessageListNode.followTail reuse callout present: `grep -c 'REUSES SectionNode\.followTail\|reuses SectionNode\.followTail\|data-follow-tail' AGENTS.md` >= 1.
    - EmptyStateNode BREAKING flag present: `grep -c 'v8\.0 WIRE BREAKING\|BREAKING' AGENTS.md` >= 1 (the section MUST call out the rename since operators reading AGENTS.md need to know).
    - AlertNode tone→icon default map named: `grep -c 'x-circle\|alert-triangle\|check-circle' AGENTS.md` >= 1.
    - AlertNode reserved-dismiss-action noted: `grep -c 'dismiss' AGENTS.md` >= 1.
    - Phase 25 + Phase 26 TBDs preserved.
    - The rest of the "Route B composite-nodes layer" governance section unchanged (verify by git diff — only the placeholder region changed).
  </acceptance_criteria>
  <done>
    AGENTS.md "Currently shipped recipes" section populated with same 4 primaries; reuse + rename callouts present; the rest of Route B governance section preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan. No code, no wire change, no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | (none) | design doc + AGENTS.md inventory update | accept | No new input parsing, no external data flow, no auth interaction, no new URL. Documentation update; zero runtime surface change. |
| T-24-05-02 | Information Disclosure | Doc could drift ahead of what ships — future agents rely on it as truth | mitigate | Task 1 acceptance criteria explicitly require Phase 25 composites listed as TBD (not populated). Task 2 mirrors that. AGENTS.md "docs shouldn't race ahead of code" convention (per plan 23-06) enforced by design. |
</threat_model>

<verification>
Task-scoped verifications above. Both files pure markdown; no build/test to run beyond grep checks.
</verification>

<success_criteria>
- Design doc `Shipped Recipe Inventory` table populated with 4 primaries + Phase 25 TBDs.
- AGENTS.md "Currently shipped recipes" placeholder replaced with populated list.
- MessageListNode.followTail-REUSES-SectionNode.followTail callout prominent in both docs.
- EmptyStateNode BREAKING RENAME flagged in both docs.
- Phase 25 + Phase 26 TBDs preserved (docs don't race ahead of code).
- Both files remain the doctrine-of-record for the Route B composite-nodes layer.
</success_criteria>

<output>
Create `.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-05-SUMMARY.md` recording: both files updated, grep-verified inventory content for each of the 4 primaries, and the reuse + rename callouts.
</output>
