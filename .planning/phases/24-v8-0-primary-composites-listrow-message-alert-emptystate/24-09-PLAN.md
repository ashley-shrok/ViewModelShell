---
phase: 24-v8-0-primary-composites-listrow-message-alert-emptystate
plan: 09
type: execute
wave: 6
depends_on: ["24-01", "24-02", "24-03", "24-04", "24-05", "24-06", "24-07", "24-08"]
files_modified: []
autonomous: false
requirements: [COMP-05, COMP-05a, COMP-06, COMP-06a, COMP-07, COMP-08]

must_haves:
  truths:
    - "The FULL green-tree gate runs GREEN: `npm run build && check:test-types && check:core-globals && check:aa-contrast && check:no-demo-style && check:demo-types && npx vitest run` in `viewmodel-shell/`, PLUS `dotnet test viewmodel-shell-dotnet/Tests`, PLUS every `demo/**/*.Tests.csproj`, PLUS `bun run parity/run.ts`."
    - "The AA-contrast hand-check is complete and documented across all 4 composite test files: ListRowNode hover-tint (39 pair-checks), MessageNode assistant surface (13 pair-checks), AlertNode (208 pair-checks — 4 tones × 4 elements × 13 themes), EmptyStateNode tinted-circle icon backdrop (13 pair-checks). Total: 273 pair-checks documented."
    - "Ashley (operator) has done a visual smoke on the Showcase Primary Composites section served over the tailnet: ListRowNode 3-tier typography visibly distinct, MessageNode assistant vs user surfaces visibly different (tinted-info vs neutral), AlertNode all 4 tones + tone→icon defaults render correctly, EmptyStateNode tinted-circle icon backdrop looks right, dismissible AlertNode close-X dismisses on click (fires a dispatch)."
    - "The MessageListNode.followTail REUSE is verified end-to-end by (a) the vitest code-search test proving no parallel snapshot code, (b) parity byte-diff green, (c) Ashley's visual observation that a MessageListNode with followTail:true stays pinned to the bottom (may need Showcase to simulate an append — planner-picked verification approach)."
    - "Every COMP-XX requirement is CLOSED: COMP-05 (ListRowNode), COMP-05a (ListNode.variant:'rows'), COMP-06 (MessageNode), COMP-06a (MessageListNode with followTail), COMP-07 (AlertNode with tone→icon default map), COMP-08 (EmptyStateNode rename). Each has: code shipped + tests green + parity tripwire green + demo adoption + CHANGELOG entry + design-doc inventory row."
    - "Open bounty `empty-state-on-collections` is CLOSED — this composite supersedes the devolve-to-collection-property direction. The bounty file (search `.claude/identities/*/bounties/` or ask operator) is marked resolved-by-COMP-08."
    - "NO release ships in this plan (batch-then-ship LOCKED per CONTEXT.md §11). No `npm publish`, no `dotnet nuget push`, no version bump, no tag. `package.json` stays 7.1.0; `.csproj` stays 7.0.0."
    - "Phase 24 SUMMARY records a requirement-to-artifact cross-check: for each of COMP-05..08 the exact files, tests, tripwires, demo instances, CHANGELOG/MIGRATION entries."
  artifacts:
    - path: ".planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md"
      provides: "Records the green-tree gate results + AA hand-check aggregate + Ashley's visual sign-off + COMP-XX requirement-to-artifact cross-check + bounty-close confirmation."
      contains: "green"
  key_links:
    - from: "All prior plans (24-01..08)"
      to: "The green-tree gate + Ashley sign-off"
      via: "Phase 24 landing verification"
      pattern: "green"
---

<objective>
Run the FULL local green-tree gate one final time with everything from Plans 24-01..08 in place, hand-check aggregate AA-contrast across all 4 composites (273 total pair-checks), run parity green across all backends, get Ashley's visual sign-off on the Showcase Primary Composites section served over the tailnet, and record the requirement-to-artifact cross-check for COMP-05..08.

Purpose: Phase 24 does NOT ship a release (batch-then-ship LOCKED per CONTEXT §11). But every AGENTS.md gate must be green before commit lands, the AA-contrast per-new-pair discipline is non-negotiable (banked lesson), the fleet-adoption discipline requires operator eyeballing for visual changes, and the bounty-close for `empty-state-on-collections` needs recording. This plan is the "landing verification" — it doesn't add code, it proves the code from Plans 24-01..08 is correct + coherent.

Output: SUMMARY.md documenting green-tree evidence, per-composite AA aggregate, Ashley's sign-off note, COMP-XX cross-check, bounty-close confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full green-tree gate + parity + every .NET test project</name>
  <files></files>
  <read_first>
    - AGENTS.md Working Agreement — the exact green-tree gate command list (`npm run build && check:test-types && check:core-globals && check:aa-contrast && check:no-demo-style && check:demo-types && npx vitest run` + .NET tests + parity + every `demo/**/*.Tests.csproj`)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md §Specifics dotnet PATH: `export PATH="$HOME/.dotnet:$PATH"` before parity harness AND .NET tests (banked from Phase 23 rediscovery)
  </read_first>
  <action>
    Ensure `~/.dotnet` is on PATH first (per AGENTS.md gotcha #9: `export PATH="$HOME/.dotnet:$PATH"`). Then run each gate command in sequence, capturing output. Any RED result blocks Phase 24 closeout — either fix the underlying issue OR (if pre-existing per AGENTS.md rule) surface to operator with an explicit waiver request.

    **Gate command sequence:**

    (a) Framework build + all `check:*` scripts + vitest, in `viewmodel-shell/`:
    ```
    cd viewmodel-shell
    npm run build
    npm run check:test-types
    npm run check:core-globals
    npm run check:aa-contrast
    npm run check:no-demo-style
    npm run check:demo-types
    npx vitest run
    ```

    (b) Framework .NET Tests project (per AGENTS.md gotcha #9 — was uncompilable 3.0.0 → 3.3.0 precisely because it was skipped; now non-negotiable):
    ```
    export PATH="$HOME/.dotnet:$PATH"
    dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal
    ```

    (c) Every `demo/**/*.Tests.csproj`:
    ```
    for p in $(find demo -name '*.Tests.csproj'); do
      echo "=== $p ==="
      dotnet test "$p" --nologo -v minimal
    done
    ```

    (d) Cross-backend parity:
    ```
    cd parity
    bun run run.ts
    ```

    RECORD every gate's result in the phase SUMMARY. Any RED result STOPS Phase 24 closeout.

    **Special check for this phase:** since Plan 24-04 renamed EmptyStateNode fields, verify no framework consumer references old field names by running:
    ```
    grep -rn '"heading":\|__heading\|__message\|\.Heading\b\|\.Message\b' viewmodel-shell/ viewmodel-shell-dotnet/ demo/ | grep -v CHANGELOG | grep -v MIGRATION | grep -v HeadingLevel | grep -v Message.*// | head
    ```
    Any hit that references EmptyStateNode's old field names (context-sensitive — grep can false-positive on other nodes' `heading:` or `message:` fields — verify each hit) is a rename-cascade miss and must be fixed.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; npm run check:test-types &amp;&amp; npm run check:core-globals &amp;&amp; npm run check:aa-contrast &amp;&amp; npm run check:no-demo-style &amp;&amp; npm run check:demo-types &amp;&amp; npx vitest run &amp;&amp; cd .. &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --nologo -v minimal &amp;&amp; (for p in $(find demo -name '*.Tests.csproj'); do dotnet test "$p" --nologo -v minimal || exit 1; done) &amp;&amp; cd parity &amp;&amp; bun run run.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `viewmodel-shell/` succeeds.
    - `npm run check:test-types` green.
    - `npm run check:core-globals` green (core index.ts has ZERO platform globals; new composite renderers legitimately live in browser.ts).
    - `npm run check:aa-contrast` green (fixed 13-pair gate — the new pairs are hand-checked in test file headers, not automated in this script).
    - `npm run check:no-demo-style` green.
    - `npm run check:demo-types` green (Showcase Primary Composites section compiles under strict tsconfig — verifies Plan 24-06's wire-type consumption).
    - `npx vitest run` green — all shipped tests + the new files (list-row.test.ts, message.test.ts, message-followtail.test.ts, alert.test.ts, empty-state.test.ts) pass.
    - `dotnet test viewmodel-shell-dotnet/Tests` green — all shipped .NET tests + the new files (ListRowNodeSerializationTests.cs, MessageNodeSerializationTests.cs, MessageListNodeSerializationTests.cs, AlertNodeSerializationTests.cs, EmptyStateNodeSerializationTests.cs) pass.
    - Every `demo/**/*.Tests.csproj` green — no red results including any demo tests that reference EmptyStateNode with new field names (Plan 24-04 rename cascade).
    - `bun run parity/run.ts` byte-identical GREEN across all backends AND all `expectBodyContains` tripwires match (foundations tripwires from Phase 23 + primary-composites tripwires from Plan 24-07).
    - EmptyStateNode rename grep-check returns no framework-internal misses (Plan 24-04 rename cascade fully complete).
    - Every gate's output captured in the phase SUMMARY with pass/fail state + command + duration.
  </acceptance_criteria>
  <done>
    Every AGENTS.md gate green; no red results anywhere; parity + all .NET test projects included; rename cascade verified complete; ready for the visual smoke.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Everything from Plans 24-01..08:
    - ListRowNode (COMP-05) + ListNode.variant:"rows" (COMP-05a) — dense list rows with 3-tier typography + whole-row action a11y + single-surface container.
    - MessageNode (COMP-06) + MessageListNode (COMP-06a) — chat/comment shape with role-tinted surfaces + follow-tail REUSING SectionNode's shipped `data-follow-tail` mechanism (one-line adapter change).
    - AlertNode (COMP-07) — 4 tones with baked tone→icon default map + dismissible variant emitting RESERVED `{name: "dismiss"}`.
    - EmptyStateNode (COMP-08) — v8.0 BREAKING rename `heading→title`, `message→description` + NEW `icon?` slot + tinted-circle icon backdrop; .NET `Tooltip` field removed.
    - Design doc `.planning/design/composite-nodes-layer.md` shipped-recipe-inventory table filled in for 4 primaries.
    - AGENTS.md "Currently shipped recipes" section populated with 4 primaries + reuse + rename callouts.
    - Showcase demo Primary Composites section demonstrating all 4 in situ.
    - FeatureProbe parity extended across all backends with 13 new tripwires — parity GREEN.
    - CHANGELOG.md Unreleased — v8.0.0 (in progress) gained 4 Added + 1 Changed (BREAKING) entry.
    - MIGRATION.md v8.0.0 section grew with EmptyStateNode rewrite + additive notes.
    - Full green-tree gate + parity + every .NET test project all green (from Task 1).
  </what-built>
  <how-to-verify>
    (1) Serve the Showcase demo over the tailnet:
    ```
    cd demo/Showcase/frontend
    npm run dev -- --host 0.0.0.0 --port <a-free-port>
    ```
    (Or grep `demo/Showcase/package.json` scripts for the actual build+serve incantation.)

    (2) Navigate to the Showcase URL on the tailnet in a real browser. Find the "v8.0.0 Primary Composites" section (added by Plan 24-06). Verify VISUALLY:

    - **ListRowNode 3-tier typography.** The primary text is body-weight-medium (semibold). Secondary is muted (lighter). Meta is text-xs muted with opacity (caption tier — the smallest). Each visibly distinct. `tone:"warning"` rows have a left-accent border in the warning color; `tone:"success"` rows in success; `tone:"danger"` in danger. Rows inside a `variant:"rows"` container render as a single bordered surface with per-row dividers — NOT card-per-row.

    - **ListRowNode whole-row action a11y.** Tab-focus a clickable row (any `tone:"success"` row from the Showcase example, e.g. Ada Lovelace). The row shows a focus ring. Press Enter — action fires. Press Space — action fires AND page doesn't scroll (the Space preventDefault worked). Click a nested button (if any exists in `trailing`) — only the button's action fires, not the row's (stopPropagation worked).

    - **MessageNode role-tinted surfaces.** The user message (Ada Lovelace) has a NEUTRAL content-surface background. The assistant message (VMS) has a TINTED-INFO content-surface (subtle blue-tinted). Avatars render correctly (initials mode for user; icon mode for assistant). Timestamps render as text-xs muted (caption tier).

    - **MessageListNode followTail** (if the Showcase can simulate an append, e.g. via a button that dispatches to add a new message and re-renders — planner-picked; if not, skip visual verification and rely on Task 1's vitest coverage). Scroll the message-list to the bottom, trigger an append — new content pins the scroll to the new bottom. Scroll up, trigger an append — scrollTop preserved.

    - **AlertNode all 4 tones + defaults.** Danger alert renders with red-ish tinted background + red border + `x-circle` icon (a circle with an X). Warning: yellow-ish + `alert-triangle` (a triangle). Success: green-ish + `check-circle` (a circle with a check). Info: blue-ish + `info` (an i-in-a-circle). Titles are body-weight-medium; messages are text-sm muted.

    - **AlertNode dismissible.** The `warning` tone Alert (or whichever alert has `dismissible:true`) shows a close-X in the top-right of its actions column. Click it — an action fires (which the Showcase's app handler receives as `{name: "dismiss"}`; the Showcase may not visibly react, but the click must not error).

    - **EmptyStateNode NEW shape.** The EmptyStateNode renders: an INBOX icon inside a tinted-accent (~12%) 3rem-diameter CIRCLE backdrop, above a text-lg semibold TITLE ("No orders yet"), above a text-sm muted DESCRIPTION (with a max-width so it doesn't sprawl), above a centered "Learn more" button. Verify the OLD class names (`.vms-empty-state__heading`, `.vms-empty-state__message`) don't appear in the DOM (inspect element).

    (3) Toggle the theme picker (Showcase has a theme switcher) — verify the Primary Composites section looks right on default + at least 2-3 dark themes + at least 2-3 light themes. The AA-contrast hand-check in Plans 24-01..04 covered the numeric ratios; this is a live sanity check.

    (4) Confirm all 4 primaries "look right" per your (Ashley's) judgment. If any look wrong (e.g. the follow-tail scroll doesn't stick to bottom, the tone-default icon renders the wrong glyph, the tinted-info assistant surface is invisible against the neutral surface), name specifically what's wrong so a follow-up task can fix it.

    Additionally, re-verify the AA-contrast hand-check aggregate. Inspect the SUMMARY.md files from Plans 24-01/02/03/04 (records for ListRow hover-tint, Message assistant surface, Alert 4-tone × 4-element, EmptyState icon backdrop). Any theme documented as failing should have a deepening or KNOCKOUT mitigation recorded in default.css. If a theme was skipped, name it now.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 primary composites look right on default + representative themes AND the AA hand-check aggregate is complete AND the EmptyStateNode rename is visually verified (no old-shape leftovers). Type "fix: {description}" if any composite needs a visual fix before Phase 25 opens. Type "AA-gap: {description}" if an AA pair was missed and needs a hand-check re-run.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Requirement-to-artifact cross-check + bounty-close + write SUMMARY.md + update ROADMAP</name>
  <files>.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md, .planning/ROADMAP.md</files>
  <read_first>
    - All 8 prior plan SUMMARY files (24-01-SUMMARY.md through 24-08-SUMMARY.md) — if they exist. If not, the plans themselves.
    - .planning/REQUIREMENTS.md — COMP-05..08 definitions (the criteria Phase 24 closes)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md (Locked Decisions — each has a landing artifact)
    - .planning/ROADMAP.md — locate the Phase 24 plan-list block (currently `**Plans:** 0 plans (to be created by /gsd:plan-phase 24)` per Bash grep earlier) and tick boxes if the convention is in use
    - Bounty file: search `~/.claude/identities/*/bounties/` for `empty-state-on-collections` — the bounty this phase resolves
  </read_first>
  <action>
    (a) Write `.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md`. Sections:

    (i) **Green-tree gate results** — table with columns `Gate | Result | Command | Notes`. Every command from Task 1 gets a row.

    (ii) **Requirement-to-artifact cross-check** — table with COMP-05, COMP-05a, COMP-06, COMP-06a, COMP-07, COMP-08 as rows. Columns: `Requirement | Wire type shipped | Renderer | CSS | .NET twin | Parity tripwire | Tests (vitest + .NET) | Demo adoption | CHANGELOG | Design-doc inventory row`. Every cell filled with exact file+line evidence.

    Example row (COMP-05):
    | COMP-05 ListRowNode | `interface ListRowNode` in `src/index.ts` | `private listRow()` in `src/browser.ts` | `.vms-list-row*` in `styles/default.css` | `public record ListRowNode` in `ViewModels.cs` | `"type":"list-row"` + `list-row-open-42` in `parity/fixtures/feature-probe.json` | `test/list-row.test.ts` + `Tests/ListRowNodeSerializationTests.cs` | Showcase Primary Composites section + FeatureProbe primaryCompositesSection | CHANGELOG.md Unreleased v8.0.0 Added | design/composite-nodes-layer.md Shipped Recipe Inventory row 1 |

    (iii) **AA-contrast hand-check aggregate** — restate per-composite totals: ListRow 39 pair-checks (hover-tint × 3 text × 13 themes), Message 13 (assistant surface × body text × 13 themes), Alert 208 (4 tones × 4 elements × 13 themes), EmptyState 13 (tinted-circle icon glyph × 13 themes). Total: 273. Anywhere any theme required deepening or KNOCKOUT mitigation, name the deepening percentage or knockout pattern (reference the specific test file's header).

    (iv) **followTail REUSE verification** — three-way proof: (a) vitest `message-followtail.test.ts` code-search test proves no parallel snapshot code in `messageList()`, (b) parity byte-diff green across backends, (c) Ashley's visual sign-off on the Showcase MessageListNode follow-tail behavior (if simulated).

    (v) **EmptyStateNode rename cascade** — three-way proof: (a) framework-internal grep clean (no `heading:`/`message:` on empty-state anywhere), (b) FeatureProbe backends updated to new field names (verified by parity tripwires `"title":"No orders yet"` + `"description":"Once customers..."`), (c) MIGRATION.md documents the rewrite.

    (vi) **Design-doc + AGENTS.md landing** — one line each:
    - `.planning/design/composite-nodes-layer.md` shipped-recipe-inventory table filled in for 4 primaries (Plan 24-05).
    - AGENTS.md "Currently shipped recipes" placeholder replaced (Plan 24-05).
    - CHANGELOG.md Unreleased v8.0.0 gained 4 Added + 1 Changed (BREAKING) (Plan 24-08).
    - MIGRATION.md v8.0.0 EmptyStateNode rewrite + additive notes (Plan 24-08).

    (vii) **Bounty close** — record: `empty-state-on-collections` RESOLVED by COMP-08 (standalone composite wins vs. devolve-to-collection-property, per tasting-approval + CONTEXT §Explicitly-NOT-out-of-scope). Bounty file location documented (from grep in `<read_first>`); if writable, mark the bounty file as `resolved-by-COMP-08` OR record in this SUMMARY that operator should manually mark it. Do NOT modify a bounty file outside this repo without operator approval.

    (viii) **Ashley's visual sign-off** — transcribe the exact resume-signal text from Task 2.

    (ix) **NO release ship** — explicitly note: no version bump (package.json still 7.1.0, .csproj still 7.0.0), no npm publish, no dotnet nuget push, no git tag. Batch-then-ship LOCKED per CONTEXT §11; v8.0.0 releases at Phase 26 closeout.

    (x) **Ready for Phase 25** — explicit "green light to run `/gsd:plan-phase 25`" if all gates + sign-off are green.

    (b) Update `.planning/ROADMAP.md` Phase 24 plan-list block. Replace `**Plans:** 0 plans (to be created by /gsd:plan-phase 24)` with:

    ```
    **Plans:** 9 plans complete

    Plans:
    - [x] 24-01-PLAN.md — ListRowNode (COMP-05) + ListNode.variant:"rows" (COMP-05a) end-to-end (wave 1)
    - [x] 24-02-PLAN.md — MessageNode (COMP-06) + MessageListNode (COMP-06a) end-to-end; followTail REUSES SectionNode.followTail (wave 2, depends on 24-01)
    - [x] 24-03-PLAN.md — AlertNode (COMP-07) end-to-end (wave 3, depends on 24-02)
    - [x] 24-04-PLAN.md — EmptyStateNode (COMP-08) BREAKING RENAME end-to-end (wave 4, depends on 24-03)
    - [x] 24-05-PLAN.md — design/composite-nodes-layer.md Shipped Recipe Inventory + AGENTS.md Currently shipped recipes (wave 1)
    - [x] 24-06-PLAN.md — Showcase Primary Composites section (wave 5, depends on 24-04)
    - [x] 24-07-PLAN.md — FeatureProbe parity extension + $comment + tripwires (wave 5, depends on 24-04)
    - [x] 24-08-PLAN.md — CHANGELOG.md + MIGRATION.md updates for Unreleased v8.0.0 (wave 5, depends on 24-04)
    - [x] 24-09-PLAN.md — Full green-tree gate + Ashley visual sign-off + requirement cross-check + bounty close (wave 6, depends on all prior)
    ```

    (c) Verify version files STILL UNTOUCHED: `grep '"version":' viewmodel-shell/package.json` returns `7.1.0`; `grep '<Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` returns `7.0.0`. This is the final defensive check against accidental release.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md &amp;&amp; grep -c 'COMP-05\|COMP-06\|COMP-07\|COMP-08' .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md &amp;&amp; grep -c 'green' .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md &amp;&amp; grep -c 'batch-then-ship\|Batch-then-ship' .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md &amp;&amp; grep -c 'empty-state-on-collections' .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md &amp;&amp; grep -c 'Phase 25' .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md &amp;&amp; grep '"version": "7\.1\.0"' viewmodel-shell/package.json &amp;&amp; grep '<Version>7\.0\.0</Version>' viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj &amp;&amp; grep -c '24-01-PLAN\|24-09-PLAN' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `24-09-SUMMARY.md` exists.
    - Contains green-tree gate results table with every gate command from Task 1.
    - Contains requirement-to-artifact table with 6 COMP-XX rows (COMP-05, 05a, 06, 06a, 07, 08) and 9+ columns of evidence per row.
    - `grep -c 'COMP-05' 24-09-SUMMARY.md` >= 2 (COMP-05 + COMP-05a).
    - `grep -c 'COMP-06' 24-09-SUMMARY.md` >= 2 (COMP-06 + COMP-06a).
    - `grep -c 'COMP-07' 24-09-SUMMARY.md` >= 1.
    - `grep -c 'COMP-08' 24-09-SUMMARY.md` >= 1.
    - `grep -c 'AA' 24-09-SUMMARY.md` >= 1 (AA-contrast aggregate section).
    - `grep -c '273 pair-checks\|273 pair\|total.*273' 24-09-SUMMARY.md` >= 1 (or the aggregate count is shown by summing per-composite).
    - `grep -c 'REUSE\|reuse\|reuses' 24-09-SUMMARY.md` >= 1 (followTail reuse verification).
    - `grep -c 'empty-state-on-collections' 24-09-SUMMARY.md` >= 1 (bounty-close recorded).
    - `grep -c 'Ashley' 24-09-SUMMARY.md` >= 1 (sign-off transcribed).
    - `grep -c 'batch-then-ship\|Batch-then-ship' 24-09-SUMMARY.md` >= 1.
    - `grep -c 'Phase 25' 24-09-SUMMARY.md` >= 1 (next-phase readiness).
    - Version files UNCHANGED (package.json = 7.1.0; .csproj = 7.0.0).
    - ROADMAP.md Phase 24 plan-list block updated with 9 plans + tick boxes.
  </acceptance_criteria>
  <done>
    Phase 24 SUMMARY records green-tree + AA aggregate + follow-tail reuse verification + rename cascade verification + Ashley sign-off + bounty close + no-release confirmation + next-phase readiness. ROADMAP updated. Phase 24 is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan. No code, no wire change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09-01 | (none) | Green-tree verification + operator sign-off | accept | This plan verifies that the code shipped in Plans 24-01..08 is correct + coherent. It doesn't add new code. Attack surface delta: zero. |
| T-24-09-02 | Operational | Accidental publish (operator sees v8.0.0 CHANGELOG + reflexes to publish v8.0.0) | mitigate | Three documented mitigations: (a) CHANGELOG Unreleased heading contains the batch-then-ship reminder, (b) Task 3 SUMMARY explicitly states no-release-in-this-phase + version-file-unchanged verification, (c) Ashley's checkpoint asks for "approved" as the resume signal, not "publish" — the sign-off boundary and the publish boundary are separate rituals. |
| T-24-09-03 | Information Disclosure | Rename cascade miss (a framework consumer still references old EmptyStateNode field names silently) | mitigate | Task 1's grep-check for old field names catches remaining misses. Task 1's `check:demo-types` catches type-mismatches at build time. Task 1's `dotnet test` catches .NET-side references. If a miss slips through Plan 24-04's rename task, this plan's gates should catch it. |
</threat_model>

<verification>
Task-scoped verifications inside each task. Overall plan complete when Task 3's SUMMARY is written + Ashley's sign-off transcribed + ROADMAP updated + version files verified untouched.
</verification>

<success_criteria>
- Full green-tree gate green across `viewmodel-shell`, `viewmodel-shell-dotnet/Tests`, every `demo/**/*.Tests.csproj`, and `parity/run.ts`.
- AA-contrast hand-check aggregate (273 pair-checks) documented across default + 12 themes for every new fg/bg pair.
- Ashley's visual sign-off recorded.
- Requirement-to-artifact cross-check complete for COMP-05, COMP-05a, COMP-06, COMP-06a, COMP-07, COMP-08.
- Open bounty `empty-state-on-collections` CLOSED (resolved-by-COMP-08).
- No release, no version bump — batch-then-ship LOCKED. Version files (package.json = 7.1.0; .csproj = 7.0.0) UNCHANGED.
- Phase 25 unblocked; explicit readiness statement in SUMMARY + ROADMAP updated with 9 completed plans.
</success_criteria>

<output>
`.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-09-SUMMARY.md` is the phase closeout artifact.
</output>
