---
phase: 24-v8-0-primary-composites-listrow-message-alert-emptystate
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - viewmodel-shell/src/index.ts
  - viewmodel-shell/src/browser.ts
  - viewmodel-shell/src/server.ts
  - viewmodel-shell/styles/default.css
  - viewmodel-shell-dotnet/ViewModels.cs
  - viewmodel-shell/test/message.test.ts
  - viewmodel-shell/test/message-followtail.test.ts
  - viewmodel-shell-dotnet/Tests/MessageNodeSerializationTests.cs
  - viewmodel-shell-dotnet/Tests/MessageListNodeSerializationTests.cs
autonomous: true
requirements: [COMP-06, COMP-06a]

must_haves:
  truths:
    - "`MessageNode` ships with wire type `\"message\"` on both backends. Slots: `avatar?`, `author` (required string), `timestamp?` (string), `content` (required, `string | ViewNode`), `role?` (closed union `\"user\" | \"assistant\" | \"system\"`), `actions?` (`ButtonNode[]`)."
    - "`MessageListNode` ships with wire type `\"message-list\"` on both backends. Slots: `children` (`MessageNode[]`), `followTail?` (optional non-nullable bool — `WhenWritingDefault` posture, matches `SectionNode.followTail` at ViewModels.cs:999)."
    - "MessageListNode `followTail:true` REUSES SectionNode's shipped `data-follow-tail` mechanism verbatim. The ONLY new adapter code is `el.dataset.followTail = ''` on the container `<div class=\"vms-message-list\">`. NO parallel snapshot/restore logic — the existing walk at browser.ts:227-246 (skip generic scroll-map) + :239-246 (pre-render snapshot) + :362-372 (post-render restore) already handles every `[data-follow-tail]` element in document order."
    - "Renderer emits `<div class=\"vms-message-list\">` (or `<div class=\"vms-message-list\" data-follow-tail>` when followTail:true) containing `<div class=\"vms-message vms-message--{role}\">` per child. Message DOM: `[avatar column | body column with header (author + timestamp) + content surface + optional actions bar]` per CONTEXT §3."
    - "Renderer wraps string `content` in `TextNode { style: \"body\" }`; ViewNode content renders as-is."
    - "role='assistant' emits `.vms-message--assistant`; CSS gives `.vms-message__content` a `color-mix(in srgb, var(--vms-info) 6%, var(--vms-surface))` tinted background. Other roles/omitted = neutral surface."
    - "Actions are ALWAYS visible — no hover-reveal (banked a11y doctrine)."
    - "Tree-validator descends into `avatar`, `content` (if ViewNode), each `actions[i]` on MessageNode; descends into every child on MessageListNode. MessageListNode rejects non-MessageNode children with `invalid_tree` (byte-identical error message across backends)."
    - "`.NET` `MessageNode` typed with `ViewNode Content` (required) and `ViewNode? Avatar`; `MessageListNode.Children` typed `IReadOnlyList<MessageNode>`. `MessageListNode.FollowTail` is a `bool` with `[JsonIgnore(WhenWritingDefault)]` (matches `SectionNode.FollowTail` posture at ViewModels.cs:999 — false is ABSENT on the wire)."
    - "vitest (`test/message.test.ts`) asserts DOM class emission per role + string-vs-ViewNode content + actions bar right-alignment + timestamp absence hides `.vms-message__timestamp`. Mutation-testable."
    - "vitest (`test/message-followtail.test.ts`) proves the followTail:true attribute AND that the shipped `data-follow-tail` snapshot/restore mechanism is triggered when the container is present — with jsdom scrollHeight/clientHeight stubs (test/follow-tail.test.ts pattern)."
    - ".NET (`Tests/MessageNodeSerializationTests.cs` + `Tests/MessageListNodeSerializationTests.cs`) asserts discriminators, WhenWritingNull on optionals, KebabEnum roundtrip for `MessageRole`, WhenWritingDefault on `FollowTail`."
    - "AA hand-check for `.vms-message--assistant .vms-message__content` (tinted-info 6% surface) × body text × default + 12 themes = 13 pair-checks recorded in test-file header."
  artifacts:
    - path: "viewmodel-shell/src/index.ts"
      provides: "`MessageNode` + `MessageListNode` interfaces + ViewNode union entries."
      contains: "\"message\""
    - path: "viewmodel-shell/src/browser.ts"
      provides: "`private message()` + `private messageList()` renderers + `renderNode` switch arms. MessageListNode.followTail REUSES data-follow-tail (one-line: `el.dataset.followTail = ''`)."
      contains: "vms-message"
    - path: "viewmodel-shell/src/server.ts"
      provides: "Walker arms for `\"message\"` + `\"message-list\"` (descend into slots + record actions + reject non-MessageNode children on MessageListNode)."
      contains: "message"
    - path: "viewmodel-shell/styles/default.css"
      provides: "`.vms-message*` + `.vms-message-list` class blocks."
      contains: ".vms-message"
    - path: "viewmodel-shell-dotnet/ViewModels.cs"
      provides: "`MessageNode` + `MessageListNode` records + `MessageRole` enum + `[JsonDerivedType]` registrations + walker arms."
      contains: "MessageNode"
    - path: "viewmodel-shell/test/message.test.ts"
      provides: "jsdom render tests + a11y + mutation coverage + AA hand-check header."
    - path: "viewmodel-shell/test/message-followtail.test.ts"
      provides: "jsdom test proving followTail:true triggers the shipped data-follow-tail mechanism (byte-identical to test/follow-tail.test.ts for SectionNode.followTail)."
    - path: "viewmodel-shell-dotnet/Tests/MessageNodeSerializationTests.cs"
      provides: ".NET wire tests for MessageNode."
    - path: "viewmodel-shell-dotnet/Tests/MessageListNodeSerializationTests.cs"
      provides: ".NET wire tests for MessageListNode including FollowTail WhenWritingDefault."
  key_links:
    - from: "viewmodel-shell/src/browser.ts (private messageList())"
      to: "viewmodel-shell/src/browser.ts (render() at lines 227-246 + 239-246 + 362-372)"
      via: "data-follow-tail attribute REUSES the shipped SectionNode.followTail mechanism verbatim — one-line addition"
      pattern: "dataset\\.followTail"
    - from: "viewmodel-shell/src/index.ts"
      to: "viewmodel-shell-dotnet/ViewModels.cs"
      via: "byte-identical wire shape for MessageNode + MessageListNode"
      pattern: "message"
---

<objective>
Land `MessageNode` (COMP-06) + `MessageListNode` (COMP-06a) end-to-end. Both backends byte-identical. `MessageListNode.followTail` REUSES SectionNode's shipped `data-follow-tail` scroll-pin mechanism verbatim (plan-checker C-4 warning — do NOT build parallel). Tree-validators descend + reject non-MessageNode MessageList children. Ships with vitest (including a jsdom follow-tail test proving the reuse works) + .NET tests + AA hand-check for the assistant tinted-info surface.

Purpose: The chat/comment/thread primitive — the shape the `/ai` chat consumer + every message/comment/log/activity-feed app needs. MessageListNode's follow-tail semantics let live feeds stay pinned to the bottom when at-bottom while preserving scroll position when scrolled up (the shipped Section.followTail behavior generalized).

Output: `MessageNode` + `MessageListNode` shipped both backends; the data-follow-tail mechanism reused (ONE line of new adapter code); full test coverage with a dedicated follow-tail test proving reuse works; AA hand-check for the assistant tinted-info body-text pair × 13 themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md
@.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md
@.planning/design/composite-nodes-layer.md
@AGENTS.md
</context>

<interfaces>
<!-- Pulled from PATTERNS.md §1, §2d, §2e, §4, §5. The MessageListNode.followTail reuse mechanism is the highest-risk part — every line reference below is directly from PATTERNS.md §2e. -->

Current SectionNode.followTail attribute emission (browser.ts:1058-1062 per PATTERNS.md §2e):
```typescript
// SectionNode.followTail — mark this as an append-only feed so render()'s
// snapshot/restore keeps its newest content in view (see render() + the
// FOLLOW_TAIL_STICK_THRESHOLD_PX constant). No CSS/class — the scroll comes
// from the element already being an overflow region (pair with fill).
if (n.followTail === true) el.dataset.followTail = "";
```

Pre-render snapshot in `render()` (browser.ts:239-246 per PATTERNS.md §2e):
```typescript
// SectionNode.followTail — snapshot, in document order, whether each
// append-only feed was scrolled near its bottom (and its prior scrollTop
// for the scrolled-up case). Ordinal-matched to the post-render walk below.
const followTail: Array<{ nearBottom: boolean; top: number }> = [];
this.container.querySelectorAll<HTMLElement>("[data-follow-tail]").forEach(el => {
  const distanceFromBottom = el.scrollHeight - el.scrollTop - el.clientHeight;
  followTail.push({
    nearBottom: distanceFromBottom <= FOLLOW_TAIL_STICK_THRESHOLD_PX,
    top: el.scrollTop,
  });
});
```

Post-render restore (browser.ts:362-372 per PATTERNS.md §2e):
```typescript
this.container.querySelectorAll<HTMLElement>("[data-follow-tail]").forEach((el, i) => {
  const snap = followTail[i];
  if (!snap || snap.nearBottom) el.scrollTop = el.scrollHeight;
  else el.scrollTop = snap.top;
});
```

The mechanism walks EVERY `[data-follow-tail]` element in document order — MessageListNode piggybacks by setting the SAME attribute. Zero new adapter code beyond the one-line `div.dataset.followTail = ""` when followTail:true.

`.NET` FollowTail posture on `SectionNode.FollowTail` at ViewModels.cs:999 (per PATTERNS.md §5 Analog D):
```csharp
[property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool FollowTail = false
```
`MessageListNode.FollowTail` uses the exact same posture — false is ABSENT on the wire.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add MessageNode + MessageListNode wire types (TS + .NET) + CSS block</name>
  <files>viewmodel-shell/src/index.ts, viewmodel-shell/styles/default.css, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell/src/index.ts (read the ViewNode union at :180-210; read the ButtonNode interface — grep `export interface ButtonNode` — the `actions?: ButtonNode[]` slot references it; find IconName import for TSDoc)
    - viewmodel-shell-dotnet/ViewModels.cs (grep for `public record SectionNode` and look at the `FollowTail` field around :999 — the exact `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool FollowTail = false` posture per PATTERNS.md §5 Analog D; grep for `[JsonPolymorphic]` and `[JsonDerivedType]` block at :751-782)
    - viewmodel-shell/styles/default.css (read the `.vms-section--*` tone rules at :443-446 — the color-mix tinted-surface pattern the assistant surface reuses; grep for `.vms-message` — should NOT exist yet)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md §3 (LOCKED MessageNode schema) + §4 (LOCKED MessageListNode schema — the followTail reuse mandate)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §1 (MessageNode + MessageListNode interfaces to copy verbatim) + §4 (default.css block verbatim — lines 886-914 of PATTERNS.md) + §5 (ViewModels.cs records)
  </read_first>
  <action>
    In `viewmodel-shell/src/index.ts`:

    (a) Add `MessageNode` interface per PATTERNS.md §1 (lines 114-130 of PATTERNS.md): `type: "message"`, `avatar?: ViewNode`, `author: string`, `timestamp?: string`, `content: string | ViewNode`, `role?: "user" | "assistant" | "system"`, `actions?: ButtonNode[]`. TSDoc from PATTERNS.md — every slot documented including string-lift trained typography.

    (b) Add `MessageListNode` interface per PATTERNS.md §1 (lines 132-141): `type: "message-list"`, `children: MessageNode[]` (tree-validator-restricted), `followTail?: boolean`. TSDoc explicitly says "Reuses SectionNode.followTail's mechanism verbatim (browser.ts:227-372). data-follow-tail attribute drives the shipped at-bottom snapshot/restore. Absent/false = normal preserve-my-place scroll."

    (c) Add `MessageNode` and `MessageListNode` to the ViewNode union at :180-210.

    In `viewmodel-shell-dotnet/ViewModels.cs`:

    (d) Add `[JsonDerivedType(typeof(MessageNode), "message")]` and `[JsonDerivedType(typeof(MessageListNode), "message-list")]` to the `[JsonPolymorphic]` block around :751-782.

    (e) Add `MessageRole` enum: `[JsonConverter(typeof(KebabEnum<MessageRole>))] public enum MessageRole { User, Assistant, System }`.

    (f) Add `MessageNode` record per PATTERNS.md §5:
    ```
    public record MessageNode(
        string Author,
        ViewNode Content,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] ViewNode? Avatar = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] string? Timestamp = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] MessageRole? Role = null,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)] IReadOnlyList<ButtonNode>? Actions = null
    ) : ViewNode;
    ```
    Slot-typing policy: `ViewNode Content` (required, polymorphic), `ViewNode? Avatar`. Actions typed `IReadOnlyList<ButtonNode>?` (narrow — the framework has always typed action-lists narrowly since ButtonNode is the only shape they accept; verify with `grep "IReadOnlyList<ButtonNode>" ViewModels.cs` for the existing analog).

    (g) Add `MessageListNode` record per PATTERNS.md §5:
    ```
    public record MessageListNode(
        IReadOnlyList<MessageNode> Children,
        [property: JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)] bool FollowTail = false
    ) : ViewNode;
    ```
    `IReadOnlyList<MessageNode>` (narrow — validator enforces at the tree level; type binds .NET compiler too). `FollowTail` uses `WhenWritingDefault` (matches `SectionNode.FollowTail` at :999 exactly — grep to confirm posture).

    In `viewmodel-shell/styles/default.css`:

    (h) Append the full MessageNode + MessageListNode CSS block verbatim from PATTERNS.md §4 (lines 886-914 of PATTERNS.md — starts with `/* ── MessageNode + MessageListNode (COMP-06, COMP-06a, v8.0.0) ─── */`). Insert after the ListRowNode block from Plan 24-01. Includes: `.vms-message-list` (flex column), `.vms-message` (grid: [avatar | body]), `.vms-message__avatar` (flex-shrink:0), `.vms-message__header` (baseline align), `.vms-message__author` (weight:600), `.vms-message__timestamp` (text-xs muted), `.vms-message__content` (padded surface with border-radius), `.vms-message--assistant .vms-message__content` (color-mix info 6% background + info 25% border), `.vms-message__actions` (right-aligned flex).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c '"message"' src/index.ts &amp;&amp; grep -c '"message-list"' src/index.ts &amp;&amp; grep -c 'export interface MessageNode' src/index.ts &amp;&amp; grep -c 'export interface MessageListNode' src/index.ts &amp;&amp; grep -c 'MessageNode\|MessageListNode\|MessageRole' ../viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; grep -c '\.vms-message' styles/default.css &amp;&amp; grep -c 'vms-message--assistant' styles/default.css &amp;&amp; grep -c 'color-mix.*--vms-info' styles/default.css &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet build ../viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export interface MessageNode' viewmodel-shell/src/index.ts` == 1.
    - `grep -c 'export interface MessageListNode' viewmodel-shell/src/index.ts` == 1.
    - `MessageNode` interface has: `type: "message"`, `author: string`, `content: string | ViewNode`, optional `avatar/timestamp/role/actions`.
    - `MessageListNode` interface has: `type: "message-list"`, `children: MessageNode[]`, optional `followTail?: boolean`.
    - `viewmodel-shell/src/index.ts` ViewNode union contains `| MessageNode` and `| MessageListNode`.
    - `grep -c 'public record MessageNode' viewmodel-shell-dotnet/ViewModels.cs` == 1.
    - `grep -c 'public record MessageListNode' viewmodel-shell-dotnet/ViewModels.cs` == 1.
    - `grep -c 'public enum MessageRole' viewmodel-shell-dotnet/ViewModels.cs` == 1.
    - `MessageListNode.FollowTail` field uses `WhenWritingDefault` (grep pattern: `WhenWritingDefault.*FollowTail` OR `FollowTail.*WhenWritingDefault` — order may vary).
    - `grep -c '\.vms-message\b\|\.vms-message__\|\.vms-message--' viewmodel-shell/styles/default.css` >= 8 (the block ships __avatar, __body, __header, __author, __timestamp, __content, __actions, --assistant + the base .vms-message and .vms-message-list).
    - `grep -c 'color-mix.*--vms-info' viewmodel-shell/styles/default.css` >= 1 (assistant tinted-info surface).
    - TS build green + .NET build green.
  </acceptance_criteria>
  <done>
    Wire types shipped byte-identical; MessageRole enum shipped .NET side; CSS block installed; builds green. Renderer + walker in Tasks 2/3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire MessageNode + MessageListNode renderers in browser.ts (REUSE data-follow-tail)</name>
  <files>viewmodel-shell/src/browser.ts</files>
  <read_first>
    - viewmodel-shell/src/browser.ts (read lines 53-58 for the FOLLOW_TAIL_STICK_THRESHOLD_PX constant + explanatory comment; read lines 227-231 for the "skip generic scroll-map" branch; read lines 239-246 for the pre-render snapshot; read lines 362-372 for the post-render restore; read lines 1044-1113 for `private section()` — the tinted-surface + heading + slot walk analog; read lines 1058-1062 for the SectionNode.followTail attribute emission — the single line MessageListNode copies verbatim)
    - viewmodel-shell/src/browser.ts (read the switch statement in `renderNode` around :535-580 to know where to insert new arms)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §2a (switch arms), §2d (full `private message()` code block to copy verbatim), §2e (`private messageList()` code block AND the critical "REUSE not re-build" callout — the four browser.ts locations that already handle the follow-tail mechanism)
  </read_first>
  <action>
    In `viewmodel-shell/src/browser.ts`:

    (a) Add `case "message": return this.message(n, parent, on);` and `case "message-list": return this.messageList(n, parent, on);` to `renderNode` switch. Placement: alphabetical among existing arms.

    (b) Add `private message(n: MessageNode, parent: HTMLElement, on: (a: ActionEvent) => void): void` per PATTERNS.md §2d code block:
      - Wrap `<div class="vms-message [vms-message--{role}]">`.
      - Avatar column: `<div class="vms-message__avatar">` — dispatch through `this.node(n.avatar, avatarEl, on)` when present.
      - Body column: `<div class="vms-message__body">`.
        - Header row: `<div class="vms-message__header">` with `<span class="vms-message__author">{author}</span>` (textContent, not innerHTML) and optional `<span class="vms-message__timestamp">{timestamp}</span>`.
        - Content surface: `<div class="vms-message__content">`. String content → `this.node({ type: "text", value: n.content, style: "body" }, content, on)`. ViewNode content → `this.node(n.content, content, on)`.
        - Actions bar (only when `n.actions && n.actions.length > 0`): `<div class="vms-message__actions">` with each `this.button(btn, actions, on)`.
      - Actions are ALWAYS rendered when present (no hover-reveal — banked doctrine).

    (c) Add `private messageList(n: MessageListNode, parent: HTMLElement, on: (a: ActionEvent) => void): void` per PATTERNS.md §2e code block:
      - Wrap `<div class="vms-message-list">`.
      - **🚨 CRITICAL — REUSE SectionNode.followTail mechanism verbatim.** The ONLY new adapter code is: `if (n.followTail === true) div.dataset.followTail = "";`. Do NOT add any snapshot/restore logic — the shipped `[data-follow-tail]` walk at browser.ts:227-231 + :239-246 + :362-372 already handles this element. The comment in PATTERNS.md §2e code block reads verbatim: "MessageListNode.followTail — reuses SectionNode's shipped mechanism. The pre-render snapshot at browser.ts:239-246 walks EVERY [data-follow-tail] element in document order; the post-render restore at browser.ts:362-372 pins each to its new bottom (or old scrollTop). NO new adapter code — this line is the only change."
      - Children walk: `this.kids(n.children as unknown as ViewNode[], div, on)` — trust the tree-validator (Task 3) rejects non-MessageNode children server-side; the renderer just dispatches through the standard node() walk.

    (d) Preparatory-comment update (optional, per PATTERNS.md §2e "Refactor DECISION"): the shipped follow-tail comments say "SectionNode.followTail" but the mechanism is really a generic "any element with data-follow-tail". Update the comments at browser.ts:53-58, :234-238, :362-368, and :1058-1062 to say "any element with data-follow-tail" (or "SectionNode + MessageListNode") — the CODE stays identical. This is a code-comment update ONLY.

    Verify with `npm run check:core-globals` — the renderer additions add zero new platform globals (they use `document.createElement`, `el.dataset`, event listeners — all already permitted in browser.ts).
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'case "message"' src/browser.ts &amp;&amp; grep -c 'case "message-list"' src/browser.ts &amp;&amp; grep -c 'private message(' src/browser.ts &amp;&amp; grep -c 'private messageList(' src/browser.ts &amp;&amp; grep -c 'vms-message-list' src/browser.ts &amp;&amp; grep -c 'vms-message__' src/browser.ts &amp;&amp; grep -c 'dataset\.followTail' src/browser.ts &amp;&amp; npm run check:core-globals</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "message"' viewmodel-shell/src/browser.ts` >= 1.
    - `grep -c 'case "message-list"' viewmodel-shell/src/browser.ts` >= 1.
    - `grep -c 'private message(' viewmodel-shell/src/browser.ts` == 1.
    - `grep -c 'private messageList(' viewmodel-shell/src/browser.ts` == 1.
    - `private messageList()` body contains ONLY: `document.createElement("div")`, className set, `if (n.followTail === true) div.dataset.followTail = "";`, `this.kids(...)`, `parent.appendChild(div);`. NO snapshot code, NO scrollTop reads inside `messageList()`, NO `querySelectorAll("[data-follow-tail]")` (that lives in `render()` and is unchanged).
    - `dataset.followTail` grep in `messageList` body: exactly ONE occurrence (the single-line reuse).
    - `private message()` body wraps string content in `this.node({ type: "text", value: n.content, style: "body" }, ...)`.
    - Actions bar is rendered UNCONDITIONALLY when `n.actions && n.actions.length > 0` (no `:hover` CSS pseudo-class or conditional class involved).
    - Verify no parallel scroll-pin logic exists: `grep -c 'scrollHeight\|scrollTop.*=' viewmodel-shell/src/browser.ts` count is UNCHANGED from before this task (compare with `git diff` — the numbers should match).
    - `cd viewmodel-shell && npm run build` succeeds.
    - `cd viewmodel-shell && npm run check:core-globals` green.
  </acceptance_criteria>
  <done>
    Renderers wired; data-follow-tail mechanism REUSED (verified: only one-line addition to messageList; no parallel logic); core-globals guard green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire tree-validator walker arms + MessageListNode child-type rejection (TS + .NET)</name>
  <files>viewmodel-shell/src/server.ts, viewmodel-shell-dotnet/ViewModels.cs</files>
  <read_first>
    - viewmodel-shell/src/server.ts (read `case "modal"` at :213-221 for typed-slots-with-Actions[] walker template; read `case "empty-state"` at :283-291 for single-slot descent + `walkForSectionAction` passthrough; read existing tree-invariant validators — grep for `invalid_tree`)
    - viewmodel-shell/src/server.ts (read the ListRowNode arm added in Plan 24-01 — same shape applies)
    - viewmodel-shell-dotnet/ViewModels.cs (find `Collect` around :2267-2415 and `WalkForSectionAction` around :2149-2264 — grep for Modal's arms as closest templates; find the ListRowNode arm added in Plan 24-01)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §3 (server.ts walker code blocks for `case "message"` + `case "message-list"` + the tree-invariant rejection for non-MessageNode children)
  </read_first>
  <action>
    In `viewmodel-shell/src/server.ts`:

    (a) Add `case "message"` to `collectActions` per PATTERNS.md §3:
    ```
    case "message": {
      const m = node as MessageNode;
      if (m.avatar) collectActions(m.avatar, enclosingForm, out);
      if (typeof m.content !== "string") collectActions(m.content, enclosingForm, out);
      for (const btn of m.actions ?? []) collectActions(btn, enclosingForm, out);
      return;
    }
    ```

    (b) Add `case "message-list"` to `collectActions`:
    ```
    case "message-list": {
      const ml = node as MessageListNode;
      for (const child of ml.children) collectActions(child, enclosingForm, out);
      return;
    }
    ```

    (c) Add passthrough arms in `walkForSectionAction` for both new types (mirror the `case "empty-state"` shape at :502-509 — descend into ViewNode-typed slots).

    (d) Add the MessageListNode child-type tree invariant: walking the tree, if `node.type === "message-list"`, assert every entry in `node.children` has `type === "message"`. Throw `invalid_tree` with an error message like `"MessageListNode.children must all be MessageNodes (found: {other.type})"`. Wire into the framework's existing tree-invariant walker OR add a new predicate — grep server.ts for `invalid_tree` OR `InvalidTreeError` to find the pattern. Reuse the exact machinery from Plan 24-01's `ListNode(variant:"rows")` mixed-children rejection.

    (e) Import `MessageNode`, `MessageListNode` at top of server.ts.

    In `viewmodel-shell-dotnet/ViewModels.cs`:

    (f) Add the .NET twin walker arms for `MessageNode` (descend into `Avatar`, `Content` when ViewNode not text, each Action in `Actions`) and `MessageListNode` (descend into each Child).

    (g) Add `WalkForSectionAction` passthrough arms for both.

    (h) Add the .NET twin MessageListNode child-type tree invariant with byte-identical error message to the TS side. Because `MessageListNode.Children` is typed `IReadOnlyList<MessageNode>` on .NET, the compiler catches this at construction — but a hostile deserialization path could still smuggle in non-MessageNode entries as `ViewNode`. The runtime validator is defense-in-depth. Reuse the pattern established in Plan 24-01.

    Error message MUST be byte-identical across TS + .NET (parity concern). Planner picks the exact wording — planner-chosen wording: `"MessageListNode.children must all be MessageNodes (found: <other-type>)"`.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npm run build &amp;&amp; grep -c 'case "message"' src/server.ts &amp;&amp; grep -c 'case "message-list"' src/server.ts &amp;&amp; grep -c 'MessageNode\|MessageListNode' src/server.ts &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; grep -c 'MessageNode\|MessageListNode' ../viewmodel-shell-dotnet/ViewModels.cs &amp;&amp; dotnet build ../viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "message"' viewmodel-shell/src/server.ts` >= 2 (in `collectActions` + `walkForSectionAction`).
    - `grep -c 'case "message-list"' viewmodel-shell/src/server.ts` >= 2.
    - Walker descends into `avatar` (typed as ViewNode), `content` (typeof-guarded to skip strings), each `actions[i]`. MessageListNode walker descends into every child.
    - Child-type tree invariant enforced: passing `{ type: "message-list", children: [ { type: "text", value: "X" } ] }` through the validator throws `invalid_tree` with the standardized error message.
    - .NET twin walker (`Collect` in `ViewModels.cs`) contains `MessageNode` and `MessageListNode` arms.
    - .NET twin `WalkForSectionAction` contains passthrough arms.
    - .NET tree-invariant validator emits the SAME error message as TS for the MessageListNode invalid-child case.
    - TS + .NET builds green.
  </acceptance_criteria>
  <done>
    Tree-validator descent wired both backends; MessageListNode child-type invariant enforced with byte-identical error messages; builds green.
  </done>
</task>

<task type="auto">
  <name>Task 4: vitest render tests + follow-tail reuse test + AA hand-check + .NET serialization tests</name>
  <files>viewmodel-shell/test/message.test.ts, viewmodel-shell/test/message-followtail.test.ts, viewmodel-shell-dotnet/Tests/MessageNodeSerializationTests.cs, viewmodel-shell-dotnet/Tests/MessageListNodeSerializationTests.cs</files>
  <read_first>
    - viewmodel-shell/test/avatar-render.test.ts (byte-aligned DOM/a11y template)
    - viewmodel-shell/test/follow-tail.test.ts (the direct template for `message-followtail.test.ts` — Phase 24 REUSES the same `data-follow-tail` mechanism; the test asserts the same at-bottom/scrolled-up behavior on `<div class="vms-message-list" data-follow-tail>` instead of `<section>`)
    - viewmodel-shell/test/text-caption.test.ts (stylesheet-load pattern for CSS-computed-style tests + AA-header format)
    - viewmodel-shell-dotnet/Tests/AvatarNodeSerializationTests.cs (byte-aligned .NET wire template)
    - viewmodel-shell/styles/themes/*.css (13 themes for AA hand-check spanning the assistant tinted-info surface × body text)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-CONTEXT.md §9 (AA-contrast checklist — MessageNode assistant surface = 13 pair-checks) + §Testing depth (test-file structure)
    - .planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-PATTERNS.md §8 (test blueprints)
  </read_first>
  <action>
    Create `viewmodel-shell/test/message.test.ts`:

    (a) AA-contrast hand-check header (mirror `test/text-caption.test.ts:23-52`): "AA-contrast hand-check for MessageNode assistant surface (COMP-06, v8.0.0)". 13 rows — one per theme (default + 12 in `viewmodel-shell/styles/themes/`). Pair: `color-mix(in srgb, var(--vms-info) 6%, var(--vms-surface))` (assistant `.vms-message__content` background) × `var(--vms-text)` (body text). Target: >= 4.5:1 (WCAG AA normal). If any theme drops below AA, document deepen mitigation (e.g. `color-mix(in srgb, var(--vms-info) 10%, var(--vms-surface))` or KNOCKOUT).

    (b) Stylesheet-load setup: reuse `test/text-caption.test.ts:64-76` pattern.

    (c) Tests in `describe("MessageNode (COMP-06)", () => { ... })`:
      - `it("emits <div class='vms-message'> with body column", ...)`.
      - `it("role='assistant' emits vms-message--assistant class", ...)`.
      - `it("role='user' emits vms-message--user class", ...)`.
      - `it("role='system' emits vms-message--system class", ...)`.
      - `it("omitted role emits no --{role} class", ...)`.
      - `it("wraps string content in TextNode with style:body", ...)`.
      - `it("passes ViewNode content through without wrapping", ...)`.
      - `it("renders author textContent in .vms-message__author", ...)`.
      - `it("timestamp present renders .vms-message__timestamp with textContent", ...)`.
      - `it("timestamp absent hides .vms-message__timestamp element entirely", ...)`.
      - `it("renders avatar slot through node dispatch (AvatarNode)", ...)` — pass an AvatarNode; assert `.vms-message__avatar > .vms-avatar` DOM.
      - `it("actions[] renders as .vms-message__actions bar with each button", ...)`.
      - `it("actions=[] or omitted hides .vms-message__actions", ...)`.
      - `it("actions bar has NO hover-reveal class — always visible", ...)` — assert `.vms-message__actions` has no `:hover` or conditional-hide class; the DOM is always present when actions are non-empty.
      - Mutation-test proof block naming the exact reverts.

    Create `viewmodel-shell/test/message-followtail.test.ts`:

    (d) File header comment: "MessageListNode.followTail REUSES SectionNode.followTail's shipped `data-follow-tail` mechanism (browser.ts:227-372). This test proves the reuse works — it asserts the attribute is emitted AND that the shipped snapshot/restore machinery pins scroll to the new bottom when the container is scrolled to the bottom before a re-render." Direct template: `test/follow-tail.test.ts`.

    (e) Use the exact scrollHeight/clientHeight jsdom stub pattern from `test/follow-tail.test.ts:15-48`:
    ```
    const SCROLL_HEIGHT = 2000;
    const CLIENT_HEIGHT = 300;
    const BOTTOM = SCROLL_HEIGHT - CLIENT_HEIGHT;
    // beforeEach: Object.defineProperty(HTMLElement.prototype, "scrollHeight", ...)
    ```

    (f) Tests:
      - `it("emits data-follow-tail attribute when followTail:true", ...)` — render MessageListNode with `followTail: true`, assert `.vms-message-list[data-follow-tail]` exists.
      - `it("omits data-follow-tail attribute when followTail:false or omitted", ...)`.
      - `it("at-bottom before render → pinned to new scrollHeight after render", ...)` — set `el.scrollTop = BOTTOM` before dispatching a re-render that appends new children; assert `el.scrollTop === el.scrollHeight` after. Proves the shipped mechanism at browser.ts:239-246 + 362-372 picks up the new MessageListNode container.
      - `it("scrolled-up before render → scrollTop preserved after render", ...)` — set `el.scrollTop = 500` (well above BOTTOM), append children, assert `el.scrollTop === 500` after.
      - `it("no parallel snapshot/restore logic exists in messageList renderer", ...)` — code-search assertion: `readFileSync("src/browser.ts")` inside the `messageList` function region contains NO `scrollTop = 0`, NO `scrollHeight`, NO explicit scroll manipulation beyond the single `dataset.followTail = ""` line.

    Create `viewmodel-shell-dotnet/Tests/MessageNodeSerializationTests.cs` and `viewmodel-shell-dotnet/Tests/MessageListNodeSerializationTests.cs` following `AvatarNodeSerializationTests.cs` structure:

    (g) `MessageNodeSerializationTests`:
      - `Type_SerializesAsMessage()`.
      - `BareNode_MinimalShape_OnlyRequiredFields()` — `new MessageNode(Author: "A", Content: new TextNode("X"))` → JSON has only `type`, `author`, `content`; every optional ABSENT.
      - `Role_User_SerializesAsKebabUser()`, `Role_Assistant_SerializesAsKebabAssistant()`, `Role_System_SerializesAsKebabSystem()`.
      - `Avatar_SetSerializesPolymorphically()` — pass an AvatarNode, assert nested `"type":"avatar"`.
      - `Actions_SerializesAsPolymorphicArray()`.
      - `Timestamp_OmittedIsAbsent()`, `Role_OmittedIsAbsent()`, `Actions_OmittedIsAbsent()`, `Avatar_OmittedIsAbsent()`.
      - `AllFieldsSet_AllPresent()`.

    (h) `MessageListNodeSerializationTests`:
      - `Type_SerializesAsMessageList()`.
      - `Children_EmptyList_SerializesAsEmptyArray()` — `new MessageListNode(Children: Array.Empty<MessageNode>())`.
      - `Children_WithMessages_SerializesPolymorphically()` — each child JSON has `"type":"message"`.
      - `FollowTail_False_SerializedAsAbsent()` — verifies `WhenWritingDefault` posture works: `new MessageListNode(Children: [...], FollowTail: false)` JSON does NOT contain `"followTail"`.
      - `FollowTail_True_SerializedAsTrue()` — verifies `"followTail":true` present.
      - `TreeInvariant_MessageListWithNonMessageChild_ThrowsInvalidTree()` — passing a `TextNode` as a "MessageNode" child via polymorphic deserialization is caught by the validator with the byte-identical error message from Task 3.
  </action>
  <verify>
    <automated>cd viewmodel-shell &amp;&amp; npx vitest run test/message.test.ts test/message-followtail.test.ts &amp;&amp; export PATH="$HOME/.dotnet:$PATH" &amp;&amp; dotnet test viewmodel-shell-dotnet/Tests --filter 'MessageNodeSerializationTests|MessageListNodeSerializationTests' --nologo -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/test/message.test.ts` exists with >= 12 `it()` blocks.
    - Header includes "AA-contrast hand-check" for the assistant tinted-info surface × body text × 13 themes.
    - `viewmodel-shell/test/message-followtail.test.ts` exists with >= 4 `it()` blocks.
    - message-followtail.test.ts uses scrollHeight/clientHeight defineProperty stubs (byte-aligned with test/follow-tail.test.ts).
    - message-followtail.test.ts includes a code-search test proving `messageList` renderer has NO parallel snapshot code (only the one-line `dataset.followTail = ""`).
    - `viewmodel-shell-dotnet/Tests/MessageNodeSerializationTests.cs` exists with >= 10 `[Fact]` methods including all three role kebab-cases + AllFieldsSet.
    - `viewmodel-shell-dotnet/Tests/MessageListNodeSerializationTests.cs` exists with >= 5 `[Fact]` methods including `FollowTail_False_SerializedAsAbsent` (WhenWritingDefault verification) + `FollowTail_True_SerializedAsTrue` + `TreeInvariant_MessageListWithNonMessageChild_ThrowsInvalidTree`.
    - `cd viewmodel-shell && npx vitest run test/message.test.ts test/message-followtail.test.ts` — green.
    - `~/.dotnet/dotnet test viewmodel-shell-dotnet/Tests --filter 'MessageNodeSerializationTests|MessageListNodeSerializationTests'` — green.
    - Mutation-verified: temporarily comment out `if (n.followTail === true) div.dataset.followTail = "";` in browser.ts messageList; run message-followtail.test.ts; at least one assertion fails; revert.
  </acceptance_criteria>
  <done>
    Tests exist and pass; the follow-tail-REUSE-not-rebuild property is proved by the code-search test; AA hand-check recorded for 13 themes; .NET wire tests including WhenWritingDefault + tree invariant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundary. MessageListNode.followTail REUSES the existing SectionNode.followTail mechanism at the same trust level; no new adapter code, no new dispatch path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | (none) | `MessageNode` + `MessageListNode` — passive rendering nodes with optional action-bearing button children | accept | No new input parsing, no external data flow, no auth interaction, no new URL. Actions on MessageNode are `ButtonNode[]` (same shape as every other action-bearing surface); action-name uniqueness enforced by the shipped walker. |
| T-24-02-02 | Denial of Service | Deeply nested MessageNode in `content` slot | accept | Same recursion depth ceiling as every other typed-slot composite; existing tree walker guards depth. No new machinery. |
| T-24-02-03 | Tampering | followTail attribute could be exploited if the mechanism were parallel-rebuilt (with subtle bugs) | mitigate | REUSE the shipped `data-follow-tail` mechanism verbatim (single-line addition `div.dataset.followTail = ""`). Task 2 acceptance criteria include `grep -c 'scrollHeight' src/browser.ts` UNCHANGED after this task — a proof-by-count that no parallel logic was added. Task 4's `message-followtail.test.ts` includes a code-search test asserting NO parallel snapshot code inside `messageList()`. |
| T-24-02-04 | Elevation of Privilege | Non-MessageNode child smuggled into MessageListNode via crafted JSON | mitigate | Tree-invariant validator (Task 3) rejects with byte-identical error message across both backends. The .NET side is additionally guarded by the compile-time `IReadOnlyList<MessageNode>` type on the record; the runtime validator catches the deserialization edge case. |
</threat_model>

<verification>
Task-scoped verifications above. Full green-tree gate deferred to Plan 24-10.
</verification>

<success_criteria>
- `MessageNode` + `MessageListNode` wire types shipped both backends byte-identical.
- MessageListNode.followTail REUSES the SectionNode.followTail mechanism verbatim — proven by (a) code-search test in message-followtail.test.ts + (b) grep-count unchanged assertion for `scrollHeight` in browser.ts.
- Renderer emits correct DOM per CONTEXT §3 + §4 (avatar column + body column with header + content surface + optional actions bar).
- Tree-validators descend + MessageListNode rejects non-MessageNode children with byte-identical error message across backends.
- vitest coverage: MessageNode (>= 12 tests) + MessageListNode follow-tail (>= 4 tests).
- .NET serialization tests including MessageRole KebabEnum + FollowTail WhenWritingDefault + tree invariant.
- AA hand-check for assistant tinted-info surface × body text × 13 themes recorded.
- Wire protocol token stays `viewmodel-shell/1.0`.
</success_criteria>

<output>
Create `.planning/phases/24-v8-0-primary-composites-listrow-message-alert-emptystate/24-02-SUMMARY.md` recording: artifacts touched, the ONE line of new adapter code for followTail (the reuse proof), mutation-test evidence, AA hand-check verdicts, and the byte-identical tree-invariant error message.
</output>
