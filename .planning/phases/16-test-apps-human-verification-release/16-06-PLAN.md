---
phase: 16-test-apps-human-verification-release
plan: 06
type: execute
wave: 4
depends_on: ["16-05"]
files_modified:
  - viewmodel-shell/package.json
  - viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj
autonomous: false
requirements: [CHART-07, NBA-09]

must_haves:
  truths:
    - "npm @ashley-shrok/viewmodel-shell 4.1.0 and NuGet AshleyShrok.ViewModelShell 4.1.0 are both live on their registries, followed by 4.2.0 of each — in that order, both published by the maintainer with the operator-gated credentials."
    - "Git tags v4.1.0 and v4.2.0 both exist, each at the correct commit, and both are reachable from origin/main (git merge-base --is-ancestor v4.2.0 main succeeds)."
    - "GitHub issue #6 is closed with a comment citing the 4.1.0 release."
    - "Both releases are announced in the #vms-changelog relay room."
  artifacts:
    - path: "viewmodel-shell/package.json"
      provides: "version field ends at 4.2.0"
      contains: "\"version\": \"4.2.0\""
    - path: "viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj"
      provides: "<Version> ends at 4.2.0"
      contains: "<Version>4.2.0</Version>"
  key_links:
    - from: "git tag v4.1.0 / v4.2.0"
      to: "origin/main"
      via: "git merge-base --is-ancestor <tag> main"
      pattern: "merge-base --is-ancestor"
---

<objective>
This is the SECOND and final maintainer-executed release plan. Every task is `checkpoint:human-action`
— publishing to npm/NuGet is operator-gated per AGENTS.md's standing rule ("a non-maintainer/anonymous
executor should NOT attempt to publish"), and pushing tags/`main` is likewise never autonomous per the
project's git safety protocol. This plan is a pure, mechanical bump→publish→tag→advance-main→announce→
close-issue sequence for TWO cumulative versions (4.1.0 first, then 4.2.0) in one operator session —
batched per Ashley's explicit direction (ROADMAP Phase 13 note: "avoid running the whole release
ceremony twice") — because 16-05 already made everything else (docs, green-tree gate) correct and
green at the commit about to be tagged.

Purpose: ship both the deferred v4.1 Data Visualization milestone (ChartNode, closing issue #6) and
the v4.2 Non-Blocking Actions milestone in one clean, ordered operator session, with `main` correctly
advanced and both releases announced/closed.

Output: npm `4.1.0` then `4.2.0` published; NuGet `4.1.0` then `4.2.0` published; git tags `v4.1.0` and
`v4.2.0`; `main` pushed and verified as an ancestor of `v4.2.0`; issue #6 closed; both releases
announced in `#vms-changelog`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@AGENTS.md

# The exact 16-05 output this plan bumps/publishes/tags on top of
@.planning/phases/16-test-apps-human-verification-release/16-05-SUMMARY.md
@CHANGELOG.md
@MIGRATION.md
</context>

<interfaces>
<!-- Current, pre-bump baseline (verified at planning time): npm package.json "version": "4.0.0";
     .csproj <Version>4.0.0</Version>. Both must move to 4.1.0, THEN to 4.2.0 — two separate bump
     +commit+publish+tag cycles, not one bump straight to 4.2.0 (each version must independently
     exist on both registries and be independently tagged, per CHART-07 and NBA-09 each naming their
     own release). Repo: `ashley-shrok/ViewModelShell` (confirmed via `gh repo view`). Issue #6:
     "Chart / data-viz support (deferred — needs design)", currently OPEN (confirmed via `gh issue
     list`). Credentials: repo-root `.env` (gitignored) holds the npm auth token / `NUGET_API_KEY` —
     `source .env` (or the maintainer's own established auth-precheck ritual, which is intentionally
     NOT documented in this repo per AGENTS.md's "Publishing is operator-gated" note) before any
     publish command below. -->
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: credential precheck, then bump + commit + publish + tag 4.1.0 (npm + NuGet)</name>
  <files>viewmodel-shell/package.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj</files>
  <read_first>
    @AGENTS.md (the full "Conventions for evolving the framework" publishing ritual: green-tree gate, the exact `npm publish`/`dotnet pack`/`dotnet nuget push` commands, the registry-confirmation curl/python one-liners, and the "credential precheck — surface gaps BEFORE bumping" rule)
  </read_first>
  <action>
    Source the maintainer's credentials (`.env` at the repo root, or the maintainer's own documented
    auth-precheck ritual — never `npm login`) and confirm both npm auth (`npm whoami`) and the
    presence of `NUGET_API_KEY` in the shell BEFORE touching any version field. If either credential is
    missing or broken, STOP and report — do not bump anything on top of a broken publish path (this is
    exactly the failure mode AGENTS.md's history records twice, at 1.1.0-1.3.0 and 0.16.0-1.2.0).

    Bump `viewmodel-shell/package.json`'s `"version"` field from `"4.0.0"` to `"4.1.0"`. Bump
    `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj`'s `&lt;Version&gt;` from `4.0.0` to
    `4.1.0`. Commit both together on `main` with a message in the established `release: npm+NuGet
    4.1.0 — ChartNode: single-series bar-chart data-viz primitive` style (mirror `git log --oneline
    -20`'s existing `release: ...` commit messages).

    Publish npm: `cd viewmodel-shell && npm publish` (the package's `prepublishOnly` script runs `npm
    run build` first automatically). Confirm live: `curl -s
    https://registry.npmjs.org/@ashley-shrok/viewmodel-shell | python3 -c "import sys,json;
    print(json.load(sys.stdin)['dist-tags']['latest'])"` must print `4.1.0` (NOT `npm view ... version`
    — that read is cached and can show a stale value).

    Publish NuGet: `cd viewmodel-shell-dotnet && dotnet pack -c Release` then `dotnet nuget push
    bin/Release/AshleyShrok.ViewModelShell.4.1.0.nupkg --api-key "$NUGET_API_KEY" --source
    https://api.nuget.org/v3/index.json`. Confirm live: `curl -s
    https://api.nuget.org/v3-flatcontainer/ashleyshrok.viewmodelshell/index.json | python3 -c "import
    sys,json; print(json.load(sys.stdin)['versions'][-1])"` must print `4.1.0`.

    Tag: `git tag -a v4.1.0 &lt;the release commit sha&gt; -m "viewmodel-shell 4.1.0"` then `git push
    origin v4.1.0`. Do NOT push `main` yet — Task 2 bumps to 4.2.0 first, then both tags/main are
    pushed together in Task 3, matching the "batch the whole ceremony" directive.
  </action>
  <verify>
    <human-check>Confirm both registry reads (curl+python one-liners above) print `4.1.0` before proceeding to Task 2.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm whoami` (or the maintainer's equivalent auth check) succeeded before any file was touched.
    - `viewmodel-shell/package.json`'s `"version"` reads `"4.1.0"`.
    - `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj`'s `&lt;Version&gt;` reads `4.1.0`.
    - The npm registry curl check prints `4.1.0`.
    - The NuGet registry curl check prints `4.1.0`.
    - `git tag -l v4.1.0` shows the tag exists locally and `git ls-remote origin v4.1.0` shows it exists on `origin`.
  </acceptance_criteria>
  <done>npm and NuGet both serve 4.1.0 live; the release commit is tagged `v4.1.0` and the tag is pushed to origin.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: bump + commit + publish + tag 4.2.0 (npm + NuGet)</name>
  <files>viewmodel-shell/package.json, viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj</files>
  <read_first>
    @AGENTS.md (same publishing ritual section as Task 1 — repeat it for the second version bump)
  </read_first>
  <action>
    Bump `viewmodel-shell/package.json`'s `"version"` from `"4.1.0"` to `"4.2.0"`. Bump
    `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj`'s `&lt;Version&gt;` from `4.1.0` to
    `4.2.0`. Commit both together on `main` (on top of the 4.1.0 release commit from Task 1) with a
    message in the style `release: npm+NuGet 4.2.0 — non-blocking actions: blocking:false dispatch
    lane + poll-fold + selection.action resurrection`.

    Publish npm: `cd viewmodel-shell && npm publish`. Confirm live via the same curl+python one-liner
    as Task 1, expecting `4.2.0`.

    Publish NuGet: `cd viewmodel-shell-dotnet && dotnet pack -c Release` then `dotnet nuget push
    bin/Release/AshleyShrok.ViewModelShell.4.2.0.nupkg --api-key "$NUGET_API_KEY" --source
    https://api.nuget.org/v3/index.json`. Confirm live via the same curl+python one-liner as Task 1,
    expecting `4.2.0`.

    Tag: `git tag -a v4.2.0 &lt;this release commit sha&gt; -m "viewmodel-shell 4.2.0"`. Do NOT push
    yet — Task 3 pushes both tags plus `main` together and verifies the ancestor relationship.
  </action>
  <verify>
    <human-check>Confirm both registry reads print `4.2.0` before proceeding to Task 3.</human-check>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/package.json`'s `"version"` reads `"4.2.0"`.
    - `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj`'s `&lt;Version&gt;` reads `4.2.0`.
    - The npm registry curl check prints `4.2.0`.
    - The NuGet registry curl check prints `4.2.0`.
    - `git tag -l v4.2.0` shows the tag exists locally, at a commit that is a descendant of `v4.1.0`'s commit (`git merge-base --is-ancestor v4.1.0 v4.2.0` succeeds).
  </acceptance_criteria>
  <done>npm and NuGet both serve 4.2.0 live; the second release commit is tagged `v4.2.0` locally (not yet pushed).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: advance main, verify ancestry, announce both releases, close issue #6</name>
  <files></files>
  <read_first>
    @AGENTS.md (the "Advance `main` to the release commit" rule — the exact failure mode this step exists to prevent: tagging + publishing without pushing `main`, stranding the release commits off a stale `main` — this actually happened once in this repo's history for v1.5.0/v1.6.0, see AGENTS.md's own recorded incident)
  </read_first>
  <action>
    Push `main` and both tags: `git push origin main` then `git push origin v4.2.0` (v4.1.0 was
    already pushed in Task 1). Verify `main` actually contains both release commits, not just the
    tags: `git merge-base --is-ancestor v4.2.0 main && echo "on main"` must print `on main` — if it
    prints nothing, `main` is stranded behind the tags and must be reconciled (rebase/merge) before
    continuing, per AGENTS.md's documented incident.

    Announce both releases in the `#vms-changelog` relay room (per the project's standing
    announcement convention) — one message (or two) naming npm/NuGet `4.1.0` (ChartNode, closes #6)
    and `4.2.0` (non-blocking actions: `blocking:false`, poll-fold, selection.action resurrection),
    each with a one-line summary and a pointer to the CHANGELOG.md entries.

    Close GitHub issue #6 citing the 4.1.0 release: `gh issue close 6 --repo ashley-shrok/ViewModelShell
    --comment "Closed by the 4.1.0 release — ChartNode: a structured single-series bar chart primitive
    (bounded declared data, agent-legible, Chart.js as a private lazy-loaded adapter implementation
    detail). See CHANGELOG.md's 4.1.0 entry for full detail."`.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor v4.2.0 main && echo "on main"</automated>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor v4.2.0 main && echo "on main"` prints `on main`.
    - `git ls-remote origin main` and `git ls-remote origin v4.1.0 v4.2.0` all show up-to-date refs on `origin`.
    - `gh issue view 6 --repo ashley-shrok/ViewModelShell --json state,comments` shows `state: "CLOSED"` with a comment citing `4.1.0`.
    - Both releases were announced (recorded in the plan's SUMMARY — this repo has no automated way to verify relay-room delivery, so the maintainer confirms it was sent).
  </acceptance_criteria>
  <done>`main` is advanced and verified as an ancestor of both tags; both releases are announced; issue #6 is closed citing 4.1.0. The v4.1 Data Visualization and v4.2 Non-Blocking Actions milestones are both fully shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer's local `.env` credentials → npm/NuGet registries | The entire attack surface this plan touches. Every publish/push step is `checkpoint:human-action` — no anonymous or automated executor ever holds or uses these credentials, per AGENTS.md's standing rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Spoofing | a stale/expired npm token silently publishing as a different identity or failing opaquely | mitigate | Task 1's credential precheck (`npm whoami`) runs BEFORE any version bump, per AGENTS.md's "surface gaps BEFORE bumping" rule — a broken credential is caught before any file changes, not after. |
| T-16-12 | Tampering | publishing a version whose green-tree gate was not actually verified | mitigate | This plan strictly depends on 16-05, which re-confirms the full gate at the commit about to be tagged — this plan performs NO code changes of its own, only version-field bumps + registry publishes, so there is no new code to regress between gate-check and publish. |
| T-16-13 | Repudiation | `main` silently left stranded behind the pushed tags (the documented historical incident) | mitigate | Task 3's `git merge-base --is-ancestor v4.2.0 main` check is a hard acceptance criterion, not a suggestion — this is the exact verification AGENTS.md's own incident writeup prescribes. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no new packages — it publishes the repo's OWN existing package to the registries; no third-party package legitimacy gate applies. |
</threat_model>

<verification>
- npm registry serves `4.1.0` then `4.2.0` (curl+python checks, not `npm view`).
- NuGet registry serves `4.1.0` then `4.2.0` (curl+python checks).
- `git merge-base --is-ancestor v4.2.0 main` succeeds.
- `gh issue view 6` shows `CLOSED` with a comment citing `4.1.0`.
- Both releases announced in `#vms-changelog`.
</verification>

<success_criteria>
- CHART-07 is fully satisfied: 4.1.0 published on both registries, tagged, `main` advanced, announced, issue #6 closed citing 4.1.0.
- NBA-09 is fully satisfied: 4.2.0 published on both registries, tagged, `main` advanced, announced.
- The v4.1 Data Visualization and v4.2 Non-Blocking Actions milestones are both closed out in ROADMAP.md terms — nothing left "in progress" or "batched pending" for either.
</success_criteria>

<output>
Create `.planning/phases/16-test-apps-human-verification-release/16-06-SUMMARY.md` when done
</output>
