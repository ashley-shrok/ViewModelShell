---
phase: 16-test-apps-human-verification-release
plan: 05
type: execute
wave: 3
depends_on: ["16-04"]
files_modified:
  - viewmodel-shell/agent-skill.md
  - viewmodel-shell-dotnet/AgentSkill.md
  - CHANGELOG.md
  - MIGRATION.md
autonomous: false
requirements: [CHART-06, CHART-07, NBA-09]

must_haves:
  truths:
    - "viewmodel-shell/agent-skill.md documents ChartNode (kind/points/title/tone) for wire-driving agents, and viewmodel-shell-dotnet/AgentSkill.md is byte-identical to it."
    - "CHANGELOG.md has a real, correctly-ordered '## 4.1.0 / 4.1.0' heading (promoted from the stale 'on `main`, unpublished' Phase-12 draft, folding in the grid/tick theme-token review fix) and a fresh '## 4.2.0 / 4.2.0' heading covering the whole non-blocking-actions milestone (NBA-01..09) — both ABOVE the existing '## 4.0.0' heading, newest first."
    - "MIGRATION.md has entries for 4.1.0 and 4.2.0, both stating clearly that no consumer action is required (purely additive)."
    - "The full green-tree gate (vitest, parity, core-globals, both .NET test suites) is green at the exact commit about to be tagged, immediately before 16-06 bumps any version."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "## 4.2.0 / 4.2.0 (top) and ## 4.1.0 / 4.1.0 headings, in that order, above ## 4.0.0"
      contains: "## 4.2.0"
    - path: "viewmodel-shell/agent-skill.md"
      provides: "a ChartNode-documenting section"
      contains: "ChartNode"
  key_links:
    - from: "viewmodel-shell/agent-skill.md"
      to: "viewmodel-shell-dotnet/AgentSkill.md"
      via: "byte-identical copy, verified by the parity skill-diff gate (parity/check-skill.ts)"
      pattern: "ChartNode"
---

<objective>
This is the FIRST of two maintainer-executed release plans (16-05 = prep/docs, 16-06 = the actual
publish/tag/announce ceremony). Every task in this plan is `checkpoint:human-action` — the maintainer
(Vicky/Ashley) runs it personally with the `.env` credentials, per AGENTS.md's standing rule that
publishing/pushing is operator-gated and never delegated to an anonymous executor. Close the one
deferred documentation gap from Phase 13 (CHART-06: `ChartNode` was never added to `agent-skill.md`),
correctly re-home the stale "on `main`, unpublished" Phase-12 ChartNode CHANGELOG draft into a real
versioned `## 4.1.0` release entry, author a brand-new `## 4.2.0` entry for the whole non-blocking-
actions milestone (which has NO existing CHANGELOG draft to promote — Phases 14/15 never added one),
write the MIGRATION.md entries, and re-confirm the full green-tree gate immediately before 16-06
touches any version number.

Purpose: get every doc/gate artifact correct and green BEFORE any package.json/.csproj version bump
happens, so 16-06 is a pure mechanical bump-publish-tag-announce sequence with nothing left to decide.

Output: `viewmodel-shell/agent-skill.md` + `viewmodel-shell-dotnet/AgentSkill.md` documenting
ChartNode (byte-identical); `CHANGELOG.md` with correctly-ordered, correctly-populated `## 4.1.0` and
`## 4.2.0` headings; `MIGRATION.md` with both entries; a confirmed-green full test suite at the
commit 16-06 will tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/design/non-blocking-actions.md
@AGENTS.md

# Read the FULL current staging state before editing — line numbers below are as of this planning pass; re-locate by heading text, not literal line numbers, since prior commits may have shifted them.
@CHANGELOG.md
@MIGRATION.md
@viewmodel-shell/agent-skill.md
@viewmodel-shell-dotnet/AgentSkill.md

# Every requirement this release closes
@.planning/phases/14-non-blocking-dispatch-core/14-01-SUMMARY.md
@.planning/phases/14-non-blocking-dispatch-core/14-04-GAP-SUMMARY.md
@.planning/phases/15-poll-fold-selection-action-resurrection/15-01-SUMMARY.md
@.planning/phases/15-poll-fold-selection-action-resurrection/15-02-SUMMARY.md
@.planning/phases/15-poll-fold-selection-action-resurrection/15-03-SUMMARY.md
</context>

<interfaces>
<!-- The EXACT, currently-broken CHANGELOG.md staging state this plan must fix. Verified by direct
     read at planning time (2026-07-08 baseline) — re-verify by heading text before editing, since
     line numbers drift as content above is added/removed. -->

CHANGELOG.md today, top-to-bottom order of top-level (`## `) headings: `## 4.0.0 / 4.0.0` (newest
released), `## 3.11.0 / 3.11.0`, ... down through `## 2.0.0 / 2.0.0`, then `## 1.12.0 / 1.10.0 —
Layout System Completeness`. **Nested INSIDE that old `## 1.12.0` section** (a pure staging artifact,
NOT semantically part of the 1.12.0 release) are FOUR `### ... — Phase N (on `main`, unpublished)`
subsections in this order: `### Alignment enums ... — Phase 8`, `### Switcher + cards minItem — Phase
9`, `### Fits node — Phase 10`, `### ChartNode — Phase 12`. The first three were legitimately part of
1.12.0 and were correctly left in place (their headers still literally say "(on `main`, unpublished)"
even though 1.12.0 shipped — this is the ESTABLISHED, INTENTIONAL repo convention: only the enclosing
`## ` version heading gets rewritten at promotion time; nested `### Phase N` subsection headers are
never rewritten). **The FOURTH one — `### ChartNode — Phase 12` — does NOT belong to 1.12.0 at all.**
It was appended there in commit `52ee8f1` purely because "the most recent `### Phase N` subsection"
happened to sit inside the 1.12.0 section at the time (see that commit's message and
`12-02-SUMMARY.md`'s "Deviations from Plan" #1 for the documented reasoning). This subsection is
Phase 12 / v4.1 (ChartNode) content that must be EXTRACTED and re-homed under a real `## 4.1.0`
heading — it must not stay nested under 1.12.0, and must not be duplicated.

The exact ChartNode subsection to extract spans from its `### ChartNode — Phase 12 (on `main`,
unpublished)` heading through the end of its own `### Migration` subsection (the line reading "None
needed — a purely additive new leaf node..."), immediately before the next `## 1.11.0 / 1.9.0`
heading. Locate it by heading text (`grep -n "### ChartNode"` and the next `grep -n "^## "` after
it), not by the line numbers cited in this doc, since edits may have shifted them by the time this
plan executes.

The exact review-fix commit to fold into the new `## 4.1.0` entry: `cfa3175 fix(12): chart
grid/tick/axis colors track theme tokens` (already committed to `viewmodel-shell/src/browser.ts` —
this plan documents it, it does not re-implement it). Confirm its content with `git show cfa3175 --stat`
before writing the CHANGELOG paragraph.

The precedent for HOW to promote a staged draft into a real version heading: `git show fcece97 --
CHANGELOG.md` (the 1.12.0 promotion) — replace the enclosing `## ` heading's text/intro paragraph
with the real version + a summary + (if applicable) a "### Review fixes (pre-release visual
verification)" subsection; leave the nested `### Phase N` subsections' own headers untouched
(matching the established convention documented above).

`viewmodel-shell/agent-skill.md`'s current top-level (`## `) heading order (for placement of the new
ChartNode section): `... ## Polling`, `## Non-blocking actions (`blocking:false`)`, `## Files`, `##
Versioning`. Insert a new `## Chart data (`type:"chart"`)` section AFTER `## Files` and BEFORE `##
Versioning` (mirroring how `## Files` documents a specific node's wire shape for agents — `ChartNode`
deserves the same treatment: `kind` [omitted = `"bar"`], `points: [{label, value}]` [self-contained
pairs, no parallel-array alignment], optional `title`, optional `tone` [`danger|warning|success|info`,
mapped to theme tokens client-side — informational only, an agent reading the wire just sees the
string]). Confirm the exact `ChartNode`/`ChartPoint` field set against `viewmodel-shell/src/index.ts`
before writing (`grep -n "ChartNode\|ChartPoint" -A 20 viewmodel-shell/src/index.ts`).
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: close CHART-06 — document ChartNode in agent-skill.md, byte-copy, verify parity</name>
  <files>viewmodel-shell/agent-skill.md, viewmodel-shell-dotnet/AgentSkill.md</files>
  <read_first>
    @viewmodel-shell/agent-skill.md (the exact current `## Files` / `## Non-blocking actions` / `## Versioning` sections — the new section slots between `## Files` and `## Versioning`; mirror the terse, rule-oriented prose style of the `## Non-blocking actions` section 15-02 added)
    @viewmodel-shell/src/index.ts (the `ChartNode`/`ChartPoint` interfaces — confirm exact current field names/types before writing)
    @.planning/phases/15-poll-fold-selection-action-resurrection/15-02-SUMMARY.md (the exact byte-copy + parity-verification procedure this task repeats: edit the npm source, `cp` it verbatim to the .NET twin, then run the full parity suite)
  </read_first>
  <action>
    Add a new `## Chart data (`type:"chart"`)` section to `viewmodel-shell/agent-skill.md`, inserted
    between the existing `## Files` section and `## Versioning` section. Content: state that a
    `ChartNode` in the `vm` tree carries bounded, agent-legible declared data — `kind` (optional,
    omitted means `"bar"`, the only value in `viewmodel-shell/1.0`), `points` (an array of
    self-contained `{label, value}` pairs — no parallel-array index alignment needed, unlike some
    other frameworks' chart data shapes), an optional `title`, and an optional `tone`
    (`danger|warning|success|info`) that only affects rendered COLOR in a browser client — a
    wire-driving agent with no renderer can simply read `points`/`title` directly and ignore `tone`.
    State plainly that there is nothing else to do to "drive" a chart over the wire — it is read-only
    structured data like any other node, with no dispatch-bearing fields of its own. Byte-copy the
    updated `viewmodel-shell/agent-skill.md` to `viewmodel-shell-dotnet/AgentSkill.md` (`cp
    viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md`) so the two canonical sources
    stay identical, per AGENTS.md's "Canonical skill source" rule. Run the full parity suite (`cd
    /home/thenasty/ViewModelShell && bun run parity/run.ts`) and confirm it reports the skill
    source-twin diff AND the skill HTTP-twin check both green.
  </action>
  <verify>
    <automated>bun run parity/run.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ChartNode" viewmodel-shell/agent-skill.md` matches at least once.
    - `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` produces NO output (byte-identical).
    - `bun run parity/run.ts` exits 0, with the skill-diff check reported green.
  </acceptance_criteria>
  <done>CHART-06's doc gap is closed: agent-skill.md documents ChartNode, the .NET twin is byte-identical, and the parity skill gate is green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: promote the ChartNode CHANGELOG draft into a real 4.1.0 heading + author 4.2.0 from scratch</name>
  <files>CHANGELOG.md</files>
  <read_first>
    @CHANGELOG.md (locate the exact current heading order and the `### ChartNode — Phase 12 (on `main`, unpublished)` subsection's exact span, per the `&lt;interfaces&gt;` block above)
    @.planning/design/non-blocking-actions.md (the whole design of record — the source of truth for what 4.2.0's entry must describe: `blocking:false`, the two-lane dispatch replacing the mutex, coalescing, the client-side lane-aware epoch, poll-fold, selection.action resurrection)
    @.planning/phases/14-non-blocking-dispatch-core/14-04-GAP-SUMMARY.md (the CR-01/CR-02 fixes folded into the shipped behavior — cite them as already-fixed, not as open issues)
    @.planning/phases/15-poll-fold-selection-action-resurrection/15-03-SUMMARY.md (confirms NBA-05/06/07 all verified — cite this as the evidentiary basis for the 4.2.0 entry's claims)
  </read_first>
  <action>
    Extract the `### ChartNode — Phase 12 (on `main`, unpublished)` subsection (its full span — lead
    paragraph through its own `### Migration` sub-subsection) OUT of its current position nested
    inside `## 1.12.0 / 1.10.0`, leaving that section otherwise completely untouched (do not touch the
    three legitimate Phase 8/9/10 subsections still nested there). Insert a new top-level heading `##
    4.1.0 / 4.1.0 — ChartNode: single-series bar-chart data-viz primitive (npm + NuGet)` directly ABOVE
    the existing `## 4.0.0 / 4.0.0` heading (at the very top of the file, right after the file's intro
    paragraphs and the first `---` divider) — this is the new newest entry. Write its intro paragraph
    in the style of the `## 4.0.0` / `## 1.12.0` entries: `**npm:** `4.1.0` (MINOR, from `4.0.0`) ·
    **NuGet:** `4.1.0` (MINOR, from `4.0.0`).` plus a summary sentence naming the `ChartNode` primitive,
    that it closes GitHub issue #6, and that the wire token stays `viewmodel-shell/1.0`. Immediately
    below the intro, add a `### Review fixes (pre-release visual verification)` subsection (mirroring
    1.12.0's precedent) describing the grid/tick/axis theme-token fix from commit `cfa3175` — the
    default Chart.js grid was a fixed color that clashed with dark themes; the fix wires the grid/
    border/tick colors to the `--vms-border`/`--vms-text-muted` theme tokens so the chart reads
    consistently in every theme, found during the operator's Phase 13 browser review. Then paste the
    extracted `### ChartNode — Phase 12 (on `main`, unpublished)` subsection content verbatim
    underneath (leave its own header text exactly as-is, matching the established convention that
    nested Phase-N subsection headers are never rewritten at promotion time — see the precedent in
    `&lt;interfaces&gt;`). End the `## 4.1.0` section with a `---` divider before the next heading.

    Then author a brand-new `## 4.2.0 / 4.2.0 — Non-blocking actions: `blocking:false` dispatch lane +
    poll-fold + selection.action resurrection (npm + NuGet)` heading directly ABOVE the new `## 4.1.0`
    heading (so 4.2.0 is the very first/newest entry in the file). No draft to promote here — write it
    fresh, grounded in the design doc + the three phase SUMMARYs in `&lt;context&gt;`. Intro: `**npm:**
    `4.2.0` (MINOR, from `4.1.0`) · **NuGet:** `4.2.0` (MINOR, from `4.1.0`).` plus a summary describing
    the milestone goal (a real concurrency model for the dispatch loop — non-blocking round trips that
    coexist with user actions instead of contending for a single global mutex) and that the wire token
    stays `viewmodel-shell/1.0` (fully additive — `ActionEvent.blocking?`/`ActionDescriptor.Blocking`
    is the only new wire-adjacent field, and it never actually rides the `_action` payload). Add a
    `### Added` subsection covering: `blocking?: boolean` on TS `ActionEvent` (omitted = `true`,
    byte-identical default) and the .NET twin `bool? Blocking` with `WhenWritingNull` (NOTE: NOT the
    usual F2 `WhenWritingDefault` pattern — because the default is `true` not `false`, the correct
    .NET shape is nullable + omit-on-null, byte-aligned with the TS optional-defaults-true field); the
    two-lane dispatch loop (`blockingInFlight`/`nonBlockingInFlight`) replacing the old single
    `dispatching` mutex, fixing the poll/user-action contention as a side effect; coalescing
    (`pendingNonBlockingRefire`, latest-wins, at most one non-blocking round trip in flight); the
    client-side lane-aware epoch (`dispatchSeq`/`appliedSeq` — a blocking response always applies
    unconditionally, a non-blocking response is discarded once a strictly newer response has applied
    OR a coalesced re-fire is already queued); `pollInterval` now formally documented as sugar over the
    non-blocking path (no behavior change from 3.x, just now provably contention-free); the
    `selection.action`/per-checkbox live-refresh pattern's correct resurrection (optimistic local
    `bind` write + `blocking:false` echo-back, closing the exact 0.15.0 rapid-toggle revert bug that
    got the old mechanism removed); `agent-skill.md`'s new `## Non-blocking actions (blocking:false)`
    section (already shipped in 15-02, cite it here). Add a `### Demo + tests` subsection citing the
    three new human-verification demos (`demo/NonBlockingActionBar-bun`, `demo/NonBlockingPoll-bun`,
    `demo/NonBlockingStaleness-bun`) and `demo/NonBlocking-VERIFICATION.md`'s operator sign-off (NBA-08)
    plus the vitest coverage added across Phases 14-15 (nonblocking-dispatch.test.ts, poll-fold.test.ts,
    checkbox-rapid-toggle.test.ts, blocking-propagation.test.ts). Add a `### Migration` subsection:
    "None needed — purely additive; `blocking` defaults to `true`, byte-identical to every existing
    app's behavior until it opts in." End with a `---` divider.

    Final file order (top to bottom): `## 4.2.0 / 4.2.0`, `## 4.1.0 / 4.1.0`, `## 4.0.0 / 4.0.0`, ...
    unchanged below.
  </action>
  <verify>
    <human-check>Read the final CHANGELOG.md top three headings and confirm order + content per acceptance criteria below.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## " CHANGELOG.md | head -3` shows, in order: `## 4.2.0 / 4.2.0`, `## 4.1.0 / 4.1.0`, `## 4.0.0 / 4.0.0`.
    - `grep -c "### ChartNode — Phase 12" CHANGELOG.md` is exactly `1` (moved, not duplicated).
    - The three legitimate Phase 8/9/10 subsections are STILL present, unchanged, nested under `## 1.12.0 / 1.10.0` (`grep -n "### Alignment enums\|### Switcher + cards minItem\|### Fits node" CHANGELOG.md` all still match, below the `## 1.12.0` heading).
    - `grep -n "cfa3175\|grid.*tick\|theme token" CHANGELOG.md` finds the review-fix paragraph under `## 4.1.0`.
    - `grep -n "NBA-0" CHANGELOG.md` — the `## 4.2.0` section's prose references the shipped behaviors (blocking lane, coalescing, epoch, poll-fold, selection.action) even if it doesn't literally spell out every NBA-ID.
    - `grep -c "^## 4\." CHANGELOG.md` is exactly `3` (4.2.0, 4.1.0, 4.0.0 — no accidental duplicate headings).
  </acceptance_criteria>
  <done>CHANGELOG.md correctly has ## 4.2.0 (newest) above ## 4.1.0 (correctly promoted from the stale 1.12.0-nested draft, including the review fix) above the existing ## 4.0.0, with no content duplicated or lost.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: MIGRATION.md entries for 4.1.0 + 4.2.0, then the full green-tree gate</name>
  <files>MIGRATION.md</files>
  <read_first>
    @MIGRATION.md (the exact heading style of the "additive, opt-in, no forced action" entries, e.g. `## Upgrading to `3.11.0` / `3.11.0` (npm + NuGet) — additive, opt-in, no forced action`)
  </read_first>
  <action>
    Add `## Upgrading to `4.1.0` / `4.1.0` (npm + NuGet) — additive, opt-in, no forced action` at the
    TOP of MIGRATION.md (above the existing `4.0.0` entry, matching CHANGELOG's newest-first order):
    nothing to do to keep working; `ChartNode` is a new optional leaf node, existing trees/agents are
    unaffected; consumers who want to RENDER a chart must install the optional `chart.js` peer
    dependency (`npm install chart.js`), everyone else needs nothing. Add `## Upgrading to `4.2.0` /
    `4.2.0` (npm + NuGet) — additive, opt-in, no forced action` directly ABOVE the `4.1.0` entry just
    added (4.2.0 is newer, so it goes first): nothing to do to keep working; `blocking` defaults to `true` on every existing
    action, byte-identical behavior; opt into a non-blocking round trip only where you want one (a
    live-selection refresh, a polling view) by adding `blocking: false` to that specific action's
    `ActionEvent`. Note `pollInterval` behavior is unchanged from the consumer's point of view (it
    already existed) — this release only proves/fixes its underlying contention-free implementation.

    Then run the FULL green-tree gate, in this exact order, from the repo root (`cd
    /home/thenasty/ViewModelShell`), confirming each exits 0 before moving to the next: `cd
    viewmodel-shell && npx vitest run && cd ..`; `cd viewmodel-shell && npm run check:core-globals &&
    cd ..`; `bun run parity/run.ts`; `~/.dotnet/dotnet test viewmodel-shell-dotnet/Tests`; then for
    EVERY `*.Tests.csproj` under `demo/` (`find demo -name '*.Tests.csproj'`), `~/.dotnet/dotnet test
    &lt;path&gt;`. If ANY step is red, STOP — do not proceed to 16-06 until it is green (per AGENTS.md's
    "NEVER PUBLISH OR PUSH ANYTHING BROKEN" rule — a pre-existing red test is not an exception).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx vitest run && npm run check:core-globals</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Upgrading to \`4\." MIGRATION.md | head -2` shows `4.2.0` above `4.1.0`.
    - Both new MIGRATION.md entries state plainly that no consumer action is required.
    - `cd viewmodel-shell && npx vitest run` exits 0.
    - `cd viewmodel-shell && npm run check:core-globals` exits 0.
    - `bun run parity/run.ts` (repo root) exits 0.
    - `~/.dotnet/dotnet test viewmodel-shell-dotnet/Tests` exits 0.
    - `~/.dotnet/dotnet test` on every `demo/**/*.Tests.csproj` exits 0 for each.
  </acceptance_criteria>
  <done>MIGRATION.md has both new entries; the full repo-wide test/parity/guard suite is green at the exact commit 16-06 will version-bump and tag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer's local `.env` (NPM_TOKEN / NUGET_API_KEY) → this plan's shell environment | This plan itself performs NO publish/auth action (that's 16-06) — it is pure documentation editing + running the existing test/parity/guard suites. No credential is read or required here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Tampering | CHANGELOG.md surgery (extracting/re-homing the ChartNode draft) | mitigate | Task 2's acceptance criteria explicitly grep-verify the ChartNode subsection appears EXACTLY ONCE post-edit and that the three legitimate Phase 8/9/10 subsections are untouched — a botched extraction (duplication or content loss) fails the gate before 16-06 ever runs. |
| T-16-10 | Repudiation | attributing the grid/tick fix to the wrong commit/release | mitigate | Task 2 requires citing the exact commit hash (`cfa3175`) confirmed via `git show` in `&lt;read_first&gt;`, not a paraphrase from memory. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan (documentation edits + running the existing test suite). |
</threat_model>

<verification>
- `diff viewmodel-shell/agent-skill.md viewmodel-shell-dotnet/AgentSkill.md` — empty (byte-identical).
- `bun run parity/run.ts` — green, including the skill-diff check.
- `grep -n "^## " CHANGELOG.md | head -3` — `4.2.0`, `4.1.0`, `4.0.0` in that order, no duplication.
- Full green-tree gate (vitest, check:core-globals, parity, both .NET test suites) — all exit 0.
</verification>

<success_criteria>
- CHART-06 is fully closed: ChartNode documented in agent-skill.md, byte-identical to the .NET twin, parity green.
- CHANGELOG.md and MIGRATION.md correctly and completely describe both 4.1.0 and 4.2.0, in the right order, with nothing lost or duplicated from the ChartNode extraction.
- The repo is green-tree-clean at the exact commit 16-06 will tag — no surprises during the actual publish ceremony.
</success_criteria>

<output>
Create `.planning/phases/16-test-apps-human-verification-release/16-05-SUMMARY.md` when done
</output>
