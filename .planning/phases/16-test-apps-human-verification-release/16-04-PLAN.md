---
phase: 16-test-apps-human-verification-release
plan: 04
type: execute
wave: 2
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - demo/NonBlocking-VERIFICATION.md
autonomous: false
requirements: [NBA-08]

must_haves:
  truths:
    - "A single markdown file exists at demo/NonBlocking-VERIFICATION.md giving exact run + tailnet-serve instructions for all three demos and a numbered 'trigger X, then Y, expect Z' script per scenario."
    - "The operator has actually run all three demos over the tailnet and confirmed rapid-toggle (no revert, no drop), poll-coexistence (click honored during a poll round trip, poll keeps ticking), and staleness-discard (fast value never reverts) all behave as specified — NBA-08's own verification method (human review, not assumed)."
  artifacts:
    - path: "demo/NonBlocking-VERIFICATION.md"
      provides: "run instructions (bun install / bun run serve / PORT env) + tailnet URLs (100.113.23.63:3008/3009/3010) + the numbered trigger/expect script for all three scenarios"
      contains: "100.113.23.63"
  key_links:
    - from: "demo/NonBlocking-VERIFICATION.md"
      to: "demo/NonBlockingActionBar-bun, demo/NonBlockingPoll-bun, demo/NonBlockingStaleness-bun"
      via: "exact `bun run serve` + tailnet URL per demo, referenced by directory name and port"
      pattern: "NonBlocking(ActionBar|Poll|Staleness)-bun"
---

<objective>
Write the ONE combined, step-by-step verification script covering all three NBA-08 demo apps built
in 16-01/16-02/16-03, then run the operator sign-off checkpoint gating entry into the release
(16-05/16-06). This is the phase's human-verification deliverable made concrete: exact run
instructions, exact tailnet URLs, and a numbered "trigger X, then Y, expect Z" sequence per scenario
so the operator (and any future re-verifier) knows precisely what "it works" means for a
concurrency/timing feature that cannot be asserted by vitest.

Purpose: satisfy NBA-08 in full (the three demos existed since wave 1; this plan is what proves the
operator actually exercised them and confirmed the specified behavior) and act as the explicit,
blocking gate the release plans (16-05/16-06) depend on — no release work starts before this
checkpoint is signed off.

Output: `demo/NonBlocking-VERIFICATION.md` (a new top-level file under `demo/`, sibling to the three
app directories it documents — discoverable the same way `demo/Tasks-fullstack-bun/README.md`
documents its own app) plus a recorded operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/design/non-blocking-actions.md
@.planning/phases/16-test-apps-human-verification-release/16-01-PLAN.md
@.planning/phases/16-test-apps-human-verification-release/16-02-PLAN.md
@.planning/phases/16-test-apps-human-verification-release/16-03-PLAN.md
@demo/Tasks-fullstack-bun/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: write the combined trigger/expect verification script</name>
  <files>demo/NonBlocking-VERIFICATION.md</files>
  <read_first>
    @.planning/phases/16-test-apps-human-verification-release/16-01-SUMMARY.md (confirm the actual seeded row data / delay constants landed as planned — cite the REAL values, not the plan's proposal, in case anything shifted during execution)
    @.planning/phases/16-test-apps-human-verification-release/16-02-SUMMARY.md (confirm actual `pollInterval`/`POLL_DELAY_MS` values)
    @.planning/phases/16-test-apps-human-verification-release/16-03-SUMMARY.md (confirm actual `BG_CHECK_DELAY_MS`/`FAST_DELAY_MS` values)
    @demo/NonBlockingActionBar-bun/server.ts, @demo/NonBlockingPoll-bun/server.ts, @demo/NonBlockingStaleness-bun/server.ts (read the actual shipped constants/row seed directly — the script's numbers must match the running code exactly, not the plan's proposal)
  </read_first>
  <action>
    Write `demo/NonBlocking-VERIFICATION.md` with these sections, in order. **"Run all three demos"**:
    for each of the three directories, the exact commands (`cd demo/&lt;Dir&gt;-bun && bun install &&
    bun run serve`, noting `PORT` env override if the operator wants a different port than the
    baked-in default) and the tailnet URL to open (`http://100.113.23.63:3008/`,
    `:3009/`, `:3010/` — one per demo, matching the ports fixed in 16-01/02/03's server.ts). Note that
    Bun's default `hostname` is `0.0.0.0`, so no extra bind configuration is needed for the tailnet URL
    to work — state this explicitly so a future re-verifier isn't confused. **"Scenario 1 — Selection
    → Live Action Bar"**: a numbered list — (1) open the tailnet URL; (2) check two "pending" rows in
    quick succession, confirm neither box visually reverts and the action bar's Approve/Reject enable
    within about a second; (3) click "Clear Selection", then check the locked row TOGETHER WITH one
    pending row, and IMMEDIATELY (within well under a second — before the action bar visibly updates)
    click "Approve Selected"; expect a visible rejection message naming the locked row, NOT a silent
    success and NOT a crash; (4) click "Reset Demo" to restore the seed for a repeat run. **"Scenario 2
    — Poll + User Coexistence"**: (1) open the tailnet URL and watch "Poll ticks" increment on its own
    within a couple of seconds with no action needed; (2) click "Click me" five times in quick
    succession; expect "Button clicks" to become 5 immediately, with NO delay waiting on the poll;
    (3) keep watching for another 5-10 seconds; expect "Poll ticks" to keep incrementing on its own
    cadence, unaffected by the clicks; (4) note for the record: pre-Phase-14 this codebase had a single
    global dispatch mutex where a poll in flight would have silently dropped the click (or vice versa)
    — that failure mode no longer exists to literally reproduce, so this step is the direct
    confirmation of its absence, not a toggle between old/new behavior. **"Scenario 3 — Out-of-Order
    Staleness"**: (1) open the tailnet URL, confirm the initial value reads "(no update yet)"; (2)
    click "① Start slow background check (3s)"; (3) within about a second, click "② Set value
    instantly (fast)"; expect the displayed value to become the fast/user result almost immediately;
    (4) WITHOUT clicking anything else, wait at least 4 more seconds (long enough for the 3s slow
    response to have arrived); expect the displayed value to remain the fast/user result — it must NOT
    revert to the background/slow result; (5) click "Reset Demo" to repeat. **"Sign-off"**: a line for
    the operator to record the date and outcome (pass / issues found, with a description).

    Cite the EXACT constants/seed data as they landed (read from the actual `server.ts` files per
    `&lt;read_first&gt;`, not the plan's proposed values, in case Tasks 2 of 16-01/02/03 made a small
    deviation during execution).
  </action>
  <verify>
    <automated>test -f demo/NonBlocking-VERIFICATION.md && grep -c "100.113.23.63" demo/NonBlocking-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " demo/NonBlocking-VERIFICATION.md` is at least 5 (Run instructions + 3 scenarios + Sign-off).
    - `grep -n "3008\|3009\|3010" demo/NonBlocking-VERIFICATION.md` matches all three ports.
    - `grep -n "100.113.23.63" demo/NonBlocking-VERIFICATION.md` matches at least 3 times (one tailnet URL per demo).
    - The three scenario sections each contain a numbered "expect" outcome the operator can objectively confirm or deny (no vague "it should feel right" language).
  </acceptance_criteria>
  <done>demo/NonBlocking-VERIFICATION.md exists with exact run instructions, tailnet URLs, and a numbered trigger/expect script per scenario, matching the actual shipped constants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: operator sign-off — run all three demos over the tailnet and confirm NBA-08</name>
  <what-built>
    Three runnable demo apps (selection→live action bar, poll+user coexistence, out-of-order
    staleness) plus `demo/NonBlocking-VERIFICATION.md`, the combined step-by-step script for all
    three.
  </what-built>
  <how-to-verify>
    Follow `demo/NonBlocking-VERIFICATION.md` exactly, in order: start all three demos (`bun install &&
    bun run serve` in each of `demo/NonBlockingActionBar-bun/`, `demo/NonBlockingPoll-bun/`,
    `demo/NonBlockingStaleness-bun/`), open each at its tailnet URL
    (`http://100.113.23.63:3008/`, `:3009/`, `:3010/`), and work through all three numbered
    scenario scripts. Confirm every "expect" outcome in the script holds: (1) rapid checkbox toggles
    never revert and a race-to-invalidity click is rejected with a visible message, not silently
    applied or crashed; (2) a blocking click during an in-flight poll round trip lands immediately and
    the poll keeps ticking afterward; (3) the fast value never reverts once the slow, late response
    finally arrives.
  </how-to-verify>
  <resume-signal>Type "approved" once all three scenarios behave as specified, or describe exactly which expected outcome did not hold (which scenario, which step, what was observed instead).</resume-signal>
  <files>demo/NonBlockingActionBar-bun, demo/NonBlockingPoll-bun, demo/NonBlockingStaleness-bun, demo/NonBlocking-VERIFICATION.md (no new edits — this task runs and reviews what Tasks 16-01/02/03 and this plan's Task 1 already produced)</files>
  <action>This is a blocking operator-gated checkpoint — do NOT auto-run it. Start all three demos, open each at its tailnet URL, and hand off to the operator to work through `demo/NonBlocking-VERIFICATION.md`'s three numbered scenario scripts exactly as written in `&lt;how-to-verify&gt;` above. Do not proceed to 16-05/16-06 until the operator explicitly confirms every "expect" outcome held, per the resume-signal.</action>
  <verify>Operator-run: all three demos started (`bun run serve` in each directory); each scenario script in `demo/NonBlocking-VERIFICATION.md` followed to completion; every numbered "expect" outcome confirmed or a specific deviation reported.</verify>
  <done>The operator has typed "approved" (or reported specific deviations to fix before re-attempting), confirming rapid-toggle, poll-coexistence, and staleness-discard all behave exactly as `demo/NonBlocking-VERIFICATION.md` specifies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan adds a documentation file and runs a human-verification checkpoint against already-built demos (16-01/02/03). No new code, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Tampering | the script itself giving inaccurate reproduction steps | mitigate | Task 1's `<read_first>` mandates citing the ACTUAL shipped constants from each `server.ts` (post-execution), not the plans' proposed values, so the script cannot silently drift from the running code. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan (documentation + a human checkpoint only). |
</threat_model>

<verification>
- `demo/NonBlocking-VERIFICATION.md` exists, has all required sections, and cites the real shipped ports/constants.
- The operator sign-off checkpoint is explicitly resolved ("approved" or a described gap) before 16-05/16-06 proceed.
</verification>

<success_criteria>
- NBA-08 is fully satisfied: the three demos exist, the combined script exists, and the operator has personally confirmed rapid-toggle, poll-coexistence, and staleness-discard all behave as specified.
- The release plans (16-05/16-06) are unblocked only after this checkpoint resolves "approved".
</success_criteria>

<output>
Create `.planning/phases/16-test-apps-human-verification-release/16-04-SUMMARY.md` when done
</output>
