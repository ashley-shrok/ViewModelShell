---
phase: 10-fits-node
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - viewmodel-shell/src/tui.tsx
  - demo/FeatureProbe-bun/handler.ts
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
  - parity/fixtures/feature-probe.json
  - demo/Showcase/frontend/src/main.ts
  - CHANGELOG.md
autonomous: true
requirements: [FITS-02, FITS-03]

must_haves:
  truths:
    - "The TUI node dispatch renders a fits node as its LAST child (the safe-fallback candidate) via the existing node renderer — a terminal has no pixel fit, so the guaranteed-fits candidate is the documented degradation; the TUI build does not break"
    - "Both FeatureProbe backends render fits nodes as static view-shape: one with axis omitted (proving omitted = absent on the wire), one with axis:\"both\", and 2-3 candidate children ordered preferred-first/fallback-last — captured by the existing GET steps with no new action arm"
    - "The WIRE {type:\"fits\", axis?, children} is byte-identical across .NET / Bun / Node — bun run parity/run.ts is byte-identical green; omitted axis is ABSENT on the wire, axis:\"both\" present; the measurement/selection is client-only and NOT part of parity"
    - "demo/Showcase renders a fits node whose first candidate is a wide horizontal row toolbar and whose last candidate is a compact stacked/switcher version — zero <style>, built only from ViewNodes"
    - "A `### Fits node — Phase 10` subsection is added under the EXISTING `## Unreleased` CHANGELOG heading; NO package.json/.csproj version bump, NO publish, NO tag"
  artifacts:
    - path: "viewmodel-shell/src/tui.tsx"
      provides: "a fits case in renderNode() rendering the LAST child; the conformance/pane walks recurse into fits's last child only"
      contains: "fits"
    - path: "demo/FeatureProbe-bun/handler.ts"
      provides: "static fits sections (axis omitted + axis:\"both\") appended to the page children"
      contains: "fits"
    - path: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs"
      provides: "byte-identical FitsNode sections appended to pageChildren"
      contains: "FitsNode"
    - path: "parity/fixtures/feature-probe.json"
      provides: "$comment documenting the static fits coverage (axis-omitted + axis:both), captured by existing GETs"
      contains: "fits"
    - path: "demo/Showcase/frontend/src/main.ts"
      provides: "a fits demo: wide-row toolbar first candidate, compact stacked last candidate"
      contains: "fits"
    - path: "CHANGELOG.md"
      provides: "the ### Fits node — Phase 10 Unreleased subsection"
      contains: "Fits node — Phase 10"
  key_links:
    - from: "demo/FeatureProbe-bun/handler.ts buildVm"
      to: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs BuildVm"
      via: "byte-identical fits sections (same axis presence, same ordered children)"
      pattern: "fits|FitsNode"
    - from: "viewmodel-shell/src/tui.tsx renderNode()"
      to: "viewmodel-shell/src/tui.tsx renderNode(lastChild)"
      via: "case \"fits\" renders node.children[node.children.length - 1]"
      pattern: "case \"fits\""
---

<objective>
Land the `fits` node's TUI degradation (FITS-02), its cross-backend wire parity (FITS-03), a minimal Showcase demo, and the deferred-release CHANGELOG record. The TUI renders a `fits` node as its LAST child (the safe-fallback candidate — a terminal has no pixel fit). Both FeatureProbe backends render `fits` nodes as static view-shape so the byte-identical wire (`{type:"fits", axis?, children}`) is parity-tested. The Showcase gains a `fits` demo (wide-row toolbar first → compact stacked last). The Phase-10 work is recorded under the existing `## Unreleased` CHANGELOG heading with NO version bump and NO publish.

Purpose: Complete FITS-02 (sensible non-browser degradation so `fits` doesn't break the TUI) and the wire half of FITS-03 (byte-identical TS/.NET serialization, parity-verified). The measurement/selection is client-only and explicitly NOT parity-tested — parity proves only that the node serializes identically across backends.

Output: A `fits` case in the TUI `renderNode()` switch (+ the conformance/pane walks handling fits); static `fits` sections in both FeatureProbe backends; the widened `feature-probe.json` `$comment`; a `fits` Showcase demo; and the `### Fits node — Phase 10` Unreleased CHANGELOG subsection. `bun run parity/run.ts` byte-identical green; the TUI build/tests pass. NO version bump, NO publish, NO tag (release deferred to Phase 11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-fits-node/10-CONTEXT.md
@.planning/ROADMAP.md
@.planning/phases/10-fits-node/10-01-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- This plan DEPENDS ON 10-01: the FitsNode TS type + .NET record + discriminator must already
     exist (10-01 ships them). This plan only CONSUMES the type in the TUI / demos / parity. -->

<!-- tui.tsx:
     - renderNode() dispatch switch is at ~L936-958 (case "page"|"section"|...; default → UnsupportedView).
       ADD `case "fits": ...` rendering the LAST child (the safe fallback) via the existing renderNode.
     - The TUI has THREE container-aware tree walks that switch on node.type and recurse into children:
       isPaneNode ~L543, countPanes/visit ~L582, and a focus-target walk ~L658. Because the TUI fits
       case renders ONLY the last child, these walks must treat a fits node as a TRANSPARENT wrapper
       around its LAST child: recurse into `node.children[node.children.length - 1]` only (NOT all
       children) so pane-counting / focus targeting matches what is actually rendered. Add a `fits`
       arm to each walk mirroring the `section` recursion idiom but visiting only the last child
       (isTopLevel=false, like section). -->

<!-- FeatureProbe precedent (Phase 9 switcher, this milestone): NEW node shapes render as STATIC
     view-shape in buildVm/BuildVm, captured by existing GET steps — NO new action arm.
     bun handler.ts buildVm: the switcher sections are built ~L304-331 and appended to the page
     children array ~L373-383 (bareSwitcherSection, ...switcherThresholdSections, switcherLimitSection,
     bareCardsSection, ...cardsMinItemSections, before buildTableSection(state)). Append the fits
     sections in that same list, after the cards sections, before buildTableSection.
     .NET BuildVm: the switcher sections are built ~L371-396 and added to pageChildren ~L429-441
     (pageChildren.Add(...) / .AddRange(...) before BuildTableSection). Append the fits sections in the
     SAME position/order as the bun side. -->

<!-- parity/fixtures/feature-probe.json: the top-level $comment documents the 1.11.0/1.12.0/1.13.0
     static coverage; static sections are captured by the existing GET steps — extend the $comment,
     do NOT add a new fixture FILE (so parity/backends.json needs no edit). -->

<!-- Showcase demo: demo/Showcase/frontend/src/main.ts componentsView() (~L217+) is a flat list of
     { type:"section", heading, children:[...] } blocks (zero <style>). The Switcher demo block is
     at ~L279-300. Add a "Fits (responsive selection)" section block after it, in the same idiom. -->

<!-- CHANGELOG.md: the `## Unreleased` heading is at L9; the Phase 8 subsection is at L13, the Phase 9
     subsection at L39 (### Switcher + cards minItem — Phase 9). ADD a new
     `### Fits node — Phase 10 (on `main`, unpublished)` subsection AFTER the Phase 9 subsection,
     mirroring the Phase 8/9 subsection structure (lead paragraph + Added / Not changed /
     Demo + tests / Migration sub-bullets). Do NOT bump any version line in package.json/.csproj. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TUI fits degradation (render the last child) + walk arms</name>
  <files>viewmodel-shell/src/tui.tsx</files>
  <read_first>
    viewmodel-shell/src/tui.tsx (renderNode() switch ~L936-958; the three container-aware walks: isPaneNode ~L543, countPanes/visit ~L582, the focus-target walk ~L658; the SectionView/renderNode child-render pattern ~L990-1018 for how a child is rendered via renderNode);
    viewmodel-shell/test/conformance.tui.test.ts and viewmodel-shell/test/tui-lifecycle.test.ts (the TUI test surface — confirm the fits case does not break rendering)
  </read_first>
  <action>
    In `viewmodel-shell/src/tui.tsx`:
    1. Add a `fits` case to the `renderNode()` dispatch switch (~L936-958), placed adjacent to the other container cases. It renders the node's LAST child (the safe-fallback candidate) via the existing `renderNode`:
       `case "fits": { const last = node.children[node.children.length - 1]; return last ? renderNode(last, ctx, key) : null; }`
       Add a comment documenting the DELIBERATE degradation rule: a terminal has no pixel fit, so the `fits` node degrades to its guaranteed-fits LAST candidate (FITS-02). The TUI is `@experimental`; the requirement is only that `fits` does not break it and degrades sensibly.
    2. For each of the THREE container-aware tree walks (isPaneNode ~L543, countPanes/visit ~L582, the focus-target walk ~L658), add a `fits` arm that treats the node as a TRANSPARENT wrapper around its LAST child — recurse into ONLY `node.children[node.children.length - 1]` (with the same `isTopLevel=false` posture as the `section` arm), so pane-counting / focus targeting matches what the TUI actually renders. Guard the empty-children case (skip if there are no children). Mirror the existing `section` arm's structure in each walk (e.g. in countPanes/visit: `else if (node.type === "fits") { const last = node.children[node.children.length - 1]; if (last) visit(last, false); }`). If a walk's helper has a different signature (e.g. isPaneNode returns a boolean rather than recursing), follow that helper's idiom — the GOAL is invariant: each walk sees only the LAST child of a fits node, consistent with the render.
    Do NOT change any non-fits behavior. The fits case must compile against the `FitsNode` type shipped in 10-01 (`node.children` is `ViewNode[]`).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && npx vitest run test/conformance.tui.test.ts test/tui-lifecycle.test.ts</automated>
  </verify>
  <done>
    The TUI `renderNode()` has a `fits` case rendering the LAST child via `renderNode`; the three container-aware walks each have a `fits` arm recursing into only the last child; the empty-children edge is guarded; `tsc --noEmit` passes and the TUI conformance + lifecycle tests are green (the fits degradation does not break the TUI build/render).
  </done>
</task>

<task type="auto">
  <name>Task 2: FeatureProbe (both backends) + parity fixture for the fits wire</name>
  <files>demo/FeatureProbe-bun/handler.ts, demo/FeatureProbe/AspNetCore/FeatureProbeController.cs, parity/fixtures/feature-probe.json</files>
  <read_first>
    demo/FeatureProbe-bun/handler.ts (the switcher sections ~L304-331 + the page children list ~L373-383 — the Phase-9 static-view-shape precedent; LinkNode/section node shapes used there);
    demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (the switcher sections ~L371-396 + the pageChildren list ~L429-441 + the PageNode return ~L442; the SectionNode/LinkNode constructor usage and named-arg style);
    parity/fixtures/feature-probe.json (the top-level $comment + GET steps — static sections captured by existing GETs);
    parity/run.ts and parity/normalize.ts (how GET responses are diffed — property order normalized, values + presence must match)
  </read_first>
  <action>
    Mirror the Phase-9 switcher precedent EXACTLY: render the new `fits` node as STATIC view-shape in both `buildVm`/`BuildVm`, captured by the existing GET steps (NO new action arm). The candidates must be ordinary existing nodes (e.g. a `layout:"row"` toolbar section first, a `layout:"stack"` compact section last) so the wire stays small and the ordering convention (preferred-first/fallback-last) is visible.

    In `demo/FeatureProbe-bun/handler.ts` `buildVm`, add fits sections and append them to the page `children` array (after the cards sections, before `buildTableSection(state)`). Cover at minimum:
    (a) a fits node with `axis` OMITTED and 2-3 candidate children — proves omitted = ABSENT on the wire. Wrap each fits in a labeled `{type:"section", heading:"fits (axis omitted)", children:[ <fits> ]}` for readability (the .NET side mirrors the same wrapping exactly).
    (b) a fits node with `axis:"both"` and 2-3 candidate children — proves the axis field present on the wire (`{type:"section", heading:"fits axis:both", children:[ <fits> ]}`).
    Use the documented ordering inside each fits: first candidate a wider layout (e.g. `{type:"section", layout:"row", children:[ ...LinkNodes ]}`), last candidate a compact one (e.g. `{type:"section", layout:"stack", children:[ ...same LinkNodes ]}`). Keep children minimal (LinkNodes with `external:false` explicit). Build the fits node shape as `{ type:"fits", axis:"both", children:[...] }` and `{ type:"fits", children:[...] }` (axis omitted).

    In `demo/FeatureProbe/AspNetCore/FeatureProbeController.cs` `BuildVm`, add the BYTE-IDENTICAL equivalents using `new FitsNode(Children: new ViewNode[]{ ... })` for the omitted-axis case and `new FitsNode(Children: new ViewNode[]{ ... }, Axis: "both")` for the axis:"both" case (Children is the first param per the 10-01 record shape; Axis is the named trailing optional). The candidate children (SectionNode/LinkNode) AND the wrapping sections (same headings) must match the TS exactly — same headings, same link labels/hrefs, same order, same wrapping. Append the fits sections to `pageChildren` in the SAME position/order as the bun side (after the cards sections, before `BuildTableSection(state)`). The serialized JSON of every new fits node MUST match the bun side field-for-field — omitted `axis` absent on the wire, `axis:"both"` present as the JSON string `"both"`, `type:"fits"` discriminator emitted by the `[JsonDerivedType]` registration.

    In `parity/fixtures/feature-probe.json`: extend the top-level `$comment` to note (mirroring the existing 1.11.0/1.12.0/1.13.0 sentences) that buildVm additionally renders the `fits` node as static view-shape — a `fits` with `axis` omitted (proving omitted = absent) and a `fits` with `axis:"both"`, each with preferred-first/fallback-last candidate children — captured by the existing GET steps so the byte-identical diff covers the `{type:"fits", axis?, children}` wire with no new step. Note explicitly that the CLIENT-SIDE measurement/selection is NOT part of parity (it's browser-only); parity proves only identical serialization. Do NOT add a new fixture FILE (so `parity/backends.json` needs no edit).
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
  </verify>
  <done>
    Both FeatureProbe backends render a `fits` with `axis` omitted and a `fits` with `axis:"both"` (each with preferred-first/fallback-last candidate children), byte-identically; `bun run parity/run.ts` is byte-identical green across dotnet-probe / bun-probe / node-probe (other backends unaffected); the fixture `$comment` documents the static fits coverage and that selection is client-only / not parity-tested.
  </done>
</task>

<task type="auto">
  <name>Task 3: Showcase fits demo + CHANGELOG Unreleased subsection</name>
  <files>demo/Showcase/frontend/src/main.ts, CHANGELOG.md</files>
  <read_first>
    demo/Showcase/frontend/src/main.ts (componentsView() ~L217+; the Switcher demo block ~L279-300 as the section-block idiom — zero <style>, built only from ViewNodes);
    CHANGELOG.md (the `## Unreleased` heading L9; the Phase 8 subsection L13 and Phase 9 subsection L39, for the subsection structure: lead paragraph + Added / Not changed / Demo + tests / Migration)
  </read_first>
  <action>
    In `demo/Showcase/frontend/src/main.ts` `componentsView()`, add a new section block after the Switcher demo block (~L300), in the same `{ type:"section", heading, children:[...] }` idiom (zero `<style>`). The block:
    - heading: "Fits (responsive selection)".
    - a muted `TextNode` explaining: resize the window — the `fits` node renders the FIRST candidate that fits and the LAST as the guaranteed-fits fallback; here a wide horizontal `row` toolbar shows when there's room, and a compact stacked version shows when there isn't — no `@media`, no app code.
    - a `{ type:"fits", children:[ <wideRowToolbar>, <compactStacked> ] }` node (axis omitted = horizontal). The FIRST candidate is a wide horizontal toolbar: `{ type:"section", layout:"row", children:[ ...several labeled LinkNodes ] }`. The LAST candidate is a compact stacked version: `{ type:"section", layout:"stack", children:[ ...the same labels, stacked ] }` (or `layout:"switcher"` if you prefer the atomic-flip compact form). Keep it minimal — the comprehensive review is Phase 11.
    Add a leading comment in the existing Showcase idiom (mirror the Switcher block's comment) noting this is the SwiftUI `ViewThatFits` port, the one non-pure-CSS primitive (selection is measured client-side), preferred-first/fallback-last ordering.

    In `CHANGELOG.md`, add a new `### Fits node — Phase 10 (on `main`, unpublished)` subsection AFTER the Phase 9 subsection (mirror the Phase 8/9 subsection structure). Include:
    - A lead paragraph: the `fits` node (SwiftUI `ViewThatFits` ported to the wire) — the one genuinely novel borrow and the ONLY non-pure-CSS layout primitive: it renders the first child whose intrinsic size fits the available container, else the next, decided CLIENT-SIDE at layout time via real measurement (a `ResizeObserver`-driven measure-and-pick in `BrowserAdapter`), zero viewport breakpoints — generalizing the `split`→`stack` collapse to arbitrary alternatives. Note it passes the governing P1 (intrinsic / zero-viewport-breakpoint) test; P2 applies to its `axis` enum.
    - `### Added`: the `FitsNode` (`type:"fits"`, `axis?:"horizontal"|"vertical"|"both"` [omitted = horizontal], ordered `children`) in the TS `ViewNode` union, mirrored as a .NET `FitsNode` record + `[JsonDerivedType(typeof(FitsNode),"fits")]` discriminator (`Axis` free-form `string?` with `[JsonIgnore(WhenWritingNull)]`); the `browser.ts` measure-and-pick renderer (first-that-fits, 1px tolerance, synchronous no-flash pick, `ResizeObserver` re-selection with a per-render leak-safe lifecycle); the `clientWidth===0`→last-child no-layout fallback; the minimal `.vms-fits { display:block; }` rule; the TUI degradation (renders the LAST child — terminals have no pixel fit, FITS-02). Document the preferred-first/fallback-last children ordering.
    - `### Not changed`: no wire-shape change (protocol token stays `viewmodel-shell/1.0` — a new optional-field-bearing ViewNode is additive); `agent-skill.md`/`AgentSkill.md` untouched; the core `index.ts` gains the TYPE only — all measurement lives in `browser.ts` so `check:core-globals` stays green.
    - `### Demo + tests`: FeatureProbe twins render a `fits` (axis omitted) + a `fits` (axis:"both") as static view-shape, parity byte-identical; `test/fits.test.ts` covers structure + no-layout fallback + axis acceptance + ResizeObserver disconnect-on-re-render and documents that real measure-and-pick is jsdom-untestable (verified by the Phase 11 human review); the Showcase gains a `fits` toolbar↔stacked demo.
    - `### Migration`: none — purely additive new node; existing trees/callers/agents unaffected. Note the known v1 limitation: a resize-triggered candidate switch rebuilds the fits subtree, so focus/caret/draft state inside a fits child may reset on a resize-switch (acceptable for v1).
    Do NOT bump any version in `package.json`/`.csproj`; do NOT publish or tag. The release is the consolidated Phase 11 lockstep bump.
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell/viewmodel-shell && npx tsc --noEmit && grep -q "Fits node — Phase 10" /home/thenasty/ViewModelShell/CHANGELOG.md && grep -q '"fits"' /home/thenasty/ViewModelShell/demo/Showcase/frontend/src/main.ts && echo OK</automated>
  </verify>
  <done>
    The Showcase `componentsView()` has a "Fits (responsive selection)" section with a `{type:"fits", children:[<wide row toolbar>, <compact stacked>]}` node (zero `<style>`); `CHANGELOG.md` has a `### Fits node — Phase 10` subsection under the existing `## Unreleased` heading (lead paragraph + Added / Not changed / Demo + tests / Migration), and NO version bump occurred in any `package.json`/`.csproj`; the Showcase frontend typechecks and the grep gate prints OK.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client wire | The `fits` node (`type:"fits"`, `axis`, `children`) crosses the wire from both backends; parity proves identical serialization. The TUI and Showcase consume the node client-side. No `axis` value is interpolated into a class name (the `.vms-fits` class is static), so there is no class-injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | FeatureProbe `fits` static view-shape across backends | accept | The fits sections are static, server-authored view-shape (no user input); parity verifies the two backends emit byte-identical wire. A hostile server already controls the view tree; rendering a `fits` adds no new capability. `axis` is a closed union (compile error off-union on honest backends) and is not interpolated into class names. No runtime sanitization added — consistent with the Phase 8/9 static-view-shape precedent (T-08-01 / T-09-01). |
| T-10-04 | Denial of Service | TUI `fits` walk recursion | accept | The TUI `fits` case + walk arms recurse into only the LAST child (a single node), not all children — no fan-out amplification beyond what a `section` already does. The empty-children edge is guarded. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure source edits to existing files; no new dependencies. |
</threat_model>

<verification>
- `cd viewmodel-shell && npx tsc --noEmit` — TS (incl. tui.tsx fits case + walk arms) compiles against the 10-01 `FitsNode` type.
- `cd viewmodel-shell && npx vitest run` — full vitest suite green (TUI conformance + lifecycle unaffected by the fits degradation; no regressions).
- `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` — byte-identical green across .NET / Bun / Node, including the new `fits` sections (axis omitted + axis:"both").
- `grep -q "Fits node — Phase 10" CHANGELOG.md` — the Unreleased subsection exists; confirm via `git diff -- viewmodel-shell/package.json viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` that NO version line changed.
</verification>

<success_criteria>
- The TUI `renderNode()` has a `fits` case rendering the LAST child (FITS-02 degradation) and the three container-aware walks recurse into only the last child; the TUI build + conformance/lifecycle tests pass.
- Both FeatureProbe backends render a `fits` with `axis` omitted (absent on the wire) and a `fits` with `axis:"both"` (present), byte-identically; `bun run parity/run.ts` is byte-identical green; the fixture `$comment` documents the static fits coverage and that selection is client-only / not parity-tested (FITS-03 wire half).
- The Showcase has a minimal `fits` demo (wide-row toolbar first → compact stacked last), zero `<style>`.
- `CHANGELOG.md` has the `### Fits node — Phase 10` subsection under the existing `## Unreleased` heading; NO version bump, NO publish, NO tag.
</success_criteria>

<output>
Create `.planning/phases/10-fits-node/10-02-SUMMARY.md` when done.
</output>
