---
phase: 03-default-design-system
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - viewmodel-shell/src/browser.ts
  - viewmodel-shell/styles/default.css
  - viewmodel-shell/test/theme-modifiers.test.ts
  - AGENTS.md
autonomous: true
requirements: [THEME-03, THEME-04, THEME-05]
must_haves:
  truths:
    - "Setting density:'compact' visibly tightens global spacing via the .vms-page--compact token remap; omitted/'comfortable' emits no modifier (THEME-03)"
    - "section variant:'card' renders a grouped surface (bg/border/radius/padding) via .vms-section--card; omitted emits no modifier (THEME-04)"
    - "density/variant omitted ⇒ class output byte-identical to pre-change ⇒ 7 existing parity fixtures stay 100% green (THEME-05, D-01)"
    - "The 11 styles/themes/*.css files require zero edits and still reskin byte-identically (THEME-05 sacred seam)"
    - "AGENTS.md node + CSS-class tables accurately list density/variant + .vms-page--compact/.vms-section--card"
  artifacts:
    - path: "viewmodel-shell/src/browser.ts"
      provides: "page/section modifier-class emission following the existing idiom"
      contains: "vms-page--compact"
    - path: "viewmodel-shell/styles/default.css"
      provides: ".vms-page--compact token remap + .vms-section--card surface"
      contains: ".vms-section--card"
    - path: "viewmodel-shell/test/theme-modifiers.test.ts"
      provides: "jsdom unit tests for class emission + omitted-field byte-identical guarantee"
      contains: "vms-page--compact"
  key_links:
    - from: "browser.ts page()/section()"
      to: "n.density / n.variant from src/index.ts types (Plan 02)"
      via: "existing `${n.x ? ` vms-y--${n.x}` : \"\"}` idiom"
      pattern: "n\\.density\\s*===\\s*\"compact\""
    - from: ".vms-page--compact"
      to: "--vms-space-sm/md/lg tokens (Plan 01)"
      via: "custom-property redeclaration scoped to the modifier"
      pattern: "\\.vms-page--compact\\s*\\{"
---

<objective>
Wire THEME-03/THEME-04 end to end on top of the Plan 01 scale tokens and Plan 02 type fields: (1) `browser.ts` emits `vms-page--compact` iff `density === "compact"` and `vms-section--card` iff `variant === "card"`, following the established modifier idiom; (2) `default.css` gains `.vms-page--compact` (a scoped scale-token remap, D-10) and `.vms-section--card` (a grouped surface reusing existing seam color/radius vars, no new tokens); (3) net-new jsdom unit tests assert class emission AND the omitted-field byte-identical guarantee; (4) AGENTS.md node + CSS-class tables get the new rows. Closes THEME-03, THEME-04, and verifies the THEME-05 regression invariant (parity 7/7 + zero theme-file edits).

Purpose: This is the wave-2 wiring plan — it depends on BOTH the scale tokens (density remaps them, D-10) and the type fields (the renderer reads them). It is also where THEME-05's regression guarantee is actively proven (parity green, theme files untouched).
Output: `browser.ts` + `default.css` + new `test/theme-modifiers.test.ts` + AGENTS.md tables; parity green; full TS test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-default-design-system/03-CONTEXT.md
@.planning/phases/03-default-design-system/03-UI-SPEC.md
@viewmodel-shell/src/browser.ts
@viewmodel-shell/styles/default.css
@viewmodel-shell/test/adapter-seam.test.ts
@AGENTS.md

<interfaces>
<!-- The exact renderer functions to modify + the established modifier idiom. -->
browser.ts page() (lines 194-205) — CURRENT:
```typescript
private page(n: PageNode, parent: HTMLElement, on: (a: ActionEvent) => void): void {
  const el = document.createElement("div");
  el.className = "vms-page";
  if (n.title) { /* h1 */ }
  this.kids(n.children, el, on);
  parent.appendChild(el);
}
```
browser.ts section() (lines 207-218) — CURRENT:
```typescript
private section(n: SectionNode, parent: HTMLElement, on: (a: ActionEvent) => void): void {
  const el = document.createElement("section");
  el.className = "vms-section";
  if (n.heading) { /* h2 */ }
  this.kids(n.children, el, on);
  parent.appendChild(el);
}
```
ESTABLISHED modifier idiom (browser.ts listItem L230):
  `li.className = \`vms-list-item${n.variant ? \` vms-list-item--${n.variant}\` : ""}\`;`
Density rule (D-04): emit ONLY for "compact" (not for "comfortable"/omitted). Card rule (D-04):
emit for "card".

Plan 02 types now available: PageNode.density?: "comfortable"|"compact"; SectionNode.variant?: "card".
Plan 01 tokens now available in :root: --vms-space-sm 0.75rem, --vms-space-md 1rem, --vms-space-lg 1.5rem;
--vms-surface, --vms-border, --vms-radius (pre-existing seam vars).

Test harness pattern (test/adapter-seam.test.ts): vitest + jsdom, imports `../src/browser.js`,
uses `new BrowserAdapter(freshContainer())`. New tests go in the same dir with this pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit vms-page--compact / vms-section--card in browser.ts (D-04 idiom)</name>
  <files>viewmodel-shell/src/browser.ts</files>
  <read_first>
    - viewmodel-shell/src/browser.ts (page() ~L194-205, section() ~L207-218, and the modifier idiom at listItem L230, button ~L459, modal ~L536 — replicate this exact ternary template-literal form)
    - viewmodel-shell/src/index.ts (PageNode.density / SectionNode.variant types added by Plan 02 — confirm field names)
    - .planning/phases/03-default-design-system/03-UI-SPEC.md (§Density Modifier, §Card Section Surface — "ONLY when density === 'compact'", "only when variant === 'card'")
    - .planning/phases/03-default-design-system/03-CONTEXT.md (D-04 — class emission not data-attributes; comfortable/omitted = byte-identical, no modifier)
  </read_first>
  <action>
Edit `viewmodel-shell/src/browser.ts`, two functions only.

In `page()` replace `el.className = "vms-page";` with the existing-idiom form that emits the modifier ONLY for compact (comfortable/omitted ⇒ exactly `"vms-page"`, byte-identical to today):
```typescript
el.className = `vms-page${n.density === "compact" ? " vms-page--compact" : ""}`;
```

In `section()` replace `el.className = "vms-section";` with:
```typescript
el.className = `vms-section${n.variant === "card" ? " vms-section--card" : ""}`;
```

Use `===` strict comparison against the exact literal strings `"compact"` / `"card"` (NOT truthiness of the whole field — `density === "comfortable"` must emit NO modifier per D-04). Do not change `n.title`/`n.heading` handling, `kids()`, or `appendChild`. Do not touch any other render function. Do not introduce any platform global (none needed — `document` is already legitimately used in browser.ts; the core-globals guard does not scan browser.ts).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('viewmodel-shell/src/browser.ts','utf8'); if(!/vms-page\$\{n\.density === \"compact\" \? \" vms-page--compact\" : \"\"\}/.test(c)){console.error('page() compact emission missing/wrong');process.exit(1)} if(!/vms-section\$\{n\.variant === \"card\" \? \" vms-section--card\" : \"\"\}/.test(c)){console.error('section() card emission missing/wrong');process.exit(1)} console.log('OK')"</automated>
    <automated>cd viewmodel-shell && npm run build && npm run check:core-globals</automated>
  </verify>
  <acceptance_criteria>
    - `browser.ts` `page()` sets `className` via `` `vms-page${n.density === "compact" ? " vms-page--compact" : ""}` ``
    - `browser.ts` `section()` sets `className` via `` `vms-section${n.variant === "card" ? " vms-section--card" : ""}` ``
    - Strict `=== "compact"` / `=== "card"` used (comfortable/omitted/other ⇒ no modifier)
    - `cd viewmodel-shell && npm run build` exits 0; `npm run check:core-globals` exits 0
  </acceptance_criteria>
  <done>page/section emit the BEM modifier only for compact/card via the established idiom; omitted/comfortable is byte-identical; tsc + core-globals green.</done>
</task>

<task type="auto">
  <name>Task 2: Add .vms-page--compact remap and .vms-section--card surface to default.css</name>
  <files>viewmodel-shell/styles/default.css</files>
  <read_first>
    - viewmodel-shell/styles/default.css (the :root scale tokens + `.vms-page` + `.vms-section` rules as left by Plan 01 — confirm `--vms-space-sm/md/lg`, `--vms-surface`, `--vms-border`, `--vms-radius` exist)
    - .planning/phases/03-default-design-system/03-UI-SPEC.md (§Density Modifier remap table — exact compact token deltas; §Card Section Surface table — exact card property values)
    - .planning/phases/03-default-design-system/03-CONTEXT.md (D-10 — density = scoped variable remap, not per-rule overrides; D-15 — no new color tokens for card)
  </read_first>
  <action>
Append two new rules to `viewmodel-shell/styles/default.css` (additive — do NOT modify existing rules; place near `.vms-page` / `.vms-section`).

(A) Density remap (D-10 — redeclare exactly the 3 rhythm tokens scoped to the modifier so all descendants inherit tighter spacing with zero per-rule churn; UI-SPEC §Density Modifier remap table; `--vms-space-2xs/xs/xl` and ALL type tokens stay unchanged — compact is density, not shrink):
```css
/* ── Density: compact (THEME-03, D-10) — remaps rhythm tokens only ── */
.vms-page--compact {
  --vms-space-sm: 0.5rem;
  --vms-space-md: 0.75rem;
  --vms-space-lg: 1rem;
}
```

(B) Card surface (THEME-04 — reuse existing seam color/radius vars; NO new color tokens, palette frozen D-15; UI-SPEC §Card Section Surface table values verbatim):
```css
/* ── Section variant: card (THEME-04) — grouped surface, existing seam vars ── */
.vms-section--card {
  background: var(--vms-surface);
  border: 1px solid var(--vms-border);
  border-radius: var(--vms-radius);
  padding: var(--vms-space-md);
}
```

Do NOT add a `gap` to `.vms-section--card` (it inherits `.vms-section`'s `gap: var(--vms-space-sm)` per UI-SPEC — content rhythm stays the coherent scale). Do NOT restyle `.vms-section__heading` inside a card (UI-SPEC: unchanged). Do NOT add new color/`--vms-*` tokens (D-15). Do NOT add `@media`.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('viewmodel-shell/styles/default.css','utf8'); const cp=c.match(/\.vms-page--compact\s*\{[^}]*\}/); const cd=c.match(/\.vms-section--card\s*\{[^}]*\}/); if(!cp||!/--vms-space-sm:\s*0\.5rem/.test(cp[0])||!/--vms-space-md:\s*0\.75rem/.test(cp[0])||!/--vms-space-lg:\s*1rem/.test(cp[0])){console.error('.vms-page--compact remap missing/wrong:',cp&&cp[0]);process.exit(1)} if(/--vms-space-2xs|--vms-space-xs:|--vms-space-xl|--vms-text-/.test(cp[0])){console.error('compact remapped a token it must not (only sm/md/lg)');process.exit(1)} if(!cd||!/background:\s*var\(--vms-surface\)/.test(cd[0])||!/border:\s*1px solid var\(--vms-border\)/.test(cd[0])||!/border-radius:\s*var\(--vms-radius\)/.test(cd[0])||!/padding:\s*var\(--vms-space-md\)/.test(cd[0])){console.error('.vms-section--card surface missing/wrong:',cd&&cd[0]);process.exit(1)} if(/@media/.test(c)){console.error('@media introduced');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `default.css` contains `.vms-page--compact { --vms-space-sm: 0.5rem; --vms-space-md: 0.75rem; --vms-space-lg: 1rem; }` (exactly those 3 token redeclarations)
    - `.vms-page--compact` does NOT redeclare `--vms-space-2xs`/`--vms-space-xs`/`--vms-space-xl` or any `--vms-text-*` token
    - `default.css` contains `.vms-section--card` with `background: var(--vms-surface)`, `border: 1px solid var(--vms-border)`, `border-radius: var(--vms-radius)`, `padding: var(--vms-space-md)`
    - `.vms-section--card` adds NO new color token and NO `gap` declaration
    - Existing `.vms-page`/`.vms-section` rules unchanged; zero `@media` in file
  </acceptance_criteria>
  <done>.vms-page--compact remaps exactly the 3 rhythm tokens; .vms-section--card is a grouped surface from existing seam vars; additive only; no new tokens; no media queries.</done>
</task>

<task type="auto">
  <name>Task 3: Add jsdom unit tests for class emission + omitted-field byte-identical guarantee; run full test + parity; update AGENTS.md tables</name>
  <files>viewmodel-shell/test/theme-modifiers.test.ts, AGENTS.md</files>
  <read_first>
    - viewmodel-shell/test/adapter-seam.test.ts (the established vitest+jsdom harness pattern — imports, freshContainer(), BrowserAdapter usage)
    - viewmodel-shell/src/browser.ts (page()/section() as edited in Task 1 — to assert emitted classes)
    - .planning/phases/03-default-design-system/03-UI-SPEC.md (§Regression / Verification Contract items 4 & 5 — the exact regression invariants to assert; §Copywriting Contract — AGENTS.md rows to add)
    - AGENTS.md (§"Node types" table ~L104-119, §"CSS classes emitted by BrowserAdapter" table ~L140-156 — current `page`/`section` rows)
    - parity/run.ts + parity/backends.json (how parity is invoked — the 7-fixture regression gate)
  </read_first>
  <action>
THREE sub-steps:

(A) Create `viewmodel-shell/test/theme-modifiers.test.ts` following the `adapter-seam.test.ts` harness pattern (vitest, jsdom, `import { BrowserAdapter } from "../src/browser.js";`, `import type { ViewNode } from "../src/index.js";`, a `freshContainer()` helper that appends a div to document.body). Assert ALL of:
  - `{ type: "page", children: [], density: "compact" }` ⇒ rendered root `.className` contains `vms-page--compact`
  - `{ type: "page", children: [], density: "comfortable" }` ⇒ root `.className` === `"vms-page"` (NO modifier — byte-identical guarantee, UI-SPEC regression item 4)
  - `{ type: "page", children: [] }` (density omitted) ⇒ root `.className` === `"vms-page"` (byte-identical)
  - `{ type: "section", children: [], variant: "card" }` ⇒ rendered section `.className` contains `vms-section--card`
  - `{ type: "section", children: [] }` (variant omitted) ⇒ section `.className` === `"vms-section"` (byte-identical, UI-SPEC regression item 4)
Render via `new BrowserAdapter(container).render(node, () => {})` then query `container.querySelector(".vms-page")` / `.vms-section` and assert `.className`. These are class-emission assertions only — NO computed pixel-value assertions (UI-SPEC regression item 5: D-08 normalization must break no test).

(B) Run the full regression gates and confirm green:
  - `cd viewmodel-shell && npm test` (full vitest suite incl. the new file + existing ~97 tests) — must exit 0
  - `cd parity && bun run run.ts` (the 7 existing fixtures: tasks, contacts, expenses, helpdesk, reorder, retro, feature-probe) — must report all backends byte-identical, exit 0. (Fixtures emit no density/variant ⇒ wire byte-identical ⇒ this proves the THEME-05/D-01 non-breaking guarantee. NO new parity fixture is added — deferred to Phase 4/5 per D-05.)
  - Confirm zero edits to `viewmodel-shell/styles/themes/*.css` (THEME-05 sacred seam): `git status --porcelain viewmodel-shell/styles/themes/` must be empty.

(C) Update `AGENTS.md` two tables for accuracy (Phase 3 scope only — full doc polish is Phase 5 EXAMPLES-03, do NOT over-edit):
  - §"Node types" table: `page` row note — append `; optional density: "comfortable" | "compact"`. `section` row note — append `; optional variant: "card" → vms-section--card`.
  - §"CSS classes emitted by BrowserAdapter" table: `page` row Classes — add `.vms-page--compact`. `section` row Classes — add `.vms-section--card`.
Keep edits minimal and table-local; do not restructure AGENTS.md.
  </action>
  <verify>
    <automated>cd viewmodel-shell && npm test</automated>
    <automated>cd parity && bun run run.ts</automated>
    <automated>node -e "const {execSync}=require('child_process'); const o=execSync('git status --porcelain viewmodel-shell/styles/themes/',{encoding:'utf8'}); if(o.trim()){console.error('THEME-05 VIOLATION — theme files edited:\\n'+o);process.exit(1)} const a=require('fs').readFileSync('AGENTS.md','utf8'); if(!a.includes('.vms-page--compact')||!a.includes('.vms-section--card')||!/density.*comfortable.*compact/.test(a)||!/variant.*card/.test(a)){console.error('AGENTS.md tables not updated for density/variant');process.exit(1)} const t=require('fs').readFileSync('viewmodel-shell/test/theme-modifiers.test.ts','utf8'); if(!t.includes('vms-page--compact')||!t.includes('vms-section--card')){console.error('new test file missing modifier assertions');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `viewmodel-shell/test/theme-modifiers.test.ts` exists, asserts: compact⇒modifier present; comfortable⇒className==="vms-page"; density omitted⇒className==="vms-page"; card⇒modifier present; variant omitted⇒className==="vms-section"
    - `cd viewmodel-shell && npm test` exits 0 (all existing ~97 tests + new file green — no computed-pixel assertions added)
    - `cd parity && bun run run.ts` exits 0, all 7 fixtures byte-identical across .NET/Bun/Node (proves D-01/THEME-05 non-breaking guarantee; no new fixture added)
    - `git status --porcelain viewmodel-shell/styles/themes/` is empty (THEME-05 sacred seam — zero theme-file edits)
    - `AGENTS.md` node-types table `page`/`section` rows mention `density`/`variant`; CSS-class table lists `.vms-page--compact` and `.vms-section--card`
  </acceptance_criteria>
  <done>jsdom tests prove emission + omitted-byte-identical; full vitest suite green; parity 7/7 green; zero theme-file edits; AGENTS.md tables accurate for shipped fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-emitted view tree → BrowserAdapter DOM render | Pre-existing boundary, unchanged in nature. The new code reads two optional presentational enum fields off the already-trusted node and conditionally appends a static, framework-authored class string. No new untrusted input is parsed; the field values never reach `innerHTML`, an attribute sink, or a URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | THEME-05 override-seam regression (the sacred invariant) | mitigate | Task 3 asserts `git status` shows zero edits to `styles/themes/*.css`; new CSS is purely additive (existing rules unchanged, asserted Task 2); the 7 parity fixtures stay byte-identical (Task 3). Active regression proof, not trust. |
| T-03-08 | Tampering | Non-breaking wire guarantee (D-01) | mitigate | jsdom tests assert omitted/comfortable ⇒ `className === "vms-page"` and omitted variant ⇒ `"vms-section"` (byte-identical); parity 7/7 green confirms no wire drift. |
| T-03-09 | Injection (XSS-adjacent) | Modifier class string built from `n.density`/`n.variant` | accept | The emitted substring is a *literal* (`" vms-page--compact"` / `" vms-section--card"`) chosen by a strict `=== "compact"`/`=== "card"` equality gate — the field value itself is never interpolated into the class, attribute, or markup. No injection vector even if the field carried a hostile string (it would simply fail the equality and emit no modifier). TS closed union (Plan 02) further constrains it. |

No high-severity threats. The change is conditional emission of a fixed class literal + additive CSS + tests; the sole security-relevant invariants are THEME-05 regression and the non-breaking guarantee, both actively proven (parity green + zero theme edits + jsdom byte-identical assertions).
</threat_model>

<verification>
- `browser.ts` emits `vms-page--compact`/`vms-section--card` via the strict-equality established idiom (Task 1)
- `default.css` has the `.vms-page--compact` 3-token remap + `.vms-section--card` surface, additive, no new tokens, no @media (Task 2)
- `test/theme-modifiers.test.ts` proves emission + omitted-field byte-identical (`className === "vms-page"`/`"vms-section"`) (Task 3)
- `cd viewmodel-shell && npm test` exits 0 (full suite + new file; no computed-pixel assertions)
- `cd parity && bun run run.ts` exits 0 — 7/7 fixtures byte-identical (THEME-05/D-01 regression proof; no new fixture)
- `git status --porcelain viewmodel-shell/styles/themes/` empty (zero theme-file edits — THEME-05 sacred seam)
- AGENTS.md node + CSS-class tables accurate for the shipped density/variant fields
</verification>

<success_criteria>
THEME-03: `density: "compact"` emits `.vms-page--compact` which remaps `--vms-space-sm/md/lg` tighter (visible global spacing change, zero app CSS); omitted/comfortable byte-identical.
THEME-04: `section variant: "card"` emits `.vms-section--card` rendering a grouped surface (bg/border/radius/padding from existing seam vars); omitted byte-identical.
THEME-05: 7 parity fixtures stay 100% green; the 11 theme files require zero edits and still reskin byte-identically; no override-seam behavior change — actively proven, regression-guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/03-default-design-system/03-03-SUMMARY.md`
</output>
