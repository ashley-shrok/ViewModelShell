---
phase: 03-default-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewmodel-shell/styles/default.css
autonomous: true
requirements: [THEME-01, THEME-02, THEME-05]
must_haves:
  truths:
    - "An app importing only default.css gets a centered, max-width (1080px) page with responsive horizontal padding and zero app CSS (THEME-01)"
    - "Every spacing/font-size literal in default.css references a coherent named scale variable (THEME-02)"
    - "Every pre-existing :root variable name and semantic is byte-identical; only --vms-text-muted's value changes #6b6b80→#9090a8 (D-17, THEME-05)"
    - "New --vms-space-*/--vms-text-*/--vms-page-max tokens are purely additive to :root (THEME-05)"
    - "Zero @media queries introduced; responsive padding is clamp()-based (D-13)"
  artifacts:
    - path: "viewmodel-shell/styles/default.css"
      provides: "Scale tokens, refactored literals, centered shell, AA-fixed muted text"
      contains: "--vms-space-md:"
  key_links:
    - from: ".vms-page rule"
      to: "--vms-page-max / --vms-space-* tokens"
      via: "max-width + margin-inline + padding-inline clamp + gap"
      pattern: "max-width:\\s*var\\(--vms-page-max\\)"
    - from: "literal spacing/font-size values"
      to: "--vms-space-* / --vms-text-* tokens"
      via: "var() reference per snapping ledger"
      pattern: "var\\(--vms-space-"
---

<objective>
Refactor the shipped `viewmodel-shell/styles/default.css` (458 lines) so that (a) `:root` gains a coherent additive spacing scale (`--vms-space-*`, 6 steps), type scale (`--vms-text-*`, 7 steps), and `--vms-page-max`; (b) every literal spacing/font-size value is replaced by the nearest scale variable per the UI-SPEC snapping ledger; (c) `.vms-page` becomes a centered, max-width, responsively-padded shell with zero media queries; (d) the one allowed default-value change `--vms-text-muted` `#6b6b80`→`#9090a8` (WCAG AA fix, D-17) is applied. THEME-01, THEME-02, and the additive-half of THEME-05.

Purpose: A coherent token surface is the prerequisite for the density remap (Plan 03, D-10) and is what makes "serviceable with zero app CSS" testable. The override seam stays byte-identical except the one D-17 value.
Output: Refactored `default.css` — additive `:root` tokens, literals→variables, centered shell, AA muted text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-default-design-system/03-CONTEXT.md
@.planning/phases/03-default-design-system/03-UI-SPEC.md
@viewmodel-shell/styles/default.css

<interfaces>
<!-- Existing :root variables (default.css lines 17-42) that MUST stay byte-identical
     except --vms-text-muted's value. Names/semantics are the THEME-05 sacred seam. -->
Existing :root tokens (DO NOT rename, DO NOT change values EXCEPT --vms-text-muted):
  --vms-bg #0f0f11, --vms-surface #18181c, --vms-surface-2 #222228, --vms-border #2e2e38,
  --vms-accent #7c6af7, --vms-accent-glow, --vms-accent-dim, --vms-text #e8e8f0,
  --vms-text-muted #6b6b80  ← THE ONLY value that changes (→ #9090a8, D-17),
  --vms-done-bg, --vms-done-text, --vms-error #e05a5a, --vms-error-glow, --vms-warning,
  --vms-priority-high, --vms-success, --vms-info, --vms-radius 10px, --vms-radius-sm 6px,
  --vms-font-body, --vms-font-head, --vms-font-mono, --vms-t 0.15s ease, --vms-color-scheme dark
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive scale + page-max tokens to :root and apply the D-17 AA fix</name>
  <files>viewmodel-shell/styles/default.css</files>
  <read_first>
    - viewmodel-shell/styles/default.css (lines 17-42 — the current :root block; current state, not assumptions)
    - .planning/phases/03-default-design-system/03-UI-SPEC.md (§Spacing Scale, §Typography, §Page Shell, §Color — the prescriptive token values; source of truth for every number)
    - .planning/phases/03-default-design-system/03-CONTEXT.md (D-06, D-07, D-09, D-12, D-15, D-17 — locked decisions)
  </read_first>
  <action>
Edit the `:root` block in `viewmodel-shell/styles/default.css` (lines 17-42). Make TWO kinds of change:

(A) Change exactly ONE existing value (D-17 — the only allowed palette change):
  `--vms-text-muted:  #6b6b80;`  →  `--vms-text-muted:  #9090a8;`
Do NOT rename it. Do NOT change any other existing `:root` value or name (sacred THEME-05 seam).

(B) ADD these new tokens to `:root` (purely additive — append after the existing variables, before the closing `}`). Use these EXACT values from UI-SPEC §Spacing Scale / §Typography / §Page Shell:

```css
  /* Spacing scale — modular ~1.5, base --vms-space-xs 0.5rem (D-06/D-07) */
  --vms-space-2xs:  0.25rem;
  --vms-space-xs:   0.5rem;
  --vms-space-sm:   0.75rem;
  --vms-space-md:   1rem;
  --vms-space-lg:   1.5rem;
  --vms-space-xl:   2.25rem;
  /* Type scale — all rem, modular ~1.2, base --vms-text-base 0.8125rem (D-06/D-09) */
  --vms-text-xs:    0.6875rem;
  --vms-text-sm:    0.75rem;
  --vms-text-base:  0.8125rem;
  --vms-text-md:    0.875rem;
  --vms-text-lg:    1rem;
  --vms-text-xl:    1.375rem;
  --vms-text-2xl:   2.25rem;
  /* Page shell (D-12) */
  --vms-page-max:   1080px;
```

Do NOT add `@media` queries. Do NOT touch `--vms-font-*` values (frozen, D-15).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('viewmodel-shell/styles/default.css','utf8'); const need=['--vms-space-2xs:','--vms-space-xs:','--vms-space-sm:','--vms-space-md:','--vms-space-lg:','--vms-space-xl:','--vms-text-xs:','--vms-text-sm:','--vms-text-base:','--vms-text-md:','--vms-text-lg:','--vms-text-xl:','--vms-text-2xl:','--vms-page-max:']; const miss=need.filter(t=>!c.includes(t)); if(miss.length){console.error('MISSING tokens:',miss);process.exit(1)} if(!/--vms-text-muted:\s*#9090a8/.test(c)){console.error('D-17 fix not applied');process.exit(1)} if(/#6b6b80/.test(c)){console.error('old muted #6b6b80 still present');process.exit(1)} if(!/--vms-bg:\s*#0f0f11/.test(c)||!/--vms-accent:\s*#7c6af7/.test(c)||!/--vms-radius:\s*10px/.test(c)){console.error('a frozen :root value changed');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `default.css` contains all 6 `--vms-space-*` token declarations with the exact rem values above
    - `default.css` contains all 7 `--vms-text-*` token declarations with the exact rem values above
    - `default.css` contains `--vms-page-max:   1080px;`
    - `grep '--vms-text-muted' default.css` shows `#9090a8`, NOT `#6b6b80`; the literal `#6b6b80` appears nowhere in the file
    - `--vms-bg #0f0f11`, `--vms-accent #7c6af7`, `--vms-radius 10px` (and all other pre-existing :root values/names) are byte-identical
    - File contains zero `@media` occurrences
  </acceptance_criteria>
  <done>:root carries the 6 spacing + 7 type + 1 page-max additive tokens; --vms-text-muted is #9090a8; every other pre-existing :root variable is byte-identical; no media queries.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all literal spacing/font-size values to scale variables per the snapping ledger</name>
  <files>viewmodel-shell/styles/default.css</files>
  <read_first>
    - viewmodel-shell/styles/default.css (the full file as edited by Task 1 — every rule with a literal rem/px spacing or font-size)
    - .planning/phases/03-default-design-system/03-UI-SPEC.md (§Spacing Scale snapping ledger table + Exceptions paragraph; §Typography token-snap table + Role table; these are the exact literal→token mappings)
    - .planning/phases/03-default-design-system/03-CONTEXT.md (D-08 — normalization/pixel-shift is accepted and intended, not a regression)
  </read_first>
  <action>
Replace literal spacing and font-size values throughout `viewmodel-shell/styles/default.css` with the scale `var()` references. Apply the UI-SPEC snapping ledger VERBATIM.

SPACING ledger (UI-SPEC §Spacing Scale — replace the literal with the token; pixel deltas are accepted per D-08):
  - `.vms-page` `gap: 1.5rem` → `gap: var(--vms-space-lg)`
  - `.vms-section` `gap: 0.75rem` → `gap: var(--vms-space-sm)`
  - `.vms-form` `gap: 0.75rem` → `gap: var(--vms-space-sm)`
  - `.vms-field` `gap: 0.3rem` → `gap: var(--vms-space-2xs)` (Δ −0.05rem, accepted)
  - `.vms-list` `gap: 0.375rem` → `gap: var(--vms-space-xs)` (Δ +0.125rem, accepted)
  - `.vms-stat-bar` `gap: 1.5rem` → `gap: var(--vms-space-lg)`
  - `.vms-stat-bar__item` `gap: 0.4rem` → `gap: var(--vms-space-xs)` (Δ +0.1rem, accepted)
  - `.vms-button` `padding: 0.55rem 0.9rem` → `padding: var(--vms-space-sm) var(--vms-space-md)` (0.75 1rem, accepted)
  - `.vms-button--primary` `padding: 0.65rem 1.25rem` → `padding: var(--vms-space-sm) var(--vms-space-lg)` (0.75 1.5rem, accepted)
  - `.vms-button--secondary` `padding: 0.35rem 0.7rem` → `padding: var(--vms-space-2xs) var(--vms-space-sm)` (snap 0.35→0.25=2xs, 0.7→0.75=sm)
  - `.vms-list-item .vms-button--danger` `padding: 0.25rem 0.4rem` → `padding: var(--vms-space-2xs) var(--vms-space-xs)`
  - `.vms-field__input` `padding: 0.65rem 1rem` → `padding: var(--vms-space-sm) var(--vms-space-md)`
  - `select.vms-field__input` `padding-right: 2.25rem` → `padding-right: var(--vms-space-xl)`
  - `.vms-field--checkbox` `gap: 0.5rem` → `gap: var(--vms-space-xs)`
  - `.vms-checkbox__label` `margin-left: 0.5rem` → `margin-left: var(--vms-space-xs)`
  - `.vms-list-item` `gap: 0.75rem` → `gap: var(--vms-space-sm)`; `padding: 0.75rem 1rem` → `padding: var(--vms-space-sm) var(--vms-space-md)`
  - `.vms-tabs` `gap: 0.25rem` → `gap: var(--vms-space-2xs)`; `padding: 0.25rem` → `padding: var(--vms-space-2xs)`
  - `.vms-tabs__tab` `padding: 0.4rem 0` → `padding: var(--vms-space-xs) 0`
  - `.vms-modal-backdrop` `padding: 1rem` → `padding: var(--vms-space-md)`
  - `.vms-modal__header` `padding: 1rem 1.25rem` → `padding: var(--vms-space-md) var(--vms-space-lg)` (1.25→1.5=lg snap)
  - `.vms-modal__body` `padding: 1.25rem` → `padding: var(--vms-space-lg)`; `gap: 0.75rem` → `gap: var(--vms-space-sm)`
  - `.vms-modal__footer` `padding: 0.75rem 1.25rem` → `padding: var(--vms-space-sm) var(--vms-space-lg)`; `gap: 0.5rem` → `gap: var(--vms-space-xs)`
  - `.vms-modal__footer .vms-button--primary` `padding: 0.55rem 1rem` → `padding: var(--vms-space-sm) var(--vms-space-md)`
  - `.vms-modal__close` `padding: 0.25rem 0.5rem` → `padding: var(--vms-space-2xs) var(--vms-space-xs)`
  - `.vms-page__title` `padding-bottom: 0.75rem` → `padding-bottom: var(--vms-space-sm)`
  - `.vms-table__th` `padding: 0.6rem 0.9rem` → `padding: var(--vms-space-sm) var(--vms-space-md)`
  - `.vms-table__td` `padding: 0.6rem 0.9rem` → `padding: var(--vms-space-sm) var(--vms-space-md)`
  - `.vms-table__filter-row th` `padding: 0.4rem 0.5rem` → `padding: var(--vms-space-xs) var(--vms-space-xs)`
  - `.vms-table__filter-input` `padding: 0.3rem 0.5rem` → `padding: var(--vms-space-2xs) var(--vms-space-xs)`
  - `.vms-error` `padding: 0.65rem 1rem` → `padding: var(--vms-space-sm) var(--vms-space-md)`; `margin-bottom: 1rem` → `margin-bottom: var(--vms-space-md)`

TYPE ledger (UI-SPEC §Typography snap table — replace font-size literal with token; convert ALL px→rem token):
  - `.vms-page__title` `font-size: 2.25rem` → `font-size: var(--vms-text-2xl)`
  - `.vms-section__heading` `font-size: 11px` → `font-size: var(--vms-text-xs)`
  - `.vms-stat-bar__value` `font-size: 16px` → `font-size: var(--vms-text-lg)`
  - `.vms-stat-bar__label` `font-size: 11px` → `font-size: var(--vms-text-xs)`
  - `.vms-field__label` `font-size: 12px` → `font-size: var(--vms-text-sm)`
  - `.vms-field__input` `font-size: 14px` → `font-size: var(--vms-text-md)`
  - `.vms-field__input--code` `font-size: 13px` → `font-size: var(--vms-text-base)` (keep `line-height: 1.5` literal — functional, UI-SPEC)
  - `.vms-checkbox__label` `font-size: 13px` → `font-size: var(--vms-text-base)`
  - `.vms-button` `font-size: 13px` → `font-size: var(--vms-text-base)`
  - `.vms-button--secondary` `font-size: 12px` → `font-size: var(--vms-text-sm)`
  - `.vms-tabs__tab` `font-size: 12px` → `font-size: var(--vms-text-sm)`
  - `.vms-text--heading` `font-size: 1.5rem` → `font-size: var(--vms-text-xl)`
  - `.vms-text--subheading` `font-size: 1.05rem` → `font-size: var(--vms-text-lg)`
  - `.vms-text--muted` `font-size: 12px` → `font-size: var(--vms-text-sm)`
  - `.vms-text--error` `font-size: 13px` → `font-size: var(--vms-text-base)`
  - `.vms-modal__title` `font-size: 1.25rem` → `font-size: var(--vms-text-xl)`
  - `.vms-modal__close` `font-size: 1.1rem` → `font-size: var(--vms-text-xl)`
  - `.vms-table` `font-size: 13px` → `font-size: var(--vms-text-base)`
  - `.vms-table__th` `font-size: 11px` → `font-size: var(--vms-text-xs)`
  - `.vms-table__filter-input` `font-size: 12px` → `font-size: var(--vms-text-sm)`
  - `.vms-error` `font-size: 13px` → `font-size: var(--vms-text-base)`
  - `.vms-error button` `font-size: 16px` → `font-size: var(--vms-text-lg)`

EXCEPTIONS — leave these LITERAL (UI-SPEC §Spacing Scale Exceptions; component geometry, not rhythm):
  - `.vms-checkbox*` `18px`/`18px` hit target, `5px`/`9px`/`4px`/`1px` tick geometry, `1.5px`/`5px` border-radius
  - `.vms-progress` `3px` height, `99px` pill radius (both `.vms-progress` and `__bar`)
  - `.vms-modal` `max-width: 520px`, `max-height: 80vh`; `.vms-modal--narrow 400px`, `--wide 800px`, `--fullscreen 95vw/95vh`; `.vms-modal-backdrop` z-index/inset
  - `.vms-radius-sm`-based `border-radius` (already a token), `letter-spacing` values, `line-height` values, `transition`/animation timings, `1px solid` border widths
Do NOT introduce `@media`. Do NOT alter selectors, colors, or non-spacing/non-font-size properties.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('viewmodel-shell/styles/default.css','utf8'); const bad=[]; if(/gap:\s*1\.5rem/.test(c))bad.push('literal gap:1.5rem remains'); if(/gap:\s*0\.75rem/.test(c))bad.push('literal gap:0.75rem remains'); if(/font-size:\s*13px/.test(c))bad.push('literal font-size:13px remains'); if(/font-size:\s*2\.25rem/.test(c))bad.push('literal font-size:2.25rem remains'); if(/padding:\s*0\.65rem 1rem/.test(c))bad.push('literal field input padding remains'); if(!/var\(--vms-space-/.test(c))bad.push('no space token references'); if(!/var\(--vms-text-(xs|sm|base|md|lg|xl|2xl)\)/.test(c))bad.push('no text token references'); if(/@media/.test(c))bad.push('@media introduced'); if(!/3px/.test(c))bad.push('progress 3px exception lost'); if(!/max-width:\s*520px/.test(c))bad.push('modal 520px exception lost'); if(bad.length){console.error(bad);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `default.css` no longer contains the literals `gap: 1.5rem`, `gap: 0.75rem`, `font-size: 13px`, `font-size: 2.25rem`, `padding: 0.65rem 1rem` (all replaced by `var()`)
    - `default.css` contains `var(--vms-space-*)` and `var(--vms-text-*)` references in the refactored rules
    - Exception literals preserved: `.vms-progress` `3px` + `99px`, `.vms-modal` `max-width: 520px`, checkbox `18px`, `--narrow` `400px`, `--wide` `800px`
    - Zero `@media` occurrences in the file
    - No selector, color, or non-spacing/non-font-size property changed
  </acceptance_criteria>
  <done>Every rhythm spacing + font-size literal references a scale token per the ledger; component-geometry exceptions stay literal; no media queries; selectors/colors unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add the centered max-width shell to .vms-page (THEME-01)</name>
  <files>viewmodel-shell/styles/default.css</files>
  <read_first>
    - viewmodel-shell/styles/default.css (the `.vms-page` rule ~line 74 as edited by Task 2; and the `.vms-page, .vms-page *` scoped box-sizing block ~lines 62-71 — DO NOT touch that block)
    - .planning/phases/03-default-design-system/03-UI-SPEC.md (§Page Shell table — exact shell property values + clamp expression)
    - .planning/phases/03-default-design-system/03-CONTEXT.md (D-11, D-12, D-13, D-14 — shell lives on .vms-page itself; .vms-page stays transparent; zero media queries; body keeps --vms-bg)
  </read_first>
  <action>
Modify ONLY the existing `.vms-page` flex rule in `viewmodel-shell/styles/default.css` (currently, after Task 2: `.vms-page { display: flex; flex-direction: column; gap: var(--vms-space-lg); color: var(--vms-text); font-family: var(--vms-font-body); }`).

Add these three shell declarations to that SAME rule (D-11 — the centered container is `.vms-page` itself; NO new DOM node, NO new emitted class, NO `browser.ts` change):
  `max-width: var(--vms-page-max);`
  `margin-inline: auto;`
  `padding-inline: clamp(1rem, 5vw, 2.25rem);`

Resulting rule (exact):
```css
.vms-page {
  display: flex;
  flex-direction: column;
  gap: var(--vms-space-lg);
  max-width: var(--vms-page-max);
  margin-inline: auto;
  padding-inline: clamp(1rem, 5vw, 2.25rem);
  color: var(--vms-text);
  font-family: var(--vms-font-body);
}
```

Do NOT add `background` to `.vms-page` — it stays transparent (D-14; `body` keeps carrying `--vms-bg`). Do NOT add `@media` (D-13 — `clamp()` is the responsive mechanism). Do NOT modify the `.vms-page, .vms-page *` scoped box-sizing block (UI-SPEC: unchanged). Do NOT modify `body` (D-14 — byte-identical).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('viewmodel-shell/styles/default.css','utf8'); const m=c.match(/\.vms-page\s*\{[^}]*\}/); if(!m){console.error('no .vms-page rule');process.exit(1)} const r=m[0]; const ok=/max-width:\s*var\(--vms-page-max\)/.test(r)&&/margin-inline:\s*auto/.test(r)&&/padding-inline:\s*clamp\(1rem,\s*5vw,\s*2\.25rem\)/.test(r); if(!ok){console.error('shell decls missing/incorrect in .vms-page:',r);process.exit(1)} if(/background/.test(r)){console.error('.vms-page must stay transparent (no background)');process.exit(1)} if(/@media/.test(c)){console.error('@media introduced');process.exit(1)} if(!/box-sizing:\s*border-box/.test(c)){console.error('scoped box-sizing block lost');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The `.vms-page` rule contains `max-width: var(--vms-page-max)`, `margin-inline: auto`, `padding-inline: clamp(1rem, 5vw, 2.25rem)`
    - The `.vms-page` rule contains NO `background` declaration (stays transparent, D-14)
    - The `.vms-page, .vms-page *` scoped box-sizing block is unchanged (still present, still `box-sizing: border-box`)
    - `body` rule is byte-identical to pre-change
    - Zero `@media` occurrences in the file
  </acceptance_criteria>
  <done>.vms-page is a centered 1080px-max, clamp-padded shell using only the scale/page-max tokens; transparent; no DOM/renderer change; zero media queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits one opt-in framework stylesheet. No request/response, auth, input parsing, network, or storage surface is touched. Token values are framework-authored compile-time constants, never user/wire input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `default.css` :root override seam (THEME-05) | mitigate | Acceptance criteria assert every pre-existing `:root` name/value is byte-identical except the one D-17 value, and that `--vms-text-muted` is `#9090a8`; the 11 theme files are not edited (verified in Plan 03). Regression-guarded by Task 1/3 grep assertions. |
| T-03-02 | Information Disclosure | WCAG AA contrast (D-17 accessibility floor) | mitigate | `--vms-text-muted` raised to `#9090a8` (UI-SPEC computed ≥5.08:1 on all 3 default surfaces) — removes the sub-AA legibility defect; asserted by Task 1. |
| T-03-03 | Denial of Service | CSS injection via tokens | accept | N/A — CSS custom-property values here are static framework constants, not user/wire-derived. No injection vector exists; PROJECT.md "Security: Dual-use N/A; standard safe-code practices". |

No high-severity threats. This is a CSS-only change to an opt-in stylesheet with no new trust boundary; the only security-relevant invariants are the THEME-05 override-seam regression guard (mitigated by assertions) and the AA contrast floor (fixed by D-17).
</threat_model>

<verification>
- `default.css` :root contains all 6 `--vms-space-*` + 7 `--vms-text-*` + `--vms-page-max` tokens (Task 1 automated check)
- `--vms-text-muted` is `#9090a8`; `#6b6b80` appears nowhere; all other pre-existing :root values byte-identical (Task 1)
- All rhythm spacing/font-size literals replaced by scale `var()`; component-geometry exceptions literal (Task 2)
- `.vms-page` is centered/max-width/clamp-padded, transparent, scoped box-sizing intact (Task 3)
- Zero `@media` occurrences anywhere in the file (all tasks)
- No theme file edited (this plan does not touch `styles/themes/*`)
</verification>

<success_criteria>
THEME-01: an app importing only `default.css` renders a centered 1080px-max page with `clamp()` horizontal padding, zero app CSS, zero media queries.
THEME-02: every spacing/font-size literal references one coherent named scale; type scale is all-rem.
THEME-05 (additive half): every pre-existing `:root` variable name+semantic byte-identical; new tokens purely additive; only `--vms-text-muted`'s value changes (D-17, not an override-seam behavior change).
</success_criteria>

<output>
After completion, create `.planning/phases/03-default-design-system/03-01-SUMMARY.md`
</output>
