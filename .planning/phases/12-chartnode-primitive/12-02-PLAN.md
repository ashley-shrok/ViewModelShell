---
phase: 12-chartnode-primitive
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - viewmodel-shell/src/tui.tsx
  - demo/FeatureProbe-bun/handler.ts
  - demo/FeatureProbe/AspNetCore/FeatureProbeController.cs
  - parity/fixtures/feature-probe.json
  - demo/Showcase/frontend/src/main.ts
  - CHANGELOG.md
autonomous: true
requirements: [CHART-01, CHART-05]

must_haves:
  truths:
    - "The TUI renderNode() renders a ChartNode as a legible text degradation (the title if present, then each point as `label  value` with a proportional ASCII bar scaled to the max value) — a terminal has no canvas, so the structured data is printed; the TUI build does not break"
    - "Both FeatureProbe backends render a static ChartNode (whole-number point values, an optional title, a tone) as view-shape, captured by the existing GET steps with no new action arm"
    - "The WIRE {type:\"chart\", kind?, points:[{label,value}], title?, tone?} is byte-identical across .NET / Bun / Node — bun run parity/run.ts is byte-identical green; whole-number values serialize identically (12 not 12.0); omitted kind is ABSENT on the wire"
    - "demo/Showcase renders a bar ChartNode (labelled categories × values + title + a tone) built only from ViewNodes, zero <style> — the live surface the Phase 13 operator review looks at"
    - "A `### ChartNode — Phase 12` subsection is added under the EXISTING `## Unreleased` CHANGELOG heading; NO package.json/.csproj version bump, NO publish, NO tag; agent-skill.md untouched (deferred to Phase 13 / CHART-06)"
  artifacts:
    - path: "viewmodel-shell/src/tui.tsx"
      provides: "a chart case in renderNode() printing title + per-point label/value/ASCII-bar; ChartNode is a leaf so no container-walk arms are needed"
      contains: "chart"
    - path: "demo/FeatureProbe-bun/handler.ts"
      provides: "a static chart section appended to the page children (whole-number points, title, tone)"
      contains: "chart"
    - path: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs"
      provides: "a byte-identical ChartNode section appended to pageChildren"
      contains: "ChartNode"
    - path: "parity/fixtures/feature-probe.json"
      provides: "$comment documenting the static chart coverage, captured by existing GETs; notes pixels are NOT parity-tested"
      contains: "chart"
    - path: "demo/Showcase/frontend/src/main.ts"
      provides: "a Chart demo section (bar, title, tone) built from ViewNodes only"
      contains: "chart"
    - path: "CHANGELOG.md"
      provides: "the ### ChartNode — Phase 12 Unreleased subsection"
      contains: "ChartNode — Phase 12"
  key_links:
    - from: "demo/FeatureProbe-bun/handler.ts buildVm"
      to: "demo/FeatureProbe/AspNetCore/FeatureProbeController.cs BuildVm"
      via: "byte-identical chart section (same points, title, tone, order)"
      pattern: "chart|ChartNode"
    - from: "viewmodel-shell/src/tui.tsx renderNode()"
      to: "TUI text degradation"
      via: "case \"chart\" prints title + points as label/value/ASCII-bar"
      pattern: "case \"chart\""
---

<objective>
Land the `ChartNode`'s TUI degradation (CHART-05 non-browser target), its cross-backend wire parity (CHART-05), a Showcase demo (the Phase 13 review surface), and the deferred-release CHANGELOG record. The TUI renders a `ChartNode` as a legible printed series with proportional ASCII bars (a terminal has no canvas — but the node is STRUCTURED data, so it prints). Both FeatureProbe backends render a static `ChartNode` so the byte-identical wire (`{type:"chart", kind?, points, title?, tone?}`) is parity-tested (data diffed, not pixels). The Showcase gains a bar-chart demo. The Phase-12 work is recorded under the existing `## Unreleased` CHANGELOG heading with NO version bump.

Purpose: Complete the TUI-degradation + wire-parity halves of CHART-05 (a sensible non-browser fallback so `ChartNode` doesn't break the TUI, and a byte-identical TS/.NET serialization proven by `parity/`), plus a visible Showcase demo for CHART-01. Client-side pixels are explicitly NOT parity-tested — parity proves only identical serialization.

Output: a `chart` case in the TUI `renderNode()` switch; a static `chart` section in both FeatureProbe backends; the widened `feature-probe.json` `$comment`; a Showcase chart demo; and the `### ChartNode — Phase 12` Unreleased CHANGELOG subsection. `bun run parity/run.ts` byte-identical green; the TUI build/tests pass. **NO version bump, NO publish, NO tag** (release deferred to Phase 13). **agent-skill.md / AgentSkill.md are NOT touched** (deferred to Phase 13 / CHART-06 so the parity skill gate stays green — both copies must change together).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-chartnode-primitive/12-01-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- DEPENDS ON 12-01: the ChartNode/ChartPoint TS type + .NET record + "chart"
     discriminator must already exist. This plan only CONSUMES the type in the
     TUI / demos / parity / CHANGELOG. Study .planning/phases/10-fits-node/
     10-02-PLAN.md — the fits TUI+parity+Showcase+CHANGELOG plan is the exact
     template. The KEY DIFFERENCE: fits is a container (needed 3 walk arms +
     last-child rendering); ChartNode is a LEAF (no children) — so it needs
     ONLY a renderNode() case, NO container-walk arms. -->

<!-- tui.tsx:
     - renderNode() dispatch switch is ~L947-975 (…case "divider" L964,
       case "fits" L967 returns last child, default → Unsupported). ADD a
       `case "chart":` that returns a text degradation. There is NO canvas in
       the terminal — print the structured data. Recommended shape (build with
       @opentui <text>/<box> primitives already used in this file): the title
       (if any) on its own line, then per point a line `<label padded>  <value>
       <bar>` where <bar> is a run of a block glyph (e.g. "█") scaled to
       Math.round(value / maxValue * WIDTH) with WIDTH ~20; guard maxValue<=0.
       Mirror how divider (L964) / statBar render text-only.
     - ChartNode is a LEAF (no children): the three container-aware walks
       (isPaneNode ~L542, countPanes/visit ~L573, focus-target walk ~L650)
       already ignore leaf nodes (StatBarNode/ProgressNode/etc. have no arm) —
       so NO walk arm is needed. Do NOT add one. Confirm the TUI conformance
       test tolerates the new leaf. -->

<!-- FeatureProbe precedent (Phase 9 switcher / Phase 10 fits, this repo): NEW
     node shapes render as STATIC view-shape in buildVm/BuildVm, captured by the
     existing GET steps — NO new action arm. Append a chart section to the page
     children list in the SAME position on both backends (e.g. after the fits
     sections, before buildTableSection / BuildTableSection). Use WHOLE-NUMBER
     values so double/number serialize byte-identically.
     - bun handler.ts buildVm: append a `{type:"section", heading:"chart (bar)",
       children:[ {type:"chart", points:[{label,value},…], title:"…",
       tone:"info"} ]}` in the page children array, matched position to .NET.
     - .NET BuildVm: `new SectionNode(Heading:"chart (bar)", Children: new
       ViewNode[]{ new ChartNode(Points: new[]{ new ChartPoint("…", 12), … },
       Title:"…", Tone:"info") })` — Points first (per the 12-01 record shape),
       Title/Tone named trailing optionals. Same headings, same labels, same
       whole-number values, same tone, same order as the bun side. Omit `kind`
       on BOTH so it is absent on the wire (proves omitted = absent). -->

<!-- parity/fixtures/feature-probe.json: the top-level $comment documents static
     coverage; static sections are captured by existing GET steps. EXTEND the
     $comment to note buildVm additionally renders a `chart` (bar) node — a
     ChartNode with whole-number points + title + tone, kind omitted (absent) —
     captured by the existing GET so the byte-identical diff covers the
     {type:"chart", points, title?, tone?} wire with no new step; note the
     CLIENT-SIDE pixels are NOT parity-tested. Do NOT add a new fixture FILE
     (parity/backends.json needs no edit). -->

<!-- Showcase: demo/Showcase/frontend/src/main.ts componentsView() is a flat list
     of {type:"section", heading, children:[…]} blocks (zero <style>). The Fits
     demo block is the neighbor idiom. ADD a "Chart (bar)" section after it: a
     muted TextNode explainer + a {type:"chart", points:[…], title:"…",
     tone:"success"} node. Keep it minimal — the comprehensive review is Phase 13. -->

<!-- CHANGELOG.md: the `## Unreleased` heading + the Phase-10 `### Fits node —
     Phase 10` subsection are the structure template (lead paragraph + Added /
     Not changed / Demo + tests / Migration). ADD a `### ChartNode — Phase 12`
     subsection AFTER the most recent Unreleased subsection. Do NOT bump any
     version line in package.json / .csproj. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TUI ChartNode degradation (printed series + ASCII bars)</name>
  <files>viewmodel-shell/src/tui.tsx</files>
  <read_first>
    .planning/phases/10-fits-node/10-02-PLAN.md (Task 1 — the TUI degradation precedent, incl. the leaf-vs-container walk distinction);
    viewmodel-shell/src/tui.tsx (renderNode() switch ~L947-975; the divider case L964 + statBar rendering for the text-only leaf idiom; the @opentui <text>/<box> primitives used elsewhere in the file);
    viewmodel-shell/test/conformance.tui.test.ts and viewmodel-shell/test/tui-lifecycle.test.ts (confirm the chart case does not break the TUI)
  </read_first>
  <action>
    In `viewmodel-shell/src/tui.tsx`:
    1. Add a `chart` case to the `renderNode()` dispatch switch (~L947-975), adjacent to the other leaf cases. It renders the ChartNode's STRUCTURED data as text (no canvas in a terminal):
       - If `node.title` is set, render it as a heading/emphasized `<text>` line.
       - Compute `const maxValue = Math.max(0, ...node.points.map(p => p.value));`.
       - For each point render a line: the label (optionally padded to a common width), the numeric value, and a proportional bar = a run of a block glyph (`"█"`) of length `maxValue > 0 ? Math.round(p.value / maxValue * 20) : 0` (WIDTH=20). Guard `maxValue <= 0` (all-zero series → no bars, still print labels+values). Guard an empty `points` array (render just the title or nothing) so it never throws.
       Use the existing @opentui `<text>`/`<box>` primitives + `key` prop idiom already in this file (mirror the divider/statBar text rendering). Add a comment: DELIBERATE degradation — a terminal has no canvas, so the structured ChartNode prints as a legible label/value/ASCII-bar series (CHART-05); the TUI is `@experimental`, the requirement is only that ChartNode does not break it and degrades legibly.
    2. Do NOT add any container-walk arm — ChartNode is a LEAF (no children), so isPaneNode/countPanes/the focus walk correctly ignore it (same as StatBarNode/ProgressNode). Confirm no walk needs a chart arm.
    Do NOT change any non-chart behavior. The chart case must compile against the `ChartNode` type shipped in 12-01 (`node.points` is `ChartPoint[]`).
  </action>
  <verify>
    <automated>cd viewmodel-shell && npx tsc --noEmit && npx vitest run test/conformance.tui.test.ts test/tui-lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'case "chart"' viewmodel-shell/src/tui.tsx`; the TUI `renderNode()` renders the title (if present) + one `label / value / ASCII-bar` line per point, scaled to the max value, with empty/all-zero guards.
    - NO container-walk arm was added for chart (it is a leaf).
    - `cd viewmodel-shell && npx tsc --noEmit` passes and the TUI conformance + lifecycle tests are green (the chart degradation does not break the TUI build/render).
  </acceptance_criteria>
  <done>
    The TUI `renderNode()` has a `chart` case printing a legible title + per-point label/value/ASCII-bar series; `tsc --noEmit` passes; the TUI conformance + lifecycle suites stay green.
  </done>
</task>

<task type="auto">
  <name>Task 2: FeatureProbe (both backends) + parity fixture for the chart wire</name>
  <files>demo/FeatureProbe-bun/handler.ts, demo/FeatureProbe/AspNetCore/FeatureProbeController.cs, parity/fixtures/feature-probe.json</files>
  <read_first>
    .planning/phases/10-fits-node/10-02-PLAN.md (Task 2 — the static-view-shape FeatureProbe + parity precedent to replicate exactly);
    demo/FeatureProbe-bun/handler.ts (the fits/switcher static sections + the page children list — where and how a new static section is appended);
    demo/FeatureProbe/AspNetCore/FeatureProbeController.cs (the fits/switcher sections + the pageChildren list + the SectionNode/named-arg constructor style; confirm the 12-01 ChartNode/ChartPoint record ctor order: Points first, Title/Tone named trailing);
    parity/fixtures/feature-probe.json (the top-level $comment + GET steps);
    parity/run.ts and parity/normalize.ts (how GET responses are diffed — presence + values must match; numbers pass through normalize unchanged, so 12 vs 12.0 WOULD mismatch → use whole numbers)
  </read_first>
  <action>
    Mirror the fits/switcher static-view-shape precedent EXACTLY: render a `ChartNode` as a static section in both `buildVm`/`BuildVm`, captured by the existing GET steps (NO new action arm).

    In `demo/FeatureProbe-bun/handler.ts` `buildVm`, append a labeled section to the page `children` (matched position to the .NET side — e.g. after the fits sections, before `buildTableSection(state)`):
    `{ type:"section", heading:"chart (bar)", children:[ { type:"chart", points:[ {label:"Mon",value:12}, {label:"Tue",value:19}, {label:"Wed",value:7} ], title:"Weekly visits", tone:"info" } ] }`
    Use WHOLE-NUMBER values. OMIT `kind` (proves omitted = absent on the wire).

    In `demo/FeatureProbe/AspNetCore/FeatureProbeController.cs` `BuildVm`, append the BYTE-IDENTICAL equivalent to `pageChildren` in the SAME position:
    `new SectionNode(Heading:"chart (bar)", Children: new ViewNode[]{ new ChartNode(Points: new ChartPoint[]{ new("Mon",12), new("Tue",19), new("Wed",7) }, Title:"Weekly visits", Tone:"info") })`
    (Points first per the 12-01 record shape; Title/Tone named trailing; Kind omitted so it is absent. Same heading, same labels, same whole-number values, same tone, same order as the bun side.) Match the exact SectionNode named-arg style already used in this file.

    In `parity/fixtures/feature-probe.json`: extend the top-level `$comment` (mirroring the existing fits/switcher sentences) to note that `buildVm` additionally renders a `chart` (bar) `ChartNode` — whole-number points + `title` + `tone`, `kind` omitted (absent) — captured by the existing GET steps so the byte-identical diff covers the `{type:"chart", points, title?, tone?}` wire with no new step. State explicitly that the CLIENT-SIDE Chart.js pixels are NOT part of parity (browser-only); parity proves only identical serialization. Do NOT add a new fixture FILE (`parity/backends.json` needs no edit).
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell && PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both FeatureProbe backends render a `chart (bar)` section with a `ChartNode` (whole-number points `Mon/Tue/Wed` = 12/19/7, `title:"Weekly visits"`, `tone:"info"`, `kind` omitted), byte-identically.
    - `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` is byte-identical green across dotnet-probe / bun-probe / node-probe (other backends unaffected).
    - The fixture `$comment` documents the static chart coverage and that pixels are client-only / not parity-tested; no new fixture file was added.
  </acceptance_criteria>
  <done>
    Both FeatureProbe backends emit a byte-identical static `ChartNode`; `bun run parity/run.ts` is byte-identical green; the fixture `$comment` records the coverage and the pixels-not-parity note.
  </done>
</task>

<task type="auto">
  <name>Task 3: Showcase chart demo + CHANGELOG Unreleased subsection</name>
  <files>demo/Showcase/frontend/src/main.ts, CHANGELOG.md</files>
  <read_first>
    .planning/phases/10-fits-node/10-02-PLAN.md (Task 3 — the Showcase-demo + Unreleased-CHANGELOG-subsection precedent);
    demo/Showcase/frontend/src/main.ts (componentsView() + the Fits demo block as the section-block idiom — zero <style>, ViewNodes only);
    CHANGELOG.md (the `## Unreleased` heading + the `### Fits node — Phase 10` subsection structure: lead paragraph + Added / Not changed / Demo + tests / Migration)
  </read_first>
  <action>
    In `demo/Showcase/frontend/src/main.ts` `componentsView()`, add a new section block (after the Fits demo block, same `{type:"section", heading, children:[…]}` idiom, zero `<style>`):
    - heading: "Chart (bar)".
    - a muted `TextNode` explaining: a `ChartNode` is structured data (labelled categories × numeric values) drawn as a single-series bar chart by Chart.js behind the adapter — the app ships only the data (`title` + a `tone` from the tone axis), no chart library and no CSS.
    - a `{ type:"chart", points:[ {label:"Jan",value:30}, {label:"Feb",value:45}, {label:"Mar",value:28}, {label:"Apr",value:52} ], title:"Signups", tone:"success" }` node.
    Add a leading comment in the Showcase idiom: VMS's first data-viz primitive; Chart.js is a private lazy/optional adapter dep (apps never touch it); appearance is `title` + `tone` only.

    In `CHANGELOG.md`, add a `### ChartNode — Phase 12 (on `main`, unpublished)` subsection AFTER the most recent `## Unreleased` subsection (mirror the `### Fits node — Phase 10` structure). Include:
    - Lead paragraph: the `ChartNode` (`type:"chart"`) — VMS's first data-visualization primitive — carries bounded declared data (`points: {label, value}[]` + optional `title` + a `tone` from the existing tone axis) an agent reads directly; it renders a single-series BAR chart drawn by **Chart.js as a PRIVATE, lazy, optional dependency of the browser adapter** (apps never touch Chart.js), redrawing in place via Chart.js `.update()` on new server data. Closes the design of GitHub issue #6. Additive — the protocol token stays `viewmodel-shell/1.0`.
    - `### Added`: `ChartNode { type:"chart"; kind?:"bar" [omitted=bar]; points:{label,value}[]; title?; tone?:"danger"|"warning"|"success"|"info" }` + the `ChartPoint` sub-record in the TS `ViewNode` union, mirrored as a .NET `ChartNode`/`ChartPoint` record + `[JsonDerivedType(typeof(ChartNode),"chart")]` (`Points` required; `Kind`/`Title`/`Tone` free-form `string?` with `[JsonIgnore(WhenWritingNull)]`; `Value` is `double` mirroring TS `number`); the `browser.ts` renderer drawing a single-series bar chart via a lazy tree-shaken `import("chart.js")` (bar controller/elements/scales only), tone→theme-token color (`danger→--vms-error`, etc.), redraw-in-place via `.update()` with a per-render mark-swept instance registry (removed charts `destroy()`'d), and a fail-loud error when the optional `chart.js` peer dep is absent; the `.vms-chart` framework CSS; the TUI degradation (printed series + ASCII bars — terminals have no canvas, CHART-05); `chart.js` declared as a devDependency + OPTIONAL peerDependency (apps that render no chart load zero chart.js bytes).
    - `### Not changed`: no wire-shape change (protocol token stays `viewmodel-shell/1.0` — a new optional-field-bearing leaf ViewNode is additive); the core `index.ts` gains the TYPE only (all rendering/measurement in `browser.ts`, `check:core-globals` green); the .NET/bun backends gain NO chart.js dependency (they only EMIT data). `agent-skill.md`/`AgentSkill.md` untouched here (documented in Phase 13).
    - `### Demo + tests`: FeatureProbe twins render a static `chart` (bar) node, parity byte-identical (data diffed, not pixels); `test/chart.test.ts` covers the bar-config integration (mocked chart.js), redraw-in-place via `.update()`, removal `.destroy()`, fail-loud on missing dep, and the validator no-blind-spot, documenting that real pixels are jsdom-untestable (verified by the Phase 13 operator browser review); the Showcase gains a bar-chart demo.
    - `### Migration`: none — purely additive new leaf node; existing trees/callers/agents unaffected. Consumers who render a `ChartNode` must install the optional `chart.js` peer dependency (apps that don't chart need nothing).
    Do NOT bump any version in `package.json`/`.csproj`; do NOT publish or tag. The release is the consolidated Phase 13 aligned `4.1.0` bump.
  </action>
  <verify>
    <automated>cd /home/thenasty/ViewModelShell/viewmodel-shell && npx tsc --noEmit && grep -q "ChartNode — Phase 12" /home/thenasty/ViewModelShell/CHANGELOG.md && grep -q '"chart"' /home/thenasty/ViewModelShell/demo/Showcase/frontend/src/main.ts && git -C /home/thenasty/ViewModelShell diff --quiet -- viewmodel-shell/package.json viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `demo/Showcase/frontend/src/main.ts` `componentsView()` has a "Chart (bar)" section with a `{type:"chart", points:[…], title:"Signups", tone:"success"}` node, zero `<style>`; the Showcase frontend typechecks.
    - `CHANGELOG.md` has a `### ChartNode — Phase 12` subsection under the existing `## Unreleased` heading (lead paragraph + Added / Not changed / Demo + tests / Migration).
    - NO version line changed in `viewmodel-shell/package.json` or `viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` (the `git diff --quiet` gate passes); `agent-skill.md` / `AgentSkill.md` were NOT edited.
    - The verify command prints `OK`.
  </acceptance_criteria>
  <done>
    The Showcase has a minimal bar-chart demo (zero `<style>`); `CHANGELOG.md` records the `### ChartNode — Phase 12` Unreleased subsection; no version bump / publish / tag occurred; agent-skill docs untouched (deferred to Phase 13).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client wire | The `ChartNode` (`type:"chart"`, points, title, tone) crosses the wire from both backends; parity proves identical serialization. The TUI and Showcase consume the node client-side. No `tone` value is interpolated into a class name (the `.vms-chart` class is static; tone maps through a closed token map in browser.ts), so there is no class-injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | FeatureProbe `chart` static view-shape across backends | accept | The chart section is static, server-authored view-shape (no user input); parity verifies the two backends emit byte-identical wire. A hostile server already controls the view tree; rendering a `chart` adds no new capability. `tone` is a closed union; whole-number values keep serialization byte-identical. Consistent with the fits/switcher static-view-shape precedent. |
| T-12-05 | Denial of Service | TUI `chart` text rendering | accept | The TUI chart case iterates `points` once to print label/value/bar lines — no fan-out beyond the (server-authored) point count, and empty/all-zero series are guarded. No recursion (leaf node). |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure source edits to existing files; the chart.js dependency is added in 12-01, not here. |
</threat_model>

<verification>
- `cd viewmodel-shell && npx tsc --noEmit` — TS (incl. tui.tsx chart case + the Showcase demo) compiles against the 12-01 `ChartNode` type.
- `cd viewmodel-shell && npx vitest run` — full vitest suite green (TUI conformance + lifecycle unaffected by the chart degradation; no regressions).
- `PATH="$HOME/.dotnet:$PATH" bun run parity/run.ts` — byte-identical green across .NET / Bun / Node, including the new static `chart` section; the parity skill gate stays green (agent-skill.md / AgentSkill.md untouched, still byte-identical to each other).
- `grep -q "ChartNode — Phase 12" CHANGELOG.md` — the Unreleased subsection exists; `git diff --quiet -- viewmodel-shell/package.json viewmodel-shell-dotnet/AshleyShrok.ViewModelShell.csproj` confirms NO version line changed.
</verification>

<success_criteria>
- The TUI `renderNode()` has a `chart` case rendering a legible printed series + ASCII bars (CHART-05 degradation); no container-walk arm (leaf); the TUI build + conformance/lifecycle tests pass.
- Both FeatureProbe backends render a byte-identical static `ChartNode` (whole-number points + title + tone, kind omitted); `bun run parity/run.ts` is byte-identical green; the fixture `$comment` documents the coverage and that pixels are client-only / not parity-tested (CHART-05 wire half).
- The Showcase has a minimal bar-chart demo (the Phase 13 review surface), zero `<style>`.
- `CHANGELOG.md` has the `### ChartNode — Phase 12` subsection under the existing `## Unreleased` heading; NO version bump, NO publish, NO tag; `agent-skill.md` untouched.
</success_criteria>

<output>
Create `.planning/phases/12-chartnode-primitive/12-02-SUMMARY.md` when done.
</output>
